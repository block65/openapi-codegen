export type MyDate = `${number}-${number}-${number}`;
export type MyTime = `${number}:${number}:${number}${string}`;
export type MyDateTime = `${number}-${number}-${number}T${number}:${number}:${number}${string}`;
export type MyDuration = `P${string}`;
