/**
 * This file was auto generated by @block65/openapi-codegen
 *
 * WARN: Do not edit directly.
 *
 * Generated on 2026-05-09T08:13:59.952Z
 *
 */
import type { Jsonifiable } from "type-fest";

/** An open port on a container */
export type Port = {
	IP?: string;
	PrivatePort: number;
	PublicPort?: number;
	Type: "tcp" | "udp" | "sctp";
};
/**
 * MountPoint represents a mount point configuration inside the container.
 * This is used for reporting the mountpoints in use by a container.
 */
export type MountPoint = {
	/**
	 * The mount type:
	 *
	 * - `bind` a mount of a file or directory from the host into the container.
	 * - `volume` a docker volume with the given `Name`.
	 * - `tmpfs` a `tmpfs`.
	 * - `npipe` a named pipe from the host into the container.
	 * - `cluster` a Swarm cluster volume
	 * @enum bind,volume,tmpfs,npipe,cluster
	 * @example volume
	 */
	Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
	/**
	 * Name is the name reference to the underlying data defined by `Source`
	 * e.g., the volume name.
	 * @example myvolume
	 */
	Name?: string;
	/**
	 * Source location of the mount.
	 *
	 * For volumes, this contains the storage location of the volume (within
	 * `/var/lib/docker/volumes/`). For bind-mounts, and `npipe`, this contains
	 * the source (host) part of the bind-mount. For `tmpfs` mount points, this
	 * field is empty.
	 * @example /var/lib/docker/volumes/myvolume/_data
	 */
	Source?: string;
	/**
	 * Destination is the path relative to the container root (`/`) where
	 * the `Source` is mounted inside the container.
	 * @example /usr/share/nginx/html/
	 */
	Destination?: string;
	/**
	 * Driver is the volume driver used to create the volume (if it is a volume).
	 * @example local
	 */
	Driver?: string;
	/**
	 * Mode is a comma separated list of options supplied by the user when
	 * creating the bind/volume mount.
	 *
	 * The default is platform-specific (`"z"` on Linux, empty on Windows).
	 * @example z
	 */
	Mode?: string;
	/**
	 * Whether the mount is mounted writable (read-write).
	 * @example true
	 */
	RW?: boolean;
	/**
	 * Propagation describes how mounts are propagated from the host into the
	 * mount point, and vice-versa. Refer to the [Linux kernel
	 * documentation](https://www.kernel.org/doc/Documentation/filesystems/sharedsubtree.txt)
	 * for details. This field is not used on Windows.
	 * @example
	 */
	Propagation?: string;
};
/** A device mapping between the host and container */
export type DeviceMapping = {
	PathOnHost?: string;
	PathInContainer?: string;
	CgroupPermissions?: string;
};
/** A request for devices to be sent to device drivers */
export type DeviceRequest = {
	Driver?: string;
	Count?: number;
	DeviceIDs?: readonly string[];
	/**
	 * A list of capabilities; an OR list of AND lists of capabilities.
	 * @example gpu,nvidia,compute
	 */
	Capabilities?: readonly (readonly string[])[];
	Options?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
};
export type ThrottleDevice = {
	Path?: string;
	Rate?: number;
};
export type Mount = {
	Target?: string;
	Source?: string;
	/**
	 * The mount type. Available types:
	 *
	 * - `bind` Mounts a file or directory from the host into the container. Must
	 * exist prior to creating the container.
	 * - `volume` Creates a volume with the given name and options (or uses a
	 * pre-existing volume with the same name and options). These are **not**
	 * removed when the container is removed.
	 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
	 * specified for tmpfs.
	 * - `npipe` Mounts a named pipe from the host into the container. Must exist
	 * prior to creating the container.
	 * - `cluster` a Swarm cluster volume
	 * @enum bind,volume,tmpfs,npipe,cluster
	 */
	Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
	ReadOnly?: boolean;
	Consistency?: string;
	BindOptions?: {
		/**
		 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
		 * @enum private,rprivate,shared,rshared,slave,rslave
		 */
		Propagation?:
			| "private"
			| "rprivate"
			| "shared"
			| "rshared"
			| "slave"
			| "rslave";
		NonRecursive?: boolean;
		CreateMountpoint?: boolean;
	};
	VolumeOptions?: {
		NoCopy?: boolean;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		DriverConfig?: {
			Name?: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
	TmpfsOptions?: {
		SizeBytes?: number;
		Mode?: number;
	};
};
/**
 * The behavior to apply when the container exits. The default is not to
 * restart.
 *
 * An ever increasing delay (double the previous delay, starting at 100ms) is
 * added before each restart to prevent flooding the server.
 */
export type RestartPolicy = {
	/**
	 * - Empty string means not to restart
	 * - `no` Do not automatically restart
	 * - `always` Always restart
	 * - `unless-stopped` Restart always except when the user has manually stopped
	 * the container
	 * - `on-failure` Restart only when the container exit code is non-zero
	 * @enum ,no,always,unless-stopped,on-failure
	 */
	Name?: "" | "no" | "always" | "unless-stopped" | "on-failure";
	MaximumRetryCount?: number;
};
/** A container's resources (cgroups config, ulimits, etc) */
export type Resources = {
	CpuShares?: number;
	Memory?: number;
	CgroupParent?: string;
	BlkioWeight?: number;
	BlkioWeightDevice?: readonly {
		Path?: string;
		Weight?: number;
	}[];
	BlkioDeviceReadBps?: readonly {
		Path?: string;
		Rate?: number;
	}[];
	BlkioDeviceWriteBps?: readonly {
		Path?: string;
		Rate?: number;
	}[];
	BlkioDeviceReadIOps?: readonly {
		Path?: string;
		Rate?: number;
	}[];
	BlkioDeviceWriteIOps?: readonly {
		Path?: string;
		Rate?: number;
	}[];
	CpuPeriod?: number;
	CpuQuota?: number;
	CpuRealtimePeriod?: number;
	CpuRealtimeRuntime?: number;
	/**
	 * CPUs in which to allow execution (e.g., `0-3`, `0,1`).
	 * @example 0-3
	 */
	CpusetCpus?: string;
	CpusetMems?: string;
	Devices?: readonly {
		PathOnHost?: string;
		PathInContainer?: string;
		CgroupPermissions?: string;
	}[];
	DeviceCgroupRules?: readonly string[];
	DeviceRequests?: readonly {
		Driver?: string;
		Count?: number;
		DeviceIDs?: readonly string[];
		/**
		 * A list of capabilities; an OR list of AND lists of capabilities.
		 * @example gpu,nvidia,compute
		 */
		Capabilities?: readonly (readonly string[])[];
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	}[];
	KernelMemoryTCP?: number;
	MemoryReservation?: number;
	MemorySwap?: number;
	MemorySwappiness?: number;
	NanoCpus?: number;
	OomKillDisable?: boolean;
	Init?: boolean | null;
	PidsLimit?: number | null;
	Ulimits?: readonly {
		Name?: string;
		Soft?: number;
		Hard?: number;
	}[];
	CpuCount?: number;
	CpuPercent?: number;
	IOMaximumIOps?: number;
	IOMaximumBandwidth?: number;
};
/** An object describing a limit on resources which can be requested by a task. */
export type Limit = {
	NanoCPUs?: number;
	MemoryBytes?: number;
	/**
	 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
	 * @example 100
	 */
	Pids?: number;
};
/**
 * An object describing the resources which can be advertised by a node and
 * requested by a task.
 */
export type ResourceObject = {
	NanoCPUs?: number;
	MemoryBytes?: number;
	/**
	 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
	 * String resources (e.g, `GPU=UUID1`).
	 * @example [object Object],[object Object],[object Object]
	 */
	GenericResources?: readonly {
		NamedResourceSpec?: {
			Kind?: string;
			Value?: string;
		};
		DiscreteResourceSpec?: {
			Kind?: string;
			Value?: number;
		};
	}[];
};
/** A test to perform to check that the container is healthy. */
export type HealthConfig = {
	Test?: readonly string[];
	Interval?: number;
	Timeout?: number;
	Retries?: number;
	StartPeriod?: number;
};
/** Health stores information about the container's healthcheck results. */
export type Health = {
	/**
	 * Status is one of `none`, `starting`, `healthy` or `unhealthy`
	 *
	 * - "none"      Indicates there is no healthcheck
	 * - "starting"  Starting indicates that the container is not yet ready
	 * - "healthy"   Healthy indicates that the container is running correctly
	 * - "unhealthy" Unhealthy indicates that the container has a problem
	 * @enum none,starting,healthy,unhealthy
	 * @example healthy
	 */
	Status?: "none" | "starting" | "healthy" | "unhealthy";
	/**
	 * FailingStreak is the number of consecutive failures
	 * @example 0
	 */
	FailingStreak?: number;
	Log?: readonly {
		/**
		 * Date and time at which this check started in
		 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
		 * @example 2020-01-04T10:44:24.496525531Z
		 */
		Start?: string;
		/**
		 * Date and time at which this check ended in
		 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
		 * @example 2020-01-04T10:45:21.364524523Z
		 */
		End?: string;
		/**
		 * ExitCode meanings:
		 *
		 * - `0` healthy
		 * - `1` unhealthy
		 * - `2` reserved (considered unhealthy)
		 * - other values: error running probe
		 * @example 0
		 */
		ExitCode?: number;
		Output?: string;
	}[];
};
/**
 * HealthcheckResult stores information about a single run of a healthcheck
 * probe
 */
export type HealthcheckResult = {
	/**
	 * Date and time at which this check started in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2020-01-04T10:44:24.496525531Z
	 */
	Start?: string;
	/**
	 * Date and time at which this check ended in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2020-01-04T10:45:21.364524523Z
	 */
	End?: string;
	/**
	 * ExitCode meanings:
	 *
	 * - `0` healthy
	 * - `1` unhealthy
	 * - `2` reserved (considered unhealthy)
	 * - other values: error running probe
	 * @example 0
	 */
	ExitCode?: number;
	Output?: string;
};
/** Container configuration that depends on the host we are running on */
export type HostConfig = {
	CpuShares?: number;
	Memory?: number;
	CgroupParent?: string;
	BlkioWeight?: number;
	BlkioWeightDevice?: readonly {
		Path?: string;
		Weight?: number;
	}[];
	BlkioDeviceReadBps?: readonly {
		Path?: string;
		Rate?: number;
	}[];
	BlkioDeviceWriteBps?: readonly {
		Path?: string;
		Rate?: number;
	}[];
	BlkioDeviceReadIOps?: readonly {
		Path?: string;
		Rate?: number;
	}[];
	BlkioDeviceWriteIOps?: readonly {
		Path?: string;
		Rate?: number;
	}[];
	CpuPeriod?: number;
	CpuQuota?: number;
	CpuRealtimePeriod?: number;
	CpuRealtimeRuntime?: number;
	/**
	 * CPUs in which to allow execution (e.g., `0-3`, `0,1`).
	 * @example 0-3
	 */
	CpusetCpus?: string;
	CpusetMems?: string;
	Devices?: readonly {
		PathOnHost?: string;
		PathInContainer?: string;
		CgroupPermissions?: string;
	}[];
	DeviceCgroupRules?: readonly string[];
	DeviceRequests?: readonly {
		Driver?: string;
		Count?: number;
		DeviceIDs?: readonly string[];
		/**
		 * A list of capabilities; an OR list of AND lists of capabilities.
		 * @example gpu,nvidia,compute
		 */
		Capabilities?: readonly (readonly string[])[];
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	}[];
	KernelMemoryTCP?: number;
	MemoryReservation?: number;
	MemorySwap?: number;
	MemorySwappiness?: number;
	NanoCpus?: number;
	OomKillDisable?: boolean;
	Init?: boolean | null;
	PidsLimit?: number | null;
	Ulimits?: readonly {
		Name?: string;
		Soft?: number;
		Hard?: number;
	}[];
	CpuCount?: number;
	CpuPercent?: number;
	IOMaximumIOps?: number;
	IOMaximumBandwidth?: number;
} & {
	Binds?: readonly string[];
	ContainerIDFile?: string;
	LogConfig?: {
		Type?:
			| "json-file"
			| "syslog"
			| "journald"
			| "gelf"
			| "fluentd"
			| "awslogs"
			| "splunk"
			| "etwlogs"
			| "none";
		Config?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	NetworkMode?: string;
	PortBindings?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	RestartPolicy?: {
		/**
		 * - Empty string means not to restart
		 * - `no` Do not automatically restart
		 * - `always` Always restart
		 * - `unless-stopped` Restart always except when the user has manually stopped
		 * the container
		 * - `on-failure` Restart only when the container exit code is non-zero
		 * @enum ,no,always,unless-stopped,on-failure
		 */
		Name?: "" | "no" | "always" | "unless-stopped" | "on-failure";
		MaximumRetryCount?: number;
	};
	AutoRemove?: boolean;
	VolumeDriver?: string;
	VolumesFrom?: readonly string[];
	Mounts?: readonly {
		Target?: string;
		Source?: string;
		/**
		 * The mount type. Available types:
		 *
		 * - `bind` Mounts a file or directory from the host into the container. Must
		 * exist prior to creating the container.
		 * - `volume` Creates a volume with the given name and options (or uses a
		 * pre-existing volume with the same name and options). These are **not**
		 * removed when the container is removed.
		 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
		 * specified for tmpfs.
		 * - `npipe` Mounts a named pipe from the host into the container. Must exist
		 * prior to creating the container.
		 * - `cluster` a Swarm cluster volume
		 * @enum bind,volume,tmpfs,npipe,cluster
		 */
		Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
		ReadOnly?: boolean;
		Consistency?: string;
		BindOptions?: {
			/**
			 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
			 * @enum private,rprivate,shared,rshared,slave,rslave
			 */
			Propagation?:
				| "private"
				| "rprivate"
				| "shared"
				| "rshared"
				| "slave"
				| "rslave";
			NonRecursive?: boolean;
			CreateMountpoint?: boolean;
		};
		VolumeOptions?: {
			NoCopy?: boolean;
			Labels?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			DriverConfig?: {
				Name?: string;
				Options?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			};
		};
		TmpfsOptions?: {
			SizeBytes?: number;
			Mode?: number;
		};
	}[];
	ConsoleSize?: readonly number[];
	Annotations?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	CapAdd?: readonly string[];
	CapDrop?: readonly string[];
	/**
	 * cgroup namespace mode for the container. Possible values are:
	 *
	 * - `"private"`: the container runs in its own private cgroup namespace
	 * - `"host"`: use the host system's cgroup namespace
	 *
	 * If not specified, the daemon default is used, which can either be
	 * `"private"`
	 * or `"host"`, depending on daemon version, kernel support and configuration.
	 * @enum private,host
	 */
	CgroupnsMode?: "private" | "host";
	Dns?: readonly string[];
	DnsOptions?: readonly string[];
	DnsSearch?: readonly string[];
	ExtraHosts?: readonly string[];
	GroupAdd?: readonly string[];
	IpcMode?: string;
	Cgroup?: string;
	Links?: readonly string[];
	/**
	 * An integer value containing the score given to the container in
	 * order to tune OOM killer preferences.
	 * @example 500
	 */
	OomScoreAdj?: number;
	PidMode?: string;
	Privileged?: boolean;
	PublishAllPorts?: boolean;
	ReadonlyRootfs?: boolean;
	SecurityOpt?: readonly string[];
	StorageOpt?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Tmpfs?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	UTSMode?: string;
	UsernsMode?: string;
	ShmSize?: number;
	Sysctls?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Runtime?: string;
	/**
	 * Isolation technology of the container. (Windows only)
	 * @enum default,process,hyperv,
	 */
	Isolation?: "default" | "process" | "hyperv" | "";
	MaskedPaths?: readonly string[];
	ReadonlyPaths?: readonly string[];
};
/**
 * Configuration for a container that is portable between hosts.
 *
 * When used as `ContainerConfig` field in an image, `ContainerConfig` is an
 * optional field containing the configuration of the container that was last
 * committed when creating the image.
 *
 * Previous versions of Docker builder used this field to store build cache,
 * and it is not in active use anymore.
 */
export type ContainerConfig = {
	/**
	 * The hostname to use for the container, as a valid RFC 1123 hostname.
	 * @example 439f4e91bd1d
	 */
	Hostname?: string;
	Domainname?: string;
	User?: string;
	AttachStdin?: boolean;
	/**
	 * Whether to attach to `stdout`.
	 * @default true
	 */
	AttachStdout?: boolean;
	/**
	 * Whether to attach to `stderr`.
	 * @default true
	 */
	AttachStderr?: boolean;
	ExposedPorts?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Tty?: boolean;
	OpenStdin?: boolean;
	StdinOnce?: boolean;
	/**
	 * A list of environment variables to set inside the container in the
	 * form `["VAR=value", ...]`. A variable without `=` is removed from the
	 * environment, rather than to have an empty value.
	 * @example PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
	 */
	Env?: readonly string[];
	/**
	 * Command to run specified as a string or an array of strings.
	 * @example /bin/sh
	 */
	Cmd?: readonly string[];
	Healthcheck?: {
		Test?: readonly string[];
		Interval?: number;
		Timeout?: number;
		Retries?: number;
		StartPeriod?: number;
	};
	/**
	 * Command is already escaped (Windows only)
	 * @example false
	 */
	ArgsEscaped?: boolean | null;
	/**
	 * The name (or reference) of the image to use when creating the container,
	 * or which was used when the container was created.
	 * @example example-image:1.0
	 */
	Image?: string;
	Volumes?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * The working directory for commands to run in.
	 * @example /public/
	 */
	WorkingDir?: string;
	/**
	 * The entry point for the container as a string or an array of strings.
	 *
	 * If the array consists of exactly one empty string (`[""]`) then the
	 * entry point is reset to system default (i.e., the entry point used by
	 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
	 * @example
	 */
	Entrypoint?: readonly string[];
	NetworkDisabled?: boolean | null;
	MacAddress?: string;
	/**
	 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
	 * @example
	 */
	OnBuild?: readonly string[];
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Signal to stop a container as a string or unsigned integer.
	 * @example SIGTERM
	 */
	StopSignal?: string;
	StopTimeout?: number | null;
	/**
	 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
	 * @example /bin/sh,-c
	 */
	Shell?: readonly string[];
};
/**
 * Configuration of the image. These fields are used as defaults
 * when starting a container from the image.
 */
export type ImageConfig = {
	/**
	 * The hostname to use for the container, as a valid RFC 1123 hostname.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always empty and must not be used.
	 * @example
	 */
	Hostname?: string;
	/**
	 * The domain name to use for the container.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always empty and must not be used.
	 * @example
	 */
	Domainname?: string;
	/**
	 * The user that commands are run as inside the container.
	 * @example web:web
	 */
	User?: string;
	/**
	 * Whether to attach to `stdin`.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always false and must not be used.
	 * @example false
	 */
	AttachStdin?: boolean;
	/**
	 * Whether to attach to `stdout`.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always false and must not be used.
	 * @example false
	 */
	AttachStdout?: boolean;
	/**
	 * Whether to attach to `stderr`.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always false and must not be used.
	 * @example false
	 */
	AttachStderr?: boolean;
	ExposedPorts?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Attach standard streams to a TTY, including `stdin` if it is not closed.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always false and must not be used.
	 * @example false
	 */
	Tty?: boolean;
	/**
	 * Open `stdin`
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always false and must not be used.
	 * @example false
	 */
	OpenStdin?: boolean;
	/**
	 * Close `stdin` after one attached client disconnects.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always false and must not be used.
	 * @example false
	 */
	StdinOnce?: boolean;
	/**
	 * A list of environment variables to set inside the container in the
	 * form `["VAR=value", ...]`. A variable without `=` is removed from the
	 * environment, rather than to have an empty value.
	 * @example PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
	 */
	Env?: readonly string[];
	/**
	 * Command to run specified as a string or an array of strings.
	 * @example /bin/sh
	 */
	Cmd?: readonly string[];
	Healthcheck?: {
		Test?: readonly string[];
		Interval?: number;
		Timeout?: number;
		Retries?: number;
		StartPeriod?: number;
	};
	/**
	 * Command is already escaped (Windows only)
	 * @example false
	 */
	ArgsEscaped?: boolean | null;
	/**
	 * The name (or reference) of the image to use when creating the container,
	 * or which was used when the container was created.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always empty and must not be used.
	 * @example
	 */
	Image?: string;
	Volumes?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * The working directory for commands to run in.
	 * @example /public/
	 */
	WorkingDir?: string;
	/**
	 * The entry point for the container as a string or an array of strings.
	 *
	 * If the array consists of exactly one empty string (`[""]`) then the
	 * entry point is reset to system default (i.e., the entry point used by
	 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
	 * @example
	 */
	Entrypoint?: readonly string[];
	/**
	 * Disable networking for the container.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always omitted and must not be used.
	 * @example false
	 */
	NetworkDisabled?: boolean | null;
	/**
	 * MAC address of the container.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always omitted and must not be used.
	 * @example
	 */
	MacAddress?: string;
	/**
	 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
	 * @example
	 */
	OnBuild?: readonly string[];
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Signal to stop a container as a string or unsigned integer.
	 * @example SIGTERM
	 */
	StopSignal?: string;
	StopTimeout?: number | null;
	/**
	 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
	 * @example /bin/sh,-c
	 */
	Shell?: readonly string[];
};
/**
 * NetworkingConfig represents the container's networking configuration for
 * each of its interfaces.
 * It is used for the networking configs specified in the `docker create`
 * and `docker network connect` commands.
 */
export type NetworkingConfig = {
	EndpointsConfig?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
};
/** NetworkSettings exposes the network settings in the API */
export type NetworkSettings = {
	/**
	 * Name of the network's bridge (for example, `docker0`).
	 * @example docker0
	 */
	Bridge?: string;
	/**
	 * SandboxID uniquely represents a container's network stack.
	 * @example 9d12daf2c33f5959c8bf90aa513e4f65b561738661003029ec84830cd503a0c3
	 */
	SandboxID?: string;
	/**
	 * Indicates if hairpin NAT should be enabled on the virtual interface.
	 * @example false
	 */
	HairpinMode?: boolean;
	/**
	 * IPv6 unicast address using the link-local prefix.
	 * @example fe80::42:acff:fe11:1
	 */
	LinkLocalIPv6Address?: string;
	/**
	 * Prefix length of the IPv6 unicast address.
	 * @example 64
	 */
	LinkLocalIPv6PrefixLen?: number;
	Ports?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * SandboxKey identifies the sandbox
	 * @example /var/run/docker/netns/8ab54b426c38
	 */
	SandboxKey?: string;
	SecondaryIPAddresses?: readonly {
		Addr?: string;
		PrefixLen?: number;
	}[];
	SecondaryIPv6Addresses?: readonly {
		Addr?: string;
		PrefixLen?: number;
	}[];
	/**
	 * EndpointID uniquely represents a service endpoint in a Sandbox.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 * @example b88f5b905aabf2893f3cbc4ee42d1ea7980bbc0a92e2c8922b1e1795298afb0b
	 */
	EndpointID?: string;
	/**
	 * Gateway address for the default "bridge" network.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 * @example 172.17.0.1
	 */
	Gateway?: string;
	/**
	 * Global IPv6 address for the default "bridge" network.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 * @example 2001:db8::5689
	 */
	GlobalIPv6Address?: string;
	/**
	 * Mask length of the global IPv6 address.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 * @example 64
	 */
	GlobalIPv6PrefixLen?: number;
	/**
	 * IPv4 address for the default "bridge" network.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 * @example 172.17.0.4
	 */
	IPAddress?: string;
	/**
	 * Mask length of the IPv4 address.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 * @example 16
	 */
	IPPrefixLen?: number;
	/**
	 * IPv6 gateway address for this network.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 * @example 2001:db8:2::100
	 */
	IPv6Gateway?: string;
	/**
	 * MAC address for the container on the default "bridge" network.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 * @example 02:42:ac:11:00:04
	 */
	MacAddress?: string;
	Networks?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
};
/** Address represents an IPv4 or IPv6 IP address. */
export type Address = {
	Addr?: string;
	PrefixLen?: number;
};
/**
 * PortMap describes the mapping of container ports to host ports, using the
 * container's port-number and protocol as key in the format
 * `<port>/<protocol>`,
 * for example, `80/udp`.
 *
 * If a container's port is mapped for multiple protocols, separate entries
 * are added to the mapping table.
 */
export type PortMap = Record<string, unknown[]>;
/**
 * PortBinding represents a binding between a host IP address and a host
 * port.
 */
export type PortBinding = {
	/**
	 * Host IP address that the container's port is mapped to.
	 * @example 127.0.0.1
	 */
	HostIp?: string;
	/**
	 * Host port number that the container's port is mapped to.
	 * @example 4443
	 */
	HostPort?: string;
};
/**
 * Information about the storage driver used to store the container's and
 * image's filesystem.
 */
export type GraphDriverData = {
	/**
	 * Name of the storage driver.
	 * @example overlay2
	 */
	Name: string;
	Data: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
};
/** Change in the container's filesystem. */
export type FilesystemChange = {
	Path: string;
	/**
	 * Kind of change
	 *
	 * Can be one of:
	 *
	 * - `0`: Modified ("C")
	 * - `1`: Added ("A")
	 * - `2`: Deleted ("D")
	 * @enum 0,1,2
	 */
	Kind: number;
};
/**
 * Kind of change
 *
 * Can be one of:
 *
 * - `0`: Modified ("C")
 * - `1`: Added ("A")
 * - `2`: Deleted ("D")
 */
export type ChangeType = number;
/** Information about an image in the local image cache. */
export type ImageInspect = {
	/**
	 * ID is the content-addressable ID of an image.
	 *
	 * This identifier is a content-addressable digest calculated from the
	 * image's configuration (which includes the digests of layers used by
	 * the image).
	 *
	 * Note that this digest differs from the `RepoDigests` below, which
	 * holds digests of image manifests that reference the image.
	 * @example sha256:ec3f0931a6e6b6855d76b2d7b0be30e81860baccd891b2e243280bf1cd8ad710
	 */
	Id?: string;
	/**
	 * List of image names/tags in the local image cache that reference this
	 * image.
	 *
	 * Multiple image tags can refer to the same image, and this list may be
	 * empty if no tags reference the image, in which case the image is
	 * "untagged", in which case it can still be referenced by its ID.
	 * @example example:1.0,example:latest,example:stable,internal.registry.example.com:5000/example:1.0
	 */
	RepoTags?: readonly string[];
	/**
	 * List of content-addressable digests of locally available image manifests
	 * that the image is referenced from. Multiple manifests can refer to the
	 * same image.
	 *
	 * These digests are usually only available if the image was either pulled
	 * from a registry, or if the image was pushed to a registry, which is when
	 * the manifest is generated and its digest calculated.
	 * @example example@sha256:afcc7f1ac1b49db317a7196c902e61c6c3c4607d63599ee1a82d702d249a0ccb,internal.registry.example.com:5000/example@sha256:b69959407d21e8a062e0416bf13405bb2b71ed7a84dde4158ebafacfa06f5578
	 */
	RepoDigests?: readonly string[];
	/**
	 * ID of the parent image.
	 *
	 * Depending on how the image was created, this field may be empty and
	 * is only set for images that were built/created locally. This field
	 * is empty if the image was pulled from an image registry.
	 * @example
	 */
	Parent?: string;
	/**
	 * Optional message that was set when committing or importing the image.
	 * @example
	 */
	Comment?: string;
	/**
	 * Date and time at which the image was created, formatted in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2022-02-04T21:20:12.497794809Z
	 */
	Created?: string;
	/**
	 * The ID of the container that was used to create the image.
	 *
	 * Depending on how the image was created, this field may be empty.
	 * @example 65974bc86f1770ae4bff79f651ebdbce166ae9aada632ee3fa9af3a264911735
	 */
	Container?: string;
	ContainerConfig?: {
		/**
		 * The hostname to use for the container, as a valid RFC 1123 hostname.
		 * @example 439f4e91bd1d
		 */
		Hostname?: string;
		Domainname?: string;
		User?: string;
		AttachStdin?: boolean;
		/**
		 * Whether to attach to `stdout`.
		 * @default true
		 */
		AttachStdout?: boolean;
		/**
		 * Whether to attach to `stderr`.
		 * @default true
		 */
		AttachStderr?: boolean;
		ExposedPorts?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Tty?: boolean;
		OpenStdin?: boolean;
		StdinOnce?: boolean;
		/**
		 * A list of environment variables to set inside the container in the
		 * form `["VAR=value", ...]`. A variable without `=` is removed from the
		 * environment, rather than to have an empty value.
		 * @example PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
		 */
		Env?: readonly string[];
		/**
		 * Command to run specified as a string or an array of strings.
		 * @example /bin/sh
		 */
		Cmd?: readonly string[];
		Healthcheck?: {
			Test?: readonly string[];
			Interval?: number;
			Timeout?: number;
			Retries?: number;
			StartPeriod?: number;
		};
		/**
		 * Command is already escaped (Windows only)
		 * @example false
		 */
		ArgsEscaped?: boolean | null;
		/**
		 * The name (or reference) of the image to use when creating the container,
		 * or which was used when the container was created.
		 * @example example-image:1.0
		 */
		Image?: string;
		Volumes?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * The working directory for commands to run in.
		 * @example /public/
		 */
		WorkingDir?: string;
		/**
		 * The entry point for the container as a string or an array of strings.
		 *
		 * If the array consists of exactly one empty string (`[""]`) then the
		 * entry point is reset to system default (i.e., the entry point used by
		 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
		 * @example
		 */
		Entrypoint?: readonly string[];
		NetworkDisabled?: boolean | null;
		MacAddress?: string;
		/**
		 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
		 * @example
		 */
		OnBuild?: readonly string[];
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Signal to stop a container as a string or unsigned integer.
		 * @example SIGTERM
		 */
		StopSignal?: string;
		StopTimeout?: number | null;
		/**
		 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
		 * @example /bin/sh,-c
		 */
		Shell?: readonly string[];
	};
	/**
	 * The version of Docker that was used to build the image.
	 *
	 * Depending on how the image was created, this field may be empty.
	 * @example 20.10.7
	 */
	DockerVersion?: string;
	/**
	 * Name of the author that was specified when committing the image, or as
	 * specified through MAINTAINER (deprecated) in the Dockerfile.
	 * @example
	 */
	Author?: string;
	/**
	 * Configuration of the image. These fields are used as defaults
	 * when starting a container from the image.
	 * @example [object Object]
	 */
	Config?: {
		/**
		 * The hostname to use for the container, as a valid RFC 1123 hostname.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always empty and must not be used.
		 * @example
		 */
		Hostname?: string;
		/**
		 * The domain name to use for the container.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always empty and must not be used.
		 * @example
		 */
		Domainname?: string;
		/**
		 * The user that commands are run as inside the container.
		 * @example web:web
		 */
		User?: string;
		/**
		 * Whether to attach to `stdin`.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always false and must not be used.
		 * @example false
		 */
		AttachStdin?: boolean;
		/**
		 * Whether to attach to `stdout`.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always false and must not be used.
		 * @example false
		 */
		AttachStdout?: boolean;
		/**
		 * Whether to attach to `stderr`.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always false and must not be used.
		 * @example false
		 */
		AttachStderr?: boolean;
		ExposedPorts?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Attach standard streams to a TTY, including `stdin` if it is not closed.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always false and must not be used.
		 * @example false
		 */
		Tty?: boolean;
		/**
		 * Open `stdin`
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always false and must not be used.
		 * @example false
		 */
		OpenStdin?: boolean;
		/**
		 * Close `stdin` after one attached client disconnects.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always false and must not be used.
		 * @example false
		 */
		StdinOnce?: boolean;
		/**
		 * A list of environment variables to set inside the container in the
		 * form `["VAR=value", ...]`. A variable without `=` is removed from the
		 * environment, rather than to have an empty value.
		 * @example PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
		 */
		Env?: readonly string[];
		/**
		 * Command to run specified as a string or an array of strings.
		 * @example /bin/sh
		 */
		Cmd?: readonly string[];
		Healthcheck?: {
			Test?: readonly string[];
			Interval?: number;
			Timeout?: number;
			Retries?: number;
			StartPeriod?: number;
		};
		/**
		 * Command is already escaped (Windows only)
		 * @example false
		 */
		ArgsEscaped?: boolean | null;
		/**
		 * The name (or reference) of the image to use when creating the container,
		 * or which was used when the container was created.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always empty and must not be used.
		 * @example
		 */
		Image?: string;
		Volumes?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * The working directory for commands to run in.
		 * @example /public/
		 */
		WorkingDir?: string;
		/**
		 * The entry point for the container as a string or an array of strings.
		 *
		 * If the array consists of exactly one empty string (`[""]`) then the
		 * entry point is reset to system default (i.e., the entry point used by
		 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
		 * @example
		 */
		Entrypoint?: readonly string[];
		/**
		 * Disable networking for the container.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always omitted and must not be used.
		 * @example false
		 */
		NetworkDisabled?: boolean | null;
		/**
		 * MAC address of the container.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always omitted and must not be used.
		 * @example
		 */
		MacAddress?: string;
		/**
		 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
		 * @example
		 */
		OnBuild?: readonly string[];
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Signal to stop a container as a string or unsigned integer.
		 * @example SIGTERM
		 */
		StopSignal?: string;
		StopTimeout?: number | null;
		/**
		 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
		 * @example /bin/sh,-c
		 */
		Shell?: readonly string[];
	};
	/**
	 * Hardware CPU architecture that the image runs on.
	 * @example arm
	 */
	Architecture?: string;
	/**
	 * CPU architecture variant (presently ARM-only).
	 * @example v7
	 */
	Variant?: string;
	/**
	 * Operating System the image is built to run on.
	 * @example linux
	 */
	Os?: string;
	/**
	 * Operating System version the image is built to run on (especially
	 * for Windows).
	 * @example
	 */
	OsVersion?: string;
	/**
	 * Total size of the image including all layers it is composed of.
	 * @example 1239828
	 */
	Size?: number;
	/**
	 * Total size of the image including all layers it is composed of.
	 *
	 * In versions of Docker before v1.10, this field was calculated from
	 * the image itself and all of its parent images. Images are now stored
	 * self-contained, and no longer use a parent-chain, making this field
	 * an equivalent of the Size field.
	 *
	 * > **Deprecated**: this field is kept for backward compatibility, but
	 * > will be removed in API v1.44.
	 * @example 1239828
	 */
	VirtualSize?: number;
	GraphDriver?: {
		/**
		 * Name of the storage driver.
		 * @example overlay2
		 */
		Name: string;
		Data: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	RootFS?: {
		Type: string;
		Layers?: readonly string[];
	};
	Metadata?: {
		/**
		 * Date and time at which the image was last tagged in
		 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
		 *
		 * This information is only available if the image was tagged locally,
		 * and omitted otherwise.
		 * @example 2022-02-28T14:40:02.623929178Z
		 */
		LastTagTime?: string;
	};
};
export type ImageSummary = {
	/**
	 * ID is the content-addressable ID of an image.
	 *
	 * This identifier is a content-addressable digest calculated from the
	 * image's configuration (which includes the digests of layers used by
	 * the image).
	 *
	 * Note that this digest differs from the `RepoDigests` below, which
	 * holds digests of image manifests that reference the image.
	 * @example sha256:ec3f0931a6e6b6855d76b2d7b0be30e81860baccd891b2e243280bf1cd8ad710
	 */
	Id: string;
	/**
	 * ID of the parent image.
	 *
	 * Depending on how the image was created, this field may be empty and
	 * is only set for images that were built/created locally. This field
	 * is empty if the image was pulled from an image registry.
	 * @example
	 */
	ParentId: string;
	/**
	 * List of image names/tags in the local image cache that reference this
	 * image.
	 *
	 * Multiple image tags can refer to the same image, and this list may be
	 * empty if no tags reference the image, in which case the image is
	 * "untagged", in which case it can still be referenced by its ID.
	 * @example example:1.0,example:latest,example:stable,internal.registry.example.com:5000/example:1.0
	 */
	RepoTags: readonly string[];
	/**
	 * List of content-addressable digests of locally available image manifests
	 * that the image is referenced from. Multiple manifests can refer to the
	 * same image.
	 *
	 * These digests are usually only available if the image was either pulled
	 * from a registry, or if the image was pushed to a registry, which is when
	 * the manifest is generated and its digest calculated.
	 * @example example@sha256:afcc7f1ac1b49db317a7196c902e61c6c3c4607d63599ee1a82d702d249a0ccb,internal.registry.example.com:5000/example@sha256:b69959407d21e8a062e0416bf13405bb2b71ed7a84dde4158ebafacfa06f5578
	 */
	RepoDigests: readonly string[];
	/**
	 * Date and time at which the image was created as a Unix timestamp
	 * (number of seconds since EPOCH).
	 * @example 1644009612
	 */
	Created: number;
	/**
	 * Total size of the image including all layers it is composed of.
	 * @example 172064416
	 */
	Size: number;
	/**
	 * Total size of image layers that are shared between this image and other
	 * images.
	 *
	 * This size is not calculated by default. `-1` indicates that the value
	 * has not been set / calculated.
	 * @example 1239828
	 */
	SharedSize: number;
	/**
	 * Total size of the image including all layers it is composed of.
	 *
	 * In versions of Docker before v1.10, this field was calculated from
	 * the image itself and all of its parent images. Images are now stored
	 * self-contained, and no longer use a parent-chain, making this field
	 * an equivalent of the Size field.
	 *
	 * Deprecated: this field is kept for backward compatibility, and will be
	 * removed in API v1.44.
	 * @example 172064416
	 */
	VirtualSize?: number;
	Labels: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Number of containers using this image. Includes both stopped and running
	 * containers.
	 *
	 * This size is not calculated by default, and depends on which API endpoint
	 * is used. `-1` indicates that the value has not been set / calculated.
	 * @example 2
	 */
	Containers: number;
};
export type AuthConfig = {
	username?: string;
	password?: string;
	email?: string;
	serveraddress?: string;
};
export type ProcessConfig = {
	privileged?: boolean;
	user?: string;
	tty?: boolean;
	entrypoint?: string;
	arguments?: readonly string[];
};
export type Volume = {
	/**
	 * Name of the volume.
	 * @example tardis
	 */
	Name: string;
	/**
	 * Name of the volume driver used by the volume.
	 * @example custom
	 */
	Driver: string;
	/**
	 * Mount path of the volume on the host.
	 * @example /var/lib/docker/volumes/tardis
	 */
	Mountpoint: string;
	/**
	 * Date/Time the volume was created.
	 * @example 2016-06-07T20:31:11.853781916Z
	 */
	CreatedAt?: string;
	Status?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Labels: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * The level at which the volume exists. Either `global` for cluster-wide,
	 * or `local` for machine level.
	 * @default local
	 * @enum local,global
	 * @example local
	 */
	Scope: "local" | "global";
	ClusterVolume?: {
		ID?: string;
		Version?: {
			Index?: number;
		};
		CreatedAt?: string;
		UpdatedAt?: string;
		Spec?: {
			Group?: string;
			AccessMode?: {
				/**
				 * The set of nodes this volume can be used on at one time.
				 * - `single` The volume may only be scheduled to one node at a time.
				 * - `multi` the volume may be scheduled to any supported number of nodes at a
				 * time.
				 * @default single
				 * @enum single,multi
				 */
				Scope?: "single" | "multi";
				/**
				 * The number and way that different tasks can use this volume
				 * at one time.
				 * - `none` The volume may only be used by one task at a time.
				 * - `readonly` The volume may be used by any number of tasks, but they all
				 * must mount the volume as readonly
				 * - `onewriter` The volume may be used by any number of tasks, but only one
				 * may mount it as read/write.
				 * - `all` The volume may have any number of readers and writers.
				 * @default none
				 * @enum none,readonly,onewriter,all
				 */
				Sharing?: "none" | "readonly" | "onewriter" | "all";
				MountVolume?: {};
				Secrets?: readonly {
					Key?: string;
					Secret?: string;
				}[];
				AccessibilityRequirements?: {
					Requisite?: readonly Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>[];
					Preferred?: readonly Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>[];
				};
				CapacityRange?: {
					RequiredBytes?: number;
					LimitBytes?: number;
				};
				/**
				 * The availability of the volume for use in tasks.
				 * - `active` The volume is fully available for scheduling on the cluster
				 * - `pause` No new workloads should use the volume, but existing workloads
				 * are not stopped.
				 * - `drain` All workloads using this volume should be stopped and
				 * rescheduled, and no new ones should be started.
				 * @default active
				 * @enum active,pause,drain
				 */
				Availability?: "active" | "pause" | "drain";
			};
		};
		Info?: {
			CapacityBytes?: number;
			VolumeContext?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			VolumeID?: string;
			AccessibleTopology?: readonly Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>[];
		};
		PublishStatus?: readonly {
			NodeID?: string;
			/**
			 * The published state of the volume.
			 * * `pending-publish` The volume should be published to this node, but the
			 * call to the controller plugin to do so has not yet been successfully
			 * completed.
			 * * `published` The volume is published successfully to the node.
			 * * `pending-node-unpublish` The volume should be unpublished from the node,
			 * and the manager is awaiting confirmation from the worker that it has done
			 * so.
			 * * `pending-controller-unpublish` The volume is successfully unpublished
			 * from the node, but has not yet been successfully unpublished on the
			 * controller.
			 * @enum pending-publish,published,pending-node-unpublish,pending-controller-unpublish
			 */
			State?:
				| "pending-publish"
				| "published"
				| "pending-node-unpublish"
				| "pending-controller-unpublish";
			PublishContext?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
	};
	Options: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	UsageData?: {
		/**
		 * Amount of disk space used by the volume (in bytes). This information
		 * is only available for volumes created with the `"local"` volume
		 * driver. For volumes created with other volume drivers, this field
		 * is set to `-1` ("not available")
		 * @default -1
		 */
		Size: number;
		/**
		 * The number of containers referencing this volume. This field
		 * is set to `-1` if the reference-count is not available.
		 * @default -1
		 */
		RefCount: number;
	};
};
/** Volume configuration */
export type VolumeCreateOptions = {
	/**
	 * The new volume's name. If not specified, Docker generates a name.
	 * @example tardis
	 */
	Name?: string;
	/**
	 * Name of the volume driver to use.
	 * @default local
	 * @example custom
	 */
	Driver?: string;
	DriverOpts?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	ClusterVolumeSpec?: {
		Group?: string;
		AccessMode?: {
			/**
			 * The set of nodes this volume can be used on at one time.
			 * - `single` The volume may only be scheduled to one node at a time.
			 * - `multi` the volume may be scheduled to any supported number of nodes at a
			 * time.
			 * @default single
			 * @enum single,multi
			 */
			Scope?: "single" | "multi";
			/**
			 * The number and way that different tasks can use this volume
			 * at one time.
			 * - `none` The volume may only be used by one task at a time.
			 * - `readonly` The volume may be used by any number of tasks, but they all
			 * must mount the volume as readonly
			 * - `onewriter` The volume may be used by any number of tasks, but only one
			 * may mount it as read/write.
			 * - `all` The volume may have any number of readers and writers.
			 * @default none
			 * @enum none,readonly,onewriter,all
			 */
			Sharing?: "none" | "readonly" | "onewriter" | "all";
			MountVolume?: {};
			Secrets?: readonly {
				Key?: string;
				Secret?: string;
			}[];
			AccessibilityRequirements?: {
				Requisite?: readonly Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>[];
				Preferred?: readonly Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>[];
			};
			CapacityRange?: {
				RequiredBytes?: number;
				LimitBytes?: number;
			};
			/**
			 * The availability of the volume for use in tasks.
			 * - `active` The volume is fully available for scheduling on the cluster
			 * - `pause` No new workloads should use the volume, but existing workloads
			 * are not stopped.
			 * - `drain` All workloads using this volume should be stopped and
			 * rescheduled, and no new ones should be started.
			 * @default active
			 * @enum active,pause,drain
			 */
			Availability?: "active" | "pause" | "drain";
		};
	};
};
/** Volume list response */
export type VolumeListResponse = {
	Volumes?: readonly {
		/**
		 * Name of the volume.
		 * @example tardis
		 */
		Name: string;
		/**
		 * Name of the volume driver used by the volume.
		 * @example custom
		 */
		Driver: string;
		/**
		 * Mount path of the volume on the host.
		 * @example /var/lib/docker/volumes/tardis
		 */
		Mountpoint: string;
		/**
		 * Date/Time the volume was created.
		 * @example 2016-06-07T20:31:11.853781916Z
		 */
		CreatedAt?: string;
		Status?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Labels: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * The level at which the volume exists. Either `global` for cluster-wide,
		 * or `local` for machine level.
		 * @default local
		 * @enum local,global
		 * @example local
		 */
		Scope: "local" | "global";
		ClusterVolume?: {
			ID?: string;
			Version?: {
				Index?: number;
			};
			CreatedAt?: string;
			UpdatedAt?: string;
			Spec?: {
				Group?: string;
				AccessMode?: {
					/**
					 * The set of nodes this volume can be used on at one time.
					 * - `single` The volume may only be scheduled to one node at a time.
					 * - `multi` the volume may be scheduled to any supported number of nodes at a
					 * time.
					 * @default single
					 * @enum single,multi
					 */
					Scope?: "single" | "multi";
					/**
					 * The number and way that different tasks can use this volume
					 * at one time.
					 * - `none` The volume may only be used by one task at a time.
					 * - `readonly` The volume may be used by any number of tasks, but they all
					 * must mount the volume as readonly
					 * - `onewriter` The volume may be used by any number of tasks, but only one
					 * may mount it as read/write.
					 * - `all` The volume may have any number of readers and writers.
					 * @default none
					 * @enum none,readonly,onewriter,all
					 */
					Sharing?: "none" | "readonly" | "onewriter" | "all";
					MountVolume?: {};
					Secrets?: readonly {
						Key?: string;
						Secret?: string;
					}[];
					AccessibilityRequirements?: {
						Requisite?: readonly Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>[];
						Preferred?: readonly Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>[];
					};
					CapacityRange?: {
						RequiredBytes?: number;
						LimitBytes?: number;
					};
					/**
					 * The availability of the volume for use in tasks.
					 * - `active` The volume is fully available for scheduling on the cluster
					 * - `pause` No new workloads should use the volume, but existing workloads
					 * are not stopped.
					 * - `drain` All workloads using this volume should be stopped and
					 * rescheduled, and no new ones should be started.
					 * @default active
					 * @enum active,pause,drain
					 */
					Availability?: "active" | "pause" | "drain";
				};
			};
			Info?: {
				CapacityBytes?: number;
				VolumeContext?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				VolumeID?: string;
				AccessibleTopology?: readonly Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>[];
			};
			PublishStatus?: readonly {
				NodeID?: string;
				/**
				 * The published state of the volume.
				 * * `pending-publish` The volume should be published to this node, but the
				 * call to the controller plugin to do so has not yet been successfully
				 * completed.
				 * * `published` The volume is published successfully to the node.
				 * * `pending-node-unpublish` The volume should be unpublished from the node,
				 * and the manager is awaiting confirmation from the worker that it has done
				 * so.
				 * * `pending-controller-unpublish` The volume is successfully unpublished
				 * from the node, but has not yet been successfully unpublished on the
				 * controller.
				 * @enum pending-publish,published,pending-node-unpublish,pending-controller-unpublish
				 */
				State?:
					| "pending-publish"
					| "published"
					| "pending-node-unpublish"
					| "pending-controller-unpublish";
				PublishContext?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			}[];
		};
		Options: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		UsageData?: {
			/**
			 * Amount of disk space used by the volume (in bytes). This information
			 * is only available for volumes created with the `"local"` volume
			 * driver. For volumes created with other volume drivers, this field
			 * is set to `-1` ("not available")
			 * @default -1
			 */
			Size: number;
			/**
			 * The number of containers referencing this volume. This field
			 * is set to `-1` if the reference-count is not available.
			 * @default -1
			 */
			RefCount: number;
		};
	}[];
	/**
	 * Warnings that occurred when fetching the list of volumes.
	 * @example
	 */
	Warnings?: readonly string[];
};
export type Network = {
	/**
	 * Name of the network.
	 * @example my_network
	 */
	Name?: string;
	/**
	 * ID that uniquely identifies a network on a single machine.
	 * @example 7d86d31b1478e7cca9ebed7e73aa0fdeec46c5ca29497431d3007d2d9e15ed99
	 */
	Id?: string;
	/**
	 * Date and time at which the network was created in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2016-10-19T04:33:30.360899459Z
	 */
	Created?: string;
	/**
	 * The level at which the network exists (e.g. `swarm` for cluster-wide
	 * or `local` for machine level)
	 * @example local
	 */
	Scope?: string;
	/**
	 * The name of the driver used to create the network (e.g. `bridge`,
	 * `overlay`).
	 * @example overlay
	 */
	Driver?: string;
	/**
	 * Whether the network was created with IPv6 enabled.
	 * @example false
	 */
	EnableIPv6?: boolean;
	IPAM?: {
		/**
		 * Name of the IPAM driver to use.
		 * @default default
		 * @example default
		 */
		Driver?: string;
		Config?: readonly {
			Subnet?: string;
			IPRange?: string;
			Gateway?: string;
			AuxiliaryAddresses?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	/**
	 * Whether the network is created to only allow internal networking
	 * connectivity.
	 * @example false
	 */
	Internal?: boolean;
	/**
	 * Whether a global / swarm scope network is manually attachable by regular
	 * containers from workers in swarm mode.
	 * @example false
	 */
	Attachable?: boolean;
	/**
	 * Whether the network is providing the routing-mesh for the swarm cluster.
	 * @example false
	 */
	Ingress?: boolean;
	ConfigFrom?: {
		/**
		 * The name of the config-only network that provides the network's
		 * configuration. The specified network must be an existing config-only
		 * network. Only network names are allowed, not network IDs.
		 * @example config_only_network_01
		 */
		Network?: string;
	};
	ConfigOnly?: boolean;
	Containers?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Options?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Peers?: readonly {
		/**
		 * ID of the peer-node in the Swarm cluster.
		 * @example 6869d7c1732b
		 */
		Name?: string;
		/**
		 * IP-address of the peer-node in the Swarm cluster.
		 * @example 10.133.77.91
		 */
		IP?: string;
	}[];
};
/**
 * The config-only network source to provide the configuration for
 * this network.
 */
export type ConfigReference = {
	/**
	 * The name of the config-only network that provides the network's
	 * configuration. The specified network must be an existing config-only
	 * network. Only network names are allowed, not network IDs.
	 * @example config_only_network_01
	 */
	Network?: string;
};
export type Ipam = {
	/**
	 * Name of the IPAM driver to use.
	 * @default default
	 * @example default
	 */
	Driver?: string;
	Config?: readonly {
		Subnet?: string;
		IPRange?: string;
		Gateway?: string;
		AuxiliaryAddresses?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	}[];
	Options?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
};
export type IpamConfig = {
	Subnet?: string;
	IPRange?: string;
	Gateway?: string;
	AuxiliaryAddresses?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
};
export type NetworkContainer = {
	Name?: string;
	EndpointID?: string;
	MacAddress?: string;
	IPv4Address?: string;
	IPv6Address?: string;
};
/** PeerInfo represents one peer of an overlay network. */
export type PeerInfo = {
	/**
	 * ID of the peer-node in the Swarm cluster.
	 * @example 6869d7c1732b
	 */
	Name?: string;
	/**
	 * IP-address of the peer-node in the Swarm cluster.
	 * @example 10.133.77.91
	 */
	IP?: string;
};
export type BuildInfo = {
	id?: string;
	stream?: string;
	error?: string;
	errorDetail?: {
		code?: number;
		message?: string;
	};
	status?: string;
	progress?: string;
	progressDetail?: {
		current?: number;
		total?: number;
	};
	/**
	 * Image ID or Digest
	 * @example [object Object]
	 */
	aux?: {
		ID?: string;
	};
};
/** BuildCache contains information about a build cache record. */
export type BuildCache = {
	/**
	 * Unique ID of the build cache record.
	 * @example ndlpt0hhvkqcdfkputsk4cq9c
	 */
	ID?: string;
	/**
	 * ID of the parent build cache record.
	 *
	 * > **Deprecated**: This field is deprecated, and omitted if empty.
	 * @example
	 */
	Parent?: string;
	/**
	 * List of parent build cache record IDs.
	 * @example hw53o5aio51xtltp5xjp8v7fx
	 */
	Parents?: readonly string[];
	/**
	 * Cache record type.
	 * @enum internal,frontend,source.local,source.git.checkout,exec.cachemount,regular
	 * @example regular
	 */
	Type?:
		| "internal"
		| "frontend"
		| "source.local"
		| "source.git.checkout"
		| "exec.cachemount"
		| "regular";
	/**
	 * Description of the build-step that produced the build cache.
	 * @example mount / from exec /bin/sh -c echo 'Binary::apt::APT::Keep-Downloaded-Packages "true";' > /etc/apt/apt.conf.d/keep-cache
	 */
	Description?: string;
	/**
	 * Indicates if the build cache is in use.
	 * @example false
	 */
	InUse?: boolean;
	/**
	 * Indicates if the build cache is shared.
	 * @example true
	 */
	Shared?: boolean;
	/**
	 * Amount of disk space used by the build cache (in bytes).
	 * @example 51
	 */
	Size?: number;
	/**
	 * Date and time at which the build cache was created in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2016-08-18T10:44:24.496525531Z
	 */
	CreatedAt?: string;
	/**
	 * Date and time at which the build cache was last used in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2017-08-09T07:09:37.632105588Z
	 */
	LastUsedAt?: string;
	UsageCount?: number;
};
/** Image ID or Digest */
export type ImageId = {
	ID?: string;
};
export type CreateImageInfo = {
	id?: string;
	error?: string;
	errorDetail?: {
		code?: number;
		message?: string;
	};
	status?: string;
	progress?: string;
	progressDetail?: {
		current?: number;
		total?: number;
	};
};
export type PushImageInfo = {
	error?: string;
	status?: string;
	progress?: string;
	progressDetail?: {
		current?: number;
		total?: number;
	};
};
export type ErrorDetail = {
	code?: number;
	message?: string;
};
export type ProgressDetail = {
	current?: number;
	total?: number;
};
/** Represents an error. */
export type ErrorResponse = {
	message: string;
};
/** Response to an API call that returns just an Id */
export type IdResponse = {
	Id: string;
};
/** Configuration for a network endpoint. */
export type EndpointSettings = {
	IPAMConfig?: {
		IPv4Address?: string;
		IPv6Address?: string;
		LinkLocalIPs?: readonly string[];
	};
	Links?: readonly string[];
	Aliases?: readonly string[];
	/**
	 * Unique ID of the network.
	 * @example 08754567f1f40222263eab4102e1c733ae697e8e354aa9cd6e18d7402835292a
	 */
	NetworkID?: string;
	/**
	 * Unique ID for the service endpoint in a Sandbox.
	 * @example b88f5b905aabf2893f3cbc4ee42d1ea7980bbc0a92e2c8922b1e1795298afb0b
	 */
	EndpointID?: string;
	/**
	 * Gateway address for this network.
	 * @example 172.17.0.1
	 */
	Gateway?: string;
	/**
	 * IPv4 address.
	 * @example 172.17.0.4
	 */
	IPAddress?: string;
	/**
	 * Mask length of the IPv4 address.
	 * @example 16
	 */
	IPPrefixLen?: number;
	/**
	 * IPv6 gateway address.
	 * @example 2001:db8:2::100
	 */
	IPv6Gateway?: string;
	/**
	 * Global IPv6 address.
	 * @example 2001:db8::5689
	 */
	GlobalIPv6Address?: string;
	/**
	 * Mask length of the global IPv6 address.
	 * @example 64
	 */
	GlobalIPv6PrefixLen?: number;
	/**
	 * MAC address for the endpoint on this network.
	 * @example 02:42:ac:11:00:04
	 */
	MacAddress?: string;
	DriverOpts?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
};
/** EndpointIPAMConfig represents an endpoint's IPAM configuration. */
export type EndpointIpamConfig = {
	IPv4Address?: string;
	IPv6Address?: string;
	LinkLocalIPs?: readonly string[];
};
export type PluginMount = {
	Name: string;
	Description: string;
	Settable: readonly string[];
	Source: string;
	Destination: string;
	Type: string;
	Options: readonly string[];
};
export type PluginDevice = {
	Name: string;
	Description: string;
	Settable: readonly string[];
	Path: string;
};
export type PluginEnv = {
	Name: string;
	Description: string;
	Settable: readonly string[];
	Value: string;
};
export type PluginInterfaceType = {
	Prefix: string;
	Capability: string;
	Version: string;
};
/**
 * Describes a permission the user has to accept upon installing
 * the plugin.
 */
export type PluginPrivilege = {
	Name?: string;
	Description?: string;
	Value?: readonly string[];
};
/** A plugin for the Engine API */
export type Plugin = {
	Id?: string;
	Name: string;
	/**
	 * True if the plugin is running. False if the plugin is not running, only
	 * installed.
	 * @example true
	 */
	Enabled: boolean;
	Settings: {
		Mounts: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Source: string;
			Destination: string;
			Type: string;
			Options: readonly string[];
		}[];
		Env: readonly string[];
		Args: readonly string[];
		Devices: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Path: string;
		}[];
	};
	/**
	 * plugin remote reference used to push/pull the plugin
	 * @example localhost:5000/tiborvass/sample-volume-plugin:latest
	 */
	PluginReference?: string;
	Config: {
		/**
		 * Docker Version used to create the plugin
		 * @example 17.06.0-ce
		 */
		DockerVersion?: string;
		Description: string;
		Documentation: string;
		Interface: {
			Types: readonly {
				Prefix: string;
				Capability: string;
				Version: string;
			}[];
			Socket: string;
			/**
			 * Protocol to use for clients connecting to the plugin.
			 * @enum ,moby.plugins.http/v1
			 * @example some.protocol/v1.0
			 */
			ProtocolScheme?: "" | "moby.plugins.http/v1";
		};
		Entrypoint: readonly string[];
		WorkDir: string;
		User?: {
			UID?: number;
			GID?: number;
		};
		Network: {
			Type: string;
		};
		Linux: {
			Capabilities: readonly string[];
			AllowAllDevices: boolean;
			Devices: readonly {
				Name: string;
				Description: string;
				Settable: readonly string[];
				Path: string;
			}[];
		};
		PropagatedMount: string;
		IpcHost: boolean;
		PidHost: boolean;
		Mounts: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Source: string;
			Destination: string;
			Type: string;
			Options: readonly string[];
		}[];
		Env: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Value: string;
		}[];
		Args: {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Value: readonly string[];
		};
		rootfs?: {
			type?: string;
			diff_ids?: readonly string[];
		};
	};
};
/**
 * The version number of the object such as node, service, etc. This is needed
 * to avoid conflicting writes. The client must send the version number along
 * with the modified specification when updating these objects.
 *
 * This approach ensures safe concurrency and determinism in that the change
 * on the object may not be applied if the version number has changed from the
 * last read. In other words, if two update requests specify the same base
 * version, only one of the requests can succeed. As a result, two separate
 * update requests that happen at the same time will not unintentionally
 * overwrite each other.
 */
export type ObjectVersion = {
	Index?: number;
};
export type NodeSpec = {
	/**
	 * Name for the node.
	 * @example my-node
	 */
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Role of the node.
	 * @enum worker,manager
	 * @example manager
	 */
	Role?: "worker" | "manager";
	/**
	 * Availability of the node.
	 * @enum active,pause,drain
	 * @example active
	 */
	Availability?: "active" | "pause" | "drain";
};
export type Node = {
	ID?: string;
	Version?: {
		Index?: number;
	};
	/**
	 * Date and time at which the node was added to the swarm in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2016-08-18T10:44:24.496525531Z
	 */
	CreatedAt?: string;
	/**
	 * Date and time at which the node was last updated in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2017-08-09T07:09:37.632105588Z
	 */
	UpdatedAt?: string;
	Spec?: {
		/**
		 * Name for the node.
		 * @example my-node
		 */
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Role of the node.
		 * @enum worker,manager
		 * @example manager
		 */
		Role?: "worker" | "manager";
		/**
		 * Availability of the node.
		 * @enum active,pause,drain
		 * @example active
		 */
		Availability?: "active" | "pause" | "drain";
	};
	Description?: {
		Hostname?: string;
		Platform?: {
			/**
			 * Architecture represents the hardware architecture (for example,
			 * `x86_64`).
			 * @example x86_64
			 */
			Architecture?: string;
			/**
			 * OS represents the Operating System (for example, `linux` or `windows`).
			 * @example linux
			 */
			OS?: string;
		};
		Resources?: {
			NanoCPUs?: number;
			MemoryBytes?: number;
			/**
			 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
			 * String resources (e.g, `GPU=UUID1`).
			 * @example [object Object],[object Object],[object Object]
			 */
			GenericResources?: readonly {
				NamedResourceSpec?: {
					Kind?: string;
					Value?: string;
				};
				DiscreteResourceSpec?: {
					Kind?: string;
					Value?: number;
				};
			}[];
		};
		Engine?: {
			EngineVersion?: string;
			Labels?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			Plugins?: readonly {
				Type?: string;
				Name?: string;
			}[];
		};
		/**
		 * Information about the issuer of leaf TLS certificates and the trusted root
		 * CA certificate.
		 * @example [object Object]
		 */
		TLSInfo?: {
			TrustRoot?: string;
			CertIssuerSubject?: string;
			CertIssuerPublicKey?: string;
		};
	};
	Status?: {
		/**
		 * NodeState represents the state of a node.
		 * @enum unknown,down,ready,disconnected
		 * @example ready
		 */
		State?: "unknown" | "down" | "ready" | "disconnected";
		Message?: string;
		/**
		 * IP address of the node.
		 * @example 172.17.0.2
		 */
		Addr?: string;
	};
	ManagerStatus?: {
		Leader?: boolean;
		/**
		 * Reachability represents the reachability of a node.
		 * @enum unknown,unreachable,reachable
		 * @example reachable
		 */
		Reachability?: "unknown" | "unreachable" | "reachable";
		/**
		 * The IP address and port at which the manager is reachable.
		 * @example 10.0.0.46:2377
		 */
		Addr?: string;
	};
};
/**
 * NodeDescription encapsulates the properties of the Node as reported by the
 * agent.
 */
export type NodeDescription = {
	Hostname?: string;
	Platform?: {
		/**
		 * Architecture represents the hardware architecture (for example,
		 * `x86_64`).
		 * @example x86_64
		 */
		Architecture?: string;
		/**
		 * OS represents the Operating System (for example, `linux` or `windows`).
		 * @example linux
		 */
		OS?: string;
	};
	Resources?: {
		NanoCPUs?: number;
		MemoryBytes?: number;
		/**
		 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
		 * String resources (e.g, `GPU=UUID1`).
		 * @example [object Object],[object Object],[object Object]
		 */
		GenericResources?: readonly {
			NamedResourceSpec?: {
				Kind?: string;
				Value?: string;
			};
			DiscreteResourceSpec?: {
				Kind?: string;
				Value?: number;
			};
		}[];
	};
	Engine?: {
		EngineVersion?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Plugins?: readonly {
			Type?: string;
			Name?: string;
		}[];
	};
	/**
	 * Information about the issuer of leaf TLS certificates and the trusted root
	 * CA certificate.
	 * @example [object Object]
	 */
	TLSInfo?: {
		TrustRoot?: string;
		CertIssuerSubject?: string;
		CertIssuerPublicKey?: string;
	};
};
/** Platform represents the platform (Arch/OS). */
export type Platform = {
	/**
	 * Architecture represents the hardware architecture (for example,
	 * `x86_64`).
	 * @example x86_64
	 */
	Architecture?: string;
	/**
	 * OS represents the Operating System (for example, `linux` or `windows`).
	 * @example linux
	 */
	OS?: string;
};
/** EngineDescription provides information about an engine. */
export type EngineDescription = {
	EngineVersion?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Plugins?: readonly {
		Type?: string;
		Name?: string;
	}[];
};
/**
 * Information about the issuer of leaf TLS certificates and the trusted root
 * CA certificate.
 */
export type TlsInfo = {
	TrustRoot?: string;
	CertIssuerSubject?: string;
	CertIssuerPublicKey?: string;
};
/**
 * NodeStatus represents the status of a node.
 *
 * It provides the current status of the node, as seen by the manager.
 */
export type NodeStatus = {
	/**
	 * NodeState represents the state of a node.
	 * @enum unknown,down,ready,disconnected
	 * @example ready
	 */
	State?: "unknown" | "down" | "ready" | "disconnected";
	Message?: string;
	/**
	 * IP address of the node.
	 * @example 172.17.0.2
	 */
	Addr?: string;
};
/** NodeState represents the state of a node. */
export type NodeState = "unknown" | "down" | "ready" | "disconnected";
/**
 * ManagerStatus represents the status of a manager.
 *
 * It provides the current status of a node's manager component, if the node
 * is a manager.
 */
export type ManagerStatus = {
	Leader?: boolean;
	/**
	 * Reachability represents the reachability of a node.
	 * @enum unknown,unreachable,reachable
	 * @example reachable
	 */
	Reachability?: "unknown" | "unreachable" | "reachable";
	/**
	 * The IP address and port at which the manager is reachable.
	 * @example 10.0.0.46:2377
	 */
	Addr?: string;
};
/** Reachability represents the reachability of a node. */
export type Reachability = "unknown" | "unreachable" | "reachable";
/** User modifiable swarm configuration. */
export type SwarmSpec = {
	/**
	 * Name of the swarm.
	 * @example default
	 */
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Orchestration?: {
		/**
		 * The number of historic tasks to keep per instance or node. If
		 * negative, never remove completed or failed tasks.
		 * @example 10
		 */
		TaskHistoryRetentionLimit?: number;
	};
	Raft?: {
		/**
		 * The number of log entries between snapshots.
		 * @example 10000
		 */
		SnapshotInterval?: number;
		KeepOldSnapshots?: number;
		/**
		 * The number of log entries to keep around to sync up slow followers
		 * after a snapshot is created.
		 * @example 500
		 */
		LogEntriesForSlowFollowers?: number;
		/**
		 * The number of ticks that a follower will wait for a message from
		 * the leader before becoming a candidate and starting an election.
		 * `ElectionTick` must be greater than `HeartbeatTick`.
		 *
		 * A tick currently defaults to one second, so these translate
		 * directly to seconds currently, but this is NOT guaranteed.
		 * @example 3
		 */
		ElectionTick?: number;
		/**
		 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
		 * the leader will send a heartbeat to the followers.
		 *
		 * A tick currently defaults to one second, so these translate
		 * directly to seconds currently, but this is NOT guaranteed.
		 * @example 1
		 */
		HeartbeatTick?: number;
	};
	Dispatcher?: {
		/**
		 * The delay for an agent to send a heartbeat to the dispatcher.
		 * @example 5000000000
		 */
		HeartbeatPeriod?: number;
	};
	CAConfig?: {
		/**
		 * The duration node certificates are issued for.
		 * @example 7776000000000000
		 */
		NodeCertExpiry?: number;
		ExternalCAs?: readonly {
			/**
			 * Protocol for communication with the external CA (currently
			 * only `cfssl` is supported).
			 * @default cfssl
			 * @enum cfssl
			 */
			Protocol?: "cfssl";
			URL?: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			CACert?: string;
		}[];
		SigningCACert?: string;
		SigningCAKey?: string;
		ForceRotate?: number;
	};
	EncryptionConfig?: {
		/**
		 * If set, generate a key and use it to lock data stored on the
		 * managers.
		 * @example false
		 */
		AutoLockManagers?: boolean;
	};
	TaskDefaults?: {
		LogDriver?: {
			/**
			 * The log driver to use as a default for new tasks.
			 * @example json-file
			 */
			Name?: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
};
/**
 * ClusterInfo represents information about the swarm as is returned by the
 * "/info" endpoint. Join-tokens are not included.
 */
export type ClusterInfo = {
	/**
	 * The ID of the swarm.
	 * @example abajmipo7b4xz5ip2nrla6b11
	 */
	ID?: string;
	Version?: {
		Index?: number;
	};
	/**
	 * Date and time at which the swarm was initialised in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2016-08-18T10:44:24.496525531Z
	 */
	CreatedAt?: string;
	/**
	 * Date and time at which the swarm was last updated in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2017-08-09T07:09:37.632105588Z
	 */
	UpdatedAt?: string;
	Spec?: {
		/**
		 * Name of the swarm.
		 * @example default
		 */
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Orchestration?: {
			/**
			 * The number of historic tasks to keep per instance or node. If
			 * negative, never remove completed or failed tasks.
			 * @example 10
			 */
			TaskHistoryRetentionLimit?: number;
		};
		Raft?: {
			/**
			 * The number of log entries between snapshots.
			 * @example 10000
			 */
			SnapshotInterval?: number;
			KeepOldSnapshots?: number;
			/**
			 * The number of log entries to keep around to sync up slow followers
			 * after a snapshot is created.
			 * @example 500
			 */
			LogEntriesForSlowFollowers?: number;
			/**
			 * The number of ticks that a follower will wait for a message from
			 * the leader before becoming a candidate and starting an election.
			 * `ElectionTick` must be greater than `HeartbeatTick`.
			 *
			 * A tick currently defaults to one second, so these translate
			 * directly to seconds currently, but this is NOT guaranteed.
			 * @example 3
			 */
			ElectionTick?: number;
			/**
			 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
			 * the leader will send a heartbeat to the followers.
			 *
			 * A tick currently defaults to one second, so these translate
			 * directly to seconds currently, but this is NOT guaranteed.
			 * @example 1
			 */
			HeartbeatTick?: number;
		};
		Dispatcher?: {
			/**
			 * The delay for an agent to send a heartbeat to the dispatcher.
			 * @example 5000000000
			 */
			HeartbeatPeriod?: number;
		};
		CAConfig?: {
			/**
			 * The duration node certificates are issued for.
			 * @example 7776000000000000
			 */
			NodeCertExpiry?: number;
			ExternalCAs?: readonly {
				/**
				 * Protocol for communication with the external CA (currently
				 * only `cfssl` is supported).
				 * @default cfssl
				 * @enum cfssl
				 */
				Protocol?: "cfssl";
				URL?: string;
				Options?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				CACert?: string;
			}[];
			SigningCACert?: string;
			SigningCAKey?: string;
			ForceRotate?: number;
		};
		EncryptionConfig?: {
			/**
			 * If set, generate a key and use it to lock data stored on the
			 * managers.
			 * @example false
			 */
			AutoLockManagers?: boolean;
		};
		TaskDefaults?: {
			LogDriver?: {
				/**
				 * The log driver to use as a default for new tasks.
				 * @example json-file
				 */
				Name?: string;
				Options?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			};
		};
	};
	/**
	 * Information about the issuer of leaf TLS certificates and the trusted root
	 * CA certificate.
	 * @example [object Object]
	 */
	TLSInfo?: {
		TrustRoot?: string;
		CertIssuerSubject?: string;
		CertIssuerPublicKey?: string;
	};
	/**
	 * Whether there is currently a root CA rotation in progress for the swarm
	 * @example false
	 */
	RootRotationInProgress?: boolean;
	/**
	 * DataPathPort specifies the data path port number for data traffic.
	 * Acceptable port range is 1024 to 49151.
	 * If no port is set or is set to 0, the default port (4789) is used.
	 * @example 4789
	 */
	DataPathPort?: number;
	DefaultAddrPool?: readonly string[];
	/**
	 * SubnetSize specifies the subnet size of the networks created from the
	 * default subnet pool.
	 * @example 24
	 */
	SubnetSize?: number;
};
/** JoinTokens contains the tokens workers and managers need to join the swarm. */
export type JoinTokens = {
	/**
	 * The token workers can use to join the swarm.
	 * @example SWMTKN-1-3pu6hszjas19xyp7ghgosyx9k8atbfcr8p2is99znpy26u2lkl-1awxwuwd3z9j1z3puu7rcgdbx
	 */
	Worker?: string;
	/**
	 * The token managers can use to join the swarm.
	 * @example SWMTKN-1-3pu6hszjas19xyp7ghgosyx9k8atbfcr8p2is99znpy26u2lkl-7p73s1dx5in4tatdymyhg9hu2
	 */
	Manager?: string;
};
export type Swarm = {
	/**
	 * The ID of the swarm.
	 * @example abajmipo7b4xz5ip2nrla6b11
	 */
	ID?: string;
	Version?: {
		Index?: number;
	};
	/**
	 * Date and time at which the swarm was initialised in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2016-08-18T10:44:24.496525531Z
	 */
	CreatedAt?: string;
	/**
	 * Date and time at which the swarm was last updated in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2017-08-09T07:09:37.632105588Z
	 */
	UpdatedAt?: string;
	Spec?: {
		/**
		 * Name of the swarm.
		 * @example default
		 */
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Orchestration?: {
			/**
			 * The number of historic tasks to keep per instance or node. If
			 * negative, never remove completed or failed tasks.
			 * @example 10
			 */
			TaskHistoryRetentionLimit?: number;
		};
		Raft?: {
			/**
			 * The number of log entries between snapshots.
			 * @example 10000
			 */
			SnapshotInterval?: number;
			KeepOldSnapshots?: number;
			/**
			 * The number of log entries to keep around to sync up slow followers
			 * after a snapshot is created.
			 * @example 500
			 */
			LogEntriesForSlowFollowers?: number;
			/**
			 * The number of ticks that a follower will wait for a message from
			 * the leader before becoming a candidate and starting an election.
			 * `ElectionTick` must be greater than `HeartbeatTick`.
			 *
			 * A tick currently defaults to one second, so these translate
			 * directly to seconds currently, but this is NOT guaranteed.
			 * @example 3
			 */
			ElectionTick?: number;
			/**
			 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
			 * the leader will send a heartbeat to the followers.
			 *
			 * A tick currently defaults to one second, so these translate
			 * directly to seconds currently, but this is NOT guaranteed.
			 * @example 1
			 */
			HeartbeatTick?: number;
		};
		Dispatcher?: {
			/**
			 * The delay for an agent to send a heartbeat to the dispatcher.
			 * @example 5000000000
			 */
			HeartbeatPeriod?: number;
		};
		CAConfig?: {
			/**
			 * The duration node certificates are issued for.
			 * @example 7776000000000000
			 */
			NodeCertExpiry?: number;
			ExternalCAs?: readonly {
				/**
				 * Protocol for communication with the external CA (currently
				 * only `cfssl` is supported).
				 * @default cfssl
				 * @enum cfssl
				 */
				Protocol?: "cfssl";
				URL?: string;
				Options?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				CACert?: string;
			}[];
			SigningCACert?: string;
			SigningCAKey?: string;
			ForceRotate?: number;
		};
		EncryptionConfig?: {
			/**
			 * If set, generate a key and use it to lock data stored on the
			 * managers.
			 * @example false
			 */
			AutoLockManagers?: boolean;
		};
		TaskDefaults?: {
			LogDriver?: {
				/**
				 * The log driver to use as a default for new tasks.
				 * @example json-file
				 */
				Name?: string;
				Options?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			};
		};
	};
	/**
	 * Information about the issuer of leaf TLS certificates and the trusted root
	 * CA certificate.
	 * @example [object Object]
	 */
	TLSInfo?: {
		TrustRoot?: string;
		CertIssuerSubject?: string;
		CertIssuerPublicKey?: string;
	};
	/**
	 * Whether there is currently a root CA rotation in progress for the swarm
	 * @example false
	 */
	RootRotationInProgress?: boolean;
	/**
	 * DataPathPort specifies the data path port number for data traffic.
	 * Acceptable port range is 1024 to 49151.
	 * If no port is set or is set to 0, the default port (4789) is used.
	 * @example 4789
	 */
	DataPathPort?: number;
	DefaultAddrPool?: readonly string[];
	/**
	 * SubnetSize specifies the subnet size of the networks created from the
	 * default subnet pool.
	 * @example 24
	 */
	SubnetSize?: number;
} & {
	JoinTokens?: {
		/**
		 * The token workers can use to join the swarm.
		 * @example SWMTKN-1-3pu6hszjas19xyp7ghgosyx9k8atbfcr8p2is99znpy26u2lkl-1awxwuwd3z9j1z3puu7rcgdbx
		 */
		Worker?: string;
		/**
		 * The token managers can use to join the swarm.
		 * @example SWMTKN-1-3pu6hszjas19xyp7ghgosyx9k8atbfcr8p2is99znpy26u2lkl-7p73s1dx5in4tatdymyhg9hu2
		 */
		Manager?: string;
	};
};
/** User modifiable task configuration. */
export type TaskSpec = {
	PluginSpec?: {
		Name?: string;
		Remote?: string;
		Disabled?: boolean;
		PluginPrivilege?: readonly {
			Name?: string;
			Description?: string;
			Value?: readonly string[];
		}[];
	};
	ContainerSpec?: {
		Image?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Command?: readonly string[];
		Args?: readonly string[];
		Hostname?: string;
		Env?: readonly string[];
		Dir?: string;
		User?: string;
		Groups?: readonly string[];
		Privileges?: {
			CredentialSpec?: {
				/**
				 * Load credential spec from a Swarm Config with the given ID.
				 * The specified config must also be present in the Configs
				 * field with the Runtime property set.
				 *
				 * <p><br /></p>
				 *
				 *
				 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
				 * > and `CredentialSpec.Config` are mutually exclusive.
				 * @example 0bt9dmxjvjiqermk6xrop3ekq
				 */
				Config?: string;
				/**
				 * Load credential spec from this file. The file is read by
				 * the daemon, and must be present in the `CredentialSpecs`
				 * subdirectory in the docker data directory, which defaults
				 * to `C:\ProgramData\Docker\` on Windows.
				 *
				 * For example, specifying `spec.json` loads
				 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
				 *
				 * <p><br /></p>
				 *
				 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
				 * > and `CredentialSpec.Config` are mutually exclusive.
				 * @example spec.json
				 */
				File?: string;
				Registry?: string;
			};
			SELinuxContext?: {
				Disable?: boolean;
				User?: string;
				Role?: string;
				Type?: string;
				Level?: string;
			};
		};
		TTY?: boolean;
		OpenStdin?: boolean;
		ReadOnly?: boolean;
		Mounts?: readonly {
			Target?: string;
			Source?: string;
			/**
			 * The mount type. Available types:
			 *
			 * - `bind` Mounts a file or directory from the host into the container. Must
			 * exist prior to creating the container.
			 * - `volume` Creates a volume with the given name and options (or uses a
			 * pre-existing volume with the same name and options). These are **not**
			 * removed when the container is removed.
			 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
			 * specified for tmpfs.
			 * - `npipe` Mounts a named pipe from the host into the container. Must exist
			 * prior to creating the container.
			 * - `cluster` a Swarm cluster volume
			 * @enum bind,volume,tmpfs,npipe,cluster
			 */
			Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
			ReadOnly?: boolean;
			Consistency?: string;
			BindOptions?: {
				/**
				 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
				 * @enum private,rprivate,shared,rshared,slave,rslave
				 */
				Propagation?:
					| "private"
					| "rprivate"
					| "shared"
					| "rshared"
					| "slave"
					| "rslave";
				NonRecursive?: boolean;
				CreateMountpoint?: boolean;
			};
			VolumeOptions?: {
				NoCopy?: boolean;
				Labels?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				DriverConfig?: {
					Name?: string;
					Options?: Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>;
				};
			};
			TmpfsOptions?: {
				SizeBytes?: number;
				Mode?: number;
			};
		}[];
		StopSignal?: string;
		StopGracePeriod?: number;
		HealthCheck?: {
			Test?: readonly string[];
			Interval?: number;
			Timeout?: number;
			Retries?: number;
			StartPeriod?: number;
		};
		Hosts?: readonly string[];
		DNSConfig?: {
			Nameservers?: readonly string[];
			Search?: readonly string[];
			Options?: readonly string[];
		};
		Secrets?: readonly {
			File?: {
				Name?: string;
				UID?: string;
				GID?: string;
				Mode?: number;
			};
			SecretID?: string;
			SecretName?: string;
		}[];
		Configs?: readonly {
			File?: {
				Name?: string;
				UID?: string;
				GID?: string;
				Mode?: number;
			};
			Runtime?: {};
			ConfigID?: string;
			ConfigName?: string;
		}[];
		/**
		 * Isolation technology of the containers running the service.
		 * (Windows only)
		 * @enum default,process,hyperv,
		 */
		Isolation?: "default" | "process" | "hyperv" | "";
		Init?: boolean | null;
		Sysctls?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * A list of kernel capabilities to add to the default set
		 * for the container.
		 * @example CAP_NET_RAW,CAP_SYS_ADMIN,CAP_SYS_CHROOT,CAP_SYSLOG
		 */
		CapabilityAdd?: readonly string[];
		/**
		 * A list of kernel capabilities to drop from the default set
		 * for the container.
		 * @example CAP_NET_RAW
		 */
		CapabilityDrop?: readonly string[];
		Ulimits?: readonly {
			Name?: string;
			Soft?: number;
			Hard?: number;
		}[];
	};
	NetworkAttachmentSpec?: {
		ContainerID?: string;
	};
	Resources?: {
		Limits?: {
			NanoCPUs?: number;
			MemoryBytes?: number;
			/**
			 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
			 * @example 100
			 */
			Pids?: number;
		};
		Reservations?: {
			NanoCPUs?: number;
			MemoryBytes?: number;
			/**
			 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
			 * String resources (e.g, `GPU=UUID1`).
			 * @example [object Object],[object Object],[object Object]
			 */
			GenericResources?: readonly {
				NamedResourceSpec?: {
					Kind?: string;
					Value?: string;
				};
				DiscreteResourceSpec?: {
					Kind?: string;
					Value?: number;
				};
			}[];
		};
	};
	RestartPolicy?: {
		/**
		 * Condition for restart.
		 * @enum none,on-failure,any
		 */
		Condition?: "none" | "on-failure" | "any";
		Delay?: number;
		MaxAttempts?: number;
		Window?: number;
	};
	Placement?: {
		/**
		 * An array of constraint expressions to limit the set of nodes where
		 * a task can be scheduled. Constraint expressions can either use a
		 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
		 * nodes that satisfy every expression (AND match). Constraints can
		 * match node or Docker Engine labels as follows:
		 *
		 * node attribute       | matches                        | example
		 * ---------------------|--------------------------------|-----------------------------------------------
		 * `node.id`            | Node ID                        |
		 * `node.id==2ivku8v2gvtg4`
		 * `node.hostname`      | Node hostname                  |
		 * `node.hostname!=node-2`
		 * `node.role`          | Node role (`manager`/`worker`) |
		 * `node.role==manager`
		 * `node.platform.os`   | Node operating system          |
		 * `node.platform.os==windows`
		 * `node.platform.arch` | Node architecture              |
		 * `node.platform.arch==x86_64`
		 * `node.labels`        | User-defined node labels       |
		 * `node.labels.security==high`
		 * `engine.labels`      | Docker Engine's labels         |
		 * `engine.labels.operatingsystem==ubuntu-14.04`
		 *
		 * `engine.labels` apply to Docker Engine labels like operating system,
		 * drivers, etc. Swarm administrators add `node.labels` for operational
		 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
		 * @example node.hostname!=node3.corp.example.com,node.role!=manager,node.labels.type==production,node.platform.os==linux,node.platform.arch==x86_64
		 */
		Constraints?: readonly string[];
		/**
		 * Preferences provide a way to make the scheduler aware of factors
		 * such as topology. They are provided in order from highest to
		 * lowest precedence.
		 * @example [object Object],[object Object]
		 */
		Preferences?: readonly {
			Spread?: {
				SpreadDescriptor?: string;
			};
		}[];
		MaxReplicas?: number;
		Platforms?: readonly {
			/**
			 * Architecture represents the hardware architecture (for example,
			 * `x86_64`).
			 * @example x86_64
			 */
			Architecture?: string;
			/**
			 * OS represents the Operating System (for example, `linux` or `windows`).
			 * @example linux
			 */
			OS?: string;
		}[];
	};
	ForceUpdate?: number;
	Runtime?: string;
	Networks?: readonly {
		Target?: string;
		Aliases?: readonly string[];
		DriverOpts?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	}[];
	LogDriver?: {
		Name?: string;
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
};
export type TaskState =
	| "new"
	| "allocated"
	| "pending"
	| "assigned"
	| "accepted"
	| "preparing"
	| "ready"
	| "starting"
	| "running"
	| "complete"
	| "shutdown"
	| "failed"
	| "rejected"
	| "remove"
	| "orphaned";
export type Task = {
	ID?: string;
	Version?: {
		Index?: number;
	};
	CreatedAt?: string;
	UpdatedAt?: string;
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Spec?: {
		PluginSpec?: {
			Name?: string;
			Remote?: string;
			Disabled?: boolean;
			PluginPrivilege?: readonly {
				Name?: string;
				Description?: string;
				Value?: readonly string[];
			}[];
		};
		ContainerSpec?: {
			Image?: string;
			Labels?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			Command?: readonly string[];
			Args?: readonly string[];
			Hostname?: string;
			Env?: readonly string[];
			Dir?: string;
			User?: string;
			Groups?: readonly string[];
			Privileges?: {
				CredentialSpec?: {
					/**
					 * Load credential spec from a Swarm Config with the given ID.
					 * The specified config must also be present in the Configs
					 * field with the Runtime property set.
					 *
					 * <p><br /></p>
					 *
					 *
					 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
					 * > and `CredentialSpec.Config` are mutually exclusive.
					 * @example 0bt9dmxjvjiqermk6xrop3ekq
					 */
					Config?: string;
					/**
					 * Load credential spec from this file. The file is read by
					 * the daemon, and must be present in the `CredentialSpecs`
					 * subdirectory in the docker data directory, which defaults
					 * to `C:\ProgramData\Docker\` on Windows.
					 *
					 * For example, specifying `spec.json` loads
					 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
					 *
					 * <p><br /></p>
					 *
					 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
					 * > and `CredentialSpec.Config` are mutually exclusive.
					 * @example spec.json
					 */
					File?: string;
					Registry?: string;
				};
				SELinuxContext?: {
					Disable?: boolean;
					User?: string;
					Role?: string;
					Type?: string;
					Level?: string;
				};
			};
			TTY?: boolean;
			OpenStdin?: boolean;
			ReadOnly?: boolean;
			Mounts?: readonly {
				Target?: string;
				Source?: string;
				/**
				 * The mount type. Available types:
				 *
				 * - `bind` Mounts a file or directory from the host into the container. Must
				 * exist prior to creating the container.
				 * - `volume` Creates a volume with the given name and options (or uses a
				 * pre-existing volume with the same name and options). These are **not**
				 * removed when the container is removed.
				 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
				 * specified for tmpfs.
				 * - `npipe` Mounts a named pipe from the host into the container. Must exist
				 * prior to creating the container.
				 * - `cluster` a Swarm cluster volume
				 * @enum bind,volume,tmpfs,npipe,cluster
				 */
				Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
				ReadOnly?: boolean;
				Consistency?: string;
				BindOptions?: {
					/**
					 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
					 * @enum private,rprivate,shared,rshared,slave,rslave
					 */
					Propagation?:
						| "private"
						| "rprivate"
						| "shared"
						| "rshared"
						| "slave"
						| "rslave";
					NonRecursive?: boolean;
					CreateMountpoint?: boolean;
				};
				VolumeOptions?: {
					NoCopy?: boolean;
					Labels?: Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>;
					DriverConfig?: {
						Name?: string;
						Options?: Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>;
					};
				};
				TmpfsOptions?: {
					SizeBytes?: number;
					Mode?: number;
				};
			}[];
			StopSignal?: string;
			StopGracePeriod?: number;
			HealthCheck?: {
				Test?: readonly string[];
				Interval?: number;
				Timeout?: number;
				Retries?: number;
				StartPeriod?: number;
			};
			Hosts?: readonly string[];
			DNSConfig?: {
				Nameservers?: readonly string[];
				Search?: readonly string[];
				Options?: readonly string[];
			};
			Secrets?: readonly {
				File?: {
					Name?: string;
					UID?: string;
					GID?: string;
					Mode?: number;
				};
				SecretID?: string;
				SecretName?: string;
			}[];
			Configs?: readonly {
				File?: {
					Name?: string;
					UID?: string;
					GID?: string;
					Mode?: number;
				};
				Runtime?: {};
				ConfigID?: string;
				ConfigName?: string;
			}[];
			/**
			 * Isolation technology of the containers running the service.
			 * (Windows only)
			 * @enum default,process,hyperv,
			 */
			Isolation?: "default" | "process" | "hyperv" | "";
			Init?: boolean | null;
			Sysctls?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			/**
			 * A list of kernel capabilities to add to the default set
			 * for the container.
			 * @example CAP_NET_RAW,CAP_SYS_ADMIN,CAP_SYS_CHROOT,CAP_SYSLOG
			 */
			CapabilityAdd?: readonly string[];
			/**
			 * A list of kernel capabilities to drop from the default set
			 * for the container.
			 * @example CAP_NET_RAW
			 */
			CapabilityDrop?: readonly string[];
			Ulimits?: readonly {
				Name?: string;
				Soft?: number;
				Hard?: number;
			}[];
		};
		NetworkAttachmentSpec?: {
			ContainerID?: string;
		};
		Resources?: {
			Limits?: {
				NanoCPUs?: number;
				MemoryBytes?: number;
				/**
				 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
				 * @example 100
				 */
				Pids?: number;
			};
			Reservations?: {
				NanoCPUs?: number;
				MemoryBytes?: number;
				/**
				 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
				 * String resources (e.g, `GPU=UUID1`).
				 * @example [object Object],[object Object],[object Object]
				 */
				GenericResources?: readonly {
					NamedResourceSpec?: {
						Kind?: string;
						Value?: string;
					};
					DiscreteResourceSpec?: {
						Kind?: string;
						Value?: number;
					};
				}[];
			};
		};
		RestartPolicy?: {
			/**
			 * Condition for restart.
			 * @enum none,on-failure,any
			 */
			Condition?: "none" | "on-failure" | "any";
			Delay?: number;
			MaxAttempts?: number;
			Window?: number;
		};
		Placement?: {
			/**
			 * An array of constraint expressions to limit the set of nodes where
			 * a task can be scheduled. Constraint expressions can either use a
			 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
			 * nodes that satisfy every expression (AND match). Constraints can
			 * match node or Docker Engine labels as follows:
			 *
			 * node attribute       | matches                        | example
			 * ---------------------|--------------------------------|-----------------------------------------------
			 * `node.id`            | Node ID                        |
			 * `node.id==2ivku8v2gvtg4`
			 * `node.hostname`      | Node hostname                  |
			 * `node.hostname!=node-2`
			 * `node.role`          | Node role (`manager`/`worker`) |
			 * `node.role==manager`
			 * `node.platform.os`   | Node operating system          |
			 * `node.platform.os==windows`
			 * `node.platform.arch` | Node architecture              |
			 * `node.platform.arch==x86_64`
			 * `node.labels`        | User-defined node labels       |
			 * `node.labels.security==high`
			 * `engine.labels`      | Docker Engine's labels         |
			 * `engine.labels.operatingsystem==ubuntu-14.04`
			 *
			 * `engine.labels` apply to Docker Engine labels like operating system,
			 * drivers, etc. Swarm administrators add `node.labels` for operational
			 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
			 * @example node.hostname!=node3.corp.example.com,node.role!=manager,node.labels.type==production,node.platform.os==linux,node.platform.arch==x86_64
			 */
			Constraints?: readonly string[];
			/**
			 * Preferences provide a way to make the scheduler aware of factors
			 * such as topology. They are provided in order from highest to
			 * lowest precedence.
			 * @example [object Object],[object Object]
			 */
			Preferences?: readonly {
				Spread?: {
					SpreadDescriptor?: string;
				};
			}[];
			MaxReplicas?: number;
			Platforms?: readonly {
				/**
				 * Architecture represents the hardware architecture (for example,
				 * `x86_64`).
				 * @example x86_64
				 */
				Architecture?: string;
				/**
				 * OS represents the Operating System (for example, `linux` or `windows`).
				 * @example linux
				 */
				OS?: string;
			}[];
		};
		ForceUpdate?: number;
		Runtime?: string;
		Networks?: readonly {
			Target?: string;
			Aliases?: readonly string[];
			DriverOpts?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		LogDriver?: {
			Name?: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
	ServiceID?: string;
	Slot?: number;
	NodeID?: string;
	/**
	 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
	 * String resources (e.g, `GPU=UUID1`).
	 * @example [object Object],[object Object],[object Object]
	 */
	AssignedGenericResources?: readonly {
		NamedResourceSpec?: {
			Kind?: string;
			Value?: string;
		};
		DiscreteResourceSpec?: {
			Kind?: string;
			Value?: number;
		};
	}[];
	Status?: {
		Timestamp?: string;
		State?:
			| "new"
			| "allocated"
			| "pending"
			| "assigned"
			| "accepted"
			| "preparing"
			| "ready"
			| "starting"
			| "running"
			| "complete"
			| "shutdown"
			| "failed"
			| "rejected"
			| "remove"
			| "orphaned";
		Message?: string;
		Err?: string;
		ContainerStatus?: {
			ContainerID?: string;
			PID?: number;
			ExitCode?: number;
		};
	};
	DesiredState?:
		| "new"
		| "allocated"
		| "pending"
		| "assigned"
		| "accepted"
		| "preparing"
		| "ready"
		| "starting"
		| "running"
		| "complete"
		| "shutdown"
		| "failed"
		| "rejected"
		| "remove"
		| "orphaned";
	JobIteration?: {
		Index?: number;
	};
};
/** User modifiable configuration for a service. */
export type ServiceSpec = {
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	TaskTemplate?: {
		PluginSpec?: {
			Name?: string;
			Remote?: string;
			Disabled?: boolean;
			PluginPrivilege?: readonly {
				Name?: string;
				Description?: string;
				Value?: readonly string[];
			}[];
		};
		ContainerSpec?: {
			Image?: string;
			Labels?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			Command?: readonly string[];
			Args?: readonly string[];
			Hostname?: string;
			Env?: readonly string[];
			Dir?: string;
			User?: string;
			Groups?: readonly string[];
			Privileges?: {
				CredentialSpec?: {
					/**
					 * Load credential spec from a Swarm Config with the given ID.
					 * The specified config must also be present in the Configs
					 * field with the Runtime property set.
					 *
					 * <p><br /></p>
					 *
					 *
					 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
					 * > and `CredentialSpec.Config` are mutually exclusive.
					 * @example 0bt9dmxjvjiqermk6xrop3ekq
					 */
					Config?: string;
					/**
					 * Load credential spec from this file. The file is read by
					 * the daemon, and must be present in the `CredentialSpecs`
					 * subdirectory in the docker data directory, which defaults
					 * to `C:\ProgramData\Docker\` on Windows.
					 *
					 * For example, specifying `spec.json` loads
					 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
					 *
					 * <p><br /></p>
					 *
					 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
					 * > and `CredentialSpec.Config` are mutually exclusive.
					 * @example spec.json
					 */
					File?: string;
					Registry?: string;
				};
				SELinuxContext?: {
					Disable?: boolean;
					User?: string;
					Role?: string;
					Type?: string;
					Level?: string;
				};
			};
			TTY?: boolean;
			OpenStdin?: boolean;
			ReadOnly?: boolean;
			Mounts?: readonly {
				Target?: string;
				Source?: string;
				/**
				 * The mount type. Available types:
				 *
				 * - `bind` Mounts a file or directory from the host into the container. Must
				 * exist prior to creating the container.
				 * - `volume` Creates a volume with the given name and options (or uses a
				 * pre-existing volume with the same name and options). These are **not**
				 * removed when the container is removed.
				 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
				 * specified for tmpfs.
				 * - `npipe` Mounts a named pipe from the host into the container. Must exist
				 * prior to creating the container.
				 * - `cluster` a Swarm cluster volume
				 * @enum bind,volume,tmpfs,npipe,cluster
				 */
				Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
				ReadOnly?: boolean;
				Consistency?: string;
				BindOptions?: {
					/**
					 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
					 * @enum private,rprivate,shared,rshared,slave,rslave
					 */
					Propagation?:
						| "private"
						| "rprivate"
						| "shared"
						| "rshared"
						| "slave"
						| "rslave";
					NonRecursive?: boolean;
					CreateMountpoint?: boolean;
				};
				VolumeOptions?: {
					NoCopy?: boolean;
					Labels?: Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>;
					DriverConfig?: {
						Name?: string;
						Options?: Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>;
					};
				};
				TmpfsOptions?: {
					SizeBytes?: number;
					Mode?: number;
				};
			}[];
			StopSignal?: string;
			StopGracePeriod?: number;
			HealthCheck?: {
				Test?: readonly string[];
				Interval?: number;
				Timeout?: number;
				Retries?: number;
				StartPeriod?: number;
			};
			Hosts?: readonly string[];
			DNSConfig?: {
				Nameservers?: readonly string[];
				Search?: readonly string[];
				Options?: readonly string[];
			};
			Secrets?: readonly {
				File?: {
					Name?: string;
					UID?: string;
					GID?: string;
					Mode?: number;
				};
				SecretID?: string;
				SecretName?: string;
			}[];
			Configs?: readonly {
				File?: {
					Name?: string;
					UID?: string;
					GID?: string;
					Mode?: number;
				};
				Runtime?: {};
				ConfigID?: string;
				ConfigName?: string;
			}[];
			/**
			 * Isolation technology of the containers running the service.
			 * (Windows only)
			 * @enum default,process,hyperv,
			 */
			Isolation?: "default" | "process" | "hyperv" | "";
			Init?: boolean | null;
			Sysctls?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			/**
			 * A list of kernel capabilities to add to the default set
			 * for the container.
			 * @example CAP_NET_RAW,CAP_SYS_ADMIN,CAP_SYS_CHROOT,CAP_SYSLOG
			 */
			CapabilityAdd?: readonly string[];
			/**
			 * A list of kernel capabilities to drop from the default set
			 * for the container.
			 * @example CAP_NET_RAW
			 */
			CapabilityDrop?: readonly string[];
			Ulimits?: readonly {
				Name?: string;
				Soft?: number;
				Hard?: number;
			}[];
		};
		NetworkAttachmentSpec?: {
			ContainerID?: string;
		};
		Resources?: {
			Limits?: {
				NanoCPUs?: number;
				MemoryBytes?: number;
				/**
				 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
				 * @example 100
				 */
				Pids?: number;
			};
			Reservations?: {
				NanoCPUs?: number;
				MemoryBytes?: number;
				/**
				 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
				 * String resources (e.g, `GPU=UUID1`).
				 * @example [object Object],[object Object],[object Object]
				 */
				GenericResources?: readonly {
					NamedResourceSpec?: {
						Kind?: string;
						Value?: string;
					};
					DiscreteResourceSpec?: {
						Kind?: string;
						Value?: number;
					};
				}[];
			};
		};
		RestartPolicy?: {
			/**
			 * Condition for restart.
			 * @enum none,on-failure,any
			 */
			Condition?: "none" | "on-failure" | "any";
			Delay?: number;
			MaxAttempts?: number;
			Window?: number;
		};
		Placement?: {
			/**
			 * An array of constraint expressions to limit the set of nodes where
			 * a task can be scheduled. Constraint expressions can either use a
			 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
			 * nodes that satisfy every expression (AND match). Constraints can
			 * match node or Docker Engine labels as follows:
			 *
			 * node attribute       | matches                        | example
			 * ---------------------|--------------------------------|-----------------------------------------------
			 * `node.id`            | Node ID                        |
			 * `node.id==2ivku8v2gvtg4`
			 * `node.hostname`      | Node hostname                  |
			 * `node.hostname!=node-2`
			 * `node.role`          | Node role (`manager`/`worker`) |
			 * `node.role==manager`
			 * `node.platform.os`   | Node operating system          |
			 * `node.platform.os==windows`
			 * `node.platform.arch` | Node architecture              |
			 * `node.platform.arch==x86_64`
			 * `node.labels`        | User-defined node labels       |
			 * `node.labels.security==high`
			 * `engine.labels`      | Docker Engine's labels         |
			 * `engine.labels.operatingsystem==ubuntu-14.04`
			 *
			 * `engine.labels` apply to Docker Engine labels like operating system,
			 * drivers, etc. Swarm administrators add `node.labels` for operational
			 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
			 * @example node.hostname!=node3.corp.example.com,node.role!=manager,node.labels.type==production,node.platform.os==linux,node.platform.arch==x86_64
			 */
			Constraints?: readonly string[];
			/**
			 * Preferences provide a way to make the scheduler aware of factors
			 * such as topology. They are provided in order from highest to
			 * lowest precedence.
			 * @example [object Object],[object Object]
			 */
			Preferences?: readonly {
				Spread?: {
					SpreadDescriptor?: string;
				};
			}[];
			MaxReplicas?: number;
			Platforms?: readonly {
				/**
				 * Architecture represents the hardware architecture (for example,
				 * `x86_64`).
				 * @example x86_64
				 */
				Architecture?: string;
				/**
				 * OS represents the Operating System (for example, `linux` or `windows`).
				 * @example linux
				 */
				OS?: string;
			}[];
		};
		ForceUpdate?: number;
		Runtime?: string;
		Networks?: readonly {
			Target?: string;
			Aliases?: readonly string[];
			DriverOpts?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		LogDriver?: {
			Name?: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
	Mode?: {
		Replicated?: {
			Replicas?: number;
		};
		Global?: {};
		ReplicatedJob?: {
			/**
			 * The maximum number of replicas to run simultaneously.
			 * @default 1
			 */
			MaxConcurrent?: number;
			TotalCompletions?: number;
		};
		GlobalJob?: {};
	};
	UpdateConfig?: {
		Parallelism?: number;
		Delay?: number;
		/**
		 * Action to take if an updated task fails to run, or stops running
		 * during the update.
		 * @enum continue,pause,rollback
		 */
		FailureAction?: "continue" | "pause" | "rollback";
		Monitor?: number;
		MaxFailureRatio?: number;
		/**
		 * The order of operations when rolling out an updated task. Either
		 * the old task is shut down before the new task is started, or the
		 * new task is started before the old task is shut down.
		 * @enum stop-first,start-first
		 */
		Order?: "stop-first" | "start-first";
	};
	RollbackConfig?: {
		Parallelism?: number;
		Delay?: number;
		/**
		 * Action to take if an rolled back task fails to run, or stops
		 * running during the rollback.
		 * @enum continue,pause
		 */
		FailureAction?: "continue" | "pause";
		Monitor?: number;
		MaxFailureRatio?: number;
		/**
		 * The order of operations when rolling back a task. Either the old
		 * task is shut down before the new task is started, or the new task
		 * is started before the old task is shut down.
		 * @enum stop-first,start-first
		 */
		Order?: "stop-first" | "start-first";
	};
	Networks?: readonly {
		Target?: string;
		Aliases?: readonly string[];
		DriverOpts?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	}[];
	EndpointSpec?: {
		/**
		 * The mode of resolution to use for internal load balancing between tasks.
		 * @default vip
		 * @enum vip,dnsrr
		 */
		Mode?: "vip" | "dnsrr";
		Ports?: readonly {
			Name?: string;
			Protocol?: "tcp" | "udp" | "sctp";
			TargetPort?: number;
			PublishedPort?: number;
			/**
			 * The mode in which port is published.
			 *
			 * <p><br /></p>
			 *
			 * - "ingress" makes the target port accessible on every node,
			 *
			 * regardless of whether there is a task for the service running on
			 *
			 * that node or not.
			 * - "host" bypasses the routing mesh and publish the port directly on
			 *
			 * the swarm node where that service is running.
			 * @default ingress
			 * @enum ingress,host
			 * @example ingress
			 */
			PublishMode?: "ingress" | "host";
		}[];
	};
};
export type EndpointPortConfig = {
	Name?: string;
	Protocol?: "tcp" | "udp" | "sctp";
	TargetPort?: number;
	PublishedPort?: number;
	/**
	 * The mode in which port is published.
	 *
	 * <p><br /></p>
	 *
	 * - "ingress" makes the target port accessible on every node,
	 *
	 * regardless of whether there is a task for the service running on
	 *
	 * that node or not.
	 * - "host" bypasses the routing mesh and publish the port directly on
	 *
	 * the swarm node where that service is running.
	 * @default ingress
	 * @enum ingress,host
	 * @example ingress
	 */
	PublishMode?: "ingress" | "host";
};
/** Properties that can be configured to access and load balance a service. */
export type EndpointSpec = {
	/**
	 * The mode of resolution to use for internal load balancing between tasks.
	 * @default vip
	 * @enum vip,dnsrr
	 */
	Mode?: "vip" | "dnsrr";
	Ports?: readonly {
		Name?: string;
		Protocol?: "tcp" | "udp" | "sctp";
		TargetPort?: number;
		PublishedPort?: number;
		/**
		 * The mode in which port is published.
		 *
		 * <p><br /></p>
		 *
		 * - "ingress" makes the target port accessible on every node,
		 *
		 * regardless of whether there is a task for the service running on
		 *
		 * that node or not.
		 * - "host" bypasses the routing mesh and publish the port directly on
		 *
		 * the swarm node where that service is running.
		 * @default ingress
		 * @enum ingress,host
		 * @example ingress
		 */
		PublishMode?: "ingress" | "host";
	}[];
};
export type Service = {
	ID?: string;
	Version?: {
		Index?: number;
	};
	CreatedAt?: string;
	UpdatedAt?: string;
	Spec?: {
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		TaskTemplate?: {
			PluginSpec?: {
				Name?: string;
				Remote?: string;
				Disabled?: boolean;
				PluginPrivilege?: readonly {
					Name?: string;
					Description?: string;
					Value?: readonly string[];
				}[];
			};
			ContainerSpec?: {
				Image?: string;
				Labels?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				Command?: readonly string[];
				Args?: readonly string[];
				Hostname?: string;
				Env?: readonly string[];
				Dir?: string;
				User?: string;
				Groups?: readonly string[];
				Privileges?: {
					CredentialSpec?: {
						/**
						 * Load credential spec from a Swarm Config with the given ID.
						 * The specified config must also be present in the Configs
						 * field with the Runtime property set.
						 *
						 * <p><br /></p>
						 *
						 *
						 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
						 * > and `CredentialSpec.Config` are mutually exclusive.
						 * @example 0bt9dmxjvjiqermk6xrop3ekq
						 */
						Config?: string;
						/**
						 * Load credential spec from this file. The file is read by
						 * the daemon, and must be present in the `CredentialSpecs`
						 * subdirectory in the docker data directory, which defaults
						 * to `C:\ProgramData\Docker\` on Windows.
						 *
						 * For example, specifying `spec.json` loads
						 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
						 *
						 * <p><br /></p>
						 *
						 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
						 * > and `CredentialSpec.Config` are mutually exclusive.
						 * @example spec.json
						 */
						File?: string;
						Registry?: string;
					};
					SELinuxContext?: {
						Disable?: boolean;
						User?: string;
						Role?: string;
						Type?: string;
						Level?: string;
					};
				};
				TTY?: boolean;
				OpenStdin?: boolean;
				ReadOnly?: boolean;
				Mounts?: readonly {
					Target?: string;
					Source?: string;
					/**
					 * The mount type. Available types:
					 *
					 * - `bind` Mounts a file or directory from the host into the container. Must
					 * exist prior to creating the container.
					 * - `volume` Creates a volume with the given name and options (or uses a
					 * pre-existing volume with the same name and options). These are **not**
					 * removed when the container is removed.
					 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
					 * specified for tmpfs.
					 * - `npipe` Mounts a named pipe from the host into the container. Must exist
					 * prior to creating the container.
					 * - `cluster` a Swarm cluster volume
					 * @enum bind,volume,tmpfs,npipe,cluster
					 */
					Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
					ReadOnly?: boolean;
					Consistency?: string;
					BindOptions?: {
						/**
						 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
						 * @enum private,rprivate,shared,rshared,slave,rslave
						 */
						Propagation?:
							| "private"
							| "rprivate"
							| "shared"
							| "rshared"
							| "slave"
							| "rslave";
						NonRecursive?: boolean;
						CreateMountpoint?: boolean;
					};
					VolumeOptions?: {
						NoCopy?: boolean;
						Labels?: Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>;
						DriverConfig?: {
							Name?: string;
							Options?: Record<
								string | number,
								/* additionalProperties is not handled yet */ unknown
							>;
						};
					};
					TmpfsOptions?: {
						SizeBytes?: number;
						Mode?: number;
					};
				}[];
				StopSignal?: string;
				StopGracePeriod?: number;
				HealthCheck?: {
					Test?: readonly string[];
					Interval?: number;
					Timeout?: number;
					Retries?: number;
					StartPeriod?: number;
				};
				Hosts?: readonly string[];
				DNSConfig?: {
					Nameservers?: readonly string[];
					Search?: readonly string[];
					Options?: readonly string[];
				};
				Secrets?: readonly {
					File?: {
						Name?: string;
						UID?: string;
						GID?: string;
						Mode?: number;
					};
					SecretID?: string;
					SecretName?: string;
				}[];
				Configs?: readonly {
					File?: {
						Name?: string;
						UID?: string;
						GID?: string;
						Mode?: number;
					};
					Runtime?: {};
					ConfigID?: string;
					ConfigName?: string;
				}[];
				/**
				 * Isolation technology of the containers running the service.
				 * (Windows only)
				 * @enum default,process,hyperv,
				 */
				Isolation?: "default" | "process" | "hyperv" | "";
				Init?: boolean | null;
				Sysctls?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				/**
				 * A list of kernel capabilities to add to the default set
				 * for the container.
				 * @example CAP_NET_RAW,CAP_SYS_ADMIN,CAP_SYS_CHROOT,CAP_SYSLOG
				 */
				CapabilityAdd?: readonly string[];
				/**
				 * A list of kernel capabilities to drop from the default set
				 * for the container.
				 * @example CAP_NET_RAW
				 */
				CapabilityDrop?: readonly string[];
				Ulimits?: readonly {
					Name?: string;
					Soft?: number;
					Hard?: number;
				}[];
			};
			NetworkAttachmentSpec?: {
				ContainerID?: string;
			};
			Resources?: {
				Limits?: {
					NanoCPUs?: number;
					MemoryBytes?: number;
					/**
					 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
					 * @example 100
					 */
					Pids?: number;
				};
				Reservations?: {
					NanoCPUs?: number;
					MemoryBytes?: number;
					/**
					 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
					 * String resources (e.g, `GPU=UUID1`).
					 * @example [object Object],[object Object],[object Object]
					 */
					GenericResources?: readonly {
						NamedResourceSpec?: {
							Kind?: string;
							Value?: string;
						};
						DiscreteResourceSpec?: {
							Kind?: string;
							Value?: number;
						};
					}[];
				};
			};
			RestartPolicy?: {
				/**
				 * Condition for restart.
				 * @enum none,on-failure,any
				 */
				Condition?: "none" | "on-failure" | "any";
				Delay?: number;
				MaxAttempts?: number;
				Window?: number;
			};
			Placement?: {
				/**
				 * An array of constraint expressions to limit the set of nodes where
				 * a task can be scheduled. Constraint expressions can either use a
				 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
				 * nodes that satisfy every expression (AND match). Constraints can
				 * match node or Docker Engine labels as follows:
				 *
				 * node attribute       | matches                        | example
				 * ---------------------|--------------------------------|-----------------------------------------------
				 * `node.id`            | Node ID                        |
				 * `node.id==2ivku8v2gvtg4`
				 * `node.hostname`      | Node hostname                  |
				 * `node.hostname!=node-2`
				 * `node.role`          | Node role (`manager`/`worker`) |
				 * `node.role==manager`
				 * `node.platform.os`   | Node operating system          |
				 * `node.platform.os==windows`
				 * `node.platform.arch` | Node architecture              |
				 * `node.platform.arch==x86_64`
				 * `node.labels`        | User-defined node labels       |
				 * `node.labels.security==high`
				 * `engine.labels`      | Docker Engine's labels         |
				 * `engine.labels.operatingsystem==ubuntu-14.04`
				 *
				 * `engine.labels` apply to Docker Engine labels like operating system,
				 * drivers, etc. Swarm administrators add `node.labels` for operational
				 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
				 * @example node.hostname!=node3.corp.example.com,node.role!=manager,node.labels.type==production,node.platform.os==linux,node.platform.arch==x86_64
				 */
				Constraints?: readonly string[];
				/**
				 * Preferences provide a way to make the scheduler aware of factors
				 * such as topology. They are provided in order from highest to
				 * lowest precedence.
				 * @example [object Object],[object Object]
				 */
				Preferences?: readonly {
					Spread?: {
						SpreadDescriptor?: string;
					};
				}[];
				MaxReplicas?: number;
				Platforms?: readonly {
					/**
					 * Architecture represents the hardware architecture (for example,
					 * `x86_64`).
					 * @example x86_64
					 */
					Architecture?: string;
					/**
					 * OS represents the Operating System (for example, `linux` or `windows`).
					 * @example linux
					 */
					OS?: string;
				}[];
			};
			ForceUpdate?: number;
			Runtime?: string;
			Networks?: readonly {
				Target?: string;
				Aliases?: readonly string[];
				DriverOpts?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			}[];
			LogDriver?: {
				Name?: string;
				Options?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			};
		};
		Mode?: {
			Replicated?: {
				Replicas?: number;
			};
			Global?: {};
			ReplicatedJob?: {
				/**
				 * The maximum number of replicas to run simultaneously.
				 * @default 1
				 */
				MaxConcurrent?: number;
				TotalCompletions?: number;
			};
			GlobalJob?: {};
		};
		UpdateConfig?: {
			Parallelism?: number;
			Delay?: number;
			/**
			 * Action to take if an updated task fails to run, or stops running
			 * during the update.
			 * @enum continue,pause,rollback
			 */
			FailureAction?: "continue" | "pause" | "rollback";
			Monitor?: number;
			MaxFailureRatio?: number;
			/**
			 * The order of operations when rolling out an updated task. Either
			 * the old task is shut down before the new task is started, or the
			 * new task is started before the old task is shut down.
			 * @enum stop-first,start-first
			 */
			Order?: "stop-first" | "start-first";
		};
		RollbackConfig?: {
			Parallelism?: number;
			Delay?: number;
			/**
			 * Action to take if an rolled back task fails to run, or stops
			 * running during the rollback.
			 * @enum continue,pause
			 */
			FailureAction?: "continue" | "pause";
			Monitor?: number;
			MaxFailureRatio?: number;
			/**
			 * The order of operations when rolling back a task. Either the old
			 * task is shut down before the new task is started, or the new task
			 * is started before the old task is shut down.
			 * @enum stop-first,start-first
			 */
			Order?: "stop-first" | "start-first";
		};
		Networks?: readonly {
			Target?: string;
			Aliases?: readonly string[];
			DriverOpts?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		EndpointSpec?: {
			/**
			 * The mode of resolution to use for internal load balancing between tasks.
			 * @default vip
			 * @enum vip,dnsrr
			 */
			Mode?: "vip" | "dnsrr";
			Ports?: readonly {
				Name?: string;
				Protocol?: "tcp" | "udp" | "sctp";
				TargetPort?: number;
				PublishedPort?: number;
				/**
				 * The mode in which port is published.
				 *
				 * <p><br /></p>
				 *
				 * - "ingress" makes the target port accessible on every node,
				 *
				 * regardless of whether there is a task for the service running on
				 *
				 * that node or not.
				 * - "host" bypasses the routing mesh and publish the port directly on
				 *
				 * the swarm node where that service is running.
				 * @default ingress
				 * @enum ingress,host
				 * @example ingress
				 */
				PublishMode?: "ingress" | "host";
			}[];
		};
	};
	Endpoint?: {
		Spec?: {
			/**
			 * The mode of resolution to use for internal load balancing between tasks.
			 * @default vip
			 * @enum vip,dnsrr
			 */
			Mode?: "vip" | "dnsrr";
			Ports?: readonly {
				Name?: string;
				Protocol?: "tcp" | "udp" | "sctp";
				TargetPort?: number;
				PublishedPort?: number;
				/**
				 * The mode in which port is published.
				 *
				 * <p><br /></p>
				 *
				 * - "ingress" makes the target port accessible on every node,
				 *
				 * regardless of whether there is a task for the service running on
				 *
				 * that node or not.
				 * - "host" bypasses the routing mesh and publish the port directly on
				 *
				 * the swarm node where that service is running.
				 * @default ingress
				 * @enum ingress,host
				 * @example ingress
				 */
				PublishMode?: "ingress" | "host";
			}[];
		};
		Ports?: readonly {
			Name?: string;
			Protocol?: "tcp" | "udp" | "sctp";
			TargetPort?: number;
			PublishedPort?: number;
			/**
			 * The mode in which port is published.
			 *
			 * <p><br /></p>
			 *
			 * - "ingress" makes the target port accessible on every node,
			 *
			 * regardless of whether there is a task for the service running on
			 *
			 * that node or not.
			 * - "host" bypasses the routing mesh and publish the port directly on
			 *
			 * the swarm node where that service is running.
			 * @default ingress
			 * @enum ingress,host
			 * @example ingress
			 */
			PublishMode?: "ingress" | "host";
		}[];
		VirtualIPs?: readonly {
			NetworkID?: string;
			Addr?: string;
		}[];
	};
	UpdateStatus?: {
		State?: "updating" | "paused" | "completed";
		StartedAt?: string;
		CompletedAt?: string;
		Message?: string;
	};
	ServiceStatus?: {
		/**
		 * The number of tasks for the service currently in the Running state.
		 * @example 7
		 */
		RunningTasks?: number;
		/**
		 * The number of tasks for the service desired to be running.
		 * For replicated services, this is the replica count from the
		 * service spec. For global services, this is computed by taking
		 * count of all tasks for the service with a Desired State other
		 * than Shutdown.
		 * @example 10
		 */
		DesiredTasks?: number;
		CompletedTasks?: number;
	};
	JobStatus?: {
		JobIteration?: {
			Index?: number;
		};
		LastExecution?: string;
	};
};
export type ImageDeleteResponseItem = {
	Untagged?: string;
	Deleted?: string;
};
export type ServiceUpdateResponse = {
	Warnings?: readonly string[];
};
export type ContainerSummary = {
	Id?: string;
	Names?: readonly string[];
	Image?: string;
	ImageID?: string;
	Command?: string;
	Created?: number;
	Ports?: readonly {
		IP?: string;
		PrivatePort: number;
		PublicPort?: number;
		Type: "tcp" | "udp" | "sctp";
	}[];
	SizeRw?: number;
	SizeRootFs?: number;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	State?: string;
	Status?: string;
	HostConfig?: {
		NetworkMode?: string;
	};
	NetworkSettings?: {
		Networks?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	Mounts?: readonly {
		/**
		 * The mount type:
		 *
		 * - `bind` a mount of a file or directory from the host into the container.
		 * - `volume` a docker volume with the given `Name`.
		 * - `tmpfs` a `tmpfs`.
		 * - `npipe` a named pipe from the host into the container.
		 * - `cluster` a Swarm cluster volume
		 * @enum bind,volume,tmpfs,npipe,cluster
		 * @example volume
		 */
		Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
		/**
		 * Name is the name reference to the underlying data defined by `Source`
		 * e.g., the volume name.
		 * @example myvolume
		 */
		Name?: string;
		/**
		 * Source location of the mount.
		 *
		 * For volumes, this contains the storage location of the volume (within
		 * `/var/lib/docker/volumes/`). For bind-mounts, and `npipe`, this contains
		 * the source (host) part of the bind-mount. For `tmpfs` mount points, this
		 * field is empty.
		 * @example /var/lib/docker/volumes/myvolume/_data
		 */
		Source?: string;
		/**
		 * Destination is the path relative to the container root (`/`) where
		 * the `Source` is mounted inside the container.
		 * @example /usr/share/nginx/html/
		 */
		Destination?: string;
		/**
		 * Driver is the volume driver used to create the volume (if it is a volume).
		 * @example local
		 */
		Driver?: string;
		/**
		 * Mode is a comma separated list of options supplied by the user when
		 * creating the bind/volume mount.
		 *
		 * The default is platform-specific (`"z"` on Linux, empty on Windows).
		 * @example z
		 */
		Mode?: string;
		/**
		 * Whether the mount is mounted writable (read-write).
		 * @example true
		 */
		RW?: boolean;
		/**
		 * Propagation describes how mounts are propagated from the host into the
		 * mount point, and vice-versa. Refer to the [Linux kernel
		 * documentation](https://www.kernel.org/doc/Documentation/filesystems/sharedsubtree.txt)
		 * for details. This field is not used on Windows.
		 * @example
		 */
		Propagation?: string;
	}[];
};
/** Driver represents a driver (network, logging, secrets). */
export type Driver = {
	/**
	 * Name of the driver.
	 * @example some-driver
	 */
	Name: string;
	Options?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
};
export type SecretSpec = {
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Base64-url-safe-encoded ([RFC
	 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
	 * data to store as secret.
	 *
	 * This field is only used to _create_ a secret, and is not returned by
	 * other endpoints.
	 * @example
	 */
	Data?: string;
	Driver?: {
		/**
		 * Name of the driver.
		 * @example some-driver
		 */
		Name: string;
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	Templating?: {
		/**
		 * Name of the driver.
		 * @example some-driver
		 */
		Name: string;
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
};
export type Secret = {
	ID?: string;
	Version?: {
		Index?: number;
	};
	CreatedAt?: string;
	UpdatedAt?: string;
	Spec?: {
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Base64-url-safe-encoded ([RFC
		 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
		 * data to store as secret.
		 *
		 * This field is only used to _create_ a secret, and is not returned by
		 * other endpoints.
		 * @example
		 */
		Data?: string;
		Driver?: {
			/**
			 * Name of the driver.
			 * @example some-driver
			 */
			Name: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
		Templating?: {
			/**
			 * Name of the driver.
			 * @example some-driver
			 */
			Name: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
};
export type ConfigSpec = {
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Data?: string;
	Templating?: {
		/**
		 * Name of the driver.
		 * @example some-driver
		 */
		Name: string;
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
};
export type Config = {
	ID?: string;
	Version?: {
		Index?: number;
	};
	CreatedAt?: string;
	UpdatedAt?: string;
	Spec?: {
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Data?: string;
		Templating?: {
			/**
			 * Name of the driver.
			 * @example some-driver
			 */
			Name: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
};
/**
 * ContainerState stores container's running state. It's part of
 * ContainerJSONBase
 * and will be returned by the "inspect" command.
 */
export type ContainerState = {
	/**
	 * String representation of the container state. Can be one of "created",
	 * "running", "paused", "restarting", "removing", "exited", or "dead".
	 * @enum created,running,paused,restarting,removing,exited,dead
	 * @example running
	 */
	Status?:
		| "created"
		| "running"
		| "paused"
		| "restarting"
		| "removing"
		| "exited"
		| "dead";
	/**
	 * Whether this container is running.
	 *
	 * Note that a running container can be _paused_. The `Running` and `Paused`
	 * booleans are not mutually exclusive:
	 *
	 * When pausing a container (on Linux), the freezer cgroup is used to suspend
	 * all processes in the container. Freezing the process requires the process
	 * to
	 * be running. As a result, paused containers are both `Running` _and_
	 * `Paused`.
	 *
	 * Use the `Status` field instead to determine if a container's state is
	 * "running".
	 * @example true
	 */
	Running?: boolean;
	/**
	 * Whether this container is paused.
	 * @example false
	 */
	Paused?: boolean;
	/**
	 * Whether this container is restarting.
	 * @example false
	 */
	Restarting?: boolean;
	/**
	 * Whether a process within this container has been killed because it ran
	 * out of memory since the container was last started.
	 * @example false
	 */
	OOMKilled?: boolean;
	Dead?: boolean;
	/**
	 * The process ID of this container
	 * @example 1234
	 */
	Pid?: number;
	/**
	 * The last exit code of this container
	 * @example 0
	 */
	ExitCode?: number;
	Error?: string;
	/**
	 * The time when this container was last started.
	 * @example 2020-01-06T09:06:59.461876391Z
	 */
	StartedAt?: string;
	/**
	 * The time when this container last exited.
	 * @example 2020-01-06T09:07:59.461876391Z
	 */
	FinishedAt?: string;
	Health?: {
		/**
		 * Status is one of `none`, `starting`, `healthy` or `unhealthy`
		 *
		 * - "none"      Indicates there is no healthcheck
		 * - "starting"  Starting indicates that the container is not yet ready
		 * - "healthy"   Healthy indicates that the container is running correctly
		 * - "unhealthy" Unhealthy indicates that the container has a problem
		 * @enum none,starting,healthy,unhealthy
		 * @example healthy
		 */
		Status?: "none" | "starting" | "healthy" | "unhealthy";
		/**
		 * FailingStreak is the number of consecutive failures
		 * @example 0
		 */
		FailingStreak?: number;
		Log?: readonly {
			/**
			 * Date and time at which this check started in
			 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
			 * @example 2020-01-04T10:44:24.496525531Z
			 */
			Start?: string;
			/**
			 * Date and time at which this check ended in
			 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
			 * @example 2020-01-04T10:45:21.364524523Z
			 */
			End?: string;
			/**
			 * ExitCode meanings:
			 *
			 * - `0` healthy
			 * - `1` unhealthy
			 * - `2` reserved (considered unhealthy)
			 * - other values: error running probe
			 * @example 0
			 */
			ExitCode?: number;
			Output?: string;
		}[];
	};
};
/** OK response to ContainerCreate operation */
export type ContainerCreateResponse = {
	/**
	 * The ID of the created container
	 * @example ede54ee1afda366ab42f824e8a5ffd195155d853ceaec74a927f249ea270c743
	 */
	Id: string;
	/**
	 * Warnings encountered when creating the container
	 * @example
	 */
	Warnings: readonly string[];
};
/** OK response to ContainerWait operation */
export type ContainerWaitResponse = {
	StatusCode: number;
	Error?: {
		Message?: string;
	};
};
/** container waiting error, if any */
export type ContainerWaitExitError = {
	Message?: string;
};
/** Response of Engine API: GET "/version" */
export type SystemVersion = {
	Platform?: {
		Name: string;
	};
	Components?: readonly {
		/**
		 * Name of the component
		 * @example Engine
		 */
		Name: string;
		/**
		 * Version of the component
		 * @example 19.03.12
		 */
		Version: string;
		Details?: {};
	}[];
	/**
	 * The version of the daemon
	 * @example 19.03.12
	 */
	Version?: string;
	/**
	 * The default (and highest) API version that is supported by the daemon
	 * @example 1.40
	 */
	ApiVersion?: string;
	/**
	 * The minimum API version that is supported by the daemon
	 * @example 1.12
	 */
	MinAPIVersion?: string;
	/**
	 * The Git commit of the source code that was used to build the daemon
	 * @example 48a66213fe
	 */
	GitCommit?: string;
	/**
	 * The version Go used to compile the daemon, and the version of the Go
	 * runtime in use.
	 * @example go1.13.14
	 */
	GoVersion?: string;
	/**
	 * The operating system that the daemon is running on ("linux" or "windows")
	 * @example linux
	 */
	Os?: string;
	/**
	 * The architecture that the daemon is running on
	 * @example amd64
	 */
	Arch?: string;
	/**
	 * The kernel version (`uname -r`) that the daemon is running on.
	 *
	 * This field is omitted when empty.
	 * @example 4.19.76-linuxkit
	 */
	KernelVersion?: string;
	/**
	 * Indicates if the daemon is started with experimental features enabled.
	 *
	 * This field is omitted when empty / false.
	 * @example true
	 */
	Experimental?: boolean;
	/**
	 * The date and time that the daemon was compiled.
	 * @example 2020-06-22T15:49:27.000000000+00:00
	 */
	BuildTime?: string;
};
export type SystemInfo = {
	/**
	 * Unique identifier of the daemon.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: The format of the ID itself is not part of the API, and
	 * > should not be considered stable.
	 * @example 7TRN:IPZB:QYBB:VPBQ:UMPP:KARE:6ZNR:XE6T:7EWV:PKF4:ZOJD:TPYS
	 */
	ID?: string;
	/**
	 * Total number of containers on the host.
	 * @example 14
	 */
	Containers?: number;
	/**
	 * Number of containers with status `"running"`.
	 * @example 3
	 */
	ContainersRunning?: number;
	/**
	 * Number of containers with status `"paused"`.
	 * @example 1
	 */
	ContainersPaused?: number;
	/**
	 * Number of containers with status `"stopped"`.
	 * @example 10
	 */
	ContainersStopped?: number;
	/**
	 * Total number of images on the host.
	 *
	 * Both _tagged_ and _untagged_ (dangling) images are counted.
	 * @example 508
	 */
	Images?: number;
	/**
	 * Name of the storage driver in use.
	 * @example overlay2
	 */
	Driver?: string;
	/**
	 * Information specific to the storage driver, provided as
	 * "label" / "value" pairs.
	 *
	 * This information is provided by the storage driver, and formatted
	 * in a way consistent with the output of `docker info` on the command
	 * line.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: The information returned in this field, including the
	 * > formatting of values and labels, should not be considered stable,
	 * > and may change without notice.
	 * @example Backing Filesystem,extfs,Supports d_type,true,Native Overlay Diff,true
	 */
	DriverStatus?: readonly (readonly string[])[];
	/**
	 * Root directory of persistent Docker state.
	 *
	 * Defaults to `/var/lib/docker` on Linux, and `C:\ProgramData\docker`
	 * on Windows.
	 * @example /var/lib/docker
	 */
	DockerRootDir?: string;
	Plugins?: {
		/**
		 * Names of available volume-drivers, and network-driver plugins.
		 * @example local
		 */
		Volume?: readonly string[];
		/**
		 * Names of available network-drivers, and network-driver plugins.
		 * @example bridge,host,ipvlan,macvlan,null,overlay
		 */
		Network?: readonly string[];
		/**
		 * Names of available authorization plugins.
		 * @example img-authz-plugin,hbm
		 */
		Authorization?: readonly string[];
		/**
		 * Names of available logging-drivers, and logging-driver plugins.
		 * @example awslogs,fluentd,gcplogs,gelf,journald,json-file,splunk,syslog
		 */
		Log?: readonly string[];
	};
	/**
	 * Indicates if the host has memory limit support enabled.
	 * @example true
	 */
	MemoryLimit?: boolean;
	/**
	 * Indicates if the host has memory swap limit support enabled.
	 * @example true
	 */
	SwapLimit?: boolean;
	/**
	 * Indicates if the host has kernel memory TCP limit support enabled. This
	 * field is omitted if not supported.
	 *
	 * Kernel memory TCP limits are not supported when using cgroups v2, which
	 * does not support the corresponding `memory.kmem.tcp.limit_in_bytes` cgroup.
	 * @example true
	 */
	KernelMemoryTCP?: boolean;
	/**
	 * Indicates if CPU CFS(Completely Fair Scheduler) period is supported by
	 * the host.
	 * @example true
	 */
	CpuCfsPeriod?: boolean;
	/**
	 * Indicates if CPU CFS(Completely Fair Scheduler) quota is supported by
	 * the host.
	 * @example true
	 */
	CpuCfsQuota?: boolean;
	/**
	 * Indicates if CPU Shares limiting is supported by the host.
	 * @example true
	 */
	CPUShares?: boolean;
	/**
	 * Indicates if CPUsets (cpuset.cpus, cpuset.mems) are supported by the host.
	 *
	 * See
	 * [cpuset(7)](https://www.kernel.org/doc/Documentation/cgroup-v1/cpusets.txt)
	 * @example true
	 */
	CPUSet?: boolean;
	/**
	 * Indicates if the host kernel has PID limit support enabled.
	 * @example true
	 */
	PidsLimit?: boolean;
	OomKillDisable?: boolean;
	/**
	 * Indicates IPv4 forwarding is enabled.
	 * @example true
	 */
	IPv4Forwarding?: boolean;
	/**
	 * Indicates if `bridge-nf-call-iptables` is available on the host.
	 * @example true
	 */
	BridgeNfIptables?: boolean;
	/**
	 * Indicates if `bridge-nf-call-ip6tables` is available on the host.
	 * @example true
	 */
	BridgeNfIp6tables?: boolean;
	/**
	 * Indicates if the daemon is running in debug-mode / with debug-level
	 * logging enabled.
	 * @example true
	 */
	Debug?: boolean;
	/**
	 * The total number of file Descriptors in use by the daemon process.
	 *
	 * This information is only returned if debug-mode is enabled.
	 * @example 64
	 */
	NFd?: number;
	/**
	 * The  number of goroutines that currently exist.
	 *
	 * This information is only returned if debug-mode is enabled.
	 * @example 174
	 */
	NGoroutines?: number;
	/**
	 * Current system-time in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt)
	 * format with nano-seconds.
	 * @example 2017-08-08T20:28:29.06202363Z
	 */
	SystemTime?: string;
	LoggingDriver?: string;
	/**
	 * The driver to use for managing cgroups.
	 * @default cgroupfs
	 * @enum cgroupfs,systemd,none
	 * @example cgroupfs
	 */
	CgroupDriver?: "cgroupfs" | "systemd" | "none";
	/**
	 * The version of the cgroup.
	 * @default 1
	 * @enum 1,2
	 * @example 1
	 */
	CgroupVersion?: "1" | "2";
	/**
	 * Number of event listeners subscribed.
	 * @example 30
	 */
	NEventsListener?: number;
	/**
	 * Kernel version of the host.
	 *
	 * On Linux, this information obtained from `uname`. On Windows this
	 * information is queried from the
	 * <kbd>HKEY_LOCAL_MACHINE\\SOFTWARE\\Microsoft\\Windows
	 * NT\\CurrentVersion\\</kbd>
	 * registry value, for example _"10.0 14393
	 * (14393.1198.amd64fre.rs1_release_sec.170427-1353)"_.
	 * @example 4.9.38-moby
	 */
	KernelVersion?: string;
	/**
	 * Name of the host's operating system, for example: "Ubuntu 16.04.2 LTS"
	 * or "Windows Server 2016 Datacenter"
	 * @example Alpine Linux v3.5
	 */
	OperatingSystem?: string;
	/**
	 * Version of the host's operating system
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: The information returned in this field, including its
	 * > very existence, and the formatting of values, should not be considered
	 * > stable, and may change without notice.
	 * @example 16.04
	 */
	OSVersion?: string;
	/**
	 * Generic type of the operating system of the host, as returned by the
	 * Go runtime (`GOOS`).
	 *
	 * Currently returned values are "linux" and "windows". A full list of
	 * possible values can be found in the [Go
	 * documentation](https://go.dev/doc/install/source#environment).
	 * @example linux
	 */
	OSType?: string;
	/**
	 * Hardware architecture of the host, as returned by the Go runtime
	 * (`GOARCH`).
	 *
	 * A full list of possible values can be found in the [Go
	 * documentation](https://go.dev/doc/install/source#environment).
	 * @example x86_64
	 */
	Architecture?: string;
	/**
	 * The number of logical CPUs usable by the daemon.
	 *
	 * The number of available CPUs is checked by querying the operating
	 * system when the daemon starts. Changes to operating system CPU
	 * allocation after the daemon is started are not reflected.
	 * @example 4
	 */
	NCPU?: number;
	/**
	 * Total amount of physical memory available on the host, in bytes.
	 * @example 2095882240
	 */
	MemTotal?: number;
	/**
	 * Address / URL of the index server that is used for image search,
	 * and as a default for user authentication for Docker Hub and Docker Cloud.
	 * @default https://index.docker.io/v1/
	 * @example https://index.docker.io/v1/
	 */
	IndexServerAddress?: string;
	RegistryConfig?: {
		/**
		 * List of IP ranges to which nondistributable artifacts can be pushed,
		 * using the CIDR syntax [RFC 4632](https://tools.ietf.org/html/4632).
		 *
		 * Some images (for example, Windows base images) contain artifacts
		 * whose distribution is restricted by license. When these images are
		 * pushed to a registry, restricted artifacts are not included.
		 *
		 * This configuration override this behavior, and enables the daemon to
		 * push nondistributable artifacts to all registries whose resolved IP
		 * address is within the subnet described by the CIDR syntax.
		 *
		 * This option is useful when pushing images containing
		 * nondistributable artifacts to a registry on an air-gapped network so
		 * hosts on that network can pull the images without connecting to
		 * another server.
		 *
		 * > **Warning**: Nondistributable artifacts typically have restrictions
		 * > on how and where they can be distributed and shared. Only use this
		 * > feature to push artifacts to private registries and ensure that you
		 * > are in compliance with any terms that cover redistributing
		 * > nondistributable artifacts.
		 * @example ::1/128,127.0.0.0/8
		 */
		AllowNondistributableArtifactsCIDRs?: readonly string[];
		/**
		 * List of registry hostnames to which nondistributable artifacts can be
		 * pushed, using the format `<hostname>[:<port>]` or `<IP address>[:<port>]`.
		 *
		 * Some images (for example, Windows base images) contain artifacts
		 * whose distribution is restricted by license. When these images are
		 * pushed to a registry, restricted artifacts are not included.
		 *
		 * This configuration override this behavior for the specified
		 * registries.
		 *
		 * This option is useful when pushing images containing
		 * nondistributable artifacts to a registry on an air-gapped network so
		 * hosts on that network can pull the images without connecting to
		 * another server.
		 *
		 * > **Warning**: Nondistributable artifacts typically have restrictions
		 * > on how and where they can be distributed and shared. Only use this
		 * > feature to push artifacts to private registries and ensure that you
		 * > are in compliance with any terms that cover redistributing
		 * > nondistributable artifacts.
		 * @example registry.internal.corp.example.com:3000,[2001:db8:a0b:12f0::1]:443
		 */
		AllowNondistributableArtifactsHostnames?: readonly string[];
		/**
		 * List of IP ranges of insecure registries, using the CIDR syntax
		 * ([RFC 4632](https://tools.ietf.org/html/4632)). Insecure registries
		 * accept un-encrypted (HTTP) and/or untrusted (HTTPS with certificates
		 * from unknown CAs) communication.
		 *
		 * By default, local registries (`127.0.0.0/8`) are configured as
		 * insecure. All other registries are secure. Communicating with an
		 * insecure registry is not possible if the daemon assumes that registry
		 * is secure.
		 *
		 * This configuration override this behavior, insecure communication with
		 * registries whose resolved IP address is within the subnet described by
		 * the CIDR syntax.
		 *
		 * Registries can also be marked insecure by hostname. Those registries
		 * are listed under `IndexConfigs` and have their `Secure` field set to
		 * `false`.
		 *
		 * > **Warning**: Using this option can be useful when running a local
		 * > registry, but introduces security vulnerabilities. This option
		 * > should therefore ONLY be used for testing purposes. For increased
		 * > security, users should add their CA to their system's list of trusted
		 * > CAs instead of enabling this option.
		 * @example ::1/128,127.0.0.0/8
		 */
		InsecureRegistryCIDRs?: readonly string[];
		IndexConfigs?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * List of registry URLs that act as a mirror for the official
		 * (`docker.io`) registry.
		 * @example https://hub-mirror.corp.example.com:5000/,https://[2001:db8:a0b:12f0::1]/
		 */
		Mirrors?: readonly string[];
	};
	/**
	 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
	 * String resources (e.g, `GPU=UUID1`).
	 * @example [object Object],[object Object],[object Object]
	 */
	GenericResources?: readonly {
		NamedResourceSpec?: {
			Kind?: string;
			Value?: string;
		};
		DiscreteResourceSpec?: {
			Kind?: string;
			Value?: number;
		};
	}[];
	/**
	 * HTTP-proxy configured for the daemon. This value is obtained from the
	 * [`HTTP_PROXY`](https://www.gnu.org/software/wget/manual/html_node/Proxies.html)
	 * environment variable.
	 * Credentials ([user info
	 * component](https://tools.ietf.org/html/rfc3986#section-3.2.1)) in the proxy
	 * URL
	 * are masked in the API response.
	 *
	 * Containers do not automatically inherit this configuration.
	 * @example http://xxxxx:xxxxx@proxy.corp.example.com:8080
	 */
	HttpProxy?: string;
	/**
	 * HTTPS-proxy configured for the daemon. This value is obtained from the
	 * [`HTTPS_PROXY`](https://www.gnu.org/software/wget/manual/html_node/Proxies.html)
	 * environment variable.
	 * Credentials ([user info
	 * component](https://tools.ietf.org/html/rfc3986#section-3.2.1)) in the proxy
	 * URL
	 * are masked in the API response.
	 *
	 * Containers do not automatically inherit this configuration.
	 * @example https://xxxxx:xxxxx@proxy.corp.example.com:4443
	 */
	HttpsProxy?: string;
	/**
	 * Comma-separated list of domain extensions for which no proxy should be
	 * used. This value is obtained from the
	 * [`NO_PROXY`](https://www.gnu.org/software/wget/manual/html_node/Proxies.html)
	 * environment variable.
	 *
	 * Containers do not automatically inherit this configuration.
	 * @example *.local, 169.254/16
	 */
	NoProxy?: string;
	/**
	 * Hostname of the host.
	 * @example node5.corp.example.com
	 */
	Name?: string;
	/**
	 * User-defined labels (key/value metadata) as set on the daemon.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: When part of a Swarm, nodes can both have _daemon_ labels,
	 * > set through the daemon configuration, and _node_ labels, set from a
	 * > manager node in the Swarm. Node labels are not included in this
	 * > field. Node labels can be retrieved using the `/nodes/(id)` endpoint
	 * > on a manager node in the Swarm.
	 * @example storage=ssd,production
	 */
	Labels?: readonly string[];
	/**
	 * Indicates if experimental features are enabled on the daemon.
	 * @example true
	 */
	ExperimentalBuild?: boolean;
	/**
	 * Version string of the daemon.
	 * @example 24.0.2
	 */
	ServerVersion?: string;
	Runtimes?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Name of the default OCI runtime that is used when starting containers.
	 *
	 * The default can be overridden per-container at create time.
	 * @default runc
	 * @example runc
	 */
	DefaultRuntime?: string;
	Swarm?: {
		/**
		 * Unique identifier of for this node in the swarm.
		 * @example k67qz4598weg5unwwffg6z1m1
		 */
		NodeID?: string;
		/**
		 * IP address at which this node can be reached by other nodes in the
		 * swarm.
		 * @example 10.0.0.46
		 */
		NodeAddr?: string;
		/**
		 * Current local status of this node.
		 * @enum ,inactive,pending,active,error,locked
		 * @example active
		 */
		LocalNodeState?:
			| ""
			| "inactive"
			| "pending"
			| "active"
			| "error"
			| "locked";
		ControlAvailable?: boolean;
		Error?: string;
		/**
		 * List of ID's and addresses of other managers in the swarm.
		 * @example [object Object],[object Object],[object Object]
		 */
		RemoteManagers?: readonly {
			NodeID?: string;
			Addr?: string;
		}[];
		/**
		 * Total number of nodes in the swarm.
		 * @example 4
		 */
		Nodes?: number | null;
		/**
		 * Total number of managers in the swarm.
		 * @example 3
		 */
		Managers?: number | null;
		Cluster?: {
			/**
			 * The ID of the swarm.
			 * @example abajmipo7b4xz5ip2nrla6b11
			 */
			ID?: string;
			Version?: {
				Index?: number;
			};
			/**
			 * Date and time at which the swarm was initialised in
			 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
			 * @example 2016-08-18T10:44:24.496525531Z
			 */
			CreatedAt?: string;
			/**
			 * Date and time at which the swarm was last updated in
			 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
			 * @example 2017-08-09T07:09:37.632105588Z
			 */
			UpdatedAt?: string;
			Spec?: {
				/**
				 * Name of the swarm.
				 * @example default
				 */
				Name?: string;
				Labels?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				Orchestration?: {
					/**
					 * The number of historic tasks to keep per instance or node. If
					 * negative, never remove completed or failed tasks.
					 * @example 10
					 */
					TaskHistoryRetentionLimit?: number;
				};
				Raft?: {
					/**
					 * The number of log entries between snapshots.
					 * @example 10000
					 */
					SnapshotInterval?: number;
					KeepOldSnapshots?: number;
					/**
					 * The number of log entries to keep around to sync up slow followers
					 * after a snapshot is created.
					 * @example 500
					 */
					LogEntriesForSlowFollowers?: number;
					/**
					 * The number of ticks that a follower will wait for a message from
					 * the leader before becoming a candidate and starting an election.
					 * `ElectionTick` must be greater than `HeartbeatTick`.
					 *
					 * A tick currently defaults to one second, so these translate
					 * directly to seconds currently, but this is NOT guaranteed.
					 * @example 3
					 */
					ElectionTick?: number;
					/**
					 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
					 * the leader will send a heartbeat to the followers.
					 *
					 * A tick currently defaults to one second, so these translate
					 * directly to seconds currently, but this is NOT guaranteed.
					 * @example 1
					 */
					HeartbeatTick?: number;
				};
				Dispatcher?: {
					/**
					 * The delay for an agent to send a heartbeat to the dispatcher.
					 * @example 5000000000
					 */
					HeartbeatPeriod?: number;
				};
				CAConfig?: {
					/**
					 * The duration node certificates are issued for.
					 * @example 7776000000000000
					 */
					NodeCertExpiry?: number;
					ExternalCAs?: readonly {
						/**
						 * Protocol for communication with the external CA (currently
						 * only `cfssl` is supported).
						 * @default cfssl
						 * @enum cfssl
						 */
						Protocol?: "cfssl";
						URL?: string;
						Options?: Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>;
						CACert?: string;
					}[];
					SigningCACert?: string;
					SigningCAKey?: string;
					ForceRotate?: number;
				};
				EncryptionConfig?: {
					/**
					 * If set, generate a key and use it to lock data stored on the
					 * managers.
					 * @example false
					 */
					AutoLockManagers?: boolean;
				};
				TaskDefaults?: {
					LogDriver?: {
						/**
						 * The log driver to use as a default for new tasks.
						 * @example json-file
						 */
						Name?: string;
						Options?: Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>;
					};
				};
			};
			/**
			 * Information about the issuer of leaf TLS certificates and the trusted root
			 * CA certificate.
			 * @example [object Object]
			 */
			TLSInfo?: {
				TrustRoot?: string;
				CertIssuerSubject?: string;
				CertIssuerPublicKey?: string;
			};
			/**
			 * Whether there is currently a root CA rotation in progress for the swarm
			 * @example false
			 */
			RootRotationInProgress?: boolean;
			/**
			 * DataPathPort specifies the data path port number for data traffic.
			 * Acceptable port range is 1024 to 49151.
			 * If no port is set or is set to 0, the default port (4789) is used.
			 * @example 4789
			 */
			DataPathPort?: number;
			DefaultAddrPool?: readonly string[];
			/**
			 * SubnetSize specifies the subnet size of the networks created from the
			 * default subnet pool.
			 * @example 24
			 */
			SubnetSize?: number;
		};
	};
	/**
	 * Indicates if live restore is enabled.
	 *
	 * If enabled, containers are kept running when the daemon is shutdown
	 * or upon daemon start if running containers are detected.
	 * @example false
	 */
	LiveRestoreEnabled?: boolean;
	/**
	 * Represents the isolation technology to use as a default for containers.
	 * The supported values are platform-specific.
	 *
	 * If no isolation value is specified on daemon start, on Windows client,
	 * the default is `hyperv`, and on Windows server, the default is `process`.
	 *
	 * This option is currently not used on other platforms.
	 * @default default
	 * @enum default,hyperv,process,
	 */
	Isolation?: "default" | "hyperv" | "process" | "";
	/**
	 * Name and, optional, path of the `docker-init` binary.
	 *
	 * If the path is omitted, the daemon searches the host's `$PATH` for the
	 * binary and uses the first result.
	 * @example docker-init
	 */
	InitBinary?: string;
	ContainerdCommit?: {
		/**
		 * Actual commit ID of external tool.
		 * @example cfb82a876ecc11b5ca0977d1733adbe58599088a
		 */
		ID?: string;
		/**
		 * Commit ID of external tool expected by dockerd as set at build time.
		 * @example 2d41c047c83e09a6d61d464906feb2a2f3c52aa4
		 */
		Expected?: string;
	};
	RuncCommit?: {
		/**
		 * Actual commit ID of external tool.
		 * @example cfb82a876ecc11b5ca0977d1733adbe58599088a
		 */
		ID?: string;
		/**
		 * Commit ID of external tool expected by dockerd as set at build time.
		 * @example 2d41c047c83e09a6d61d464906feb2a2f3c52aa4
		 */
		Expected?: string;
	};
	InitCommit?: {
		/**
		 * Actual commit ID of external tool.
		 * @example cfb82a876ecc11b5ca0977d1733adbe58599088a
		 */
		ID?: string;
		/**
		 * Commit ID of external tool expected by dockerd as set at build time.
		 * @example 2d41c047c83e09a6d61d464906feb2a2f3c52aa4
		 */
		Expected?: string;
	};
	/**
	 * List of security features that are enabled on the daemon, such as
	 * apparmor, seccomp, SELinux, user-namespaces (userns), rootless and
	 * no-new-privileges.
	 *
	 * Additional configuration options for each security feature may
	 * be present, and are included as a comma-separated list of key/value
	 * pairs.
	 * @example name=apparmor,name=seccomp,profile=default,name=selinux,name=userns,name=rootless
	 */
	SecurityOptions?: readonly string[];
	/**
	 * Reports a summary of the product license on the daemon.
	 *
	 * If a commercial license has been applied to the daemon, information
	 * such as number of nodes, and expiration are included.
	 * @example Community Engine
	 */
	ProductLicense?: string;
	DefaultAddressPools?: readonly {
		/**
		 * The network address in CIDR format
		 * @example 10.10.0.0/16
		 */
		Base?: string;
		/**
		 * The network pool size
		 * @example 24
		 */
		Size?: number;
	}[];
	/**
	 * List of warnings / informational messages about missing features, or
	 * issues related to the daemon configuration.
	 *
	 * These messages can be printed by the client as information to the user.
	 * @example WARNING: No memory limit support
	 */
	Warnings?: readonly string[];
};
/**
 * Available plugins per type.
 *
 * <p><br /></p>
 *
 * > **Note**: Only unmanaged (V1) plugins are included in this list.
 * > V1 plugins are "lazily" loaded, and are not returned in this list
 * > if there is no resource using the plugin.
 */
export type PluginsInfo = {
	/**
	 * Names of available volume-drivers, and network-driver plugins.
	 * @example local
	 */
	Volume?: readonly string[];
	/**
	 * Names of available network-drivers, and network-driver plugins.
	 * @example bridge,host,ipvlan,macvlan,null,overlay
	 */
	Network?: readonly string[];
	/**
	 * Names of available authorization plugins.
	 * @example img-authz-plugin,hbm
	 */
	Authorization?: readonly string[];
	/**
	 * Names of available logging-drivers, and logging-driver plugins.
	 * @example awslogs,fluentd,gcplogs,gelf,journald,json-file,splunk,syslog
	 */
	Log?: readonly string[];
};
/** RegistryServiceConfig stores daemon registry services configuration. */
export type RegistryServiceConfig = {
	/**
	 * List of IP ranges to which nondistributable artifacts can be pushed,
	 * using the CIDR syntax [RFC 4632](https://tools.ietf.org/html/4632).
	 *
	 * Some images (for example, Windows base images) contain artifacts
	 * whose distribution is restricted by license. When these images are
	 * pushed to a registry, restricted artifacts are not included.
	 *
	 * This configuration override this behavior, and enables the daemon to
	 * push nondistributable artifacts to all registries whose resolved IP
	 * address is within the subnet described by the CIDR syntax.
	 *
	 * This option is useful when pushing images containing
	 * nondistributable artifacts to a registry on an air-gapped network so
	 * hosts on that network can pull the images without connecting to
	 * another server.
	 *
	 * > **Warning**: Nondistributable artifacts typically have restrictions
	 * > on how and where they can be distributed and shared. Only use this
	 * > feature to push artifacts to private registries and ensure that you
	 * > are in compliance with any terms that cover redistributing
	 * > nondistributable artifacts.
	 * @example ::1/128,127.0.0.0/8
	 */
	AllowNondistributableArtifactsCIDRs?: readonly string[];
	/**
	 * List of registry hostnames to which nondistributable artifacts can be
	 * pushed, using the format `<hostname>[:<port>]` or `<IP address>[:<port>]`.
	 *
	 * Some images (for example, Windows base images) contain artifacts
	 * whose distribution is restricted by license. When these images are
	 * pushed to a registry, restricted artifacts are not included.
	 *
	 * This configuration override this behavior for the specified
	 * registries.
	 *
	 * This option is useful when pushing images containing
	 * nondistributable artifacts to a registry on an air-gapped network so
	 * hosts on that network can pull the images without connecting to
	 * another server.
	 *
	 * > **Warning**: Nondistributable artifacts typically have restrictions
	 * > on how and where they can be distributed and shared. Only use this
	 * > feature to push artifacts to private registries and ensure that you
	 * > are in compliance with any terms that cover redistributing
	 * > nondistributable artifacts.
	 * @example registry.internal.corp.example.com:3000,[2001:db8:a0b:12f0::1]:443
	 */
	AllowNondistributableArtifactsHostnames?: readonly string[];
	/**
	 * List of IP ranges of insecure registries, using the CIDR syntax
	 * ([RFC 4632](https://tools.ietf.org/html/4632)). Insecure registries
	 * accept un-encrypted (HTTP) and/or untrusted (HTTPS with certificates
	 * from unknown CAs) communication.
	 *
	 * By default, local registries (`127.0.0.0/8`) are configured as
	 * insecure. All other registries are secure. Communicating with an
	 * insecure registry is not possible if the daemon assumes that registry
	 * is secure.
	 *
	 * This configuration override this behavior, insecure communication with
	 * registries whose resolved IP address is within the subnet described by
	 * the CIDR syntax.
	 *
	 * Registries can also be marked insecure by hostname. Those registries
	 * are listed under `IndexConfigs` and have their `Secure` field set to
	 * `false`.
	 *
	 * > **Warning**: Using this option can be useful when running a local
	 * > registry, but introduces security vulnerabilities. This option
	 * > should therefore ONLY be used for testing purposes. For increased
	 * > security, users should add their CA to their system's list of trusted
	 * > CAs instead of enabling this option.
	 * @example ::1/128,127.0.0.0/8
	 */
	InsecureRegistryCIDRs?: readonly string[];
	IndexConfigs?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * List of registry URLs that act as a mirror for the official
	 * (`docker.io`) registry.
	 * @example https://hub-mirror.corp.example.com:5000/,https://[2001:db8:a0b:12f0::1]/
	 */
	Mirrors?: readonly string[];
};
/** IndexInfo contains information about a registry. */
export type IndexInfo = {
	/**
	 * Name of the registry, such as "docker.io".
	 * @example docker.io
	 */
	Name?: string;
	/**
	 * List of mirrors, expressed as URIs.
	 * @example https://hub-mirror.corp.example.com:5000/,https://registry-2.docker.io/,https://registry-3.docker.io/
	 */
	Mirrors?: readonly string[];
	/**
	 * Indicates if the registry is part of the list of insecure
	 * registries.
	 *
	 * If `false`, the registry is insecure. Insecure registries accept
	 * un-encrypted (HTTP) and/or untrusted (HTTPS with certificates from
	 * unknown CAs) communication.
	 *
	 * > **Warning**: Insecure registries can be useful when running a local
	 * > registry. However, because its use creates security vulnerabilities
	 * > it should ONLY be enabled for testing purposes. For increased
	 * > security, users should add their CA to their system's list of
	 * > trusted CAs instead of enabling this option.
	 * @example true
	 */
	Secure?: boolean;
	/**
	 * Indicates whether this is an official registry (i.e., Docker Hub /
	 * docker.io)
	 * @example true
	 */
	Official?: boolean;
};
/**
 * Runtime describes an [OCI
 * compliant](https://github.com/opencontainers/runtime-spec)
 * runtime.
 *
 * The runtime is invoked by the daemon via the `containerd` daemon. OCI
 * runtimes act as an interface to the Linux kernel namespaces, cgroups,
 * and SELinux.
 */
export type Runtime = {
	/**
	 * Name and, optional, path, of the OCI executable binary.
	 *
	 * If the path is omitted, the daemon searches the host's `$PATH` for the
	 * binary and uses the first result.
	 * @example /usr/local/bin/my-oci-runtime
	 */
	path?: string;
	/**
	 * List of command-line arguments to pass to the runtime when invoked.
	 * @example --debug,--systemd-cgroup=false
	 */
	runtimeArgs?: readonly string[];
};
/**
 * Commit holds the Git-commit (SHA1) that a binary was built from, as
 * reported in the version-string of external tools, such as `containerd`,
 * or `runC`.
 */
export type Commit = {
	/**
	 * Actual commit ID of external tool.
	 * @example cfb82a876ecc11b5ca0977d1733adbe58599088a
	 */
	ID?: string;
	/**
	 * Commit ID of external tool expected by dockerd as set at build time.
	 * @example 2d41c047c83e09a6d61d464906feb2a2f3c52aa4
	 */
	Expected?: string;
};
/** Represents generic information about swarm. */
export type SwarmInfo = {
	/**
	 * Unique identifier of for this node in the swarm.
	 * @example k67qz4598weg5unwwffg6z1m1
	 */
	NodeID?: string;
	/**
	 * IP address at which this node can be reached by other nodes in the
	 * swarm.
	 * @example 10.0.0.46
	 */
	NodeAddr?: string;
	/**
	 * Current local status of this node.
	 * @enum ,inactive,pending,active,error,locked
	 * @example active
	 */
	LocalNodeState?: "" | "inactive" | "pending" | "active" | "error" | "locked";
	ControlAvailable?: boolean;
	Error?: string;
	/**
	 * List of ID's and addresses of other managers in the swarm.
	 * @example [object Object],[object Object],[object Object]
	 */
	RemoteManagers?: readonly {
		NodeID?: string;
		Addr?: string;
	}[];
	/**
	 * Total number of nodes in the swarm.
	 * @example 4
	 */
	Nodes?: number | null;
	/**
	 * Total number of managers in the swarm.
	 * @example 3
	 */
	Managers?: number | null;
	Cluster?: {
		/**
		 * The ID of the swarm.
		 * @example abajmipo7b4xz5ip2nrla6b11
		 */
		ID?: string;
		Version?: {
			Index?: number;
		};
		/**
		 * Date and time at which the swarm was initialised in
		 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
		 * @example 2016-08-18T10:44:24.496525531Z
		 */
		CreatedAt?: string;
		/**
		 * Date and time at which the swarm was last updated in
		 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
		 * @example 2017-08-09T07:09:37.632105588Z
		 */
		UpdatedAt?: string;
		Spec?: {
			/**
			 * Name of the swarm.
			 * @example default
			 */
			Name?: string;
			Labels?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			Orchestration?: {
				/**
				 * The number of historic tasks to keep per instance or node. If
				 * negative, never remove completed or failed tasks.
				 * @example 10
				 */
				TaskHistoryRetentionLimit?: number;
			};
			Raft?: {
				/**
				 * The number of log entries between snapshots.
				 * @example 10000
				 */
				SnapshotInterval?: number;
				KeepOldSnapshots?: number;
				/**
				 * The number of log entries to keep around to sync up slow followers
				 * after a snapshot is created.
				 * @example 500
				 */
				LogEntriesForSlowFollowers?: number;
				/**
				 * The number of ticks that a follower will wait for a message from
				 * the leader before becoming a candidate and starting an election.
				 * `ElectionTick` must be greater than `HeartbeatTick`.
				 *
				 * A tick currently defaults to one second, so these translate
				 * directly to seconds currently, but this is NOT guaranteed.
				 * @example 3
				 */
				ElectionTick?: number;
				/**
				 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
				 * the leader will send a heartbeat to the followers.
				 *
				 * A tick currently defaults to one second, so these translate
				 * directly to seconds currently, but this is NOT guaranteed.
				 * @example 1
				 */
				HeartbeatTick?: number;
			};
			Dispatcher?: {
				/**
				 * The delay for an agent to send a heartbeat to the dispatcher.
				 * @example 5000000000
				 */
				HeartbeatPeriod?: number;
			};
			CAConfig?: {
				/**
				 * The duration node certificates are issued for.
				 * @example 7776000000000000
				 */
				NodeCertExpiry?: number;
				ExternalCAs?: readonly {
					/**
					 * Protocol for communication with the external CA (currently
					 * only `cfssl` is supported).
					 * @default cfssl
					 * @enum cfssl
					 */
					Protocol?: "cfssl";
					URL?: string;
					Options?: Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>;
					CACert?: string;
				}[];
				SigningCACert?: string;
				SigningCAKey?: string;
				ForceRotate?: number;
			};
			EncryptionConfig?: {
				/**
				 * If set, generate a key and use it to lock data stored on the
				 * managers.
				 * @example false
				 */
				AutoLockManagers?: boolean;
			};
			TaskDefaults?: {
				LogDriver?: {
					/**
					 * The log driver to use as a default for new tasks.
					 * @example json-file
					 */
					Name?: string;
					Options?: Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>;
				};
			};
		};
		/**
		 * Information about the issuer of leaf TLS certificates and the trusted root
		 * CA certificate.
		 * @example [object Object]
		 */
		TLSInfo?: {
			TrustRoot?: string;
			CertIssuerSubject?: string;
			CertIssuerPublicKey?: string;
		};
		/**
		 * Whether there is currently a root CA rotation in progress for the swarm
		 * @example false
		 */
		RootRotationInProgress?: boolean;
		/**
		 * DataPathPort specifies the data path port number for data traffic.
		 * Acceptable port range is 1024 to 49151.
		 * If no port is set or is set to 0, the default port (4789) is used.
		 * @example 4789
		 */
		DataPathPort?: number;
		DefaultAddrPool?: readonly string[];
		/**
		 * SubnetSize specifies the subnet size of the networks created from the
		 * default subnet pool.
		 * @example 24
		 */
		SubnetSize?: number;
	};
};
/** Current local status of this node. */
export type LocalNodeState =
	| ""
	| "inactive"
	| "pending"
	| "active"
	| "error"
	| "locked";
/** Represents a peer-node in the swarm */
export type PeerNode = {
	NodeID?: string;
	Addr?: string;
};
/** Specifies how a service should be attached to a particular network. */
export type NetworkAttachmentConfig = {
	Target?: string;
	Aliases?: readonly string[];
	DriverOpts?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
};
/**
 * Actor describes something that generates events, like a container, network,
 * or a volume.
 */
export type EventActor = {
	/**
	 * The ID of the object emitting the event
	 * @example ede54ee1afda366ab42f824e8a5ffd195155d853ceaec74a927f249ea270c743
	 */
	ID?: string;
	Attributes?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
};
/** EventMessage represents the information an event contains. */
export type EventMessage = {
	/**
	 * The type of object emitting the event
	 * @enum builder,config,container,daemon,image,network,node,plugin,secret,service,volume
	 * @example container
	 */
	Type?:
		| "builder"
		| "config"
		| "container"
		| "daemon"
		| "image"
		| "network"
		| "node"
		| "plugin"
		| "secret"
		| "service"
		| "volume";
	/**
	 * The type of event
	 * @example create
	 */
	Action?: string;
	Actor?: {
		/**
		 * The ID of the object emitting the event
		 * @example ede54ee1afda366ab42f824e8a5ffd195155d853ceaec74a927f249ea270c743
		 */
		ID?: string;
		Attributes?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	/**
	 * Scope of the event. Engine events are `local` scope. Cluster (Swarm)
	 * events are `swarm` scope.
	 * @enum local,swarm
	 */
	scope?: "local" | "swarm";
	/**
	 * Timestamp of event
	 * @example 1629574695
	 */
	time?: number;
	/**
	 * Timestamp of event, with nanosecond accuracy
	 * @example 1629574695515050000
	 */
	timeNano?: number;
};
/**
 * A descriptor struct containing digest, media type, and size, as defined in
 * the [OCI Content Descriptors
 * Specification](https://github.com/opencontainers/image-spec/blob/v1.0.1/descriptor.md).
 */
export type OciDescriptor = {
	/**
	 * The media type of the object this schema refers to.
	 * @example application/vnd.docker.distribution.manifest.v2+json
	 */
	mediaType?: string;
	/**
	 * The digest of the targeted content.
	 * @example sha256:c0537ff6a5218ef531ece93d4984efc99bbf3f7497c0a7726c88e2bb7584dc96
	 */
	digest?: string;
	/**
	 * The size in bytes of the blob.
	 * @example 3987495
	 */
	size?: number;
};
/**
 * Describes the platform which the image in the manifest runs on, as defined
 * in the [OCI Image Index
 * Specification](https://github.com/opencontainers/image-spec/blob/v1.0.1/image-index.md).
 */
export type OciPlatform = {
	/**
	 * The CPU architecture, for example `amd64` or `ppc64`.
	 * @example arm
	 */
	architecture?: string;
	/**
	 * The operating system, for example `linux` or `windows`.
	 * @example windows
	 */
	os?: string;
	/**
	 * Optional field specifying the operating system version, for example on
	 * Windows `10.0.19041.1165`.
	 * @example 10.0.19041.1165
	 */
	"os.version"?: string;
	/**
	 * Optional field specifying an array of strings, each listing a required
	 * OS feature (for example on Windows `win32k`).
	 * @example win32k
	 */
	"os.features"?: readonly string[];
	/**
	 * Optional field specifying a variant of the CPU, for example `v7` to
	 * specify ARMv7 when architecture is `arm`.
	 * @example v7
	 */
	variant?: string;
};
/**
 * Describes the result obtained from contacting the registry to retrieve
 * image metadata.
 */
export type DistributionInspect = {
	Descriptor: {
		/**
		 * The media type of the object this schema refers to.
		 * @example application/vnd.docker.distribution.manifest.v2+json
		 */
		mediaType?: string;
		/**
		 * The digest of the targeted content.
		 * @example sha256:c0537ff6a5218ef531ece93d4984efc99bbf3f7497c0a7726c88e2bb7584dc96
		 */
		digest?: string;
		/**
		 * The size in bytes of the blob.
		 * @example 3987495
		 */
		size?: number;
	};
	Platforms: readonly {
		/**
		 * The CPU architecture, for example `amd64` or `ppc64`.
		 * @example arm
		 */
		architecture?: string;
		/**
		 * The operating system, for example `linux` or `windows`.
		 * @example windows
		 */
		os?: string;
		/**
		 * Optional field specifying the operating system version, for example on
		 * Windows `10.0.19041.1165`.
		 * @example 10.0.19041.1165
		 */
		"os.version"?: string;
		/**
		 * Optional field specifying an array of strings, each listing a required
		 * OS feature (for example on Windows `win32k`).
		 * @example win32k
		 */
		"os.features"?: readonly string[];
		/**
		 * Optional field specifying a variant of the CPU, for example `v7` to
		 * specify ARMv7 when architecture is `arm`.
		 * @example v7
		 */
		variant?: string;
	}[];
};
/**
 * Options and information specific to, and only present on, Swarm CSI
 * cluster volumes.
 */
export type ClusterVolume = {
	ID?: string;
	Version?: {
		Index?: number;
	};
	CreatedAt?: string;
	UpdatedAt?: string;
	Spec?: {
		Group?: string;
		AccessMode?: {
			/**
			 * The set of nodes this volume can be used on at one time.
			 * - `single` The volume may only be scheduled to one node at a time.
			 * - `multi` the volume may be scheduled to any supported number of nodes at a
			 * time.
			 * @default single
			 * @enum single,multi
			 */
			Scope?: "single" | "multi";
			/**
			 * The number and way that different tasks can use this volume
			 * at one time.
			 * - `none` The volume may only be used by one task at a time.
			 * - `readonly` The volume may be used by any number of tasks, but they all
			 * must mount the volume as readonly
			 * - `onewriter` The volume may be used by any number of tasks, but only one
			 * may mount it as read/write.
			 * - `all` The volume may have any number of readers and writers.
			 * @default none
			 * @enum none,readonly,onewriter,all
			 */
			Sharing?: "none" | "readonly" | "onewriter" | "all";
			MountVolume?: {};
			Secrets?: readonly {
				Key?: string;
				Secret?: string;
			}[];
			AccessibilityRequirements?: {
				Requisite?: readonly Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>[];
				Preferred?: readonly Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>[];
			};
			CapacityRange?: {
				RequiredBytes?: number;
				LimitBytes?: number;
			};
			/**
			 * The availability of the volume for use in tasks.
			 * - `active` The volume is fully available for scheduling on the cluster
			 * - `pause` No new workloads should use the volume, but existing workloads
			 * are not stopped.
			 * - `drain` All workloads using this volume should be stopped and
			 * rescheduled, and no new ones should be started.
			 * @default active
			 * @enum active,pause,drain
			 */
			Availability?: "active" | "pause" | "drain";
		};
	};
	Info?: {
		CapacityBytes?: number;
		VolumeContext?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		VolumeID?: string;
		AccessibleTopology?: readonly Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>[];
	};
	PublishStatus?: readonly {
		NodeID?: string;
		/**
		 * The published state of the volume.
		 * * `pending-publish` The volume should be published to this node, but the
		 * call to the controller plugin to do so has not yet been successfully
		 * completed.
		 * * `published` The volume is published successfully to the node.
		 * * `pending-node-unpublish` The volume should be unpublished from the node,
		 * and the manager is awaiting confirmation from the worker that it has done
		 * so.
		 * * `pending-controller-unpublish` The volume is successfully unpublished
		 * from the node, but has not yet been successfully unpublished on the
		 * controller.
		 * @enum pending-publish,published,pending-node-unpublish,pending-controller-unpublish
		 */
		State?:
			| "pending-publish"
			| "published"
			| "pending-node-unpublish"
			| "pending-controller-unpublish";
		PublishContext?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	}[];
};
/** Cluster-specific options used to create the volume. */
export type ClusterVolumeSpec = {
	Group?: string;
	AccessMode?: {
		/**
		 * The set of nodes this volume can be used on at one time.
		 * - `single` The volume may only be scheduled to one node at a time.
		 * - `multi` the volume may be scheduled to any supported number of nodes at a
		 * time.
		 * @default single
		 * @enum single,multi
		 */
		Scope?: "single" | "multi";
		/**
		 * The number and way that different tasks can use this volume
		 * at one time.
		 * - `none` The volume may only be used by one task at a time.
		 * - `readonly` The volume may be used by any number of tasks, but they all
		 * must mount the volume as readonly
		 * - `onewriter` The volume may be used by any number of tasks, but only one
		 * may mount it as read/write.
		 * - `all` The volume may have any number of readers and writers.
		 * @default none
		 * @enum none,readonly,onewriter,all
		 */
		Sharing?: "none" | "readonly" | "onewriter" | "all";
		MountVolume?: {};
		Secrets?: readonly {
			Key?: string;
			Secret?: string;
		}[];
		AccessibilityRequirements?: {
			Requisite?: readonly Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>[];
			Preferred?: readonly Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>[];
		};
		CapacityRange?: {
			RequiredBytes?: number;
			LimitBytes?: number;
		};
		/**
		 * The availability of the volume for use in tasks.
		 * - `active` The volume is fully available for scheduling on the cluster
		 * - `pause` No new workloads should use the volume, but existing workloads
		 * are not stopped.
		 * - `drain` All workloads using this volume should be stopped and
		 * rescheduled, and no new ones should be started.
		 * @default active
		 * @enum active,pause,drain
		 */
		Availability?: "active" | "pause" | "drain";
	};
};
/**
 * A map of topological domains to topological segments. For in depth
 * details, see documentation for the Topology object in the CSI
 * specification.
 */
export type Topology = Record<string, string>;
export type ContainerListCommandQuery = {
	all?: "true" | "false";
	limit?: `${number}` | undefined;
	size?: "true" | "false";
	filters?: string | undefined;
};
export type ContainerListCommandInput = ContainerListCommandQuery;
export type ContainerListCommandOutput = readonly {
	Id?: string;
	Names?: readonly string[];
	Image?: string;
	ImageID?: string;
	Command?: string;
	Created?: number;
	Ports?: readonly {
		IP?: string;
		PrivatePort: number;
		PublicPort?: number;
		Type: "tcp" | "udp" | "sctp";
	}[];
	SizeRw?: number;
	SizeRootFs?: number;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	State?: string;
	Status?: string;
	HostConfig?: {
		NetworkMode?: string;
	};
	NetworkSettings?: {
		Networks?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	Mounts?: readonly {
		/**
		 * The mount type:
		 *
		 * - `bind` a mount of a file or directory from the host into the container.
		 * - `volume` a docker volume with the given `Name`.
		 * - `tmpfs` a `tmpfs`.
		 * - `npipe` a named pipe from the host into the container.
		 * - `cluster` a Swarm cluster volume
		 * @enum bind,volume,tmpfs,npipe,cluster
		 * @example volume
		 */
		Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
		/**
		 * Name is the name reference to the underlying data defined by `Source`
		 * e.g., the volume name.
		 * @example myvolume
		 */
		Name?: string;
		/**
		 * Source location of the mount.
		 *
		 * For volumes, this contains the storage location of the volume (within
		 * `/var/lib/docker/volumes/`). For bind-mounts, and `npipe`, this contains
		 * the source (host) part of the bind-mount. For `tmpfs` mount points, this
		 * field is empty.
		 * @example /var/lib/docker/volumes/myvolume/_data
		 */
		Source?: string;
		/**
		 * Destination is the path relative to the container root (`/`) where
		 * the `Source` is mounted inside the container.
		 * @example /usr/share/nginx/html/
		 */
		Destination?: string;
		/**
		 * Driver is the volume driver used to create the volume (if it is a volume).
		 * @example local
		 */
		Driver?: string;
		/**
		 * Mode is a comma separated list of options supplied by the user when
		 * creating the bind/volume mount.
		 *
		 * The default is platform-specific (`"z"` on Linux, empty on Windows).
		 * @example z
		 */
		Mode?: string;
		/**
		 * Whether the mount is mounted writable (read-write).
		 * @example true
		 */
		RW?: boolean;
		/**
		 * Propagation describes how mounts are propagated from the host into the
		 * mount point, and vice-versa. Refer to the [Linux kernel
		 * documentation](https://www.kernel.org/doc/Documentation/filesystems/sharedsubtree.txt)
		 * for details. This field is not used on Windows.
		 * @example
		 */
		Propagation?: string;
	}[];
}[];
export type ContainerCreateCommandQuery = {
	name?: string | undefined;
	platform?: string | undefined;
};
type ContainerCreateJsonBody = {
	/**
	 * The hostname to use for the container, as a valid RFC 1123 hostname.
	 * @example 439f4e91bd1d
	 */
	Hostname?: string;
	Domainname?: string;
	User?: string;
	AttachStdin?: boolean;
	/**
	 * Whether to attach to `stdout`.
	 * @default true
	 */
	AttachStdout?: boolean;
	/**
	 * Whether to attach to `stderr`.
	 * @default true
	 */
	AttachStderr?: boolean;
	ExposedPorts?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Tty?: boolean;
	OpenStdin?: boolean;
	StdinOnce?: boolean;
	/**
	 * A list of environment variables to set inside the container in the
	 * form `["VAR=value", ...]`. A variable without `=` is removed from the
	 * environment, rather than to have an empty value.
	 * @example PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
	 */
	Env?: readonly string[];
	/**
	 * Command to run specified as a string or an array of strings.
	 * @example /bin/sh
	 */
	Cmd?: readonly string[];
	Healthcheck?: {
		Test?: readonly string[];
		Interval?: number;
		Timeout?: number;
		Retries?: number;
		StartPeriod?: number;
	};
	/**
	 * Command is already escaped (Windows only)
	 * @example false
	 */
	ArgsEscaped?: boolean | null;
	/**
	 * The name (or reference) of the image to use when creating the container,
	 * or which was used when the container was created.
	 * @example example-image:1.0
	 */
	Image?: string;
	Volumes?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * The working directory for commands to run in.
	 * @example /public/
	 */
	WorkingDir?: string;
	/**
	 * The entry point for the container as a string or an array of strings.
	 *
	 * If the array consists of exactly one empty string (`[""]`) then the
	 * entry point is reset to system default (i.e., the entry point used by
	 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
	 * @example
	 */
	Entrypoint?: readonly string[];
	NetworkDisabled?: boolean | null;
	MacAddress?: string;
	/**
	 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
	 * @example
	 */
	OnBuild?: readonly string[];
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Signal to stop a container as a string or unsigned integer.
	 * @example SIGTERM
	 */
	StopSignal?: string;
	StopTimeout?: number | null;
	/**
	 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
	 * @example /bin/sh,-c
	 */
	Shell?: readonly string[];
} & {
	HostConfig?: {
		CpuShares?: number;
		Memory?: number;
		CgroupParent?: string;
		BlkioWeight?: number;
		BlkioWeightDevice?: readonly {
			Path?: string;
			Weight?: number;
		}[];
		BlkioDeviceReadBps?: readonly {
			Path?: string;
			Rate?: number;
		}[];
		BlkioDeviceWriteBps?: readonly {
			Path?: string;
			Rate?: number;
		}[];
		BlkioDeviceReadIOps?: readonly {
			Path?: string;
			Rate?: number;
		}[];
		BlkioDeviceWriteIOps?: readonly {
			Path?: string;
			Rate?: number;
		}[];
		CpuPeriod?: number;
		CpuQuota?: number;
		CpuRealtimePeriod?: number;
		CpuRealtimeRuntime?: number;
		/**
		 * CPUs in which to allow execution (e.g., `0-3`, `0,1`).
		 * @example 0-3
		 */
		CpusetCpus?: string;
		CpusetMems?: string;
		Devices?: readonly {
			PathOnHost?: string;
			PathInContainer?: string;
			CgroupPermissions?: string;
		}[];
		DeviceCgroupRules?: readonly string[];
		DeviceRequests?: readonly {
			Driver?: string;
			Count?: number;
			DeviceIDs?: readonly string[];
			/**
			 * A list of capabilities; an OR list of AND lists of capabilities.
			 * @example gpu,nvidia,compute
			 */
			Capabilities?: readonly (readonly string[])[];
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		KernelMemoryTCP?: number;
		MemoryReservation?: number;
		MemorySwap?: number;
		MemorySwappiness?: number;
		NanoCpus?: number;
		OomKillDisable?: boolean;
		Init?: boolean | null;
		PidsLimit?: number | null;
		Ulimits?: readonly {
			Name?: string;
			Soft?: number;
			Hard?: number;
		}[];
		CpuCount?: number;
		CpuPercent?: number;
		IOMaximumIOps?: number;
		IOMaximumBandwidth?: number;
	} & {
		Binds?: readonly string[];
		ContainerIDFile?: string;
		LogConfig?: {
			Type?:
				| "json-file"
				| "syslog"
				| "journald"
				| "gelf"
				| "fluentd"
				| "awslogs"
				| "splunk"
				| "etwlogs"
				| "none";
			Config?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
		NetworkMode?: string;
		PortBindings?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		RestartPolicy?: {
			/**
			 * - Empty string means not to restart
			 * - `no` Do not automatically restart
			 * - `always` Always restart
			 * - `unless-stopped` Restart always except when the user has manually stopped
			 * the container
			 * - `on-failure` Restart only when the container exit code is non-zero
			 * @enum ,no,always,unless-stopped,on-failure
			 */
			Name?: "" | "no" | "always" | "unless-stopped" | "on-failure";
			MaximumRetryCount?: number;
		};
		AutoRemove?: boolean;
		VolumeDriver?: string;
		VolumesFrom?: readonly string[];
		Mounts?: readonly {
			Target?: string;
			Source?: string;
			/**
			 * The mount type. Available types:
			 *
			 * - `bind` Mounts a file or directory from the host into the container. Must
			 * exist prior to creating the container.
			 * - `volume` Creates a volume with the given name and options (or uses a
			 * pre-existing volume with the same name and options). These are **not**
			 * removed when the container is removed.
			 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
			 * specified for tmpfs.
			 * - `npipe` Mounts a named pipe from the host into the container. Must exist
			 * prior to creating the container.
			 * - `cluster` a Swarm cluster volume
			 * @enum bind,volume,tmpfs,npipe,cluster
			 */
			Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
			ReadOnly?: boolean;
			Consistency?: string;
			BindOptions?: {
				/**
				 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
				 * @enum private,rprivate,shared,rshared,slave,rslave
				 */
				Propagation?:
					| "private"
					| "rprivate"
					| "shared"
					| "rshared"
					| "slave"
					| "rslave";
				NonRecursive?: boolean;
				CreateMountpoint?: boolean;
			};
			VolumeOptions?: {
				NoCopy?: boolean;
				Labels?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				DriverConfig?: {
					Name?: string;
					Options?: Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>;
				};
			};
			TmpfsOptions?: {
				SizeBytes?: number;
				Mode?: number;
			};
		}[];
		ConsoleSize?: readonly number[];
		Annotations?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		CapAdd?: readonly string[];
		CapDrop?: readonly string[];
		/**
		 * cgroup namespace mode for the container. Possible values are:
		 *
		 * - `"private"`: the container runs in its own private cgroup namespace
		 * - `"host"`: use the host system's cgroup namespace
		 *
		 * If not specified, the daemon default is used, which can either be
		 * `"private"`
		 * or `"host"`, depending on daemon version, kernel support and configuration.
		 * @enum private,host
		 */
		CgroupnsMode?: "private" | "host";
		Dns?: readonly string[];
		DnsOptions?: readonly string[];
		DnsSearch?: readonly string[];
		ExtraHosts?: readonly string[];
		GroupAdd?: readonly string[];
		IpcMode?: string;
		Cgroup?: string;
		Links?: readonly string[];
		/**
		 * An integer value containing the score given to the container in
		 * order to tune OOM killer preferences.
		 * @example 500
		 */
		OomScoreAdj?: number;
		PidMode?: string;
		Privileged?: boolean;
		PublishAllPorts?: boolean;
		ReadonlyRootfs?: boolean;
		SecurityOpt?: readonly string[];
		StorageOpt?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Tmpfs?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		UTSMode?: string;
		UsernsMode?: string;
		ShmSize?: number;
		Sysctls?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Runtime?: string;
		/**
		 * Isolation technology of the container. (Windows only)
		 * @enum default,process,hyperv,
		 */
		Isolation?: "default" | "process" | "hyperv" | "";
		MaskedPaths?: readonly string[];
		ReadonlyPaths?: readonly string[];
	};
	/**
	 * NetworkingConfig represents the container's networking configuration for
	 * each of its interfaces.
	 * It is used for the networking configs specified in the `docker create`
	 * and `docker network connect` commands.
	 * @example [object Object]
	 */
	NetworkingConfig?: {
		EndpointsConfig?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
};
export type ContainerCreateCommandBody = ContainerCreateJsonBody;
export type ContainerCreateCommandInput = ContainerCreateJsonBody &
	ContainerCreateCommandQuery;
export type ContainerCreateCommandOutput = {
	/**
	 * The ID of the created container
	 * @example ede54ee1afda366ab42f824e8a5ffd195155d853ceaec74a927f249ea270c743
	 */
	Id: string;
	/**
	 * Warnings encountered when creating the container
	 * @example
	 */
	Warnings: readonly string[];
};
export type ContainerInspectCommandQuery = {
	size?: "true" | "false";
};
export type ContainerInspectCommandParams = {
	id: string;
};
export type ContainerInspectCommandInput = ContainerInspectCommandParams &
	ContainerInspectCommandQuery;
export type ContainerInspectCommandOutput = {
	Id?: string;
	Created?: string;
	Path?: string;
	Args?: readonly string[];
	State?: {
		/**
		 * String representation of the container state. Can be one of "created",
		 * "running", "paused", "restarting", "removing", "exited", or "dead".
		 * @enum created,running,paused,restarting,removing,exited,dead
		 * @example running
		 */
		Status?:
			| "created"
			| "running"
			| "paused"
			| "restarting"
			| "removing"
			| "exited"
			| "dead";
		/**
		 * Whether this container is running.
		 *
		 * Note that a running container can be _paused_. The `Running` and `Paused`
		 * booleans are not mutually exclusive:
		 *
		 * When pausing a container (on Linux), the freezer cgroup is used to suspend
		 * all processes in the container. Freezing the process requires the process
		 * to
		 * be running. As a result, paused containers are both `Running` _and_
		 * `Paused`.
		 *
		 * Use the `Status` field instead to determine if a container's state is
		 * "running".
		 * @example true
		 */
		Running?: boolean;
		/**
		 * Whether this container is paused.
		 * @example false
		 */
		Paused?: boolean;
		/**
		 * Whether this container is restarting.
		 * @example false
		 */
		Restarting?: boolean;
		/**
		 * Whether a process within this container has been killed because it ran
		 * out of memory since the container was last started.
		 * @example false
		 */
		OOMKilled?: boolean;
		Dead?: boolean;
		/**
		 * The process ID of this container
		 * @example 1234
		 */
		Pid?: number;
		/**
		 * The last exit code of this container
		 * @example 0
		 */
		ExitCode?: number;
		Error?: string;
		/**
		 * The time when this container was last started.
		 * @example 2020-01-06T09:06:59.461876391Z
		 */
		StartedAt?: string;
		/**
		 * The time when this container last exited.
		 * @example 2020-01-06T09:07:59.461876391Z
		 */
		FinishedAt?: string;
		Health?: {
			/**
			 * Status is one of `none`, `starting`, `healthy` or `unhealthy`
			 *
			 * - "none"      Indicates there is no healthcheck
			 * - "starting"  Starting indicates that the container is not yet ready
			 * - "healthy"   Healthy indicates that the container is running correctly
			 * - "unhealthy" Unhealthy indicates that the container has a problem
			 * @enum none,starting,healthy,unhealthy
			 * @example healthy
			 */
			Status?: "none" | "starting" | "healthy" | "unhealthy";
			/**
			 * FailingStreak is the number of consecutive failures
			 * @example 0
			 */
			FailingStreak?: number;
			Log?: readonly {
				/**
				 * Date and time at which this check started in
				 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
				 * @example 2020-01-04T10:44:24.496525531Z
				 */
				Start?: string;
				/**
				 * Date and time at which this check ended in
				 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
				 * @example 2020-01-04T10:45:21.364524523Z
				 */
				End?: string;
				/**
				 * ExitCode meanings:
				 *
				 * - `0` healthy
				 * - `1` unhealthy
				 * - `2` reserved (considered unhealthy)
				 * - other values: error running probe
				 * @example 0
				 */
				ExitCode?: number;
				Output?: string;
			}[];
		};
	};
	Image?: string;
	ResolvConfPath?: string;
	HostnamePath?: string;
	HostsPath?: string;
	LogPath?: string;
	Name?: string;
	RestartCount?: number;
	Driver?: string;
	Platform?: string;
	MountLabel?: string;
	ProcessLabel?: string;
	AppArmorProfile?: string;
	ExecIDs?: readonly string[];
	HostConfig?: {
		CpuShares?: number;
		Memory?: number;
		CgroupParent?: string;
		BlkioWeight?: number;
		BlkioWeightDevice?: readonly {
			Path?: string;
			Weight?: number;
		}[];
		BlkioDeviceReadBps?: readonly {
			Path?: string;
			Rate?: number;
		}[];
		BlkioDeviceWriteBps?: readonly {
			Path?: string;
			Rate?: number;
		}[];
		BlkioDeviceReadIOps?: readonly {
			Path?: string;
			Rate?: number;
		}[];
		BlkioDeviceWriteIOps?: readonly {
			Path?: string;
			Rate?: number;
		}[];
		CpuPeriod?: number;
		CpuQuota?: number;
		CpuRealtimePeriod?: number;
		CpuRealtimeRuntime?: number;
		/**
		 * CPUs in which to allow execution (e.g., `0-3`, `0,1`).
		 * @example 0-3
		 */
		CpusetCpus?: string;
		CpusetMems?: string;
		Devices?: readonly {
			PathOnHost?: string;
			PathInContainer?: string;
			CgroupPermissions?: string;
		}[];
		DeviceCgroupRules?: readonly string[];
		DeviceRequests?: readonly {
			Driver?: string;
			Count?: number;
			DeviceIDs?: readonly string[];
			/**
			 * A list of capabilities; an OR list of AND lists of capabilities.
			 * @example gpu,nvidia,compute
			 */
			Capabilities?: readonly (readonly string[])[];
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		KernelMemoryTCP?: number;
		MemoryReservation?: number;
		MemorySwap?: number;
		MemorySwappiness?: number;
		NanoCpus?: number;
		OomKillDisable?: boolean;
		Init?: boolean | null;
		PidsLimit?: number | null;
		Ulimits?: readonly {
			Name?: string;
			Soft?: number;
			Hard?: number;
		}[];
		CpuCount?: number;
		CpuPercent?: number;
		IOMaximumIOps?: number;
		IOMaximumBandwidth?: number;
	} & {
		Binds?: readonly string[];
		ContainerIDFile?: string;
		LogConfig?: {
			Type?:
				| "json-file"
				| "syslog"
				| "journald"
				| "gelf"
				| "fluentd"
				| "awslogs"
				| "splunk"
				| "etwlogs"
				| "none";
			Config?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
		NetworkMode?: string;
		PortBindings?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		RestartPolicy?: {
			/**
			 * - Empty string means not to restart
			 * - `no` Do not automatically restart
			 * - `always` Always restart
			 * - `unless-stopped` Restart always except when the user has manually stopped
			 * the container
			 * - `on-failure` Restart only when the container exit code is non-zero
			 * @enum ,no,always,unless-stopped,on-failure
			 */
			Name?: "" | "no" | "always" | "unless-stopped" | "on-failure";
			MaximumRetryCount?: number;
		};
		AutoRemove?: boolean;
		VolumeDriver?: string;
		VolumesFrom?: readonly string[];
		Mounts?: readonly {
			Target?: string;
			Source?: string;
			/**
			 * The mount type. Available types:
			 *
			 * - `bind` Mounts a file or directory from the host into the container. Must
			 * exist prior to creating the container.
			 * - `volume` Creates a volume with the given name and options (or uses a
			 * pre-existing volume with the same name and options). These are **not**
			 * removed when the container is removed.
			 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
			 * specified for tmpfs.
			 * - `npipe` Mounts a named pipe from the host into the container. Must exist
			 * prior to creating the container.
			 * - `cluster` a Swarm cluster volume
			 * @enum bind,volume,tmpfs,npipe,cluster
			 */
			Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
			ReadOnly?: boolean;
			Consistency?: string;
			BindOptions?: {
				/**
				 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
				 * @enum private,rprivate,shared,rshared,slave,rslave
				 */
				Propagation?:
					| "private"
					| "rprivate"
					| "shared"
					| "rshared"
					| "slave"
					| "rslave";
				NonRecursive?: boolean;
				CreateMountpoint?: boolean;
			};
			VolumeOptions?: {
				NoCopy?: boolean;
				Labels?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				DriverConfig?: {
					Name?: string;
					Options?: Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>;
				};
			};
			TmpfsOptions?: {
				SizeBytes?: number;
				Mode?: number;
			};
		}[];
		ConsoleSize?: readonly number[];
		Annotations?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		CapAdd?: readonly string[];
		CapDrop?: readonly string[];
		/**
		 * cgroup namespace mode for the container. Possible values are:
		 *
		 * - `"private"`: the container runs in its own private cgroup namespace
		 * - `"host"`: use the host system's cgroup namespace
		 *
		 * If not specified, the daemon default is used, which can either be
		 * `"private"`
		 * or `"host"`, depending on daemon version, kernel support and configuration.
		 * @enum private,host
		 */
		CgroupnsMode?: "private" | "host";
		Dns?: readonly string[];
		DnsOptions?: readonly string[];
		DnsSearch?: readonly string[];
		ExtraHosts?: readonly string[];
		GroupAdd?: readonly string[];
		IpcMode?: string;
		Cgroup?: string;
		Links?: readonly string[];
		/**
		 * An integer value containing the score given to the container in
		 * order to tune OOM killer preferences.
		 * @example 500
		 */
		OomScoreAdj?: number;
		PidMode?: string;
		Privileged?: boolean;
		PublishAllPorts?: boolean;
		ReadonlyRootfs?: boolean;
		SecurityOpt?: readonly string[];
		StorageOpt?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Tmpfs?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		UTSMode?: string;
		UsernsMode?: string;
		ShmSize?: number;
		Sysctls?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Runtime?: string;
		/**
		 * Isolation technology of the container. (Windows only)
		 * @enum default,process,hyperv,
		 */
		Isolation?: "default" | "process" | "hyperv" | "";
		MaskedPaths?: readonly string[];
		ReadonlyPaths?: readonly string[];
	};
	GraphDriver?: {
		/**
		 * Name of the storage driver.
		 * @example overlay2
		 */
		Name: string;
		Data: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	SizeRw?: number;
	SizeRootFs?: number;
	Mounts?: readonly {
		/**
		 * The mount type:
		 *
		 * - `bind` a mount of a file or directory from the host into the container.
		 * - `volume` a docker volume with the given `Name`.
		 * - `tmpfs` a `tmpfs`.
		 * - `npipe` a named pipe from the host into the container.
		 * - `cluster` a Swarm cluster volume
		 * @enum bind,volume,tmpfs,npipe,cluster
		 * @example volume
		 */
		Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
		/**
		 * Name is the name reference to the underlying data defined by `Source`
		 * e.g., the volume name.
		 * @example myvolume
		 */
		Name?: string;
		/**
		 * Source location of the mount.
		 *
		 * For volumes, this contains the storage location of the volume (within
		 * `/var/lib/docker/volumes/`). For bind-mounts, and `npipe`, this contains
		 * the source (host) part of the bind-mount. For `tmpfs` mount points, this
		 * field is empty.
		 * @example /var/lib/docker/volumes/myvolume/_data
		 */
		Source?: string;
		/**
		 * Destination is the path relative to the container root (`/`) where
		 * the `Source` is mounted inside the container.
		 * @example /usr/share/nginx/html/
		 */
		Destination?: string;
		/**
		 * Driver is the volume driver used to create the volume (if it is a volume).
		 * @example local
		 */
		Driver?: string;
		/**
		 * Mode is a comma separated list of options supplied by the user when
		 * creating the bind/volume mount.
		 *
		 * The default is platform-specific (`"z"` on Linux, empty on Windows).
		 * @example z
		 */
		Mode?: string;
		/**
		 * Whether the mount is mounted writable (read-write).
		 * @example true
		 */
		RW?: boolean;
		/**
		 * Propagation describes how mounts are propagated from the host into the
		 * mount point, and vice-versa. Refer to the [Linux kernel
		 * documentation](https://www.kernel.org/doc/Documentation/filesystems/sharedsubtree.txt)
		 * for details. This field is not used on Windows.
		 * @example
		 */
		Propagation?: string;
	}[];
	Config?: {
		/**
		 * The hostname to use for the container, as a valid RFC 1123 hostname.
		 * @example 439f4e91bd1d
		 */
		Hostname?: string;
		Domainname?: string;
		User?: string;
		AttachStdin?: boolean;
		/**
		 * Whether to attach to `stdout`.
		 * @default true
		 */
		AttachStdout?: boolean;
		/**
		 * Whether to attach to `stderr`.
		 * @default true
		 */
		AttachStderr?: boolean;
		ExposedPorts?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Tty?: boolean;
		OpenStdin?: boolean;
		StdinOnce?: boolean;
		/**
		 * A list of environment variables to set inside the container in the
		 * form `["VAR=value", ...]`. A variable without `=` is removed from the
		 * environment, rather than to have an empty value.
		 * @example PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
		 */
		Env?: readonly string[];
		/**
		 * Command to run specified as a string or an array of strings.
		 * @example /bin/sh
		 */
		Cmd?: readonly string[];
		Healthcheck?: {
			Test?: readonly string[];
			Interval?: number;
			Timeout?: number;
			Retries?: number;
			StartPeriod?: number;
		};
		/**
		 * Command is already escaped (Windows only)
		 * @example false
		 */
		ArgsEscaped?: boolean | null;
		/**
		 * The name (or reference) of the image to use when creating the container,
		 * or which was used when the container was created.
		 * @example example-image:1.0
		 */
		Image?: string;
		Volumes?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * The working directory for commands to run in.
		 * @example /public/
		 */
		WorkingDir?: string;
		/**
		 * The entry point for the container as a string or an array of strings.
		 *
		 * If the array consists of exactly one empty string (`[""]`) then the
		 * entry point is reset to system default (i.e., the entry point used by
		 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
		 * @example
		 */
		Entrypoint?: readonly string[];
		NetworkDisabled?: boolean | null;
		MacAddress?: string;
		/**
		 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
		 * @example
		 */
		OnBuild?: readonly string[];
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Signal to stop a container as a string or unsigned integer.
		 * @example SIGTERM
		 */
		StopSignal?: string;
		StopTimeout?: number | null;
		/**
		 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
		 * @example /bin/sh,-c
		 */
		Shell?: readonly string[];
	};
	NetworkSettings?: {
		/**
		 * Name of the network's bridge (for example, `docker0`).
		 * @example docker0
		 */
		Bridge?: string;
		/**
		 * SandboxID uniquely represents a container's network stack.
		 * @example 9d12daf2c33f5959c8bf90aa513e4f65b561738661003029ec84830cd503a0c3
		 */
		SandboxID?: string;
		/**
		 * Indicates if hairpin NAT should be enabled on the virtual interface.
		 * @example false
		 */
		HairpinMode?: boolean;
		/**
		 * IPv6 unicast address using the link-local prefix.
		 * @example fe80::42:acff:fe11:1
		 */
		LinkLocalIPv6Address?: string;
		/**
		 * Prefix length of the IPv6 unicast address.
		 * @example 64
		 */
		LinkLocalIPv6PrefixLen?: number;
		Ports?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * SandboxKey identifies the sandbox
		 * @example /var/run/docker/netns/8ab54b426c38
		 */
		SandboxKey?: string;
		SecondaryIPAddresses?: readonly {
			Addr?: string;
			PrefixLen?: number;
		}[];
		SecondaryIPv6Addresses?: readonly {
			Addr?: string;
			PrefixLen?: number;
		}[];
		/**
		 * EndpointID uniquely represents a service endpoint in a Sandbox.
		 *
		 * <p><br /></p>
		 *
		 * > **Deprecated**: This field is only propagated when attached to the
		 * > default "bridge" network. Use the information from the "bridge"
		 * > network inside the `Networks` map instead, which contains the same
		 * > information. This field was deprecated in Docker 1.9 and is scheduled
		 * > to be removed in Docker 17.12.0
		 * @example b88f5b905aabf2893f3cbc4ee42d1ea7980bbc0a92e2c8922b1e1795298afb0b
		 */
		EndpointID?: string;
		/**
		 * Gateway address for the default "bridge" network.
		 *
		 * <p><br /></p>
		 *
		 * > **Deprecated**: This field is only propagated when attached to the
		 * > default "bridge" network. Use the information from the "bridge"
		 * > network inside the `Networks` map instead, which contains the same
		 * > information. This field was deprecated in Docker 1.9 and is scheduled
		 * > to be removed in Docker 17.12.0
		 * @example 172.17.0.1
		 */
		Gateway?: string;
		/**
		 * Global IPv6 address for the default "bridge" network.
		 *
		 * <p><br /></p>
		 *
		 * > **Deprecated**: This field is only propagated when attached to the
		 * > default "bridge" network. Use the information from the "bridge"
		 * > network inside the `Networks` map instead, which contains the same
		 * > information. This field was deprecated in Docker 1.9 and is scheduled
		 * > to be removed in Docker 17.12.0
		 * @example 2001:db8::5689
		 */
		GlobalIPv6Address?: string;
		/**
		 * Mask length of the global IPv6 address.
		 *
		 * <p><br /></p>
		 *
		 * > **Deprecated**: This field is only propagated when attached to the
		 * > default "bridge" network. Use the information from the "bridge"
		 * > network inside the `Networks` map instead, which contains the same
		 * > information. This field was deprecated in Docker 1.9 and is scheduled
		 * > to be removed in Docker 17.12.0
		 * @example 64
		 */
		GlobalIPv6PrefixLen?: number;
		/**
		 * IPv4 address for the default "bridge" network.
		 *
		 * <p><br /></p>
		 *
		 * > **Deprecated**: This field is only propagated when attached to the
		 * > default "bridge" network. Use the information from the "bridge"
		 * > network inside the `Networks` map instead, which contains the same
		 * > information. This field was deprecated in Docker 1.9 and is scheduled
		 * > to be removed in Docker 17.12.0
		 * @example 172.17.0.4
		 */
		IPAddress?: string;
		/**
		 * Mask length of the IPv4 address.
		 *
		 * <p><br /></p>
		 *
		 * > **Deprecated**: This field is only propagated when attached to the
		 * > default "bridge" network. Use the information from the "bridge"
		 * > network inside the `Networks` map instead, which contains the same
		 * > information. This field was deprecated in Docker 1.9 and is scheduled
		 * > to be removed in Docker 17.12.0
		 * @example 16
		 */
		IPPrefixLen?: number;
		/**
		 * IPv6 gateway address for this network.
		 *
		 * <p><br /></p>
		 *
		 * > **Deprecated**: This field is only propagated when attached to the
		 * > default "bridge" network. Use the information from the "bridge"
		 * > network inside the `Networks` map instead, which contains the same
		 * > information. This field was deprecated in Docker 1.9 and is scheduled
		 * > to be removed in Docker 17.12.0
		 * @example 2001:db8:2::100
		 */
		IPv6Gateway?: string;
		/**
		 * MAC address for the container on the default "bridge" network.
		 *
		 * <p><br /></p>
		 *
		 * > **Deprecated**: This field is only propagated when attached to the
		 * > default "bridge" network. Use the information from the "bridge"
		 * > network inside the `Networks` map instead, which contains the same
		 * > information. This field was deprecated in Docker 1.9 and is scheduled
		 * > to be removed in Docker 17.12.0
		 * @example 02:42:ac:11:00:04
		 */
		MacAddress?: string;
		Networks?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
};
export type ContainerTopCommandQuery = {
	ps_args?: string | undefined;
};
export type ContainerTopCommandParams = {
	id: string;
};
export type ContainerTopCommandInput = ContainerTopCommandParams &
	ContainerTopCommandQuery;
export type ContainerTopCommandOutput = {
	Titles?: readonly string[];
	Processes?: readonly (readonly string[])[];
};
export type ContainerLogsCommandQuery = {
	follow?: "true" | "false";
	stdout?: "true" | "false";
	stderr?: "true" | "false";
	since?: `${number}` | undefined;
	until?: `${number}` | undefined;
	timestamps?: "true" | "false";
	tail?: string | undefined;
};
export type ContainerLogsCommandParams = {
	id: string;
};
export type ContainerLogsCommandInput = ContainerLogsCommandParams &
	ContainerLogsCommandQuery;
export type ContainerChangesCommandParams = {
	id: string;
};
export type ContainerChangesCommandInput = ContainerChangesCommandParams;
export type ContainerChangesCommandOutput = readonly {
	Path: string;
	/**
	 * Kind of change
	 *
	 * Can be one of:
	 *
	 * - `0`: Modified ("C")
	 * - `1`: Added ("A")
	 * - `2`: Deleted ("D")
	 * @enum 0,1,2
	 */
	Kind: number;
}[];
export type ContainerExportCommandParams = {
	id: string;
};
export type ContainerExportCommandInput = ContainerExportCommandParams;
export type ContainerStatsCommandQuery = {
	stream?: "true" | "false";
	oneshot?: "true" | "false";
};
export type ContainerStatsCommandParams = {
	id: string;
};
export type ContainerStatsCommandInput = ContainerStatsCommandParams &
	ContainerStatsCommandQuery;
export type ContainerStatsCommandOutput =
	| Record<string | number, Jsonifiable>
	| undefined;
export type ContainerResizeCommandQuery = {
	h: `${number}`;
	w: `${number}`;
};
export type ContainerResizeCommandParams = {
	id: string;
};
export type ContainerResizeCommandInput = ContainerResizeCommandParams &
	ContainerResizeCommandQuery;
export type ContainerStartCommandQuery = {
	detachKeys?: string | undefined;
};
export type ContainerStartCommandParams = {
	id: string;
};
export type ContainerStartCommandInput = ContainerStartCommandParams &
	ContainerStartCommandQuery;
export type ContainerStopCommandQuery = {
	signal?: string | undefined;
	t?: `${number}` | undefined;
};
export type ContainerStopCommandParams = {
	id: string;
};
export type ContainerStopCommandInput = ContainerStopCommandParams &
	ContainerStopCommandQuery;
export type ContainerRestartCommandQuery = {
	signal?: string | undefined;
	t?: `${number}` | undefined;
};
export type ContainerRestartCommandParams = {
	id: string;
};
export type ContainerRestartCommandInput = ContainerRestartCommandParams &
	ContainerRestartCommandQuery;
export type ContainerKillCommandQuery = {
	signal?: string | undefined;
};
export type ContainerKillCommandParams = {
	id: string;
};
export type ContainerKillCommandInput = ContainerKillCommandParams &
	ContainerKillCommandQuery;
type ContainerUpdateJsonBody = {
	CpuShares?: number;
	Memory?: number;
	CgroupParent?: string;
	BlkioWeight?: number;
	BlkioWeightDevice?: readonly {
		Path?: string;
		Weight?: number;
	}[];
	BlkioDeviceReadBps?: readonly {
		Path?: string;
		Rate?: number;
	}[];
	BlkioDeviceWriteBps?: readonly {
		Path?: string;
		Rate?: number;
	}[];
	BlkioDeviceReadIOps?: readonly {
		Path?: string;
		Rate?: number;
	}[];
	BlkioDeviceWriteIOps?: readonly {
		Path?: string;
		Rate?: number;
	}[];
	CpuPeriod?: number;
	CpuQuota?: number;
	CpuRealtimePeriod?: number;
	CpuRealtimeRuntime?: number;
	/**
	 * CPUs in which to allow execution (e.g., `0-3`, `0,1`).
	 * @example 0-3
	 */
	CpusetCpus?: string;
	CpusetMems?: string;
	Devices?: readonly {
		PathOnHost?: string;
		PathInContainer?: string;
		CgroupPermissions?: string;
	}[];
	DeviceCgroupRules?: readonly string[];
	DeviceRequests?: readonly {
		Driver?: string;
		Count?: number;
		DeviceIDs?: readonly string[];
		/**
		 * A list of capabilities; an OR list of AND lists of capabilities.
		 * @example gpu,nvidia,compute
		 */
		Capabilities?: readonly (readonly string[])[];
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	}[];
	KernelMemoryTCP?: number;
	MemoryReservation?: number;
	MemorySwap?: number;
	MemorySwappiness?: number;
	NanoCpus?: number;
	OomKillDisable?: boolean;
	Init?: boolean | null;
	PidsLimit?: number | null;
	Ulimits?: readonly {
		Name?: string;
		Soft?: number;
		Hard?: number;
	}[];
	CpuCount?: number;
	CpuPercent?: number;
	IOMaximumIOps?: number;
	IOMaximumBandwidth?: number;
} & {
	RestartPolicy?: {
		/**
		 * - Empty string means not to restart
		 * - `no` Do not automatically restart
		 * - `always` Always restart
		 * - `unless-stopped` Restart always except when the user has manually stopped
		 * the container
		 * - `on-failure` Restart only when the container exit code is non-zero
		 * @enum ,no,always,unless-stopped,on-failure
		 */
		Name?: "" | "no" | "always" | "unless-stopped" | "on-failure";
		MaximumRetryCount?: number;
	};
};
export type ContainerUpdateCommandParams = {
	id: string;
};
export type ContainerUpdateCommandBody = ContainerUpdateJsonBody;
export type ContainerUpdateCommandInput = ContainerUpdateJsonBody &
	ContainerUpdateCommandParams;
export type ContainerUpdateCommandOutput = {
	Warnings?: readonly string[];
};
export type ContainerRenameCommandQuery = {
	name: string;
};
export type ContainerRenameCommandParams = {
	id: string;
};
export type ContainerRenameCommandInput = ContainerRenameCommandParams &
	ContainerRenameCommandQuery;
export type ContainerPauseCommandParams = {
	id: string;
};
export type ContainerPauseCommandInput = ContainerPauseCommandParams;
export type ContainerUnpauseCommandParams = {
	id: string;
};
export type ContainerUnpauseCommandInput = ContainerUnpauseCommandParams;
export type ContainerAttachCommandQuery = {
	detachKeys?: string | undefined;
	logs?: "true" | "false";
	stream?: "true" | "false";
	stdin?: "true" | "false";
	stdout?: "true" | "false";
	stderr?: "true" | "false";
};
export type ContainerAttachCommandParams = {
	id: string;
};
export type ContainerAttachCommandInput = ContainerAttachCommandParams &
	ContainerAttachCommandQuery;
export type ContainerAttachWebsocketCommandQuery = {
	detachKeys?: string | undefined;
	logs?: "true" | "false";
	stream?: "true" | "false";
	stdin?: "true" | "false";
	stdout?: "true" | "false";
	stderr?: "true" | "false";
};
export type ContainerAttachWebsocketCommandParams = {
	id: string;
};
export type ContainerAttachWebsocketCommandInput =
	ContainerAttachWebsocketCommandParams & ContainerAttachWebsocketCommandQuery;
export type ContainerWaitCommandQuery = {
	condition?: "not-running" | "next-exit" | "removed";
};
export type ContainerWaitCommandParams = {
	id: string;
};
export type ContainerWaitCommandInput = ContainerWaitCommandParams &
	ContainerWaitCommandQuery;
export type ContainerWaitCommandOutput = {
	StatusCode: number;
	Error?: {
		Message?: string;
	};
};
export type ContainerDeleteCommandQuery = {
	v?: "true" | "false";
	force?: "true" | "false";
	link?: "true" | "false";
};
export type ContainerDeleteCommandParams = {
	id: string;
};
export type ContainerDeleteCommandInput = ContainerDeleteCommandParams &
	ContainerDeleteCommandQuery;
export type ContainerArchiveCommandQuery = {
	path: string;
};
export type ContainerArchiveCommandParams = {
	id: string;
};
export type ContainerArchiveCommandInput = ContainerArchiveCommandParams &
	ContainerArchiveCommandQuery;
export type PutContainerArchiveCommandQuery = {
	path: string;
	noOverwriteDirNonDir?: string | undefined;
	copyUIDGID?: string | undefined;
};
type PutContainerArchiveCommandBodyApplicationXTar = NonNullable<
	RequestInit["body"]
>;
type PutContainerArchiveCommandBodyApplicationOctetStream = NonNullable<
	RequestInit["body"]
>;
export type PutContainerArchiveCommandBodyNonJson = {
	body:
		| PutContainerArchiveCommandBodyApplicationOctetStream
		| PutContainerArchiveCommandBodyApplicationXTar;
};
export type PutContainerArchiveCommandParams = {
	id: string;
};
export type PutContainerArchiveCommandInput =
	PutContainerArchiveCommandBodyNonJson &
		PutContainerArchiveCommandParams &
		PutContainerArchiveCommandQuery;
export type ContainerArchiveInfoCommandQuery = {
	path: string;
};
export type ContainerArchiveInfoCommandParams = {
	id: string;
};
export type ContainerArchiveInfoCommandInput =
	ContainerArchiveInfoCommandParams & ContainerArchiveInfoCommandQuery;
export type ContainerPruneCommandQuery = {
	filters?: string | undefined;
};
export type ContainerPruneCommandInput = ContainerPruneCommandQuery;
export type ContainerPruneCommandOutput = {
	ContainersDeleted?: readonly string[];
	SpaceReclaimed?: number;
};
export type ImageListCommandQuery = {
	all?: "true" | "false";
	filters?: string | undefined;
	sharedsize?: "true" | "false";
	digests?: "true" | "false";
};
export type ImageListCommandInput = ImageListCommandQuery;
export type ImageListCommandOutput = readonly {
	/**
	 * ID is the content-addressable ID of an image.
	 *
	 * This identifier is a content-addressable digest calculated from the
	 * image's configuration (which includes the digests of layers used by
	 * the image).
	 *
	 * Note that this digest differs from the `RepoDigests` below, which
	 * holds digests of image manifests that reference the image.
	 * @example sha256:ec3f0931a6e6b6855d76b2d7b0be30e81860baccd891b2e243280bf1cd8ad710
	 */
	Id: string;
	/**
	 * ID of the parent image.
	 *
	 * Depending on how the image was created, this field may be empty and
	 * is only set for images that were built/created locally. This field
	 * is empty if the image was pulled from an image registry.
	 * @example
	 */
	ParentId: string;
	/**
	 * List of image names/tags in the local image cache that reference this
	 * image.
	 *
	 * Multiple image tags can refer to the same image, and this list may be
	 * empty if no tags reference the image, in which case the image is
	 * "untagged", in which case it can still be referenced by its ID.
	 * @example example:1.0,example:latest,example:stable,internal.registry.example.com:5000/example:1.0
	 */
	RepoTags: readonly string[];
	/**
	 * List of content-addressable digests of locally available image manifests
	 * that the image is referenced from. Multiple manifests can refer to the
	 * same image.
	 *
	 * These digests are usually only available if the image was either pulled
	 * from a registry, or if the image was pushed to a registry, which is when
	 * the manifest is generated and its digest calculated.
	 * @example example@sha256:afcc7f1ac1b49db317a7196c902e61c6c3c4607d63599ee1a82d702d249a0ccb,internal.registry.example.com:5000/example@sha256:b69959407d21e8a062e0416bf13405bb2b71ed7a84dde4158ebafacfa06f5578
	 */
	RepoDigests: readonly string[];
	/**
	 * Date and time at which the image was created as a Unix timestamp
	 * (number of seconds since EPOCH).
	 * @example 1644009612
	 */
	Created: number;
	/**
	 * Total size of the image including all layers it is composed of.
	 * @example 172064416
	 */
	Size: number;
	/**
	 * Total size of image layers that are shared between this image and other
	 * images.
	 *
	 * This size is not calculated by default. `-1` indicates that the value
	 * has not been set / calculated.
	 * @example 1239828
	 */
	SharedSize: number;
	/**
	 * Total size of the image including all layers it is composed of.
	 *
	 * In versions of Docker before v1.10, this field was calculated from
	 * the image itself and all of its parent images. Images are now stored
	 * self-contained, and no longer use a parent-chain, making this field
	 * an equivalent of the Size field.
	 *
	 * Deprecated: this field is kept for backward compatibility, and will be
	 * removed in API v1.44.
	 * @example 172064416
	 */
	VirtualSize?: number;
	Labels: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Number of containers using this image. Includes both stopped and running
	 * containers.
	 *
	 * This size is not calculated by default, and depends on which API endpoint
	 * is used. `-1` indicates that the value has not been set / calculated.
	 * @example 2
	 */
	Containers: number;
}[];
export type ImageBuildCommandQuery = {
	dockerfile?: string | undefined;
	t?: string | undefined;
	extrahosts?: string | undefined;
	remote?: string | undefined;
	q?: "true" | "false";
	nocache?: "true" | "false";
	cachefrom?: string | undefined;
	pull?: string | undefined;
	rm?: "true" | "false";
	forcerm?: "true" | "false";
	memory?: `${number}` | undefined;
	memswap?: `${number}` | undefined;
	cpushares?: `${number}` | undefined;
	cpusetcpus?: string | undefined;
	cpuperiod?: `${number}` | undefined;
	cpuquota?: `${number}` | undefined;
	buildargs?: string | undefined;
	shmsize?: `${number}` | undefined;
	squash?: "true" | "false";
	labels?: string | undefined;
	networkmode?: string | undefined;
	platform?: string | undefined;
	target?: string | undefined;
	outputs?: string | undefined;
	version?: "1" | "2";
};
export type ImageBuildCommandHeader = {
	"content-type"?: "application/x-tar";
	"x-registry-config"?: string;
};
type ImageBuildCommandBodyApplicationOctetStream = NonNullable<
	RequestInit["body"]
>;
export type ImageBuildCommandBodyNonJson = {
	body: ImageBuildCommandBodyApplicationOctetStream;
};
export type ImageBuildCommandInput = ImageBuildCommandBodyNonJson &
	ImageBuildCommandQuery;
export type BuildPruneCommandQuery = {
	keepstorage?: `${number}` | undefined;
	all?: "true" | "false";
	filters?: string | undefined;
};
export type BuildPruneCommandInput = BuildPruneCommandQuery;
export type BuildPruneCommandOutput = {
	CachesDeleted?: readonly string[];
	SpaceReclaimed?: number;
};
export type ImageCreateCommandQuery = {
	fromImage?: string | undefined;
	fromSrc?: string | undefined;
	repo?: string | undefined;
	tag?: string | undefined;
	message?: string | undefined;
	changes?: readonly string[] | undefined;
	platform?: string | undefined;
};
export type ImageCreateCommandHeader = {
	"x-registry-auth"?: string;
};
type ImageCreateCommandBodyTextPlain = NonNullable<RequestInit["body"]>;
type ImageCreateCommandBodyApplicationOctetStream = NonNullable<
	RequestInit["body"]
>;
export type ImageCreateCommandBodyNonJson = {
	body:
		| ImageCreateCommandBodyApplicationOctetStream
		| ImageCreateCommandBodyTextPlain;
};
export type ImageCreateCommandInput = ImageCreateCommandBodyNonJson &
	ImageCreateCommandQuery;
export type ImageInspectCommandParams = {
	name: string;
};
export type ImageInspectCommandInput = ImageInspectCommandParams;
export type ImageInspectCommandOutput = {
	/**
	 * ID is the content-addressable ID of an image.
	 *
	 * This identifier is a content-addressable digest calculated from the
	 * image's configuration (which includes the digests of layers used by
	 * the image).
	 *
	 * Note that this digest differs from the `RepoDigests` below, which
	 * holds digests of image manifests that reference the image.
	 * @example sha256:ec3f0931a6e6b6855d76b2d7b0be30e81860baccd891b2e243280bf1cd8ad710
	 */
	Id?: string;
	/**
	 * List of image names/tags in the local image cache that reference this
	 * image.
	 *
	 * Multiple image tags can refer to the same image, and this list may be
	 * empty if no tags reference the image, in which case the image is
	 * "untagged", in which case it can still be referenced by its ID.
	 * @example example:1.0,example:latest,example:stable,internal.registry.example.com:5000/example:1.0
	 */
	RepoTags?: readonly string[];
	/**
	 * List of content-addressable digests of locally available image manifests
	 * that the image is referenced from. Multiple manifests can refer to the
	 * same image.
	 *
	 * These digests are usually only available if the image was either pulled
	 * from a registry, or if the image was pushed to a registry, which is when
	 * the manifest is generated and its digest calculated.
	 * @example example@sha256:afcc7f1ac1b49db317a7196c902e61c6c3c4607d63599ee1a82d702d249a0ccb,internal.registry.example.com:5000/example@sha256:b69959407d21e8a062e0416bf13405bb2b71ed7a84dde4158ebafacfa06f5578
	 */
	RepoDigests?: readonly string[];
	/**
	 * ID of the parent image.
	 *
	 * Depending on how the image was created, this field may be empty and
	 * is only set for images that were built/created locally. This field
	 * is empty if the image was pulled from an image registry.
	 * @example
	 */
	Parent?: string;
	/**
	 * Optional message that was set when committing or importing the image.
	 * @example
	 */
	Comment?: string;
	/**
	 * Date and time at which the image was created, formatted in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2022-02-04T21:20:12.497794809Z
	 */
	Created?: string;
	/**
	 * The ID of the container that was used to create the image.
	 *
	 * Depending on how the image was created, this field may be empty.
	 * @example 65974bc86f1770ae4bff79f651ebdbce166ae9aada632ee3fa9af3a264911735
	 */
	Container?: string;
	ContainerConfig?: {
		/**
		 * The hostname to use for the container, as a valid RFC 1123 hostname.
		 * @example 439f4e91bd1d
		 */
		Hostname?: string;
		Domainname?: string;
		User?: string;
		AttachStdin?: boolean;
		/**
		 * Whether to attach to `stdout`.
		 * @default true
		 */
		AttachStdout?: boolean;
		/**
		 * Whether to attach to `stderr`.
		 * @default true
		 */
		AttachStderr?: boolean;
		ExposedPorts?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Tty?: boolean;
		OpenStdin?: boolean;
		StdinOnce?: boolean;
		/**
		 * A list of environment variables to set inside the container in the
		 * form `["VAR=value", ...]`. A variable without `=` is removed from the
		 * environment, rather than to have an empty value.
		 * @example PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
		 */
		Env?: readonly string[];
		/**
		 * Command to run specified as a string or an array of strings.
		 * @example /bin/sh
		 */
		Cmd?: readonly string[];
		Healthcheck?: {
			Test?: readonly string[];
			Interval?: number;
			Timeout?: number;
			Retries?: number;
			StartPeriod?: number;
		};
		/**
		 * Command is already escaped (Windows only)
		 * @example false
		 */
		ArgsEscaped?: boolean | null;
		/**
		 * The name (or reference) of the image to use when creating the container,
		 * or which was used when the container was created.
		 * @example example-image:1.0
		 */
		Image?: string;
		Volumes?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * The working directory for commands to run in.
		 * @example /public/
		 */
		WorkingDir?: string;
		/**
		 * The entry point for the container as a string or an array of strings.
		 *
		 * If the array consists of exactly one empty string (`[""]`) then the
		 * entry point is reset to system default (i.e., the entry point used by
		 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
		 * @example
		 */
		Entrypoint?: readonly string[];
		NetworkDisabled?: boolean | null;
		MacAddress?: string;
		/**
		 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
		 * @example
		 */
		OnBuild?: readonly string[];
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Signal to stop a container as a string or unsigned integer.
		 * @example SIGTERM
		 */
		StopSignal?: string;
		StopTimeout?: number | null;
		/**
		 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
		 * @example /bin/sh,-c
		 */
		Shell?: readonly string[];
	};
	/**
	 * The version of Docker that was used to build the image.
	 *
	 * Depending on how the image was created, this field may be empty.
	 * @example 20.10.7
	 */
	DockerVersion?: string;
	/**
	 * Name of the author that was specified when committing the image, or as
	 * specified through MAINTAINER (deprecated) in the Dockerfile.
	 * @example
	 */
	Author?: string;
	/**
	 * Configuration of the image. These fields are used as defaults
	 * when starting a container from the image.
	 * @example [object Object]
	 */
	Config?: {
		/**
		 * The hostname to use for the container, as a valid RFC 1123 hostname.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always empty and must not be used.
		 * @example
		 */
		Hostname?: string;
		/**
		 * The domain name to use for the container.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always empty and must not be used.
		 * @example
		 */
		Domainname?: string;
		/**
		 * The user that commands are run as inside the container.
		 * @example web:web
		 */
		User?: string;
		/**
		 * Whether to attach to `stdin`.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always false and must not be used.
		 * @example false
		 */
		AttachStdin?: boolean;
		/**
		 * Whether to attach to `stdout`.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always false and must not be used.
		 * @example false
		 */
		AttachStdout?: boolean;
		/**
		 * Whether to attach to `stderr`.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always false and must not be used.
		 * @example false
		 */
		AttachStderr?: boolean;
		ExposedPorts?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Attach standard streams to a TTY, including `stdin` if it is not closed.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always false and must not be used.
		 * @example false
		 */
		Tty?: boolean;
		/**
		 * Open `stdin`
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always false and must not be used.
		 * @example false
		 */
		OpenStdin?: boolean;
		/**
		 * Close `stdin` after one attached client disconnects.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always false and must not be used.
		 * @example false
		 */
		StdinOnce?: boolean;
		/**
		 * A list of environment variables to set inside the container in the
		 * form `["VAR=value", ...]`. A variable without `=` is removed from the
		 * environment, rather than to have an empty value.
		 * @example PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
		 */
		Env?: readonly string[];
		/**
		 * Command to run specified as a string or an array of strings.
		 * @example /bin/sh
		 */
		Cmd?: readonly string[];
		Healthcheck?: {
			Test?: readonly string[];
			Interval?: number;
			Timeout?: number;
			Retries?: number;
			StartPeriod?: number;
		};
		/**
		 * Command is already escaped (Windows only)
		 * @example false
		 */
		ArgsEscaped?: boolean | null;
		/**
		 * The name (or reference) of the image to use when creating the container,
		 * or which was used when the container was created.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always empty and must not be used.
		 * @example
		 */
		Image?: string;
		Volumes?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * The working directory for commands to run in.
		 * @example /public/
		 */
		WorkingDir?: string;
		/**
		 * The entry point for the container as a string or an array of strings.
		 *
		 * If the array consists of exactly one empty string (`[""]`) then the
		 * entry point is reset to system default (i.e., the entry point used by
		 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
		 * @example
		 */
		Entrypoint?: readonly string[];
		/**
		 * Disable networking for the container.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always omitted and must not be used.
		 * @example false
		 */
		NetworkDisabled?: boolean | null;
		/**
		 * MAC address of the container.
		 *
		 * <p><br /></p>
		 *
		 * > **Note**: this field is always omitted and must not be used.
		 * @example
		 */
		MacAddress?: string;
		/**
		 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
		 * @example
		 */
		OnBuild?: readonly string[];
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Signal to stop a container as a string or unsigned integer.
		 * @example SIGTERM
		 */
		StopSignal?: string;
		StopTimeout?: number | null;
		/**
		 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
		 * @example /bin/sh,-c
		 */
		Shell?: readonly string[];
	};
	/**
	 * Hardware CPU architecture that the image runs on.
	 * @example arm
	 */
	Architecture?: string;
	/**
	 * CPU architecture variant (presently ARM-only).
	 * @example v7
	 */
	Variant?: string;
	/**
	 * Operating System the image is built to run on.
	 * @example linux
	 */
	Os?: string;
	/**
	 * Operating System version the image is built to run on (especially
	 * for Windows).
	 * @example
	 */
	OsVersion?: string;
	/**
	 * Total size of the image including all layers it is composed of.
	 * @example 1239828
	 */
	Size?: number;
	/**
	 * Total size of the image including all layers it is composed of.
	 *
	 * In versions of Docker before v1.10, this field was calculated from
	 * the image itself and all of its parent images. Images are now stored
	 * self-contained, and no longer use a parent-chain, making this field
	 * an equivalent of the Size field.
	 *
	 * > **Deprecated**: this field is kept for backward compatibility, but
	 * > will be removed in API v1.44.
	 * @example 1239828
	 */
	VirtualSize?: number;
	GraphDriver?: {
		/**
		 * Name of the storage driver.
		 * @example overlay2
		 */
		Name: string;
		Data: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	RootFS?: {
		Type: string;
		Layers?: readonly string[];
	};
	Metadata?: {
		/**
		 * Date and time at which the image was last tagged in
		 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
		 *
		 * This information is only available if the image was tagged locally,
		 * and omitted otherwise.
		 * @example 2022-02-28T14:40:02.623929178Z
		 */
		LastTagTime?: string;
	};
};
export type ImageHistoryCommandParams = {
	name: string;
};
export type ImageHistoryCommandInput = ImageHistoryCommandParams;
export type ImageHistoryCommandOutput = readonly {
	Id: string;
	Created: number;
	CreatedBy: string;
	Tags: readonly string[];
	Size: number;
	Comment: string;
}[];
export type ImagePushCommandQuery = {
	tag?: string | undefined;
};
export type ImagePushCommandHeader = {
	"x-registry-auth": string;
};
export type ImagePushCommandParams = {
	name: string;
};
export type ImagePushCommandInput = ImagePushCommandParams &
	ImagePushCommandQuery;
export type ImageTagCommandQuery = {
	repo?: string | undefined;
	tag?: string | undefined;
};
export type ImageTagCommandParams = {
	name: string;
};
export type ImageTagCommandInput = ImageTagCommandParams & ImageTagCommandQuery;
export type ImageDeleteCommandQuery = {
	force?: "true" | "false";
	noprune?: "true" | "false";
};
export type ImageDeleteCommandParams = {
	name: string;
};
export type ImageDeleteCommandInput = ImageDeleteCommandParams &
	ImageDeleteCommandQuery;
export type ImageDeleteCommandOutput = readonly {
	Untagged?: string;
	Deleted?: string;
}[];
export type ImageSearchCommandQuery = {
	term: string;
	limit?: `${number}` | undefined;
	filters?: string | undefined;
};
export type ImageSearchCommandInput = ImageSearchCommandQuery;
export type ImageSearchCommandOutput = readonly {
	description?: string;
	is_official?: boolean;
	is_automated?: boolean;
	name?: string;
	star_count?: number;
}[];
export type ImagePruneCommandQuery = {
	filters?: string | undefined;
};
export type ImagePruneCommandInput = ImagePruneCommandQuery;
export type ImagePruneCommandOutput = {
	ImagesDeleted?: readonly {
		Untagged?: string;
		Deleted?: string;
	}[];
	SpaceReclaimed?: number;
};
type SystemAuthJsonBody = {
	username?: string;
	password?: string;
	email?: string;
	serveraddress?: string;
};
export type SystemAuthCommandBody = SystemAuthJsonBody;
export type SystemAuthCommandInput = SystemAuthJsonBody;
export type SystemAuthCommandOutput = {
	Status: string;
	IdentityToken?: string;
};
export type SystemInfoCommandInput = never;
export type SystemInfoCommandOutput = {
	/**
	 * Unique identifier of the daemon.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: The format of the ID itself is not part of the API, and
	 * > should not be considered stable.
	 * @example 7TRN:IPZB:QYBB:VPBQ:UMPP:KARE:6ZNR:XE6T:7EWV:PKF4:ZOJD:TPYS
	 */
	ID?: string;
	/**
	 * Total number of containers on the host.
	 * @example 14
	 */
	Containers?: number;
	/**
	 * Number of containers with status `"running"`.
	 * @example 3
	 */
	ContainersRunning?: number;
	/**
	 * Number of containers with status `"paused"`.
	 * @example 1
	 */
	ContainersPaused?: number;
	/**
	 * Number of containers with status `"stopped"`.
	 * @example 10
	 */
	ContainersStopped?: number;
	/**
	 * Total number of images on the host.
	 *
	 * Both _tagged_ and _untagged_ (dangling) images are counted.
	 * @example 508
	 */
	Images?: number;
	/**
	 * Name of the storage driver in use.
	 * @example overlay2
	 */
	Driver?: string;
	/**
	 * Information specific to the storage driver, provided as
	 * "label" / "value" pairs.
	 *
	 * This information is provided by the storage driver, and formatted
	 * in a way consistent with the output of `docker info` on the command
	 * line.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: The information returned in this field, including the
	 * > formatting of values and labels, should not be considered stable,
	 * > and may change without notice.
	 * @example Backing Filesystem,extfs,Supports d_type,true,Native Overlay Diff,true
	 */
	DriverStatus?: readonly (readonly string[])[];
	/**
	 * Root directory of persistent Docker state.
	 *
	 * Defaults to `/var/lib/docker` on Linux, and `C:\ProgramData\docker`
	 * on Windows.
	 * @example /var/lib/docker
	 */
	DockerRootDir?: string;
	Plugins?: {
		/**
		 * Names of available volume-drivers, and network-driver plugins.
		 * @example local
		 */
		Volume?: readonly string[];
		/**
		 * Names of available network-drivers, and network-driver plugins.
		 * @example bridge,host,ipvlan,macvlan,null,overlay
		 */
		Network?: readonly string[];
		/**
		 * Names of available authorization plugins.
		 * @example img-authz-plugin,hbm
		 */
		Authorization?: readonly string[];
		/**
		 * Names of available logging-drivers, and logging-driver plugins.
		 * @example awslogs,fluentd,gcplogs,gelf,journald,json-file,splunk,syslog
		 */
		Log?: readonly string[];
	};
	/**
	 * Indicates if the host has memory limit support enabled.
	 * @example true
	 */
	MemoryLimit?: boolean;
	/**
	 * Indicates if the host has memory swap limit support enabled.
	 * @example true
	 */
	SwapLimit?: boolean;
	/**
	 * Indicates if the host has kernel memory TCP limit support enabled. This
	 * field is omitted if not supported.
	 *
	 * Kernel memory TCP limits are not supported when using cgroups v2, which
	 * does not support the corresponding `memory.kmem.tcp.limit_in_bytes` cgroup.
	 * @example true
	 */
	KernelMemoryTCP?: boolean;
	/**
	 * Indicates if CPU CFS(Completely Fair Scheduler) period is supported by
	 * the host.
	 * @example true
	 */
	CpuCfsPeriod?: boolean;
	/**
	 * Indicates if CPU CFS(Completely Fair Scheduler) quota is supported by
	 * the host.
	 * @example true
	 */
	CpuCfsQuota?: boolean;
	/**
	 * Indicates if CPU Shares limiting is supported by the host.
	 * @example true
	 */
	CPUShares?: boolean;
	/**
	 * Indicates if CPUsets (cpuset.cpus, cpuset.mems) are supported by the host.
	 *
	 * See
	 * [cpuset(7)](https://www.kernel.org/doc/Documentation/cgroup-v1/cpusets.txt)
	 * @example true
	 */
	CPUSet?: boolean;
	/**
	 * Indicates if the host kernel has PID limit support enabled.
	 * @example true
	 */
	PidsLimit?: boolean;
	OomKillDisable?: boolean;
	/**
	 * Indicates IPv4 forwarding is enabled.
	 * @example true
	 */
	IPv4Forwarding?: boolean;
	/**
	 * Indicates if `bridge-nf-call-iptables` is available on the host.
	 * @example true
	 */
	BridgeNfIptables?: boolean;
	/**
	 * Indicates if `bridge-nf-call-ip6tables` is available on the host.
	 * @example true
	 */
	BridgeNfIp6tables?: boolean;
	/**
	 * Indicates if the daemon is running in debug-mode / with debug-level
	 * logging enabled.
	 * @example true
	 */
	Debug?: boolean;
	/**
	 * The total number of file Descriptors in use by the daemon process.
	 *
	 * This information is only returned if debug-mode is enabled.
	 * @example 64
	 */
	NFd?: number;
	/**
	 * The  number of goroutines that currently exist.
	 *
	 * This information is only returned if debug-mode is enabled.
	 * @example 174
	 */
	NGoroutines?: number;
	/**
	 * Current system-time in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt)
	 * format with nano-seconds.
	 * @example 2017-08-08T20:28:29.06202363Z
	 */
	SystemTime?: string;
	LoggingDriver?: string;
	/**
	 * The driver to use for managing cgroups.
	 * @default cgroupfs
	 * @enum cgroupfs,systemd,none
	 * @example cgroupfs
	 */
	CgroupDriver?: "cgroupfs" | "systemd" | "none";
	/**
	 * The version of the cgroup.
	 * @default 1
	 * @enum 1,2
	 * @example 1
	 */
	CgroupVersion?: "1" | "2";
	/**
	 * Number of event listeners subscribed.
	 * @example 30
	 */
	NEventsListener?: number;
	/**
	 * Kernel version of the host.
	 *
	 * On Linux, this information obtained from `uname`. On Windows this
	 * information is queried from the
	 * <kbd>HKEY_LOCAL_MACHINE\\SOFTWARE\\Microsoft\\Windows
	 * NT\\CurrentVersion\\</kbd>
	 * registry value, for example _"10.0 14393
	 * (14393.1198.amd64fre.rs1_release_sec.170427-1353)"_.
	 * @example 4.9.38-moby
	 */
	KernelVersion?: string;
	/**
	 * Name of the host's operating system, for example: "Ubuntu 16.04.2 LTS"
	 * or "Windows Server 2016 Datacenter"
	 * @example Alpine Linux v3.5
	 */
	OperatingSystem?: string;
	/**
	 * Version of the host's operating system
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: The information returned in this field, including its
	 * > very existence, and the formatting of values, should not be considered
	 * > stable, and may change without notice.
	 * @example 16.04
	 */
	OSVersion?: string;
	/**
	 * Generic type of the operating system of the host, as returned by the
	 * Go runtime (`GOOS`).
	 *
	 * Currently returned values are "linux" and "windows". A full list of
	 * possible values can be found in the [Go
	 * documentation](https://go.dev/doc/install/source#environment).
	 * @example linux
	 */
	OSType?: string;
	/**
	 * Hardware architecture of the host, as returned by the Go runtime
	 * (`GOARCH`).
	 *
	 * A full list of possible values can be found in the [Go
	 * documentation](https://go.dev/doc/install/source#environment).
	 * @example x86_64
	 */
	Architecture?: string;
	/**
	 * The number of logical CPUs usable by the daemon.
	 *
	 * The number of available CPUs is checked by querying the operating
	 * system when the daemon starts. Changes to operating system CPU
	 * allocation after the daemon is started are not reflected.
	 * @example 4
	 */
	NCPU?: number;
	/**
	 * Total amount of physical memory available on the host, in bytes.
	 * @example 2095882240
	 */
	MemTotal?: number;
	/**
	 * Address / URL of the index server that is used for image search,
	 * and as a default for user authentication for Docker Hub and Docker Cloud.
	 * @default https://index.docker.io/v1/
	 * @example https://index.docker.io/v1/
	 */
	IndexServerAddress?: string;
	RegistryConfig?: {
		/**
		 * List of IP ranges to which nondistributable artifacts can be pushed,
		 * using the CIDR syntax [RFC 4632](https://tools.ietf.org/html/4632).
		 *
		 * Some images (for example, Windows base images) contain artifacts
		 * whose distribution is restricted by license. When these images are
		 * pushed to a registry, restricted artifacts are not included.
		 *
		 * This configuration override this behavior, and enables the daemon to
		 * push nondistributable artifacts to all registries whose resolved IP
		 * address is within the subnet described by the CIDR syntax.
		 *
		 * This option is useful when pushing images containing
		 * nondistributable artifacts to a registry on an air-gapped network so
		 * hosts on that network can pull the images without connecting to
		 * another server.
		 *
		 * > **Warning**: Nondistributable artifacts typically have restrictions
		 * > on how and where they can be distributed and shared. Only use this
		 * > feature to push artifacts to private registries and ensure that you
		 * > are in compliance with any terms that cover redistributing
		 * > nondistributable artifacts.
		 * @example ::1/128,127.0.0.0/8
		 */
		AllowNondistributableArtifactsCIDRs?: readonly string[];
		/**
		 * List of registry hostnames to which nondistributable artifacts can be
		 * pushed, using the format `<hostname>[:<port>]` or `<IP address>[:<port>]`.
		 *
		 * Some images (for example, Windows base images) contain artifacts
		 * whose distribution is restricted by license. When these images are
		 * pushed to a registry, restricted artifacts are not included.
		 *
		 * This configuration override this behavior for the specified
		 * registries.
		 *
		 * This option is useful when pushing images containing
		 * nondistributable artifacts to a registry on an air-gapped network so
		 * hosts on that network can pull the images without connecting to
		 * another server.
		 *
		 * > **Warning**: Nondistributable artifacts typically have restrictions
		 * > on how and where they can be distributed and shared. Only use this
		 * > feature to push artifacts to private registries and ensure that you
		 * > are in compliance with any terms that cover redistributing
		 * > nondistributable artifacts.
		 * @example registry.internal.corp.example.com:3000,[2001:db8:a0b:12f0::1]:443
		 */
		AllowNondistributableArtifactsHostnames?: readonly string[];
		/**
		 * List of IP ranges of insecure registries, using the CIDR syntax
		 * ([RFC 4632](https://tools.ietf.org/html/4632)). Insecure registries
		 * accept un-encrypted (HTTP) and/or untrusted (HTTPS with certificates
		 * from unknown CAs) communication.
		 *
		 * By default, local registries (`127.0.0.0/8`) are configured as
		 * insecure. All other registries are secure. Communicating with an
		 * insecure registry is not possible if the daemon assumes that registry
		 * is secure.
		 *
		 * This configuration override this behavior, insecure communication with
		 * registries whose resolved IP address is within the subnet described by
		 * the CIDR syntax.
		 *
		 * Registries can also be marked insecure by hostname. Those registries
		 * are listed under `IndexConfigs` and have their `Secure` field set to
		 * `false`.
		 *
		 * > **Warning**: Using this option can be useful when running a local
		 * > registry, but introduces security vulnerabilities. This option
		 * > should therefore ONLY be used for testing purposes. For increased
		 * > security, users should add their CA to their system's list of trusted
		 * > CAs instead of enabling this option.
		 * @example ::1/128,127.0.0.0/8
		 */
		InsecureRegistryCIDRs?: readonly string[];
		IndexConfigs?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * List of registry URLs that act as a mirror for the official
		 * (`docker.io`) registry.
		 * @example https://hub-mirror.corp.example.com:5000/,https://[2001:db8:a0b:12f0::1]/
		 */
		Mirrors?: readonly string[];
	};
	/**
	 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
	 * String resources (e.g, `GPU=UUID1`).
	 * @example [object Object],[object Object],[object Object]
	 */
	GenericResources?: readonly {
		NamedResourceSpec?: {
			Kind?: string;
			Value?: string;
		};
		DiscreteResourceSpec?: {
			Kind?: string;
			Value?: number;
		};
	}[];
	/**
	 * HTTP-proxy configured for the daemon. This value is obtained from the
	 * [`HTTP_PROXY`](https://www.gnu.org/software/wget/manual/html_node/Proxies.html)
	 * environment variable.
	 * Credentials ([user info
	 * component](https://tools.ietf.org/html/rfc3986#section-3.2.1)) in the proxy
	 * URL
	 * are masked in the API response.
	 *
	 * Containers do not automatically inherit this configuration.
	 * @example http://xxxxx:xxxxx@proxy.corp.example.com:8080
	 */
	HttpProxy?: string;
	/**
	 * HTTPS-proxy configured for the daemon. This value is obtained from the
	 * [`HTTPS_PROXY`](https://www.gnu.org/software/wget/manual/html_node/Proxies.html)
	 * environment variable.
	 * Credentials ([user info
	 * component](https://tools.ietf.org/html/rfc3986#section-3.2.1)) in the proxy
	 * URL
	 * are masked in the API response.
	 *
	 * Containers do not automatically inherit this configuration.
	 * @example https://xxxxx:xxxxx@proxy.corp.example.com:4443
	 */
	HttpsProxy?: string;
	/**
	 * Comma-separated list of domain extensions for which no proxy should be
	 * used. This value is obtained from the
	 * [`NO_PROXY`](https://www.gnu.org/software/wget/manual/html_node/Proxies.html)
	 * environment variable.
	 *
	 * Containers do not automatically inherit this configuration.
	 * @example *.local, 169.254/16
	 */
	NoProxy?: string;
	/**
	 * Hostname of the host.
	 * @example node5.corp.example.com
	 */
	Name?: string;
	/**
	 * User-defined labels (key/value metadata) as set on the daemon.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: When part of a Swarm, nodes can both have _daemon_ labels,
	 * > set through the daemon configuration, and _node_ labels, set from a
	 * > manager node in the Swarm. Node labels are not included in this
	 * > field. Node labels can be retrieved using the `/nodes/(id)` endpoint
	 * > on a manager node in the Swarm.
	 * @example storage=ssd,production
	 */
	Labels?: readonly string[];
	/**
	 * Indicates if experimental features are enabled on the daemon.
	 * @example true
	 */
	ExperimentalBuild?: boolean;
	/**
	 * Version string of the daemon.
	 * @example 24.0.2
	 */
	ServerVersion?: string;
	Runtimes?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Name of the default OCI runtime that is used when starting containers.
	 *
	 * The default can be overridden per-container at create time.
	 * @default runc
	 * @example runc
	 */
	DefaultRuntime?: string;
	Swarm?: {
		/**
		 * Unique identifier of for this node in the swarm.
		 * @example k67qz4598weg5unwwffg6z1m1
		 */
		NodeID?: string;
		/**
		 * IP address at which this node can be reached by other nodes in the
		 * swarm.
		 * @example 10.0.0.46
		 */
		NodeAddr?: string;
		/**
		 * Current local status of this node.
		 * @enum ,inactive,pending,active,error,locked
		 * @example active
		 */
		LocalNodeState?:
			| ""
			| "inactive"
			| "pending"
			| "active"
			| "error"
			| "locked";
		ControlAvailable?: boolean;
		Error?: string;
		/**
		 * List of ID's and addresses of other managers in the swarm.
		 * @example [object Object],[object Object],[object Object]
		 */
		RemoteManagers?: readonly {
			NodeID?: string;
			Addr?: string;
		}[];
		/**
		 * Total number of nodes in the swarm.
		 * @example 4
		 */
		Nodes?: number | null;
		/**
		 * Total number of managers in the swarm.
		 * @example 3
		 */
		Managers?: number | null;
		Cluster?: {
			/**
			 * The ID of the swarm.
			 * @example abajmipo7b4xz5ip2nrla6b11
			 */
			ID?: string;
			Version?: {
				Index?: number;
			};
			/**
			 * Date and time at which the swarm was initialised in
			 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
			 * @example 2016-08-18T10:44:24.496525531Z
			 */
			CreatedAt?: string;
			/**
			 * Date and time at which the swarm was last updated in
			 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
			 * @example 2017-08-09T07:09:37.632105588Z
			 */
			UpdatedAt?: string;
			Spec?: {
				/**
				 * Name of the swarm.
				 * @example default
				 */
				Name?: string;
				Labels?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				Orchestration?: {
					/**
					 * The number of historic tasks to keep per instance or node. If
					 * negative, never remove completed or failed tasks.
					 * @example 10
					 */
					TaskHistoryRetentionLimit?: number;
				};
				Raft?: {
					/**
					 * The number of log entries between snapshots.
					 * @example 10000
					 */
					SnapshotInterval?: number;
					KeepOldSnapshots?: number;
					/**
					 * The number of log entries to keep around to sync up slow followers
					 * after a snapshot is created.
					 * @example 500
					 */
					LogEntriesForSlowFollowers?: number;
					/**
					 * The number of ticks that a follower will wait for a message from
					 * the leader before becoming a candidate and starting an election.
					 * `ElectionTick` must be greater than `HeartbeatTick`.
					 *
					 * A tick currently defaults to one second, so these translate
					 * directly to seconds currently, but this is NOT guaranteed.
					 * @example 3
					 */
					ElectionTick?: number;
					/**
					 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
					 * the leader will send a heartbeat to the followers.
					 *
					 * A tick currently defaults to one second, so these translate
					 * directly to seconds currently, but this is NOT guaranteed.
					 * @example 1
					 */
					HeartbeatTick?: number;
				};
				Dispatcher?: {
					/**
					 * The delay for an agent to send a heartbeat to the dispatcher.
					 * @example 5000000000
					 */
					HeartbeatPeriod?: number;
				};
				CAConfig?: {
					/**
					 * The duration node certificates are issued for.
					 * @example 7776000000000000
					 */
					NodeCertExpiry?: number;
					ExternalCAs?: readonly {
						/**
						 * Protocol for communication with the external CA (currently
						 * only `cfssl` is supported).
						 * @default cfssl
						 * @enum cfssl
						 */
						Protocol?: "cfssl";
						URL?: string;
						Options?: Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>;
						CACert?: string;
					}[];
					SigningCACert?: string;
					SigningCAKey?: string;
					ForceRotate?: number;
				};
				EncryptionConfig?: {
					/**
					 * If set, generate a key and use it to lock data stored on the
					 * managers.
					 * @example false
					 */
					AutoLockManagers?: boolean;
				};
				TaskDefaults?: {
					LogDriver?: {
						/**
						 * The log driver to use as a default for new tasks.
						 * @example json-file
						 */
						Name?: string;
						Options?: Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>;
					};
				};
			};
			/**
			 * Information about the issuer of leaf TLS certificates and the trusted root
			 * CA certificate.
			 * @example [object Object]
			 */
			TLSInfo?: {
				TrustRoot?: string;
				CertIssuerSubject?: string;
				CertIssuerPublicKey?: string;
			};
			/**
			 * Whether there is currently a root CA rotation in progress for the swarm
			 * @example false
			 */
			RootRotationInProgress?: boolean;
			/**
			 * DataPathPort specifies the data path port number for data traffic.
			 * Acceptable port range is 1024 to 49151.
			 * If no port is set or is set to 0, the default port (4789) is used.
			 * @example 4789
			 */
			DataPathPort?: number;
			DefaultAddrPool?: readonly string[];
			/**
			 * SubnetSize specifies the subnet size of the networks created from the
			 * default subnet pool.
			 * @example 24
			 */
			SubnetSize?: number;
		};
	};
	/**
	 * Indicates if live restore is enabled.
	 *
	 * If enabled, containers are kept running when the daemon is shutdown
	 * or upon daemon start if running containers are detected.
	 * @example false
	 */
	LiveRestoreEnabled?: boolean;
	/**
	 * Represents the isolation technology to use as a default for containers.
	 * The supported values are platform-specific.
	 *
	 * If no isolation value is specified on daemon start, on Windows client,
	 * the default is `hyperv`, and on Windows server, the default is `process`.
	 *
	 * This option is currently not used on other platforms.
	 * @default default
	 * @enum default,hyperv,process,
	 */
	Isolation?: "default" | "hyperv" | "process" | "";
	/**
	 * Name and, optional, path of the `docker-init` binary.
	 *
	 * If the path is omitted, the daemon searches the host's `$PATH` for the
	 * binary and uses the first result.
	 * @example docker-init
	 */
	InitBinary?: string;
	ContainerdCommit?: {
		/**
		 * Actual commit ID of external tool.
		 * @example cfb82a876ecc11b5ca0977d1733adbe58599088a
		 */
		ID?: string;
		/**
		 * Commit ID of external tool expected by dockerd as set at build time.
		 * @example 2d41c047c83e09a6d61d464906feb2a2f3c52aa4
		 */
		Expected?: string;
	};
	RuncCommit?: {
		/**
		 * Actual commit ID of external tool.
		 * @example cfb82a876ecc11b5ca0977d1733adbe58599088a
		 */
		ID?: string;
		/**
		 * Commit ID of external tool expected by dockerd as set at build time.
		 * @example 2d41c047c83e09a6d61d464906feb2a2f3c52aa4
		 */
		Expected?: string;
	};
	InitCommit?: {
		/**
		 * Actual commit ID of external tool.
		 * @example cfb82a876ecc11b5ca0977d1733adbe58599088a
		 */
		ID?: string;
		/**
		 * Commit ID of external tool expected by dockerd as set at build time.
		 * @example 2d41c047c83e09a6d61d464906feb2a2f3c52aa4
		 */
		Expected?: string;
	};
	/**
	 * List of security features that are enabled on the daemon, such as
	 * apparmor, seccomp, SELinux, user-namespaces (userns), rootless and
	 * no-new-privileges.
	 *
	 * Additional configuration options for each security feature may
	 * be present, and are included as a comma-separated list of key/value
	 * pairs.
	 * @example name=apparmor,name=seccomp,profile=default,name=selinux,name=userns,name=rootless
	 */
	SecurityOptions?: readonly string[];
	/**
	 * Reports a summary of the product license on the daemon.
	 *
	 * If a commercial license has been applied to the daemon, information
	 * such as number of nodes, and expiration are included.
	 * @example Community Engine
	 */
	ProductLicense?: string;
	DefaultAddressPools?: readonly {
		/**
		 * The network address in CIDR format
		 * @example 10.10.0.0/16
		 */
		Base?: string;
		/**
		 * The network pool size
		 * @example 24
		 */
		Size?: number;
	}[];
	/**
	 * List of warnings / informational messages about missing features, or
	 * issues related to the daemon configuration.
	 *
	 * These messages can be printed by the client as information to the user.
	 * @example WARNING: No memory limit support
	 */
	Warnings?: readonly string[];
};
export type SystemVersionCommandInput = never;
export type SystemVersionCommandOutput = {
	Platform?: {
		Name: string;
	};
	Components?: readonly {
		/**
		 * Name of the component
		 * @example Engine
		 */
		Name: string;
		/**
		 * Version of the component
		 * @example 19.03.12
		 */
		Version: string;
		Details?: {};
	}[];
	/**
	 * The version of the daemon
	 * @example 19.03.12
	 */
	Version?: string;
	/**
	 * The default (and highest) API version that is supported by the daemon
	 * @example 1.40
	 */
	ApiVersion?: string;
	/**
	 * The minimum API version that is supported by the daemon
	 * @example 1.12
	 */
	MinAPIVersion?: string;
	/**
	 * The Git commit of the source code that was used to build the daemon
	 * @example 48a66213fe
	 */
	GitCommit?: string;
	/**
	 * The version Go used to compile the daemon, and the version of the Go
	 * runtime in use.
	 * @example go1.13.14
	 */
	GoVersion?: string;
	/**
	 * The operating system that the daemon is running on ("linux" or "windows")
	 * @example linux
	 */
	Os?: string;
	/**
	 * The architecture that the daemon is running on
	 * @example amd64
	 */
	Arch?: string;
	/**
	 * The kernel version (`uname -r`) that the daemon is running on.
	 *
	 * This field is omitted when empty.
	 * @example 4.19.76-linuxkit
	 */
	KernelVersion?: string;
	/**
	 * Indicates if the daemon is started with experimental features enabled.
	 *
	 * This field is omitted when empty / false.
	 * @example true
	 */
	Experimental?: boolean;
	/**
	 * The date and time that the daemon was compiled.
	 * @example 2020-06-22T15:49:27.000000000+00:00
	 */
	BuildTime?: string;
};
export type SystemPingCommandInput = never;
export type SystemPingHeadCommandInput = never;
export type ImageCommitCommandQuery = {
	container?: string | undefined;
	repo?: string | undefined;
	tag?: string | undefined;
	comment?: string | undefined;
	author?: string | undefined;
	pause?: "true" | "false";
	changes?: string | undefined;
};
type ImageCommitJsonBody = {
	/**
	 * The hostname to use for the container, as a valid RFC 1123 hostname.
	 * @example 439f4e91bd1d
	 */
	Hostname?: string;
	Domainname?: string;
	User?: string;
	AttachStdin?: boolean;
	/**
	 * Whether to attach to `stdout`.
	 * @default true
	 */
	AttachStdout?: boolean;
	/**
	 * Whether to attach to `stderr`.
	 * @default true
	 */
	AttachStderr?: boolean;
	ExposedPorts?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Tty?: boolean;
	OpenStdin?: boolean;
	StdinOnce?: boolean;
	/**
	 * A list of environment variables to set inside the container in the
	 * form `["VAR=value", ...]`. A variable without `=` is removed from the
	 * environment, rather than to have an empty value.
	 * @example PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
	 */
	Env?: readonly string[];
	/**
	 * Command to run specified as a string or an array of strings.
	 * @example /bin/sh
	 */
	Cmd?: readonly string[];
	Healthcheck?: {
		Test?: readonly string[];
		Interval?: number;
		Timeout?: number;
		Retries?: number;
		StartPeriod?: number;
	};
	/**
	 * Command is already escaped (Windows only)
	 * @example false
	 */
	ArgsEscaped?: boolean | null;
	/**
	 * The name (or reference) of the image to use when creating the container,
	 * or which was used when the container was created.
	 * @example example-image:1.0
	 */
	Image?: string;
	Volumes?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * The working directory for commands to run in.
	 * @example /public/
	 */
	WorkingDir?: string;
	/**
	 * The entry point for the container as a string or an array of strings.
	 *
	 * If the array consists of exactly one empty string (`[""]`) then the
	 * entry point is reset to system default (i.e., the entry point used by
	 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
	 * @example
	 */
	Entrypoint?: readonly string[];
	NetworkDisabled?: boolean | null;
	MacAddress?: string;
	/**
	 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
	 * @example
	 */
	OnBuild?: readonly string[];
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Signal to stop a container as a string or unsigned integer.
	 * @example SIGTERM
	 */
	StopSignal?: string;
	StopTimeout?: number | null;
	/**
	 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
	 * @example /bin/sh,-c
	 */
	Shell?: readonly string[];
};
export type ImageCommitCommandBody = ImageCommitJsonBody;
export type ImageCommitCommandInput = ImageCommitJsonBody &
	ImageCommitCommandQuery;
export type ImageCommitCommandOutput = {
	Id: string;
};
export type SystemEventsCommandQuery = {
	since?: string | undefined;
	until?: string | undefined;
	filters?: string | undefined;
};
export type SystemEventsCommandInput = SystemEventsCommandQuery;
export type SystemEventsCommandOutput = {
	/**
	 * The type of object emitting the event
	 * @enum builder,config,container,daemon,image,network,node,plugin,secret,service,volume
	 * @example container
	 */
	Type?:
		| "builder"
		| "config"
		| "container"
		| "daemon"
		| "image"
		| "network"
		| "node"
		| "plugin"
		| "secret"
		| "service"
		| "volume";
	/**
	 * The type of event
	 * @example create
	 */
	Action?: string;
	Actor?: {
		/**
		 * The ID of the object emitting the event
		 * @example ede54ee1afda366ab42f824e8a5ffd195155d853ceaec74a927f249ea270c743
		 */
		ID?: string;
		Attributes?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	/**
	 * Scope of the event. Engine events are `local` scope. Cluster (Swarm)
	 * events are `swarm` scope.
	 * @enum local,swarm
	 */
	scope?: "local" | "swarm";
	/**
	 * Timestamp of event
	 * @example 1629574695
	 */
	time?: number;
	/**
	 * Timestamp of event, with nanosecond accuracy
	 * @example 1629574695515050000
	 */
	timeNano?: number;
};
export type SystemDataUsageCommandQuery = {
	type?: readonly ("container" | "image" | "volume" | "build-cache")[];
};
export type SystemDataUsageCommandInput = SystemDataUsageCommandQuery;
export type SystemDataUsageCommandOutput = {
	LayersSize?: number;
	Images?: readonly {
		/**
		 * ID is the content-addressable ID of an image.
		 *
		 * This identifier is a content-addressable digest calculated from the
		 * image's configuration (which includes the digests of layers used by
		 * the image).
		 *
		 * Note that this digest differs from the `RepoDigests` below, which
		 * holds digests of image manifests that reference the image.
		 * @example sha256:ec3f0931a6e6b6855d76b2d7b0be30e81860baccd891b2e243280bf1cd8ad710
		 */
		Id: string;
		/**
		 * ID of the parent image.
		 *
		 * Depending on how the image was created, this field may be empty and
		 * is only set for images that were built/created locally. This field
		 * is empty if the image was pulled from an image registry.
		 * @example
		 */
		ParentId: string;
		/**
		 * List of image names/tags in the local image cache that reference this
		 * image.
		 *
		 * Multiple image tags can refer to the same image, and this list may be
		 * empty if no tags reference the image, in which case the image is
		 * "untagged", in which case it can still be referenced by its ID.
		 * @example example:1.0,example:latest,example:stable,internal.registry.example.com:5000/example:1.0
		 */
		RepoTags: readonly string[];
		/**
		 * List of content-addressable digests of locally available image manifests
		 * that the image is referenced from. Multiple manifests can refer to the
		 * same image.
		 *
		 * These digests are usually only available if the image was either pulled
		 * from a registry, or if the image was pushed to a registry, which is when
		 * the manifest is generated and its digest calculated.
		 * @example example@sha256:afcc7f1ac1b49db317a7196c902e61c6c3c4607d63599ee1a82d702d249a0ccb,internal.registry.example.com:5000/example@sha256:b69959407d21e8a062e0416bf13405bb2b71ed7a84dde4158ebafacfa06f5578
		 */
		RepoDigests: readonly string[];
		/**
		 * Date and time at which the image was created as a Unix timestamp
		 * (number of seconds since EPOCH).
		 * @example 1644009612
		 */
		Created: number;
		/**
		 * Total size of the image including all layers it is composed of.
		 * @example 172064416
		 */
		Size: number;
		/**
		 * Total size of image layers that are shared between this image and other
		 * images.
		 *
		 * This size is not calculated by default. `-1` indicates that the value
		 * has not been set / calculated.
		 * @example 1239828
		 */
		SharedSize: number;
		/**
		 * Total size of the image including all layers it is composed of.
		 *
		 * In versions of Docker before v1.10, this field was calculated from
		 * the image itself and all of its parent images. Images are now stored
		 * self-contained, and no longer use a parent-chain, making this field
		 * an equivalent of the Size field.
		 *
		 * Deprecated: this field is kept for backward compatibility, and will be
		 * removed in API v1.44.
		 * @example 172064416
		 */
		VirtualSize?: number;
		Labels: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Number of containers using this image. Includes both stopped and running
		 * containers.
		 *
		 * This size is not calculated by default, and depends on which API endpoint
		 * is used. `-1` indicates that the value has not been set / calculated.
		 * @example 2
		 */
		Containers: number;
	}[];
	Containers?: readonly {
		Id?: string;
		Names?: readonly string[];
		Image?: string;
		ImageID?: string;
		Command?: string;
		Created?: number;
		Ports?: readonly {
			IP?: string;
			PrivatePort: number;
			PublicPort?: number;
			Type: "tcp" | "udp" | "sctp";
		}[];
		SizeRw?: number;
		SizeRootFs?: number;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		State?: string;
		Status?: string;
		HostConfig?: {
			NetworkMode?: string;
		};
		NetworkSettings?: {
			Networks?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
		Mounts?: readonly {
			/**
			 * The mount type:
			 *
			 * - `bind` a mount of a file or directory from the host into the container.
			 * - `volume` a docker volume with the given `Name`.
			 * - `tmpfs` a `tmpfs`.
			 * - `npipe` a named pipe from the host into the container.
			 * - `cluster` a Swarm cluster volume
			 * @enum bind,volume,tmpfs,npipe,cluster
			 * @example volume
			 */
			Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
			/**
			 * Name is the name reference to the underlying data defined by `Source`
			 * e.g., the volume name.
			 * @example myvolume
			 */
			Name?: string;
			/**
			 * Source location of the mount.
			 *
			 * For volumes, this contains the storage location of the volume (within
			 * `/var/lib/docker/volumes/`). For bind-mounts, and `npipe`, this contains
			 * the source (host) part of the bind-mount. For `tmpfs` mount points, this
			 * field is empty.
			 * @example /var/lib/docker/volumes/myvolume/_data
			 */
			Source?: string;
			/**
			 * Destination is the path relative to the container root (`/`) where
			 * the `Source` is mounted inside the container.
			 * @example /usr/share/nginx/html/
			 */
			Destination?: string;
			/**
			 * Driver is the volume driver used to create the volume (if it is a volume).
			 * @example local
			 */
			Driver?: string;
			/**
			 * Mode is a comma separated list of options supplied by the user when
			 * creating the bind/volume mount.
			 *
			 * The default is platform-specific (`"z"` on Linux, empty on Windows).
			 * @example z
			 */
			Mode?: string;
			/**
			 * Whether the mount is mounted writable (read-write).
			 * @example true
			 */
			RW?: boolean;
			/**
			 * Propagation describes how mounts are propagated from the host into the
			 * mount point, and vice-versa. Refer to the [Linux kernel
			 * documentation](https://www.kernel.org/doc/Documentation/filesystems/sharedsubtree.txt)
			 * for details. This field is not used on Windows.
			 * @example
			 */
			Propagation?: string;
		}[];
	}[];
	Volumes?: readonly {
		/**
		 * Name of the volume.
		 * @example tardis
		 */
		Name: string;
		/**
		 * Name of the volume driver used by the volume.
		 * @example custom
		 */
		Driver: string;
		/**
		 * Mount path of the volume on the host.
		 * @example /var/lib/docker/volumes/tardis
		 */
		Mountpoint: string;
		/**
		 * Date/Time the volume was created.
		 * @example 2016-06-07T20:31:11.853781916Z
		 */
		CreatedAt?: string;
		Status?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Labels: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * The level at which the volume exists. Either `global` for cluster-wide,
		 * or `local` for machine level.
		 * @default local
		 * @enum local,global
		 * @example local
		 */
		Scope: "local" | "global";
		ClusterVolume?: {
			ID?: string;
			Version?: {
				Index?: number;
			};
			CreatedAt?: string;
			UpdatedAt?: string;
			Spec?: {
				Group?: string;
				AccessMode?: {
					/**
					 * The set of nodes this volume can be used on at one time.
					 * - `single` The volume may only be scheduled to one node at a time.
					 * - `multi` the volume may be scheduled to any supported number of nodes at a
					 * time.
					 * @default single
					 * @enum single,multi
					 */
					Scope?: "single" | "multi";
					/**
					 * The number and way that different tasks can use this volume
					 * at one time.
					 * - `none` The volume may only be used by one task at a time.
					 * - `readonly` The volume may be used by any number of tasks, but they all
					 * must mount the volume as readonly
					 * - `onewriter` The volume may be used by any number of tasks, but only one
					 * may mount it as read/write.
					 * - `all` The volume may have any number of readers and writers.
					 * @default none
					 * @enum none,readonly,onewriter,all
					 */
					Sharing?: "none" | "readonly" | "onewriter" | "all";
					MountVolume?: {};
					Secrets?: readonly {
						Key?: string;
						Secret?: string;
					}[];
					AccessibilityRequirements?: {
						Requisite?: readonly Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>[];
						Preferred?: readonly Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>[];
					};
					CapacityRange?: {
						RequiredBytes?: number;
						LimitBytes?: number;
					};
					/**
					 * The availability of the volume for use in tasks.
					 * - `active` The volume is fully available for scheduling on the cluster
					 * - `pause` No new workloads should use the volume, but existing workloads
					 * are not stopped.
					 * - `drain` All workloads using this volume should be stopped and
					 * rescheduled, and no new ones should be started.
					 * @default active
					 * @enum active,pause,drain
					 */
					Availability?: "active" | "pause" | "drain";
				};
			};
			Info?: {
				CapacityBytes?: number;
				VolumeContext?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				VolumeID?: string;
				AccessibleTopology?: readonly Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>[];
			};
			PublishStatus?: readonly {
				NodeID?: string;
				/**
				 * The published state of the volume.
				 * * `pending-publish` The volume should be published to this node, but the
				 * call to the controller plugin to do so has not yet been successfully
				 * completed.
				 * * `published` The volume is published successfully to the node.
				 * * `pending-node-unpublish` The volume should be unpublished from the node,
				 * and the manager is awaiting confirmation from the worker that it has done
				 * so.
				 * * `pending-controller-unpublish` The volume is successfully unpublished
				 * from the node, but has not yet been successfully unpublished on the
				 * controller.
				 * @enum pending-publish,published,pending-node-unpublish,pending-controller-unpublish
				 */
				State?:
					| "pending-publish"
					| "published"
					| "pending-node-unpublish"
					| "pending-controller-unpublish";
				PublishContext?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			}[];
		};
		Options: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		UsageData?: {
			/**
			 * Amount of disk space used by the volume (in bytes). This information
			 * is only available for volumes created with the `"local"` volume
			 * driver. For volumes created with other volume drivers, this field
			 * is set to `-1` ("not available")
			 * @default -1
			 */
			Size: number;
			/**
			 * The number of containers referencing this volume. This field
			 * is set to `-1` if the reference-count is not available.
			 * @default -1
			 */
			RefCount: number;
		};
	}[];
	BuildCache?: readonly {
		/**
		 * Unique ID of the build cache record.
		 * @example ndlpt0hhvkqcdfkputsk4cq9c
		 */
		ID?: string;
		/**
		 * ID of the parent build cache record.
		 *
		 * > **Deprecated**: This field is deprecated, and omitted if empty.
		 * @example
		 */
		Parent?: string;
		/**
		 * List of parent build cache record IDs.
		 * @example hw53o5aio51xtltp5xjp8v7fx
		 */
		Parents?: readonly string[];
		/**
		 * Cache record type.
		 * @enum internal,frontend,source.local,source.git.checkout,exec.cachemount,regular
		 * @example regular
		 */
		Type?:
			| "internal"
			| "frontend"
			| "source.local"
			| "source.git.checkout"
			| "exec.cachemount"
			| "regular";
		/**
		 * Description of the build-step that produced the build cache.
		 * @example mount / from exec /bin/sh -c echo 'Binary::apt::APT::Keep-Downloaded-Packages "true";' > /etc/apt/apt.conf.d/keep-cache
		 */
		Description?: string;
		/**
		 * Indicates if the build cache is in use.
		 * @example false
		 */
		InUse?: boolean;
		/**
		 * Indicates if the build cache is shared.
		 * @example true
		 */
		Shared?: boolean;
		/**
		 * Amount of disk space used by the build cache (in bytes).
		 * @example 51
		 */
		Size?: number;
		/**
		 * Date and time at which the build cache was created in
		 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
		 * @example 2016-08-18T10:44:24.496525531Z
		 */
		CreatedAt?: string;
		/**
		 * Date and time at which the build cache was last used in
		 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
		 * @example 2017-08-09T07:09:37.632105588Z
		 */
		LastUsedAt?: string;
		UsageCount?: number;
	}[];
};
export type ImageGetCommandParams = {
	name: string;
};
export type ImageGetCommandInput = ImageGetCommandParams;
export type ImageGetAllCommandQuery = {
	names?: readonly string[] | undefined;
};
export type ImageGetAllCommandInput = ImageGetAllCommandQuery;
export type ImageLoadCommandQuery = {
	quiet?: "true" | "false";
};
type ImageLoadCommandBodyApplicationXTar = NonNullable<RequestInit["body"]>;
export type ImageLoadCommandBodyNonJson = {
	body: ImageLoadCommandBodyApplicationXTar;
};
export type ImageLoadCommandInput = ImageLoadCommandBodyNonJson &
	ImageLoadCommandQuery;
type ContainerExecJsonBody = {
	AttachStdin?: boolean;
	AttachStdout?: boolean;
	AttachStderr?: boolean;
	ConsoleSize?: readonly number[];
	DetachKeys?: string;
	Tty?: boolean;
	Env?: readonly string[];
	Cmd?: readonly string[];
	Privileged?: boolean;
	User?: string;
	WorkingDir?: string;
};
export type ContainerExecCommandParams = {
	id: string;
};
export type ContainerExecCommandBody = ContainerExecJsonBody;
export type ContainerExecCommandInput = ContainerExecJsonBody &
	ContainerExecCommandParams;
export type ContainerExecCommandOutput = {
	Id: string;
};
type ExecStartJsonBody = {
	Detach?: boolean;
	Tty?: boolean;
	ConsoleSize?: readonly number[];
};
export type ExecStartCommandParams = {
	id: string;
};
export type ExecStartCommandBody = ExecStartJsonBody;
export type ExecStartCommandInput = ExecStartJsonBody & ExecStartCommandParams;
export type ExecResizeCommandQuery = {
	h: `${number}`;
	w: `${number}`;
};
export type ExecResizeCommandParams = {
	id: string;
};
export type ExecResizeCommandInput = ExecResizeCommandParams &
	ExecResizeCommandQuery;
export type ExecInspectCommandParams = {
	id: string;
};
export type ExecInspectCommandInput = ExecInspectCommandParams;
export type ExecInspectCommandOutput = {
	CanRemove?: boolean;
	DetachKeys?: string;
	ID?: string;
	Running?: boolean;
	ExitCode?: number;
	ProcessConfig?: {
		privileged?: boolean;
		user?: string;
		tty?: boolean;
		entrypoint?: string;
		arguments?: readonly string[];
	};
	OpenStdin?: boolean;
	OpenStderr?: boolean;
	OpenStdout?: boolean;
	ContainerID?: string;
	Pid?: number;
};
export type VolumeListCommandQuery = {
	filters?: string | undefined;
};
export type VolumeListCommandInput = VolumeListCommandQuery;
export type VolumeListCommandOutput = {
	Volumes?: readonly {
		/**
		 * Name of the volume.
		 * @example tardis
		 */
		Name: string;
		/**
		 * Name of the volume driver used by the volume.
		 * @example custom
		 */
		Driver: string;
		/**
		 * Mount path of the volume on the host.
		 * @example /var/lib/docker/volumes/tardis
		 */
		Mountpoint: string;
		/**
		 * Date/Time the volume was created.
		 * @example 2016-06-07T20:31:11.853781916Z
		 */
		CreatedAt?: string;
		Status?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Labels: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * The level at which the volume exists. Either `global` for cluster-wide,
		 * or `local` for machine level.
		 * @default local
		 * @enum local,global
		 * @example local
		 */
		Scope: "local" | "global";
		ClusterVolume?: {
			ID?: string;
			Version?: {
				Index?: number;
			};
			CreatedAt?: string;
			UpdatedAt?: string;
			Spec?: {
				Group?: string;
				AccessMode?: {
					/**
					 * The set of nodes this volume can be used on at one time.
					 * - `single` The volume may only be scheduled to one node at a time.
					 * - `multi` the volume may be scheduled to any supported number of nodes at a
					 * time.
					 * @default single
					 * @enum single,multi
					 */
					Scope?: "single" | "multi";
					/**
					 * The number and way that different tasks can use this volume
					 * at one time.
					 * - `none` The volume may only be used by one task at a time.
					 * - `readonly` The volume may be used by any number of tasks, but they all
					 * must mount the volume as readonly
					 * - `onewriter` The volume may be used by any number of tasks, but only one
					 * may mount it as read/write.
					 * - `all` The volume may have any number of readers and writers.
					 * @default none
					 * @enum none,readonly,onewriter,all
					 */
					Sharing?: "none" | "readonly" | "onewriter" | "all";
					MountVolume?: {};
					Secrets?: readonly {
						Key?: string;
						Secret?: string;
					}[];
					AccessibilityRequirements?: {
						Requisite?: readonly Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>[];
						Preferred?: readonly Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>[];
					};
					CapacityRange?: {
						RequiredBytes?: number;
						LimitBytes?: number;
					};
					/**
					 * The availability of the volume for use in tasks.
					 * - `active` The volume is fully available for scheduling on the cluster
					 * - `pause` No new workloads should use the volume, but existing workloads
					 * are not stopped.
					 * - `drain` All workloads using this volume should be stopped and
					 * rescheduled, and no new ones should be started.
					 * @default active
					 * @enum active,pause,drain
					 */
					Availability?: "active" | "pause" | "drain";
				};
			};
			Info?: {
				CapacityBytes?: number;
				VolumeContext?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				VolumeID?: string;
				AccessibleTopology?: readonly Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>[];
			};
			PublishStatus?: readonly {
				NodeID?: string;
				/**
				 * The published state of the volume.
				 * * `pending-publish` The volume should be published to this node, but the
				 * call to the controller plugin to do so has not yet been successfully
				 * completed.
				 * * `published` The volume is published successfully to the node.
				 * * `pending-node-unpublish` The volume should be unpublished from the node,
				 * and the manager is awaiting confirmation from the worker that it has done
				 * so.
				 * * `pending-controller-unpublish` The volume is successfully unpublished
				 * from the node, but has not yet been successfully unpublished on the
				 * controller.
				 * @enum pending-publish,published,pending-node-unpublish,pending-controller-unpublish
				 */
				State?:
					| "pending-publish"
					| "published"
					| "pending-node-unpublish"
					| "pending-controller-unpublish";
				PublishContext?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			}[];
		};
		Options: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		UsageData?: {
			/**
			 * Amount of disk space used by the volume (in bytes). This information
			 * is only available for volumes created with the `"local"` volume
			 * driver. For volumes created with other volume drivers, this field
			 * is set to `-1` ("not available")
			 * @default -1
			 */
			Size: number;
			/**
			 * The number of containers referencing this volume. This field
			 * is set to `-1` if the reference-count is not available.
			 * @default -1
			 */
			RefCount: number;
		};
	}[];
	/**
	 * Warnings that occurred when fetching the list of volumes.
	 * @example
	 */
	Warnings?: readonly string[];
};
type VolumeCreateJsonBody = {
	/**
	 * The new volume's name. If not specified, Docker generates a name.
	 * @example tardis
	 */
	Name?: string;
	/**
	 * Name of the volume driver to use.
	 * @default local
	 * @example custom
	 */
	Driver?: string;
	DriverOpts?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	ClusterVolumeSpec?: {
		Group?: string;
		AccessMode?: {
			/**
			 * The set of nodes this volume can be used on at one time.
			 * - `single` The volume may only be scheduled to one node at a time.
			 * - `multi` the volume may be scheduled to any supported number of nodes at a
			 * time.
			 * @default single
			 * @enum single,multi
			 */
			Scope?: "single" | "multi";
			/**
			 * The number and way that different tasks can use this volume
			 * at one time.
			 * - `none` The volume may only be used by one task at a time.
			 * - `readonly` The volume may be used by any number of tasks, but they all
			 * must mount the volume as readonly
			 * - `onewriter` The volume may be used by any number of tasks, but only one
			 * may mount it as read/write.
			 * - `all` The volume may have any number of readers and writers.
			 * @default none
			 * @enum none,readonly,onewriter,all
			 */
			Sharing?: "none" | "readonly" | "onewriter" | "all";
			MountVolume?: {};
			Secrets?: readonly {
				Key?: string;
				Secret?: string;
			}[];
			AccessibilityRequirements?: {
				Requisite?: readonly Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>[];
				Preferred?: readonly Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>[];
			};
			CapacityRange?: {
				RequiredBytes?: number;
				LimitBytes?: number;
			};
			/**
			 * The availability of the volume for use in tasks.
			 * - `active` The volume is fully available for scheduling on the cluster
			 * - `pause` No new workloads should use the volume, but existing workloads
			 * are not stopped.
			 * - `drain` All workloads using this volume should be stopped and
			 * rescheduled, and no new ones should be started.
			 * @default active
			 * @enum active,pause,drain
			 */
			Availability?: "active" | "pause" | "drain";
		};
	};
};
export type VolumeCreateCommandBody = VolumeCreateJsonBody;
export type VolumeCreateCommandInput = VolumeCreateJsonBody;
export type VolumeCreateCommandOutput = {
	/**
	 * Name of the volume.
	 * @example tardis
	 */
	Name: string;
	/**
	 * Name of the volume driver used by the volume.
	 * @example custom
	 */
	Driver: string;
	/**
	 * Mount path of the volume on the host.
	 * @example /var/lib/docker/volumes/tardis
	 */
	Mountpoint: string;
	/**
	 * Date/Time the volume was created.
	 * @example 2016-06-07T20:31:11.853781916Z
	 */
	CreatedAt?: string;
	Status?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Labels: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * The level at which the volume exists. Either `global` for cluster-wide,
	 * or `local` for machine level.
	 * @default local
	 * @enum local,global
	 * @example local
	 */
	Scope: "local" | "global";
	ClusterVolume?: {
		ID?: string;
		Version?: {
			Index?: number;
		};
		CreatedAt?: string;
		UpdatedAt?: string;
		Spec?: {
			Group?: string;
			AccessMode?: {
				/**
				 * The set of nodes this volume can be used on at one time.
				 * - `single` The volume may only be scheduled to one node at a time.
				 * - `multi` the volume may be scheduled to any supported number of nodes at a
				 * time.
				 * @default single
				 * @enum single,multi
				 */
				Scope?: "single" | "multi";
				/**
				 * The number and way that different tasks can use this volume
				 * at one time.
				 * - `none` The volume may only be used by one task at a time.
				 * - `readonly` The volume may be used by any number of tasks, but they all
				 * must mount the volume as readonly
				 * - `onewriter` The volume may be used by any number of tasks, but only one
				 * may mount it as read/write.
				 * - `all` The volume may have any number of readers and writers.
				 * @default none
				 * @enum none,readonly,onewriter,all
				 */
				Sharing?: "none" | "readonly" | "onewriter" | "all";
				MountVolume?: {};
				Secrets?: readonly {
					Key?: string;
					Secret?: string;
				}[];
				AccessibilityRequirements?: {
					Requisite?: readonly Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>[];
					Preferred?: readonly Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>[];
				};
				CapacityRange?: {
					RequiredBytes?: number;
					LimitBytes?: number;
				};
				/**
				 * The availability of the volume for use in tasks.
				 * - `active` The volume is fully available for scheduling on the cluster
				 * - `pause` No new workloads should use the volume, but existing workloads
				 * are not stopped.
				 * - `drain` All workloads using this volume should be stopped and
				 * rescheduled, and no new ones should be started.
				 * @default active
				 * @enum active,pause,drain
				 */
				Availability?: "active" | "pause" | "drain";
			};
		};
		Info?: {
			CapacityBytes?: number;
			VolumeContext?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			VolumeID?: string;
			AccessibleTopology?: readonly Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>[];
		};
		PublishStatus?: readonly {
			NodeID?: string;
			/**
			 * The published state of the volume.
			 * * `pending-publish` The volume should be published to this node, but the
			 * call to the controller plugin to do so has not yet been successfully
			 * completed.
			 * * `published` The volume is published successfully to the node.
			 * * `pending-node-unpublish` The volume should be unpublished from the node,
			 * and the manager is awaiting confirmation from the worker that it has done
			 * so.
			 * * `pending-controller-unpublish` The volume is successfully unpublished
			 * from the node, but has not yet been successfully unpublished on the
			 * controller.
			 * @enum pending-publish,published,pending-node-unpublish,pending-controller-unpublish
			 */
			State?:
				| "pending-publish"
				| "published"
				| "pending-node-unpublish"
				| "pending-controller-unpublish";
			PublishContext?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
	};
	Options: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	UsageData?: {
		/**
		 * Amount of disk space used by the volume (in bytes). This information
		 * is only available for volumes created with the `"local"` volume
		 * driver. For volumes created with other volume drivers, this field
		 * is set to `-1` ("not available")
		 * @default -1
		 */
		Size: number;
		/**
		 * The number of containers referencing this volume. This field
		 * is set to `-1` if the reference-count is not available.
		 * @default -1
		 */
		RefCount: number;
	};
};
export type VolumeInspectCommandParams = {
	name: string;
};
export type VolumeInspectCommandInput = VolumeInspectCommandParams;
export type VolumeInspectCommandOutput = {
	/**
	 * Name of the volume.
	 * @example tardis
	 */
	Name: string;
	/**
	 * Name of the volume driver used by the volume.
	 * @example custom
	 */
	Driver: string;
	/**
	 * Mount path of the volume on the host.
	 * @example /var/lib/docker/volumes/tardis
	 */
	Mountpoint: string;
	/**
	 * Date/Time the volume was created.
	 * @example 2016-06-07T20:31:11.853781916Z
	 */
	CreatedAt?: string;
	Status?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Labels: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * The level at which the volume exists. Either `global` for cluster-wide,
	 * or `local` for machine level.
	 * @default local
	 * @enum local,global
	 * @example local
	 */
	Scope: "local" | "global";
	ClusterVolume?: {
		ID?: string;
		Version?: {
			Index?: number;
		};
		CreatedAt?: string;
		UpdatedAt?: string;
		Spec?: {
			Group?: string;
			AccessMode?: {
				/**
				 * The set of nodes this volume can be used on at one time.
				 * - `single` The volume may only be scheduled to one node at a time.
				 * - `multi` the volume may be scheduled to any supported number of nodes at a
				 * time.
				 * @default single
				 * @enum single,multi
				 */
				Scope?: "single" | "multi";
				/**
				 * The number and way that different tasks can use this volume
				 * at one time.
				 * - `none` The volume may only be used by one task at a time.
				 * - `readonly` The volume may be used by any number of tasks, but they all
				 * must mount the volume as readonly
				 * - `onewriter` The volume may be used by any number of tasks, but only one
				 * may mount it as read/write.
				 * - `all` The volume may have any number of readers and writers.
				 * @default none
				 * @enum none,readonly,onewriter,all
				 */
				Sharing?: "none" | "readonly" | "onewriter" | "all";
				MountVolume?: {};
				Secrets?: readonly {
					Key?: string;
					Secret?: string;
				}[];
				AccessibilityRequirements?: {
					Requisite?: readonly Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>[];
					Preferred?: readonly Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>[];
				};
				CapacityRange?: {
					RequiredBytes?: number;
					LimitBytes?: number;
				};
				/**
				 * The availability of the volume for use in tasks.
				 * - `active` The volume is fully available for scheduling on the cluster
				 * - `pause` No new workloads should use the volume, but existing workloads
				 * are not stopped.
				 * - `drain` All workloads using this volume should be stopped and
				 * rescheduled, and no new ones should be started.
				 * @default active
				 * @enum active,pause,drain
				 */
				Availability?: "active" | "pause" | "drain";
			};
		};
		Info?: {
			CapacityBytes?: number;
			VolumeContext?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			VolumeID?: string;
			AccessibleTopology?: readonly Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>[];
		};
		PublishStatus?: readonly {
			NodeID?: string;
			/**
			 * The published state of the volume.
			 * * `pending-publish` The volume should be published to this node, but the
			 * call to the controller plugin to do so has not yet been successfully
			 * completed.
			 * * `published` The volume is published successfully to the node.
			 * * `pending-node-unpublish` The volume should be unpublished from the node,
			 * and the manager is awaiting confirmation from the worker that it has done
			 * so.
			 * * `pending-controller-unpublish` The volume is successfully unpublished
			 * from the node, but has not yet been successfully unpublished on the
			 * controller.
			 * @enum pending-publish,published,pending-node-unpublish,pending-controller-unpublish
			 */
			State?:
				| "pending-publish"
				| "published"
				| "pending-node-unpublish"
				| "pending-controller-unpublish";
			PublishContext?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
	};
	Options: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	UsageData?: {
		/**
		 * Amount of disk space used by the volume (in bytes). This information
		 * is only available for volumes created with the `"local"` volume
		 * driver. For volumes created with other volume drivers, this field
		 * is set to `-1` ("not available")
		 * @default -1
		 */
		Size: number;
		/**
		 * The number of containers referencing this volume. This field
		 * is set to `-1` if the reference-count is not available.
		 * @default -1
		 */
		RefCount: number;
	};
};
export type VolumeUpdateCommandQuery = {
	version: `${number}`;
};
type VolumeUpdateJsonBody = {
	Spec?: {
		Group?: string;
		AccessMode?: {
			/**
			 * The set of nodes this volume can be used on at one time.
			 * - `single` The volume may only be scheduled to one node at a time.
			 * - `multi` the volume may be scheduled to any supported number of nodes at a
			 * time.
			 * @default single
			 * @enum single,multi
			 */
			Scope?: "single" | "multi";
			/**
			 * The number and way that different tasks can use this volume
			 * at one time.
			 * - `none` The volume may only be used by one task at a time.
			 * - `readonly` The volume may be used by any number of tasks, but they all
			 * must mount the volume as readonly
			 * - `onewriter` The volume may be used by any number of tasks, but only one
			 * may mount it as read/write.
			 * - `all` The volume may have any number of readers and writers.
			 * @default none
			 * @enum none,readonly,onewriter,all
			 */
			Sharing?: "none" | "readonly" | "onewriter" | "all";
			MountVolume?: {};
			Secrets?: readonly {
				Key?: string;
				Secret?: string;
			}[];
			AccessibilityRequirements?: {
				Requisite?: readonly Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>[];
				Preferred?: readonly Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>[];
			};
			CapacityRange?: {
				RequiredBytes?: number;
				LimitBytes?: number;
			};
			/**
			 * The availability of the volume for use in tasks.
			 * - `active` The volume is fully available for scheduling on the cluster
			 * - `pause` No new workloads should use the volume, but existing workloads
			 * are not stopped.
			 * - `drain` All workloads using this volume should be stopped and
			 * rescheduled, and no new ones should be started.
			 * @default active
			 * @enum active,pause,drain
			 */
			Availability?: "active" | "pause" | "drain";
		};
	};
};
export type VolumeUpdateCommandParams = {
	name: string;
};
export type VolumeUpdateCommandBody = VolumeUpdateJsonBody;
export type VolumeUpdateCommandInput = VolumeUpdateJsonBody &
	VolumeUpdateCommandParams &
	VolumeUpdateCommandQuery;
export type VolumeDeleteCommandQuery = {
	force?: "true" | "false";
};
export type VolumeDeleteCommandParams = {
	name: string;
};
export type VolumeDeleteCommandInput = VolumeDeleteCommandParams &
	VolumeDeleteCommandQuery;
export type VolumePruneCommandQuery = {
	filters?: string | undefined;
};
export type VolumePruneCommandInput = VolumePruneCommandQuery;
export type VolumePruneCommandOutput = {
	VolumesDeleted?: readonly string[];
	SpaceReclaimed?: number;
};
export type NetworkListCommandQuery = {
	filters?: string | undefined;
};
export type NetworkListCommandInput = NetworkListCommandQuery;
export type NetworkListCommandOutput = readonly {
	/**
	 * Name of the network.
	 * @example my_network
	 */
	Name?: string;
	/**
	 * ID that uniquely identifies a network on a single machine.
	 * @example 7d86d31b1478e7cca9ebed7e73aa0fdeec46c5ca29497431d3007d2d9e15ed99
	 */
	Id?: string;
	/**
	 * Date and time at which the network was created in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2016-10-19T04:33:30.360899459Z
	 */
	Created?: string;
	/**
	 * The level at which the network exists (e.g. `swarm` for cluster-wide
	 * or `local` for machine level)
	 * @example local
	 */
	Scope?: string;
	/**
	 * The name of the driver used to create the network (e.g. `bridge`,
	 * `overlay`).
	 * @example overlay
	 */
	Driver?: string;
	/**
	 * Whether the network was created with IPv6 enabled.
	 * @example false
	 */
	EnableIPv6?: boolean;
	IPAM?: {
		/**
		 * Name of the IPAM driver to use.
		 * @default default
		 * @example default
		 */
		Driver?: string;
		Config?: readonly {
			Subnet?: string;
			IPRange?: string;
			Gateway?: string;
			AuxiliaryAddresses?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	/**
	 * Whether the network is created to only allow internal networking
	 * connectivity.
	 * @example false
	 */
	Internal?: boolean;
	/**
	 * Whether a global / swarm scope network is manually attachable by regular
	 * containers from workers in swarm mode.
	 * @example false
	 */
	Attachable?: boolean;
	/**
	 * Whether the network is providing the routing-mesh for the swarm cluster.
	 * @example false
	 */
	Ingress?: boolean;
	ConfigFrom?: {
		/**
		 * The name of the config-only network that provides the network's
		 * configuration. The specified network must be an existing config-only
		 * network. Only network names are allowed, not network IDs.
		 * @example config_only_network_01
		 */
		Network?: string;
	};
	ConfigOnly?: boolean;
	Containers?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Options?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Peers?: readonly {
		/**
		 * ID of the peer-node in the Swarm cluster.
		 * @example 6869d7c1732b
		 */
		Name?: string;
		/**
		 * IP-address of the peer-node in the Swarm cluster.
		 * @example 10.133.77.91
		 */
		IP?: string;
	}[];
}[];
export type NetworkInspectCommandQuery = {
	verbose?: "true" | "false";
	scope?: string | undefined;
};
export type NetworkInspectCommandParams = {
	id: string;
};
export type NetworkInspectCommandInput = NetworkInspectCommandParams &
	NetworkInspectCommandQuery;
export type NetworkInspectCommandOutput = {
	/**
	 * Name of the network.
	 * @example my_network
	 */
	Name?: string;
	/**
	 * ID that uniquely identifies a network on a single machine.
	 * @example 7d86d31b1478e7cca9ebed7e73aa0fdeec46c5ca29497431d3007d2d9e15ed99
	 */
	Id?: string;
	/**
	 * Date and time at which the network was created in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2016-10-19T04:33:30.360899459Z
	 */
	Created?: string;
	/**
	 * The level at which the network exists (e.g. `swarm` for cluster-wide
	 * or `local` for machine level)
	 * @example local
	 */
	Scope?: string;
	/**
	 * The name of the driver used to create the network (e.g. `bridge`,
	 * `overlay`).
	 * @example overlay
	 */
	Driver?: string;
	/**
	 * Whether the network was created with IPv6 enabled.
	 * @example false
	 */
	EnableIPv6?: boolean;
	IPAM?: {
		/**
		 * Name of the IPAM driver to use.
		 * @default default
		 * @example default
		 */
		Driver?: string;
		Config?: readonly {
			Subnet?: string;
			IPRange?: string;
			Gateway?: string;
			AuxiliaryAddresses?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	/**
	 * Whether the network is created to only allow internal networking
	 * connectivity.
	 * @example false
	 */
	Internal?: boolean;
	/**
	 * Whether a global / swarm scope network is manually attachable by regular
	 * containers from workers in swarm mode.
	 * @example false
	 */
	Attachable?: boolean;
	/**
	 * Whether the network is providing the routing-mesh for the swarm cluster.
	 * @example false
	 */
	Ingress?: boolean;
	ConfigFrom?: {
		/**
		 * The name of the config-only network that provides the network's
		 * configuration. The specified network must be an existing config-only
		 * network. Only network names are allowed, not network IDs.
		 * @example config_only_network_01
		 */
		Network?: string;
	};
	ConfigOnly?: boolean;
	Containers?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Options?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Peers?: readonly {
		/**
		 * ID of the peer-node in the Swarm cluster.
		 * @example 6869d7c1732b
		 */
		Name?: string;
		/**
		 * IP-address of the peer-node in the Swarm cluster.
		 * @example 10.133.77.91
		 */
		IP?: string;
	}[];
};
export type NetworkDeleteCommandParams = {
	id: string;
};
export type NetworkDeleteCommandInput = NetworkDeleteCommandParams;
type NetworkCreateJsonBody = {
	/**
	 * The network's name.
	 * @example my_network
	 */
	Name: string;
	/**
	 * Check for networks with duplicate names. Since Network is
	 * primarily keyed based on a random ID and not on the name, and
	 * network name is strictly a user-friendly alias to the network
	 * which is uniquely identified using ID, there is no guaranteed
	 * way to check for duplicates. CheckDuplicate is there to provide
	 * a best effort checking of any networks which has the same name
	 * but it is not guaranteed to catch all name collisions.
	 * @example true
	 */
	CheckDuplicate?: boolean;
	/**
	 * Name of the network driver plugin to use.
	 * @default bridge
	 * @example bridge
	 */
	Driver?: string;
	Scope?: string;
	Internal?: boolean;
	/**
	 * Globally scoped network is manually attachable by regular
	 * containers from workers in swarm mode.
	 * @example true
	 */
	Attachable?: boolean;
	/**
	 * Ingress network is the network which provides the routing-mesh
	 * in swarm mode.
	 * @example false
	 */
	Ingress?: boolean;
	/**
	 * Creates a config-only network. Config-only networks are placeholder
	 * networks for network configurations to be used by other networks.
	 * Config-only networks cannot be used directly to run containers
	 * or services.
	 * @example false
	 */
	ConfigOnly?: boolean;
	ConfigFrom?: {
		/**
		 * The name of the config-only network that provides the network's
		 * configuration. The specified network must be an existing config-only
		 * network. Only network names are allowed, not network IDs.
		 * @example config_only_network_01
		 */
		Network?: string;
	};
	IPAM?: {
		/**
		 * Name of the IPAM driver to use.
		 * @default default
		 * @example default
		 */
		Driver?: string;
		Config?: readonly {
			Subnet?: string;
			IPRange?: string;
			Gateway?: string;
			AuxiliaryAddresses?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	/**
	 * Enable IPv6 on the network.
	 * @example true
	 */
	EnableIPv6?: boolean;
	Options?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
};
export type NetworkCreateCommandBody = NetworkCreateJsonBody;
export type NetworkCreateCommandInput = NetworkCreateJsonBody;
export type NetworkCreateCommandOutput = {
	Id?: string;
	Warning?: string;
};
type NetworkConnectJsonBody = {
	Container?: string;
	EndpointConfig?: {
		IPAMConfig?: {
			IPv4Address?: string;
			IPv6Address?: string;
			LinkLocalIPs?: readonly string[];
		};
		Links?: readonly string[];
		Aliases?: readonly string[];
		/**
		 * Unique ID of the network.
		 * @example 08754567f1f40222263eab4102e1c733ae697e8e354aa9cd6e18d7402835292a
		 */
		NetworkID?: string;
		/**
		 * Unique ID for the service endpoint in a Sandbox.
		 * @example b88f5b905aabf2893f3cbc4ee42d1ea7980bbc0a92e2c8922b1e1795298afb0b
		 */
		EndpointID?: string;
		/**
		 * Gateway address for this network.
		 * @example 172.17.0.1
		 */
		Gateway?: string;
		/**
		 * IPv4 address.
		 * @example 172.17.0.4
		 */
		IPAddress?: string;
		/**
		 * Mask length of the IPv4 address.
		 * @example 16
		 */
		IPPrefixLen?: number;
		/**
		 * IPv6 gateway address.
		 * @example 2001:db8:2::100
		 */
		IPv6Gateway?: string;
		/**
		 * Global IPv6 address.
		 * @example 2001:db8::5689
		 */
		GlobalIPv6Address?: string;
		/**
		 * Mask length of the global IPv6 address.
		 * @example 64
		 */
		GlobalIPv6PrefixLen?: number;
		/**
		 * MAC address for the endpoint on this network.
		 * @example 02:42:ac:11:00:04
		 */
		MacAddress?: string;
		DriverOpts?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
};
export type NetworkConnectCommandParams = {
	id: string;
};
export type NetworkConnectCommandBody = NetworkConnectJsonBody;
export type NetworkConnectCommandInput = NetworkConnectJsonBody &
	NetworkConnectCommandParams;
type NetworkDisconnectJsonBody = {
	Container?: string;
	Force?: boolean;
};
export type NetworkDisconnectCommandParams = {
	id: string;
};
export type NetworkDisconnectCommandBody = NetworkDisconnectJsonBody;
export type NetworkDisconnectCommandInput = NetworkDisconnectJsonBody &
	NetworkDisconnectCommandParams;
export type NetworkPruneCommandQuery = {
	filters?: string | undefined;
};
export type NetworkPruneCommandInput = NetworkPruneCommandQuery;
export type NetworkPruneCommandOutput = {
	NetworksDeleted?: readonly string[];
};
export type PluginListCommandQuery = {
	filters?: string | undefined;
};
export type PluginListCommandInput = PluginListCommandQuery;
export type PluginListCommandOutput = readonly {
	Id?: string;
	Name: string;
	/**
	 * True if the plugin is running. False if the plugin is not running, only
	 * installed.
	 * @example true
	 */
	Enabled: boolean;
	Settings: {
		Mounts: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Source: string;
			Destination: string;
			Type: string;
			Options: readonly string[];
		}[];
		Env: readonly string[];
		Args: readonly string[];
		Devices: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Path: string;
		}[];
	};
	/**
	 * plugin remote reference used to push/pull the plugin
	 * @example localhost:5000/tiborvass/sample-volume-plugin:latest
	 */
	PluginReference?: string;
	Config: {
		/**
		 * Docker Version used to create the plugin
		 * @example 17.06.0-ce
		 */
		DockerVersion?: string;
		Description: string;
		Documentation: string;
		Interface: {
			Types: readonly {
				Prefix: string;
				Capability: string;
				Version: string;
			}[];
			Socket: string;
			/**
			 * Protocol to use for clients connecting to the plugin.
			 * @enum ,moby.plugins.http/v1
			 * @example some.protocol/v1.0
			 */
			ProtocolScheme?: "" | "moby.plugins.http/v1";
		};
		Entrypoint: readonly string[];
		WorkDir: string;
		User?: {
			UID?: number;
			GID?: number;
		};
		Network: {
			Type: string;
		};
		Linux: {
			Capabilities: readonly string[];
			AllowAllDevices: boolean;
			Devices: readonly {
				Name: string;
				Description: string;
				Settable: readonly string[];
				Path: string;
			}[];
		};
		PropagatedMount: string;
		IpcHost: boolean;
		PidHost: boolean;
		Mounts: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Source: string;
			Destination: string;
			Type: string;
			Options: readonly string[];
		}[];
		Env: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Value: string;
		}[];
		Args: {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Value: readonly string[];
		};
		rootfs?: {
			type?: string;
			diff_ids?: readonly string[];
		};
	};
}[];
export type GetPluginPrivilegesCommandQuery = {
	remote: string;
};
export type GetPluginPrivilegesCommandInput = GetPluginPrivilegesCommandQuery;
export type GetPluginPrivilegesCommandOutput = readonly {
	Name?: string;
	Description?: string;
	Value?: readonly string[];
}[];
export type PluginPullCommandQuery = {
	remote: string;
	name?: string | undefined;
};
export type PluginPullCommandHeader = {
	"x-registry-auth"?: string;
};
type PluginPullJsonBody = readonly {
	Name?: string;
	Description?: string;
	Value?: readonly string[];
}[];
export type PluginPullCommandBody = PluginPullJsonBody;
export type PluginPullCommandInput = PluginPullJsonBody &
	PluginPullCommandQuery;
export type PluginInspectCommandParams = {
	name: string;
};
export type PluginInspectCommandInput = PluginInspectCommandParams;
export type PluginInspectCommandOutput = {
	Id?: string;
	Name: string;
	/**
	 * True if the plugin is running. False if the plugin is not running, only
	 * installed.
	 * @example true
	 */
	Enabled: boolean;
	Settings: {
		Mounts: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Source: string;
			Destination: string;
			Type: string;
			Options: readonly string[];
		}[];
		Env: readonly string[];
		Args: readonly string[];
		Devices: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Path: string;
		}[];
	};
	/**
	 * plugin remote reference used to push/pull the plugin
	 * @example localhost:5000/tiborvass/sample-volume-plugin:latest
	 */
	PluginReference?: string;
	Config: {
		/**
		 * Docker Version used to create the plugin
		 * @example 17.06.0-ce
		 */
		DockerVersion?: string;
		Description: string;
		Documentation: string;
		Interface: {
			Types: readonly {
				Prefix: string;
				Capability: string;
				Version: string;
			}[];
			Socket: string;
			/**
			 * Protocol to use for clients connecting to the plugin.
			 * @enum ,moby.plugins.http/v1
			 * @example some.protocol/v1.0
			 */
			ProtocolScheme?: "" | "moby.plugins.http/v1";
		};
		Entrypoint: readonly string[];
		WorkDir: string;
		User?: {
			UID?: number;
			GID?: number;
		};
		Network: {
			Type: string;
		};
		Linux: {
			Capabilities: readonly string[];
			AllowAllDevices: boolean;
			Devices: readonly {
				Name: string;
				Description: string;
				Settable: readonly string[];
				Path: string;
			}[];
		};
		PropagatedMount: string;
		IpcHost: boolean;
		PidHost: boolean;
		Mounts: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Source: string;
			Destination: string;
			Type: string;
			Options: readonly string[];
		}[];
		Env: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Value: string;
		}[];
		Args: {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Value: readonly string[];
		};
		rootfs?: {
			type?: string;
			diff_ids?: readonly string[];
		};
	};
};
export type PluginDeleteCommandQuery = {
	force?: "true" | "false";
};
export type PluginDeleteCommandParams = {
	name: string;
};
export type PluginDeleteCommandInput = PluginDeleteCommandParams &
	PluginDeleteCommandQuery;
export type PluginDeleteCommandOutput = {
	Id?: string;
	Name: string;
	/**
	 * True if the plugin is running. False if the plugin is not running, only
	 * installed.
	 * @example true
	 */
	Enabled: boolean;
	Settings: {
		Mounts: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Source: string;
			Destination: string;
			Type: string;
			Options: readonly string[];
		}[];
		Env: readonly string[];
		Args: readonly string[];
		Devices: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Path: string;
		}[];
	};
	/**
	 * plugin remote reference used to push/pull the plugin
	 * @example localhost:5000/tiborvass/sample-volume-plugin:latest
	 */
	PluginReference?: string;
	Config: {
		/**
		 * Docker Version used to create the plugin
		 * @example 17.06.0-ce
		 */
		DockerVersion?: string;
		Description: string;
		Documentation: string;
		Interface: {
			Types: readonly {
				Prefix: string;
				Capability: string;
				Version: string;
			}[];
			Socket: string;
			/**
			 * Protocol to use for clients connecting to the plugin.
			 * @enum ,moby.plugins.http/v1
			 * @example some.protocol/v1.0
			 */
			ProtocolScheme?: "" | "moby.plugins.http/v1";
		};
		Entrypoint: readonly string[];
		WorkDir: string;
		User?: {
			UID?: number;
			GID?: number;
		};
		Network: {
			Type: string;
		};
		Linux: {
			Capabilities: readonly string[];
			AllowAllDevices: boolean;
			Devices: readonly {
				Name: string;
				Description: string;
				Settable: readonly string[];
				Path: string;
			}[];
		};
		PropagatedMount: string;
		IpcHost: boolean;
		PidHost: boolean;
		Mounts: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Source: string;
			Destination: string;
			Type: string;
			Options: readonly string[];
		}[];
		Env: readonly {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Value: string;
		}[];
		Args: {
			Name: string;
			Description: string;
			Settable: readonly string[];
			Value: readonly string[];
		};
		rootfs?: {
			type?: string;
			diff_ids?: readonly string[];
		};
	};
};
export type PluginEnableCommandQuery = {
	timeout?: `${number}` | undefined;
};
export type PluginEnableCommandParams = {
	name: string;
};
export type PluginEnableCommandInput = PluginEnableCommandParams &
	PluginEnableCommandQuery;
export type PluginDisableCommandQuery = {
	force?: "true" | "false";
};
export type PluginDisableCommandParams = {
	name: string;
};
export type PluginDisableCommandInput = PluginDisableCommandParams &
	PluginDisableCommandQuery;
export type PluginUpgradeCommandQuery = {
	remote: string;
};
export type PluginUpgradeCommandHeader = {
	"x-registry-auth"?: string;
};
type PluginUpgradeJsonBody = readonly {
	Name?: string;
	Description?: string;
	Value?: readonly string[];
}[];
export type PluginUpgradeCommandParams = {
	name: string;
};
export type PluginUpgradeCommandBody = PluginUpgradeJsonBody;
export type PluginUpgradeCommandInput = PluginUpgradeJsonBody &
	PluginUpgradeCommandParams &
	PluginUpgradeCommandQuery;
export type PluginCreateCommandQuery = {
	name: string;
};
type PluginCreateCommandBodyApplicationXTar = NonNullable<RequestInit["body"]>;
export type PluginCreateCommandBodyNonJson = {
	body: PluginCreateCommandBodyApplicationXTar;
};
export type PluginCreateCommandInput = PluginCreateCommandBodyNonJson &
	PluginCreateCommandQuery;
export type PluginPushCommandParams = {
	name: string;
};
export type PluginPushCommandInput = PluginPushCommandParams;
type PluginSetJsonBody = readonly string[];
export type PluginSetCommandParams = {
	name: string;
};
export type PluginSetCommandBody = PluginSetJsonBody;
export type PluginSetCommandInput = PluginSetJsonBody & PluginSetCommandParams;
export type NodeListCommandQuery = {
	filters?: string | undefined;
};
export type NodeListCommandInput = NodeListCommandQuery;
export type NodeListCommandOutput = readonly {
	ID?: string;
	Version?: {
		Index?: number;
	};
	/**
	 * Date and time at which the node was added to the swarm in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2016-08-18T10:44:24.496525531Z
	 */
	CreatedAt?: string;
	/**
	 * Date and time at which the node was last updated in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2017-08-09T07:09:37.632105588Z
	 */
	UpdatedAt?: string;
	Spec?: {
		/**
		 * Name for the node.
		 * @example my-node
		 */
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Role of the node.
		 * @enum worker,manager
		 * @example manager
		 */
		Role?: "worker" | "manager";
		/**
		 * Availability of the node.
		 * @enum active,pause,drain
		 * @example active
		 */
		Availability?: "active" | "pause" | "drain";
	};
	Description?: {
		Hostname?: string;
		Platform?: {
			/**
			 * Architecture represents the hardware architecture (for example,
			 * `x86_64`).
			 * @example x86_64
			 */
			Architecture?: string;
			/**
			 * OS represents the Operating System (for example, `linux` or `windows`).
			 * @example linux
			 */
			OS?: string;
		};
		Resources?: {
			NanoCPUs?: number;
			MemoryBytes?: number;
			/**
			 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
			 * String resources (e.g, `GPU=UUID1`).
			 * @example [object Object],[object Object],[object Object]
			 */
			GenericResources?: readonly {
				NamedResourceSpec?: {
					Kind?: string;
					Value?: string;
				};
				DiscreteResourceSpec?: {
					Kind?: string;
					Value?: number;
				};
			}[];
		};
		Engine?: {
			EngineVersion?: string;
			Labels?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			Plugins?: readonly {
				Type?: string;
				Name?: string;
			}[];
		};
		/**
		 * Information about the issuer of leaf TLS certificates and the trusted root
		 * CA certificate.
		 * @example [object Object]
		 */
		TLSInfo?: {
			TrustRoot?: string;
			CertIssuerSubject?: string;
			CertIssuerPublicKey?: string;
		};
	};
	Status?: {
		/**
		 * NodeState represents the state of a node.
		 * @enum unknown,down,ready,disconnected
		 * @example ready
		 */
		State?: "unknown" | "down" | "ready" | "disconnected";
		Message?: string;
		/**
		 * IP address of the node.
		 * @example 172.17.0.2
		 */
		Addr?: string;
	};
	ManagerStatus?: {
		Leader?: boolean;
		/**
		 * Reachability represents the reachability of a node.
		 * @enum unknown,unreachable,reachable
		 * @example reachable
		 */
		Reachability?: "unknown" | "unreachable" | "reachable";
		/**
		 * The IP address and port at which the manager is reachable.
		 * @example 10.0.0.46:2377
		 */
		Addr?: string;
	};
}[];
export type NodeInspectCommandParams = {
	id: string;
};
export type NodeInspectCommandInput = NodeInspectCommandParams;
export type NodeInspectCommandOutput = {
	ID?: string;
	Version?: {
		Index?: number;
	};
	/**
	 * Date and time at which the node was added to the swarm in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2016-08-18T10:44:24.496525531Z
	 */
	CreatedAt?: string;
	/**
	 * Date and time at which the node was last updated in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2017-08-09T07:09:37.632105588Z
	 */
	UpdatedAt?: string;
	Spec?: {
		/**
		 * Name for the node.
		 * @example my-node
		 */
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Role of the node.
		 * @enum worker,manager
		 * @example manager
		 */
		Role?: "worker" | "manager";
		/**
		 * Availability of the node.
		 * @enum active,pause,drain
		 * @example active
		 */
		Availability?: "active" | "pause" | "drain";
	};
	Description?: {
		Hostname?: string;
		Platform?: {
			/**
			 * Architecture represents the hardware architecture (for example,
			 * `x86_64`).
			 * @example x86_64
			 */
			Architecture?: string;
			/**
			 * OS represents the Operating System (for example, `linux` or `windows`).
			 * @example linux
			 */
			OS?: string;
		};
		Resources?: {
			NanoCPUs?: number;
			MemoryBytes?: number;
			/**
			 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
			 * String resources (e.g, `GPU=UUID1`).
			 * @example [object Object],[object Object],[object Object]
			 */
			GenericResources?: readonly {
				NamedResourceSpec?: {
					Kind?: string;
					Value?: string;
				};
				DiscreteResourceSpec?: {
					Kind?: string;
					Value?: number;
				};
			}[];
		};
		Engine?: {
			EngineVersion?: string;
			Labels?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			Plugins?: readonly {
				Type?: string;
				Name?: string;
			}[];
		};
		/**
		 * Information about the issuer of leaf TLS certificates and the trusted root
		 * CA certificate.
		 * @example [object Object]
		 */
		TLSInfo?: {
			TrustRoot?: string;
			CertIssuerSubject?: string;
			CertIssuerPublicKey?: string;
		};
	};
	Status?: {
		/**
		 * NodeState represents the state of a node.
		 * @enum unknown,down,ready,disconnected
		 * @example ready
		 */
		State?: "unknown" | "down" | "ready" | "disconnected";
		Message?: string;
		/**
		 * IP address of the node.
		 * @example 172.17.0.2
		 */
		Addr?: string;
	};
	ManagerStatus?: {
		Leader?: boolean;
		/**
		 * Reachability represents the reachability of a node.
		 * @enum unknown,unreachable,reachable
		 * @example reachable
		 */
		Reachability?: "unknown" | "unreachable" | "reachable";
		/**
		 * The IP address and port at which the manager is reachable.
		 * @example 10.0.0.46:2377
		 */
		Addr?: string;
	};
};
export type NodeDeleteCommandQuery = {
	force?: "true" | "false";
};
export type NodeDeleteCommandParams = {
	id: string;
};
export type NodeDeleteCommandInput = NodeDeleteCommandParams &
	NodeDeleteCommandQuery;
export type NodeUpdateCommandQuery = {
	version: `${number}`;
};
type NodeUpdateJsonBody = {
	/**
	 * Name for the node.
	 * @example my-node
	 */
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Role of the node.
	 * @enum worker,manager
	 * @example manager
	 */
	Role?: "worker" | "manager";
	/**
	 * Availability of the node.
	 * @enum active,pause,drain
	 * @example active
	 */
	Availability?: "active" | "pause" | "drain";
};
export type NodeUpdateCommandParams = {
	id: string;
};
export type NodeUpdateCommandBody = NodeUpdateJsonBody;
export type NodeUpdateCommandInput = NodeUpdateJsonBody &
	NodeUpdateCommandParams &
	NodeUpdateCommandQuery;
export type SwarmInspectCommandInput = never;
export type SwarmInspectCommandOutput = {
	/**
	 * The ID of the swarm.
	 * @example abajmipo7b4xz5ip2nrla6b11
	 */
	ID?: string;
	Version?: {
		Index?: number;
	};
	/**
	 * Date and time at which the swarm was initialised in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2016-08-18T10:44:24.496525531Z
	 */
	CreatedAt?: string;
	/**
	 * Date and time at which the swarm was last updated in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 * @example 2017-08-09T07:09:37.632105588Z
	 */
	UpdatedAt?: string;
	Spec?: {
		/**
		 * Name of the swarm.
		 * @example default
		 */
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Orchestration?: {
			/**
			 * The number of historic tasks to keep per instance or node. If
			 * negative, never remove completed or failed tasks.
			 * @example 10
			 */
			TaskHistoryRetentionLimit?: number;
		};
		Raft?: {
			/**
			 * The number of log entries between snapshots.
			 * @example 10000
			 */
			SnapshotInterval?: number;
			KeepOldSnapshots?: number;
			/**
			 * The number of log entries to keep around to sync up slow followers
			 * after a snapshot is created.
			 * @example 500
			 */
			LogEntriesForSlowFollowers?: number;
			/**
			 * The number of ticks that a follower will wait for a message from
			 * the leader before becoming a candidate and starting an election.
			 * `ElectionTick` must be greater than `HeartbeatTick`.
			 *
			 * A tick currently defaults to one second, so these translate
			 * directly to seconds currently, but this is NOT guaranteed.
			 * @example 3
			 */
			ElectionTick?: number;
			/**
			 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
			 * the leader will send a heartbeat to the followers.
			 *
			 * A tick currently defaults to one second, so these translate
			 * directly to seconds currently, but this is NOT guaranteed.
			 * @example 1
			 */
			HeartbeatTick?: number;
		};
		Dispatcher?: {
			/**
			 * The delay for an agent to send a heartbeat to the dispatcher.
			 * @example 5000000000
			 */
			HeartbeatPeriod?: number;
		};
		CAConfig?: {
			/**
			 * The duration node certificates are issued for.
			 * @example 7776000000000000
			 */
			NodeCertExpiry?: number;
			ExternalCAs?: readonly {
				/**
				 * Protocol for communication with the external CA (currently
				 * only `cfssl` is supported).
				 * @default cfssl
				 * @enum cfssl
				 */
				Protocol?: "cfssl";
				URL?: string;
				Options?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				CACert?: string;
			}[];
			SigningCACert?: string;
			SigningCAKey?: string;
			ForceRotate?: number;
		};
		EncryptionConfig?: {
			/**
			 * If set, generate a key and use it to lock data stored on the
			 * managers.
			 * @example false
			 */
			AutoLockManagers?: boolean;
		};
		TaskDefaults?: {
			LogDriver?: {
				/**
				 * The log driver to use as a default for new tasks.
				 * @example json-file
				 */
				Name?: string;
				Options?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			};
		};
	};
	/**
	 * Information about the issuer of leaf TLS certificates and the trusted root
	 * CA certificate.
	 * @example [object Object]
	 */
	TLSInfo?: {
		TrustRoot?: string;
		CertIssuerSubject?: string;
		CertIssuerPublicKey?: string;
	};
	/**
	 * Whether there is currently a root CA rotation in progress for the swarm
	 * @example false
	 */
	RootRotationInProgress?: boolean;
	/**
	 * DataPathPort specifies the data path port number for data traffic.
	 * Acceptable port range is 1024 to 49151.
	 * If no port is set or is set to 0, the default port (4789) is used.
	 * @example 4789
	 */
	DataPathPort?: number;
	DefaultAddrPool?: readonly string[];
	/**
	 * SubnetSize specifies the subnet size of the networks created from the
	 * default subnet pool.
	 * @example 24
	 */
	SubnetSize?: number;
} & {
	JoinTokens?: {
		/**
		 * The token workers can use to join the swarm.
		 * @example SWMTKN-1-3pu6hszjas19xyp7ghgosyx9k8atbfcr8p2is99znpy26u2lkl-1awxwuwd3z9j1z3puu7rcgdbx
		 */
		Worker?: string;
		/**
		 * The token managers can use to join the swarm.
		 * @example SWMTKN-1-3pu6hszjas19xyp7ghgosyx9k8atbfcr8p2is99znpy26u2lkl-7p73s1dx5in4tatdymyhg9hu2
		 */
		Manager?: string;
	};
};
type SwarmInitJsonBody = {
	ListenAddr?: string;
	AdvertiseAddr?: string;
	DataPathAddr?: string;
	DataPathPort?: number;
	DefaultAddrPool?: readonly string[];
	ForceNewCluster?: boolean;
	SubnetSize?: number;
	Spec?: {
		/**
		 * Name of the swarm.
		 * @example default
		 */
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Orchestration?: {
			/**
			 * The number of historic tasks to keep per instance or node. If
			 * negative, never remove completed or failed tasks.
			 * @example 10
			 */
			TaskHistoryRetentionLimit?: number;
		};
		Raft?: {
			/**
			 * The number of log entries between snapshots.
			 * @example 10000
			 */
			SnapshotInterval?: number;
			KeepOldSnapshots?: number;
			/**
			 * The number of log entries to keep around to sync up slow followers
			 * after a snapshot is created.
			 * @example 500
			 */
			LogEntriesForSlowFollowers?: number;
			/**
			 * The number of ticks that a follower will wait for a message from
			 * the leader before becoming a candidate and starting an election.
			 * `ElectionTick` must be greater than `HeartbeatTick`.
			 *
			 * A tick currently defaults to one second, so these translate
			 * directly to seconds currently, but this is NOT guaranteed.
			 * @example 3
			 */
			ElectionTick?: number;
			/**
			 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
			 * the leader will send a heartbeat to the followers.
			 *
			 * A tick currently defaults to one second, so these translate
			 * directly to seconds currently, but this is NOT guaranteed.
			 * @example 1
			 */
			HeartbeatTick?: number;
		};
		Dispatcher?: {
			/**
			 * The delay for an agent to send a heartbeat to the dispatcher.
			 * @example 5000000000
			 */
			HeartbeatPeriod?: number;
		};
		CAConfig?: {
			/**
			 * The duration node certificates are issued for.
			 * @example 7776000000000000
			 */
			NodeCertExpiry?: number;
			ExternalCAs?: readonly {
				/**
				 * Protocol for communication with the external CA (currently
				 * only `cfssl` is supported).
				 * @default cfssl
				 * @enum cfssl
				 */
				Protocol?: "cfssl";
				URL?: string;
				Options?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				CACert?: string;
			}[];
			SigningCACert?: string;
			SigningCAKey?: string;
			ForceRotate?: number;
		};
		EncryptionConfig?: {
			/**
			 * If set, generate a key and use it to lock data stored on the
			 * managers.
			 * @example false
			 */
			AutoLockManagers?: boolean;
		};
		TaskDefaults?: {
			LogDriver?: {
				/**
				 * The log driver to use as a default for new tasks.
				 * @example json-file
				 */
				Name?: string;
				Options?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			};
		};
	};
};
export type SwarmInitCommandBody = SwarmInitJsonBody;
export type SwarmInitCommandInput = SwarmInitJsonBody;
export type SwarmInitCommandOutput = string | undefined;
type SwarmJoinJsonBody = {
	ListenAddr?: string;
	AdvertiseAddr?: string;
	DataPathAddr?: string;
	RemoteAddrs?: readonly string[];
	JoinToken?: string;
};
export type SwarmJoinCommandBody = SwarmJoinJsonBody;
export type SwarmJoinCommandInput = SwarmJoinJsonBody;
export type SwarmLeaveCommandQuery = {
	force?: "true" | "false";
};
export type SwarmLeaveCommandInput = SwarmLeaveCommandQuery;
export type SwarmUpdateCommandQuery = {
	version: `${number}`;
	rotateWorkerToken?: "true" | "false";
	rotateManagerToken?: "true" | "false";
	rotateManagerUnlockKey?: "true" | "false";
};
type SwarmUpdateJsonBody = {
	/**
	 * Name of the swarm.
	 * @example default
	 */
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Orchestration?: {
		/**
		 * The number of historic tasks to keep per instance or node. If
		 * negative, never remove completed or failed tasks.
		 * @example 10
		 */
		TaskHistoryRetentionLimit?: number;
	};
	Raft?: {
		/**
		 * The number of log entries between snapshots.
		 * @example 10000
		 */
		SnapshotInterval?: number;
		KeepOldSnapshots?: number;
		/**
		 * The number of log entries to keep around to sync up slow followers
		 * after a snapshot is created.
		 * @example 500
		 */
		LogEntriesForSlowFollowers?: number;
		/**
		 * The number of ticks that a follower will wait for a message from
		 * the leader before becoming a candidate and starting an election.
		 * `ElectionTick` must be greater than `HeartbeatTick`.
		 *
		 * A tick currently defaults to one second, so these translate
		 * directly to seconds currently, but this is NOT guaranteed.
		 * @example 3
		 */
		ElectionTick?: number;
		/**
		 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
		 * the leader will send a heartbeat to the followers.
		 *
		 * A tick currently defaults to one second, so these translate
		 * directly to seconds currently, but this is NOT guaranteed.
		 * @example 1
		 */
		HeartbeatTick?: number;
	};
	Dispatcher?: {
		/**
		 * The delay for an agent to send a heartbeat to the dispatcher.
		 * @example 5000000000
		 */
		HeartbeatPeriod?: number;
	};
	CAConfig?: {
		/**
		 * The duration node certificates are issued for.
		 * @example 7776000000000000
		 */
		NodeCertExpiry?: number;
		ExternalCAs?: readonly {
			/**
			 * Protocol for communication with the external CA (currently
			 * only `cfssl` is supported).
			 * @default cfssl
			 * @enum cfssl
			 */
			Protocol?: "cfssl";
			URL?: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			CACert?: string;
		}[];
		SigningCACert?: string;
		SigningCAKey?: string;
		ForceRotate?: number;
	};
	EncryptionConfig?: {
		/**
		 * If set, generate a key and use it to lock data stored on the
		 * managers.
		 * @example false
		 */
		AutoLockManagers?: boolean;
	};
	TaskDefaults?: {
		LogDriver?: {
			/**
			 * The log driver to use as a default for new tasks.
			 * @example json-file
			 */
			Name?: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
};
export type SwarmUpdateCommandBody = SwarmUpdateJsonBody;
export type SwarmUpdateCommandInput = SwarmUpdateJsonBody &
	SwarmUpdateCommandQuery;
export type SwarmUnlockkeyCommandInput = never;
export type SwarmUnlockkeyCommandOutput = {
	UnlockKey?: string;
};
type SwarmUnlockJsonBody = {
	UnlockKey?: string;
};
export type SwarmUnlockCommandBody = SwarmUnlockJsonBody;
export type SwarmUnlockCommandInput = SwarmUnlockJsonBody;
export type ServiceListCommandQuery = {
	filters?: string | undefined;
	status?: "true" | "false";
};
export type ServiceListCommandInput = ServiceListCommandQuery;
export type ServiceListCommandOutput = readonly {
	ID?: string;
	Version?: {
		Index?: number;
	};
	CreatedAt?: string;
	UpdatedAt?: string;
	Spec?: {
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		TaskTemplate?: {
			PluginSpec?: {
				Name?: string;
				Remote?: string;
				Disabled?: boolean;
				PluginPrivilege?: readonly {
					Name?: string;
					Description?: string;
					Value?: readonly string[];
				}[];
			};
			ContainerSpec?: {
				Image?: string;
				Labels?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				Command?: readonly string[];
				Args?: readonly string[];
				Hostname?: string;
				Env?: readonly string[];
				Dir?: string;
				User?: string;
				Groups?: readonly string[];
				Privileges?: {
					CredentialSpec?: {
						/**
						 * Load credential spec from a Swarm Config with the given ID.
						 * The specified config must also be present in the Configs
						 * field with the Runtime property set.
						 *
						 * <p><br /></p>
						 *
						 *
						 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
						 * > and `CredentialSpec.Config` are mutually exclusive.
						 * @example 0bt9dmxjvjiqermk6xrop3ekq
						 */
						Config?: string;
						/**
						 * Load credential spec from this file. The file is read by
						 * the daemon, and must be present in the `CredentialSpecs`
						 * subdirectory in the docker data directory, which defaults
						 * to `C:\ProgramData\Docker\` on Windows.
						 *
						 * For example, specifying `spec.json` loads
						 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
						 *
						 * <p><br /></p>
						 *
						 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
						 * > and `CredentialSpec.Config` are mutually exclusive.
						 * @example spec.json
						 */
						File?: string;
						Registry?: string;
					};
					SELinuxContext?: {
						Disable?: boolean;
						User?: string;
						Role?: string;
						Type?: string;
						Level?: string;
					};
				};
				TTY?: boolean;
				OpenStdin?: boolean;
				ReadOnly?: boolean;
				Mounts?: readonly {
					Target?: string;
					Source?: string;
					/**
					 * The mount type. Available types:
					 *
					 * - `bind` Mounts a file or directory from the host into the container. Must
					 * exist prior to creating the container.
					 * - `volume` Creates a volume with the given name and options (or uses a
					 * pre-existing volume with the same name and options). These are **not**
					 * removed when the container is removed.
					 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
					 * specified for tmpfs.
					 * - `npipe` Mounts a named pipe from the host into the container. Must exist
					 * prior to creating the container.
					 * - `cluster` a Swarm cluster volume
					 * @enum bind,volume,tmpfs,npipe,cluster
					 */
					Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
					ReadOnly?: boolean;
					Consistency?: string;
					BindOptions?: {
						/**
						 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
						 * @enum private,rprivate,shared,rshared,slave,rslave
						 */
						Propagation?:
							| "private"
							| "rprivate"
							| "shared"
							| "rshared"
							| "slave"
							| "rslave";
						NonRecursive?: boolean;
						CreateMountpoint?: boolean;
					};
					VolumeOptions?: {
						NoCopy?: boolean;
						Labels?: Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>;
						DriverConfig?: {
							Name?: string;
							Options?: Record<
								string | number,
								/* additionalProperties is not handled yet */ unknown
							>;
						};
					};
					TmpfsOptions?: {
						SizeBytes?: number;
						Mode?: number;
					};
				}[];
				StopSignal?: string;
				StopGracePeriod?: number;
				HealthCheck?: {
					Test?: readonly string[];
					Interval?: number;
					Timeout?: number;
					Retries?: number;
					StartPeriod?: number;
				};
				Hosts?: readonly string[];
				DNSConfig?: {
					Nameservers?: readonly string[];
					Search?: readonly string[];
					Options?: readonly string[];
				};
				Secrets?: readonly {
					File?: {
						Name?: string;
						UID?: string;
						GID?: string;
						Mode?: number;
					};
					SecretID?: string;
					SecretName?: string;
				}[];
				Configs?: readonly {
					File?: {
						Name?: string;
						UID?: string;
						GID?: string;
						Mode?: number;
					};
					Runtime?: {};
					ConfigID?: string;
					ConfigName?: string;
				}[];
				/**
				 * Isolation technology of the containers running the service.
				 * (Windows only)
				 * @enum default,process,hyperv,
				 */
				Isolation?: "default" | "process" | "hyperv" | "";
				Init?: boolean | null;
				Sysctls?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				/**
				 * A list of kernel capabilities to add to the default set
				 * for the container.
				 * @example CAP_NET_RAW,CAP_SYS_ADMIN,CAP_SYS_CHROOT,CAP_SYSLOG
				 */
				CapabilityAdd?: readonly string[];
				/**
				 * A list of kernel capabilities to drop from the default set
				 * for the container.
				 * @example CAP_NET_RAW
				 */
				CapabilityDrop?: readonly string[];
				Ulimits?: readonly {
					Name?: string;
					Soft?: number;
					Hard?: number;
				}[];
			};
			NetworkAttachmentSpec?: {
				ContainerID?: string;
			};
			Resources?: {
				Limits?: {
					NanoCPUs?: number;
					MemoryBytes?: number;
					/**
					 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
					 * @example 100
					 */
					Pids?: number;
				};
				Reservations?: {
					NanoCPUs?: number;
					MemoryBytes?: number;
					/**
					 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
					 * String resources (e.g, `GPU=UUID1`).
					 * @example [object Object],[object Object],[object Object]
					 */
					GenericResources?: readonly {
						NamedResourceSpec?: {
							Kind?: string;
							Value?: string;
						};
						DiscreteResourceSpec?: {
							Kind?: string;
							Value?: number;
						};
					}[];
				};
			};
			RestartPolicy?: {
				/**
				 * Condition for restart.
				 * @enum none,on-failure,any
				 */
				Condition?: "none" | "on-failure" | "any";
				Delay?: number;
				MaxAttempts?: number;
				Window?: number;
			};
			Placement?: {
				/**
				 * An array of constraint expressions to limit the set of nodes where
				 * a task can be scheduled. Constraint expressions can either use a
				 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
				 * nodes that satisfy every expression (AND match). Constraints can
				 * match node or Docker Engine labels as follows:
				 *
				 * node attribute       | matches                        | example
				 * ---------------------|--------------------------------|-----------------------------------------------
				 * `node.id`            | Node ID                        |
				 * `node.id==2ivku8v2gvtg4`
				 * `node.hostname`      | Node hostname                  |
				 * `node.hostname!=node-2`
				 * `node.role`          | Node role (`manager`/`worker`) |
				 * `node.role==manager`
				 * `node.platform.os`   | Node operating system          |
				 * `node.platform.os==windows`
				 * `node.platform.arch` | Node architecture              |
				 * `node.platform.arch==x86_64`
				 * `node.labels`        | User-defined node labels       |
				 * `node.labels.security==high`
				 * `engine.labels`      | Docker Engine's labels         |
				 * `engine.labels.operatingsystem==ubuntu-14.04`
				 *
				 * `engine.labels` apply to Docker Engine labels like operating system,
				 * drivers, etc. Swarm administrators add `node.labels` for operational
				 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
				 * @example node.hostname!=node3.corp.example.com,node.role!=manager,node.labels.type==production,node.platform.os==linux,node.platform.arch==x86_64
				 */
				Constraints?: readonly string[];
				/**
				 * Preferences provide a way to make the scheduler aware of factors
				 * such as topology. They are provided in order from highest to
				 * lowest precedence.
				 * @example [object Object],[object Object]
				 */
				Preferences?: readonly {
					Spread?: {
						SpreadDescriptor?: string;
					};
				}[];
				MaxReplicas?: number;
				Platforms?: readonly {
					/**
					 * Architecture represents the hardware architecture (for example,
					 * `x86_64`).
					 * @example x86_64
					 */
					Architecture?: string;
					/**
					 * OS represents the Operating System (for example, `linux` or `windows`).
					 * @example linux
					 */
					OS?: string;
				}[];
			};
			ForceUpdate?: number;
			Runtime?: string;
			Networks?: readonly {
				Target?: string;
				Aliases?: readonly string[];
				DriverOpts?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			}[];
			LogDriver?: {
				Name?: string;
				Options?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			};
		};
		Mode?: {
			Replicated?: {
				Replicas?: number;
			};
			Global?: {};
			ReplicatedJob?: {
				/**
				 * The maximum number of replicas to run simultaneously.
				 * @default 1
				 */
				MaxConcurrent?: number;
				TotalCompletions?: number;
			};
			GlobalJob?: {};
		};
		UpdateConfig?: {
			Parallelism?: number;
			Delay?: number;
			/**
			 * Action to take if an updated task fails to run, or stops running
			 * during the update.
			 * @enum continue,pause,rollback
			 */
			FailureAction?: "continue" | "pause" | "rollback";
			Monitor?: number;
			MaxFailureRatio?: number;
			/**
			 * The order of operations when rolling out an updated task. Either
			 * the old task is shut down before the new task is started, or the
			 * new task is started before the old task is shut down.
			 * @enum stop-first,start-first
			 */
			Order?: "stop-first" | "start-first";
		};
		RollbackConfig?: {
			Parallelism?: number;
			Delay?: number;
			/**
			 * Action to take if an rolled back task fails to run, or stops
			 * running during the rollback.
			 * @enum continue,pause
			 */
			FailureAction?: "continue" | "pause";
			Monitor?: number;
			MaxFailureRatio?: number;
			/**
			 * The order of operations when rolling back a task. Either the old
			 * task is shut down before the new task is started, or the new task
			 * is started before the old task is shut down.
			 * @enum stop-first,start-first
			 */
			Order?: "stop-first" | "start-first";
		};
		Networks?: readonly {
			Target?: string;
			Aliases?: readonly string[];
			DriverOpts?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		EndpointSpec?: {
			/**
			 * The mode of resolution to use for internal load balancing between tasks.
			 * @default vip
			 * @enum vip,dnsrr
			 */
			Mode?: "vip" | "dnsrr";
			Ports?: readonly {
				Name?: string;
				Protocol?: "tcp" | "udp" | "sctp";
				TargetPort?: number;
				PublishedPort?: number;
				/**
				 * The mode in which port is published.
				 *
				 * <p><br /></p>
				 *
				 * - "ingress" makes the target port accessible on every node,
				 *
				 * regardless of whether there is a task for the service running on
				 *
				 * that node or not.
				 * - "host" bypasses the routing mesh and publish the port directly on
				 *
				 * the swarm node where that service is running.
				 * @default ingress
				 * @enum ingress,host
				 * @example ingress
				 */
				PublishMode?: "ingress" | "host";
			}[];
		};
	};
	Endpoint?: {
		Spec?: {
			/**
			 * The mode of resolution to use for internal load balancing between tasks.
			 * @default vip
			 * @enum vip,dnsrr
			 */
			Mode?: "vip" | "dnsrr";
			Ports?: readonly {
				Name?: string;
				Protocol?: "tcp" | "udp" | "sctp";
				TargetPort?: number;
				PublishedPort?: number;
				/**
				 * The mode in which port is published.
				 *
				 * <p><br /></p>
				 *
				 * - "ingress" makes the target port accessible on every node,
				 *
				 * regardless of whether there is a task for the service running on
				 *
				 * that node or not.
				 * - "host" bypasses the routing mesh and publish the port directly on
				 *
				 * the swarm node where that service is running.
				 * @default ingress
				 * @enum ingress,host
				 * @example ingress
				 */
				PublishMode?: "ingress" | "host";
			}[];
		};
		Ports?: readonly {
			Name?: string;
			Protocol?: "tcp" | "udp" | "sctp";
			TargetPort?: number;
			PublishedPort?: number;
			/**
			 * The mode in which port is published.
			 *
			 * <p><br /></p>
			 *
			 * - "ingress" makes the target port accessible on every node,
			 *
			 * regardless of whether there is a task for the service running on
			 *
			 * that node or not.
			 * - "host" bypasses the routing mesh and publish the port directly on
			 *
			 * the swarm node where that service is running.
			 * @default ingress
			 * @enum ingress,host
			 * @example ingress
			 */
			PublishMode?: "ingress" | "host";
		}[];
		VirtualIPs?: readonly {
			NetworkID?: string;
			Addr?: string;
		}[];
	};
	UpdateStatus?: {
		State?: "updating" | "paused" | "completed";
		StartedAt?: string;
		CompletedAt?: string;
		Message?: string;
	};
	ServiceStatus?: {
		/**
		 * The number of tasks for the service currently in the Running state.
		 * @example 7
		 */
		RunningTasks?: number;
		/**
		 * The number of tasks for the service desired to be running.
		 * For replicated services, this is the replica count from the
		 * service spec. For global services, this is computed by taking
		 * count of all tasks for the service with a Desired State other
		 * than Shutdown.
		 * @example 10
		 */
		DesiredTasks?: number;
		CompletedTasks?: number;
	};
	JobStatus?: {
		JobIteration?: {
			Index?: number;
		};
		LastExecution?: string;
	};
}[];
export type ServiceCreateCommandHeader = {
	"x-registry-auth"?: string;
};
type ServiceCreateJsonBody = {
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	TaskTemplate?: {
		PluginSpec?: {
			Name?: string;
			Remote?: string;
			Disabled?: boolean;
			PluginPrivilege?: readonly {
				Name?: string;
				Description?: string;
				Value?: readonly string[];
			}[];
		};
		ContainerSpec?: {
			Image?: string;
			Labels?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			Command?: readonly string[];
			Args?: readonly string[];
			Hostname?: string;
			Env?: readonly string[];
			Dir?: string;
			User?: string;
			Groups?: readonly string[];
			Privileges?: {
				CredentialSpec?: {
					/**
					 * Load credential spec from a Swarm Config with the given ID.
					 * The specified config must also be present in the Configs
					 * field with the Runtime property set.
					 *
					 * <p><br /></p>
					 *
					 *
					 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
					 * > and `CredentialSpec.Config` are mutually exclusive.
					 * @example 0bt9dmxjvjiqermk6xrop3ekq
					 */
					Config?: string;
					/**
					 * Load credential spec from this file. The file is read by
					 * the daemon, and must be present in the `CredentialSpecs`
					 * subdirectory in the docker data directory, which defaults
					 * to `C:\ProgramData\Docker\` on Windows.
					 *
					 * For example, specifying `spec.json` loads
					 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
					 *
					 * <p><br /></p>
					 *
					 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
					 * > and `CredentialSpec.Config` are mutually exclusive.
					 * @example spec.json
					 */
					File?: string;
					Registry?: string;
				};
				SELinuxContext?: {
					Disable?: boolean;
					User?: string;
					Role?: string;
					Type?: string;
					Level?: string;
				};
			};
			TTY?: boolean;
			OpenStdin?: boolean;
			ReadOnly?: boolean;
			Mounts?: readonly {
				Target?: string;
				Source?: string;
				/**
				 * The mount type. Available types:
				 *
				 * - `bind` Mounts a file or directory from the host into the container. Must
				 * exist prior to creating the container.
				 * - `volume` Creates a volume with the given name and options (or uses a
				 * pre-existing volume with the same name and options). These are **not**
				 * removed when the container is removed.
				 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
				 * specified for tmpfs.
				 * - `npipe` Mounts a named pipe from the host into the container. Must exist
				 * prior to creating the container.
				 * - `cluster` a Swarm cluster volume
				 * @enum bind,volume,tmpfs,npipe,cluster
				 */
				Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
				ReadOnly?: boolean;
				Consistency?: string;
				BindOptions?: {
					/**
					 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
					 * @enum private,rprivate,shared,rshared,slave,rslave
					 */
					Propagation?:
						| "private"
						| "rprivate"
						| "shared"
						| "rshared"
						| "slave"
						| "rslave";
					NonRecursive?: boolean;
					CreateMountpoint?: boolean;
				};
				VolumeOptions?: {
					NoCopy?: boolean;
					Labels?: Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>;
					DriverConfig?: {
						Name?: string;
						Options?: Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>;
					};
				};
				TmpfsOptions?: {
					SizeBytes?: number;
					Mode?: number;
				};
			}[];
			StopSignal?: string;
			StopGracePeriod?: number;
			HealthCheck?: {
				Test?: readonly string[];
				Interval?: number;
				Timeout?: number;
				Retries?: number;
				StartPeriod?: number;
			};
			Hosts?: readonly string[];
			DNSConfig?: {
				Nameservers?: readonly string[];
				Search?: readonly string[];
				Options?: readonly string[];
			};
			Secrets?: readonly {
				File?: {
					Name?: string;
					UID?: string;
					GID?: string;
					Mode?: number;
				};
				SecretID?: string;
				SecretName?: string;
			}[];
			Configs?: readonly {
				File?: {
					Name?: string;
					UID?: string;
					GID?: string;
					Mode?: number;
				};
				Runtime?: {};
				ConfigID?: string;
				ConfigName?: string;
			}[];
			/**
			 * Isolation technology of the containers running the service.
			 * (Windows only)
			 * @enum default,process,hyperv,
			 */
			Isolation?: "default" | "process" | "hyperv" | "";
			Init?: boolean | null;
			Sysctls?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			/**
			 * A list of kernel capabilities to add to the default set
			 * for the container.
			 * @example CAP_NET_RAW,CAP_SYS_ADMIN,CAP_SYS_CHROOT,CAP_SYSLOG
			 */
			CapabilityAdd?: readonly string[];
			/**
			 * A list of kernel capabilities to drop from the default set
			 * for the container.
			 * @example CAP_NET_RAW
			 */
			CapabilityDrop?: readonly string[];
			Ulimits?: readonly {
				Name?: string;
				Soft?: number;
				Hard?: number;
			}[];
		};
		NetworkAttachmentSpec?: {
			ContainerID?: string;
		};
		Resources?: {
			Limits?: {
				NanoCPUs?: number;
				MemoryBytes?: number;
				/**
				 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
				 * @example 100
				 */
				Pids?: number;
			};
			Reservations?: {
				NanoCPUs?: number;
				MemoryBytes?: number;
				/**
				 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
				 * String resources (e.g, `GPU=UUID1`).
				 * @example [object Object],[object Object],[object Object]
				 */
				GenericResources?: readonly {
					NamedResourceSpec?: {
						Kind?: string;
						Value?: string;
					};
					DiscreteResourceSpec?: {
						Kind?: string;
						Value?: number;
					};
				}[];
			};
		};
		RestartPolicy?: {
			/**
			 * Condition for restart.
			 * @enum none,on-failure,any
			 */
			Condition?: "none" | "on-failure" | "any";
			Delay?: number;
			MaxAttempts?: number;
			Window?: number;
		};
		Placement?: {
			/**
			 * An array of constraint expressions to limit the set of nodes where
			 * a task can be scheduled. Constraint expressions can either use a
			 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
			 * nodes that satisfy every expression (AND match). Constraints can
			 * match node or Docker Engine labels as follows:
			 *
			 * node attribute       | matches                        | example
			 * ---------------------|--------------------------------|-----------------------------------------------
			 * `node.id`            | Node ID                        |
			 * `node.id==2ivku8v2gvtg4`
			 * `node.hostname`      | Node hostname                  |
			 * `node.hostname!=node-2`
			 * `node.role`          | Node role (`manager`/`worker`) |
			 * `node.role==manager`
			 * `node.platform.os`   | Node operating system          |
			 * `node.platform.os==windows`
			 * `node.platform.arch` | Node architecture              |
			 * `node.platform.arch==x86_64`
			 * `node.labels`        | User-defined node labels       |
			 * `node.labels.security==high`
			 * `engine.labels`      | Docker Engine's labels         |
			 * `engine.labels.operatingsystem==ubuntu-14.04`
			 *
			 * `engine.labels` apply to Docker Engine labels like operating system,
			 * drivers, etc. Swarm administrators add `node.labels` for operational
			 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
			 * @example node.hostname!=node3.corp.example.com,node.role!=manager,node.labels.type==production,node.platform.os==linux,node.platform.arch==x86_64
			 */
			Constraints?: readonly string[];
			/**
			 * Preferences provide a way to make the scheduler aware of factors
			 * such as topology. They are provided in order from highest to
			 * lowest precedence.
			 * @example [object Object],[object Object]
			 */
			Preferences?: readonly {
				Spread?: {
					SpreadDescriptor?: string;
				};
			}[];
			MaxReplicas?: number;
			Platforms?: readonly {
				/**
				 * Architecture represents the hardware architecture (for example,
				 * `x86_64`).
				 * @example x86_64
				 */
				Architecture?: string;
				/**
				 * OS represents the Operating System (for example, `linux` or `windows`).
				 * @example linux
				 */
				OS?: string;
			}[];
		};
		ForceUpdate?: number;
		Runtime?: string;
		Networks?: readonly {
			Target?: string;
			Aliases?: readonly string[];
			DriverOpts?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		LogDriver?: {
			Name?: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
	Mode?: {
		Replicated?: {
			Replicas?: number;
		};
		Global?: {};
		ReplicatedJob?: {
			/**
			 * The maximum number of replicas to run simultaneously.
			 * @default 1
			 */
			MaxConcurrent?: number;
			TotalCompletions?: number;
		};
		GlobalJob?: {};
	};
	UpdateConfig?: {
		Parallelism?: number;
		Delay?: number;
		/**
		 * Action to take if an updated task fails to run, or stops running
		 * during the update.
		 * @enum continue,pause,rollback
		 */
		FailureAction?: "continue" | "pause" | "rollback";
		Monitor?: number;
		MaxFailureRatio?: number;
		/**
		 * The order of operations when rolling out an updated task. Either
		 * the old task is shut down before the new task is started, or the
		 * new task is started before the old task is shut down.
		 * @enum stop-first,start-first
		 */
		Order?: "stop-first" | "start-first";
	};
	RollbackConfig?: {
		Parallelism?: number;
		Delay?: number;
		/**
		 * Action to take if an rolled back task fails to run, or stops
		 * running during the rollback.
		 * @enum continue,pause
		 */
		FailureAction?: "continue" | "pause";
		Monitor?: number;
		MaxFailureRatio?: number;
		/**
		 * The order of operations when rolling back a task. Either the old
		 * task is shut down before the new task is started, or the new task
		 * is started before the old task is shut down.
		 * @enum stop-first,start-first
		 */
		Order?: "stop-first" | "start-first";
	};
	Networks?: readonly {
		Target?: string;
		Aliases?: readonly string[];
		DriverOpts?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	}[];
	EndpointSpec?: {
		/**
		 * The mode of resolution to use for internal load balancing between tasks.
		 * @default vip
		 * @enum vip,dnsrr
		 */
		Mode?: "vip" | "dnsrr";
		Ports?: readonly {
			Name?: string;
			Protocol?: "tcp" | "udp" | "sctp";
			TargetPort?: number;
			PublishedPort?: number;
			/**
			 * The mode in which port is published.
			 *
			 * <p><br /></p>
			 *
			 * - "ingress" makes the target port accessible on every node,
			 *
			 * regardless of whether there is a task for the service running on
			 *
			 * that node or not.
			 * - "host" bypasses the routing mesh and publish the port directly on
			 *
			 * the swarm node where that service is running.
			 * @default ingress
			 * @enum ingress,host
			 * @example ingress
			 */
			PublishMode?: "ingress" | "host";
		}[];
	};
} & Record<string | number, Jsonifiable>;
export type ServiceCreateCommandBody = ServiceCreateJsonBody;
export type ServiceCreateCommandInput = ServiceCreateJsonBody;
export type ServiceCreateCommandOutput = {
	ID?: string;
	Warning?: string;
};
export type ServiceInspectCommandQuery = {
	insertDefaults?: "true" | "false";
};
export type ServiceInspectCommandParams = {
	id: string;
};
export type ServiceInspectCommandInput = ServiceInspectCommandParams &
	ServiceInspectCommandQuery;
export type ServiceInspectCommandOutput = {
	ID?: string;
	Version?: {
		Index?: number;
	};
	CreatedAt?: string;
	UpdatedAt?: string;
	Spec?: {
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		TaskTemplate?: {
			PluginSpec?: {
				Name?: string;
				Remote?: string;
				Disabled?: boolean;
				PluginPrivilege?: readonly {
					Name?: string;
					Description?: string;
					Value?: readonly string[];
				}[];
			};
			ContainerSpec?: {
				Image?: string;
				Labels?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				Command?: readonly string[];
				Args?: readonly string[];
				Hostname?: string;
				Env?: readonly string[];
				Dir?: string;
				User?: string;
				Groups?: readonly string[];
				Privileges?: {
					CredentialSpec?: {
						/**
						 * Load credential spec from a Swarm Config with the given ID.
						 * The specified config must also be present in the Configs
						 * field with the Runtime property set.
						 *
						 * <p><br /></p>
						 *
						 *
						 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
						 * > and `CredentialSpec.Config` are mutually exclusive.
						 * @example 0bt9dmxjvjiqermk6xrop3ekq
						 */
						Config?: string;
						/**
						 * Load credential spec from this file. The file is read by
						 * the daemon, and must be present in the `CredentialSpecs`
						 * subdirectory in the docker data directory, which defaults
						 * to `C:\ProgramData\Docker\` on Windows.
						 *
						 * For example, specifying `spec.json` loads
						 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
						 *
						 * <p><br /></p>
						 *
						 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
						 * > and `CredentialSpec.Config` are mutually exclusive.
						 * @example spec.json
						 */
						File?: string;
						Registry?: string;
					};
					SELinuxContext?: {
						Disable?: boolean;
						User?: string;
						Role?: string;
						Type?: string;
						Level?: string;
					};
				};
				TTY?: boolean;
				OpenStdin?: boolean;
				ReadOnly?: boolean;
				Mounts?: readonly {
					Target?: string;
					Source?: string;
					/**
					 * The mount type. Available types:
					 *
					 * - `bind` Mounts a file or directory from the host into the container. Must
					 * exist prior to creating the container.
					 * - `volume` Creates a volume with the given name and options (or uses a
					 * pre-existing volume with the same name and options). These are **not**
					 * removed when the container is removed.
					 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
					 * specified for tmpfs.
					 * - `npipe` Mounts a named pipe from the host into the container. Must exist
					 * prior to creating the container.
					 * - `cluster` a Swarm cluster volume
					 * @enum bind,volume,tmpfs,npipe,cluster
					 */
					Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
					ReadOnly?: boolean;
					Consistency?: string;
					BindOptions?: {
						/**
						 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
						 * @enum private,rprivate,shared,rshared,slave,rslave
						 */
						Propagation?:
							| "private"
							| "rprivate"
							| "shared"
							| "rshared"
							| "slave"
							| "rslave";
						NonRecursive?: boolean;
						CreateMountpoint?: boolean;
					};
					VolumeOptions?: {
						NoCopy?: boolean;
						Labels?: Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>;
						DriverConfig?: {
							Name?: string;
							Options?: Record<
								string | number,
								/* additionalProperties is not handled yet */ unknown
							>;
						};
					};
					TmpfsOptions?: {
						SizeBytes?: number;
						Mode?: number;
					};
				}[];
				StopSignal?: string;
				StopGracePeriod?: number;
				HealthCheck?: {
					Test?: readonly string[];
					Interval?: number;
					Timeout?: number;
					Retries?: number;
					StartPeriod?: number;
				};
				Hosts?: readonly string[];
				DNSConfig?: {
					Nameservers?: readonly string[];
					Search?: readonly string[];
					Options?: readonly string[];
				};
				Secrets?: readonly {
					File?: {
						Name?: string;
						UID?: string;
						GID?: string;
						Mode?: number;
					};
					SecretID?: string;
					SecretName?: string;
				}[];
				Configs?: readonly {
					File?: {
						Name?: string;
						UID?: string;
						GID?: string;
						Mode?: number;
					};
					Runtime?: {};
					ConfigID?: string;
					ConfigName?: string;
				}[];
				/**
				 * Isolation technology of the containers running the service.
				 * (Windows only)
				 * @enum default,process,hyperv,
				 */
				Isolation?: "default" | "process" | "hyperv" | "";
				Init?: boolean | null;
				Sysctls?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
				/**
				 * A list of kernel capabilities to add to the default set
				 * for the container.
				 * @example CAP_NET_RAW,CAP_SYS_ADMIN,CAP_SYS_CHROOT,CAP_SYSLOG
				 */
				CapabilityAdd?: readonly string[];
				/**
				 * A list of kernel capabilities to drop from the default set
				 * for the container.
				 * @example CAP_NET_RAW
				 */
				CapabilityDrop?: readonly string[];
				Ulimits?: readonly {
					Name?: string;
					Soft?: number;
					Hard?: number;
				}[];
			};
			NetworkAttachmentSpec?: {
				ContainerID?: string;
			};
			Resources?: {
				Limits?: {
					NanoCPUs?: number;
					MemoryBytes?: number;
					/**
					 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
					 * @example 100
					 */
					Pids?: number;
				};
				Reservations?: {
					NanoCPUs?: number;
					MemoryBytes?: number;
					/**
					 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
					 * String resources (e.g, `GPU=UUID1`).
					 * @example [object Object],[object Object],[object Object]
					 */
					GenericResources?: readonly {
						NamedResourceSpec?: {
							Kind?: string;
							Value?: string;
						};
						DiscreteResourceSpec?: {
							Kind?: string;
							Value?: number;
						};
					}[];
				};
			};
			RestartPolicy?: {
				/**
				 * Condition for restart.
				 * @enum none,on-failure,any
				 */
				Condition?: "none" | "on-failure" | "any";
				Delay?: number;
				MaxAttempts?: number;
				Window?: number;
			};
			Placement?: {
				/**
				 * An array of constraint expressions to limit the set of nodes where
				 * a task can be scheduled. Constraint expressions can either use a
				 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
				 * nodes that satisfy every expression (AND match). Constraints can
				 * match node or Docker Engine labels as follows:
				 *
				 * node attribute       | matches                        | example
				 * ---------------------|--------------------------------|-----------------------------------------------
				 * `node.id`            | Node ID                        |
				 * `node.id==2ivku8v2gvtg4`
				 * `node.hostname`      | Node hostname                  |
				 * `node.hostname!=node-2`
				 * `node.role`          | Node role (`manager`/`worker`) |
				 * `node.role==manager`
				 * `node.platform.os`   | Node operating system          |
				 * `node.platform.os==windows`
				 * `node.platform.arch` | Node architecture              |
				 * `node.platform.arch==x86_64`
				 * `node.labels`        | User-defined node labels       |
				 * `node.labels.security==high`
				 * `engine.labels`      | Docker Engine's labels         |
				 * `engine.labels.operatingsystem==ubuntu-14.04`
				 *
				 * `engine.labels` apply to Docker Engine labels like operating system,
				 * drivers, etc. Swarm administrators add `node.labels` for operational
				 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
				 * @example node.hostname!=node3.corp.example.com,node.role!=manager,node.labels.type==production,node.platform.os==linux,node.platform.arch==x86_64
				 */
				Constraints?: readonly string[];
				/**
				 * Preferences provide a way to make the scheduler aware of factors
				 * such as topology. They are provided in order from highest to
				 * lowest precedence.
				 * @example [object Object],[object Object]
				 */
				Preferences?: readonly {
					Spread?: {
						SpreadDescriptor?: string;
					};
				}[];
				MaxReplicas?: number;
				Platforms?: readonly {
					/**
					 * Architecture represents the hardware architecture (for example,
					 * `x86_64`).
					 * @example x86_64
					 */
					Architecture?: string;
					/**
					 * OS represents the Operating System (for example, `linux` or `windows`).
					 * @example linux
					 */
					OS?: string;
				}[];
			};
			ForceUpdate?: number;
			Runtime?: string;
			Networks?: readonly {
				Target?: string;
				Aliases?: readonly string[];
				DriverOpts?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			}[];
			LogDriver?: {
				Name?: string;
				Options?: Record<
					string | number,
					/* additionalProperties is not handled yet */ unknown
				>;
			};
		};
		Mode?: {
			Replicated?: {
				Replicas?: number;
			};
			Global?: {};
			ReplicatedJob?: {
				/**
				 * The maximum number of replicas to run simultaneously.
				 * @default 1
				 */
				MaxConcurrent?: number;
				TotalCompletions?: number;
			};
			GlobalJob?: {};
		};
		UpdateConfig?: {
			Parallelism?: number;
			Delay?: number;
			/**
			 * Action to take if an updated task fails to run, or stops running
			 * during the update.
			 * @enum continue,pause,rollback
			 */
			FailureAction?: "continue" | "pause" | "rollback";
			Monitor?: number;
			MaxFailureRatio?: number;
			/**
			 * The order of operations when rolling out an updated task. Either
			 * the old task is shut down before the new task is started, or the
			 * new task is started before the old task is shut down.
			 * @enum stop-first,start-first
			 */
			Order?: "stop-first" | "start-first";
		};
		RollbackConfig?: {
			Parallelism?: number;
			Delay?: number;
			/**
			 * Action to take if an rolled back task fails to run, or stops
			 * running during the rollback.
			 * @enum continue,pause
			 */
			FailureAction?: "continue" | "pause";
			Monitor?: number;
			MaxFailureRatio?: number;
			/**
			 * The order of operations when rolling back a task. Either the old
			 * task is shut down before the new task is started, or the new task
			 * is started before the old task is shut down.
			 * @enum stop-first,start-first
			 */
			Order?: "stop-first" | "start-first";
		};
		Networks?: readonly {
			Target?: string;
			Aliases?: readonly string[];
			DriverOpts?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		EndpointSpec?: {
			/**
			 * The mode of resolution to use for internal load balancing between tasks.
			 * @default vip
			 * @enum vip,dnsrr
			 */
			Mode?: "vip" | "dnsrr";
			Ports?: readonly {
				Name?: string;
				Protocol?: "tcp" | "udp" | "sctp";
				TargetPort?: number;
				PublishedPort?: number;
				/**
				 * The mode in which port is published.
				 *
				 * <p><br /></p>
				 *
				 * - "ingress" makes the target port accessible on every node,
				 *
				 * regardless of whether there is a task for the service running on
				 *
				 * that node or not.
				 * - "host" bypasses the routing mesh and publish the port directly on
				 *
				 * the swarm node where that service is running.
				 * @default ingress
				 * @enum ingress,host
				 * @example ingress
				 */
				PublishMode?: "ingress" | "host";
			}[];
		};
	};
	Endpoint?: {
		Spec?: {
			/**
			 * The mode of resolution to use for internal load balancing between tasks.
			 * @default vip
			 * @enum vip,dnsrr
			 */
			Mode?: "vip" | "dnsrr";
			Ports?: readonly {
				Name?: string;
				Protocol?: "tcp" | "udp" | "sctp";
				TargetPort?: number;
				PublishedPort?: number;
				/**
				 * The mode in which port is published.
				 *
				 * <p><br /></p>
				 *
				 * - "ingress" makes the target port accessible on every node,
				 *
				 * regardless of whether there is a task for the service running on
				 *
				 * that node or not.
				 * - "host" bypasses the routing mesh and publish the port directly on
				 *
				 * the swarm node where that service is running.
				 * @default ingress
				 * @enum ingress,host
				 * @example ingress
				 */
				PublishMode?: "ingress" | "host";
			}[];
		};
		Ports?: readonly {
			Name?: string;
			Protocol?: "tcp" | "udp" | "sctp";
			TargetPort?: number;
			PublishedPort?: number;
			/**
			 * The mode in which port is published.
			 *
			 * <p><br /></p>
			 *
			 * - "ingress" makes the target port accessible on every node,
			 *
			 * regardless of whether there is a task for the service running on
			 *
			 * that node or not.
			 * - "host" bypasses the routing mesh and publish the port directly on
			 *
			 * the swarm node where that service is running.
			 * @default ingress
			 * @enum ingress,host
			 * @example ingress
			 */
			PublishMode?: "ingress" | "host";
		}[];
		VirtualIPs?: readonly {
			NetworkID?: string;
			Addr?: string;
		}[];
	};
	UpdateStatus?: {
		State?: "updating" | "paused" | "completed";
		StartedAt?: string;
		CompletedAt?: string;
		Message?: string;
	};
	ServiceStatus?: {
		/**
		 * The number of tasks for the service currently in the Running state.
		 * @example 7
		 */
		RunningTasks?: number;
		/**
		 * The number of tasks for the service desired to be running.
		 * For replicated services, this is the replica count from the
		 * service spec. For global services, this is computed by taking
		 * count of all tasks for the service with a Desired State other
		 * than Shutdown.
		 * @example 10
		 */
		DesiredTasks?: number;
		CompletedTasks?: number;
	};
	JobStatus?: {
		JobIteration?: {
			Index?: number;
		};
		LastExecution?: string;
	};
};
export type ServiceDeleteCommandParams = {
	id: string;
};
export type ServiceDeleteCommandInput = ServiceDeleteCommandParams;
export type ServiceUpdateCommandQuery = {
	version: `${number}`;
	registryAuthFrom?: "spec" | "previous-spec";
	rollback?: string | undefined;
};
export type ServiceUpdateCommandHeader = {
	"x-registry-auth"?: string;
};
type ServiceUpdateJsonBody = {
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	TaskTemplate?: {
		PluginSpec?: {
			Name?: string;
			Remote?: string;
			Disabled?: boolean;
			PluginPrivilege?: readonly {
				Name?: string;
				Description?: string;
				Value?: readonly string[];
			}[];
		};
		ContainerSpec?: {
			Image?: string;
			Labels?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			Command?: readonly string[];
			Args?: readonly string[];
			Hostname?: string;
			Env?: readonly string[];
			Dir?: string;
			User?: string;
			Groups?: readonly string[];
			Privileges?: {
				CredentialSpec?: {
					/**
					 * Load credential spec from a Swarm Config with the given ID.
					 * The specified config must also be present in the Configs
					 * field with the Runtime property set.
					 *
					 * <p><br /></p>
					 *
					 *
					 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
					 * > and `CredentialSpec.Config` are mutually exclusive.
					 * @example 0bt9dmxjvjiqermk6xrop3ekq
					 */
					Config?: string;
					/**
					 * Load credential spec from this file. The file is read by
					 * the daemon, and must be present in the `CredentialSpecs`
					 * subdirectory in the docker data directory, which defaults
					 * to `C:\ProgramData\Docker\` on Windows.
					 *
					 * For example, specifying `spec.json` loads
					 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
					 *
					 * <p><br /></p>
					 *
					 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
					 * > and `CredentialSpec.Config` are mutually exclusive.
					 * @example spec.json
					 */
					File?: string;
					Registry?: string;
				};
				SELinuxContext?: {
					Disable?: boolean;
					User?: string;
					Role?: string;
					Type?: string;
					Level?: string;
				};
			};
			TTY?: boolean;
			OpenStdin?: boolean;
			ReadOnly?: boolean;
			Mounts?: readonly {
				Target?: string;
				Source?: string;
				/**
				 * The mount type. Available types:
				 *
				 * - `bind` Mounts a file or directory from the host into the container. Must
				 * exist prior to creating the container.
				 * - `volume` Creates a volume with the given name and options (or uses a
				 * pre-existing volume with the same name and options). These are **not**
				 * removed when the container is removed.
				 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
				 * specified for tmpfs.
				 * - `npipe` Mounts a named pipe from the host into the container. Must exist
				 * prior to creating the container.
				 * - `cluster` a Swarm cluster volume
				 * @enum bind,volume,tmpfs,npipe,cluster
				 */
				Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
				ReadOnly?: boolean;
				Consistency?: string;
				BindOptions?: {
					/**
					 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
					 * @enum private,rprivate,shared,rshared,slave,rslave
					 */
					Propagation?:
						| "private"
						| "rprivate"
						| "shared"
						| "rshared"
						| "slave"
						| "rslave";
					NonRecursive?: boolean;
					CreateMountpoint?: boolean;
				};
				VolumeOptions?: {
					NoCopy?: boolean;
					Labels?: Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>;
					DriverConfig?: {
						Name?: string;
						Options?: Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>;
					};
				};
				TmpfsOptions?: {
					SizeBytes?: number;
					Mode?: number;
				};
			}[];
			StopSignal?: string;
			StopGracePeriod?: number;
			HealthCheck?: {
				Test?: readonly string[];
				Interval?: number;
				Timeout?: number;
				Retries?: number;
				StartPeriod?: number;
			};
			Hosts?: readonly string[];
			DNSConfig?: {
				Nameservers?: readonly string[];
				Search?: readonly string[];
				Options?: readonly string[];
			};
			Secrets?: readonly {
				File?: {
					Name?: string;
					UID?: string;
					GID?: string;
					Mode?: number;
				};
				SecretID?: string;
				SecretName?: string;
			}[];
			Configs?: readonly {
				File?: {
					Name?: string;
					UID?: string;
					GID?: string;
					Mode?: number;
				};
				Runtime?: {};
				ConfigID?: string;
				ConfigName?: string;
			}[];
			/**
			 * Isolation technology of the containers running the service.
			 * (Windows only)
			 * @enum default,process,hyperv,
			 */
			Isolation?: "default" | "process" | "hyperv" | "";
			Init?: boolean | null;
			Sysctls?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			/**
			 * A list of kernel capabilities to add to the default set
			 * for the container.
			 * @example CAP_NET_RAW,CAP_SYS_ADMIN,CAP_SYS_CHROOT,CAP_SYSLOG
			 */
			CapabilityAdd?: readonly string[];
			/**
			 * A list of kernel capabilities to drop from the default set
			 * for the container.
			 * @example CAP_NET_RAW
			 */
			CapabilityDrop?: readonly string[];
			Ulimits?: readonly {
				Name?: string;
				Soft?: number;
				Hard?: number;
			}[];
		};
		NetworkAttachmentSpec?: {
			ContainerID?: string;
		};
		Resources?: {
			Limits?: {
				NanoCPUs?: number;
				MemoryBytes?: number;
				/**
				 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
				 * @example 100
				 */
				Pids?: number;
			};
			Reservations?: {
				NanoCPUs?: number;
				MemoryBytes?: number;
				/**
				 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
				 * String resources (e.g, `GPU=UUID1`).
				 * @example [object Object],[object Object],[object Object]
				 */
				GenericResources?: readonly {
					NamedResourceSpec?: {
						Kind?: string;
						Value?: string;
					};
					DiscreteResourceSpec?: {
						Kind?: string;
						Value?: number;
					};
				}[];
			};
		};
		RestartPolicy?: {
			/**
			 * Condition for restart.
			 * @enum none,on-failure,any
			 */
			Condition?: "none" | "on-failure" | "any";
			Delay?: number;
			MaxAttempts?: number;
			Window?: number;
		};
		Placement?: {
			/**
			 * An array of constraint expressions to limit the set of nodes where
			 * a task can be scheduled. Constraint expressions can either use a
			 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
			 * nodes that satisfy every expression (AND match). Constraints can
			 * match node or Docker Engine labels as follows:
			 *
			 * node attribute       | matches                        | example
			 * ---------------------|--------------------------------|-----------------------------------------------
			 * `node.id`            | Node ID                        |
			 * `node.id==2ivku8v2gvtg4`
			 * `node.hostname`      | Node hostname                  |
			 * `node.hostname!=node-2`
			 * `node.role`          | Node role (`manager`/`worker`) |
			 * `node.role==manager`
			 * `node.platform.os`   | Node operating system          |
			 * `node.platform.os==windows`
			 * `node.platform.arch` | Node architecture              |
			 * `node.platform.arch==x86_64`
			 * `node.labels`        | User-defined node labels       |
			 * `node.labels.security==high`
			 * `engine.labels`      | Docker Engine's labels         |
			 * `engine.labels.operatingsystem==ubuntu-14.04`
			 *
			 * `engine.labels` apply to Docker Engine labels like operating system,
			 * drivers, etc. Swarm administrators add `node.labels` for operational
			 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
			 * @example node.hostname!=node3.corp.example.com,node.role!=manager,node.labels.type==production,node.platform.os==linux,node.platform.arch==x86_64
			 */
			Constraints?: readonly string[];
			/**
			 * Preferences provide a way to make the scheduler aware of factors
			 * such as topology. They are provided in order from highest to
			 * lowest precedence.
			 * @example [object Object],[object Object]
			 */
			Preferences?: readonly {
				Spread?: {
					SpreadDescriptor?: string;
				};
			}[];
			MaxReplicas?: number;
			Platforms?: readonly {
				/**
				 * Architecture represents the hardware architecture (for example,
				 * `x86_64`).
				 * @example x86_64
				 */
				Architecture?: string;
				/**
				 * OS represents the Operating System (for example, `linux` or `windows`).
				 * @example linux
				 */
				OS?: string;
			}[];
		};
		ForceUpdate?: number;
		Runtime?: string;
		Networks?: readonly {
			Target?: string;
			Aliases?: readonly string[];
			DriverOpts?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		LogDriver?: {
			Name?: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
	Mode?: {
		Replicated?: {
			Replicas?: number;
		};
		Global?: {};
		ReplicatedJob?: {
			/**
			 * The maximum number of replicas to run simultaneously.
			 * @default 1
			 */
			MaxConcurrent?: number;
			TotalCompletions?: number;
		};
		GlobalJob?: {};
	};
	UpdateConfig?: {
		Parallelism?: number;
		Delay?: number;
		/**
		 * Action to take if an updated task fails to run, or stops running
		 * during the update.
		 * @enum continue,pause,rollback
		 */
		FailureAction?: "continue" | "pause" | "rollback";
		Monitor?: number;
		MaxFailureRatio?: number;
		/**
		 * The order of operations when rolling out an updated task. Either
		 * the old task is shut down before the new task is started, or the
		 * new task is started before the old task is shut down.
		 * @enum stop-first,start-first
		 */
		Order?: "stop-first" | "start-first";
	};
	RollbackConfig?: {
		Parallelism?: number;
		Delay?: number;
		/**
		 * Action to take if an rolled back task fails to run, or stops
		 * running during the rollback.
		 * @enum continue,pause
		 */
		FailureAction?: "continue" | "pause";
		Monitor?: number;
		MaxFailureRatio?: number;
		/**
		 * The order of operations when rolling back a task. Either the old
		 * task is shut down before the new task is started, or the new task
		 * is started before the old task is shut down.
		 * @enum stop-first,start-first
		 */
		Order?: "stop-first" | "start-first";
	};
	Networks?: readonly {
		Target?: string;
		Aliases?: readonly string[];
		DriverOpts?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	}[];
	EndpointSpec?: {
		/**
		 * The mode of resolution to use for internal load balancing between tasks.
		 * @default vip
		 * @enum vip,dnsrr
		 */
		Mode?: "vip" | "dnsrr";
		Ports?: readonly {
			Name?: string;
			Protocol?: "tcp" | "udp" | "sctp";
			TargetPort?: number;
			PublishedPort?: number;
			/**
			 * The mode in which port is published.
			 *
			 * <p><br /></p>
			 *
			 * - "ingress" makes the target port accessible on every node,
			 *
			 * regardless of whether there is a task for the service running on
			 *
			 * that node or not.
			 * - "host" bypasses the routing mesh and publish the port directly on
			 *
			 * the swarm node where that service is running.
			 * @default ingress
			 * @enum ingress,host
			 * @example ingress
			 */
			PublishMode?: "ingress" | "host";
		}[];
	};
} & Record<string | number, Jsonifiable>;
export type ServiceUpdateCommandParams = {
	id: string;
};
export type ServiceUpdateCommandBody = ServiceUpdateJsonBody;
export type ServiceUpdateCommandInput = ServiceUpdateJsonBody &
	ServiceUpdateCommandParams &
	ServiceUpdateCommandQuery;
export type ServiceUpdateCommandOutput = {
	Warnings?: readonly string[];
};
export type ServiceLogsCommandQuery = {
	details?: "true" | "false";
	follow?: "true" | "false";
	stdout?: "true" | "false";
	stderr?: "true" | "false";
	since?: `${number}` | undefined;
	timestamps?: "true" | "false";
	tail?: string | undefined;
};
export type ServiceLogsCommandParams = {
	id: string;
};
export type ServiceLogsCommandInput = ServiceLogsCommandParams &
	ServiceLogsCommandQuery;
export type TaskListCommandQuery = {
	filters?: string | undefined;
};
export type TaskListCommandInput = TaskListCommandQuery;
export type TaskListCommandOutput = readonly {
	ID?: string;
	Version?: {
		Index?: number;
	};
	CreatedAt?: string;
	UpdatedAt?: string;
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Spec?: {
		PluginSpec?: {
			Name?: string;
			Remote?: string;
			Disabled?: boolean;
			PluginPrivilege?: readonly {
				Name?: string;
				Description?: string;
				Value?: readonly string[];
			}[];
		};
		ContainerSpec?: {
			Image?: string;
			Labels?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			Command?: readonly string[];
			Args?: readonly string[];
			Hostname?: string;
			Env?: readonly string[];
			Dir?: string;
			User?: string;
			Groups?: readonly string[];
			Privileges?: {
				CredentialSpec?: {
					/**
					 * Load credential spec from a Swarm Config with the given ID.
					 * The specified config must also be present in the Configs
					 * field with the Runtime property set.
					 *
					 * <p><br /></p>
					 *
					 *
					 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
					 * > and `CredentialSpec.Config` are mutually exclusive.
					 * @example 0bt9dmxjvjiqermk6xrop3ekq
					 */
					Config?: string;
					/**
					 * Load credential spec from this file. The file is read by
					 * the daemon, and must be present in the `CredentialSpecs`
					 * subdirectory in the docker data directory, which defaults
					 * to `C:\ProgramData\Docker\` on Windows.
					 *
					 * For example, specifying `spec.json` loads
					 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
					 *
					 * <p><br /></p>
					 *
					 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
					 * > and `CredentialSpec.Config` are mutually exclusive.
					 * @example spec.json
					 */
					File?: string;
					Registry?: string;
				};
				SELinuxContext?: {
					Disable?: boolean;
					User?: string;
					Role?: string;
					Type?: string;
					Level?: string;
				};
			};
			TTY?: boolean;
			OpenStdin?: boolean;
			ReadOnly?: boolean;
			Mounts?: readonly {
				Target?: string;
				Source?: string;
				/**
				 * The mount type. Available types:
				 *
				 * - `bind` Mounts a file or directory from the host into the container. Must
				 * exist prior to creating the container.
				 * - `volume` Creates a volume with the given name and options (or uses a
				 * pre-existing volume with the same name and options). These are **not**
				 * removed when the container is removed.
				 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
				 * specified for tmpfs.
				 * - `npipe` Mounts a named pipe from the host into the container. Must exist
				 * prior to creating the container.
				 * - `cluster` a Swarm cluster volume
				 * @enum bind,volume,tmpfs,npipe,cluster
				 */
				Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
				ReadOnly?: boolean;
				Consistency?: string;
				BindOptions?: {
					/**
					 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
					 * @enum private,rprivate,shared,rshared,slave,rslave
					 */
					Propagation?:
						| "private"
						| "rprivate"
						| "shared"
						| "rshared"
						| "slave"
						| "rslave";
					NonRecursive?: boolean;
					CreateMountpoint?: boolean;
				};
				VolumeOptions?: {
					NoCopy?: boolean;
					Labels?: Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>;
					DriverConfig?: {
						Name?: string;
						Options?: Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>;
					};
				};
				TmpfsOptions?: {
					SizeBytes?: number;
					Mode?: number;
				};
			}[];
			StopSignal?: string;
			StopGracePeriod?: number;
			HealthCheck?: {
				Test?: readonly string[];
				Interval?: number;
				Timeout?: number;
				Retries?: number;
				StartPeriod?: number;
			};
			Hosts?: readonly string[];
			DNSConfig?: {
				Nameservers?: readonly string[];
				Search?: readonly string[];
				Options?: readonly string[];
			};
			Secrets?: readonly {
				File?: {
					Name?: string;
					UID?: string;
					GID?: string;
					Mode?: number;
				};
				SecretID?: string;
				SecretName?: string;
			}[];
			Configs?: readonly {
				File?: {
					Name?: string;
					UID?: string;
					GID?: string;
					Mode?: number;
				};
				Runtime?: {};
				ConfigID?: string;
				ConfigName?: string;
			}[];
			/**
			 * Isolation technology of the containers running the service.
			 * (Windows only)
			 * @enum default,process,hyperv,
			 */
			Isolation?: "default" | "process" | "hyperv" | "";
			Init?: boolean | null;
			Sysctls?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			/**
			 * A list of kernel capabilities to add to the default set
			 * for the container.
			 * @example CAP_NET_RAW,CAP_SYS_ADMIN,CAP_SYS_CHROOT,CAP_SYSLOG
			 */
			CapabilityAdd?: readonly string[];
			/**
			 * A list of kernel capabilities to drop from the default set
			 * for the container.
			 * @example CAP_NET_RAW
			 */
			CapabilityDrop?: readonly string[];
			Ulimits?: readonly {
				Name?: string;
				Soft?: number;
				Hard?: number;
			}[];
		};
		NetworkAttachmentSpec?: {
			ContainerID?: string;
		};
		Resources?: {
			Limits?: {
				NanoCPUs?: number;
				MemoryBytes?: number;
				/**
				 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
				 * @example 100
				 */
				Pids?: number;
			};
			Reservations?: {
				NanoCPUs?: number;
				MemoryBytes?: number;
				/**
				 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
				 * String resources (e.g, `GPU=UUID1`).
				 * @example [object Object],[object Object],[object Object]
				 */
				GenericResources?: readonly {
					NamedResourceSpec?: {
						Kind?: string;
						Value?: string;
					};
					DiscreteResourceSpec?: {
						Kind?: string;
						Value?: number;
					};
				}[];
			};
		};
		RestartPolicy?: {
			/**
			 * Condition for restart.
			 * @enum none,on-failure,any
			 */
			Condition?: "none" | "on-failure" | "any";
			Delay?: number;
			MaxAttempts?: number;
			Window?: number;
		};
		Placement?: {
			/**
			 * An array of constraint expressions to limit the set of nodes where
			 * a task can be scheduled. Constraint expressions can either use a
			 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
			 * nodes that satisfy every expression (AND match). Constraints can
			 * match node or Docker Engine labels as follows:
			 *
			 * node attribute       | matches                        | example
			 * ---------------------|--------------------------------|-----------------------------------------------
			 * `node.id`            | Node ID                        |
			 * `node.id==2ivku8v2gvtg4`
			 * `node.hostname`      | Node hostname                  |
			 * `node.hostname!=node-2`
			 * `node.role`          | Node role (`manager`/`worker`) |
			 * `node.role==manager`
			 * `node.platform.os`   | Node operating system          |
			 * `node.platform.os==windows`
			 * `node.platform.arch` | Node architecture              |
			 * `node.platform.arch==x86_64`
			 * `node.labels`        | User-defined node labels       |
			 * `node.labels.security==high`
			 * `engine.labels`      | Docker Engine's labels         |
			 * `engine.labels.operatingsystem==ubuntu-14.04`
			 *
			 * `engine.labels` apply to Docker Engine labels like operating system,
			 * drivers, etc. Swarm administrators add `node.labels` for operational
			 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
			 * @example node.hostname!=node3.corp.example.com,node.role!=manager,node.labels.type==production,node.platform.os==linux,node.platform.arch==x86_64
			 */
			Constraints?: readonly string[];
			/**
			 * Preferences provide a way to make the scheduler aware of factors
			 * such as topology. They are provided in order from highest to
			 * lowest precedence.
			 * @example [object Object],[object Object]
			 */
			Preferences?: readonly {
				Spread?: {
					SpreadDescriptor?: string;
				};
			}[];
			MaxReplicas?: number;
			Platforms?: readonly {
				/**
				 * Architecture represents the hardware architecture (for example,
				 * `x86_64`).
				 * @example x86_64
				 */
				Architecture?: string;
				/**
				 * OS represents the Operating System (for example, `linux` or `windows`).
				 * @example linux
				 */
				OS?: string;
			}[];
		};
		ForceUpdate?: number;
		Runtime?: string;
		Networks?: readonly {
			Target?: string;
			Aliases?: readonly string[];
			DriverOpts?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		LogDriver?: {
			Name?: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
	ServiceID?: string;
	Slot?: number;
	NodeID?: string;
	/**
	 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
	 * String resources (e.g, `GPU=UUID1`).
	 * @example [object Object],[object Object],[object Object]
	 */
	AssignedGenericResources?: readonly {
		NamedResourceSpec?: {
			Kind?: string;
			Value?: string;
		};
		DiscreteResourceSpec?: {
			Kind?: string;
			Value?: number;
		};
	}[];
	Status?: {
		Timestamp?: string;
		State?:
			| "new"
			| "allocated"
			| "pending"
			| "assigned"
			| "accepted"
			| "preparing"
			| "ready"
			| "starting"
			| "running"
			| "complete"
			| "shutdown"
			| "failed"
			| "rejected"
			| "remove"
			| "orphaned";
		Message?: string;
		Err?: string;
		ContainerStatus?: {
			ContainerID?: string;
			PID?: number;
			ExitCode?: number;
		};
	};
	DesiredState?:
		| "new"
		| "allocated"
		| "pending"
		| "assigned"
		| "accepted"
		| "preparing"
		| "ready"
		| "starting"
		| "running"
		| "complete"
		| "shutdown"
		| "failed"
		| "rejected"
		| "remove"
		| "orphaned";
	JobIteration?: {
		Index?: number;
	};
}[];
export type TaskInspectCommandParams = {
	id: string;
};
export type TaskInspectCommandInput = TaskInspectCommandParams;
export type TaskInspectCommandOutput = {
	ID?: string;
	Version?: {
		Index?: number;
	};
	CreatedAt?: string;
	UpdatedAt?: string;
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Spec?: {
		PluginSpec?: {
			Name?: string;
			Remote?: string;
			Disabled?: boolean;
			PluginPrivilege?: readonly {
				Name?: string;
				Description?: string;
				Value?: readonly string[];
			}[];
		};
		ContainerSpec?: {
			Image?: string;
			Labels?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			Command?: readonly string[];
			Args?: readonly string[];
			Hostname?: string;
			Env?: readonly string[];
			Dir?: string;
			User?: string;
			Groups?: readonly string[];
			Privileges?: {
				CredentialSpec?: {
					/**
					 * Load credential spec from a Swarm Config with the given ID.
					 * The specified config must also be present in the Configs
					 * field with the Runtime property set.
					 *
					 * <p><br /></p>
					 *
					 *
					 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
					 * > and `CredentialSpec.Config` are mutually exclusive.
					 * @example 0bt9dmxjvjiqermk6xrop3ekq
					 */
					Config?: string;
					/**
					 * Load credential spec from this file. The file is read by
					 * the daemon, and must be present in the `CredentialSpecs`
					 * subdirectory in the docker data directory, which defaults
					 * to `C:\ProgramData\Docker\` on Windows.
					 *
					 * For example, specifying `spec.json` loads
					 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
					 *
					 * <p><br /></p>
					 *
					 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
					 * > and `CredentialSpec.Config` are mutually exclusive.
					 * @example spec.json
					 */
					File?: string;
					Registry?: string;
				};
				SELinuxContext?: {
					Disable?: boolean;
					User?: string;
					Role?: string;
					Type?: string;
					Level?: string;
				};
			};
			TTY?: boolean;
			OpenStdin?: boolean;
			ReadOnly?: boolean;
			Mounts?: readonly {
				Target?: string;
				Source?: string;
				/**
				 * The mount type. Available types:
				 *
				 * - `bind` Mounts a file or directory from the host into the container. Must
				 * exist prior to creating the container.
				 * - `volume` Creates a volume with the given name and options (or uses a
				 * pre-existing volume with the same name and options). These are **not**
				 * removed when the container is removed.
				 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
				 * specified for tmpfs.
				 * - `npipe` Mounts a named pipe from the host into the container. Must exist
				 * prior to creating the container.
				 * - `cluster` a Swarm cluster volume
				 * @enum bind,volume,tmpfs,npipe,cluster
				 */
				Type?: "bind" | "volume" | "tmpfs" | "npipe" | "cluster";
				ReadOnly?: boolean;
				Consistency?: string;
				BindOptions?: {
					/**
					 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
					 * @enum private,rprivate,shared,rshared,slave,rslave
					 */
					Propagation?:
						| "private"
						| "rprivate"
						| "shared"
						| "rshared"
						| "slave"
						| "rslave";
					NonRecursive?: boolean;
					CreateMountpoint?: boolean;
				};
				VolumeOptions?: {
					NoCopy?: boolean;
					Labels?: Record<
						string | number,
						/* additionalProperties is not handled yet */ unknown
					>;
					DriverConfig?: {
						Name?: string;
						Options?: Record<
							string | number,
							/* additionalProperties is not handled yet */ unknown
						>;
					};
				};
				TmpfsOptions?: {
					SizeBytes?: number;
					Mode?: number;
				};
			}[];
			StopSignal?: string;
			StopGracePeriod?: number;
			HealthCheck?: {
				Test?: readonly string[];
				Interval?: number;
				Timeout?: number;
				Retries?: number;
				StartPeriod?: number;
			};
			Hosts?: readonly string[];
			DNSConfig?: {
				Nameservers?: readonly string[];
				Search?: readonly string[];
				Options?: readonly string[];
			};
			Secrets?: readonly {
				File?: {
					Name?: string;
					UID?: string;
					GID?: string;
					Mode?: number;
				};
				SecretID?: string;
				SecretName?: string;
			}[];
			Configs?: readonly {
				File?: {
					Name?: string;
					UID?: string;
					GID?: string;
					Mode?: number;
				};
				Runtime?: {};
				ConfigID?: string;
				ConfigName?: string;
			}[];
			/**
			 * Isolation technology of the containers running the service.
			 * (Windows only)
			 * @enum default,process,hyperv,
			 */
			Isolation?: "default" | "process" | "hyperv" | "";
			Init?: boolean | null;
			Sysctls?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
			/**
			 * A list of kernel capabilities to add to the default set
			 * for the container.
			 * @example CAP_NET_RAW,CAP_SYS_ADMIN,CAP_SYS_CHROOT,CAP_SYSLOG
			 */
			CapabilityAdd?: readonly string[];
			/**
			 * A list of kernel capabilities to drop from the default set
			 * for the container.
			 * @example CAP_NET_RAW
			 */
			CapabilityDrop?: readonly string[];
			Ulimits?: readonly {
				Name?: string;
				Soft?: number;
				Hard?: number;
			}[];
		};
		NetworkAttachmentSpec?: {
			ContainerID?: string;
		};
		Resources?: {
			Limits?: {
				NanoCPUs?: number;
				MemoryBytes?: number;
				/**
				 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
				 * @example 100
				 */
				Pids?: number;
			};
			Reservations?: {
				NanoCPUs?: number;
				MemoryBytes?: number;
				/**
				 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
				 * String resources (e.g, `GPU=UUID1`).
				 * @example [object Object],[object Object],[object Object]
				 */
				GenericResources?: readonly {
					NamedResourceSpec?: {
						Kind?: string;
						Value?: string;
					};
					DiscreteResourceSpec?: {
						Kind?: string;
						Value?: number;
					};
				}[];
			};
		};
		RestartPolicy?: {
			/**
			 * Condition for restart.
			 * @enum none,on-failure,any
			 */
			Condition?: "none" | "on-failure" | "any";
			Delay?: number;
			MaxAttempts?: number;
			Window?: number;
		};
		Placement?: {
			/**
			 * An array of constraint expressions to limit the set of nodes where
			 * a task can be scheduled. Constraint expressions can either use a
			 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
			 * nodes that satisfy every expression (AND match). Constraints can
			 * match node or Docker Engine labels as follows:
			 *
			 * node attribute       | matches                        | example
			 * ---------------------|--------------------------------|-----------------------------------------------
			 * `node.id`            | Node ID                        |
			 * `node.id==2ivku8v2gvtg4`
			 * `node.hostname`      | Node hostname                  |
			 * `node.hostname!=node-2`
			 * `node.role`          | Node role (`manager`/`worker`) |
			 * `node.role==manager`
			 * `node.platform.os`   | Node operating system          |
			 * `node.platform.os==windows`
			 * `node.platform.arch` | Node architecture              |
			 * `node.platform.arch==x86_64`
			 * `node.labels`        | User-defined node labels       |
			 * `node.labels.security==high`
			 * `engine.labels`      | Docker Engine's labels         |
			 * `engine.labels.operatingsystem==ubuntu-14.04`
			 *
			 * `engine.labels` apply to Docker Engine labels like operating system,
			 * drivers, etc. Swarm administrators add `node.labels` for operational
			 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
			 * @example node.hostname!=node3.corp.example.com,node.role!=manager,node.labels.type==production,node.platform.os==linux,node.platform.arch==x86_64
			 */
			Constraints?: readonly string[];
			/**
			 * Preferences provide a way to make the scheduler aware of factors
			 * such as topology. They are provided in order from highest to
			 * lowest precedence.
			 * @example [object Object],[object Object]
			 */
			Preferences?: readonly {
				Spread?: {
					SpreadDescriptor?: string;
				};
			}[];
			MaxReplicas?: number;
			Platforms?: readonly {
				/**
				 * Architecture represents the hardware architecture (for example,
				 * `x86_64`).
				 * @example x86_64
				 */
				Architecture?: string;
				/**
				 * OS represents the Operating System (for example, `linux` or `windows`).
				 * @example linux
				 */
				OS?: string;
			}[];
		};
		ForceUpdate?: number;
		Runtime?: string;
		Networks?: readonly {
			Target?: string;
			Aliases?: readonly string[];
			DriverOpts?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		}[];
		LogDriver?: {
			Name?: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
	ServiceID?: string;
	Slot?: number;
	NodeID?: string;
	/**
	 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
	 * String resources (e.g, `GPU=UUID1`).
	 * @example [object Object],[object Object],[object Object]
	 */
	AssignedGenericResources?: readonly {
		NamedResourceSpec?: {
			Kind?: string;
			Value?: string;
		};
		DiscreteResourceSpec?: {
			Kind?: string;
			Value?: number;
		};
	}[];
	Status?: {
		Timestamp?: string;
		State?:
			| "new"
			| "allocated"
			| "pending"
			| "assigned"
			| "accepted"
			| "preparing"
			| "ready"
			| "starting"
			| "running"
			| "complete"
			| "shutdown"
			| "failed"
			| "rejected"
			| "remove"
			| "orphaned";
		Message?: string;
		Err?: string;
		ContainerStatus?: {
			ContainerID?: string;
			PID?: number;
			ExitCode?: number;
		};
	};
	DesiredState?:
		| "new"
		| "allocated"
		| "pending"
		| "assigned"
		| "accepted"
		| "preparing"
		| "ready"
		| "starting"
		| "running"
		| "complete"
		| "shutdown"
		| "failed"
		| "rejected"
		| "remove"
		| "orphaned";
	JobIteration?: {
		Index?: number;
	};
};
export type TaskLogsCommandQuery = {
	details?: "true" | "false";
	follow?: "true" | "false";
	stdout?: "true" | "false";
	stderr?: "true" | "false";
	since?: `${number}` | undefined;
	timestamps?: "true" | "false";
	tail?: string | undefined;
};
export type TaskLogsCommandParams = {
	id: string;
};
export type TaskLogsCommandInput = TaskLogsCommandParams & TaskLogsCommandQuery;
export type SecretListCommandQuery = {
	filters?: string | undefined;
};
export type SecretListCommandInput = SecretListCommandQuery;
export type SecretListCommandOutput = readonly {
	ID?: string;
	Version?: {
		Index?: number;
	};
	CreatedAt?: string;
	UpdatedAt?: string;
	Spec?: {
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Base64-url-safe-encoded ([RFC
		 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
		 * data to store as secret.
		 *
		 * This field is only used to _create_ a secret, and is not returned by
		 * other endpoints.
		 * @example
		 */
		Data?: string;
		Driver?: {
			/**
			 * Name of the driver.
			 * @example some-driver
			 */
			Name: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
		Templating?: {
			/**
			 * Name of the driver.
			 * @example some-driver
			 */
			Name: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
}[];
type SecretCreateJsonBody = {
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Base64-url-safe-encoded ([RFC
	 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
	 * data to store as secret.
	 *
	 * This field is only used to _create_ a secret, and is not returned by
	 * other endpoints.
	 * @example
	 */
	Data?: string;
	Driver?: {
		/**
		 * Name of the driver.
		 * @example some-driver
		 */
		Name: string;
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	Templating?: {
		/**
		 * Name of the driver.
		 * @example some-driver
		 */
		Name: string;
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
} & Record<string | number, Jsonifiable>;
export type SecretCreateCommandBody = SecretCreateJsonBody;
export type SecretCreateCommandInput = SecretCreateJsonBody;
export type SecretCreateCommandOutput = {
	Id: string;
};
export type SecretInspectCommandParams = {
	id: string;
};
export type SecretInspectCommandInput = SecretInspectCommandParams;
export type SecretInspectCommandOutput = {
	ID?: string;
	Version?: {
		Index?: number;
	};
	CreatedAt?: string;
	UpdatedAt?: string;
	Spec?: {
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		/**
		 * Base64-url-safe-encoded ([RFC
		 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
		 * data to store as secret.
		 *
		 * This field is only used to _create_ a secret, and is not returned by
		 * other endpoints.
		 * @example
		 */
		Data?: string;
		Driver?: {
			/**
			 * Name of the driver.
			 * @example some-driver
			 */
			Name: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
		Templating?: {
			/**
			 * Name of the driver.
			 * @example some-driver
			 */
			Name: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
};
export type SecretDeleteCommandParams = {
	id: string;
};
export type SecretDeleteCommandInput = SecretDeleteCommandParams;
export type SecretUpdateCommandQuery = {
	version: `${number}`;
};
type SecretUpdateJsonBody = {
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	/**
	 * Base64-url-safe-encoded ([RFC
	 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
	 * data to store as secret.
	 *
	 * This field is only used to _create_ a secret, and is not returned by
	 * other endpoints.
	 * @example
	 */
	Data?: string;
	Driver?: {
		/**
		 * Name of the driver.
		 * @example some-driver
		 */
		Name: string;
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
	Templating?: {
		/**
		 * Name of the driver.
		 * @example some-driver
		 */
		Name: string;
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
};
export type SecretUpdateCommandParams = {
	id: string;
};
export type SecretUpdateCommandBody = SecretUpdateJsonBody;
export type SecretUpdateCommandInput = SecretUpdateJsonBody &
	SecretUpdateCommandParams &
	SecretUpdateCommandQuery;
export type ConfigListCommandQuery = {
	filters?: string | undefined;
};
export type ConfigListCommandInput = ConfigListCommandQuery;
export type ConfigListCommandOutput = readonly {
	ID?: string;
	Version?: {
		Index?: number;
	};
	CreatedAt?: string;
	UpdatedAt?: string;
	Spec?: {
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Data?: string;
		Templating?: {
			/**
			 * Name of the driver.
			 * @example some-driver
			 */
			Name: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
}[];
type ConfigCreateJsonBody = {
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Data?: string;
	Templating?: {
		/**
		 * Name of the driver.
		 * @example some-driver
		 */
		Name: string;
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
} & Record<string | number, Jsonifiable>;
export type ConfigCreateCommandBody = ConfigCreateJsonBody;
export type ConfigCreateCommandInput = ConfigCreateJsonBody;
export type ConfigCreateCommandOutput = {
	Id: string;
};
export type ConfigInspectCommandParams = {
	id: string;
};
export type ConfigInspectCommandInput = ConfigInspectCommandParams;
export type ConfigInspectCommandOutput = {
	ID?: string;
	Version?: {
		Index?: number;
	};
	CreatedAt?: string;
	UpdatedAt?: string;
	Spec?: {
		Name?: string;
		Labels?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
		Data?: string;
		Templating?: {
			/**
			 * Name of the driver.
			 * @example some-driver
			 */
			Name: string;
			Options?: Record<
				string | number,
				/* additionalProperties is not handled yet */ unknown
			>;
		};
	};
};
export type ConfigDeleteCommandParams = {
	id: string;
};
export type ConfigDeleteCommandInput = ConfigDeleteCommandParams;
export type ConfigUpdateCommandQuery = {
	version: `${number}`;
};
type ConfigUpdateJsonBody = {
	Name?: string;
	Labels?: Record<
		string | number,
		/* additionalProperties is not handled yet */ unknown
	>;
	Data?: string;
	Templating?: {
		/**
		 * Name of the driver.
		 * @example some-driver
		 */
		Name: string;
		Options?: Record<
			string | number,
			/* additionalProperties is not handled yet */ unknown
		>;
	};
};
export type ConfigUpdateCommandParams = {
	id: string;
};
export type ConfigUpdateCommandBody = ConfigUpdateJsonBody;
export type ConfigUpdateCommandInput = ConfigUpdateJsonBody &
	ConfigUpdateCommandParams &
	ConfigUpdateCommandQuery;
export type DistributionInspectCommandParams = {
	name: string;
};
export type DistributionInspectCommandInput = DistributionInspectCommandParams;
export type DistributionInspectCommandOutput = {
	Descriptor: {
		/**
		 * The media type of the object this schema refers to.
		 * @example application/vnd.docker.distribution.manifest.v2+json
		 */
		mediaType?: string;
		/**
		 * The digest of the targeted content.
		 * @example sha256:c0537ff6a5218ef531ece93d4984efc99bbf3f7497c0a7726c88e2bb7584dc96
		 */
		digest?: string;
		/**
		 * The size in bytes of the blob.
		 * @example 3987495
		 */
		size?: number;
	};
	Platforms: readonly {
		/**
		 * The CPU architecture, for example `amd64` or `ppc64`.
		 * @example arm
		 */
		architecture?: string;
		/**
		 * The operating system, for example `linux` or `windows`.
		 * @example windows
		 */
		os?: string;
		/**
		 * Optional field specifying the operating system version, for example on
		 * Windows `10.0.19041.1165`.
		 * @example 10.0.19041.1165
		 */
		"os.version"?: string;
		/**
		 * Optional field specifying an array of strings, each listing a required
		 * OS feature (for example on Windows `win32k`).
		 * @example win32k
		 */
		"os.features"?: readonly string[];
		/**
		 * Optional field specifying a variant of the CPU, for example `v7` to
		 * specify ARMv7 when architecture is `arm`.
		 * @example v7
		 */
		variant?: string;
	}[];
};
export type SessionCommandInput = never;
