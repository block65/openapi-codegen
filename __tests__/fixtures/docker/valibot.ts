/**
 * This file was auto generated by @block65/openapi-codegen
 *
 * WARN: Do not edit directly.
 *
 * Generated on 2026-03-06T07:45:59.608Z
 *
 */
import * as v from "valibot";

/**
 * An open port on a container
 * @example {
 *   "PrivatePort": 8080,
 *   "PublicPort": 80,
 *   "Type": "tcp"
 * }
 */
export const portSchema = v.strictObject({
	/**
	 * Host IP address that the container's port is mapped to
	 */
	IP: v.exactOptional(v.string()),
	/**
	 * Port on the container
	 */
	PrivatePort: v.pipe(v.number(), v.integer()),
	/**
	 * Port exposed on the host
	 */
	PublicPort: v.exactOptional(v.pipe(v.number(), v.integer())),
	Type: v.picklist(["tcp", "udp", "sctp"]),
});
/**
 * MountPoint represents a mount point configuration inside the container.
 * This is used for reporting the mountpoints in use by a container.
 */
export const mountPointSchema = v.strictObject({
	/**
	 * The mount type:
	 *
	 * - `bind` a mount of a file or directory from the host into the container.
	 * - `volume` a docker volume with the given `Name`.
	 * - `tmpfs` a `tmpfs`.
	 * - `npipe` a named pipe from the host into the container.
	 * - `cluster` a Swarm cluster volume
	 */
	Type: v.exactOptional(
		v.picklist(["bind", "volume", "tmpfs", "npipe", "cluster"]),
	),
	/**
	 * Name is the name reference to the underlying data defined by `Source`
	 * e.g., the volume name.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * Source location of the mount.
	 *
	 * For volumes, this contains the storage location of the volume (within
	 * `/var/lib/docker/volumes/`). For bind-mounts, and `npipe`, this contains
	 * the source (host) part of the bind-mount. For `tmpfs` mount points, this
	 * field is empty.
	 */
	Source: v.exactOptional(v.string()),
	/**
	 * Destination is the path relative to the container root (`/`) where
	 * the `Source` is mounted inside the container.
	 */
	Destination: v.exactOptional(v.string()),
	/**
	 * Driver is the volume driver used to create the volume (if it is a volume).
	 */
	Driver: v.exactOptional(v.string()),
	/**
	 * Mode is a comma separated list of options supplied by the user when
	 * creating the bind/volume mount.
	 *
	 * The default is platform-specific (`"z"` on Linux, empty on Windows).
	 */
	Mode: v.exactOptional(v.string()),
	/**
	 * Whether the mount is mounted writable (read-write).
	 */
	RW: v.exactOptional(v.boolean()),
	/**
	 * Propagation describes how mounts are propagated from the host into the
	 * mount point, and vice-versa. Refer to the [Linux kernel
	 * documentation](https://www.kernel.org/doc/Documentation/filesystems/sharedsubtree.txt)
	 * for details. This field is not used on Windows.
	 */
	Propagation: v.exactOptional(v.string()),
});
/**
 * A device mapping between the host and container
 * @example {
 *   "PathOnHost": "/dev/deviceName",
 *   "PathInContainer": "/dev/deviceName",
 *   "CgroupPermissions": "mrw"
 * }
 */
export const deviceMappingSchema = v.strictObject({
	PathOnHost: v.exactOptional(v.string()),
	PathInContainer: v.exactOptional(v.string()),
	CgroupPermissions: v.exactOptional(v.string()),
});
/** A request for devices to be sent to device drivers */
export const deviceRequestSchema = v.strictObject({
	Driver: v.exactOptional(v.string()),
	Count: v.exactOptional(v.pipe(v.number(), v.integer())),
	DeviceIDs: v.exactOptional(v.array(v.string())),
	/**
	 * A list of capabilities; an OR list of AND lists of capabilities.
	 */
	Capabilities: v.exactOptional(v.array(v.array(v.string()))),
	/**
	 * Driver-specific options, specified as a key/value pairs. These options
	 * are passed directly to the driver.
	 */
	Options: v.exactOptional(v.record(v.string(), v.unknown())),
});
export const throttleDeviceSchema = v.strictObject({
	/**
	 * Device path
	 */
	Path: v.exactOptional(v.string()),
	/**
	 * Rate
	 */
	Rate: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
});
export const mountSchema = v.strictObject({
	/**
	 * Container path.
	 */
	Target: v.exactOptional(v.string()),
	/**
	 * Mount source (e.g. a volume name, a host path).
	 */
	Source: v.exactOptional(v.string()),
	/**
	 * The mount type. Available types:
	 *
	 * - `bind` Mounts a file or directory from the host into the container. Must
	 * exist prior to creating the container.
	 * - `volume` Creates a volume with the given name and options (or uses a
	 * pre-existing volume with the same name and options). These are **not**
	 * removed when the container is removed.
	 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
	 * specified for tmpfs.
	 * - `npipe` Mounts a named pipe from the host into the container. Must exist
	 * prior to creating the container.
	 * - `cluster` a Swarm cluster volume
	 */
	Type: v.exactOptional(
		v.picklist(["bind", "volume", "tmpfs", "npipe", "cluster"]),
	),
	/**
	 * Whether the mount should be read-only.
	 */
	ReadOnly: v.exactOptional(v.boolean()),
	/**
	 * The consistency requirement for the mount: `default`, `consistent`,
	 * `cached`, or `delegated`.
	 */
	Consistency: v.exactOptional(v.string()),
	/**
	 * Optional configuration for the `bind` type.
	 */
	BindOptions: v.exactOptional(
		v.strictObject({
			/**
			 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
			 */
			Propagation: v.exactOptional(
				v.picklist([
					"private",
					"rprivate",
					"shared",
					"rshared",
					"slave",
					"rslave",
				]),
			),
			/**
			 * Disable recursive bind mount.
			 */
			NonRecursive: v.exactOptional(v.boolean()),
			/**
			 * Create mount point on host if missing
			 */
			CreateMountpoint: v.exactOptional(v.boolean()),
		}),
	),
	/**
	 * Optional configuration for the `volume` type.
	 */
	VolumeOptions: v.exactOptional(
		v.strictObject({
			/**
			 * Populate volume with data from the target.
			 */
			NoCopy: v.exactOptional(v.boolean()),
			/**
			 * User-defined key/value metadata.
			 */
			Labels: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * Map of driver specific options
			 */
			DriverConfig: v.exactOptional(
				v.strictObject({
					/**
					 * Name of the driver to use to create the volume.
					 */
					Name: v.exactOptional(v.string()),
					/**
					 * key/value map of driver specific options.
					 */
					Options: v.exactOptional(v.record(v.string(), v.unknown())),
				}),
			),
		}),
	),
	/**
	 * Optional configuration for the `tmpfs` type.
	 */
	TmpfsOptions: v.exactOptional(
		v.strictObject({
			/**
			 * The size for the tmpfs mount in bytes.
			 */
			SizeBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The permission mode for the tmpfs mount in an integer.
			 */
			Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
});
/**
 * The behavior to apply when the container exits. The default is not to
 * restart.
 *
 * An ever increasing delay (double the previous delay, starting at 100ms) is
 * added before each restart to prevent flooding the server.
 */
export const restartPolicySchema = v.strictObject({
	/**
	 * - Empty string means not to restart
	 * - `no` Do not automatically restart
	 * - `always` Always restart
	 * - `unless-stopped` Restart always except when the user has manually stopped
	 * the container
	 * - `on-failure` Restart only when the container exit code is non-zero
	 */
	Name: v.exactOptional(
		v.picklist(["", "no", "always", "unless-stopped", "on-failure"]),
	),
	/**
	 * If `on-failure` is used, the number of times to retry before giving up.
	 */
	MaximumRetryCount: v.exactOptional(v.pipe(v.number(), v.integer())),
});
/** A container's resources (cgroups config, ulimits, etc) */
export const resourcesSchema = v.strictObject({
	/**
	 * An integer value representing this container's relative CPU weight
	 * versus other containers.
	 */
	CpuShares: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Memory limit in bytes.
	 */
	Memory: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Path to `cgroups` under which the container's `cgroup` is created. If
	 * the path is not absolute, the path is considered to be relative to the
	 * `cgroups` path of the init process. Cgroups are created if they do not
	 * already exist.
	 */
	CgroupParent: v.exactOptional(v.string()),
	/**
	 * Block IO weight (relative weight).
	 */
	BlkioWeight: v.exactOptional(
		v.pipe(v.number(), v.integer(), v.minValue(0), v.maxValue(1000)),
	),
	/**
	 * Block IO weight (relative device weight) in the form:
	 *
	 * ```
	 * [{"Path": "device_path", "Weight": weight}]
	 * ```
	 */
	BlkioWeightDevice: v.exactOptional(
		v.array(
			v.strictObject({
				Path: v.exactOptional(v.string()),
				Weight: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
			}),
		),
	),
	/**
	 * Limit read rate (bytes per second) from a device, in the form:
	 *
	 * ```
	 * [{"Path": "device_path", "Rate": rate}]
	 * ```
	 */
	BlkioDeviceReadBps: v.exactOptional(
		v.array(
			v.strictObject({
				/**
				 * Device path
				 */
				Path: v.exactOptional(v.string()),
				/**
				 * Rate
				 */
				Rate: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
			}),
		),
	),
	/**
	 * Limit write rate (bytes per second) to a device, in the form:
	 *
	 * ```
	 * [{"Path": "device_path", "Rate": rate}]
	 * ```
	 */
	BlkioDeviceWriteBps: v.exactOptional(
		v.array(
			v.strictObject({
				/**
				 * Device path
				 */
				Path: v.exactOptional(v.string()),
				/**
				 * Rate
				 */
				Rate: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
			}),
		),
	),
	/**
	 * Limit read rate (IO per second) from a device, in the form:
	 *
	 * ```
	 * [{"Path": "device_path", "Rate": rate}]
	 * ```
	 */
	BlkioDeviceReadIOps: v.exactOptional(
		v.array(
			v.strictObject({
				/**
				 * Device path
				 */
				Path: v.exactOptional(v.string()),
				/**
				 * Rate
				 */
				Rate: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
			}),
		),
	),
	/**
	 * Limit write rate (IO per second) to a device, in the form:
	 *
	 * ```
	 * [{"Path": "device_path", "Rate": rate}]
	 * ```
	 */
	BlkioDeviceWriteIOps: v.exactOptional(
		v.array(
			v.strictObject({
				/**
				 * Device path
				 */
				Path: v.exactOptional(v.string()),
				/**
				 * Rate
				 */
				Rate: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
			}),
		),
	),
	/**
	 * The length of a CPU period in microseconds.
	 */
	CpuPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Microseconds of CPU time that the container can get in a CPU period.
	 */
	CpuQuota: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * The length of a CPU real-time period in microseconds. Set to 0 to
	 * allocate no time allocated to real-time tasks.
	 */
	CpuRealtimePeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * The length of a CPU real-time runtime in microseconds. Set to 0 to
	 * allocate no time allocated to real-time tasks.
	 */
	CpuRealtimeRuntime: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * CPUs in which to allow execution (e.g., `0-3`, `0,1`).
	 */
	CpusetCpus: v.exactOptional(v.string()),
	/**
	 * Memory nodes (MEMs) in which to allow execution (0-3, 0,1). Only
	 * effective on NUMA systems.
	 */
	CpusetMems: v.exactOptional(v.string()),
	/**
	 * A list of devices to add to the container.
	 */
	Devices: v.exactOptional(
		v.array(
			v.strictObject({
				PathOnHost: v.exactOptional(v.string()),
				PathInContainer: v.exactOptional(v.string()),
				CgroupPermissions: v.exactOptional(v.string()),
			}),
		),
	),
	/**
	 * a list of cgroup rules to apply to the container
	 */
	DeviceCgroupRules: v.exactOptional(v.array(v.string())),
	/**
	 * A list of requests for devices to be sent to device drivers.
	 */
	DeviceRequests: v.exactOptional(
		v.array(
			v.strictObject({
				Driver: v.exactOptional(v.string()),
				Count: v.exactOptional(v.pipe(v.number(), v.integer())),
				DeviceIDs: v.exactOptional(v.array(v.string())),
				/**
				 * A list of capabilities; an OR list of AND lists of capabilities.
				 */
				Capabilities: v.exactOptional(v.array(v.array(v.string()))),
				/**
				 * Driver-specific options, specified as a key/value pairs. These options
				 * are passed directly to the driver.
				 */
				Options: v.exactOptional(v.record(v.string(), v.unknown())),
			}),
		),
	),
	/**
	 * Hard limit for kernel TCP buffer memory (in bytes). Depending on the
	 * OCI runtime in use, this option may be ignored. It is no longer supported
	 * by the default (runc) runtime.
	 *
	 * This field is omitted when empty.
	 */
	KernelMemoryTCP: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Memory soft limit in bytes.
	 */
	MemoryReservation: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Total memory limit (memory + swap). Set as `-1` to enable unlimited
	 * swap.
	 */
	MemorySwap: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Tune a container's memory swappiness behavior. Accepts an integer
	 * between 0 and 100.
	 */
	MemorySwappiness: v.exactOptional(
		v.pipe(v.number(), v.integer(), v.minValue(0), v.maxValue(100)),
	),
	/**
	 * CPU quota in units of 10<sup>-9</sup> CPUs.
	 */
	NanoCpus: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Disable OOM Killer for the container.
	 */
	OomKillDisable: v.exactOptional(v.boolean()),
	/**
	 * Run an init inside the container that forwards signals and reaps
	 * processes. This field is omitted if empty, and the default (as
	 * configured on the daemon) is used.
	 */
	Init: v.exactOptional(v.nullable(v.boolean())),
	/**
	 * Tune a container's PIDs limit. Set `0` or `-1` for unlimited, or `null`
	 * to not change.
	 */
	PidsLimit: v.exactOptional(v.nullable(v.pipe(v.number(), v.integer()))),
	/**
	 * A list of resource limits to set in the container. For example:
	 *
	 * ```
	 * {"Name": "nofile", "Soft": 1024, "Hard": 2048}
	 * ```
	 */
	Ulimits: v.exactOptional(
		v.array(
			v.strictObject({
				/**
				 * Name of ulimit
				 */
				Name: v.exactOptional(v.string()),
				/**
				 * Soft limit
				 */
				Soft: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Hard limit
				 */
				Hard: v.exactOptional(v.pipe(v.number(), v.integer())),
			}),
		),
	),
	/**
	 * The number of usable CPUs (Windows only).
	 *
	 * On Windows Server containers, the processor resource controls are
	 * mutually exclusive. The order of precedence is `CPUCount` first, then
	 * `CPUShares`, and `CPUPercent` last.
	 */
	CpuCount: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * The usable percentage of the available CPUs (Windows only).
	 *
	 * On Windows Server containers, the processor resource controls are
	 * mutually exclusive. The order of precedence is `CPUCount` first, then
	 * `CPUShares`, and `CPUPercent` last.
	 */
	CpuPercent: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Maximum IOps for the container system drive (Windows only)
	 */
	IOMaximumIOps: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Maximum IO in bytes per second for the container system drive
	 * (Windows only).
	 */
	IOMaximumBandwidth: v.exactOptional(v.pipe(v.number(), v.integer())),
});
/** An object describing a limit on resources which can be requested by a task. */
export const limitSchema = v.strictObject({
	NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
	MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
	 */
	Pids: v.exactOptional(v.pipe(v.number(), v.integer())),
});
/**
 * An object describing the resources which can be advertised by a node and
 * requested by a task.
 */
export const resourceObjectSchema = v.strictObject({
	NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
	MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
	 * String resources (e.g, `GPU=UUID1`).
	 */
	GenericResources: v.exactOptional(
		v.array(
			v.strictObject({
				NamedResourceSpec: v.exactOptional(
					v.strictObject({
						Kind: v.exactOptional(v.string()),
						Value: v.exactOptional(v.string()),
					}),
				),
				DiscreteResourceSpec: v.exactOptional(
					v.strictObject({
						Kind: v.exactOptional(v.string()),
						Value: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
			}),
		),
	),
});
/**
 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
 * String resources (e.g, `GPU=UUID1`).
 * @example [
 *   {
 *     "DiscreteResourceSpec": {
 *       "Kind": "SSD",
 *       "Value": 3
 *     }
 *   },
 *   {
 *     "NamedResourceSpec": {
 *       "Kind": "GPU",
 *       "Value": "UUID1"
 *     }
 *   },
 *   {
 *     "NamedResourceSpec": {
 *       "Kind": "GPU",
 *       "Value": "UUID2"
 *     }
 *   }
 * ]
 */
export const genericResourcesSchema = v.array(
	v.strictObject({
		NamedResourceSpec: v.exactOptional(
			v.strictObject({
				Kind: v.exactOptional(v.string()),
				Value: v.exactOptional(v.string()),
			}),
		),
		DiscreteResourceSpec: v.exactOptional(
			v.strictObject({
				Kind: v.exactOptional(v.string()),
				Value: v.exactOptional(v.pipe(v.number(), v.integer())),
			}),
		),
	}),
);
/** A test to perform to check that the container is healthy. */
export const healthConfigSchema = v.strictObject({
	/**
	 * The test to perform. Possible values are:
	 *
	 * - `[]` inherit healthcheck from image or parent image
	 * - `["NONE"]` disable healthcheck
	 * - `["CMD", args...]` exec arguments directly
	 * - `["CMD-SHELL", command]` run command with system's default shell
	 */
	Test: v.exactOptional(v.array(v.string())),
	/**
	 * The time to wait between checks in nanoseconds. It should be 0 or at
	 * least 1000000 (1 ms). 0 means inherit.
	 */
	Interval: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * The time to wait before considering the check to have hung. It should
	 * be 0 or at least 1000000 (1 ms). 0 means inherit.
	 */
	Timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * The number of consecutive failures needed to consider a container as
	 * unhealthy. 0 means inherit.
	 */
	Retries: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Start period for the container to initialize before starting
	 * health-retries countdown in nanoseconds. It should be 0 or at least
	 * 1000000 (1 ms). 0 means inherit.
	 */
	StartPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
});
/** Health stores information about the container's healthcheck results. */
export const healthSchema = v.nullable(
	v.strictObject({
		/**
		 * Status is one of `none`, `starting`, `healthy` or `unhealthy`
		 *
		 * - "none"      Indicates there is no healthcheck
		 * - "starting"  Starting indicates that the container is not yet ready
		 * - "healthy"   Healthy indicates that the container is running correctly
		 * - "unhealthy" Unhealthy indicates that the container has a problem
		 */
		Status: v.exactOptional(
			v.picklist(["none", "starting", "healthy", "unhealthy"]),
		),
		/**
		 * FailingStreak is the number of consecutive failures
		 */
		FailingStreak: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Log contains the last few results (oldest first)
		 */
		Log: v.exactOptional(
			v.array(
				v.nullable(
					v.strictObject({
						/**
						 * Date and time at which this check started in
						 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
						 */
						Start: v.exactOptional(v.string()),
						/**
						 * Date and time at which this check ended in
						 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
						 */
						End: v.exactOptional(v.string()),
						/**
						 * ExitCode meanings:
						 *
						 * - `0` healthy
						 * - `1` unhealthy
						 * - `2` reserved (considered unhealthy)
						 * - other values: error running probe
						 */
						ExitCode: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * Output from last check
						 */
						Output: v.exactOptional(v.string()),
					}),
				),
			),
		),
	}),
);
/**
 * HealthcheckResult stores information about a single run of a healthcheck
 * probe
 */
export const healthcheckResultSchema = v.nullable(
	v.strictObject({
		/**
		 * Date and time at which this check started in
		 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
		 */
		Start: v.exactOptional(v.string()),
		/**
		 * Date and time at which this check ended in
		 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
		 */
		End: v.exactOptional(v.string()),
		/**
		 * ExitCode meanings:
		 *
		 * - `0` healthy
		 * - `1` unhealthy
		 * - `2` reserved (considered unhealthy)
		 * - other values: error running probe
		 */
		ExitCode: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Output from last check
		 */
		Output: v.exactOptional(v.string()),
	}),
);
/** Container configuration that depends on the host we are running on */
export const hostConfigSchema = v.intersect([
	v.strictObject({
		/**
		 * An integer value representing this container's relative CPU weight
		 * versus other containers.
		 */
		CpuShares: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Memory limit in bytes.
		 */
		Memory: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Path to `cgroups` under which the container's `cgroup` is created. If
		 * the path is not absolute, the path is considered to be relative to the
		 * `cgroups` path of the init process. Cgroups are created if they do not
		 * already exist.
		 */
		CgroupParent: v.exactOptional(v.string()),
		/**
		 * Block IO weight (relative weight).
		 */
		BlkioWeight: v.exactOptional(
			v.pipe(v.number(), v.integer(), v.minValue(0), v.maxValue(1000)),
		),
		/**
		 * Block IO weight (relative device weight) in the form:
		 *
		 * ```
		 * [{"Path": "device_path", "Weight": weight}]
		 * ```
		 */
		BlkioWeightDevice: v.exactOptional(
			v.array(
				v.strictObject({
					Path: v.exactOptional(v.string()),
					Weight: v.exactOptional(
						v.pipe(v.number(), v.integer(), v.minValue(0)),
					),
				}),
			),
		),
		/**
		 * Limit read rate (bytes per second) from a device, in the form:
		 *
		 * ```
		 * [{"Path": "device_path", "Rate": rate}]
		 * ```
		 */
		BlkioDeviceReadBps: v.exactOptional(
			v.array(
				v.strictObject({
					/**
					 * Device path
					 */
					Path: v.exactOptional(v.string()),
					/**
					 * Rate
					 */
					Rate: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
				}),
			),
		),
		/**
		 * Limit write rate (bytes per second) to a device, in the form:
		 *
		 * ```
		 * [{"Path": "device_path", "Rate": rate}]
		 * ```
		 */
		BlkioDeviceWriteBps: v.exactOptional(
			v.array(
				v.strictObject({
					/**
					 * Device path
					 */
					Path: v.exactOptional(v.string()),
					/**
					 * Rate
					 */
					Rate: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
				}),
			),
		),
		/**
		 * Limit read rate (IO per second) from a device, in the form:
		 *
		 * ```
		 * [{"Path": "device_path", "Rate": rate}]
		 * ```
		 */
		BlkioDeviceReadIOps: v.exactOptional(
			v.array(
				v.strictObject({
					/**
					 * Device path
					 */
					Path: v.exactOptional(v.string()),
					/**
					 * Rate
					 */
					Rate: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
				}),
			),
		),
		/**
		 * Limit write rate (IO per second) to a device, in the form:
		 *
		 * ```
		 * [{"Path": "device_path", "Rate": rate}]
		 * ```
		 */
		BlkioDeviceWriteIOps: v.exactOptional(
			v.array(
				v.strictObject({
					/**
					 * Device path
					 */
					Path: v.exactOptional(v.string()),
					/**
					 * Rate
					 */
					Rate: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
				}),
			),
		),
		/**
		 * The length of a CPU period in microseconds.
		 */
		CpuPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Microseconds of CPU time that the container can get in a CPU period.
		 */
		CpuQuota: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * The length of a CPU real-time period in microseconds. Set to 0 to
		 * allocate no time allocated to real-time tasks.
		 */
		CpuRealtimePeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * The length of a CPU real-time runtime in microseconds. Set to 0 to
		 * allocate no time allocated to real-time tasks.
		 */
		CpuRealtimeRuntime: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * CPUs in which to allow execution (e.g., `0-3`, `0,1`).
		 */
		CpusetCpus: v.exactOptional(v.string()),
		/**
		 * Memory nodes (MEMs) in which to allow execution (0-3, 0,1). Only
		 * effective on NUMA systems.
		 */
		CpusetMems: v.exactOptional(v.string()),
		/**
		 * A list of devices to add to the container.
		 */
		Devices: v.exactOptional(
			v.array(
				v.strictObject({
					PathOnHost: v.exactOptional(v.string()),
					PathInContainer: v.exactOptional(v.string()),
					CgroupPermissions: v.exactOptional(v.string()),
				}),
			),
		),
		/**
		 * a list of cgroup rules to apply to the container
		 */
		DeviceCgroupRules: v.exactOptional(v.array(v.string())),
		/**
		 * A list of requests for devices to be sent to device drivers.
		 */
		DeviceRequests: v.exactOptional(
			v.array(
				v.strictObject({
					Driver: v.exactOptional(v.string()),
					Count: v.exactOptional(v.pipe(v.number(), v.integer())),
					DeviceIDs: v.exactOptional(v.array(v.string())),
					/**
					 * A list of capabilities; an OR list of AND lists of capabilities.
					 */
					Capabilities: v.exactOptional(v.array(v.array(v.string()))),
					/**
					 * Driver-specific options, specified as a key/value pairs. These options
					 * are passed directly to the driver.
					 */
					Options: v.exactOptional(v.record(v.string(), v.unknown())),
				}),
			),
		),
		/**
		 * Hard limit for kernel TCP buffer memory (in bytes). Depending on the
		 * OCI runtime in use, this option may be ignored. It is no longer supported
		 * by the default (runc) runtime.
		 *
		 * This field is omitted when empty.
		 */
		KernelMemoryTCP: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Memory soft limit in bytes.
		 */
		MemoryReservation: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Total memory limit (memory + swap). Set as `-1` to enable unlimited
		 * swap.
		 */
		MemorySwap: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Tune a container's memory swappiness behavior. Accepts an integer
		 * between 0 and 100.
		 */
		MemorySwappiness: v.exactOptional(
			v.pipe(v.number(), v.integer(), v.minValue(0), v.maxValue(100)),
		),
		/**
		 * CPU quota in units of 10<sup>-9</sup> CPUs.
		 */
		NanoCpus: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Disable OOM Killer for the container.
		 */
		OomKillDisable: v.exactOptional(v.boolean()),
		/**
		 * Run an init inside the container that forwards signals and reaps
		 * processes. This field is omitted if empty, and the default (as
		 * configured on the daemon) is used.
		 */
		Init: v.exactOptional(v.nullable(v.boolean())),
		/**
		 * Tune a container's PIDs limit. Set `0` or `-1` for unlimited, or `null`
		 * to not change.
		 */
		PidsLimit: v.exactOptional(v.nullable(v.pipe(v.number(), v.integer()))),
		/**
		 * A list of resource limits to set in the container. For example:
		 *
		 * ```
		 * {"Name": "nofile", "Soft": 1024, "Hard": 2048}
		 * ```
		 */
		Ulimits: v.exactOptional(
			v.array(
				v.strictObject({
					/**
					 * Name of ulimit
					 */
					Name: v.exactOptional(v.string()),
					/**
					 * Soft limit
					 */
					Soft: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Hard limit
					 */
					Hard: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
		),
		/**
		 * The number of usable CPUs (Windows only).
		 *
		 * On Windows Server containers, the processor resource controls are
		 * mutually exclusive. The order of precedence is `CPUCount` first, then
		 * `CPUShares`, and `CPUPercent` last.
		 */
		CpuCount: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * The usable percentage of the available CPUs (Windows only).
		 *
		 * On Windows Server containers, the processor resource controls are
		 * mutually exclusive. The order of precedence is `CPUCount` first, then
		 * `CPUShares`, and `CPUPercent` last.
		 */
		CpuPercent: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Maximum IOps for the container system drive (Windows only)
		 */
		IOMaximumIOps: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Maximum IO in bytes per second for the container system drive
		 * (Windows only).
		 */
		IOMaximumBandwidth: v.exactOptional(v.pipe(v.number(), v.integer())),
	}),
	v.strictObject({
		/**
		 * A list of volume bindings for this container. Each volume binding
		 * is a string in one of these forms:
		 *
		 * - `host-src:container-dest[:options]` to bind-mount a host path
		 *
		 * into the container. Both `host-src`, and `container-dest` must
		 *
		 * be an _absolute_ path.
		 * - `volume-name:container-dest[:options]` to bind-mount a volume
		 *
		 * managed by a volume driver into the container. `container-dest`
		 *
		 * must be an _absolute_ path.
		 *
		 * `options` is an optional, comma-delimited list of:
		 *
		 * - `nocopy` disables automatic copying of data from the container
		 *
		 * path to the volume. The `nocopy` flag only applies to named volumes.
		 * - `[ro|rw]` mounts a volume read-only or read-write, respectively.
		 *
		 * If omitted or set to `rw`, volumes are mounted read-write.
		 * - `[z|Z]` applies SELinux labels to allow or deny multiple containers
		 *
		 * to read and write to the same volume.
		 *
		 * - `z`: a _shared_ content label is applied to the content. This
		 *
		 * label indicates that multiple containers can share the volume
		 *
		 * content, for both reading and writing.
		 *
		 * - `Z`: a _private unshared_ label is applied to the content.
		 *
		 * This label indicates that only the current container can use
		 *
		 * a private volume. Labeling systems such as SELinux require
		 *
		 * proper labels to be placed on volume content that is mounted
		 *
		 * into a container. Without a label, the security system can
		 *
		 * prevent a container's processes from using the content. By
		 *
		 * default, the labels set by the host operating system are not
		 *
		 * modified.
		 * - `[[r]shared|[r]slave|[r]private]` specifies mount
		 *
		 * [propagation
		 * behavior](https://www.kernel.org/doc/Documentation/filesystems/sharedsubtree.txt).
		 *
		 * This only applies to bind-mounted volumes, not internal volumes
		 *
		 * or named volumes. Mount propagation requires the source mount
		 *
		 * point (the location where the source directory is mounted in the
		 *
		 * host operating system) to have the correct propagation properties.
		 *
		 * For shared volumes, the source mount point must be set to `shared`.
		 *
		 * For slave volumes, the mount must be set to either `shared` or
		 *
		 * `slave`.
		 */
		Binds: v.exactOptional(v.array(v.string())),
		/**
		 * Path to a file where the container ID is written
		 */
		ContainerIDFile: v.exactOptional(v.string()),
		/**
		 * The logging configuration for this container
		 */
		LogConfig: v.exactOptional(
			v.strictObject({
				Type: v.exactOptional(
					v.picklist([
						"json-file",
						"syslog",
						"journald",
						"gelf",
						"fluentd",
						"awslogs",
						"splunk",
						"etwlogs",
						"none",
					]),
				),
				Config: v.exactOptional(v.record(v.string(), v.unknown())),
			}),
		),
		/**
		 * Network mode to use for this container. Supported standard values
		 * are: `bridge`, `host`, `none`, and `container:<name|id>`. Any
		 * other value is taken as a custom network's name to which this
		 * container should connect to.
		 */
		NetworkMode: v.exactOptional(v.string()),
		/**
		 * PortMap describes the mapping of container ports to host ports, using the
		 * container's port-number and protocol as key in the format
		 * `<port>/<protocol>`,
		 * for example, `80/udp`.
		 *
		 * If a container's port is mapped for multiple protocols, separate entries
		 * are added to the mapping table.
		 */
		PortBindings: v.exactOptional(v.record(v.string(), v.unknown())),
		/**
		 * The behavior to apply when the container exits. The default is not to
		 * restart.
		 *
		 * An ever increasing delay (double the previous delay, starting at 100ms) is
		 * added before each restart to prevent flooding the server.
		 */
		RestartPolicy: v.exactOptional(
			v.strictObject({
				/**
				 * - Empty string means not to restart
				 * - `no` Do not automatically restart
				 * - `always` Always restart
				 * - `unless-stopped` Restart always except when the user has manually stopped
				 * the container
				 * - `on-failure` Restart only when the container exit code is non-zero
				 */
				Name: v.exactOptional(
					v.picklist(["", "no", "always", "unless-stopped", "on-failure"]),
				),
				/**
				 * If `on-failure` is used, the number of times to retry before giving up.
				 */
				MaximumRetryCount: v.exactOptional(v.pipe(v.number(), v.integer())),
			}),
		),
		/**
		 * Automatically remove the container when the container's process
		 * exits. This has no effect if `RestartPolicy` is set.
		 */
		AutoRemove: v.exactOptional(v.boolean()),
		/**
		 * Driver that this container uses to mount volumes.
		 */
		VolumeDriver: v.exactOptional(v.string()),
		/**
		 * A list of volumes to inherit from another container, specified in
		 * the form `<container name>[:<ro|rw>]`.
		 */
		VolumesFrom: v.exactOptional(v.array(v.string())),
		/**
		 * Specification for mounts to be added to the container.
		 */
		Mounts: v.exactOptional(
			v.array(
				v.strictObject({
					/**
					 * Container path.
					 */
					Target: v.exactOptional(v.string()),
					/**
					 * Mount source (e.g. a volume name, a host path).
					 */
					Source: v.exactOptional(v.string()),
					/**
					 * The mount type. Available types:
					 *
					 * - `bind` Mounts a file or directory from the host into the container. Must
					 * exist prior to creating the container.
					 * - `volume` Creates a volume with the given name and options (or uses a
					 * pre-existing volume with the same name and options). These are **not**
					 * removed when the container is removed.
					 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
					 * specified for tmpfs.
					 * - `npipe` Mounts a named pipe from the host into the container. Must exist
					 * prior to creating the container.
					 * - `cluster` a Swarm cluster volume
					 */
					Type: v.exactOptional(
						v.picklist(["bind", "volume", "tmpfs", "npipe", "cluster"]),
					),
					/**
					 * Whether the mount should be read-only.
					 */
					ReadOnly: v.exactOptional(v.boolean()),
					/**
					 * The consistency requirement for the mount: `default`, `consistent`,
					 * `cached`, or `delegated`.
					 */
					Consistency: v.exactOptional(v.string()),
					/**
					 * Optional configuration for the `bind` type.
					 */
					BindOptions: v.exactOptional(
						v.strictObject({
							/**
							 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
							 */
							Propagation: v.exactOptional(
								v.picklist([
									"private",
									"rprivate",
									"shared",
									"rshared",
									"slave",
									"rslave",
								]),
							),
							/**
							 * Disable recursive bind mount.
							 */
							NonRecursive: v.exactOptional(v.boolean()),
							/**
							 * Create mount point on host if missing
							 */
							CreateMountpoint: v.exactOptional(v.boolean()),
						}),
					),
					/**
					 * Optional configuration for the `volume` type.
					 */
					VolumeOptions: v.exactOptional(
						v.strictObject({
							/**
							 * Populate volume with data from the target.
							 */
							NoCopy: v.exactOptional(v.boolean()),
							/**
							 * User-defined key/value metadata.
							 */
							Labels: v.exactOptional(v.record(v.string(), v.unknown())),
							/**
							 * Map of driver specific options
							 */
							DriverConfig: v.exactOptional(
								v.strictObject({
									/**
									 * Name of the driver to use to create the volume.
									 */
									Name: v.exactOptional(v.string()),
									/**
									 * key/value map of driver specific options.
									 */
									Options: v.exactOptional(v.record(v.string(), v.unknown())),
								}),
							),
						}),
					),
					/**
					 * Optional configuration for the `tmpfs` type.
					 */
					TmpfsOptions: v.exactOptional(
						v.strictObject({
							/**
							 * The size for the tmpfs mount in bytes.
							 */
							SizeBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The permission mode for the tmpfs mount in an integer.
							 */
							Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
				}),
			),
		),
		/**
		 * Initial console size, as an `[height, width]` array.
		 */
		ConsoleSize: v.exactOptional(
			v.nullable(
				v.pipe(
					v.array(v.pipe(v.number(), v.integer(), v.minValue(0))),
					v.minLength(2),
					v.maxLength(2),
				),
			),
		),
		/**
		 * Arbitrary non-identifying metadata attached to container and
		 * provided to the runtime when the container is started.
		 */
		Annotations: v.exactOptional(v.record(v.string(), v.unknown())),
		/**
		 * A list of kernel capabilities to add to the container. Conflicts
		 * with option 'Capabilities'.
		 */
		CapAdd: v.exactOptional(v.array(v.string())),
		/**
		 * A list of kernel capabilities to drop from the container. Conflicts
		 * with option 'Capabilities'.
		 */
		CapDrop: v.exactOptional(v.array(v.string())),
		/**
		 * cgroup namespace mode for the container. Possible values are:
		 *
		 * - `"private"`: the container runs in its own private cgroup namespace
		 * - `"host"`: use the host system's cgroup namespace
		 *
		 * If not specified, the daemon default is used, which can either be
		 * `"private"`
		 * or `"host"`, depending on daemon version, kernel support and configuration.
		 */
		CgroupnsMode: v.exactOptional(v.picklist(["private", "host"])),
		/**
		 * A list of DNS servers for the container to use.
		 */
		Dns: v.exactOptional(v.array(v.string())),
		/**
		 * A list of DNS options.
		 */
		DnsOptions: v.exactOptional(v.array(v.string())),
		/**
		 * A list of DNS search domains.
		 */
		DnsSearch: v.exactOptional(v.array(v.string())),
		/**
		 * A list of hostnames/IP mappings to add to the container's `/etc/hosts`
		 * file. Specified in the form `["hostname:IP"]`.
		 */
		ExtraHosts: v.exactOptional(v.array(v.string())),
		/**
		 * A list of additional groups that the container process will run as.
		 */
		GroupAdd: v.exactOptional(v.array(v.string())),
		/**
		 * IPC sharing mode for the container. Possible values are:
		 *
		 * - `"none"`: own private IPC namespace, with /dev/shm not mounted
		 * - `"private"`: own private IPC namespace
		 * - `"shareable"`: own private IPC namespace, with a possibility to share it
		 * with other containers
		 * - `"container:<name|id>"`: join another (shareable) container's IPC
		 * namespace
		 * - `"host"`: use the host system's IPC namespace
		 *
		 * If not specified, daemon default is used, which can either be `"private"`
		 * or `"shareable"`, depending on daemon version and configuration.
		 */
		IpcMode: v.exactOptional(v.string()),
		/**
		 * Cgroup to use for the container.
		 */
		Cgroup: v.exactOptional(v.string()),
		/**
		 * A list of links for the container in the form `container_name:alias`.
		 */
		Links: v.exactOptional(v.array(v.string())),
		/**
		 * An integer value containing the score given to the container in
		 * order to tune OOM killer preferences.
		 */
		OomScoreAdj: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Set the PID (Process) Namespace mode for the container. It can be
		 * either:
		 *
		 * - `"container:<name|id>"`: joins another container's PID namespace
		 * - `"host"`: use the host's PID namespace inside the container
		 */
		PidMode: v.exactOptional(v.string()),
		/**
		 * Gives the container full access to the host.
		 */
		Privileged: v.exactOptional(v.boolean()),
		/**
		 * Allocates an ephemeral host port for all of a container's
		 * exposed ports.
		 *
		 * Ports are de-allocated when the container stops and allocated when
		 * the container starts. The allocated port might be changed when
		 * restarting the container.
		 *
		 * The port is selected from the ephemeral port range that depends on
		 * the kernel. For example, on Linux the range is defined by
		 * `/proc/sys/net/ipv4/ip_local_port_range`.
		 */
		PublishAllPorts: v.exactOptional(v.boolean()),
		/**
		 * Mount the container's root filesystem as read only.
		 */
		ReadonlyRootfs: v.exactOptional(v.boolean()),
		/**
		 * A list of string values to customize labels for MLS systems, such
		 * as SELinux.
		 */
		SecurityOpt: v.exactOptional(v.array(v.string())),
		/**
		 * Storage driver options for this container, in the form `{"size": "120G"}`.
		 */
		StorageOpt: v.exactOptional(v.record(v.string(), v.unknown())),
		/**
		 * A map of container directories which should be replaced by tmpfs
		 * mounts, and their corresponding mount options. For example:
		 *
		 * ```
		 * { "/run": "rw,noexec,nosuid,size=65536k" }
		 * ```
		 */
		Tmpfs: v.exactOptional(v.record(v.string(), v.unknown())),
		/**
		 * UTS namespace to use for the container.
		 */
		UTSMode: v.exactOptional(v.string()),
		/**
		 * Sets the usernamespace mode for the container when usernamespace
		 * remapping option is enabled.
		 */
		UsernsMode: v.exactOptional(v.string()),
		/**
		 * Size of `/dev/shm` in bytes. If omitted, the system uses 64MB.
		 */
		ShmSize: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
		/**
		 * A list of kernel parameters (sysctls) to set in the container.
		 * For example:
		 *
		 * ```
		 * {"net.ipv4.ip_forward": "1"}
		 * ```
		 */
		Sysctls: v.exactOptional(v.record(v.string(), v.unknown())),
		/**
		 * Runtime to use with this container.
		 */
		Runtime: v.exactOptional(v.string()),
		/**
		 * Isolation technology of the container. (Windows only)
		 */
		Isolation: v.exactOptional(
			v.picklist(["default", "process", "hyperv", ""]),
		),
		/**
		 * The list of paths to be masked inside the container (this overrides
		 * the default set of paths).
		 */
		MaskedPaths: v.exactOptional(v.array(v.string())),
		/**
		 * The list of paths to be set as read-only inside the container
		 * (this overrides the default set of paths).
		 */
		ReadonlyPaths: v.exactOptional(v.array(v.string())),
	}),
]);
/**
 * Configuration for a container that is portable between hosts.
 *
 * When used as `ContainerConfig` field in an image, `ContainerConfig` is an
 * optional field containing the configuration of the container that was last
 * committed when creating the image.
 *
 * Previous versions of Docker builder used this field to store build cache,
 * and it is not in active use anymore.
 */
export const containerConfigSchema = v.strictObject({
	/**
	 * The hostname to use for the container, as a valid RFC 1123 hostname.
	 */
	Hostname: v.exactOptional(v.string()),
	/**
	 * The domain name to use for the container.
	 */
	Domainname: v.exactOptional(v.string()),
	/**
	 * The user that commands are run as inside the container.
	 */
	User: v.exactOptional(v.string()),
	/**
	 * Whether to attach to `stdin`.
	 */
	AttachStdin: v.exactOptional(v.boolean()),
	/**
	 * Whether to attach to `stdout`.
	 */
	AttachStdout: v.exactOptional(v.boolean()),
	/**
	 * Whether to attach to `stderr`.
	 */
	AttachStderr: v.exactOptional(v.boolean()),
	/**
	 * An object mapping ports to an empty object in the form:
	 *
	 * `{"<port>/<tcp|udp|sctp>": {}}`
	 */
	ExposedPorts: v.exactOptional(v.nullable(v.record(v.string(), v.unknown()))),
	/**
	 * Attach standard streams to a TTY, including `stdin` if it is not closed.
	 */
	Tty: v.exactOptional(v.boolean()),
	/**
	 * Open `stdin`
	 */
	OpenStdin: v.exactOptional(v.boolean()),
	/**
	 * Close `stdin` after one attached client disconnects
	 */
	StdinOnce: v.exactOptional(v.boolean()),
	/**
	 * A list of environment variables to set inside the container in the
	 * form `["VAR=value", ...]`. A variable without `=` is removed from the
	 * environment, rather than to have an empty value.
	 */
	Env: v.exactOptional(v.array(v.string())),
	/**
	 * Command to run specified as a string or an array of strings.
	 */
	Cmd: v.exactOptional(v.array(v.string())),
	/**
	 * A test to perform to check that the container is healthy.
	 */
	Healthcheck: v.exactOptional(
		v.strictObject({
			/**
			 * The test to perform. Possible values are:
			 *
			 * - `[]` inherit healthcheck from image or parent image
			 * - `["NONE"]` disable healthcheck
			 * - `["CMD", args...]` exec arguments directly
			 * - `["CMD-SHELL", command]` run command with system's default shell
			 */
			Test: v.exactOptional(v.array(v.string())),
			/**
			 * The time to wait between checks in nanoseconds. It should be 0 or at
			 * least 1000000 (1 ms). 0 means inherit.
			 */
			Interval: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The time to wait before considering the check to have hung. It should
			 * be 0 or at least 1000000 (1 ms). 0 means inherit.
			 */
			Timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The number of consecutive failures needed to consider a container as
			 * unhealthy. 0 means inherit.
			 */
			Retries: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * Start period for the container to initialize before starting
			 * health-retries countdown in nanoseconds. It should be 0 or at least
			 * 1000000 (1 ms). 0 means inherit.
			 */
			StartPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	/**
	 * Command is already escaped (Windows only)
	 */
	ArgsEscaped: v.exactOptional(v.nullable(v.boolean())),
	/**
	 * The name (or reference) of the image to use when creating the container,
	 * or which was used when the container was created.
	 */
	Image: v.exactOptional(v.string()),
	/**
	 * An object mapping mount point paths inside the container to empty
	 * objects.
	 */
	Volumes: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * The working directory for commands to run in.
	 */
	WorkingDir: v.exactOptional(v.string()),
	/**
	 * The entry point for the container as a string or an array of strings.
	 *
	 * If the array consists of exactly one empty string (`[""]`) then the
	 * entry point is reset to system default (i.e., the entry point used by
	 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
	 */
	Entrypoint: v.exactOptional(v.array(v.string())),
	/**
	 * Disable networking for the container.
	 */
	NetworkDisabled: v.exactOptional(v.nullable(v.boolean())),
	/**
	 * MAC address of the container.
	 */
	MacAddress: v.exactOptional(v.nullable(v.string())),
	/**
	 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
	 */
	OnBuild: v.exactOptional(v.nullable(v.array(v.string()))),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Signal to stop a container as a string or unsigned integer.
	 */
	StopSignal: v.exactOptional(v.nullable(v.string())),
	/**
	 * Timeout to stop a container in seconds.
	 */
	StopTimeout: v.exactOptional(v.nullable(v.pipe(v.number(), v.integer()))),
	/**
	 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
	 */
	Shell: v.exactOptional(v.nullable(v.array(v.string()))),
});
/**
 * Configuration of the image. These fields are used as defaults
 * when starting a container from the image.
 * @example {
 *   "Hostname": "",
 *   "Domainname": "",
 *   "User": "web:web",
 *   "AttachStdin": false,
 *   "AttachStdout": false,
 *   "AttachStderr": false,
 *   "ExposedPorts": {
 *     "80/tcp": {},
 *     "443/tcp": {}
 *   },
 *   "Tty": false,
 *   "OpenStdin": false,
 *   "StdinOnce": false,
 *   "Env": [
 *     "PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
 *   ],
 *   "Cmd": [
 *     "/bin/sh"
 *   ],
 *   "Healthcheck": {
 *     "Test": [
 *       "string"
 *     ],
 *     "Interval": 0,
 *     "Timeout": 0,
 *     "Retries": 0,
 *     "StartPeriod": 0,
 *     "StartInterval": 0
 *   },
 *   "ArgsEscaped": true,
 *   "Image": "",
 *   "Volumes": {
 *     "/app/data": {},
 *     "/app/config": {}
 *   },
 *   "WorkingDir": "/public/",
 *   "Entrypoint": [],
 *   "OnBuild": [],
 *   "Labels": {
 *     "com.example.some-label": "some-value",
 *     "com.example.some-other-label": "some-other-value"
 *   },
 *   "StopSignal": "SIGTERM",
 *   "Shell": [
 *     "/bin/sh",
 *     "-c"
 *   ]
 * }
 */
export const imageConfigSchema = v.strictObject({
	/**
	 * The hostname to use for the container, as a valid RFC 1123 hostname.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always empty and must not be used.
	 */
	Hostname: v.exactOptional(v.string()),
	/**
	 * The domain name to use for the container.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always empty and must not be used.
	 */
	Domainname: v.exactOptional(v.string()),
	/**
	 * The user that commands are run as inside the container.
	 */
	User: v.exactOptional(v.string()),
	/**
	 * Whether to attach to `stdin`.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always false and must not be used.
	 */
	AttachStdin: v.exactOptional(v.boolean()),
	/**
	 * Whether to attach to `stdout`.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always false and must not be used.
	 */
	AttachStdout: v.exactOptional(v.boolean()),
	/**
	 * Whether to attach to `stderr`.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always false and must not be used.
	 */
	AttachStderr: v.exactOptional(v.boolean()),
	/**
	 * An object mapping ports to an empty object in the form:
	 *
	 * `{"<port>/<tcp|udp|sctp>": {}}`
	 */
	ExposedPorts: v.exactOptional(v.nullable(v.record(v.string(), v.unknown()))),
	/**
	 * Attach standard streams to a TTY, including `stdin` if it is not closed.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always false and must not be used.
	 */
	Tty: v.exactOptional(v.boolean()),
	/**
	 * Open `stdin`
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always false and must not be used.
	 */
	OpenStdin: v.exactOptional(v.boolean()),
	/**
	 * Close `stdin` after one attached client disconnects.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always false and must not be used.
	 */
	StdinOnce: v.exactOptional(v.boolean()),
	/**
	 * A list of environment variables to set inside the container in the
	 * form `["VAR=value", ...]`. A variable without `=` is removed from the
	 * environment, rather than to have an empty value.
	 */
	Env: v.exactOptional(v.array(v.string())),
	/**
	 * Command to run specified as a string or an array of strings.
	 */
	Cmd: v.exactOptional(v.array(v.string())),
	/**
	 * A test to perform to check that the container is healthy.
	 */
	Healthcheck: v.exactOptional(
		v.strictObject({
			/**
			 * The test to perform. Possible values are:
			 *
			 * - `[]` inherit healthcheck from image or parent image
			 * - `["NONE"]` disable healthcheck
			 * - `["CMD", args...]` exec arguments directly
			 * - `["CMD-SHELL", command]` run command with system's default shell
			 */
			Test: v.exactOptional(v.array(v.string())),
			/**
			 * The time to wait between checks in nanoseconds. It should be 0 or at
			 * least 1000000 (1 ms). 0 means inherit.
			 */
			Interval: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The time to wait before considering the check to have hung. It should
			 * be 0 or at least 1000000 (1 ms). 0 means inherit.
			 */
			Timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The number of consecutive failures needed to consider a container as
			 * unhealthy. 0 means inherit.
			 */
			Retries: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * Start period for the container to initialize before starting
			 * health-retries countdown in nanoseconds. It should be 0 or at least
			 * 1000000 (1 ms). 0 means inherit.
			 */
			StartPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	/**
	 * Command is already escaped (Windows only)
	 */
	ArgsEscaped: v.exactOptional(v.nullable(v.boolean())),
	/**
	 * The name (or reference) of the image to use when creating the container,
	 * or which was used when the container was created.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always empty and must not be used.
	 */
	Image: v.exactOptional(v.string()),
	/**
	 * An object mapping mount point paths inside the container to empty
	 * objects.
	 */
	Volumes: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * The working directory for commands to run in.
	 */
	WorkingDir: v.exactOptional(v.string()),
	/**
	 * The entry point for the container as a string or an array of strings.
	 *
	 * If the array consists of exactly one empty string (`[""]`) then the
	 * entry point is reset to system default (i.e., the entry point used by
	 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
	 */
	Entrypoint: v.exactOptional(v.array(v.string())),
	/**
	 * Disable networking for the container.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always omitted and must not be used.
	 */
	NetworkDisabled: v.exactOptional(v.nullable(v.boolean())),
	/**
	 * MAC address of the container.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always omitted and must not be used.
	 */
	MacAddress: v.exactOptional(v.nullable(v.string())),
	/**
	 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
	 */
	OnBuild: v.exactOptional(v.nullable(v.array(v.string()))),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Signal to stop a container as a string or unsigned integer.
	 */
	StopSignal: v.exactOptional(v.nullable(v.string())),
	/**
	 * Timeout to stop a container in seconds.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: this field is always omitted and must not be used.
	 */
	StopTimeout: v.exactOptional(v.nullable(v.pipe(v.number(), v.integer()))),
	/**
	 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
	 */
	Shell: v.exactOptional(v.nullable(v.array(v.string()))),
});
/**
 * NetworkingConfig represents the container's networking configuration for
 * each of its interfaces.
 * It is used for the networking configs specified in the `docker create`
 * and `docker network connect` commands.
 * @example {
 *   "EndpointsConfig": {
 *     "isolated_nw": {
 *       "IPAMConfig": {
 *         "IPv4Address": "172.20.30.33",
 *         "IPv6Address": "2001:db8:abcd::3033",
 *         "LinkLocalIPs": [
 *           "169.254.34.68",
 *           "fe80::3468"
 *         ]
 *       },
 *       "Links": [
 *         "container_1",
 *         "container_2"
 *       ],
 *       "Aliases": [
 *         "server_x",
 *         "server_y"
 *       ]
 *     }
 *   }
 * }
 */
export const networkingConfigSchema = v.strictObject({
	/**
	 * A mapping of network name to endpoint configuration for that network.
	 */
	EndpointsConfig: v.exactOptional(v.record(v.string(), v.unknown())),
});
/** NetworkSettings exposes the network settings in the API */
export const networkSettingsSchema = v.strictObject({
	/**
	 * Name of the network's bridge (for example, `docker0`).
	 */
	Bridge: v.exactOptional(v.string()),
	/**
	 * SandboxID uniquely represents a container's network stack.
	 */
	SandboxID: v.exactOptional(v.string()),
	/**
	 * Indicates if hairpin NAT should be enabled on the virtual interface.
	 */
	HairpinMode: v.exactOptional(v.boolean()),
	/**
	 * IPv6 unicast address using the link-local prefix.
	 */
	LinkLocalIPv6Address: v.exactOptional(v.string()),
	/**
	 * Prefix length of the IPv6 unicast address.
	 */
	LinkLocalIPv6PrefixLen: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * PortMap describes the mapping of container ports to host ports, using the
	 * container's port-number and protocol as key in the format
	 * `<port>/<protocol>`,
	 * for example, `80/udp`.
	 *
	 * If a container's port is mapped for multiple protocols, separate entries
	 * are added to the mapping table.
	 */
	Ports: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * SandboxKey identifies the sandbox
	 */
	SandboxKey: v.exactOptional(v.string()),
	SecondaryIPAddresses: v.exactOptional(
		v.nullable(
			v.array(
				v.strictObject({
					/**
					 * IP address.
					 */
					Addr: v.exactOptional(v.string()),
					/**
					 * Mask length of the IP address.
					 */
					PrefixLen: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
		),
	),
	SecondaryIPv6Addresses: v.exactOptional(
		v.nullable(
			v.array(
				v.strictObject({
					/**
					 * IP address.
					 */
					Addr: v.exactOptional(v.string()),
					/**
					 * Mask length of the IP address.
					 */
					PrefixLen: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
		),
	),
	/**
	 * EndpointID uniquely represents a service endpoint in a Sandbox.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 */
	EndpointID: v.exactOptional(v.string()),
	/**
	 * Gateway address for the default "bridge" network.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 */
	Gateway: v.exactOptional(v.string()),
	/**
	 * Global IPv6 address for the default "bridge" network.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 */
	GlobalIPv6Address: v.exactOptional(v.string()),
	/**
	 * Mask length of the global IPv6 address.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 */
	GlobalIPv6PrefixLen: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * IPv4 address for the default "bridge" network.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 */
	IPAddress: v.exactOptional(v.string()),
	/**
	 * Mask length of the IPv4 address.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 */
	IPPrefixLen: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * IPv6 gateway address for this network.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 */
	IPv6Gateway: v.exactOptional(v.string()),
	/**
	 * MAC address for the container on the default "bridge" network.
	 *
	 * <p><br /></p>
	 *
	 * > **Deprecated**: This field is only propagated when attached to the
	 * > default "bridge" network. Use the information from the "bridge"
	 * > network inside the `Networks` map instead, which contains the same
	 * > information. This field was deprecated in Docker 1.9 and is scheduled
	 * > to be removed in Docker 17.12.0
	 */
	MacAddress: v.exactOptional(v.string()),
	/**
	 * Information about all networks that the container is connected to.
	 */
	Networks: v.exactOptional(v.record(v.string(), v.unknown())),
});
/** Address represents an IPv4 or IPv6 IP address. */
export const addressSchema = v.strictObject({
	/**
	 * IP address.
	 */
	Addr: v.exactOptional(v.string()),
	/**
	 * Mask length of the IP address.
	 */
	PrefixLen: v.exactOptional(v.pipe(v.number(), v.integer())),
});
/**
 * PortMap describes the mapping of container ports to host ports, using the
 * container's port-number and protocol as key in the format
 * `<port>/<protocol>`,
 * for example, `80/udp`.
 *
 * If a container's port is mapped for multiple protocols, separate entries
 * are added to the mapping table.
 * @example {
 *   "443/tcp": [
 *     {
 *       "HostIp": "127.0.0.1",
 *       "HostPort": "4443"
 *     }
 *   ],
 *   "80/tcp": [
 *     {
 *       "HostIp": "0.0.0.0",
 *       "HostPort": "80"
 *     },
 *     {
 *       "HostIp": "0.0.0.0",
 *       "HostPort": "8080"
 *     }
 *   ],
 *   "80/udp": [
 *     {
 *       "HostIp": "0.0.0.0",
 *       "HostPort": "80"
 *     }
 *   ],
 *   "53/udp": [
 *     {
 *       "HostIp": "0.0.0.0",
 *       "HostPort": "53"
 *     }
 *   ]
 * }
 */
export const portMapSchema = v.record(v.string(), v.unknown());
/**
 * PortBinding represents a binding between a host IP address and a host
 * port.
 */
export const portBindingSchema = v.strictObject({
	/**
	 * Host IP address that the container's port is mapped to.
	 */
	HostIp: v.exactOptional(v.string()),
	/**
	 * Host port number that the container's port is mapped to.
	 */
	HostPort: v.exactOptional(v.string()),
});
/**
 * Information about the storage driver used to store the container's and
 * image's filesystem.
 */
export const graphDriverDataSchema = v.strictObject({
	/**
	 * Name of the storage driver.
	 */
	Name: v.string(),
	/**
	 * Low-level storage metadata, provided as key/value pairs.
	 *
	 * This information is driver-specific, and depends on the storage-driver
	 * in use, and should be used for informational purposes only.
	 */
	Data: v.record(v.string(), v.unknown()),
});
/** Change in the container's filesystem. */
export const filesystemChangeSchema = v.strictObject({
	/**
	 * Path to file or directory that has changed.
	 */
	Path: v.string(),
	/**
	 * Kind of change
	 *
	 * Can be one of:
	 *
	 * - `0`: Modified ("C")
	 * - `1`: Added ("A")
	 * - `2`: Deleted ("D")
	 */
	Kind: v.pipe(v.number(), v.integer()),
});
/**
 * Kind of change
 *
 * Can be one of:
 *
 * - `0`: Modified ("C")
 * - `1`: Added ("A")
 * - `2`: Deleted ("D")
 */
export const changeTypeSchema = v.pipe(v.number(), v.integer());
/** Information about an image in the local image cache. */
export const imageInspectSchema = v.strictObject({
	/**
	 * ID is the content-addressable ID of an image.
	 *
	 * This identifier is a content-addressable digest calculated from the
	 * image's configuration (which includes the digests of layers used by
	 * the image).
	 *
	 * Note that this digest differs from the `RepoDigests` below, which
	 * holds digests of image manifests that reference the image.
	 */
	Id: v.exactOptional(v.string()),
	/**
	 * List of image names/tags in the local image cache that reference this
	 * image.
	 *
	 * Multiple image tags can refer to the same image, and this list may be
	 * empty if no tags reference the image, in which case the image is
	 * "untagged", in which case it can still be referenced by its ID.
	 */
	RepoTags: v.exactOptional(v.array(v.string())),
	/**
	 * List of content-addressable digests of locally available image manifests
	 * that the image is referenced from. Multiple manifests can refer to the
	 * same image.
	 *
	 * These digests are usually only available if the image was either pulled
	 * from a registry, or if the image was pushed to a registry, which is when
	 * the manifest is generated and its digest calculated.
	 */
	RepoDigests: v.exactOptional(v.array(v.string())),
	/**
	 * ID of the parent image.
	 *
	 * Depending on how the image was created, this field may be empty and
	 * is only set for images that were built/created locally. This field
	 * is empty if the image was pulled from an image registry.
	 */
	Parent: v.exactOptional(v.string()),
	/**
	 * Optional message that was set when committing or importing the image.
	 */
	Comment: v.exactOptional(v.string()),
	/**
	 * Date and time at which the image was created, formatted in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 */
	Created: v.exactOptional(v.string()),
	/**
	 * The ID of the container that was used to create the image.
	 *
	 * Depending on how the image was created, this field may be empty.
	 */
	Container: v.exactOptional(v.string()),
	/**
	 * Configuration for a container that is portable between hosts.
	 *
	 * When used as `ContainerConfig` field in an image, `ContainerConfig` is an
	 * optional field containing the configuration of the container that was last
	 * committed when creating the image.
	 *
	 * Previous versions of Docker builder used this field to store build cache,
	 * and it is not in active use anymore.
	 */
	ContainerConfig: v.exactOptional(
		v.strictObject({
			/**
			 * The hostname to use for the container, as a valid RFC 1123 hostname.
			 */
			Hostname: v.exactOptional(v.string()),
			/**
			 * The domain name to use for the container.
			 */
			Domainname: v.exactOptional(v.string()),
			/**
			 * The user that commands are run as inside the container.
			 */
			User: v.exactOptional(v.string()),
			/**
			 * Whether to attach to `stdin`.
			 */
			AttachStdin: v.exactOptional(v.boolean()),
			/**
			 * Whether to attach to `stdout`.
			 */
			AttachStdout: v.exactOptional(v.boolean()),
			/**
			 * Whether to attach to `stderr`.
			 */
			AttachStderr: v.exactOptional(v.boolean()),
			/**
			 * An object mapping ports to an empty object in the form:
			 *
			 * `{"<port>/<tcp|udp|sctp>": {}}`
			 */
			ExposedPorts: v.exactOptional(
				v.nullable(v.record(v.string(), v.unknown())),
			),
			/**
			 * Attach standard streams to a TTY, including `stdin` if it is not closed.
			 */
			Tty: v.exactOptional(v.boolean()),
			/**
			 * Open `stdin`
			 */
			OpenStdin: v.exactOptional(v.boolean()),
			/**
			 * Close `stdin` after one attached client disconnects
			 */
			StdinOnce: v.exactOptional(v.boolean()),
			/**
			 * A list of environment variables to set inside the container in the
			 * form `["VAR=value", ...]`. A variable without `=` is removed from the
			 * environment, rather than to have an empty value.
			 */
			Env: v.exactOptional(v.array(v.string())),
			/**
			 * Command to run specified as a string or an array of strings.
			 */
			Cmd: v.exactOptional(v.array(v.string())),
			/**
			 * A test to perform to check that the container is healthy.
			 */
			Healthcheck: v.exactOptional(
				v.strictObject({
					/**
					 * The test to perform. Possible values are:
					 *
					 * - `[]` inherit healthcheck from image or parent image
					 * - `["NONE"]` disable healthcheck
					 * - `["CMD", args...]` exec arguments directly
					 * - `["CMD-SHELL", command]` run command with system's default shell
					 */
					Test: v.exactOptional(v.array(v.string())),
					/**
					 * The time to wait between checks in nanoseconds. It should be 0 or at
					 * least 1000000 (1 ms). 0 means inherit.
					 */
					Interval: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The time to wait before considering the check to have hung. It should
					 * be 0 or at least 1000000 (1 ms). 0 means inherit.
					 */
					Timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The number of consecutive failures needed to consider a container as
					 * unhealthy. 0 means inherit.
					 */
					Retries: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Start period for the container to initialize before starting
					 * health-retries countdown in nanoseconds. It should be 0 or at least
					 * 1000000 (1 ms). 0 means inherit.
					 */
					StartPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
			/**
			 * Command is already escaped (Windows only)
			 */
			ArgsEscaped: v.exactOptional(v.nullable(v.boolean())),
			/**
			 * The name (or reference) of the image to use when creating the container,
			 * or which was used when the container was created.
			 */
			Image: v.exactOptional(v.string()),
			/**
			 * An object mapping mount point paths inside the container to empty
			 * objects.
			 */
			Volumes: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * The working directory for commands to run in.
			 */
			WorkingDir: v.exactOptional(v.string()),
			/**
			 * The entry point for the container as a string or an array of strings.
			 *
			 * If the array consists of exactly one empty string (`[""]`) then the
			 * entry point is reset to system default (i.e., the entry point used by
			 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
			 */
			Entrypoint: v.exactOptional(v.array(v.string())),
			/**
			 * Disable networking for the container.
			 */
			NetworkDisabled: v.exactOptional(v.nullable(v.boolean())),
			/**
			 * MAC address of the container.
			 */
			MacAddress: v.exactOptional(v.nullable(v.string())),
			/**
			 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
			 */
			OnBuild: v.exactOptional(v.nullable(v.array(v.string()))),
			/**
			 * User-defined key/value metadata.
			 */
			Labels: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * Signal to stop a container as a string or unsigned integer.
			 */
			StopSignal: v.exactOptional(v.nullable(v.string())),
			/**
			 * Timeout to stop a container in seconds.
			 */
			StopTimeout: v.exactOptional(v.nullable(v.pipe(v.number(), v.integer()))),
			/**
			 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
			 */
			Shell: v.exactOptional(v.nullable(v.array(v.string()))),
		}),
	),
	/**
	 * The version of Docker that was used to build the image.
	 *
	 * Depending on how the image was created, this field may be empty.
	 */
	DockerVersion: v.exactOptional(v.string()),
	/**
	 * Name of the author that was specified when committing the image, or as
	 * specified through MAINTAINER (deprecated) in the Dockerfile.
	 */
	Author: v.exactOptional(v.string()),
	/**
	 * Configuration of the image. These fields are used as defaults
	 * when starting a container from the image.
	 */
	Config: v.exactOptional(
		v.strictObject({
			/**
			 * The hostname to use for the container, as a valid RFC 1123 hostname.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: this field is always empty and must not be used.
			 */
			Hostname: v.exactOptional(v.string()),
			/**
			 * The domain name to use for the container.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: this field is always empty and must not be used.
			 */
			Domainname: v.exactOptional(v.string()),
			/**
			 * The user that commands are run as inside the container.
			 */
			User: v.exactOptional(v.string()),
			/**
			 * Whether to attach to `stdin`.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: this field is always false and must not be used.
			 */
			AttachStdin: v.exactOptional(v.boolean()),
			/**
			 * Whether to attach to `stdout`.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: this field is always false and must not be used.
			 */
			AttachStdout: v.exactOptional(v.boolean()),
			/**
			 * Whether to attach to `stderr`.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: this field is always false and must not be used.
			 */
			AttachStderr: v.exactOptional(v.boolean()),
			/**
			 * An object mapping ports to an empty object in the form:
			 *
			 * `{"<port>/<tcp|udp|sctp>": {}}`
			 */
			ExposedPorts: v.exactOptional(
				v.nullable(v.record(v.string(), v.unknown())),
			),
			/**
			 * Attach standard streams to a TTY, including `stdin` if it is not closed.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: this field is always false and must not be used.
			 */
			Tty: v.exactOptional(v.boolean()),
			/**
			 * Open `stdin`
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: this field is always false and must not be used.
			 */
			OpenStdin: v.exactOptional(v.boolean()),
			/**
			 * Close `stdin` after one attached client disconnects.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: this field is always false and must not be used.
			 */
			StdinOnce: v.exactOptional(v.boolean()),
			/**
			 * A list of environment variables to set inside the container in the
			 * form `["VAR=value", ...]`. A variable without `=` is removed from the
			 * environment, rather than to have an empty value.
			 */
			Env: v.exactOptional(v.array(v.string())),
			/**
			 * Command to run specified as a string or an array of strings.
			 */
			Cmd: v.exactOptional(v.array(v.string())),
			/**
			 * A test to perform to check that the container is healthy.
			 */
			Healthcheck: v.exactOptional(
				v.strictObject({
					/**
					 * The test to perform. Possible values are:
					 *
					 * - `[]` inherit healthcheck from image or parent image
					 * - `["NONE"]` disable healthcheck
					 * - `["CMD", args...]` exec arguments directly
					 * - `["CMD-SHELL", command]` run command with system's default shell
					 */
					Test: v.exactOptional(v.array(v.string())),
					/**
					 * The time to wait between checks in nanoseconds. It should be 0 or at
					 * least 1000000 (1 ms). 0 means inherit.
					 */
					Interval: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The time to wait before considering the check to have hung. It should
					 * be 0 or at least 1000000 (1 ms). 0 means inherit.
					 */
					Timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The number of consecutive failures needed to consider a container as
					 * unhealthy. 0 means inherit.
					 */
					Retries: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Start period for the container to initialize before starting
					 * health-retries countdown in nanoseconds. It should be 0 or at least
					 * 1000000 (1 ms). 0 means inherit.
					 */
					StartPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
			/**
			 * Command is already escaped (Windows only)
			 */
			ArgsEscaped: v.exactOptional(v.nullable(v.boolean())),
			/**
			 * The name (or reference) of the image to use when creating the container,
			 * or which was used when the container was created.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: this field is always empty and must not be used.
			 */
			Image: v.exactOptional(v.string()),
			/**
			 * An object mapping mount point paths inside the container to empty
			 * objects.
			 */
			Volumes: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * The working directory for commands to run in.
			 */
			WorkingDir: v.exactOptional(v.string()),
			/**
			 * The entry point for the container as a string or an array of strings.
			 *
			 * If the array consists of exactly one empty string (`[""]`) then the
			 * entry point is reset to system default (i.e., the entry point used by
			 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
			 */
			Entrypoint: v.exactOptional(v.array(v.string())),
			/**
			 * Disable networking for the container.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: this field is always omitted and must not be used.
			 */
			NetworkDisabled: v.exactOptional(v.nullable(v.boolean())),
			/**
			 * MAC address of the container.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: this field is always omitted and must not be used.
			 */
			MacAddress: v.exactOptional(v.nullable(v.string())),
			/**
			 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
			 */
			OnBuild: v.exactOptional(v.nullable(v.array(v.string()))),
			/**
			 * User-defined key/value metadata.
			 */
			Labels: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * Signal to stop a container as a string or unsigned integer.
			 */
			StopSignal: v.exactOptional(v.nullable(v.string())),
			/**
			 * Timeout to stop a container in seconds.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: this field is always omitted and must not be used.
			 */
			StopTimeout: v.exactOptional(v.nullable(v.pipe(v.number(), v.integer()))),
			/**
			 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
			 */
			Shell: v.exactOptional(v.nullable(v.array(v.string()))),
		}),
	),
	/**
	 * Hardware CPU architecture that the image runs on.
	 */
	Architecture: v.exactOptional(v.string()),
	/**
	 * CPU architecture variant (presently ARM-only).
	 */
	Variant: v.exactOptional(v.nullable(v.string())),
	/**
	 * Operating System the image is built to run on.
	 */
	Os: v.exactOptional(v.string()),
	/**
	 * Operating System version the image is built to run on (especially
	 * for Windows).
	 */
	OsVersion: v.exactOptional(v.nullable(v.string())),
	/**
	 * Total size of the image including all layers it is composed of.
	 */
	Size: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Total size of the image including all layers it is composed of.
	 *
	 * In versions of Docker before v1.10, this field was calculated from
	 * the image itself and all of its parent images. Images are now stored
	 * self-contained, and no longer use a parent-chain, making this field
	 * an equivalent of the Size field.
	 *
	 * > **Deprecated**: this field is kept for backward compatibility, but
	 * > will be removed in API v1.44.
	 */
	VirtualSize: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Information about the storage driver used to store the container's and
	 * image's filesystem.
	 */
	GraphDriver: v.exactOptional(
		v.strictObject({
			/**
			 * Name of the storage driver.
			 */
			Name: v.string(),
			/**
			 * Low-level storage metadata, provided as key/value pairs.
			 *
			 * This information is driver-specific, and depends on the storage-driver
			 * in use, and should be used for informational purposes only.
			 */
			Data: v.record(v.string(), v.unknown()),
		}),
	),
	/**
	 * Information about the image's RootFS, including the layer IDs.
	 */
	RootFS: v.exactOptional(
		v.strictObject({
			Type: v.string(),
			Layers: v.exactOptional(v.array(v.string())),
		}),
	),
	/**
	 * Additional metadata of the image in the local cache. This information
	 * is local to the daemon, and not part of the image itself.
	 */
	Metadata: v.exactOptional(
		v.strictObject({
			/**
			 * Date and time at which the image was last tagged in
			 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
			 *
			 * This information is only available if the image was tagged locally,
			 * and omitted otherwise.
			 */
			LastTagTime: v.exactOptional(v.nullable(v.string())),
		}),
	),
});
export const imageSummarySchema = v.strictObject({
	/**
	 * ID is the content-addressable ID of an image.
	 *
	 * This identifier is a content-addressable digest calculated from the
	 * image's configuration (which includes the digests of layers used by
	 * the image).
	 *
	 * Note that this digest differs from the `RepoDigests` below, which
	 * holds digests of image manifests that reference the image.
	 */
	Id: v.string(),
	/**
	 * ID of the parent image.
	 *
	 * Depending on how the image was created, this field may be empty and
	 * is only set for images that were built/created locally. This field
	 * is empty if the image was pulled from an image registry.
	 */
	ParentId: v.string(),
	/**
	 * List of image names/tags in the local image cache that reference this
	 * image.
	 *
	 * Multiple image tags can refer to the same image, and this list may be
	 * empty if no tags reference the image, in which case the image is
	 * "untagged", in which case it can still be referenced by its ID.
	 */
	RepoTags: v.array(v.string()),
	/**
	 * List of content-addressable digests of locally available image manifests
	 * that the image is referenced from. Multiple manifests can refer to the
	 * same image.
	 *
	 * These digests are usually only available if the image was either pulled
	 * from a registry, or if the image was pushed to a registry, which is when
	 * the manifest is generated and its digest calculated.
	 */
	RepoDigests: v.array(v.string()),
	/**
	 * Date and time at which the image was created as a Unix timestamp
	 * (number of seconds since EPOCH).
	 */
	Created: v.pipe(v.number(), v.integer()),
	/**
	 * Total size of the image including all layers it is composed of.
	 */
	Size: v.pipe(v.number(), v.integer()),
	/**
	 * Total size of image layers that are shared between this image and other
	 * images.
	 *
	 * This size is not calculated by default. `-1` indicates that the value
	 * has not been set / calculated.
	 */
	SharedSize: v.pipe(v.number(), v.integer()),
	/**
	 * Total size of the image including all layers it is composed of.
	 *
	 * In versions of Docker before v1.10, this field was calculated from
	 * the image itself and all of its parent images. Images are now stored
	 * self-contained, and no longer use a parent-chain, making this field
	 * an equivalent of the Size field.
	 *
	 * Deprecated: this field is kept for backward compatibility, and will be
	 * removed in API v1.44.
	 */
	VirtualSize: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.record(v.string(), v.unknown()),
	/**
	 * Number of containers using this image. Includes both stopped and running
	 * containers.
	 *
	 * This size is not calculated by default, and depends on which API endpoint
	 * is used. `-1` indicates that the value has not been set / calculated.
	 */
	Containers: v.pipe(v.number(), v.integer()),
});
export const authConfigSchema = v.strictObject({
	username: v.exactOptional(v.string()),
	password: v.exactOptional(v.string()),
	email: v.exactOptional(v.string()),
	serveraddress: v.exactOptional(v.string()),
});
export const processConfigSchema = v.strictObject({
	privileged: v.exactOptional(v.boolean()),
	user: v.exactOptional(v.string()),
	tty: v.exactOptional(v.boolean()),
	entrypoint: v.exactOptional(v.string()),
	arguments: v.exactOptional(v.array(v.string())),
});
export const volumeSchema = v.strictObject({
	/**
	 * Name of the volume.
	 */
	Name: v.string(),
	/**
	 * Name of the volume driver used by the volume.
	 */
	Driver: v.string(),
	/**
	 * Mount path of the volume on the host.
	 */
	Mountpoint: v.string(),
	/**
	 * Date/Time the volume was created.
	 */
	CreatedAt: v.exactOptional(v.string()),
	/**
	 * Low-level details about the volume, provided by the volume driver.
	 * Details are returned as a map with key/value pairs:
	 * `{"key":"value","key2":"value2"}`.
	 *
	 * The `Status` field is optional, and is omitted if the volume driver
	 * does not support this feature.
	 */
	Status: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.record(v.string(), v.unknown()),
	/**
	 * The level at which the volume exists. Either `global` for cluster-wide,
	 * or `local` for machine level.
	 */
	Scope: v.picklist(["local", "global"]),
	/**
	 * Options and information specific to, and only present on, Swarm CSI
	 * cluster volumes.
	 */
	ClusterVolume: v.exactOptional(
		v.strictObject({
			/**
			 * The Swarm ID of this volume. Because cluster volumes are Swarm
			 * objects, they have an ID, unlike non-cluster volumes. This ID can
			 * be used to refer to the Volume instead of the name.
			 */
			ID: v.exactOptional(v.string()),
			/**
			 * The version number of the object such as node, service, etc. This is needed
			 * to avoid conflicting writes. The client must send the version number along
			 * with the modified specification when updating these objects.
			 *
			 * This approach ensures safe concurrency and determinism in that the change
			 * on the object may not be applied if the version number has changed from the
			 * last read. In other words, if two update requests specify the same base
			 * version, only one of the requests can succeed. As a result, two separate
			 * update requests that happen at the same time will not unintentionally
			 * overwrite each other.
			 */
			Version: v.exactOptional(
				v.strictObject({
					Index: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
			CreatedAt: v.exactOptional(v.string()),
			UpdatedAt: v.exactOptional(v.string()),
			/**
			 * Cluster-specific options used to create the volume.
			 */
			Spec: v.exactOptional(
				v.strictObject({
					/**
					 * Group defines the volume group of this volume. Volumes belonging to
					 * the same group can be referred to by group name when creating
					 * Services.  Referring to a volume by group instructs Swarm to treat
					 * volumes in that group interchangeably for the purpose of scheduling.
					 * Volumes with an empty string for a group technically all belong to
					 * the same, emptystring group.
					 */
					Group: v.exactOptional(v.string()),
					/**
					 * Defines how the volume is used by tasks.
					 */
					AccessMode: v.exactOptional(
						v.strictObject({
							/**
							 * The set of nodes this volume can be used on at one time.
							 * - `single` The volume may only be scheduled to one node at a time.
							 * - `multi` the volume may be scheduled to any supported number of nodes at a
							 * time.
							 */
							Scope: v.exactOptional(v.picklist(["single", "multi"])),
							/**
							 * The number and way that different tasks can use this volume
							 * at one time.
							 * - `none` The volume may only be used by one task at a time.
							 * - `readonly` The volume may be used by any number of tasks, but they all
							 * must mount the volume as readonly
							 * - `onewriter` The volume may be used by any number of tasks, but only one
							 * may mount it as read/write.
							 * - `all` The volume may have any number of readers and writers.
							 */
							Sharing: v.exactOptional(
								v.picklist(["none", "readonly", "onewriter", "all"]),
							),
							/**
							 * Options for using this volume as a Mount-type volume.
							 *
							 *
							 * Either MountVolume or BlockVolume, but not both, must be
							 *
							 * present.
							 *
							 * properties:
							 *
							 * FsType:
							 *
							 * type: "string"
							 *
							 * description: |
							 *
							 * Specifies the filesystem type for the mount volume.
							 *
							 * Optional.
							 *
							 * MountFlags:
							 *
							 * type: "array"
							 *
							 * description: |
							 *
							 * Flags to pass when mounting the volume. Optional.
							 *
							 * items:
							 *
							 * type: "string"
							 * BlockVolume:
							 *
							 * type: "object"
							 *
							 * description: |
							 *
							 * Options for using this volume as a Block-type volume.
							 *
							 * Intentionally empty.
							 */
							MountVolume: v.exactOptional(v.record(v.string(), v.unknown())),
							/**
							 * Swarm Secrets that are passed to the CSI storage plugin when
							 * operating on this volume.
							 */
							Secrets: v.exactOptional(
								v.array(
									v.strictObject({
										/**
										 * Key is the name of the key of the key-value pair passed to
										 * the plugin.
										 */
										Key: v.exactOptional(v.string()),
										/**
										 * Secret is the swarm Secret object from which to read data.
										 * This can be a Secret name or ID. The Secret data is
										 * retrieved by swarm and used as the value of the key-value
										 * pair passed to the plugin.
										 */
										Secret: v.exactOptional(v.string()),
									}),
								),
							),
							/**
							 * Requirements for the accessible topology of the volume. These
							 * fields are optional. For an in-depth description of what these
							 * fields mean, see the CSI specification.
							 */
							AccessibilityRequirements: v.exactOptional(
								v.strictObject({
									/**
									 * A list of required topologies, at least one of which the
									 * volume must be accessible from.
									 */
									Requisite: v.exactOptional(
										v.array(v.record(v.string(), v.unknown())),
									),
									/**
									 * A list of topologies that the volume should attempt to be
									 * provisioned in.
									 */
									Preferred: v.exactOptional(
										v.array(v.record(v.string(), v.unknown())),
									),
								}),
							),
							/**
							 * The desired capacity that the volume should be created with. If
							 * empty, the plugin will decide the capacity.
							 */
							CapacityRange: v.exactOptional(
								v.strictObject({
									/**
									 * The volume must be at least this big. The value of 0
									 * indicates an unspecified minimum
									 */
									RequiredBytes: v.exactOptional(
										v.pipe(v.number(), v.integer()),
									),
									/**
									 * The volume must not be bigger than this. The value of 0
									 * indicates an unspecified maximum.
									 */
									LimitBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
								}),
							),
							/**
							 * The availability of the volume for use in tasks.
							 * - `active` The volume is fully available for scheduling on the cluster
							 * - `pause` No new workloads should use the volume, but existing workloads
							 * are not stopped.
							 * - `drain` All workloads using this volume should be stopped and
							 * rescheduled, and no new ones should be started.
							 */
							Availability: v.exactOptional(
								v.picklist(["active", "pause", "drain"]),
							),
						}),
					),
				}),
			),
			/**
			 * Information about the global status of the volume.
			 */
			Info: v.exactOptional(
				v.strictObject({
					/**
					 * The capacity of the volume in bytes. A value of 0 indicates that
					 * the capacity is unknown.
					 */
					CapacityBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * A map of strings to strings returned from the storage plugin when
					 * the volume is created.
					 */
					VolumeContext: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * The ID of the volume as returned by the CSI storage plugin. This
					 * is distinct from the volume's ID as provided by Docker. This ID
					 * is never used by the user when communicating with Docker to refer
					 * to this volume. If the ID is blank, then the Volume has not been
					 * successfully created in the plugin yet.
					 */
					VolumeID: v.exactOptional(v.string()),
					/**
					 * The topology this volume is actually accessible from.
					 */
					AccessibleTopology: v.exactOptional(
						v.array(v.record(v.string(), v.unknown())),
					),
				}),
			),
			/**
			 * The status of the volume as it pertains to its publishing and use on
			 * specific nodes
			 */
			PublishStatus: v.exactOptional(
				v.array(
					v.strictObject({
						/**
						 * The ID of the Swarm node the volume is published on.
						 */
						NodeID: v.exactOptional(v.string()),
						/**
						 * The published state of the volume.
						 * * `pending-publish` The volume should be published to this node, but the
						 * call to the controller plugin to do so has not yet been successfully
						 * completed.
						 * * `published` The volume is published successfully to the node.
						 * * `pending-node-unpublish` The volume should be unpublished from the node,
						 * and the manager is awaiting confirmation from the worker that it has done
						 * so.
						 * * `pending-controller-unpublish` The volume is successfully unpublished
						 * from the node, but has not yet been successfully unpublished on the
						 * controller.
						 */
						State: v.exactOptional(
							v.picklist([
								"pending-publish",
								"published",
								"pending-node-unpublish",
								"pending-controller-unpublish",
							]),
						),
						/**
						 * A map of strings to strings returned by the CSI controller
						 * plugin when a volume is published.
						 */
						PublishContext: v.exactOptional(v.record(v.string(), v.unknown())),
					}),
				),
			),
		}),
	),
	/**
	 * The driver specific options used when creating the volume.
	 */
	Options: v.record(v.string(), v.unknown()),
	/**
	 * Usage details about the volume. This information is used by the
	 * `GET /system/df` endpoint, and omitted in other endpoints.
	 */
	UsageData: v.exactOptional(
		v.nullable(
			v.strictObject({
				/**
				 * Amount of disk space used by the volume (in bytes). This information
				 * is only available for volumes created with the `"local"` volume
				 * driver. For volumes created with other volume drivers, this field
				 * is set to `-1` ("not available")
				 */
				Size: v.pipe(v.number(), v.integer()),
				/**
				 * The number of containers referencing this volume. This field
				 * is set to `-1` if the reference-count is not available.
				 */
				RefCount: v.pipe(v.number(), v.integer()),
			}),
		),
	),
});
/**
 * Volume configuration
 * @title VolumeConfig
 */
export const volumeCreateOptionsSchema = v.strictObject({
	/**
	 * The new volume's name. If not specified, Docker generates a name.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * Name of the volume driver to use.
	 */
	Driver: v.exactOptional(v.string()),
	/**
	 * A mapping of driver options and values. These options are
	 * passed directly to the driver and are driver specific.
	 */
	DriverOpts: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Cluster-specific options used to create the volume.
	 */
	ClusterVolumeSpec: v.exactOptional(
		v.strictObject({
			/**
			 * Group defines the volume group of this volume. Volumes belonging to
			 * the same group can be referred to by group name when creating
			 * Services.  Referring to a volume by group instructs Swarm to treat
			 * volumes in that group interchangeably for the purpose of scheduling.
			 * Volumes with an empty string for a group technically all belong to
			 * the same, emptystring group.
			 */
			Group: v.exactOptional(v.string()),
			/**
			 * Defines how the volume is used by tasks.
			 */
			AccessMode: v.exactOptional(
				v.strictObject({
					/**
					 * The set of nodes this volume can be used on at one time.
					 * - `single` The volume may only be scheduled to one node at a time.
					 * - `multi` the volume may be scheduled to any supported number of nodes at a
					 * time.
					 */
					Scope: v.exactOptional(v.picklist(["single", "multi"])),
					/**
					 * The number and way that different tasks can use this volume
					 * at one time.
					 * - `none` The volume may only be used by one task at a time.
					 * - `readonly` The volume may be used by any number of tasks, but they all
					 * must mount the volume as readonly
					 * - `onewriter` The volume may be used by any number of tasks, but only one
					 * may mount it as read/write.
					 * - `all` The volume may have any number of readers and writers.
					 */
					Sharing: v.exactOptional(
						v.picklist(["none", "readonly", "onewriter", "all"]),
					),
					/**
					 * Options for using this volume as a Mount-type volume.
					 *
					 *
					 * Either MountVolume or BlockVolume, but not both, must be
					 *
					 * present.
					 *
					 * properties:
					 *
					 * FsType:
					 *
					 * type: "string"
					 *
					 * description: |
					 *
					 * Specifies the filesystem type for the mount volume.
					 *
					 * Optional.
					 *
					 * MountFlags:
					 *
					 * type: "array"
					 *
					 * description: |
					 *
					 * Flags to pass when mounting the volume. Optional.
					 *
					 * items:
					 *
					 * type: "string"
					 * BlockVolume:
					 *
					 * type: "object"
					 *
					 * description: |
					 *
					 * Options for using this volume as a Block-type volume.
					 *
					 * Intentionally empty.
					 */
					MountVolume: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * Swarm Secrets that are passed to the CSI storage plugin when
					 * operating on this volume.
					 */
					Secrets: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Key is the name of the key of the key-value pair passed to
								 * the plugin.
								 */
								Key: v.exactOptional(v.string()),
								/**
								 * Secret is the swarm Secret object from which to read data.
								 * This can be a Secret name or ID. The Secret data is
								 * retrieved by swarm and used as the value of the key-value
								 * pair passed to the plugin.
								 */
								Secret: v.exactOptional(v.string()),
							}),
						),
					),
					/**
					 * Requirements for the accessible topology of the volume. These
					 * fields are optional. For an in-depth description of what these
					 * fields mean, see the CSI specification.
					 */
					AccessibilityRequirements: v.exactOptional(
						v.strictObject({
							/**
							 * A list of required topologies, at least one of which the
							 * volume must be accessible from.
							 */
							Requisite: v.exactOptional(
								v.array(v.record(v.string(), v.unknown())),
							),
							/**
							 * A list of topologies that the volume should attempt to be
							 * provisioned in.
							 */
							Preferred: v.exactOptional(
								v.array(v.record(v.string(), v.unknown())),
							),
						}),
					),
					/**
					 * The desired capacity that the volume should be created with. If
					 * empty, the plugin will decide the capacity.
					 */
					CapacityRange: v.exactOptional(
						v.strictObject({
							/**
							 * The volume must be at least this big. The value of 0
							 * indicates an unspecified minimum
							 */
							RequiredBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The volume must not be bigger than this. The value of 0
							 * indicates an unspecified maximum.
							 */
							LimitBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
					/**
					 * The availability of the volume for use in tasks.
					 * - `active` The volume is fully available for scheduling on the cluster
					 * - `pause` No new workloads should use the volume, but existing workloads
					 * are not stopped.
					 * - `drain` All workloads using this volume should be stopped and
					 * rescheduled, and no new ones should be started.
					 */
					Availability: v.exactOptional(
						v.picklist(["active", "pause", "drain"]),
					),
				}),
			),
		}),
	),
});
/**
 * Volume list response
 * @title VolumeListResponse
 */
export const volumeListResponseSchema = v.strictObject({
	/**
	 * List of volumes
	 */
	Volumes: v.exactOptional(
		v.array(
			v.strictObject({
				/**
				 * Name of the volume.
				 */
				Name: v.string(),
				/**
				 * Name of the volume driver used by the volume.
				 */
				Driver: v.string(),
				/**
				 * Mount path of the volume on the host.
				 */
				Mountpoint: v.string(),
				/**
				 * Date/Time the volume was created.
				 */
				CreatedAt: v.exactOptional(v.string()),
				/**
				 * Low-level details about the volume, provided by the volume driver.
				 * Details are returned as a map with key/value pairs:
				 * `{"key":"value","key2":"value2"}`.
				 *
				 * The `Status` field is optional, and is omitted if the volume driver
				 * does not support this feature.
				 */
				Status: v.exactOptional(v.record(v.string(), v.unknown())),
				/**
				 * User-defined key/value metadata.
				 */
				Labels: v.record(v.string(), v.unknown()),
				/**
				 * The level at which the volume exists. Either `global` for cluster-wide,
				 * or `local` for machine level.
				 */
				Scope: v.picklist(["local", "global"]),
				/**
				 * Options and information specific to, and only present on, Swarm CSI
				 * cluster volumes.
				 */
				ClusterVolume: v.exactOptional(
					v.strictObject({
						/**
						 * The Swarm ID of this volume. Because cluster volumes are Swarm
						 * objects, they have an ID, unlike non-cluster volumes. This ID can
						 * be used to refer to the Volume instead of the name.
						 */
						ID: v.exactOptional(v.string()),
						/**
						 * The version number of the object such as node, service, etc. This is needed
						 * to avoid conflicting writes. The client must send the version number along
						 * with the modified specification when updating these objects.
						 *
						 * This approach ensures safe concurrency and determinism in that the change
						 * on the object may not be applied if the version number has changed from the
						 * last read. In other words, if two update requests specify the same base
						 * version, only one of the requests can succeed. As a result, two separate
						 * update requests that happen at the same time will not unintentionally
						 * overwrite each other.
						 */
						Version: v.exactOptional(
							v.strictObject({
								Index: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
						CreatedAt: v.exactOptional(v.string()),
						UpdatedAt: v.exactOptional(v.string()),
						/**
						 * Cluster-specific options used to create the volume.
						 */
						Spec: v.exactOptional(
							v.strictObject({
								/**
								 * Group defines the volume group of this volume. Volumes belonging to
								 * the same group can be referred to by group name when creating
								 * Services.  Referring to a volume by group instructs Swarm to treat
								 * volumes in that group interchangeably for the purpose of scheduling.
								 * Volumes with an empty string for a group technically all belong to
								 * the same, emptystring group.
								 */
								Group: v.exactOptional(v.string()),
								/**
								 * Defines how the volume is used by tasks.
								 */
								AccessMode: v.exactOptional(
									v.strictObject({
										/**
										 * The set of nodes this volume can be used on at one time.
										 * - `single` The volume may only be scheduled to one node at a time.
										 * - `multi` the volume may be scheduled to any supported number of nodes at a
										 * time.
										 */
										Scope: v.exactOptional(v.picklist(["single", "multi"])),
										/**
										 * The number and way that different tasks can use this volume
										 * at one time.
										 * - `none` The volume may only be used by one task at a time.
										 * - `readonly` The volume may be used by any number of tasks, but they all
										 * must mount the volume as readonly
										 * - `onewriter` The volume may be used by any number of tasks, but only one
										 * may mount it as read/write.
										 * - `all` The volume may have any number of readers and writers.
										 */
										Sharing: v.exactOptional(
											v.picklist(["none", "readonly", "onewriter", "all"]),
										),
										/**
										 * Options for using this volume as a Mount-type volume.
										 *
										 *
										 * Either MountVolume or BlockVolume, but not both, must be
										 *
										 * present.
										 *
										 * properties:
										 *
										 * FsType:
										 *
										 * type: "string"
										 *
										 * description: |
										 *
										 * Specifies the filesystem type for the mount volume.
										 *
										 * Optional.
										 *
										 * MountFlags:
										 *
										 * type: "array"
										 *
										 * description: |
										 *
										 * Flags to pass when mounting the volume. Optional.
										 *
										 * items:
										 *
										 * type: "string"
										 * BlockVolume:
										 *
										 * type: "object"
										 *
										 * description: |
										 *
										 * Options for using this volume as a Block-type volume.
										 *
										 * Intentionally empty.
										 */
										MountVolume: v.exactOptional(
											v.record(v.string(), v.unknown()),
										),
										/**
										 * Swarm Secrets that are passed to the CSI storage plugin when
										 * operating on this volume.
										 */
										Secrets: v.exactOptional(
											v.array(
												v.strictObject({
													/**
													 * Key is the name of the key of the key-value pair passed to
													 * the plugin.
													 */
													Key: v.exactOptional(v.string()),
													/**
													 * Secret is the swarm Secret object from which to read data.
													 * This can be a Secret name or ID. The Secret data is
													 * retrieved by swarm and used as the value of the key-value
													 * pair passed to the plugin.
													 */
													Secret: v.exactOptional(v.string()),
												}),
											),
										),
										/**
										 * Requirements for the accessible topology of the volume. These
										 * fields are optional. For an in-depth description of what these
										 * fields mean, see the CSI specification.
										 */
										AccessibilityRequirements: v.exactOptional(
											v.strictObject({
												/**
												 * A list of required topologies, at least one of which the
												 * volume must be accessible from.
												 */
												Requisite: v.exactOptional(
													v.array(v.record(v.string(), v.unknown())),
												),
												/**
												 * A list of topologies that the volume should attempt to be
												 * provisioned in.
												 */
												Preferred: v.exactOptional(
													v.array(v.record(v.string(), v.unknown())),
												),
											}),
										),
										/**
										 * The desired capacity that the volume should be created with. If
										 * empty, the plugin will decide the capacity.
										 */
										CapacityRange: v.exactOptional(
											v.strictObject({
												/**
												 * The volume must be at least this big. The value of 0
												 * indicates an unspecified minimum
												 */
												RequiredBytes: v.exactOptional(
													v.pipe(v.number(), v.integer()),
												),
												/**
												 * The volume must not be bigger than this. The value of 0
												 * indicates an unspecified maximum.
												 */
												LimitBytes: v.exactOptional(
													v.pipe(v.number(), v.integer()),
												),
											}),
										),
										/**
										 * The availability of the volume for use in tasks.
										 * - `active` The volume is fully available for scheduling on the cluster
										 * - `pause` No new workloads should use the volume, but existing workloads
										 * are not stopped.
										 * - `drain` All workloads using this volume should be stopped and
										 * rescheduled, and no new ones should be started.
										 */
										Availability: v.exactOptional(
											v.picklist(["active", "pause", "drain"]),
										),
									}),
								),
							}),
						),
						/**
						 * Information about the global status of the volume.
						 */
						Info: v.exactOptional(
							v.strictObject({
								/**
								 * The capacity of the volume in bytes. A value of 0 indicates that
								 * the capacity is unknown.
								 */
								CapacityBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * A map of strings to strings returned from the storage plugin when
								 * the volume is created.
								 */
								VolumeContext: v.exactOptional(
									v.record(v.string(), v.unknown()),
								),
								/**
								 * The ID of the volume as returned by the CSI storage plugin. This
								 * is distinct from the volume's ID as provided by Docker. This ID
								 * is never used by the user when communicating with Docker to refer
								 * to this volume. If the ID is blank, then the Volume has not been
								 * successfully created in the plugin yet.
								 */
								VolumeID: v.exactOptional(v.string()),
								/**
								 * The topology this volume is actually accessible from.
								 */
								AccessibleTopology: v.exactOptional(
									v.array(v.record(v.string(), v.unknown())),
								),
							}),
						),
						/**
						 * The status of the volume as it pertains to its publishing and use on
						 * specific nodes
						 */
						PublishStatus: v.exactOptional(
							v.array(
								v.strictObject({
									/**
									 * The ID of the Swarm node the volume is published on.
									 */
									NodeID: v.exactOptional(v.string()),
									/**
									 * The published state of the volume.
									 * * `pending-publish` The volume should be published to this node, but the
									 * call to the controller plugin to do so has not yet been successfully
									 * completed.
									 * * `published` The volume is published successfully to the node.
									 * * `pending-node-unpublish` The volume should be unpublished from the node,
									 * and the manager is awaiting confirmation from the worker that it has done
									 * so.
									 * * `pending-controller-unpublish` The volume is successfully unpublished
									 * from the node, but has not yet been successfully unpublished on the
									 * controller.
									 */
									State: v.exactOptional(
										v.picklist([
											"pending-publish",
											"published",
											"pending-node-unpublish",
											"pending-controller-unpublish",
										]),
									),
									/**
									 * A map of strings to strings returned by the CSI controller
									 * plugin when a volume is published.
									 */
									PublishContext: v.exactOptional(
										v.record(v.string(), v.unknown()),
									),
								}),
							),
						),
					}),
				),
				/**
				 * The driver specific options used when creating the volume.
				 */
				Options: v.record(v.string(), v.unknown()),
				/**
				 * Usage details about the volume. This information is used by the
				 * `GET /system/df` endpoint, and omitted in other endpoints.
				 */
				UsageData: v.exactOptional(
					v.nullable(
						v.strictObject({
							/**
							 * Amount of disk space used by the volume (in bytes). This information
							 * is only available for volumes created with the `"local"` volume
							 * driver. For volumes created with other volume drivers, this field
							 * is set to `-1` ("not available")
							 */
							Size: v.pipe(v.number(), v.integer()),
							/**
							 * The number of containers referencing this volume. This field
							 * is set to `-1` if the reference-count is not available.
							 */
							RefCount: v.pipe(v.number(), v.integer()),
						}),
					),
				),
			}),
		),
	),
	/**
	 * Warnings that occurred when fetching the list of volumes.
	 */
	Warnings: v.exactOptional(v.array(v.string())),
});
export const networkSchema = v.strictObject({
	/**
	 * Name of the network.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * ID that uniquely identifies a network on a single machine.
	 */
	Id: v.exactOptional(v.string()),
	/**
	 * Date and time at which the network was created in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 */
	Created: v.exactOptional(v.string()),
	/**
	 * The level at which the network exists (e.g. `swarm` for cluster-wide
	 * or `local` for machine level)
	 */
	Scope: v.exactOptional(v.string()),
	/**
	 * The name of the driver used to create the network (e.g. `bridge`,
	 * `overlay`).
	 */
	Driver: v.exactOptional(v.string()),
	/**
	 * Whether the network was created with IPv6 enabled.
	 */
	EnableIPv6: v.exactOptional(v.boolean()),
	IPAM: v.exactOptional(
		v.strictObject({
			/**
			 * Name of the IPAM driver to use.
			 */
			Driver: v.exactOptional(v.string()),
			/**
			 * List of IPAM configuration options, specified as a map:
			 *
			 * ```
			 * {"Subnet": <CIDR>, "IPRange": <CIDR>, "Gateway": <IP address>,
			 * "AuxAddress": <device_name:IP address>}
			 * ```
			 */
			Config: v.exactOptional(
				v.array(
					v.strictObject({
						Subnet: v.exactOptional(v.string()),
						IPRange: v.exactOptional(v.string()),
						Gateway: v.exactOptional(v.string()),
						AuxiliaryAddresses: v.exactOptional(
							v.record(v.string(), v.unknown()),
						),
					}),
				),
			),
			/**
			 * Driver-specific options, specified as a map.
			 */
			Options: v.exactOptional(v.record(v.string(), v.unknown())),
		}),
	),
	/**
	 * Whether the network is created to only allow internal networking
	 * connectivity.
	 */
	Internal: v.exactOptional(v.boolean()),
	/**
	 * Whether a global / swarm scope network is manually attachable by regular
	 * containers from workers in swarm mode.
	 */
	Attachable: v.exactOptional(v.boolean()),
	/**
	 * Whether the network is providing the routing-mesh for the swarm cluster.
	 */
	Ingress: v.exactOptional(v.boolean()),
	/**
	 * The config-only network source to provide the configuration for
	 * this network.
	 */
	ConfigFrom: v.exactOptional(
		v.strictObject({
			/**
			 * The name of the config-only network that provides the network's
			 * configuration. The specified network must be an existing config-only
			 * network. Only network names are allowed, not network IDs.
			 */
			Network: v.exactOptional(v.string()),
		}),
	),
	/**
	 * Whether the network is a config-only network. Config-only networks are
	 * placeholder networks for network configurations to be used by other
	 * networks. Config-only networks cannot be used directly to run containers
	 * or services.
	 */
	ConfigOnly: v.exactOptional(v.boolean()),
	/**
	 * Contains endpoints attached to the network.
	 */
	Containers: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Network-specific options uses when creating the network.
	 */
	Options: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * List of peer nodes for an overlay network. This field is only present
	 * for overlay networks, and omitted for other network types.
	 */
	Peers: v.exactOptional(
		v.nullable(
			v.array(
				v.strictObject({
					/**
					 * ID of the peer-node in the Swarm cluster.
					 */
					Name: v.exactOptional(v.string()),
					/**
					 * IP-address of the peer-node in the Swarm cluster.
					 */
					IP: v.exactOptional(v.string()),
				}),
			),
		),
	),
});
/**
 * The config-only network source to provide the configuration for
 * this network.
 */
export const configReferenceSchema = v.strictObject({
	/**
	 * The name of the config-only network that provides the network's
	 * configuration. The specified network must be an existing config-only
	 * network. Only network names are allowed, not network IDs.
	 */
	Network: v.exactOptional(v.string()),
});
export const ipamSchema = v.strictObject({
	/**
	 * Name of the IPAM driver to use.
	 */
	Driver: v.exactOptional(v.string()),
	/**
	 * List of IPAM configuration options, specified as a map:
	 *
	 * ```
	 * {"Subnet": <CIDR>, "IPRange": <CIDR>, "Gateway": <IP address>,
	 * "AuxAddress": <device_name:IP address>}
	 * ```
	 */
	Config: v.exactOptional(
		v.array(
			v.strictObject({
				Subnet: v.exactOptional(v.string()),
				IPRange: v.exactOptional(v.string()),
				Gateway: v.exactOptional(v.string()),
				AuxiliaryAddresses: v.exactOptional(v.record(v.string(), v.unknown())),
			}),
		),
	),
	/**
	 * Driver-specific options, specified as a map.
	 */
	Options: v.exactOptional(v.record(v.string(), v.unknown())),
});
export const ipamConfigSchema = v.strictObject({
	Subnet: v.exactOptional(v.string()),
	IPRange: v.exactOptional(v.string()),
	Gateway: v.exactOptional(v.string()),
	AuxiliaryAddresses: v.exactOptional(v.record(v.string(), v.unknown())),
});
export const networkContainerSchema = v.strictObject({
	Name: v.exactOptional(v.string()),
	EndpointID: v.exactOptional(v.string()),
	MacAddress: v.exactOptional(v.string()),
	IPv4Address: v.exactOptional(v.string()),
	IPv6Address: v.exactOptional(v.string()),
});
/** PeerInfo represents one peer of an overlay network. */
export const peerInfoSchema = v.strictObject({
	/**
	 * ID of the peer-node in the Swarm cluster.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * IP-address of the peer-node in the Swarm cluster.
	 */
	IP: v.exactOptional(v.string()),
});
export const buildInfoSchema = v.strictObject({
	id: v.exactOptional(v.string()),
	stream: v.exactOptional(v.string()),
	error: v.exactOptional(v.string()),
	errorDetail: v.exactOptional(
		v.strictObject({
			code: v.exactOptional(v.pipe(v.number(), v.integer())),
			message: v.exactOptional(v.string()),
		}),
	),
	status: v.exactOptional(v.string()),
	progress: v.exactOptional(v.string()),
	progressDetail: v.exactOptional(
		v.strictObject({
			current: v.exactOptional(v.pipe(v.number(), v.integer())),
			total: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	/**
	 * Image ID or Digest
	 */
	aux: v.exactOptional(
		v.strictObject({
			ID: v.exactOptional(v.string()),
		}),
	),
});
/** BuildCache contains information about a build cache record. */
export const buildCacheSchema = v.strictObject({
	/**
	 * Unique ID of the build cache record.
	 */
	ID: v.exactOptional(v.string()),
	/**
	 * ID of the parent build cache record.
	 *
	 * > **Deprecated**: This field is deprecated, and omitted if empty.
	 */
	Parent: v.exactOptional(v.nullable(v.string())),
	/**
	 * List of parent build cache record IDs.
	 */
	Parents: v.exactOptional(v.nullable(v.array(v.string()))),
	/**
	 * Cache record type.
	 */
	Type: v.exactOptional(
		v.picklist([
			"internal",
			"frontend",
			"source.local",
			"source.git.checkout",
			"exec.cachemount",
			"regular",
		]),
	),
	/**
	 * Description of the build-step that produced the build cache.
	 */
	Description: v.exactOptional(v.string()),
	/**
	 * Indicates if the build cache is in use.
	 */
	InUse: v.exactOptional(v.boolean()),
	/**
	 * Indicates if the build cache is shared.
	 */
	Shared: v.exactOptional(v.boolean()),
	/**
	 * Amount of disk space used by the build cache (in bytes).
	 */
	Size: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Date and time at which the build cache was created in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 */
	CreatedAt: v.exactOptional(v.string()),
	/**
	 * Date and time at which the build cache was last used in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 */
	LastUsedAt: v.exactOptional(v.nullable(v.string())),
	UsageCount: v.exactOptional(v.pipe(v.number(), v.integer())),
});
/**
 * Image ID or Digest
 * @example {
 *   "ID": "sha256:85f05633ddc1c50679be2b16a0479ab6f7637f8884e0cfe0f4d20e1ebb3d6e7c"
 * }
 */
export const imageIdSchema = v.strictObject({
	ID: v.exactOptional(v.string()),
});
export const createImageInfoSchema = v.strictObject({
	id: v.exactOptional(v.string()),
	error: v.exactOptional(v.string()),
	errorDetail: v.exactOptional(
		v.strictObject({
			code: v.exactOptional(v.pipe(v.number(), v.integer())),
			message: v.exactOptional(v.string()),
		}),
	),
	status: v.exactOptional(v.string()),
	progress: v.exactOptional(v.string()),
	progressDetail: v.exactOptional(
		v.strictObject({
			current: v.exactOptional(v.pipe(v.number(), v.integer())),
			total: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
});
export const pushImageInfoSchema = v.strictObject({
	error: v.exactOptional(v.string()),
	status: v.exactOptional(v.string()),
	progress: v.exactOptional(v.string()),
	progressDetail: v.exactOptional(
		v.strictObject({
			current: v.exactOptional(v.pipe(v.number(), v.integer())),
			total: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
});
export const errorDetailSchema = v.strictObject({
	code: v.exactOptional(v.pipe(v.number(), v.integer())),
	message: v.exactOptional(v.string()),
});
export const progressDetailSchema = v.strictObject({
	current: v.exactOptional(v.pipe(v.number(), v.integer())),
	total: v.exactOptional(v.pipe(v.number(), v.integer())),
});
/**
 * Represents an error.
 * @example {
 *   "message": "Something went wrong."
 * }
 */
export const errorResponseSchema = v.strictObject({
	/**
	 * The error message.
	 */
	message: v.string(),
});
/** Response to an API call that returns just an Id */
export const idResponseSchema = v.strictObject({
	/**
	 * The id of the newly created object.
	 */
	Id: v.string(),
});
/** Configuration for a network endpoint. */
export const endpointSettingsSchema = v.strictObject({
	/**
	 * EndpointIPAMConfig represents an endpoint's IPAM configuration.
	 */
	IPAMConfig: v.exactOptional(
		v.nullable(
			v.strictObject({
				IPv4Address: v.exactOptional(v.string()),
				IPv6Address: v.exactOptional(v.string()),
				LinkLocalIPs: v.exactOptional(v.array(v.string())),
			}),
		),
	),
	Links: v.exactOptional(v.array(v.string())),
	Aliases: v.exactOptional(v.array(v.string())),
	/**
	 * Unique ID of the network.
	 */
	NetworkID: v.exactOptional(v.string()),
	/**
	 * Unique ID for the service endpoint in a Sandbox.
	 */
	EndpointID: v.exactOptional(v.string()),
	/**
	 * Gateway address for this network.
	 */
	Gateway: v.exactOptional(v.string()),
	/**
	 * IPv4 address.
	 */
	IPAddress: v.exactOptional(v.string()),
	/**
	 * Mask length of the IPv4 address.
	 */
	IPPrefixLen: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * IPv6 gateway address.
	 */
	IPv6Gateway: v.exactOptional(v.string()),
	/**
	 * Global IPv6 address.
	 */
	GlobalIPv6Address: v.exactOptional(v.string()),
	/**
	 * Mask length of the global IPv6 address.
	 */
	GlobalIPv6PrefixLen: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * MAC address for the endpoint on this network.
	 */
	MacAddress: v.exactOptional(v.string()),
	/**
	 * DriverOpts is a mapping of driver options and values. These options
	 * are passed directly to the driver and are driver specific.
	 */
	DriverOpts: v.exactOptional(v.nullable(v.record(v.string(), v.unknown()))),
});
/** EndpointIPAMConfig represents an endpoint's IPAM configuration. */
export const endpointIpamConfigSchema = v.nullable(
	v.strictObject({
		IPv4Address: v.exactOptional(v.string()),
		IPv6Address: v.exactOptional(v.string()),
		LinkLocalIPs: v.exactOptional(v.array(v.string())),
	}),
);
export const pluginMountSchema = v.strictObject({
	Name: v.string(),
	Description: v.string(),
	Settable: v.array(v.string()),
	Source: v.string(),
	Destination: v.string(),
	Type: v.string(),
	Options: v.array(v.string()),
});
export const pluginDeviceSchema = v.strictObject({
	Name: v.string(),
	Description: v.string(),
	Settable: v.array(v.string()),
	Path: v.string(),
});
export const pluginEnvSchema = v.strictObject({
	Name: v.string(),
	Description: v.string(),
	Settable: v.array(v.string()),
	Value: v.string(),
});
export const pluginInterfaceTypeSchema = v.strictObject({
	Prefix: v.string(),
	Capability: v.string(),
	Version: v.string(),
});
/**
 * Describes a permission the user has to accept upon installing
 * the plugin.
 */
export const pluginPrivilegeSchema = v.strictObject({
	Name: v.exactOptional(v.string()),
	Description: v.exactOptional(v.string()),
	Value: v.exactOptional(v.array(v.string())),
});
/** A plugin for the Engine API */
export const pluginSchema = v.strictObject({
	Id: v.exactOptional(v.string()),
	Name: v.string(),
	/**
	 * True if the plugin is running. False if the plugin is not running, only
	 * installed.
	 */
	Enabled: v.boolean(),
	/**
	 * Settings that can be modified by users.
	 */
	Settings: v.strictObject({
		Mounts: v.array(
			v.strictObject({
				Name: v.string(),
				Description: v.string(),
				Settable: v.array(v.string()),
				Source: v.string(),
				Destination: v.string(),
				Type: v.string(),
				Options: v.array(v.string()),
			}),
		),
		Env: v.array(v.string()),
		Args: v.array(v.string()),
		Devices: v.array(
			v.strictObject({
				Name: v.string(),
				Description: v.string(),
				Settable: v.array(v.string()),
				Path: v.string(),
			}),
		),
	}),
	/**
	 * plugin remote reference used to push/pull the plugin
	 */
	PluginReference: v.exactOptional(v.string()),
	/**
	 * The config of a plugin.
	 */
	Config: v.strictObject({
		/**
		 * Docker Version used to create the plugin
		 */
		DockerVersion: v.exactOptional(v.string()),
		Description: v.string(),
		Documentation: v.string(),
		/**
		 * The interface between Docker and the plugin
		 */
		Interface: v.strictObject({
			Types: v.array(
				v.strictObject({
					Prefix: v.string(),
					Capability: v.string(),
					Version: v.string(),
				}),
			),
			Socket: v.string(),
			/**
			 * Protocol to use for clients connecting to the plugin.
			 */
			ProtocolScheme: v.exactOptional(v.picklist(["", "moby.plugins.http/v1"])),
		}),
		Entrypoint: v.array(v.string()),
		WorkDir: v.string(),
		User: v.exactOptional(
			v.strictObject({
				UID: v.exactOptional(v.pipe(v.number(), v.integer())),
				GID: v.exactOptional(v.pipe(v.number(), v.integer())),
			}),
		),
		Network: v.strictObject({
			Type: v.string(),
		}),
		Linux: v.strictObject({
			Capabilities: v.array(v.string()),
			AllowAllDevices: v.boolean(),
			Devices: v.array(
				v.strictObject({
					Name: v.string(),
					Description: v.string(),
					Settable: v.array(v.string()),
					Path: v.string(),
				}),
			),
		}),
		PropagatedMount: v.string(),
		IpcHost: v.boolean(),
		PidHost: v.boolean(),
		Mounts: v.array(
			v.strictObject({
				Name: v.string(),
				Description: v.string(),
				Settable: v.array(v.string()),
				Source: v.string(),
				Destination: v.string(),
				Type: v.string(),
				Options: v.array(v.string()),
			}),
		),
		Env: v.array(
			v.strictObject({
				Name: v.string(),
				Description: v.string(),
				Settable: v.array(v.string()),
				Value: v.string(),
			}),
		),
		Args: v.strictObject({
			Name: v.string(),
			Description: v.string(),
			Settable: v.array(v.string()),
			Value: v.array(v.string()),
		}),
		rootfs: v.exactOptional(
			v.strictObject({
				type: v.exactOptional(v.string()),
				diff_ids: v.exactOptional(v.array(v.string())),
			}),
		),
	}),
});
/**
 * The version number of the object such as node, service, etc. This is needed
 * to avoid conflicting writes. The client must send the version number along
 * with the modified specification when updating these objects.
 *
 * This approach ensures safe concurrency and determinism in that the change
 * on the object may not be applied if the version number has changed from the
 * last read. In other words, if two update requests specify the same base
 * version, only one of the requests can succeed. As a result, two separate
 * update requests that happen at the same time will not unintentionally
 * overwrite each other.
 */
export const objectVersionSchema = v.strictObject({
	Index: v.exactOptional(v.pipe(v.number(), v.integer())),
});
export const nodeSpecSchema = v.strictObject({
	/**
	 * Name for the node.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Role of the node.
	 */
	Role: v.exactOptional(v.picklist(["worker", "manager"])),
	/**
	 * Availability of the node.
	 */
	Availability: v.exactOptional(v.picklist(["active", "pause", "drain"])),
});
export const nodeSchema = v.strictObject({
	ID: v.exactOptional(v.string()),
	/**
	 * The version number of the object such as node, service, etc. This is needed
	 * to avoid conflicting writes. The client must send the version number along
	 * with the modified specification when updating these objects.
	 *
	 * This approach ensures safe concurrency and determinism in that the change
	 * on the object may not be applied if the version number has changed from the
	 * last read. In other words, if two update requests specify the same base
	 * version, only one of the requests can succeed. As a result, two separate
	 * update requests that happen at the same time will not unintentionally
	 * overwrite each other.
	 */
	Version: v.exactOptional(
		v.strictObject({
			Index: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	/**
	 * Date and time at which the node was added to the swarm in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 */
	CreatedAt: v.exactOptional(v.string()),
	/**
	 * Date and time at which the node was last updated in
	 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
	 */
	UpdatedAt: v.exactOptional(v.string()),
	Spec: v.exactOptional(
		v.strictObject({
			/**
			 * Name for the node.
			 */
			Name: v.exactOptional(v.string()),
			/**
			 * User-defined key/value metadata.
			 */
			Labels: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * Role of the node.
			 */
			Role: v.exactOptional(v.picklist(["worker", "manager"])),
			/**
			 * Availability of the node.
			 */
			Availability: v.exactOptional(v.picklist(["active", "pause", "drain"])),
		}),
	),
	/**
	 * NodeDescription encapsulates the properties of the Node as reported by the
	 * agent.
	 */
	Description: v.exactOptional(
		v.strictObject({
			Hostname: v.exactOptional(v.string()),
			/**
			 * Platform represents the platform (Arch/OS).
			 */
			Platform: v.exactOptional(
				v.strictObject({
					/**
					 * Architecture represents the hardware architecture (for example,
					 * `x86_64`).
					 */
					Architecture: v.exactOptional(v.string()),
					/**
					 * OS represents the Operating System (for example, `linux` or `windows`).
					 */
					OS: v.exactOptional(v.string()),
				}),
			),
			/**
			 * An object describing the resources which can be advertised by a node and
			 * requested by a task.
			 */
			Resources: v.exactOptional(
				v.strictObject({
					NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
					MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
					 * String resources (e.g, `GPU=UUID1`).
					 */
					GenericResources: v.exactOptional(
						v.array(
							v.strictObject({
								NamedResourceSpec: v.exactOptional(
									v.strictObject({
										Kind: v.exactOptional(v.string()),
										Value: v.exactOptional(v.string()),
									}),
								),
								DiscreteResourceSpec: v.exactOptional(
									v.strictObject({
										Kind: v.exactOptional(v.string()),
										Value: v.exactOptional(v.pipe(v.number(), v.integer())),
									}),
								),
							}),
						),
					),
				}),
			),
			/**
			 * EngineDescription provides information about an engine.
			 */
			Engine: v.exactOptional(
				v.strictObject({
					EngineVersion: v.exactOptional(v.string()),
					Labels: v.exactOptional(v.record(v.string(), v.unknown())),
					Plugins: v.exactOptional(
						v.array(
							v.strictObject({
								Type: v.exactOptional(v.string()),
								Name: v.exactOptional(v.string()),
							}),
						),
					),
				}),
			),
			/**
			 * Information about the issuer of leaf TLS certificates and the trusted root
			 * CA certificate.
			 */
			TLSInfo: v.exactOptional(
				v.strictObject({
					/**
					 * The root CA certificate(s) that are used to validate leaf TLS
					 * certificates.
					 */
					TrustRoot: v.exactOptional(v.string()),
					/**
					 * The base64-url-safe-encoded raw subject bytes of the issuer.
					 */
					CertIssuerSubject: v.exactOptional(v.string()),
					/**
					 * The base64-url-safe-encoded raw public key bytes of the issuer.
					 */
					CertIssuerPublicKey: v.exactOptional(v.string()),
				}),
			),
		}),
	),
	/**
	 * NodeStatus represents the status of a node.
	 *
	 * It provides the current status of the node, as seen by the manager.
	 */
	Status: v.exactOptional(
		v.strictObject({
			/**
			 * NodeState represents the state of a node.
			 */
			State: v.exactOptional(
				v.picklist(["unknown", "down", "ready", "disconnected"]),
			),
			Message: v.exactOptional(v.string()),
			/**
			 * IP address of the node.
			 */
			Addr: v.exactOptional(v.string()),
		}),
	),
	/**
	 * ManagerStatus represents the status of a manager.
	 *
	 * It provides the current status of a node's manager component, if the node
	 * is a manager.
	 */
	ManagerStatus: v.exactOptional(
		v.nullable(
			v.strictObject({
				Leader: v.exactOptional(v.boolean()),
				/**
				 * Reachability represents the reachability of a node.
				 */
				Reachability: v.exactOptional(
					v.picklist(["unknown", "unreachable", "reachable"]),
				),
				/**
				 * The IP address and port at which the manager is reachable.
				 */
				Addr: v.exactOptional(v.string()),
			}),
		),
	),
});
/**
 * NodeDescription encapsulates the properties of the Node as reported by the
 * agent.
 */
export const nodeDescriptionSchema = v.strictObject({
	Hostname: v.exactOptional(v.string()),
	/**
	 * Platform represents the platform (Arch/OS).
	 */
	Platform: v.exactOptional(
		v.strictObject({
			/**
			 * Architecture represents the hardware architecture (for example,
			 * `x86_64`).
			 */
			Architecture: v.exactOptional(v.string()),
			/**
			 * OS represents the Operating System (for example, `linux` or `windows`).
			 */
			OS: v.exactOptional(v.string()),
		}),
	),
	/**
	 * An object describing the resources which can be advertised by a node and
	 * requested by a task.
	 */
	Resources: v.exactOptional(
		v.strictObject({
			NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
			MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
			 * String resources (e.g, `GPU=UUID1`).
			 */
			GenericResources: v.exactOptional(
				v.array(
					v.strictObject({
						NamedResourceSpec: v.exactOptional(
							v.strictObject({
								Kind: v.exactOptional(v.string()),
								Value: v.exactOptional(v.string()),
							}),
						),
						DiscreteResourceSpec: v.exactOptional(
							v.strictObject({
								Kind: v.exactOptional(v.string()),
								Value: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
					}),
				),
			),
		}),
	),
	/**
	 * EngineDescription provides information about an engine.
	 */
	Engine: v.exactOptional(
		v.strictObject({
			EngineVersion: v.exactOptional(v.string()),
			Labels: v.exactOptional(v.record(v.string(), v.unknown())),
			Plugins: v.exactOptional(
				v.array(
					v.strictObject({
						Type: v.exactOptional(v.string()),
						Name: v.exactOptional(v.string()),
					}),
				),
			),
		}),
	),
	/**
	 * Information about the issuer of leaf TLS certificates and the trusted root
	 * CA certificate.
	 */
	TLSInfo: v.exactOptional(
		v.strictObject({
			/**
			 * The root CA certificate(s) that are used to validate leaf TLS
			 * certificates.
			 */
			TrustRoot: v.exactOptional(v.string()),
			/**
			 * The base64-url-safe-encoded raw subject bytes of the issuer.
			 */
			CertIssuerSubject: v.exactOptional(v.string()),
			/**
			 * The base64-url-safe-encoded raw public key bytes of the issuer.
			 */
			CertIssuerPublicKey: v.exactOptional(v.string()),
		}),
	),
});
/** Platform represents the platform (Arch/OS). */
export const platformSchema = v.strictObject({
	/**
	 * Architecture represents the hardware architecture (for example,
	 * `x86_64`).
	 */
	Architecture: v.exactOptional(v.string()),
	/**
	 * OS represents the Operating System (for example, `linux` or `windows`).
	 */
	OS: v.exactOptional(v.string()),
});
/** EngineDescription provides information about an engine. */
export const engineDescriptionSchema = v.strictObject({
	EngineVersion: v.exactOptional(v.string()),
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	Plugins: v.exactOptional(
		v.array(
			v.strictObject({
				Type: v.exactOptional(v.string()),
				Name: v.exactOptional(v.string()),
			}),
		),
	),
});
/**
 * Information about the issuer of leaf TLS certificates and the trusted root
 * CA certificate.
 * @example {
 *   "TrustRoot": "-----BEGIN CERTIFICATE-----\\npxv6zFeVEkAEEkqIYi0omA9+CjanB/6Bz4n1uw8H\n-----END CERTIFICATE-----\n",
 *   "CertIssuerSubject": "MBMxETAPBgNVBAMTCHN3YXJtLWNh",
 *   "CertIssuerPublicKey": "MFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcDQgAEmT9XIw9h1qoNclv9VeHmf/Vi6/uI2vFXdBveXTpcPjqx6i9wNazchk1XWV/dKTKvSh9xyGKmiIeRcE4OiMnJ1A=="
 * }
 */
export const tlsInfoSchema = v.strictObject({
	/**
	 * The root CA certificate(s) that are used to validate leaf TLS
	 * certificates.
	 */
	TrustRoot: v.exactOptional(v.string()),
	/**
	 * The base64-url-safe-encoded raw subject bytes of the issuer.
	 */
	CertIssuerSubject: v.exactOptional(v.string()),
	/**
	 * The base64-url-safe-encoded raw public key bytes of the issuer.
	 */
	CertIssuerPublicKey: v.exactOptional(v.string()),
});
/**
 * NodeStatus represents the status of a node.
 *
 * It provides the current status of the node, as seen by the manager.
 */
export const nodeStatusSchema = v.strictObject({
	/**
	 * NodeState represents the state of a node.
	 */
	State: v.exactOptional(
		v.picklist(["unknown", "down", "ready", "disconnected"]),
	),
	Message: v.exactOptional(v.string()),
	/**
	 * IP address of the node.
	 */
	Addr: v.exactOptional(v.string()),
});
/**
 * NodeState represents the state of a node.
 * @example "ready"
 */
export const nodeStateSchema = v.picklist([
	"unknown",
	"down",
	"ready",
	"disconnected",
]);
/**
 * ManagerStatus represents the status of a manager.
 *
 * It provides the current status of a node's manager component, if the node
 * is a manager.
 */
export const managerStatusSchema = v.nullable(
	v.strictObject({
		Leader: v.exactOptional(v.boolean()),
		/**
		 * Reachability represents the reachability of a node.
		 */
		Reachability: v.exactOptional(
			v.picklist(["unknown", "unreachable", "reachable"]),
		),
		/**
		 * The IP address and port at which the manager is reachable.
		 */
		Addr: v.exactOptional(v.string()),
	}),
);
/**
 * Reachability represents the reachability of a node.
 * @example "reachable"
 */
export const reachabilitySchema = v.picklist([
	"unknown",
	"unreachable",
	"reachable",
]);
/** User modifiable swarm configuration. */
export const swarmSpecSchema = v.strictObject({
	/**
	 * Name of the swarm.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Orchestration configuration.
	 */
	Orchestration: v.exactOptional(
		v.nullable(
			v.strictObject({
				/**
				 * The number of historic tasks to keep per instance or node. If
				 * negative, never remove completed or failed tasks.
				 */
				TaskHistoryRetentionLimit: v.exactOptional(
					v.pipe(v.number(), v.integer()),
				),
			}),
		),
	),
	/**
	 * Raft configuration.
	 */
	Raft: v.exactOptional(
		v.strictObject({
			/**
			 * The number of log entries between snapshots.
			 */
			SnapshotInterval: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The number of snapshots to keep beyond the current snapshot.
			 */
			KeepOldSnapshots: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The number of log entries to keep around to sync up slow followers
			 * after a snapshot is created.
			 */
			LogEntriesForSlowFollowers: v.exactOptional(
				v.pipe(v.number(), v.integer()),
			),
			/**
			 * The number of ticks that a follower will wait for a message from
			 * the leader before becoming a candidate and starting an election.
			 * `ElectionTick` must be greater than `HeartbeatTick`.
			 *
			 * A tick currently defaults to one second, so these translate
			 * directly to seconds currently, but this is NOT guaranteed.
			 */
			ElectionTick: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
			 * the leader will send a heartbeat to the followers.
			 *
			 * A tick currently defaults to one second, so these translate
			 * directly to seconds currently, but this is NOT guaranteed.
			 */
			HeartbeatTick: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	/**
	 * Dispatcher configuration.
	 */
	Dispatcher: v.exactOptional(
		v.nullable(
			v.strictObject({
				/**
				 * The delay for an agent to send a heartbeat to the dispatcher.
				 */
				HeartbeatPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
			}),
		),
	),
	/**
	 * CA configuration.
	 */
	CAConfig: v.exactOptional(
		v.nullable(
			v.strictObject({
				/**
				 * The duration node certificates are issued for.
				 */
				NodeCertExpiry: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Configuration for forwarding signing requests to an external
				 * certificate authority.
				 */
				ExternalCAs: v.exactOptional(
					v.array(
						v.strictObject({
							/**
							 * Protocol for communication with the external CA (currently
							 * only `cfssl` is supported).
							 */
							Protocol: v.exactOptional(v.picklist(["cfssl"])),
							/**
							 * URL where certificate signing requests should be sent.
							 */
							URL: v.exactOptional(v.string()),
							/**
							 * An object with key/value pairs that are interpreted as
							 * protocol-specific options for the external CA driver.
							 */
							Options: v.exactOptional(v.record(v.string(), v.unknown())),
							/**
							 * The root CA certificate (in PEM format) this external CA uses
							 * to issue TLS certificates (assumed to be to the current swarm
							 * root CA certificate if not provided).
							 */
							CACert: v.exactOptional(v.string()),
						}),
					),
				),
				/**
				 * The desired signing CA certificate for all swarm node TLS leaf
				 * certificates, in PEM format.
				 */
				SigningCACert: v.exactOptional(v.string()),
				/**
				 * The desired signing CA key for all swarm node TLS leaf certificates,
				 * in PEM format.
				 */
				SigningCAKey: v.exactOptional(v.string()),
				/**
				 * An integer whose purpose is to force swarm to generate a new
				 * signing CA certificate and key, if none have been specified in
				 * `SigningCACert` and `SigningCAKey`
				 */
				ForceRotate: v.exactOptional(v.pipe(v.number(), v.integer())),
			}),
		),
	),
	/**
	 * Parameters related to encryption-at-rest.
	 */
	EncryptionConfig: v.exactOptional(
		v.strictObject({
			/**
			 * If set, generate a key and use it to lock data stored on the
			 * managers.
			 */
			AutoLockManagers: v.exactOptional(v.boolean()),
		}),
	),
	/**
	 * Defaults for creating tasks in this cluster.
	 */
	TaskDefaults: v.exactOptional(
		v.strictObject({
			/**
			 * The log driver to use for tasks created in the orchestrator if
			 * unspecified by a service.
			 *
			 * Updating this value only affects new tasks. Existing tasks continue
			 * to use their previously configured log driver until recreated.
			 */
			LogDriver: v.exactOptional(
				v.strictObject({
					/**
					 * The log driver to use as a default for new tasks.
					 */
					Name: v.exactOptional(v.string()),
					/**
					 * Driver-specific options for the selected log driver, specified
					 * as key/value pairs.
					 */
					Options: v.exactOptional(v.record(v.string(), v.unknown())),
				}),
			),
		}),
	),
});
/**
 * ClusterInfo represents information about the swarm as is returned by the
 * "/info" endpoint. Join-tokens are not included.
 */
export const clusterInfoSchema = v.nullable(
	v.strictObject({
		/**
		 * The ID of the swarm.
		 */
		ID: v.exactOptional(v.string()),
		/**
		 * The version number of the object such as node, service, etc. This is needed
		 * to avoid conflicting writes. The client must send the version number along
		 * with the modified specification when updating these objects.
		 *
		 * This approach ensures safe concurrency and determinism in that the change
		 * on the object may not be applied if the version number has changed from the
		 * last read. In other words, if two update requests specify the same base
		 * version, only one of the requests can succeed. As a result, two separate
		 * update requests that happen at the same time will not unintentionally
		 * overwrite each other.
		 */
		Version: v.exactOptional(
			v.strictObject({
				Index: v.exactOptional(v.pipe(v.number(), v.integer())),
			}),
		),
		/**
		 * Date and time at which the swarm was initialised in
		 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
		 */
		CreatedAt: v.exactOptional(v.string()),
		/**
		 * Date and time at which the swarm was last updated in
		 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
		 */
		UpdatedAt: v.exactOptional(v.string()),
		/**
		 * User modifiable swarm configuration.
		 */
		Spec: v.exactOptional(
			v.strictObject({
				/**
				 * Name of the swarm.
				 */
				Name: v.exactOptional(v.string()),
				/**
				 * User-defined key/value metadata.
				 */
				Labels: v.exactOptional(v.record(v.string(), v.unknown())),
				/**
				 * Orchestration configuration.
				 */
				Orchestration: v.exactOptional(
					v.nullable(
						v.strictObject({
							/**
							 * The number of historic tasks to keep per instance or node. If
							 * negative, never remove completed or failed tasks.
							 */
							TaskHistoryRetentionLimit: v.exactOptional(
								v.pipe(v.number(), v.integer()),
							),
						}),
					),
				),
				/**
				 * Raft configuration.
				 */
				Raft: v.exactOptional(
					v.strictObject({
						/**
						 * The number of log entries between snapshots.
						 */
						SnapshotInterval: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * The number of snapshots to keep beyond the current snapshot.
						 */
						KeepOldSnapshots: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * The number of log entries to keep around to sync up slow followers
						 * after a snapshot is created.
						 */
						LogEntriesForSlowFollowers: v.exactOptional(
							v.pipe(v.number(), v.integer()),
						),
						/**
						 * The number of ticks that a follower will wait for a message from
						 * the leader before becoming a candidate and starting an election.
						 * `ElectionTick` must be greater than `HeartbeatTick`.
						 *
						 * A tick currently defaults to one second, so these translate
						 * directly to seconds currently, but this is NOT guaranteed.
						 */
						ElectionTick: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
						 * the leader will send a heartbeat to the followers.
						 *
						 * A tick currently defaults to one second, so these translate
						 * directly to seconds currently, but this is NOT guaranteed.
						 */
						HeartbeatTick: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
				/**
				 * Dispatcher configuration.
				 */
				Dispatcher: v.exactOptional(
					v.nullable(
						v.strictObject({
							/**
							 * The delay for an agent to send a heartbeat to the dispatcher.
							 */
							HeartbeatPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
				),
				/**
				 * CA configuration.
				 */
				CAConfig: v.exactOptional(
					v.nullable(
						v.strictObject({
							/**
							 * The duration node certificates are issued for.
							 */
							NodeCertExpiry: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * Configuration for forwarding signing requests to an external
							 * certificate authority.
							 */
							ExternalCAs: v.exactOptional(
								v.array(
									v.strictObject({
										/**
										 * Protocol for communication with the external CA (currently
										 * only `cfssl` is supported).
										 */
										Protocol: v.exactOptional(v.picklist(["cfssl"])),
										/**
										 * URL where certificate signing requests should be sent.
										 */
										URL: v.exactOptional(v.string()),
										/**
										 * An object with key/value pairs that are interpreted as
										 * protocol-specific options for the external CA driver.
										 */
										Options: v.exactOptional(v.record(v.string(), v.unknown())),
										/**
										 * The root CA certificate (in PEM format) this external CA uses
										 * to issue TLS certificates (assumed to be to the current swarm
										 * root CA certificate if not provided).
										 */
										CACert: v.exactOptional(v.string()),
									}),
								),
							),
							/**
							 * The desired signing CA certificate for all swarm node TLS leaf
							 * certificates, in PEM format.
							 */
							SigningCACert: v.exactOptional(v.string()),
							/**
							 * The desired signing CA key for all swarm node TLS leaf certificates,
							 * in PEM format.
							 */
							SigningCAKey: v.exactOptional(v.string()),
							/**
							 * An integer whose purpose is to force swarm to generate a new
							 * signing CA certificate and key, if none have been specified in
							 * `SigningCACert` and `SigningCAKey`
							 */
							ForceRotate: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
				),
				/**
				 * Parameters related to encryption-at-rest.
				 */
				EncryptionConfig: v.exactOptional(
					v.strictObject({
						/**
						 * If set, generate a key and use it to lock data stored on the
						 * managers.
						 */
						AutoLockManagers: v.exactOptional(v.boolean()),
					}),
				),
				/**
				 * Defaults for creating tasks in this cluster.
				 */
				TaskDefaults: v.exactOptional(
					v.strictObject({
						/**
						 * The log driver to use for tasks created in the orchestrator if
						 * unspecified by a service.
						 *
						 * Updating this value only affects new tasks. Existing tasks continue
						 * to use their previously configured log driver until recreated.
						 */
						LogDriver: v.exactOptional(
							v.strictObject({
								/**
								 * The log driver to use as a default for new tasks.
								 */
								Name: v.exactOptional(v.string()),
								/**
								 * Driver-specific options for the selected log driver, specified
								 * as key/value pairs.
								 */
								Options: v.exactOptional(v.record(v.string(), v.unknown())),
							}),
						),
					}),
				),
			}),
		),
		/**
		 * Information about the issuer of leaf TLS certificates and the trusted root
		 * CA certificate.
		 */
		TLSInfo: v.exactOptional(
			v.strictObject({
				/**
				 * The root CA certificate(s) that are used to validate leaf TLS
				 * certificates.
				 */
				TrustRoot: v.exactOptional(v.string()),
				/**
				 * The base64-url-safe-encoded raw subject bytes of the issuer.
				 */
				CertIssuerSubject: v.exactOptional(v.string()),
				/**
				 * The base64-url-safe-encoded raw public key bytes of the issuer.
				 */
				CertIssuerPublicKey: v.exactOptional(v.string()),
			}),
		),
		/**
		 * Whether there is currently a root CA rotation in progress for the swarm
		 */
		RootRotationInProgress: v.exactOptional(v.boolean()),
		/**
		 * DataPathPort specifies the data path port number for data traffic.
		 * Acceptable port range is 1024 to 49151.
		 * If no port is set or is set to 0, the default port (4789) is used.
		 */
		DataPathPort: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Default Address Pool specifies default subnet pools for global scope
		 * networks.
		 */
		DefaultAddrPool: v.exactOptional(v.array(v.string())),
		/**
		 * SubnetSize specifies the subnet size of the networks created from the
		 * default subnet pool.
		 */
		SubnetSize: v.exactOptional(
			v.pipe(v.number(), v.integer(), v.maxValue(29)),
		),
	}),
);
/** JoinTokens contains the tokens workers and managers need to join the swarm. */
export const joinTokensSchema = v.strictObject({
	/**
	 * The token workers can use to join the swarm.
	 */
	Worker: v.exactOptional(v.string()),
	/**
	 * The token managers can use to join the swarm.
	 */
	Manager: v.exactOptional(v.string()),
});
export const swarmSchema = v.intersect([
	v.nullable(
		v.strictObject({
			/**
			 * The ID of the swarm.
			 */
			ID: v.exactOptional(v.string()),
			/**
			 * The version number of the object such as node, service, etc. This is needed
			 * to avoid conflicting writes. The client must send the version number along
			 * with the modified specification when updating these objects.
			 *
			 * This approach ensures safe concurrency and determinism in that the change
			 * on the object may not be applied if the version number has changed from the
			 * last read. In other words, if two update requests specify the same base
			 * version, only one of the requests can succeed. As a result, two separate
			 * update requests that happen at the same time will not unintentionally
			 * overwrite each other.
			 */
			Version: v.exactOptional(
				v.strictObject({
					Index: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
			/**
			 * Date and time at which the swarm was initialised in
			 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
			 */
			CreatedAt: v.exactOptional(v.string()),
			/**
			 * Date and time at which the swarm was last updated in
			 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
			 */
			UpdatedAt: v.exactOptional(v.string()),
			/**
			 * User modifiable swarm configuration.
			 */
			Spec: v.exactOptional(
				v.strictObject({
					/**
					 * Name of the swarm.
					 */
					Name: v.exactOptional(v.string()),
					/**
					 * User-defined key/value metadata.
					 */
					Labels: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * Orchestration configuration.
					 */
					Orchestration: v.exactOptional(
						v.nullable(
							v.strictObject({
								/**
								 * The number of historic tasks to keep per instance or node. If
								 * negative, never remove completed or failed tasks.
								 */
								TaskHistoryRetentionLimit: v.exactOptional(
									v.pipe(v.number(), v.integer()),
								),
							}),
						),
					),
					/**
					 * Raft configuration.
					 */
					Raft: v.exactOptional(
						v.strictObject({
							/**
							 * The number of log entries between snapshots.
							 */
							SnapshotInterval: v.exactOptional(
								v.pipe(v.number(), v.integer()),
							),
							/**
							 * The number of snapshots to keep beyond the current snapshot.
							 */
							KeepOldSnapshots: v.exactOptional(
								v.pipe(v.number(), v.integer()),
							),
							/**
							 * The number of log entries to keep around to sync up slow followers
							 * after a snapshot is created.
							 */
							LogEntriesForSlowFollowers: v.exactOptional(
								v.pipe(v.number(), v.integer()),
							),
							/**
							 * The number of ticks that a follower will wait for a message from
							 * the leader before becoming a candidate and starting an election.
							 * `ElectionTick` must be greater than `HeartbeatTick`.
							 *
							 * A tick currently defaults to one second, so these translate
							 * directly to seconds currently, but this is NOT guaranteed.
							 */
							ElectionTick: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
							 * the leader will send a heartbeat to the followers.
							 *
							 * A tick currently defaults to one second, so these translate
							 * directly to seconds currently, but this is NOT guaranteed.
							 */
							HeartbeatTick: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
					/**
					 * Dispatcher configuration.
					 */
					Dispatcher: v.exactOptional(
						v.nullable(
							v.strictObject({
								/**
								 * The delay for an agent to send a heartbeat to the dispatcher.
								 */
								HeartbeatPeriod: v.exactOptional(
									v.pipe(v.number(), v.integer()),
								),
							}),
						),
					),
					/**
					 * CA configuration.
					 */
					CAConfig: v.exactOptional(
						v.nullable(
							v.strictObject({
								/**
								 * The duration node certificates are issued for.
								 */
								NodeCertExpiry: v.exactOptional(
									v.pipe(v.number(), v.integer()),
								),
								/**
								 * Configuration for forwarding signing requests to an external
								 * certificate authority.
								 */
								ExternalCAs: v.exactOptional(
									v.array(
										v.strictObject({
											/**
											 * Protocol for communication with the external CA (currently
											 * only `cfssl` is supported).
											 */
											Protocol: v.exactOptional(v.picklist(["cfssl"])),
											/**
											 * URL where certificate signing requests should be sent.
											 */
											URL: v.exactOptional(v.string()),
											/**
											 * An object with key/value pairs that are interpreted as
											 * protocol-specific options for the external CA driver.
											 */
											Options: v.exactOptional(
												v.record(v.string(), v.unknown()),
											),
											/**
											 * The root CA certificate (in PEM format) this external CA uses
											 * to issue TLS certificates (assumed to be to the current swarm
											 * root CA certificate if not provided).
											 */
											CACert: v.exactOptional(v.string()),
										}),
									),
								),
								/**
								 * The desired signing CA certificate for all swarm node TLS leaf
								 * certificates, in PEM format.
								 */
								SigningCACert: v.exactOptional(v.string()),
								/**
								 * The desired signing CA key for all swarm node TLS leaf certificates,
								 * in PEM format.
								 */
								SigningCAKey: v.exactOptional(v.string()),
								/**
								 * An integer whose purpose is to force swarm to generate a new
								 * signing CA certificate and key, if none have been specified in
								 * `SigningCACert` and `SigningCAKey`
								 */
								ForceRotate: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
					),
					/**
					 * Parameters related to encryption-at-rest.
					 */
					EncryptionConfig: v.exactOptional(
						v.strictObject({
							/**
							 * If set, generate a key and use it to lock data stored on the
							 * managers.
							 */
							AutoLockManagers: v.exactOptional(v.boolean()),
						}),
					),
					/**
					 * Defaults for creating tasks in this cluster.
					 */
					TaskDefaults: v.exactOptional(
						v.strictObject({
							/**
							 * The log driver to use for tasks created in the orchestrator if
							 * unspecified by a service.
							 *
							 * Updating this value only affects new tasks. Existing tasks continue
							 * to use their previously configured log driver until recreated.
							 */
							LogDriver: v.exactOptional(
								v.strictObject({
									/**
									 * The log driver to use as a default for new tasks.
									 */
									Name: v.exactOptional(v.string()),
									/**
									 * Driver-specific options for the selected log driver, specified
									 * as key/value pairs.
									 */
									Options: v.exactOptional(v.record(v.string(), v.unknown())),
								}),
							),
						}),
					),
				}),
			),
			/**
			 * Information about the issuer of leaf TLS certificates and the trusted root
			 * CA certificate.
			 */
			TLSInfo: v.exactOptional(
				v.strictObject({
					/**
					 * The root CA certificate(s) that are used to validate leaf TLS
					 * certificates.
					 */
					TrustRoot: v.exactOptional(v.string()),
					/**
					 * The base64-url-safe-encoded raw subject bytes of the issuer.
					 */
					CertIssuerSubject: v.exactOptional(v.string()),
					/**
					 * The base64-url-safe-encoded raw public key bytes of the issuer.
					 */
					CertIssuerPublicKey: v.exactOptional(v.string()),
				}),
			),
			/**
			 * Whether there is currently a root CA rotation in progress for the swarm
			 */
			RootRotationInProgress: v.exactOptional(v.boolean()),
			/**
			 * DataPathPort specifies the data path port number for data traffic.
			 * Acceptable port range is 1024 to 49151.
			 * If no port is set or is set to 0, the default port (4789) is used.
			 */
			DataPathPort: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * Default Address Pool specifies default subnet pools for global scope
			 * networks.
			 */
			DefaultAddrPool: v.exactOptional(v.array(v.string())),
			/**
			 * SubnetSize specifies the subnet size of the networks created from the
			 * default subnet pool.
			 */
			SubnetSize: v.exactOptional(
				v.pipe(v.number(), v.integer(), v.maxValue(29)),
			),
		}),
	),
	v.strictObject({
		/**
		 * JoinTokens contains the tokens workers and managers need to join the swarm.
		 */
		JoinTokens: v.exactOptional(
			v.strictObject({
				/**
				 * The token workers can use to join the swarm.
				 */
				Worker: v.exactOptional(v.string()),
				/**
				 * The token managers can use to join the swarm.
				 */
				Manager: v.exactOptional(v.string()),
			}),
		),
	}),
]);
/** User modifiable task configuration. */
export const taskSpecSchema = v.strictObject({
	/**
	 * Plugin spec for the service.  *(Experimental release only.)*
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
	 * > mutually exclusive. PluginSpec is only used when the Runtime field
	 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
	 * > field is set to `attachment`.
	 */
	PluginSpec: v.exactOptional(
		v.strictObject({
			/**
			 * The name or 'alias' to use for the plugin.
			 */
			Name: v.exactOptional(v.string()),
			/**
			 * The plugin image reference to use.
			 */
			Remote: v.exactOptional(v.string()),
			/**
			 * Disable the plugin once scheduled.
			 */
			Disabled: v.exactOptional(v.boolean()),
			PluginPrivilege: v.exactOptional(
				v.array(
					v.strictObject({
						Name: v.exactOptional(v.string()),
						Description: v.exactOptional(v.string()),
						Value: v.exactOptional(v.array(v.string())),
					}),
				),
			),
		}),
	),
	/**
	 * Container spec for the service.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
	 * > mutually exclusive. PluginSpec is only used when the Runtime field
	 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
	 * > field is set to `attachment`.
	 */
	ContainerSpec: v.exactOptional(
		v.strictObject({
			/**
			 * The image name to use for the container
			 */
			Image: v.exactOptional(v.string()),
			/**
			 * User-defined key/value data.
			 */
			Labels: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * The command to be run in the image.
			 */
			Command: v.exactOptional(v.array(v.string())),
			/**
			 * Arguments to the command.
			 */
			Args: v.exactOptional(v.array(v.string())),
			/**
			 * The hostname to use for the container, as a valid
			 * [RFC 1123](https://tools.ietf.org/html/rfc1123) hostname.
			 */
			Hostname: v.exactOptional(v.string()),
			/**
			 * A list of environment variables in the form `VAR=value`.
			 */
			Env: v.exactOptional(v.array(v.string())),
			/**
			 * The working directory for commands to run in.
			 */
			Dir: v.exactOptional(v.string()),
			/**
			 * The user inside the container.
			 */
			User: v.exactOptional(v.string()),
			/**
			 * A list of additional groups that the container process will run as.
			 */
			Groups: v.exactOptional(v.array(v.string())),
			/**
			 * Security options for the container
			 */
			Privileges: v.exactOptional(
				v.strictObject({
					/**
					 * CredentialSpec for managed service account (Windows only)
					 */
					CredentialSpec: v.exactOptional(
						v.strictObject({
							/**
							 * Load credential spec from a Swarm Config with the given ID.
							 * The specified config must also be present in the Configs
							 * field with the Runtime property set.
							 *
							 * <p><br /></p>
							 *
							 *
							 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
							 * > and `CredentialSpec.Config` are mutually exclusive.
							 */
							Config: v.exactOptional(v.string()),
							/**
							 * Load credential spec from this file. The file is read by
							 * the daemon, and must be present in the `CredentialSpecs`
							 * subdirectory in the docker data directory, which defaults
							 * to `C:\ProgramData\Docker\` on Windows.
							 *
							 * For example, specifying `spec.json` loads
							 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
							 *
							 * <p><br /></p>
							 *
							 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
							 * > and `CredentialSpec.Config` are mutually exclusive.
							 */
							File: v.exactOptional(v.string()),
							/**
							 * Load credential spec from this value in the Windows
							 * registry. The specified registry value must be located in:
							 *
							 * `HKLM\SOFTWARE\Microsoft\Windows
							 * NT\CurrentVersion\Virtualization\Containers\CredentialSpecs`
							 *
							 * <p><br /></p>
							 *
							 *
							 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
							 * > and `CredentialSpec.Config` are mutually exclusive.
							 */
							Registry: v.exactOptional(v.string()),
						}),
					),
					/**
					 * SELinux labels of the container
					 */
					SELinuxContext: v.exactOptional(
						v.strictObject({
							/**
							 * Disable SELinux
							 */
							Disable: v.exactOptional(v.boolean()),
							/**
							 * SELinux user label
							 */
							User: v.exactOptional(v.string()),
							/**
							 * SELinux role label
							 */
							Role: v.exactOptional(v.string()),
							/**
							 * SELinux type label
							 */
							Type: v.exactOptional(v.string()),
							/**
							 * SELinux level label
							 */
							Level: v.exactOptional(v.string()),
						}),
					),
				}),
			),
			/**
			 * Whether a pseudo-TTY should be allocated.
			 */
			TTY: v.exactOptional(v.boolean()),
			/**
			 * Open `stdin`
			 */
			OpenStdin: v.exactOptional(v.boolean()),
			/**
			 * Mount the container's root filesystem as read only.
			 */
			ReadOnly: v.exactOptional(v.boolean()),
			/**
			 * Specification for mounts to be added to containers created as part
			 * of the service.
			 */
			Mounts: v.exactOptional(
				v.array(
					v.strictObject({
						/**
						 * Container path.
						 */
						Target: v.exactOptional(v.string()),
						/**
						 * Mount source (e.g. a volume name, a host path).
						 */
						Source: v.exactOptional(v.string()),
						/**
						 * The mount type. Available types:
						 *
						 * - `bind` Mounts a file or directory from the host into the container. Must
						 * exist prior to creating the container.
						 * - `volume` Creates a volume with the given name and options (or uses a
						 * pre-existing volume with the same name and options). These are **not**
						 * removed when the container is removed.
						 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
						 * specified for tmpfs.
						 * - `npipe` Mounts a named pipe from the host into the container. Must exist
						 * prior to creating the container.
						 * - `cluster` a Swarm cluster volume
						 */
						Type: v.exactOptional(
							v.picklist(["bind", "volume", "tmpfs", "npipe", "cluster"]),
						),
						/**
						 * Whether the mount should be read-only.
						 */
						ReadOnly: v.exactOptional(v.boolean()),
						/**
						 * The consistency requirement for the mount: `default`, `consistent`,
						 * `cached`, or `delegated`.
						 */
						Consistency: v.exactOptional(v.string()),
						/**
						 * Optional configuration for the `bind` type.
						 */
						BindOptions: v.exactOptional(
							v.strictObject({
								/**
								 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
								 */
								Propagation: v.exactOptional(
									v.picklist([
										"private",
										"rprivate",
										"shared",
										"rshared",
										"slave",
										"rslave",
									]),
								),
								/**
								 * Disable recursive bind mount.
								 */
								NonRecursive: v.exactOptional(v.boolean()),
								/**
								 * Create mount point on host if missing
								 */
								CreateMountpoint: v.exactOptional(v.boolean()),
							}),
						),
						/**
						 * Optional configuration for the `volume` type.
						 */
						VolumeOptions: v.exactOptional(
							v.strictObject({
								/**
								 * Populate volume with data from the target.
								 */
								NoCopy: v.exactOptional(v.boolean()),
								/**
								 * User-defined key/value metadata.
								 */
								Labels: v.exactOptional(v.record(v.string(), v.unknown())),
								/**
								 * Map of driver specific options
								 */
								DriverConfig: v.exactOptional(
									v.strictObject({
										/**
										 * Name of the driver to use to create the volume.
										 */
										Name: v.exactOptional(v.string()),
										/**
										 * key/value map of driver specific options.
										 */
										Options: v.exactOptional(v.record(v.string(), v.unknown())),
									}),
								),
							}),
						),
						/**
						 * Optional configuration for the `tmpfs` type.
						 */
						TmpfsOptions: v.exactOptional(
							v.strictObject({
								/**
								 * The size for the tmpfs mount in bytes.
								 */
								SizeBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * The permission mode for the tmpfs mount in an integer.
								 */
								Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
					}),
				),
			),
			/**
			 * Signal to stop the container.
			 */
			StopSignal: v.exactOptional(v.string()),
			/**
			 * Amount of time to wait for the container to terminate before
			 * forcefully killing it.
			 */
			StopGracePeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * A test to perform to check that the container is healthy.
			 */
			HealthCheck: v.exactOptional(
				v.strictObject({
					/**
					 * The test to perform. Possible values are:
					 *
					 * - `[]` inherit healthcheck from image or parent image
					 * - `["NONE"]` disable healthcheck
					 * - `["CMD", args...]` exec arguments directly
					 * - `["CMD-SHELL", command]` run command with system's default shell
					 */
					Test: v.exactOptional(v.array(v.string())),
					/**
					 * The time to wait between checks in nanoseconds. It should be 0 or at
					 * least 1000000 (1 ms). 0 means inherit.
					 */
					Interval: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The time to wait before considering the check to have hung. It should
					 * be 0 or at least 1000000 (1 ms). 0 means inherit.
					 */
					Timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The number of consecutive failures needed to consider a container as
					 * unhealthy. 0 means inherit.
					 */
					Retries: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Start period for the container to initialize before starting
					 * health-retries countdown in nanoseconds. It should be 0 or at least
					 * 1000000 (1 ms). 0 means inherit.
					 */
					StartPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
			/**
			 * A list of hostname/IP mappings to add to the container's `hosts`
			 * file. The format of extra hosts is specified in the
			 * [hosts(5)](http://man7.org/linux/man-pages/man5/hosts.5.html)
			 * man page:
			 *
			 *
			 * IP_address canonical_hostname [aliases...]
			 */
			Hosts: v.exactOptional(v.array(v.string())),
			/**
			 * Specification for DNS related configurations in resolver configuration
			 * file (`resolv.conf`).
			 */
			DNSConfig: v.exactOptional(
				v.strictObject({
					/**
					 * The IP addresses of the name servers.
					 */
					Nameservers: v.exactOptional(v.array(v.string())),
					/**
					 * A search list for host-name lookup.
					 */
					Search: v.exactOptional(v.array(v.string())),
					/**
					 * A list of internal resolver variables to be modified (e.g.,
					 * `debug`, `ndots:3`, etc.).
					 */
					Options: v.exactOptional(v.array(v.string())),
				}),
			),
			/**
			 * Secrets contains references to zero or more secrets that will be
			 * exposed to the service.
			 */
			Secrets: v.exactOptional(
				v.array(
					v.strictObject({
						/**
						 * File represents a specific target that is backed by a file.
						 */
						File: v.exactOptional(
							v.strictObject({
								/**
								 * Name represents the final filename in the filesystem.
								 */
								Name: v.exactOptional(v.string()),
								/**
								 * UID represents the file UID.
								 */
								UID: v.exactOptional(v.string()),
								/**
								 * GID represents the file GID.
								 */
								GID: v.exactOptional(v.string()),
								/**
								 * Mode represents the FileMode of the file.
								 */
								Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
						/**
						 * SecretID represents the ID of the specific secret that we're
						 * referencing.
						 */
						SecretID: v.exactOptional(v.string()),
						/**
						 * SecretName is the name of the secret that this references,
						 * but this is just provided for lookup/display purposes. The
						 * secret in the reference will be identified by its ID.
						 */
						SecretName: v.exactOptional(v.string()),
					}),
				),
			),
			/**
			 * Configs contains references to zero or more configs that will be
			 * exposed to the service.
			 */
			Configs: v.exactOptional(
				v.array(
					v.strictObject({
						/**
						 * File represents a specific target that is backed by a file.
						 *
						 * <p><br /><p>
						 *
						 * > **Note**: `Configs.File` and `Configs.Runtime` are mutually exclusive
						 */
						File: v.exactOptional(
							v.strictObject({
								/**
								 * Name represents the final filename in the filesystem.
								 */
								Name: v.exactOptional(v.string()),
								/**
								 * UID represents the file UID.
								 */
								UID: v.exactOptional(v.string()),
								/**
								 * GID represents the file GID.
								 */
								GID: v.exactOptional(v.string()),
								/**
								 * Mode represents the FileMode of the file.
								 */
								Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
						/**
						 * Runtime represents a target that is not mounted into the
						 * container but is used by the task
						 *
						 * <p><br /><p>
						 *
						 * > **Note**: `Configs.File` and `Configs.Runtime` are mutually
						 * > exclusive
						 */
						Runtime: v.exactOptional(v.record(v.string(), v.unknown())),
						/**
						 * ConfigID represents the ID of the specific config that we're
						 * referencing.
						 */
						ConfigID: v.exactOptional(v.string()),
						/**
						 * ConfigName is the name of the config that this references,
						 * but this is just provided for lookup/display purposes. The
						 * config in the reference will be identified by its ID.
						 */
						ConfigName: v.exactOptional(v.string()),
					}),
				),
			),
			/**
			 * Isolation technology of the containers running the service.
			 * (Windows only)
			 */
			Isolation: v.exactOptional(
				v.picklist(["default", "process", "hyperv", ""]),
			),
			/**
			 * Run an init inside the container that forwards signals and reaps
			 * processes. This field is omitted if empty, and the default (as
			 * configured on the daemon) is used.
			 */
			Init: v.exactOptional(v.nullable(v.boolean())),
			/**
			 * Set kernel namedspaced parameters (sysctls) in the container.
			 * The Sysctls option on services accepts the same sysctls as the
			 * are supported on containers. Note that while the same sysctls are
			 * supported, no guarantees or checks are made about their
			 * suitability for a clustered environment, and it's up to the user
			 * to determine whether a given sysctl will work properly in a
			 * Service.
			 */
			Sysctls: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * A list of kernel capabilities to add to the default set
			 * for the container.
			 */
			CapabilityAdd: v.exactOptional(v.array(v.string())),
			/**
			 * A list of kernel capabilities to drop from the default set
			 * for the container.
			 */
			CapabilityDrop: v.exactOptional(v.array(v.string())),
			/**
			 * A list of resource limits to set in the container. For example: `{"Name":
			 * "nofile", "Soft": 1024, "Hard": 2048}`"
			 */
			Ulimits: v.exactOptional(
				v.array(
					v.strictObject({
						/**
						 * Name of ulimit
						 */
						Name: v.exactOptional(v.string()),
						/**
						 * Soft limit
						 */
						Soft: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * Hard limit
						 */
						Hard: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
			),
		}),
	),
	/**
	 * Read-only spec type for non-swarm containers attached to swarm overlay
	 * networks.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
	 * > mutually exclusive. PluginSpec is only used when the Runtime field
	 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
	 * > field is set to `attachment`.
	 */
	NetworkAttachmentSpec: v.exactOptional(
		v.strictObject({
			/**
			 * ID of the container represented by this task
			 */
			ContainerID: v.exactOptional(v.string()),
		}),
	),
	/**
	 * Resource requirements which apply to each individual container created
	 * as part of the service.
	 */
	Resources: v.exactOptional(
		v.strictObject({
			/**
			 * An object describing a limit on resources which can be requested by a task.
			 */
			Limits: v.exactOptional(
				v.strictObject({
					NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
					MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
					 */
					Pids: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
			/**
			 * An object describing the resources which can be advertised by a node and
			 * requested by a task.
			 */
			Reservations: v.exactOptional(
				v.strictObject({
					NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
					MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
					 * String resources (e.g, `GPU=UUID1`).
					 */
					GenericResources: v.exactOptional(
						v.array(
							v.strictObject({
								NamedResourceSpec: v.exactOptional(
									v.strictObject({
										Kind: v.exactOptional(v.string()),
										Value: v.exactOptional(v.string()),
									}),
								),
								DiscreteResourceSpec: v.exactOptional(
									v.strictObject({
										Kind: v.exactOptional(v.string()),
										Value: v.exactOptional(v.pipe(v.number(), v.integer())),
									}),
								),
							}),
						),
					),
				}),
			),
		}),
	),
	/**
	 * Specification for the restart policy which applies to containers
	 * created as part of this service.
	 */
	RestartPolicy: v.exactOptional(
		v.strictObject({
			/**
			 * Condition for restart.
			 */
			Condition: v.exactOptional(v.picklist(["none", "on-failure", "any"])),
			/**
			 * Delay between restart attempts.
			 */
			Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * Maximum attempts to restart a given container before giving up
			 * (default value is 0, which is ignored).
			 */
			MaxAttempts: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * Windows is the time window used to evaluate the restart policy
			 * (default value is 0, which is unbounded).
			 */
			Window: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	Placement: v.exactOptional(
		v.strictObject({
			/**
			 * An array of constraint expressions to limit the set of nodes where
			 * a task can be scheduled. Constraint expressions can either use a
			 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
			 * nodes that satisfy every expression (AND match). Constraints can
			 * match node or Docker Engine labels as follows:
			 *
			 * node attribute       | matches                        | example
			 * ---------------------|--------------------------------|-----------------------------------------------
			 * `node.id`            | Node ID                        |
			 * `node.id==2ivku8v2gvtg4`
			 * `node.hostname`      | Node hostname                  |
			 * `node.hostname!=node-2`
			 * `node.role`          | Node role (`manager`/`worker`) |
			 * `node.role==manager`
			 * `node.platform.os`   | Node operating system          |
			 * `node.platform.os==windows`
			 * `node.platform.arch` | Node architecture              |
			 * `node.platform.arch==x86_64`
			 * `node.labels`        | User-defined node labels       |
			 * `node.labels.security==high`
			 * `engine.labels`      | Docker Engine's labels         |
			 * `engine.labels.operatingsystem==ubuntu-14.04`
			 *
			 * `engine.labels` apply to Docker Engine labels like operating system,
			 * drivers, etc. Swarm administrators add `node.labels` for operational
			 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
			 */
			Constraints: v.exactOptional(v.array(v.string())),
			/**
			 * Preferences provide a way to make the scheduler aware of factors
			 * such as topology. They are provided in order from highest to
			 * lowest precedence.
			 */
			Preferences: v.exactOptional(
				v.array(
					v.strictObject({
						Spread: v.exactOptional(
							v.strictObject({
								/**
								 * label descriptor, such as `engine.labels.az`.
								 */
								SpreadDescriptor: v.exactOptional(v.string()),
							}),
						),
					}),
				),
			),
			/**
			 * Maximum number of replicas for per node (default value is 0, which
			 * is unlimited)
			 */
			MaxReplicas: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * Platforms stores all the platforms that the service's image can
			 * run on. This field is used in the platform filter for scheduling.
			 * If empty, then the platform filter is off, meaning there are no
			 * scheduling restrictions.
			 */
			Platforms: v.exactOptional(
				v.array(
					v.strictObject({
						/**
						 * Architecture represents the hardware architecture (for example,
						 * `x86_64`).
						 */
						Architecture: v.exactOptional(v.string()),
						/**
						 * OS represents the Operating System (for example, `linux` or `windows`).
						 */
						OS: v.exactOptional(v.string()),
					}),
				),
			),
		}),
	),
	/**
	 * A counter that triggers an update even if no relevant parameters have
	 * been changed.
	 */
	ForceUpdate: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Runtime is the type of runtime specified for the task executor.
	 */
	Runtime: v.exactOptional(v.string()),
	/**
	 * Specifies which networks the service should attach to.
	 */
	Networks: v.exactOptional(
		v.array(
			v.strictObject({
				/**
				 * The target network for attachment. Must be a network name or ID.
				 */
				Target: v.exactOptional(v.string()),
				/**
				 * Discoverable alternate names for the service on this network.
				 */
				Aliases: v.exactOptional(v.array(v.string())),
				/**
				 * Driver attachment options for the network target.
				 */
				DriverOpts: v.exactOptional(v.record(v.string(), v.unknown())),
			}),
		),
	),
	/**
	 * Specifies the log driver to use for tasks created from this spec. If
	 * not present, the default one for the swarm will be used, finally
	 * falling back to the engine default if not specified.
	 */
	LogDriver: v.exactOptional(
		v.strictObject({
			Name: v.exactOptional(v.string()),
			Options: v.exactOptional(v.record(v.string(), v.unknown())),
		}),
	),
});
export const taskStateSchema = v.picklist([
	"new",
	"allocated",
	"pending",
	"assigned",
	"accepted",
	"preparing",
	"ready",
	"starting",
	"running",
	"complete",
	"shutdown",
	"failed",
	"rejected",
	"remove",
	"orphaned",
]);
export const taskSchema = v.strictObject({
	/**
	 * The ID of the task.
	 */
	ID: v.exactOptional(v.string()),
	/**
	 * The version number of the object such as node, service, etc. This is needed
	 * to avoid conflicting writes. The client must send the version number along
	 * with the modified specification when updating these objects.
	 *
	 * This approach ensures safe concurrency and determinism in that the change
	 * on the object may not be applied if the version number has changed from the
	 * last read. In other words, if two update requests specify the same base
	 * version, only one of the requests can succeed. As a result, two separate
	 * update requests that happen at the same time will not unintentionally
	 * overwrite each other.
	 */
	Version: v.exactOptional(
		v.strictObject({
			Index: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	CreatedAt: v.exactOptional(v.string()),
	UpdatedAt: v.exactOptional(v.string()),
	/**
	 * Name of the task.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * User modifiable task configuration.
	 */
	Spec: v.exactOptional(
		v.strictObject({
			/**
			 * Plugin spec for the service.  *(Experimental release only.)*
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
			 * > mutually exclusive. PluginSpec is only used when the Runtime field
			 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
			 * > field is set to `attachment`.
			 */
			PluginSpec: v.exactOptional(
				v.strictObject({
					/**
					 * The name or 'alias' to use for the plugin.
					 */
					Name: v.exactOptional(v.string()),
					/**
					 * The plugin image reference to use.
					 */
					Remote: v.exactOptional(v.string()),
					/**
					 * Disable the plugin once scheduled.
					 */
					Disabled: v.exactOptional(v.boolean()),
					PluginPrivilege: v.exactOptional(
						v.array(
							v.strictObject({
								Name: v.exactOptional(v.string()),
								Description: v.exactOptional(v.string()),
								Value: v.exactOptional(v.array(v.string())),
							}),
						),
					),
				}),
			),
			/**
			 * Container spec for the service.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
			 * > mutually exclusive. PluginSpec is only used when the Runtime field
			 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
			 * > field is set to `attachment`.
			 */
			ContainerSpec: v.exactOptional(
				v.strictObject({
					/**
					 * The image name to use for the container
					 */
					Image: v.exactOptional(v.string()),
					/**
					 * User-defined key/value data.
					 */
					Labels: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * The command to be run in the image.
					 */
					Command: v.exactOptional(v.array(v.string())),
					/**
					 * Arguments to the command.
					 */
					Args: v.exactOptional(v.array(v.string())),
					/**
					 * The hostname to use for the container, as a valid
					 * [RFC 1123](https://tools.ietf.org/html/rfc1123) hostname.
					 */
					Hostname: v.exactOptional(v.string()),
					/**
					 * A list of environment variables in the form `VAR=value`.
					 */
					Env: v.exactOptional(v.array(v.string())),
					/**
					 * The working directory for commands to run in.
					 */
					Dir: v.exactOptional(v.string()),
					/**
					 * The user inside the container.
					 */
					User: v.exactOptional(v.string()),
					/**
					 * A list of additional groups that the container process will run as.
					 */
					Groups: v.exactOptional(v.array(v.string())),
					/**
					 * Security options for the container
					 */
					Privileges: v.exactOptional(
						v.strictObject({
							/**
							 * CredentialSpec for managed service account (Windows only)
							 */
							CredentialSpec: v.exactOptional(
								v.strictObject({
									/**
									 * Load credential spec from a Swarm Config with the given ID.
									 * The specified config must also be present in the Configs
									 * field with the Runtime property set.
									 *
									 * <p><br /></p>
									 *
									 *
									 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
									 * > and `CredentialSpec.Config` are mutually exclusive.
									 */
									Config: v.exactOptional(v.string()),
									/**
									 * Load credential spec from this file. The file is read by
									 * the daemon, and must be present in the `CredentialSpecs`
									 * subdirectory in the docker data directory, which defaults
									 * to `C:\ProgramData\Docker\` on Windows.
									 *
									 * For example, specifying `spec.json` loads
									 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
									 *
									 * <p><br /></p>
									 *
									 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
									 * > and `CredentialSpec.Config` are mutually exclusive.
									 */
									File: v.exactOptional(v.string()),
									/**
									 * Load credential spec from this value in the Windows
									 * registry. The specified registry value must be located in:
									 *
									 * `HKLM\SOFTWARE\Microsoft\Windows
									 * NT\CurrentVersion\Virtualization\Containers\CredentialSpecs`
									 *
									 * <p><br /></p>
									 *
									 *
									 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
									 * > and `CredentialSpec.Config` are mutually exclusive.
									 */
									Registry: v.exactOptional(v.string()),
								}),
							),
							/**
							 * SELinux labels of the container
							 */
							SELinuxContext: v.exactOptional(
								v.strictObject({
									/**
									 * Disable SELinux
									 */
									Disable: v.exactOptional(v.boolean()),
									/**
									 * SELinux user label
									 */
									User: v.exactOptional(v.string()),
									/**
									 * SELinux role label
									 */
									Role: v.exactOptional(v.string()),
									/**
									 * SELinux type label
									 */
									Type: v.exactOptional(v.string()),
									/**
									 * SELinux level label
									 */
									Level: v.exactOptional(v.string()),
								}),
							),
						}),
					),
					/**
					 * Whether a pseudo-TTY should be allocated.
					 */
					TTY: v.exactOptional(v.boolean()),
					/**
					 * Open `stdin`
					 */
					OpenStdin: v.exactOptional(v.boolean()),
					/**
					 * Mount the container's root filesystem as read only.
					 */
					ReadOnly: v.exactOptional(v.boolean()),
					/**
					 * Specification for mounts to be added to containers created as part
					 * of the service.
					 */
					Mounts: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Container path.
								 */
								Target: v.exactOptional(v.string()),
								/**
								 * Mount source (e.g. a volume name, a host path).
								 */
								Source: v.exactOptional(v.string()),
								/**
								 * The mount type. Available types:
								 *
								 * - `bind` Mounts a file or directory from the host into the container. Must
								 * exist prior to creating the container.
								 * - `volume` Creates a volume with the given name and options (or uses a
								 * pre-existing volume with the same name and options). These are **not**
								 * removed when the container is removed.
								 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
								 * specified for tmpfs.
								 * - `npipe` Mounts a named pipe from the host into the container. Must exist
								 * prior to creating the container.
								 * - `cluster` a Swarm cluster volume
								 */
								Type: v.exactOptional(
									v.picklist(["bind", "volume", "tmpfs", "npipe", "cluster"]),
								),
								/**
								 * Whether the mount should be read-only.
								 */
								ReadOnly: v.exactOptional(v.boolean()),
								/**
								 * The consistency requirement for the mount: `default`, `consistent`,
								 * `cached`, or `delegated`.
								 */
								Consistency: v.exactOptional(v.string()),
								/**
								 * Optional configuration for the `bind` type.
								 */
								BindOptions: v.exactOptional(
									v.strictObject({
										/**
										 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
										 */
										Propagation: v.exactOptional(
											v.picklist([
												"private",
												"rprivate",
												"shared",
												"rshared",
												"slave",
												"rslave",
											]),
										),
										/**
										 * Disable recursive bind mount.
										 */
										NonRecursive: v.exactOptional(v.boolean()),
										/**
										 * Create mount point on host if missing
										 */
										CreateMountpoint: v.exactOptional(v.boolean()),
									}),
								),
								/**
								 * Optional configuration for the `volume` type.
								 */
								VolumeOptions: v.exactOptional(
									v.strictObject({
										/**
										 * Populate volume with data from the target.
										 */
										NoCopy: v.exactOptional(v.boolean()),
										/**
										 * User-defined key/value metadata.
										 */
										Labels: v.exactOptional(v.record(v.string(), v.unknown())),
										/**
										 * Map of driver specific options
										 */
										DriverConfig: v.exactOptional(
											v.strictObject({
												/**
												 * Name of the driver to use to create the volume.
												 */
												Name: v.exactOptional(v.string()),
												/**
												 * key/value map of driver specific options.
												 */
												Options: v.exactOptional(
													v.record(v.string(), v.unknown()),
												),
											}),
										),
									}),
								),
								/**
								 * Optional configuration for the `tmpfs` type.
								 */
								TmpfsOptions: v.exactOptional(
									v.strictObject({
										/**
										 * The size for the tmpfs mount in bytes.
										 */
										SizeBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
										/**
										 * The permission mode for the tmpfs mount in an integer.
										 */
										Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
									}),
								),
							}),
						),
					),
					/**
					 * Signal to stop the container.
					 */
					StopSignal: v.exactOptional(v.string()),
					/**
					 * Amount of time to wait for the container to terminate before
					 * forcefully killing it.
					 */
					StopGracePeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * A test to perform to check that the container is healthy.
					 */
					HealthCheck: v.exactOptional(
						v.strictObject({
							/**
							 * The test to perform. Possible values are:
							 *
							 * - `[]` inherit healthcheck from image or parent image
							 * - `["NONE"]` disable healthcheck
							 * - `["CMD", args...]` exec arguments directly
							 * - `["CMD-SHELL", command]` run command with system's default shell
							 */
							Test: v.exactOptional(v.array(v.string())),
							/**
							 * The time to wait between checks in nanoseconds. It should be 0 or at
							 * least 1000000 (1 ms). 0 means inherit.
							 */
							Interval: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The time to wait before considering the check to have hung. It should
							 * be 0 or at least 1000000 (1 ms). 0 means inherit.
							 */
							Timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The number of consecutive failures needed to consider a container as
							 * unhealthy. 0 means inherit.
							 */
							Retries: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * Start period for the container to initialize before starting
							 * health-retries countdown in nanoseconds. It should be 0 or at least
							 * 1000000 (1 ms). 0 means inherit.
							 */
							StartPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
					/**
					 * A list of hostname/IP mappings to add to the container's `hosts`
					 * file. The format of extra hosts is specified in the
					 * [hosts(5)](http://man7.org/linux/man-pages/man5/hosts.5.html)
					 * man page:
					 *
					 *
					 * IP_address canonical_hostname [aliases...]
					 */
					Hosts: v.exactOptional(v.array(v.string())),
					/**
					 * Specification for DNS related configurations in resolver configuration
					 * file (`resolv.conf`).
					 */
					DNSConfig: v.exactOptional(
						v.strictObject({
							/**
							 * The IP addresses of the name servers.
							 */
							Nameservers: v.exactOptional(v.array(v.string())),
							/**
							 * A search list for host-name lookup.
							 */
							Search: v.exactOptional(v.array(v.string())),
							/**
							 * A list of internal resolver variables to be modified (e.g.,
							 * `debug`, `ndots:3`, etc.).
							 */
							Options: v.exactOptional(v.array(v.string())),
						}),
					),
					/**
					 * Secrets contains references to zero or more secrets that will be
					 * exposed to the service.
					 */
					Secrets: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * File represents a specific target that is backed by a file.
								 */
								File: v.exactOptional(
									v.strictObject({
										/**
										 * Name represents the final filename in the filesystem.
										 */
										Name: v.exactOptional(v.string()),
										/**
										 * UID represents the file UID.
										 */
										UID: v.exactOptional(v.string()),
										/**
										 * GID represents the file GID.
										 */
										GID: v.exactOptional(v.string()),
										/**
										 * Mode represents the FileMode of the file.
										 */
										Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
									}),
								),
								/**
								 * SecretID represents the ID of the specific secret that we're
								 * referencing.
								 */
								SecretID: v.exactOptional(v.string()),
								/**
								 * SecretName is the name of the secret that this references,
								 * but this is just provided for lookup/display purposes. The
								 * secret in the reference will be identified by its ID.
								 */
								SecretName: v.exactOptional(v.string()),
							}),
						),
					),
					/**
					 * Configs contains references to zero or more configs that will be
					 * exposed to the service.
					 */
					Configs: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * File represents a specific target that is backed by a file.
								 *
								 * <p><br /><p>
								 *
								 * > **Note**: `Configs.File` and `Configs.Runtime` are mutually exclusive
								 */
								File: v.exactOptional(
									v.strictObject({
										/**
										 * Name represents the final filename in the filesystem.
										 */
										Name: v.exactOptional(v.string()),
										/**
										 * UID represents the file UID.
										 */
										UID: v.exactOptional(v.string()),
										/**
										 * GID represents the file GID.
										 */
										GID: v.exactOptional(v.string()),
										/**
										 * Mode represents the FileMode of the file.
										 */
										Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
									}),
								),
								/**
								 * Runtime represents a target that is not mounted into the
								 * container but is used by the task
								 *
								 * <p><br /><p>
								 *
								 * > **Note**: `Configs.File` and `Configs.Runtime` are mutually
								 * > exclusive
								 */
								Runtime: v.exactOptional(v.record(v.string(), v.unknown())),
								/**
								 * ConfigID represents the ID of the specific config that we're
								 * referencing.
								 */
								ConfigID: v.exactOptional(v.string()),
								/**
								 * ConfigName is the name of the config that this references,
								 * but this is just provided for lookup/display purposes. The
								 * config in the reference will be identified by its ID.
								 */
								ConfigName: v.exactOptional(v.string()),
							}),
						),
					),
					/**
					 * Isolation technology of the containers running the service.
					 * (Windows only)
					 */
					Isolation: v.exactOptional(
						v.picklist(["default", "process", "hyperv", ""]),
					),
					/**
					 * Run an init inside the container that forwards signals and reaps
					 * processes. This field is omitted if empty, and the default (as
					 * configured on the daemon) is used.
					 */
					Init: v.exactOptional(v.nullable(v.boolean())),
					/**
					 * Set kernel namedspaced parameters (sysctls) in the container.
					 * The Sysctls option on services accepts the same sysctls as the
					 * are supported on containers. Note that while the same sysctls are
					 * supported, no guarantees or checks are made about their
					 * suitability for a clustered environment, and it's up to the user
					 * to determine whether a given sysctl will work properly in a
					 * Service.
					 */
					Sysctls: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * A list of kernel capabilities to add to the default set
					 * for the container.
					 */
					CapabilityAdd: v.exactOptional(v.array(v.string())),
					/**
					 * A list of kernel capabilities to drop from the default set
					 * for the container.
					 */
					CapabilityDrop: v.exactOptional(v.array(v.string())),
					/**
					 * A list of resource limits to set in the container. For example: `{"Name":
					 * "nofile", "Soft": 1024, "Hard": 2048}`"
					 */
					Ulimits: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Name of ulimit
								 */
								Name: v.exactOptional(v.string()),
								/**
								 * Soft limit
								 */
								Soft: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * Hard limit
								 */
								Hard: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
					),
				}),
			),
			/**
			 * Read-only spec type for non-swarm containers attached to swarm overlay
			 * networks.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
			 * > mutually exclusive. PluginSpec is only used when the Runtime field
			 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
			 * > field is set to `attachment`.
			 */
			NetworkAttachmentSpec: v.exactOptional(
				v.strictObject({
					/**
					 * ID of the container represented by this task
					 */
					ContainerID: v.exactOptional(v.string()),
				}),
			),
			/**
			 * Resource requirements which apply to each individual container created
			 * as part of the service.
			 */
			Resources: v.exactOptional(
				v.strictObject({
					/**
					 * An object describing a limit on resources which can be requested by a task.
					 */
					Limits: v.exactOptional(
						v.strictObject({
							NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
							MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
							 */
							Pids: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
					/**
					 * An object describing the resources which can be advertised by a node and
					 * requested by a task.
					 */
					Reservations: v.exactOptional(
						v.strictObject({
							NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
							MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
							 * String resources (e.g, `GPU=UUID1`).
							 */
							GenericResources: v.exactOptional(
								v.array(
									v.strictObject({
										NamedResourceSpec: v.exactOptional(
											v.strictObject({
												Kind: v.exactOptional(v.string()),
												Value: v.exactOptional(v.string()),
											}),
										),
										DiscreteResourceSpec: v.exactOptional(
											v.strictObject({
												Kind: v.exactOptional(v.string()),
												Value: v.exactOptional(v.pipe(v.number(), v.integer())),
											}),
										),
									}),
								),
							),
						}),
					),
				}),
			),
			/**
			 * Specification for the restart policy which applies to containers
			 * created as part of this service.
			 */
			RestartPolicy: v.exactOptional(
				v.strictObject({
					/**
					 * Condition for restart.
					 */
					Condition: v.exactOptional(v.picklist(["none", "on-failure", "any"])),
					/**
					 * Delay between restart attempts.
					 */
					Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Maximum attempts to restart a given container before giving up
					 * (default value is 0, which is ignored).
					 */
					MaxAttempts: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Windows is the time window used to evaluate the restart policy
					 * (default value is 0, which is unbounded).
					 */
					Window: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
			Placement: v.exactOptional(
				v.strictObject({
					/**
					 * An array of constraint expressions to limit the set of nodes where
					 * a task can be scheduled. Constraint expressions can either use a
					 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
					 * nodes that satisfy every expression (AND match). Constraints can
					 * match node or Docker Engine labels as follows:
					 *
					 * node attribute       | matches                        | example
					 * ---------------------|--------------------------------|-----------------------------------------------
					 * `node.id`            | Node ID                        |
					 * `node.id==2ivku8v2gvtg4`
					 * `node.hostname`      | Node hostname                  |
					 * `node.hostname!=node-2`
					 * `node.role`          | Node role (`manager`/`worker`) |
					 * `node.role==manager`
					 * `node.platform.os`   | Node operating system          |
					 * `node.platform.os==windows`
					 * `node.platform.arch` | Node architecture              |
					 * `node.platform.arch==x86_64`
					 * `node.labels`        | User-defined node labels       |
					 * `node.labels.security==high`
					 * `engine.labels`      | Docker Engine's labels         |
					 * `engine.labels.operatingsystem==ubuntu-14.04`
					 *
					 * `engine.labels` apply to Docker Engine labels like operating system,
					 * drivers, etc. Swarm administrators add `node.labels` for operational
					 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
					 */
					Constraints: v.exactOptional(v.array(v.string())),
					/**
					 * Preferences provide a way to make the scheduler aware of factors
					 * such as topology. They are provided in order from highest to
					 * lowest precedence.
					 */
					Preferences: v.exactOptional(
						v.array(
							v.strictObject({
								Spread: v.exactOptional(
									v.strictObject({
										/**
										 * label descriptor, such as `engine.labels.az`.
										 */
										SpreadDescriptor: v.exactOptional(v.string()),
									}),
								),
							}),
						),
					),
					/**
					 * Maximum number of replicas for per node (default value is 0, which
					 * is unlimited)
					 */
					MaxReplicas: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Platforms stores all the platforms that the service's image can
					 * run on. This field is used in the platform filter for scheduling.
					 * If empty, then the platform filter is off, meaning there are no
					 * scheduling restrictions.
					 */
					Platforms: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Architecture represents the hardware architecture (for example,
								 * `x86_64`).
								 */
								Architecture: v.exactOptional(v.string()),
								/**
								 * OS represents the Operating System (for example, `linux` or `windows`).
								 */
								OS: v.exactOptional(v.string()),
							}),
						),
					),
				}),
			),
			/**
			 * A counter that triggers an update even if no relevant parameters have
			 * been changed.
			 */
			ForceUpdate: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * Runtime is the type of runtime specified for the task executor.
			 */
			Runtime: v.exactOptional(v.string()),
			/**
			 * Specifies which networks the service should attach to.
			 */
			Networks: v.exactOptional(
				v.array(
					v.strictObject({
						/**
						 * The target network for attachment. Must be a network name or ID.
						 */
						Target: v.exactOptional(v.string()),
						/**
						 * Discoverable alternate names for the service on this network.
						 */
						Aliases: v.exactOptional(v.array(v.string())),
						/**
						 * Driver attachment options for the network target.
						 */
						DriverOpts: v.exactOptional(v.record(v.string(), v.unknown())),
					}),
				),
			),
			/**
			 * Specifies the log driver to use for tasks created from this spec. If
			 * not present, the default one for the swarm will be used, finally
			 * falling back to the engine default if not specified.
			 */
			LogDriver: v.exactOptional(
				v.strictObject({
					Name: v.exactOptional(v.string()),
					Options: v.exactOptional(v.record(v.string(), v.unknown())),
				}),
			),
		}),
	),
	/**
	 * The ID of the service this task is part of.
	 */
	ServiceID: v.exactOptional(v.string()),
	Slot: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * The ID of the node that this task is on.
	 */
	NodeID: v.exactOptional(v.string()),
	/**
	 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
	 * String resources (e.g, `GPU=UUID1`).
	 */
	AssignedGenericResources: v.exactOptional(
		v.array(
			v.strictObject({
				NamedResourceSpec: v.exactOptional(
					v.strictObject({
						Kind: v.exactOptional(v.string()),
						Value: v.exactOptional(v.string()),
					}),
				),
				DiscreteResourceSpec: v.exactOptional(
					v.strictObject({
						Kind: v.exactOptional(v.string()),
						Value: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
			}),
		),
	),
	Status: v.exactOptional(
		v.strictObject({
			Timestamp: v.exactOptional(v.string()),
			State: v.exactOptional(
				v.picklist([
					"new",
					"allocated",
					"pending",
					"assigned",
					"accepted",
					"preparing",
					"ready",
					"starting",
					"running",
					"complete",
					"shutdown",
					"failed",
					"rejected",
					"remove",
					"orphaned",
				]),
			),
			Message: v.exactOptional(v.string()),
			Err: v.exactOptional(v.string()),
			ContainerStatus: v.exactOptional(
				v.strictObject({
					ContainerID: v.exactOptional(v.string()),
					PID: v.exactOptional(v.pipe(v.number(), v.integer())),
					ExitCode: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
		}),
	),
	DesiredState: v.exactOptional(
		v.picklist([
			"new",
			"allocated",
			"pending",
			"assigned",
			"accepted",
			"preparing",
			"ready",
			"starting",
			"running",
			"complete",
			"shutdown",
			"failed",
			"rejected",
			"remove",
			"orphaned",
		]),
	),
	/**
	 * The version number of the object such as node, service, etc. This is needed
	 * to avoid conflicting writes. The client must send the version number along
	 * with the modified specification when updating these objects.
	 *
	 * This approach ensures safe concurrency and determinism in that the change
	 * on the object may not be applied if the version number has changed from the
	 * last read. In other words, if two update requests specify the same base
	 * version, only one of the requests can succeed. As a result, two separate
	 * update requests that happen at the same time will not unintentionally
	 * overwrite each other.
	 */
	JobIteration: v.exactOptional(
		v.strictObject({
			Index: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
});
/** User modifiable configuration for a service. */
export const serviceSpecSchema = v.strictObject({
	/**
	 * Name of the service.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * User modifiable task configuration.
	 */
	TaskTemplate: v.exactOptional(
		v.strictObject({
			/**
			 * Plugin spec for the service.  *(Experimental release only.)*
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
			 * > mutually exclusive. PluginSpec is only used when the Runtime field
			 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
			 * > field is set to `attachment`.
			 */
			PluginSpec: v.exactOptional(
				v.strictObject({
					/**
					 * The name or 'alias' to use for the plugin.
					 */
					Name: v.exactOptional(v.string()),
					/**
					 * The plugin image reference to use.
					 */
					Remote: v.exactOptional(v.string()),
					/**
					 * Disable the plugin once scheduled.
					 */
					Disabled: v.exactOptional(v.boolean()),
					PluginPrivilege: v.exactOptional(
						v.array(
							v.strictObject({
								Name: v.exactOptional(v.string()),
								Description: v.exactOptional(v.string()),
								Value: v.exactOptional(v.array(v.string())),
							}),
						),
					),
				}),
			),
			/**
			 * Container spec for the service.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
			 * > mutually exclusive. PluginSpec is only used when the Runtime field
			 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
			 * > field is set to `attachment`.
			 */
			ContainerSpec: v.exactOptional(
				v.strictObject({
					/**
					 * The image name to use for the container
					 */
					Image: v.exactOptional(v.string()),
					/**
					 * User-defined key/value data.
					 */
					Labels: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * The command to be run in the image.
					 */
					Command: v.exactOptional(v.array(v.string())),
					/**
					 * Arguments to the command.
					 */
					Args: v.exactOptional(v.array(v.string())),
					/**
					 * The hostname to use for the container, as a valid
					 * [RFC 1123](https://tools.ietf.org/html/rfc1123) hostname.
					 */
					Hostname: v.exactOptional(v.string()),
					/**
					 * A list of environment variables in the form `VAR=value`.
					 */
					Env: v.exactOptional(v.array(v.string())),
					/**
					 * The working directory for commands to run in.
					 */
					Dir: v.exactOptional(v.string()),
					/**
					 * The user inside the container.
					 */
					User: v.exactOptional(v.string()),
					/**
					 * A list of additional groups that the container process will run as.
					 */
					Groups: v.exactOptional(v.array(v.string())),
					/**
					 * Security options for the container
					 */
					Privileges: v.exactOptional(
						v.strictObject({
							/**
							 * CredentialSpec for managed service account (Windows only)
							 */
							CredentialSpec: v.exactOptional(
								v.strictObject({
									/**
									 * Load credential spec from a Swarm Config with the given ID.
									 * The specified config must also be present in the Configs
									 * field with the Runtime property set.
									 *
									 * <p><br /></p>
									 *
									 *
									 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
									 * > and `CredentialSpec.Config` are mutually exclusive.
									 */
									Config: v.exactOptional(v.string()),
									/**
									 * Load credential spec from this file. The file is read by
									 * the daemon, and must be present in the `CredentialSpecs`
									 * subdirectory in the docker data directory, which defaults
									 * to `C:\ProgramData\Docker\` on Windows.
									 *
									 * For example, specifying `spec.json` loads
									 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
									 *
									 * <p><br /></p>
									 *
									 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
									 * > and `CredentialSpec.Config` are mutually exclusive.
									 */
									File: v.exactOptional(v.string()),
									/**
									 * Load credential spec from this value in the Windows
									 * registry. The specified registry value must be located in:
									 *
									 * `HKLM\SOFTWARE\Microsoft\Windows
									 * NT\CurrentVersion\Virtualization\Containers\CredentialSpecs`
									 *
									 * <p><br /></p>
									 *
									 *
									 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
									 * > and `CredentialSpec.Config` are mutually exclusive.
									 */
									Registry: v.exactOptional(v.string()),
								}),
							),
							/**
							 * SELinux labels of the container
							 */
							SELinuxContext: v.exactOptional(
								v.strictObject({
									/**
									 * Disable SELinux
									 */
									Disable: v.exactOptional(v.boolean()),
									/**
									 * SELinux user label
									 */
									User: v.exactOptional(v.string()),
									/**
									 * SELinux role label
									 */
									Role: v.exactOptional(v.string()),
									/**
									 * SELinux type label
									 */
									Type: v.exactOptional(v.string()),
									/**
									 * SELinux level label
									 */
									Level: v.exactOptional(v.string()),
								}),
							),
						}),
					),
					/**
					 * Whether a pseudo-TTY should be allocated.
					 */
					TTY: v.exactOptional(v.boolean()),
					/**
					 * Open `stdin`
					 */
					OpenStdin: v.exactOptional(v.boolean()),
					/**
					 * Mount the container's root filesystem as read only.
					 */
					ReadOnly: v.exactOptional(v.boolean()),
					/**
					 * Specification for mounts to be added to containers created as part
					 * of the service.
					 */
					Mounts: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Container path.
								 */
								Target: v.exactOptional(v.string()),
								/**
								 * Mount source (e.g. a volume name, a host path).
								 */
								Source: v.exactOptional(v.string()),
								/**
								 * The mount type. Available types:
								 *
								 * - `bind` Mounts a file or directory from the host into the container. Must
								 * exist prior to creating the container.
								 * - `volume` Creates a volume with the given name and options (or uses a
								 * pre-existing volume with the same name and options). These are **not**
								 * removed when the container is removed.
								 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
								 * specified for tmpfs.
								 * - `npipe` Mounts a named pipe from the host into the container. Must exist
								 * prior to creating the container.
								 * - `cluster` a Swarm cluster volume
								 */
								Type: v.exactOptional(
									v.picklist(["bind", "volume", "tmpfs", "npipe", "cluster"]),
								),
								/**
								 * Whether the mount should be read-only.
								 */
								ReadOnly: v.exactOptional(v.boolean()),
								/**
								 * The consistency requirement for the mount: `default`, `consistent`,
								 * `cached`, or `delegated`.
								 */
								Consistency: v.exactOptional(v.string()),
								/**
								 * Optional configuration for the `bind` type.
								 */
								BindOptions: v.exactOptional(
									v.strictObject({
										/**
										 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
										 */
										Propagation: v.exactOptional(
											v.picklist([
												"private",
												"rprivate",
												"shared",
												"rshared",
												"slave",
												"rslave",
											]),
										),
										/**
										 * Disable recursive bind mount.
										 */
										NonRecursive: v.exactOptional(v.boolean()),
										/**
										 * Create mount point on host if missing
										 */
										CreateMountpoint: v.exactOptional(v.boolean()),
									}),
								),
								/**
								 * Optional configuration for the `volume` type.
								 */
								VolumeOptions: v.exactOptional(
									v.strictObject({
										/**
										 * Populate volume with data from the target.
										 */
										NoCopy: v.exactOptional(v.boolean()),
										/**
										 * User-defined key/value metadata.
										 */
										Labels: v.exactOptional(v.record(v.string(), v.unknown())),
										/**
										 * Map of driver specific options
										 */
										DriverConfig: v.exactOptional(
											v.strictObject({
												/**
												 * Name of the driver to use to create the volume.
												 */
												Name: v.exactOptional(v.string()),
												/**
												 * key/value map of driver specific options.
												 */
												Options: v.exactOptional(
													v.record(v.string(), v.unknown()),
												),
											}),
										),
									}),
								),
								/**
								 * Optional configuration for the `tmpfs` type.
								 */
								TmpfsOptions: v.exactOptional(
									v.strictObject({
										/**
										 * The size for the tmpfs mount in bytes.
										 */
										SizeBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
										/**
										 * The permission mode for the tmpfs mount in an integer.
										 */
										Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
									}),
								),
							}),
						),
					),
					/**
					 * Signal to stop the container.
					 */
					StopSignal: v.exactOptional(v.string()),
					/**
					 * Amount of time to wait for the container to terminate before
					 * forcefully killing it.
					 */
					StopGracePeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * A test to perform to check that the container is healthy.
					 */
					HealthCheck: v.exactOptional(
						v.strictObject({
							/**
							 * The test to perform. Possible values are:
							 *
							 * - `[]` inherit healthcheck from image or parent image
							 * - `["NONE"]` disable healthcheck
							 * - `["CMD", args...]` exec arguments directly
							 * - `["CMD-SHELL", command]` run command with system's default shell
							 */
							Test: v.exactOptional(v.array(v.string())),
							/**
							 * The time to wait between checks in nanoseconds. It should be 0 or at
							 * least 1000000 (1 ms). 0 means inherit.
							 */
							Interval: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The time to wait before considering the check to have hung. It should
							 * be 0 or at least 1000000 (1 ms). 0 means inherit.
							 */
							Timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The number of consecutive failures needed to consider a container as
							 * unhealthy. 0 means inherit.
							 */
							Retries: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * Start period for the container to initialize before starting
							 * health-retries countdown in nanoseconds. It should be 0 or at least
							 * 1000000 (1 ms). 0 means inherit.
							 */
							StartPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
					/**
					 * A list of hostname/IP mappings to add to the container's `hosts`
					 * file. The format of extra hosts is specified in the
					 * [hosts(5)](http://man7.org/linux/man-pages/man5/hosts.5.html)
					 * man page:
					 *
					 *
					 * IP_address canonical_hostname [aliases...]
					 */
					Hosts: v.exactOptional(v.array(v.string())),
					/**
					 * Specification for DNS related configurations in resolver configuration
					 * file (`resolv.conf`).
					 */
					DNSConfig: v.exactOptional(
						v.strictObject({
							/**
							 * The IP addresses of the name servers.
							 */
							Nameservers: v.exactOptional(v.array(v.string())),
							/**
							 * A search list for host-name lookup.
							 */
							Search: v.exactOptional(v.array(v.string())),
							/**
							 * A list of internal resolver variables to be modified (e.g.,
							 * `debug`, `ndots:3`, etc.).
							 */
							Options: v.exactOptional(v.array(v.string())),
						}),
					),
					/**
					 * Secrets contains references to zero or more secrets that will be
					 * exposed to the service.
					 */
					Secrets: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * File represents a specific target that is backed by a file.
								 */
								File: v.exactOptional(
									v.strictObject({
										/**
										 * Name represents the final filename in the filesystem.
										 */
										Name: v.exactOptional(v.string()),
										/**
										 * UID represents the file UID.
										 */
										UID: v.exactOptional(v.string()),
										/**
										 * GID represents the file GID.
										 */
										GID: v.exactOptional(v.string()),
										/**
										 * Mode represents the FileMode of the file.
										 */
										Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
									}),
								),
								/**
								 * SecretID represents the ID of the specific secret that we're
								 * referencing.
								 */
								SecretID: v.exactOptional(v.string()),
								/**
								 * SecretName is the name of the secret that this references,
								 * but this is just provided for lookup/display purposes. The
								 * secret in the reference will be identified by its ID.
								 */
								SecretName: v.exactOptional(v.string()),
							}),
						),
					),
					/**
					 * Configs contains references to zero or more configs that will be
					 * exposed to the service.
					 */
					Configs: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * File represents a specific target that is backed by a file.
								 *
								 * <p><br /><p>
								 *
								 * > **Note**: `Configs.File` and `Configs.Runtime` are mutually exclusive
								 */
								File: v.exactOptional(
									v.strictObject({
										/**
										 * Name represents the final filename in the filesystem.
										 */
										Name: v.exactOptional(v.string()),
										/**
										 * UID represents the file UID.
										 */
										UID: v.exactOptional(v.string()),
										/**
										 * GID represents the file GID.
										 */
										GID: v.exactOptional(v.string()),
										/**
										 * Mode represents the FileMode of the file.
										 */
										Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
									}),
								),
								/**
								 * Runtime represents a target that is not mounted into the
								 * container but is used by the task
								 *
								 * <p><br /><p>
								 *
								 * > **Note**: `Configs.File` and `Configs.Runtime` are mutually
								 * > exclusive
								 */
								Runtime: v.exactOptional(v.record(v.string(), v.unknown())),
								/**
								 * ConfigID represents the ID of the specific config that we're
								 * referencing.
								 */
								ConfigID: v.exactOptional(v.string()),
								/**
								 * ConfigName is the name of the config that this references,
								 * but this is just provided for lookup/display purposes. The
								 * config in the reference will be identified by its ID.
								 */
								ConfigName: v.exactOptional(v.string()),
							}),
						),
					),
					/**
					 * Isolation technology of the containers running the service.
					 * (Windows only)
					 */
					Isolation: v.exactOptional(
						v.picklist(["default", "process", "hyperv", ""]),
					),
					/**
					 * Run an init inside the container that forwards signals and reaps
					 * processes. This field is omitted if empty, and the default (as
					 * configured on the daemon) is used.
					 */
					Init: v.exactOptional(v.nullable(v.boolean())),
					/**
					 * Set kernel namedspaced parameters (sysctls) in the container.
					 * The Sysctls option on services accepts the same sysctls as the
					 * are supported on containers. Note that while the same sysctls are
					 * supported, no guarantees or checks are made about their
					 * suitability for a clustered environment, and it's up to the user
					 * to determine whether a given sysctl will work properly in a
					 * Service.
					 */
					Sysctls: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * A list of kernel capabilities to add to the default set
					 * for the container.
					 */
					CapabilityAdd: v.exactOptional(v.array(v.string())),
					/**
					 * A list of kernel capabilities to drop from the default set
					 * for the container.
					 */
					CapabilityDrop: v.exactOptional(v.array(v.string())),
					/**
					 * A list of resource limits to set in the container. For example: `{"Name":
					 * "nofile", "Soft": 1024, "Hard": 2048}`"
					 */
					Ulimits: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Name of ulimit
								 */
								Name: v.exactOptional(v.string()),
								/**
								 * Soft limit
								 */
								Soft: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * Hard limit
								 */
								Hard: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
					),
				}),
			),
			/**
			 * Read-only spec type for non-swarm containers attached to swarm overlay
			 * networks.
			 *
			 * <p><br /></p>
			 *
			 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
			 * > mutually exclusive. PluginSpec is only used when the Runtime field
			 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
			 * > field is set to `attachment`.
			 */
			NetworkAttachmentSpec: v.exactOptional(
				v.strictObject({
					/**
					 * ID of the container represented by this task
					 */
					ContainerID: v.exactOptional(v.string()),
				}),
			),
			/**
			 * Resource requirements which apply to each individual container created
			 * as part of the service.
			 */
			Resources: v.exactOptional(
				v.strictObject({
					/**
					 * An object describing a limit on resources which can be requested by a task.
					 */
					Limits: v.exactOptional(
						v.strictObject({
							NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
							MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
							 */
							Pids: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
					/**
					 * An object describing the resources which can be advertised by a node and
					 * requested by a task.
					 */
					Reservations: v.exactOptional(
						v.strictObject({
							NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
							MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
							 * String resources (e.g, `GPU=UUID1`).
							 */
							GenericResources: v.exactOptional(
								v.array(
									v.strictObject({
										NamedResourceSpec: v.exactOptional(
											v.strictObject({
												Kind: v.exactOptional(v.string()),
												Value: v.exactOptional(v.string()),
											}),
										),
										DiscreteResourceSpec: v.exactOptional(
											v.strictObject({
												Kind: v.exactOptional(v.string()),
												Value: v.exactOptional(v.pipe(v.number(), v.integer())),
											}),
										),
									}),
								),
							),
						}),
					),
				}),
			),
			/**
			 * Specification for the restart policy which applies to containers
			 * created as part of this service.
			 */
			RestartPolicy: v.exactOptional(
				v.strictObject({
					/**
					 * Condition for restart.
					 */
					Condition: v.exactOptional(v.picklist(["none", "on-failure", "any"])),
					/**
					 * Delay between restart attempts.
					 */
					Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Maximum attempts to restart a given container before giving up
					 * (default value is 0, which is ignored).
					 */
					MaxAttempts: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Windows is the time window used to evaluate the restart policy
					 * (default value is 0, which is unbounded).
					 */
					Window: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
			Placement: v.exactOptional(
				v.strictObject({
					/**
					 * An array of constraint expressions to limit the set of nodes where
					 * a task can be scheduled. Constraint expressions can either use a
					 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
					 * nodes that satisfy every expression (AND match). Constraints can
					 * match node or Docker Engine labels as follows:
					 *
					 * node attribute       | matches                        | example
					 * ---------------------|--------------------------------|-----------------------------------------------
					 * `node.id`            | Node ID                        |
					 * `node.id==2ivku8v2gvtg4`
					 * `node.hostname`      | Node hostname                  |
					 * `node.hostname!=node-2`
					 * `node.role`          | Node role (`manager`/`worker`) |
					 * `node.role==manager`
					 * `node.platform.os`   | Node operating system          |
					 * `node.platform.os==windows`
					 * `node.platform.arch` | Node architecture              |
					 * `node.platform.arch==x86_64`
					 * `node.labels`        | User-defined node labels       |
					 * `node.labels.security==high`
					 * `engine.labels`      | Docker Engine's labels         |
					 * `engine.labels.operatingsystem==ubuntu-14.04`
					 *
					 * `engine.labels` apply to Docker Engine labels like operating system,
					 * drivers, etc. Swarm administrators add `node.labels` for operational
					 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
					 */
					Constraints: v.exactOptional(v.array(v.string())),
					/**
					 * Preferences provide a way to make the scheduler aware of factors
					 * such as topology. They are provided in order from highest to
					 * lowest precedence.
					 */
					Preferences: v.exactOptional(
						v.array(
							v.strictObject({
								Spread: v.exactOptional(
									v.strictObject({
										/**
										 * label descriptor, such as `engine.labels.az`.
										 */
										SpreadDescriptor: v.exactOptional(v.string()),
									}),
								),
							}),
						),
					),
					/**
					 * Maximum number of replicas for per node (default value is 0, which
					 * is unlimited)
					 */
					MaxReplicas: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Platforms stores all the platforms that the service's image can
					 * run on. This field is used in the platform filter for scheduling.
					 * If empty, then the platform filter is off, meaning there are no
					 * scheduling restrictions.
					 */
					Platforms: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Architecture represents the hardware architecture (for example,
								 * `x86_64`).
								 */
								Architecture: v.exactOptional(v.string()),
								/**
								 * OS represents the Operating System (for example, `linux` or `windows`).
								 */
								OS: v.exactOptional(v.string()),
							}),
						),
					),
				}),
			),
			/**
			 * A counter that triggers an update even if no relevant parameters have
			 * been changed.
			 */
			ForceUpdate: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * Runtime is the type of runtime specified for the task executor.
			 */
			Runtime: v.exactOptional(v.string()),
			/**
			 * Specifies which networks the service should attach to.
			 */
			Networks: v.exactOptional(
				v.array(
					v.strictObject({
						/**
						 * The target network for attachment. Must be a network name or ID.
						 */
						Target: v.exactOptional(v.string()),
						/**
						 * Discoverable alternate names for the service on this network.
						 */
						Aliases: v.exactOptional(v.array(v.string())),
						/**
						 * Driver attachment options for the network target.
						 */
						DriverOpts: v.exactOptional(v.record(v.string(), v.unknown())),
					}),
				),
			),
			/**
			 * Specifies the log driver to use for tasks created from this spec. If
			 * not present, the default one for the swarm will be used, finally
			 * falling back to the engine default if not specified.
			 */
			LogDriver: v.exactOptional(
				v.strictObject({
					Name: v.exactOptional(v.string()),
					Options: v.exactOptional(v.record(v.string(), v.unknown())),
				}),
			),
		}),
	),
	/**
	 * Scheduling mode for the service.
	 */
	Mode: v.exactOptional(
		v.strictObject({
			Replicated: v.exactOptional(
				v.strictObject({
					Replicas: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
			Global: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * The mode used for services with a finite number of tasks that run
			 * to a completed state.
			 */
			ReplicatedJob: v.exactOptional(
				v.strictObject({
					/**
					 * The maximum number of replicas to run simultaneously.
					 */
					MaxConcurrent: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The total number of replicas desired to reach the Completed
					 * state. If unset, will default to the value of `MaxConcurrent`
					 */
					TotalCompletions: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
			/**
			 * The mode used for services which run a task to the completed state
			 * on each valid node.
			 */
			GlobalJob: v.exactOptional(v.record(v.string(), v.unknown())),
		}),
	),
	/**
	 * Specification for the update strategy of the service.
	 */
	UpdateConfig: v.exactOptional(
		v.strictObject({
			/**
			 * Maximum number of tasks to be updated in one iteration (0 means
			 * unlimited parallelism).
			 */
			Parallelism: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * Amount of time between updates, in nanoseconds.
			 */
			Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * Action to take if an updated task fails to run, or stops running
			 * during the update.
			 */
			FailureAction: v.exactOptional(
				v.picklist(["continue", "pause", "rollback"]),
			),
			/**
			 * Amount of time to monitor each updated task for failures, in
			 * nanoseconds.
			 */
			Monitor: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The fraction of tasks that may fail during an update before the
			 * failure action is invoked, specified as a floating point number
			 * between 0 and 1.
			 */
			MaxFailureRatio: v.exactOptional(v.number()),
			/**
			 * The order of operations when rolling out an updated task. Either
			 * the old task is shut down before the new task is started, or the
			 * new task is started before the old task is shut down.
			 */
			Order: v.exactOptional(v.picklist(["stop-first", "start-first"])),
		}),
	),
	/**
	 * Specification for the rollback strategy of the service.
	 */
	RollbackConfig: v.exactOptional(
		v.strictObject({
			/**
			 * Maximum number of tasks to be rolled back in one iteration (0 means
			 * unlimited parallelism).
			 */
			Parallelism: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * Amount of time between rollback iterations, in nanoseconds.
			 */
			Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * Action to take if an rolled back task fails to run, or stops
			 * running during the rollback.
			 */
			FailureAction: v.exactOptional(v.picklist(["continue", "pause"])),
			/**
			 * Amount of time to monitor each rolled back task for failures, in
			 * nanoseconds.
			 */
			Monitor: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The fraction of tasks that may fail during a rollback before the
			 * failure action is invoked, specified as a floating point number
			 * between 0 and 1.
			 */
			MaxFailureRatio: v.exactOptional(v.number()),
			/**
			 * The order of operations when rolling back a task. Either the old
			 * task is shut down before the new task is started, or the new task
			 * is started before the old task is shut down.
			 */
			Order: v.exactOptional(v.picklist(["stop-first", "start-first"])),
		}),
	),
	/**
	 * Specifies which networks the service should attach to.
	 */
	Networks: v.exactOptional(
		v.array(
			v.strictObject({
				/**
				 * The target network for attachment. Must be a network name or ID.
				 */
				Target: v.exactOptional(v.string()),
				/**
				 * Discoverable alternate names for the service on this network.
				 */
				Aliases: v.exactOptional(v.array(v.string())),
				/**
				 * Driver attachment options for the network target.
				 */
				DriverOpts: v.exactOptional(v.record(v.string(), v.unknown())),
			}),
		),
	),
	/**
	 * Properties that can be configured to access and load balance a service.
	 */
	EndpointSpec: v.exactOptional(
		v.strictObject({
			/**
			 * The mode of resolution to use for internal load balancing between tasks.
			 */
			Mode: v.exactOptional(v.picklist(["vip", "dnsrr"])),
			/**
			 * List of exposed ports that this service is accessible on from the
			 * outside. Ports can only be provided if `vip` resolution mode is used.
			 */
			Ports: v.exactOptional(
				v.array(
					v.strictObject({
						Name: v.exactOptional(v.string()),
						Protocol: v.exactOptional(v.picklist(["tcp", "udp", "sctp"])),
						/**
						 * The port inside the container.
						 */
						TargetPort: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * The port on the swarm hosts.
						 */
						PublishedPort: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * The mode in which port is published.
						 *
						 * <p><br /></p>
						 *
						 * - "ingress" makes the target port accessible on every node,
						 *
						 * regardless of whether there is a task for the service running on
						 *
						 * that node or not.
						 * - "host" bypasses the routing mesh and publish the port directly on
						 *
						 * the swarm node where that service is running.
						 */
						PublishMode: v.exactOptional(v.picklist(["ingress", "host"])),
					}),
				),
			),
		}),
	),
});
export const endpointPortConfigSchema = v.strictObject({
	Name: v.exactOptional(v.string()),
	Protocol: v.exactOptional(v.picklist(["tcp", "udp", "sctp"])),
	/**
	 * The port inside the container.
	 */
	TargetPort: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * The port on the swarm hosts.
	 */
	PublishedPort: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * The mode in which port is published.
	 *
	 * <p><br /></p>
	 *
	 * - "ingress" makes the target port accessible on every node,
	 *
	 * regardless of whether there is a task for the service running on
	 *
	 * that node or not.
	 * - "host" bypasses the routing mesh and publish the port directly on
	 *
	 * the swarm node where that service is running.
	 */
	PublishMode: v.exactOptional(v.picklist(["ingress", "host"])),
});
/** Properties that can be configured to access and load balance a service. */
export const endpointSpecSchema = v.strictObject({
	/**
	 * The mode of resolution to use for internal load balancing between tasks.
	 */
	Mode: v.exactOptional(v.picklist(["vip", "dnsrr"])),
	/**
	 * List of exposed ports that this service is accessible on from the
	 * outside. Ports can only be provided if `vip` resolution mode is used.
	 */
	Ports: v.exactOptional(
		v.array(
			v.strictObject({
				Name: v.exactOptional(v.string()),
				Protocol: v.exactOptional(v.picklist(["tcp", "udp", "sctp"])),
				/**
				 * The port inside the container.
				 */
				TargetPort: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * The port on the swarm hosts.
				 */
				PublishedPort: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * The mode in which port is published.
				 *
				 * <p><br /></p>
				 *
				 * - "ingress" makes the target port accessible on every node,
				 *
				 * regardless of whether there is a task for the service running on
				 *
				 * that node or not.
				 * - "host" bypasses the routing mesh and publish the port directly on
				 *
				 * the swarm node where that service is running.
				 */
				PublishMode: v.exactOptional(v.picklist(["ingress", "host"])),
			}),
		),
	),
});
export const serviceSchema = v.strictObject({
	ID: v.exactOptional(v.string()),
	/**
	 * The version number of the object such as node, service, etc. This is needed
	 * to avoid conflicting writes. The client must send the version number along
	 * with the modified specification when updating these objects.
	 *
	 * This approach ensures safe concurrency and determinism in that the change
	 * on the object may not be applied if the version number has changed from the
	 * last read. In other words, if two update requests specify the same base
	 * version, only one of the requests can succeed. As a result, two separate
	 * update requests that happen at the same time will not unintentionally
	 * overwrite each other.
	 */
	Version: v.exactOptional(
		v.strictObject({
			Index: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	CreatedAt: v.exactOptional(v.string()),
	UpdatedAt: v.exactOptional(v.string()),
	/**
	 * User modifiable configuration for a service.
	 */
	Spec: v.exactOptional(
		v.strictObject({
			/**
			 * Name of the service.
			 */
			Name: v.exactOptional(v.string()),
			/**
			 * User-defined key/value metadata.
			 */
			Labels: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * User modifiable task configuration.
			 */
			TaskTemplate: v.exactOptional(
				v.strictObject({
					/**
					 * Plugin spec for the service.  *(Experimental release only.)*
					 *
					 * <p><br /></p>
					 *
					 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
					 * > mutually exclusive. PluginSpec is only used when the Runtime field
					 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
					 * > field is set to `attachment`.
					 */
					PluginSpec: v.exactOptional(
						v.strictObject({
							/**
							 * The name or 'alias' to use for the plugin.
							 */
							Name: v.exactOptional(v.string()),
							/**
							 * The plugin image reference to use.
							 */
							Remote: v.exactOptional(v.string()),
							/**
							 * Disable the plugin once scheduled.
							 */
							Disabled: v.exactOptional(v.boolean()),
							PluginPrivilege: v.exactOptional(
								v.array(
									v.strictObject({
										Name: v.exactOptional(v.string()),
										Description: v.exactOptional(v.string()),
										Value: v.exactOptional(v.array(v.string())),
									}),
								),
							),
						}),
					),
					/**
					 * Container spec for the service.
					 *
					 * <p><br /></p>
					 *
					 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
					 * > mutually exclusive. PluginSpec is only used when the Runtime field
					 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
					 * > field is set to `attachment`.
					 */
					ContainerSpec: v.exactOptional(
						v.strictObject({
							/**
							 * The image name to use for the container
							 */
							Image: v.exactOptional(v.string()),
							/**
							 * User-defined key/value data.
							 */
							Labels: v.exactOptional(v.record(v.string(), v.unknown())),
							/**
							 * The command to be run in the image.
							 */
							Command: v.exactOptional(v.array(v.string())),
							/**
							 * Arguments to the command.
							 */
							Args: v.exactOptional(v.array(v.string())),
							/**
							 * The hostname to use for the container, as a valid
							 * [RFC 1123](https://tools.ietf.org/html/rfc1123) hostname.
							 */
							Hostname: v.exactOptional(v.string()),
							/**
							 * A list of environment variables in the form `VAR=value`.
							 */
							Env: v.exactOptional(v.array(v.string())),
							/**
							 * The working directory for commands to run in.
							 */
							Dir: v.exactOptional(v.string()),
							/**
							 * The user inside the container.
							 */
							User: v.exactOptional(v.string()),
							/**
							 * A list of additional groups that the container process will run as.
							 */
							Groups: v.exactOptional(v.array(v.string())),
							/**
							 * Security options for the container
							 */
							Privileges: v.exactOptional(
								v.strictObject({
									/**
									 * CredentialSpec for managed service account (Windows only)
									 */
									CredentialSpec: v.exactOptional(
										v.strictObject({
											/**
											 * Load credential spec from a Swarm Config with the given ID.
											 * The specified config must also be present in the Configs
											 * field with the Runtime property set.
											 *
											 * <p><br /></p>
											 *
											 *
											 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
											 * > and `CredentialSpec.Config` are mutually exclusive.
											 */
											Config: v.exactOptional(v.string()),
											/**
											 * Load credential spec from this file. The file is read by
											 * the daemon, and must be present in the `CredentialSpecs`
											 * subdirectory in the docker data directory, which defaults
											 * to `C:\ProgramData\Docker\` on Windows.
											 *
											 * For example, specifying `spec.json` loads
											 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
											 *
											 * <p><br /></p>
											 *
											 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
											 * > and `CredentialSpec.Config` are mutually exclusive.
											 */
											File: v.exactOptional(v.string()),
											/**
											 * Load credential spec from this value in the Windows
											 * registry. The specified registry value must be located in:
											 *
											 * `HKLM\SOFTWARE\Microsoft\Windows
											 * NT\CurrentVersion\Virtualization\Containers\CredentialSpecs`
											 *
											 * <p><br /></p>
											 *
											 *
											 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
											 * > and `CredentialSpec.Config` are mutually exclusive.
											 */
											Registry: v.exactOptional(v.string()),
										}),
									),
									/**
									 * SELinux labels of the container
									 */
									SELinuxContext: v.exactOptional(
										v.strictObject({
											/**
											 * Disable SELinux
											 */
											Disable: v.exactOptional(v.boolean()),
											/**
											 * SELinux user label
											 */
											User: v.exactOptional(v.string()),
											/**
											 * SELinux role label
											 */
											Role: v.exactOptional(v.string()),
											/**
											 * SELinux type label
											 */
											Type: v.exactOptional(v.string()),
											/**
											 * SELinux level label
											 */
											Level: v.exactOptional(v.string()),
										}),
									),
								}),
							),
							/**
							 * Whether a pseudo-TTY should be allocated.
							 */
							TTY: v.exactOptional(v.boolean()),
							/**
							 * Open `stdin`
							 */
							OpenStdin: v.exactOptional(v.boolean()),
							/**
							 * Mount the container's root filesystem as read only.
							 */
							ReadOnly: v.exactOptional(v.boolean()),
							/**
							 * Specification for mounts to be added to containers created as part
							 * of the service.
							 */
							Mounts: v.exactOptional(
								v.array(
									v.strictObject({
										/**
										 * Container path.
										 */
										Target: v.exactOptional(v.string()),
										/**
										 * Mount source (e.g. a volume name, a host path).
										 */
										Source: v.exactOptional(v.string()),
										/**
										 * The mount type. Available types:
										 *
										 * - `bind` Mounts a file or directory from the host into the container. Must
										 * exist prior to creating the container.
										 * - `volume` Creates a volume with the given name and options (or uses a
										 * pre-existing volume with the same name and options). These are **not**
										 * removed when the container is removed.
										 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
										 * specified for tmpfs.
										 * - `npipe` Mounts a named pipe from the host into the container. Must exist
										 * prior to creating the container.
										 * - `cluster` a Swarm cluster volume
										 */
										Type: v.exactOptional(
											v.picklist([
												"bind",
												"volume",
												"tmpfs",
												"npipe",
												"cluster",
											]),
										),
										/**
										 * Whether the mount should be read-only.
										 */
										ReadOnly: v.exactOptional(v.boolean()),
										/**
										 * The consistency requirement for the mount: `default`, `consistent`,
										 * `cached`, or `delegated`.
										 */
										Consistency: v.exactOptional(v.string()),
										/**
										 * Optional configuration for the `bind` type.
										 */
										BindOptions: v.exactOptional(
											v.strictObject({
												/**
												 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
												 */
												Propagation: v.exactOptional(
													v.picklist([
														"private",
														"rprivate",
														"shared",
														"rshared",
														"slave",
														"rslave",
													]),
												),
												/**
												 * Disable recursive bind mount.
												 */
												NonRecursive: v.exactOptional(v.boolean()),
												/**
												 * Create mount point on host if missing
												 */
												CreateMountpoint: v.exactOptional(v.boolean()),
											}),
										),
										/**
										 * Optional configuration for the `volume` type.
										 */
										VolumeOptions: v.exactOptional(
											v.strictObject({
												/**
												 * Populate volume with data from the target.
												 */
												NoCopy: v.exactOptional(v.boolean()),
												/**
												 * User-defined key/value metadata.
												 */
												Labels: v.exactOptional(
													v.record(v.string(), v.unknown()),
												),
												/**
												 * Map of driver specific options
												 */
												DriverConfig: v.exactOptional(
													v.strictObject({
														/**
														 * Name of the driver to use to create the volume.
														 */
														Name: v.exactOptional(v.string()),
														/**
														 * key/value map of driver specific options.
														 */
														Options: v.exactOptional(
															v.record(v.string(), v.unknown()),
														),
													}),
												),
											}),
										),
										/**
										 * Optional configuration for the `tmpfs` type.
										 */
										TmpfsOptions: v.exactOptional(
											v.strictObject({
												/**
												 * The size for the tmpfs mount in bytes.
												 */
												SizeBytes: v.exactOptional(
													v.pipe(v.number(), v.integer()),
												),
												/**
												 * The permission mode for the tmpfs mount in an integer.
												 */
												Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
											}),
										),
									}),
								),
							),
							/**
							 * Signal to stop the container.
							 */
							StopSignal: v.exactOptional(v.string()),
							/**
							 * Amount of time to wait for the container to terminate before
							 * forcefully killing it.
							 */
							StopGracePeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * A test to perform to check that the container is healthy.
							 */
							HealthCheck: v.exactOptional(
								v.strictObject({
									/**
									 * The test to perform. Possible values are:
									 *
									 * - `[]` inherit healthcheck from image or parent image
									 * - `["NONE"]` disable healthcheck
									 * - `["CMD", args...]` exec arguments directly
									 * - `["CMD-SHELL", command]` run command with system's default shell
									 */
									Test: v.exactOptional(v.array(v.string())),
									/**
									 * The time to wait between checks in nanoseconds. It should be 0 or at
									 * least 1000000 (1 ms). 0 means inherit.
									 */
									Interval: v.exactOptional(v.pipe(v.number(), v.integer())),
									/**
									 * The time to wait before considering the check to have hung. It should
									 * be 0 or at least 1000000 (1 ms). 0 means inherit.
									 */
									Timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
									/**
									 * The number of consecutive failures needed to consider a container as
									 * unhealthy. 0 means inherit.
									 */
									Retries: v.exactOptional(v.pipe(v.number(), v.integer())),
									/**
									 * Start period for the container to initialize before starting
									 * health-retries countdown in nanoseconds. It should be 0 or at least
									 * 1000000 (1 ms). 0 means inherit.
									 */
									StartPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
								}),
							),
							/**
							 * A list of hostname/IP mappings to add to the container's `hosts`
							 * file. The format of extra hosts is specified in the
							 * [hosts(5)](http://man7.org/linux/man-pages/man5/hosts.5.html)
							 * man page:
							 *
							 *
							 * IP_address canonical_hostname [aliases...]
							 */
							Hosts: v.exactOptional(v.array(v.string())),
							/**
							 * Specification for DNS related configurations in resolver configuration
							 * file (`resolv.conf`).
							 */
							DNSConfig: v.exactOptional(
								v.strictObject({
									/**
									 * The IP addresses of the name servers.
									 */
									Nameservers: v.exactOptional(v.array(v.string())),
									/**
									 * A search list for host-name lookup.
									 */
									Search: v.exactOptional(v.array(v.string())),
									/**
									 * A list of internal resolver variables to be modified (e.g.,
									 * `debug`, `ndots:3`, etc.).
									 */
									Options: v.exactOptional(v.array(v.string())),
								}),
							),
							/**
							 * Secrets contains references to zero or more secrets that will be
							 * exposed to the service.
							 */
							Secrets: v.exactOptional(
								v.array(
									v.strictObject({
										/**
										 * File represents a specific target that is backed by a file.
										 */
										File: v.exactOptional(
											v.strictObject({
												/**
												 * Name represents the final filename in the filesystem.
												 */
												Name: v.exactOptional(v.string()),
												/**
												 * UID represents the file UID.
												 */
												UID: v.exactOptional(v.string()),
												/**
												 * GID represents the file GID.
												 */
												GID: v.exactOptional(v.string()),
												/**
												 * Mode represents the FileMode of the file.
												 */
												Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
											}),
										),
										/**
										 * SecretID represents the ID of the specific secret that we're
										 * referencing.
										 */
										SecretID: v.exactOptional(v.string()),
										/**
										 * SecretName is the name of the secret that this references,
										 * but this is just provided for lookup/display purposes. The
										 * secret in the reference will be identified by its ID.
										 */
										SecretName: v.exactOptional(v.string()),
									}),
								),
							),
							/**
							 * Configs contains references to zero or more configs that will be
							 * exposed to the service.
							 */
							Configs: v.exactOptional(
								v.array(
									v.strictObject({
										/**
										 * File represents a specific target that is backed by a file.
										 *
										 * <p><br /><p>
										 *
										 * > **Note**: `Configs.File` and `Configs.Runtime` are mutually exclusive
										 */
										File: v.exactOptional(
											v.strictObject({
												/**
												 * Name represents the final filename in the filesystem.
												 */
												Name: v.exactOptional(v.string()),
												/**
												 * UID represents the file UID.
												 */
												UID: v.exactOptional(v.string()),
												/**
												 * GID represents the file GID.
												 */
												GID: v.exactOptional(v.string()),
												/**
												 * Mode represents the FileMode of the file.
												 */
												Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
											}),
										),
										/**
										 * Runtime represents a target that is not mounted into the
										 * container but is used by the task
										 *
										 * <p><br /><p>
										 *
										 * > **Note**: `Configs.File` and `Configs.Runtime` are mutually
										 * > exclusive
										 */
										Runtime: v.exactOptional(v.record(v.string(), v.unknown())),
										/**
										 * ConfigID represents the ID of the specific config that we're
										 * referencing.
										 */
										ConfigID: v.exactOptional(v.string()),
										/**
										 * ConfigName is the name of the config that this references,
										 * but this is just provided for lookup/display purposes. The
										 * config in the reference will be identified by its ID.
										 */
										ConfigName: v.exactOptional(v.string()),
									}),
								),
							),
							/**
							 * Isolation technology of the containers running the service.
							 * (Windows only)
							 */
							Isolation: v.exactOptional(
								v.picklist(["default", "process", "hyperv", ""]),
							),
							/**
							 * Run an init inside the container that forwards signals and reaps
							 * processes. This field is omitted if empty, and the default (as
							 * configured on the daemon) is used.
							 */
							Init: v.exactOptional(v.nullable(v.boolean())),
							/**
							 * Set kernel namedspaced parameters (sysctls) in the container.
							 * The Sysctls option on services accepts the same sysctls as the
							 * are supported on containers. Note that while the same sysctls are
							 * supported, no guarantees or checks are made about their
							 * suitability for a clustered environment, and it's up to the user
							 * to determine whether a given sysctl will work properly in a
							 * Service.
							 */
							Sysctls: v.exactOptional(v.record(v.string(), v.unknown())),
							/**
							 * A list of kernel capabilities to add to the default set
							 * for the container.
							 */
							CapabilityAdd: v.exactOptional(v.array(v.string())),
							/**
							 * A list of kernel capabilities to drop from the default set
							 * for the container.
							 */
							CapabilityDrop: v.exactOptional(v.array(v.string())),
							/**
							 * A list of resource limits to set in the container. For example: `{"Name":
							 * "nofile", "Soft": 1024, "Hard": 2048}`"
							 */
							Ulimits: v.exactOptional(
								v.array(
									v.strictObject({
										/**
										 * Name of ulimit
										 */
										Name: v.exactOptional(v.string()),
										/**
										 * Soft limit
										 */
										Soft: v.exactOptional(v.pipe(v.number(), v.integer())),
										/**
										 * Hard limit
										 */
										Hard: v.exactOptional(v.pipe(v.number(), v.integer())),
									}),
								),
							),
						}),
					),
					/**
					 * Read-only spec type for non-swarm containers attached to swarm overlay
					 * networks.
					 *
					 * <p><br /></p>
					 *
					 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
					 * > mutually exclusive. PluginSpec is only used when the Runtime field
					 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
					 * > field is set to `attachment`.
					 */
					NetworkAttachmentSpec: v.exactOptional(
						v.strictObject({
							/**
							 * ID of the container represented by this task
							 */
							ContainerID: v.exactOptional(v.string()),
						}),
					),
					/**
					 * Resource requirements which apply to each individual container created
					 * as part of the service.
					 */
					Resources: v.exactOptional(
						v.strictObject({
							/**
							 * An object describing a limit on resources which can be requested by a task.
							 */
							Limits: v.exactOptional(
								v.strictObject({
									NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
									MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
									/**
									 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
									 */
									Pids: v.exactOptional(v.pipe(v.number(), v.integer())),
								}),
							),
							/**
							 * An object describing the resources which can be advertised by a node and
							 * requested by a task.
							 */
							Reservations: v.exactOptional(
								v.strictObject({
									NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
									MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
									/**
									 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
									 * String resources (e.g, `GPU=UUID1`).
									 */
									GenericResources: v.exactOptional(
										v.array(
											v.strictObject({
												NamedResourceSpec: v.exactOptional(
													v.strictObject({
														Kind: v.exactOptional(v.string()),
														Value: v.exactOptional(v.string()),
													}),
												),
												DiscreteResourceSpec: v.exactOptional(
													v.strictObject({
														Kind: v.exactOptional(v.string()),
														Value: v.exactOptional(
															v.pipe(v.number(), v.integer()),
														),
													}),
												),
											}),
										),
									),
								}),
							),
						}),
					),
					/**
					 * Specification for the restart policy which applies to containers
					 * created as part of this service.
					 */
					RestartPolicy: v.exactOptional(
						v.strictObject({
							/**
							 * Condition for restart.
							 */
							Condition: v.exactOptional(
								v.picklist(["none", "on-failure", "any"]),
							),
							/**
							 * Delay between restart attempts.
							 */
							Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * Maximum attempts to restart a given container before giving up
							 * (default value is 0, which is ignored).
							 */
							MaxAttempts: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * Windows is the time window used to evaluate the restart policy
							 * (default value is 0, which is unbounded).
							 */
							Window: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
					Placement: v.exactOptional(
						v.strictObject({
							/**
							 * An array of constraint expressions to limit the set of nodes where
							 * a task can be scheduled. Constraint expressions can either use a
							 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
							 * nodes that satisfy every expression (AND match). Constraints can
							 * match node or Docker Engine labels as follows:
							 *
							 * node attribute       | matches                        | example
							 * ---------------------|--------------------------------|-----------------------------------------------
							 * `node.id`            | Node ID                        |
							 * `node.id==2ivku8v2gvtg4`
							 * `node.hostname`      | Node hostname                  |
							 * `node.hostname!=node-2`
							 * `node.role`          | Node role (`manager`/`worker`) |
							 * `node.role==manager`
							 * `node.platform.os`   | Node operating system          |
							 * `node.platform.os==windows`
							 * `node.platform.arch` | Node architecture              |
							 * `node.platform.arch==x86_64`
							 * `node.labels`        | User-defined node labels       |
							 * `node.labels.security==high`
							 * `engine.labels`      | Docker Engine's labels         |
							 * `engine.labels.operatingsystem==ubuntu-14.04`
							 *
							 * `engine.labels` apply to Docker Engine labels like operating system,
							 * drivers, etc. Swarm administrators add `node.labels` for operational
							 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
							 */
							Constraints: v.exactOptional(v.array(v.string())),
							/**
							 * Preferences provide a way to make the scheduler aware of factors
							 * such as topology. They are provided in order from highest to
							 * lowest precedence.
							 */
							Preferences: v.exactOptional(
								v.array(
									v.strictObject({
										Spread: v.exactOptional(
											v.strictObject({
												/**
												 * label descriptor, such as `engine.labels.az`.
												 */
												SpreadDescriptor: v.exactOptional(v.string()),
											}),
										),
									}),
								),
							),
							/**
							 * Maximum number of replicas for per node (default value is 0, which
							 * is unlimited)
							 */
							MaxReplicas: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * Platforms stores all the platforms that the service's image can
							 * run on. This field is used in the platform filter for scheduling.
							 * If empty, then the platform filter is off, meaning there are no
							 * scheduling restrictions.
							 */
							Platforms: v.exactOptional(
								v.array(
									v.strictObject({
										/**
										 * Architecture represents the hardware architecture (for example,
										 * `x86_64`).
										 */
										Architecture: v.exactOptional(v.string()),
										/**
										 * OS represents the Operating System (for example, `linux` or `windows`).
										 */
										OS: v.exactOptional(v.string()),
									}),
								),
							),
						}),
					),
					/**
					 * A counter that triggers an update even if no relevant parameters have
					 * been changed.
					 */
					ForceUpdate: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Runtime is the type of runtime specified for the task executor.
					 */
					Runtime: v.exactOptional(v.string()),
					/**
					 * Specifies which networks the service should attach to.
					 */
					Networks: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * The target network for attachment. Must be a network name or ID.
								 */
								Target: v.exactOptional(v.string()),
								/**
								 * Discoverable alternate names for the service on this network.
								 */
								Aliases: v.exactOptional(v.array(v.string())),
								/**
								 * Driver attachment options for the network target.
								 */
								DriverOpts: v.exactOptional(v.record(v.string(), v.unknown())),
							}),
						),
					),
					/**
					 * Specifies the log driver to use for tasks created from this spec. If
					 * not present, the default one for the swarm will be used, finally
					 * falling back to the engine default if not specified.
					 */
					LogDriver: v.exactOptional(
						v.strictObject({
							Name: v.exactOptional(v.string()),
							Options: v.exactOptional(v.record(v.string(), v.unknown())),
						}),
					),
				}),
			),
			/**
			 * Scheduling mode for the service.
			 */
			Mode: v.exactOptional(
				v.strictObject({
					Replicated: v.exactOptional(
						v.strictObject({
							Replicas: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
					Global: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * The mode used for services with a finite number of tasks that run
					 * to a completed state.
					 */
					ReplicatedJob: v.exactOptional(
						v.strictObject({
							/**
							 * The maximum number of replicas to run simultaneously.
							 */
							MaxConcurrent: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The total number of replicas desired to reach the Completed
							 * state. If unset, will default to the value of `MaxConcurrent`
							 */
							TotalCompletions: v.exactOptional(
								v.pipe(v.number(), v.integer()),
							),
						}),
					),
					/**
					 * The mode used for services which run a task to the completed state
					 * on each valid node.
					 */
					GlobalJob: v.exactOptional(v.record(v.string(), v.unknown())),
				}),
			),
			/**
			 * Specification for the update strategy of the service.
			 */
			UpdateConfig: v.exactOptional(
				v.strictObject({
					/**
					 * Maximum number of tasks to be updated in one iteration (0 means
					 * unlimited parallelism).
					 */
					Parallelism: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Amount of time between updates, in nanoseconds.
					 */
					Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Action to take if an updated task fails to run, or stops running
					 * during the update.
					 */
					FailureAction: v.exactOptional(
						v.picklist(["continue", "pause", "rollback"]),
					),
					/**
					 * Amount of time to monitor each updated task for failures, in
					 * nanoseconds.
					 */
					Monitor: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The fraction of tasks that may fail during an update before the
					 * failure action is invoked, specified as a floating point number
					 * between 0 and 1.
					 */
					MaxFailureRatio: v.exactOptional(v.number()),
					/**
					 * The order of operations when rolling out an updated task. Either
					 * the old task is shut down before the new task is started, or the
					 * new task is started before the old task is shut down.
					 */
					Order: v.exactOptional(v.picklist(["stop-first", "start-first"])),
				}),
			),
			/**
			 * Specification for the rollback strategy of the service.
			 */
			RollbackConfig: v.exactOptional(
				v.strictObject({
					/**
					 * Maximum number of tasks to be rolled back in one iteration (0 means
					 * unlimited parallelism).
					 */
					Parallelism: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Amount of time between rollback iterations, in nanoseconds.
					 */
					Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Action to take if an rolled back task fails to run, or stops
					 * running during the rollback.
					 */
					FailureAction: v.exactOptional(v.picklist(["continue", "pause"])),
					/**
					 * Amount of time to monitor each rolled back task for failures, in
					 * nanoseconds.
					 */
					Monitor: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The fraction of tasks that may fail during a rollback before the
					 * failure action is invoked, specified as a floating point number
					 * between 0 and 1.
					 */
					MaxFailureRatio: v.exactOptional(v.number()),
					/**
					 * The order of operations when rolling back a task. Either the old
					 * task is shut down before the new task is started, or the new task
					 * is started before the old task is shut down.
					 */
					Order: v.exactOptional(v.picklist(["stop-first", "start-first"])),
				}),
			),
			/**
			 * Specifies which networks the service should attach to.
			 */
			Networks: v.exactOptional(
				v.array(
					v.strictObject({
						/**
						 * The target network for attachment. Must be a network name or ID.
						 */
						Target: v.exactOptional(v.string()),
						/**
						 * Discoverable alternate names for the service on this network.
						 */
						Aliases: v.exactOptional(v.array(v.string())),
						/**
						 * Driver attachment options for the network target.
						 */
						DriverOpts: v.exactOptional(v.record(v.string(), v.unknown())),
					}),
				),
			),
			/**
			 * Properties that can be configured to access and load balance a service.
			 */
			EndpointSpec: v.exactOptional(
				v.strictObject({
					/**
					 * The mode of resolution to use for internal load balancing between tasks.
					 */
					Mode: v.exactOptional(v.picklist(["vip", "dnsrr"])),
					/**
					 * List of exposed ports that this service is accessible on from the
					 * outside. Ports can only be provided if `vip` resolution mode is used.
					 */
					Ports: v.exactOptional(
						v.array(
							v.strictObject({
								Name: v.exactOptional(v.string()),
								Protocol: v.exactOptional(v.picklist(["tcp", "udp", "sctp"])),
								/**
								 * The port inside the container.
								 */
								TargetPort: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * The port on the swarm hosts.
								 */
								PublishedPort: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * The mode in which port is published.
								 *
								 * <p><br /></p>
								 *
								 * - "ingress" makes the target port accessible on every node,
								 *
								 * regardless of whether there is a task for the service running on
								 *
								 * that node or not.
								 * - "host" bypasses the routing mesh and publish the port directly on
								 *
								 * the swarm node where that service is running.
								 */
								PublishMode: v.exactOptional(v.picklist(["ingress", "host"])),
							}),
						),
					),
				}),
			),
		}),
	),
	Endpoint: v.exactOptional(
		v.strictObject({
			/**
			 * Properties that can be configured to access and load balance a service.
			 */
			Spec: v.exactOptional(
				v.strictObject({
					/**
					 * The mode of resolution to use for internal load balancing between tasks.
					 */
					Mode: v.exactOptional(v.picklist(["vip", "dnsrr"])),
					/**
					 * List of exposed ports that this service is accessible on from the
					 * outside. Ports can only be provided if `vip` resolution mode is used.
					 */
					Ports: v.exactOptional(
						v.array(
							v.strictObject({
								Name: v.exactOptional(v.string()),
								Protocol: v.exactOptional(v.picklist(["tcp", "udp", "sctp"])),
								/**
								 * The port inside the container.
								 */
								TargetPort: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * The port on the swarm hosts.
								 */
								PublishedPort: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * The mode in which port is published.
								 *
								 * <p><br /></p>
								 *
								 * - "ingress" makes the target port accessible on every node,
								 *
								 * regardless of whether there is a task for the service running on
								 *
								 * that node or not.
								 * - "host" bypasses the routing mesh and publish the port directly on
								 *
								 * the swarm node where that service is running.
								 */
								PublishMode: v.exactOptional(v.picklist(["ingress", "host"])),
							}),
						),
					),
				}),
			),
			Ports: v.exactOptional(
				v.array(
					v.strictObject({
						Name: v.exactOptional(v.string()),
						Protocol: v.exactOptional(v.picklist(["tcp", "udp", "sctp"])),
						/**
						 * The port inside the container.
						 */
						TargetPort: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * The port on the swarm hosts.
						 */
						PublishedPort: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * The mode in which port is published.
						 *
						 * <p><br /></p>
						 *
						 * - "ingress" makes the target port accessible on every node,
						 *
						 * regardless of whether there is a task for the service running on
						 *
						 * that node or not.
						 * - "host" bypasses the routing mesh and publish the port directly on
						 *
						 * the swarm node where that service is running.
						 */
						PublishMode: v.exactOptional(v.picklist(["ingress", "host"])),
					}),
				),
			),
			VirtualIPs: v.exactOptional(
				v.array(
					v.strictObject({
						NetworkID: v.exactOptional(v.string()),
						Addr: v.exactOptional(v.string()),
					}),
				),
			),
		}),
	),
	/**
	 * The status of a service update.
	 */
	UpdateStatus: v.exactOptional(
		v.strictObject({
			State: v.exactOptional(v.picklist(["updating", "paused", "completed"])),
			StartedAt: v.exactOptional(v.string()),
			CompletedAt: v.exactOptional(v.string()),
			Message: v.exactOptional(v.string()),
		}),
	),
	/**
	 * The status of the service's tasks. Provided only when requested as
	 * part of a ServiceList operation.
	 */
	ServiceStatus: v.exactOptional(
		v.strictObject({
			/**
			 * The number of tasks for the service currently in the Running state.
			 */
			RunningTasks: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The number of tasks for the service desired to be running.
			 * For replicated services, this is the replica count from the
			 * service spec. For global services, this is computed by taking
			 * count of all tasks for the service with a Desired State other
			 * than Shutdown.
			 */
			DesiredTasks: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The number of tasks for a job that are in the Completed state.
			 * This field must be cross-referenced with the service type, as the
			 * value of 0 may mean the service is not in a job mode, or it may
			 * mean the job-mode service has no tasks yet Completed.
			 */
			CompletedTasks: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	/**
	 * The status of the service when it is in one of ReplicatedJob or
	 * GlobalJob modes. Absent on Replicated and Global mode services. The
	 * JobIteration is an ObjectVersion, but unlike the Service's version,
	 * does not need to be sent with an update request.
	 */
	JobStatus: v.exactOptional(
		v.strictObject({
			/**
			 * The version number of the object such as node, service, etc. This is needed
			 * to avoid conflicting writes. The client must send the version number along
			 * with the modified specification when updating these objects.
			 *
			 * This approach ensures safe concurrency and determinism in that the change
			 * on the object may not be applied if the version number has changed from the
			 * last read. In other words, if two update requests specify the same base
			 * version, only one of the requests can succeed. As a result, two separate
			 * update requests that happen at the same time will not unintentionally
			 * overwrite each other.
			 */
			JobIteration: v.exactOptional(
				v.strictObject({
					Index: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
			/**
			 * The last time, as observed by the server, that this job was
			 * started.
			 */
			LastExecution: v.exactOptional(v.string()),
		}),
	),
});
export const imageDeleteResponseItemSchema = v.strictObject({
	/**
	 * The image ID of an image that was untagged
	 */
	Untagged: v.exactOptional(v.string()),
	/**
	 * The image ID of an image that was deleted
	 */
	Deleted: v.exactOptional(v.string()),
});
export const serviceUpdateResponseSchema = v.strictObject({
	/**
	 * Optional warning messages
	 */
	Warnings: v.exactOptional(v.array(v.string())),
});
export const containerSummarySchema = v.strictObject({
	/**
	 * The ID of this container
	 */
	Id: v.exactOptional(v.string()),
	/**
	 * The names that this container has been given
	 */
	Names: v.exactOptional(v.array(v.string())),
	/**
	 * The name of the image used when creating this container
	 */
	Image: v.exactOptional(v.string()),
	/**
	 * The ID of the image that this container was created from
	 */
	ImageID: v.exactOptional(v.string()),
	/**
	 * Command to run when starting the container
	 */
	Command: v.exactOptional(v.string()),
	/**
	 * When the container was created
	 */
	Created: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * The ports exposed by this container
	 */
	Ports: v.exactOptional(
		v.array(
			v.strictObject({
				/**
				 * Host IP address that the container's port is mapped to
				 */
				IP: v.exactOptional(v.string()),
				/**
				 * Port on the container
				 */
				PrivatePort: v.pipe(v.number(), v.integer()),
				/**
				 * Port exposed on the host
				 */
				PublicPort: v.exactOptional(v.pipe(v.number(), v.integer())),
				Type: v.picklist(["tcp", "udp", "sctp"]),
			}),
		),
	),
	/**
	 * The size of files that have been created or changed by this container
	 */
	SizeRw: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * The total size of all the files in this container
	 */
	SizeRootFs: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * The state of this container (e.g. `Exited`)
	 */
	State: v.exactOptional(v.string()),
	/**
	 * Additional human-readable status of this container (e.g. `Exit 0`)
	 */
	Status: v.exactOptional(v.string()),
	HostConfig: v.exactOptional(
		v.strictObject({
			NetworkMode: v.exactOptional(v.string()),
		}),
	),
	/**
	 * A summary of the container's network settings
	 */
	NetworkSettings: v.exactOptional(
		v.strictObject({
			Networks: v.exactOptional(v.record(v.string(), v.unknown())),
		}),
	),
	Mounts: v.exactOptional(
		v.array(
			v.strictObject({
				/**
				 * The mount type:
				 *
				 * - `bind` a mount of a file or directory from the host into the container.
				 * - `volume` a docker volume with the given `Name`.
				 * - `tmpfs` a `tmpfs`.
				 * - `npipe` a named pipe from the host into the container.
				 * - `cluster` a Swarm cluster volume
				 */
				Type: v.exactOptional(
					v.picklist(["bind", "volume", "tmpfs", "npipe", "cluster"]),
				),
				/**
				 * Name is the name reference to the underlying data defined by `Source`
				 * e.g., the volume name.
				 */
				Name: v.exactOptional(v.string()),
				/**
				 * Source location of the mount.
				 *
				 * For volumes, this contains the storage location of the volume (within
				 * `/var/lib/docker/volumes/`). For bind-mounts, and `npipe`, this contains
				 * the source (host) part of the bind-mount. For `tmpfs` mount points, this
				 * field is empty.
				 */
				Source: v.exactOptional(v.string()),
				/**
				 * Destination is the path relative to the container root (`/`) where
				 * the `Source` is mounted inside the container.
				 */
				Destination: v.exactOptional(v.string()),
				/**
				 * Driver is the volume driver used to create the volume (if it is a volume).
				 */
				Driver: v.exactOptional(v.string()),
				/**
				 * Mode is a comma separated list of options supplied by the user when
				 * creating the bind/volume mount.
				 *
				 * The default is platform-specific (`"z"` on Linux, empty on Windows).
				 */
				Mode: v.exactOptional(v.string()),
				/**
				 * Whether the mount is mounted writable (read-write).
				 */
				RW: v.exactOptional(v.boolean()),
				/**
				 * Propagation describes how mounts are propagated from the host into the
				 * mount point, and vice-versa. Refer to the [Linux kernel
				 * documentation](https://www.kernel.org/doc/Documentation/filesystems/sharedsubtree.txt)
				 * for details. This field is not used on Windows.
				 */
				Propagation: v.exactOptional(v.string()),
			}),
		),
	),
});
/** Driver represents a driver (network, logging, secrets). */
export const driverSchema = v.strictObject({
	/**
	 * Name of the driver.
	 */
	Name: v.string(),
	/**
	 * Key/value map of driver-specific options.
	 */
	Options: v.exactOptional(v.record(v.string(), v.unknown())),
});
export const secretSpecSchema = v.strictObject({
	/**
	 * User-defined name of the secret.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Base64-url-safe-encoded ([RFC
	 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
	 * data to store as secret.
	 *
	 * This field is only used to _create_ a secret, and is not returned by
	 * other endpoints.
	 */
	Data: v.exactOptional(v.string()),
	/**
	 * Driver represents a driver (network, logging, secrets).
	 */
	Driver: v.exactOptional(
		v.strictObject({
			/**
			 * Name of the driver.
			 */
			Name: v.string(),
			/**
			 * Key/value map of driver-specific options.
			 */
			Options: v.exactOptional(v.record(v.string(), v.unknown())),
		}),
	),
	/**
	 * Driver represents a driver (network, logging, secrets).
	 */
	Templating: v.exactOptional(
		v.strictObject({
			/**
			 * Name of the driver.
			 */
			Name: v.string(),
			/**
			 * Key/value map of driver-specific options.
			 */
			Options: v.exactOptional(v.record(v.string(), v.unknown())),
		}),
	),
});
export const secretSchema = v.strictObject({
	ID: v.exactOptional(v.string()),
	/**
	 * The version number of the object such as node, service, etc. This is needed
	 * to avoid conflicting writes. The client must send the version number along
	 * with the modified specification when updating these objects.
	 *
	 * This approach ensures safe concurrency and determinism in that the change
	 * on the object may not be applied if the version number has changed from the
	 * last read. In other words, if two update requests specify the same base
	 * version, only one of the requests can succeed. As a result, two separate
	 * update requests that happen at the same time will not unintentionally
	 * overwrite each other.
	 */
	Version: v.exactOptional(
		v.strictObject({
			Index: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	CreatedAt: v.exactOptional(v.string()),
	UpdatedAt: v.exactOptional(v.string()),
	Spec: v.exactOptional(
		v.strictObject({
			/**
			 * User-defined name of the secret.
			 */
			Name: v.exactOptional(v.string()),
			/**
			 * User-defined key/value metadata.
			 */
			Labels: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * Base64-url-safe-encoded ([RFC
			 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
			 * data to store as secret.
			 *
			 * This field is only used to _create_ a secret, and is not returned by
			 * other endpoints.
			 */
			Data: v.exactOptional(v.string()),
			/**
			 * Driver represents a driver (network, logging, secrets).
			 */
			Driver: v.exactOptional(
				v.strictObject({
					/**
					 * Name of the driver.
					 */
					Name: v.string(),
					/**
					 * Key/value map of driver-specific options.
					 */
					Options: v.exactOptional(v.record(v.string(), v.unknown())),
				}),
			),
			/**
			 * Driver represents a driver (network, logging, secrets).
			 */
			Templating: v.exactOptional(
				v.strictObject({
					/**
					 * Name of the driver.
					 */
					Name: v.string(),
					/**
					 * Key/value map of driver-specific options.
					 */
					Options: v.exactOptional(v.record(v.string(), v.unknown())),
				}),
			),
		}),
	),
});
export const configSpecSchema = v.strictObject({
	/**
	 * User-defined name of the config.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Base64-url-safe-encoded ([RFC
	 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
	 * config data.
	 */
	Data: v.exactOptional(v.string()),
	/**
	 * Driver represents a driver (network, logging, secrets).
	 */
	Templating: v.exactOptional(
		v.strictObject({
			/**
			 * Name of the driver.
			 */
			Name: v.string(),
			/**
			 * Key/value map of driver-specific options.
			 */
			Options: v.exactOptional(v.record(v.string(), v.unknown())),
		}),
	),
});
export const configSchema = v.strictObject({
	ID: v.exactOptional(v.string()),
	/**
	 * The version number of the object such as node, service, etc. This is needed
	 * to avoid conflicting writes. The client must send the version number along
	 * with the modified specification when updating these objects.
	 *
	 * This approach ensures safe concurrency and determinism in that the change
	 * on the object may not be applied if the version number has changed from the
	 * last read. In other words, if two update requests specify the same base
	 * version, only one of the requests can succeed. As a result, two separate
	 * update requests that happen at the same time will not unintentionally
	 * overwrite each other.
	 */
	Version: v.exactOptional(
		v.strictObject({
			Index: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	CreatedAt: v.exactOptional(v.string()),
	UpdatedAt: v.exactOptional(v.string()),
	Spec: v.exactOptional(
		v.strictObject({
			/**
			 * User-defined name of the config.
			 */
			Name: v.exactOptional(v.string()),
			/**
			 * User-defined key/value metadata.
			 */
			Labels: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * Base64-url-safe-encoded ([RFC
			 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
			 * config data.
			 */
			Data: v.exactOptional(v.string()),
			/**
			 * Driver represents a driver (network, logging, secrets).
			 */
			Templating: v.exactOptional(
				v.strictObject({
					/**
					 * Name of the driver.
					 */
					Name: v.string(),
					/**
					 * Key/value map of driver-specific options.
					 */
					Options: v.exactOptional(v.record(v.string(), v.unknown())),
				}),
			),
		}),
	),
});
/**
 * ContainerState stores container's running state. It's part of
 * ContainerJSONBase
 * and will be returned by the "inspect" command.
 */
export const containerStateSchema = v.nullable(
	v.strictObject({
		/**
		 * String representation of the container state. Can be one of "created",
		 * "running", "paused", "restarting", "removing", "exited", or "dead".
		 */
		Status: v.exactOptional(
			v.picklist([
				"created",
				"running",
				"paused",
				"restarting",
				"removing",
				"exited",
				"dead",
			]),
		),
		/**
		 * Whether this container is running.
		 *
		 * Note that a running container can be _paused_. The `Running` and `Paused`
		 * booleans are not mutually exclusive:
		 *
		 * When pausing a container (on Linux), the freezer cgroup is used to suspend
		 * all processes in the container. Freezing the process requires the process
		 * to
		 * be running. As a result, paused containers are both `Running` _and_
		 * `Paused`.
		 *
		 * Use the `Status` field instead to determine if a container's state is
		 * "running".
		 */
		Running: v.exactOptional(v.boolean()),
		/**
		 * Whether this container is paused.
		 */
		Paused: v.exactOptional(v.boolean()),
		/**
		 * Whether this container is restarting.
		 */
		Restarting: v.exactOptional(v.boolean()),
		/**
		 * Whether a process within this container has been killed because it ran
		 * out of memory since the container was last started.
		 */
		OOMKilled: v.exactOptional(v.boolean()),
		Dead: v.exactOptional(v.boolean()),
		/**
		 * The process ID of this container
		 */
		Pid: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * The last exit code of this container
		 */
		ExitCode: v.exactOptional(v.pipe(v.number(), v.integer())),
		Error: v.exactOptional(v.string()),
		/**
		 * The time when this container was last started.
		 */
		StartedAt: v.exactOptional(v.string()),
		/**
		 * The time when this container last exited.
		 */
		FinishedAt: v.exactOptional(v.string()),
		/**
		 * Health stores information about the container's healthcheck results.
		 */
		Health: v.exactOptional(
			v.nullable(
				v.strictObject({
					/**
					 * Status is one of `none`, `starting`, `healthy` or `unhealthy`
					 *
					 * - "none"      Indicates there is no healthcheck
					 * - "starting"  Starting indicates that the container is not yet ready
					 * - "healthy"   Healthy indicates that the container is running correctly
					 * - "unhealthy" Unhealthy indicates that the container has a problem
					 */
					Status: v.exactOptional(
						v.picklist(["none", "starting", "healthy", "unhealthy"]),
					),
					/**
					 * FailingStreak is the number of consecutive failures
					 */
					FailingStreak: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Log contains the last few results (oldest first)
					 */
					Log: v.exactOptional(
						v.array(
							v.nullable(
								v.strictObject({
									/**
									 * Date and time at which this check started in
									 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
									 */
									Start: v.exactOptional(v.string()),
									/**
									 * Date and time at which this check ended in
									 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
									 */
									End: v.exactOptional(v.string()),
									/**
									 * ExitCode meanings:
									 *
									 * - `0` healthy
									 * - `1` unhealthy
									 * - `2` reserved (considered unhealthy)
									 * - other values: error running probe
									 */
									ExitCode: v.exactOptional(v.pipe(v.number(), v.integer())),
									/**
									 * Output from last check
									 */
									Output: v.exactOptional(v.string()),
								}),
							),
						),
					),
				}),
			),
		),
	}),
);
/**
 * OK response to ContainerCreate operation
 * @title ContainerCreateResponse
 */
export const containerCreateResponseSchema = v.strictObject({
	/**
	 * The ID of the created container
	 */
	Id: v.string(),
	/**
	 * Warnings encountered when creating the container
	 */
	Warnings: v.array(v.string()),
});
/**
 * OK response to ContainerWait operation
 * @title ContainerWaitResponse
 */
export const containerWaitResponseSchema = v.strictObject({
	/**
	 * Exit code of the container
	 */
	StatusCode: v.pipe(v.number(), v.integer()),
	/**
	 * container waiting error, if any
	 */
	Error: v.exactOptional(
		v.strictObject({
			/**
			 * Details of an error
			 */
			Message: v.exactOptional(v.string()),
		}),
	),
});
/** container waiting error, if any */
export const containerWaitExitErrorSchema = v.strictObject({
	/**
	 * Details of an error
	 */
	Message: v.exactOptional(v.string()),
});
/** Response of Engine API: GET "/version" */
export const systemVersionSchema = v.strictObject({
	Platform: v.exactOptional(
		v.strictObject({
			Name: v.string(),
		}),
	),
	/**
	 * Information about system components
	 */
	Components: v.exactOptional(
		v.array(
			v.strictObject({
				/**
				 * Name of the component
				 */
				Name: v.string(),
				/**
				 * Version of the component
				 */
				Version: v.string(),
				/**
				 * Key/value pairs of strings with additional information about the
				 * component. These values are intended for informational purposes
				 * only, and their content is not defined, and not part of the API
				 * specification.
				 *
				 * These messages can be printed by the client as information to the user.
				 */
				Details: v.exactOptional(v.nullable(v.record(v.string(), v.unknown()))),
			}),
		),
	),
	/**
	 * The version of the daemon
	 */
	Version: v.exactOptional(v.string()),
	/**
	 * The default (and highest) API version that is supported by the daemon
	 */
	ApiVersion: v.exactOptional(v.string()),
	/**
	 * The minimum API version that is supported by the daemon
	 */
	MinAPIVersion: v.exactOptional(v.string()),
	/**
	 * The Git commit of the source code that was used to build the daemon
	 */
	GitCommit: v.exactOptional(v.string()),
	/**
	 * The version Go used to compile the daemon, and the version of the Go
	 * runtime in use.
	 */
	GoVersion: v.exactOptional(v.string()),
	/**
	 * The operating system that the daemon is running on ("linux" or "windows")
	 */
	Os: v.exactOptional(v.string()),
	/**
	 * The architecture that the daemon is running on
	 */
	Arch: v.exactOptional(v.string()),
	/**
	 * The kernel version (`uname -r`) that the daemon is running on.
	 *
	 * This field is omitted when empty.
	 */
	KernelVersion: v.exactOptional(v.string()),
	/**
	 * Indicates if the daemon is started with experimental features enabled.
	 *
	 * This field is omitted when empty / false.
	 */
	Experimental: v.exactOptional(v.boolean()),
	/**
	 * The date and time that the daemon was compiled.
	 */
	BuildTime: v.exactOptional(v.string()),
});
export const systemInfoSchema = v.strictObject({
	/**
	 * Unique identifier of the daemon.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: The format of the ID itself is not part of the API, and
	 * > should not be considered stable.
	 */
	ID: v.exactOptional(v.string()),
	/**
	 * Total number of containers on the host.
	 */
	Containers: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Number of containers with status `"running"`.
	 */
	ContainersRunning: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Number of containers with status `"paused"`.
	 */
	ContainersPaused: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Number of containers with status `"stopped"`.
	 */
	ContainersStopped: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Total number of images on the host.
	 *
	 * Both _tagged_ and _untagged_ (dangling) images are counted.
	 */
	Images: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Name of the storage driver in use.
	 */
	Driver: v.exactOptional(v.string()),
	/**
	 * Information specific to the storage driver, provided as
	 * "label" / "value" pairs.
	 *
	 * This information is provided by the storage driver, and formatted
	 * in a way consistent with the output of `docker info` on the command
	 * line.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: The information returned in this field, including the
	 * > formatting of values and labels, should not be considered stable,
	 * > and may change without notice.
	 */
	DriverStatus: v.exactOptional(v.array(v.array(v.string()))),
	/**
	 * Root directory of persistent Docker state.
	 *
	 * Defaults to `/var/lib/docker` on Linux, and `C:\ProgramData\docker`
	 * on Windows.
	 */
	DockerRootDir: v.exactOptional(v.string()),
	/**
	 * Available plugins per type.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: Only unmanaged (V1) plugins are included in this list.
	 * > V1 plugins are "lazily" loaded, and are not returned in this list
	 * > if there is no resource using the plugin.
	 */
	Plugins: v.exactOptional(
		v.strictObject({
			/**
			 * Names of available volume-drivers, and network-driver plugins.
			 */
			Volume: v.exactOptional(v.array(v.string())),
			/**
			 * Names of available network-drivers, and network-driver plugins.
			 */
			Network: v.exactOptional(v.array(v.string())),
			/**
			 * Names of available authorization plugins.
			 */
			Authorization: v.exactOptional(v.array(v.string())),
			/**
			 * Names of available logging-drivers, and logging-driver plugins.
			 */
			Log: v.exactOptional(v.array(v.string())),
		}),
	),
	/**
	 * Indicates if the host has memory limit support enabled.
	 */
	MemoryLimit: v.exactOptional(v.boolean()),
	/**
	 * Indicates if the host has memory swap limit support enabled.
	 */
	SwapLimit: v.exactOptional(v.boolean()),
	/**
	 * Indicates if the host has kernel memory TCP limit support enabled. This
	 * field is omitted if not supported.
	 *
	 * Kernel memory TCP limits are not supported when using cgroups v2, which
	 * does not support the corresponding `memory.kmem.tcp.limit_in_bytes` cgroup.
	 */
	KernelMemoryTCP: v.exactOptional(v.boolean()),
	/**
	 * Indicates if CPU CFS(Completely Fair Scheduler) period is supported by
	 * the host.
	 */
	CpuCfsPeriod: v.exactOptional(v.boolean()),
	/**
	 * Indicates if CPU CFS(Completely Fair Scheduler) quota is supported by
	 * the host.
	 */
	CpuCfsQuota: v.exactOptional(v.boolean()),
	/**
	 * Indicates if CPU Shares limiting is supported by the host.
	 */
	CPUShares: v.exactOptional(v.boolean()),
	/**
	 * Indicates if CPUsets (cpuset.cpus, cpuset.mems) are supported by the host.
	 *
	 * See
	 * [cpuset(7)](https://www.kernel.org/doc/Documentation/cgroup-v1/cpusets.txt)
	 */
	CPUSet: v.exactOptional(v.boolean()),
	/**
	 * Indicates if the host kernel has PID limit support enabled.
	 */
	PidsLimit: v.exactOptional(v.boolean()),
	/**
	 * Indicates if OOM killer disable is supported on the host.
	 */
	OomKillDisable: v.exactOptional(v.boolean()),
	/**
	 * Indicates IPv4 forwarding is enabled.
	 */
	IPv4Forwarding: v.exactOptional(v.boolean()),
	/**
	 * Indicates if `bridge-nf-call-iptables` is available on the host.
	 */
	BridgeNfIptables: v.exactOptional(v.boolean()),
	/**
	 * Indicates if `bridge-nf-call-ip6tables` is available on the host.
	 */
	BridgeNfIp6tables: v.exactOptional(v.boolean()),
	/**
	 * Indicates if the daemon is running in debug-mode / with debug-level
	 * logging enabled.
	 */
	Debug: v.exactOptional(v.boolean()),
	/**
	 * The total number of file Descriptors in use by the daemon process.
	 *
	 * This information is only returned if debug-mode is enabled.
	 */
	NFd: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * The  number of goroutines that currently exist.
	 *
	 * This information is only returned if debug-mode is enabled.
	 */
	NGoroutines: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Current system-time in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt)
	 * format with nano-seconds.
	 */
	SystemTime: v.exactOptional(v.string()),
	/**
	 * The logging driver to use as a default for new containers.
	 */
	LoggingDriver: v.exactOptional(v.string()),
	/**
	 * The driver to use for managing cgroups.
	 */
	CgroupDriver: v.exactOptional(v.picklist(["cgroupfs", "systemd", "none"])),
	/**
	 * The version of the cgroup.
	 */
	CgroupVersion: v.exactOptional(v.picklist(["1", "2"])),
	/**
	 * Number of event listeners subscribed.
	 */
	NEventsListener: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Kernel version of the host.
	 *
	 * On Linux, this information obtained from `uname`. On Windows this
	 * information is queried from the
	 * <kbd>HKEY_LOCAL_MACHINE\\SOFTWARE\\Microsoft\\Windows
	 * NT\\CurrentVersion\\</kbd>
	 * registry value, for example _"10.0 14393
	 * (14393.1198.amd64fre.rs1_release_sec.170427-1353)"_.
	 */
	KernelVersion: v.exactOptional(v.string()),
	/**
	 * Name of the host's operating system, for example: "Ubuntu 16.04.2 LTS"
	 * or "Windows Server 2016 Datacenter"
	 */
	OperatingSystem: v.exactOptional(v.string()),
	/**
	 * Version of the host's operating system
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: The information returned in this field, including its
	 * > very existence, and the formatting of values, should not be considered
	 * > stable, and may change without notice.
	 */
	OSVersion: v.exactOptional(v.string()),
	/**
	 * Generic type of the operating system of the host, as returned by the
	 * Go runtime (`GOOS`).
	 *
	 * Currently returned values are "linux" and "windows". A full list of
	 * possible values can be found in the [Go
	 * documentation](https://go.dev/doc/install/source#environment).
	 */
	OSType: v.exactOptional(v.string()),
	/**
	 * Hardware architecture of the host, as returned by the Go runtime
	 * (`GOARCH`).
	 *
	 * A full list of possible values can be found in the [Go
	 * documentation](https://go.dev/doc/install/source#environment).
	 */
	Architecture: v.exactOptional(v.string()),
	/**
	 * The number of logical CPUs usable by the daemon.
	 *
	 * The number of available CPUs is checked by querying the operating
	 * system when the daemon starts. Changes to operating system CPU
	 * allocation after the daemon is started are not reflected.
	 */
	NCPU: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Total amount of physical memory available on the host, in bytes.
	 */
	MemTotal: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Address / URL of the index server that is used for image search,
	 * and as a default for user authentication for Docker Hub and Docker Cloud.
	 */
	IndexServerAddress: v.exactOptional(v.string()),
	/**
	 * RegistryServiceConfig stores daemon registry services configuration.
	 */
	RegistryConfig: v.exactOptional(
		v.nullable(
			v.strictObject({
				/**
				 * List of IP ranges to which nondistributable artifacts can be pushed,
				 * using the CIDR syntax [RFC 4632](https://tools.ietf.org/html/4632).
				 *
				 * Some images (for example, Windows base images) contain artifacts
				 * whose distribution is restricted by license. When these images are
				 * pushed to a registry, restricted artifacts are not included.
				 *
				 * This configuration override this behavior, and enables the daemon to
				 * push nondistributable artifacts to all registries whose resolved IP
				 * address is within the subnet described by the CIDR syntax.
				 *
				 * This option is useful when pushing images containing
				 * nondistributable artifacts to a registry on an air-gapped network so
				 * hosts on that network can pull the images without connecting to
				 * another server.
				 *
				 * > **Warning**: Nondistributable artifacts typically have restrictions
				 * > on how and where they can be distributed and shared. Only use this
				 * > feature to push artifacts to private registries and ensure that you
				 * > are in compliance with any terms that cover redistributing
				 * > nondistributable artifacts.
				 */
				AllowNondistributableArtifactsCIDRs: v.exactOptional(
					v.array(v.string()),
				),
				/**
				 * List of registry hostnames to which nondistributable artifacts can be
				 * pushed, using the format `<hostname>[:<port>]` or `<IP address>[:<port>]`.
				 *
				 * Some images (for example, Windows base images) contain artifacts
				 * whose distribution is restricted by license. When these images are
				 * pushed to a registry, restricted artifacts are not included.
				 *
				 * This configuration override this behavior for the specified
				 * registries.
				 *
				 * This option is useful when pushing images containing
				 * nondistributable artifacts to a registry on an air-gapped network so
				 * hosts on that network can pull the images without connecting to
				 * another server.
				 *
				 * > **Warning**: Nondistributable artifacts typically have restrictions
				 * > on how and where they can be distributed and shared. Only use this
				 * > feature to push artifacts to private registries and ensure that you
				 * > are in compliance with any terms that cover redistributing
				 * > nondistributable artifacts.
				 */
				AllowNondistributableArtifactsHostnames: v.exactOptional(
					v.array(v.string()),
				),
				/**
				 * List of IP ranges of insecure registries, using the CIDR syntax
				 * ([RFC 4632](https://tools.ietf.org/html/4632)). Insecure registries
				 * accept un-encrypted (HTTP) and/or untrusted (HTTPS with certificates
				 * from unknown CAs) communication.
				 *
				 * By default, local registries (`127.0.0.0/8`) are configured as
				 * insecure. All other registries are secure. Communicating with an
				 * insecure registry is not possible if the daemon assumes that registry
				 * is secure.
				 *
				 * This configuration override this behavior, insecure communication with
				 * registries whose resolved IP address is within the subnet described by
				 * the CIDR syntax.
				 *
				 * Registries can also be marked insecure by hostname. Those registries
				 * are listed under `IndexConfigs` and have their `Secure` field set to
				 * `false`.
				 *
				 * > **Warning**: Using this option can be useful when running a local
				 * > registry, but introduces security vulnerabilities. This option
				 * > should therefore ONLY be used for testing purposes. For increased
				 * > security, users should add their CA to their system's list of trusted
				 * > CAs instead of enabling this option.
				 */
				InsecureRegistryCIDRs: v.exactOptional(v.array(v.string())),
				IndexConfigs: v.exactOptional(v.record(v.string(), v.unknown())),
				/**
				 * List of registry URLs that act as a mirror for the official
				 * (`docker.io`) registry.
				 */
				Mirrors: v.exactOptional(v.array(v.string())),
			}),
		),
	),
	/**
	 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
	 * String resources (e.g, `GPU=UUID1`).
	 */
	GenericResources: v.exactOptional(
		v.array(
			v.strictObject({
				NamedResourceSpec: v.exactOptional(
					v.strictObject({
						Kind: v.exactOptional(v.string()),
						Value: v.exactOptional(v.string()),
					}),
				),
				DiscreteResourceSpec: v.exactOptional(
					v.strictObject({
						Kind: v.exactOptional(v.string()),
						Value: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
			}),
		),
	),
	/**
	 * HTTP-proxy configured for the daemon. This value is obtained from the
	 * [`HTTP_PROXY`](https://www.gnu.org/software/wget/manual/html_node/Proxies.html)
	 * environment variable.
	 * Credentials ([user info
	 * component](https://tools.ietf.org/html/rfc3986#section-3.2.1)) in the proxy
	 * URL
	 * are masked in the API response.
	 *
	 * Containers do not automatically inherit this configuration.
	 */
	HttpProxy: v.exactOptional(v.string()),
	/**
	 * HTTPS-proxy configured for the daemon. This value is obtained from the
	 * [`HTTPS_PROXY`](https://www.gnu.org/software/wget/manual/html_node/Proxies.html)
	 * environment variable.
	 * Credentials ([user info
	 * component](https://tools.ietf.org/html/rfc3986#section-3.2.1)) in the proxy
	 * URL
	 * are masked in the API response.
	 *
	 * Containers do not automatically inherit this configuration.
	 */
	HttpsProxy: v.exactOptional(v.string()),
	/**
	 * Comma-separated list of domain extensions for which no proxy should be
	 * used. This value is obtained from the
	 * [`NO_PROXY`](https://www.gnu.org/software/wget/manual/html_node/Proxies.html)
	 * environment variable.
	 *
	 * Containers do not automatically inherit this configuration.
	 */
	NoProxy: v.exactOptional(v.string()),
	/**
	 * Hostname of the host.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * User-defined labels (key/value metadata) as set on the daemon.
	 *
	 * <p><br /></p>
	 *
	 * > **Note**: When part of a Swarm, nodes can both have _daemon_ labels,
	 * > set through the daemon configuration, and _node_ labels, set from a
	 * > manager node in the Swarm. Node labels are not included in this
	 * > field. Node labels can be retrieved using the `/nodes/(id)` endpoint
	 * > on a manager node in the Swarm.
	 */
	Labels: v.exactOptional(v.array(v.string())),
	/**
	 * Indicates if experimental features are enabled on the daemon.
	 */
	ExperimentalBuild: v.exactOptional(v.boolean()),
	/**
	 * Version string of the daemon.
	 */
	ServerVersion: v.exactOptional(v.string()),
	/**
	 * List of [OCI compliant](https://github.com/opencontainers/runtime-spec)
	 * runtimes configured on the daemon. Keys hold the "name" used to
	 * reference the runtime.
	 *
	 * The Docker daemon relies on an OCI compliant runtime (invoked via the
	 * `containerd` daemon) as its interface to the Linux kernel namespaces,
	 * cgroups, and SELinux.
	 *
	 * The default runtime is `runc`, and automatically configured. Additional
	 * runtimes can be configured by the user and will be listed here.
	 */
	Runtimes: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Name of the default OCI runtime that is used when starting containers.
	 *
	 * The default can be overridden per-container at create time.
	 */
	DefaultRuntime: v.exactOptional(v.string()),
	/**
	 * Represents generic information about swarm.
	 */
	Swarm: v.exactOptional(
		v.strictObject({
			/**
			 * Unique identifier of for this node in the swarm.
			 */
			NodeID: v.exactOptional(v.string()),
			/**
			 * IP address at which this node can be reached by other nodes in the
			 * swarm.
			 */
			NodeAddr: v.exactOptional(v.string()),
			/**
			 * Current local status of this node.
			 */
			LocalNodeState: v.exactOptional(
				v.picklist(["", "inactive", "pending", "active", "error", "locked"]),
			),
			ControlAvailable: v.exactOptional(v.boolean()),
			Error: v.exactOptional(v.string()),
			/**
			 * List of ID's and addresses of other managers in the swarm.
			 */
			RemoteManagers: v.exactOptional(
				v.nullable(
					v.array(
						v.strictObject({
							/**
							 * Unique identifier of for this node in the swarm.
							 */
							NodeID: v.exactOptional(v.string()),
							/**
							 * IP address and ports at which this node can be reached.
							 */
							Addr: v.exactOptional(v.string()),
						}),
					),
				),
			),
			/**
			 * Total number of nodes in the swarm.
			 */
			Nodes: v.exactOptional(v.nullable(v.pipe(v.number(), v.integer()))),
			/**
			 * Total number of managers in the swarm.
			 */
			Managers: v.exactOptional(v.nullable(v.pipe(v.number(), v.integer()))),
			/**
			 * ClusterInfo represents information about the swarm as is returned by the
			 * "/info" endpoint. Join-tokens are not included.
			 */
			Cluster: v.exactOptional(
				v.nullable(
					v.strictObject({
						/**
						 * The ID of the swarm.
						 */
						ID: v.exactOptional(v.string()),
						/**
						 * The version number of the object such as node, service, etc. This is needed
						 * to avoid conflicting writes. The client must send the version number along
						 * with the modified specification when updating these objects.
						 *
						 * This approach ensures safe concurrency and determinism in that the change
						 * on the object may not be applied if the version number has changed from the
						 * last read. In other words, if two update requests specify the same base
						 * version, only one of the requests can succeed. As a result, two separate
						 * update requests that happen at the same time will not unintentionally
						 * overwrite each other.
						 */
						Version: v.exactOptional(
							v.strictObject({
								Index: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
						/**
						 * Date and time at which the swarm was initialised in
						 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
						 */
						CreatedAt: v.exactOptional(v.string()),
						/**
						 * Date and time at which the swarm was last updated in
						 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
						 */
						UpdatedAt: v.exactOptional(v.string()),
						/**
						 * User modifiable swarm configuration.
						 */
						Spec: v.exactOptional(
							v.strictObject({
								/**
								 * Name of the swarm.
								 */
								Name: v.exactOptional(v.string()),
								/**
								 * User-defined key/value metadata.
								 */
								Labels: v.exactOptional(v.record(v.string(), v.unknown())),
								/**
								 * Orchestration configuration.
								 */
								Orchestration: v.exactOptional(
									v.nullable(
										v.strictObject({
											/**
											 * The number of historic tasks to keep per instance or node. If
											 * negative, never remove completed or failed tasks.
											 */
											TaskHistoryRetentionLimit: v.exactOptional(
												v.pipe(v.number(), v.integer()),
											),
										}),
									),
								),
								/**
								 * Raft configuration.
								 */
								Raft: v.exactOptional(
									v.strictObject({
										/**
										 * The number of log entries between snapshots.
										 */
										SnapshotInterval: v.exactOptional(
											v.pipe(v.number(), v.integer()),
										),
										/**
										 * The number of snapshots to keep beyond the current snapshot.
										 */
										KeepOldSnapshots: v.exactOptional(
											v.pipe(v.number(), v.integer()),
										),
										/**
										 * The number of log entries to keep around to sync up slow followers
										 * after a snapshot is created.
										 */
										LogEntriesForSlowFollowers: v.exactOptional(
											v.pipe(v.number(), v.integer()),
										),
										/**
										 * The number of ticks that a follower will wait for a message from
										 * the leader before becoming a candidate and starting an election.
										 * `ElectionTick` must be greater than `HeartbeatTick`.
										 *
										 * A tick currently defaults to one second, so these translate
										 * directly to seconds currently, but this is NOT guaranteed.
										 */
										ElectionTick: v.exactOptional(
											v.pipe(v.number(), v.integer()),
										),
										/**
										 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
										 * the leader will send a heartbeat to the followers.
										 *
										 * A tick currently defaults to one second, so these translate
										 * directly to seconds currently, but this is NOT guaranteed.
										 */
										HeartbeatTick: v.exactOptional(
											v.pipe(v.number(), v.integer()),
										),
									}),
								),
								/**
								 * Dispatcher configuration.
								 */
								Dispatcher: v.exactOptional(
									v.nullable(
										v.strictObject({
											/**
											 * The delay for an agent to send a heartbeat to the dispatcher.
											 */
											HeartbeatPeriod: v.exactOptional(
												v.pipe(v.number(), v.integer()),
											),
										}),
									),
								),
								/**
								 * CA configuration.
								 */
								CAConfig: v.exactOptional(
									v.nullable(
										v.strictObject({
											/**
											 * The duration node certificates are issued for.
											 */
											NodeCertExpiry: v.exactOptional(
												v.pipe(v.number(), v.integer()),
											),
											/**
											 * Configuration for forwarding signing requests to an external
											 * certificate authority.
											 */
											ExternalCAs: v.exactOptional(
												v.array(
													v.strictObject({
														/**
														 * Protocol for communication with the external CA (currently
														 * only `cfssl` is supported).
														 */
														Protocol: v.exactOptional(v.picklist(["cfssl"])),
														/**
														 * URL where certificate signing requests should be sent.
														 */
														URL: v.exactOptional(v.string()),
														/**
														 * An object with key/value pairs that are interpreted as
														 * protocol-specific options for the external CA driver.
														 */
														Options: v.exactOptional(
															v.record(v.string(), v.unknown()),
														),
														/**
														 * The root CA certificate (in PEM format) this external CA uses
														 * to issue TLS certificates (assumed to be to the current swarm
														 * root CA certificate if not provided).
														 */
														CACert: v.exactOptional(v.string()),
													}),
												),
											),
											/**
											 * The desired signing CA certificate for all swarm node TLS leaf
											 * certificates, in PEM format.
											 */
											SigningCACert: v.exactOptional(v.string()),
											/**
											 * The desired signing CA key for all swarm node TLS leaf certificates,
											 * in PEM format.
											 */
											SigningCAKey: v.exactOptional(v.string()),
											/**
											 * An integer whose purpose is to force swarm to generate a new
											 * signing CA certificate and key, if none have been specified in
											 * `SigningCACert` and `SigningCAKey`
											 */
											ForceRotate: v.exactOptional(
												v.pipe(v.number(), v.integer()),
											),
										}),
									),
								),
								/**
								 * Parameters related to encryption-at-rest.
								 */
								EncryptionConfig: v.exactOptional(
									v.strictObject({
										/**
										 * If set, generate a key and use it to lock data stored on the
										 * managers.
										 */
										AutoLockManagers: v.exactOptional(v.boolean()),
									}),
								),
								/**
								 * Defaults for creating tasks in this cluster.
								 */
								TaskDefaults: v.exactOptional(
									v.strictObject({
										/**
										 * The log driver to use for tasks created in the orchestrator if
										 * unspecified by a service.
										 *
										 * Updating this value only affects new tasks. Existing tasks continue
										 * to use their previously configured log driver until recreated.
										 */
										LogDriver: v.exactOptional(
											v.strictObject({
												/**
												 * The log driver to use as a default for new tasks.
												 */
												Name: v.exactOptional(v.string()),
												/**
												 * Driver-specific options for the selected log driver, specified
												 * as key/value pairs.
												 */
												Options: v.exactOptional(
													v.record(v.string(), v.unknown()),
												),
											}),
										),
									}),
								),
							}),
						),
						/**
						 * Information about the issuer of leaf TLS certificates and the trusted root
						 * CA certificate.
						 */
						TLSInfo: v.exactOptional(
							v.strictObject({
								/**
								 * The root CA certificate(s) that are used to validate leaf TLS
								 * certificates.
								 */
								TrustRoot: v.exactOptional(v.string()),
								/**
								 * The base64-url-safe-encoded raw subject bytes of the issuer.
								 */
								CertIssuerSubject: v.exactOptional(v.string()),
								/**
								 * The base64-url-safe-encoded raw public key bytes of the issuer.
								 */
								CertIssuerPublicKey: v.exactOptional(v.string()),
							}),
						),
						/**
						 * Whether there is currently a root CA rotation in progress for the swarm
						 */
						RootRotationInProgress: v.exactOptional(v.boolean()),
						/**
						 * DataPathPort specifies the data path port number for data traffic.
						 * Acceptable port range is 1024 to 49151.
						 * If no port is set or is set to 0, the default port (4789) is used.
						 */
						DataPathPort: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * Default Address Pool specifies default subnet pools for global scope
						 * networks.
						 */
						DefaultAddrPool: v.exactOptional(v.array(v.string())),
						/**
						 * SubnetSize specifies the subnet size of the networks created from the
						 * default subnet pool.
						 */
						SubnetSize: v.exactOptional(
							v.pipe(v.number(), v.integer(), v.maxValue(29)),
						),
					}),
				),
			),
		}),
	),
	/**
	 * Indicates if live restore is enabled.
	 *
	 * If enabled, containers are kept running when the daemon is shutdown
	 * or upon daemon start if running containers are detected.
	 */
	LiveRestoreEnabled: v.exactOptional(v.boolean()),
	/**
	 * Represents the isolation technology to use as a default for containers.
	 * The supported values are platform-specific.
	 *
	 * If no isolation value is specified on daemon start, on Windows client,
	 * the default is `hyperv`, and on Windows server, the default is `process`.
	 *
	 * This option is currently not used on other platforms.
	 */
	Isolation: v.exactOptional(v.picklist(["default", "hyperv", "process", ""])),
	/**
	 * Name and, optional, path of the `docker-init` binary.
	 *
	 * If the path is omitted, the daemon searches the host's `$PATH` for the
	 * binary and uses the first result.
	 */
	InitBinary: v.exactOptional(v.string()),
	/**
	 * Commit holds the Git-commit (SHA1) that a binary was built from, as
	 * reported in the version-string of external tools, such as `containerd`,
	 * or `runC`.
	 */
	ContainerdCommit: v.exactOptional(
		v.strictObject({
			/**
			 * Actual commit ID of external tool.
			 */
			ID: v.exactOptional(v.string()),
			/**
			 * Commit ID of external tool expected by dockerd as set at build time.
			 */
			Expected: v.exactOptional(v.string()),
		}),
	),
	/**
	 * Commit holds the Git-commit (SHA1) that a binary was built from, as
	 * reported in the version-string of external tools, such as `containerd`,
	 * or `runC`.
	 */
	RuncCommit: v.exactOptional(
		v.strictObject({
			/**
			 * Actual commit ID of external tool.
			 */
			ID: v.exactOptional(v.string()),
			/**
			 * Commit ID of external tool expected by dockerd as set at build time.
			 */
			Expected: v.exactOptional(v.string()),
		}),
	),
	/**
	 * Commit holds the Git-commit (SHA1) that a binary was built from, as
	 * reported in the version-string of external tools, such as `containerd`,
	 * or `runC`.
	 */
	InitCommit: v.exactOptional(
		v.strictObject({
			/**
			 * Actual commit ID of external tool.
			 */
			ID: v.exactOptional(v.string()),
			/**
			 * Commit ID of external tool expected by dockerd as set at build time.
			 */
			Expected: v.exactOptional(v.string()),
		}),
	),
	/**
	 * List of security features that are enabled on the daemon, such as
	 * apparmor, seccomp, SELinux, user-namespaces (userns), rootless and
	 * no-new-privileges.
	 *
	 * Additional configuration options for each security feature may
	 * be present, and are included as a comma-separated list of key/value
	 * pairs.
	 */
	SecurityOptions: v.exactOptional(v.array(v.string())),
	/**
	 * Reports a summary of the product license on the daemon.
	 *
	 * If a commercial license has been applied to the daemon, information
	 * such as number of nodes, and expiration are included.
	 */
	ProductLicense: v.exactOptional(v.string()),
	/**
	 * List of custom default address pools for local networks, which can be
	 * specified in the daemon.json file or dockerd option.
	 *
	 * Example: a Base "10.10.0.0/16" with Size 24 will define the set of 256
	 * 10.10.[0-255].0/24 address pools.
	 */
	DefaultAddressPools: v.exactOptional(
		v.array(
			v.strictObject({
				/**
				 * The network address in CIDR format
				 */
				Base: v.exactOptional(v.string()),
				/**
				 * The network pool size
				 */
				Size: v.exactOptional(v.pipe(v.number(), v.integer())),
			}),
		),
	),
	/**
	 * List of warnings / informational messages about missing features, or
	 * issues related to the daemon configuration.
	 *
	 * These messages can be printed by the client as information to the user.
	 */
	Warnings: v.exactOptional(v.array(v.string())),
});
/**
 * Available plugins per type.
 *
 * <p><br /></p>
 *
 * > **Note**: Only unmanaged (V1) plugins are included in this list.
 * > V1 plugins are "lazily" loaded, and are not returned in this list
 * > if there is no resource using the plugin.
 */
export const pluginsInfoSchema = v.strictObject({
	/**
	 * Names of available volume-drivers, and network-driver plugins.
	 */
	Volume: v.exactOptional(v.array(v.string())),
	/**
	 * Names of available network-drivers, and network-driver plugins.
	 */
	Network: v.exactOptional(v.array(v.string())),
	/**
	 * Names of available authorization plugins.
	 */
	Authorization: v.exactOptional(v.array(v.string())),
	/**
	 * Names of available logging-drivers, and logging-driver plugins.
	 */
	Log: v.exactOptional(v.array(v.string())),
});
/** RegistryServiceConfig stores daemon registry services configuration. */
export const registryServiceConfigSchema = v.nullable(
	v.strictObject({
		/**
		 * List of IP ranges to which nondistributable artifacts can be pushed,
		 * using the CIDR syntax [RFC 4632](https://tools.ietf.org/html/4632).
		 *
		 * Some images (for example, Windows base images) contain artifacts
		 * whose distribution is restricted by license. When these images are
		 * pushed to a registry, restricted artifacts are not included.
		 *
		 * This configuration override this behavior, and enables the daemon to
		 * push nondistributable artifacts to all registries whose resolved IP
		 * address is within the subnet described by the CIDR syntax.
		 *
		 * This option is useful when pushing images containing
		 * nondistributable artifacts to a registry on an air-gapped network so
		 * hosts on that network can pull the images without connecting to
		 * another server.
		 *
		 * > **Warning**: Nondistributable artifacts typically have restrictions
		 * > on how and where they can be distributed and shared. Only use this
		 * > feature to push artifacts to private registries and ensure that you
		 * > are in compliance with any terms that cover redistributing
		 * > nondistributable artifacts.
		 */
		AllowNondistributableArtifactsCIDRs: v.exactOptional(v.array(v.string())),
		/**
		 * List of registry hostnames to which nondistributable artifacts can be
		 * pushed, using the format `<hostname>[:<port>]` or `<IP address>[:<port>]`.
		 *
		 * Some images (for example, Windows base images) contain artifacts
		 * whose distribution is restricted by license. When these images are
		 * pushed to a registry, restricted artifacts are not included.
		 *
		 * This configuration override this behavior for the specified
		 * registries.
		 *
		 * This option is useful when pushing images containing
		 * nondistributable artifacts to a registry on an air-gapped network so
		 * hosts on that network can pull the images without connecting to
		 * another server.
		 *
		 * > **Warning**: Nondistributable artifacts typically have restrictions
		 * > on how and where they can be distributed and shared. Only use this
		 * > feature to push artifacts to private registries and ensure that you
		 * > are in compliance with any terms that cover redistributing
		 * > nondistributable artifacts.
		 */
		AllowNondistributableArtifactsHostnames: v.exactOptional(
			v.array(v.string()),
		),
		/**
		 * List of IP ranges of insecure registries, using the CIDR syntax
		 * ([RFC 4632](https://tools.ietf.org/html/4632)). Insecure registries
		 * accept un-encrypted (HTTP) and/or untrusted (HTTPS with certificates
		 * from unknown CAs) communication.
		 *
		 * By default, local registries (`127.0.0.0/8`) are configured as
		 * insecure. All other registries are secure. Communicating with an
		 * insecure registry is not possible if the daemon assumes that registry
		 * is secure.
		 *
		 * This configuration override this behavior, insecure communication with
		 * registries whose resolved IP address is within the subnet described by
		 * the CIDR syntax.
		 *
		 * Registries can also be marked insecure by hostname. Those registries
		 * are listed under `IndexConfigs` and have their `Secure` field set to
		 * `false`.
		 *
		 * > **Warning**: Using this option can be useful when running a local
		 * > registry, but introduces security vulnerabilities. This option
		 * > should therefore ONLY be used for testing purposes. For increased
		 * > security, users should add their CA to their system's list of trusted
		 * > CAs instead of enabling this option.
		 */
		InsecureRegistryCIDRs: v.exactOptional(v.array(v.string())),
		IndexConfigs: v.exactOptional(v.record(v.string(), v.unknown())),
		/**
		 * List of registry URLs that act as a mirror for the official
		 * (`docker.io`) registry.
		 */
		Mirrors: v.exactOptional(v.array(v.string())),
	}),
);
/** IndexInfo contains information about a registry. */
export const indexInfoSchema = v.nullable(
	v.strictObject({
		/**
		 * Name of the registry, such as "docker.io".
		 */
		Name: v.exactOptional(v.string()),
		/**
		 * List of mirrors, expressed as URIs.
		 */
		Mirrors: v.exactOptional(v.array(v.string())),
		/**
		 * Indicates if the registry is part of the list of insecure
		 * registries.
		 *
		 * If `false`, the registry is insecure. Insecure registries accept
		 * un-encrypted (HTTP) and/or untrusted (HTTPS with certificates from
		 * unknown CAs) communication.
		 *
		 * > **Warning**: Insecure registries can be useful when running a local
		 * > registry. However, because its use creates security vulnerabilities
		 * > it should ONLY be enabled for testing purposes. For increased
		 * > security, users should add their CA to their system's list of
		 * > trusted CAs instead of enabling this option.
		 */
		Secure: v.exactOptional(v.boolean()),
		/**
		 * Indicates whether this is an official registry (i.e., Docker Hub /
		 * docker.io)
		 */
		Official: v.exactOptional(v.boolean()),
	}),
);
/**
 * Runtime describes an [OCI
 * compliant](https://github.com/opencontainers/runtime-spec)
 * runtime.
 *
 * The runtime is invoked by the daemon via the `containerd` daemon. OCI
 * runtimes act as an interface to the Linux kernel namespaces, cgroups,
 * and SELinux.
 */
export const runtimeSchema = v.strictObject({
	/**
	 * Name and, optional, path, of the OCI executable binary.
	 *
	 * If the path is omitted, the daemon searches the host's `$PATH` for the
	 * binary and uses the first result.
	 */
	path: v.exactOptional(v.string()),
	/**
	 * List of command-line arguments to pass to the runtime when invoked.
	 */
	runtimeArgs: v.exactOptional(v.nullable(v.array(v.string()))),
});
/**
 * Commit holds the Git-commit (SHA1) that a binary was built from, as
 * reported in the version-string of external tools, such as `containerd`,
 * or `runC`.
 */
export const commitSchema = v.strictObject({
	/**
	 * Actual commit ID of external tool.
	 */
	ID: v.exactOptional(v.string()),
	/**
	 * Commit ID of external tool expected by dockerd as set at build time.
	 */
	Expected: v.exactOptional(v.string()),
});
/** Represents generic information about swarm. */
export const swarmInfoSchema = v.strictObject({
	/**
	 * Unique identifier of for this node in the swarm.
	 */
	NodeID: v.exactOptional(v.string()),
	/**
	 * IP address at which this node can be reached by other nodes in the
	 * swarm.
	 */
	NodeAddr: v.exactOptional(v.string()),
	/**
	 * Current local status of this node.
	 */
	LocalNodeState: v.exactOptional(
		v.picklist(["", "inactive", "pending", "active", "error", "locked"]),
	),
	ControlAvailable: v.exactOptional(v.boolean()),
	Error: v.exactOptional(v.string()),
	/**
	 * List of ID's and addresses of other managers in the swarm.
	 */
	RemoteManagers: v.exactOptional(
		v.nullable(
			v.array(
				v.strictObject({
					/**
					 * Unique identifier of for this node in the swarm.
					 */
					NodeID: v.exactOptional(v.string()),
					/**
					 * IP address and ports at which this node can be reached.
					 */
					Addr: v.exactOptional(v.string()),
				}),
			),
		),
	),
	/**
	 * Total number of nodes in the swarm.
	 */
	Nodes: v.exactOptional(v.nullable(v.pipe(v.number(), v.integer()))),
	/**
	 * Total number of managers in the swarm.
	 */
	Managers: v.exactOptional(v.nullable(v.pipe(v.number(), v.integer()))),
	/**
	 * ClusterInfo represents information about the swarm as is returned by the
	 * "/info" endpoint. Join-tokens are not included.
	 */
	Cluster: v.exactOptional(
		v.nullable(
			v.strictObject({
				/**
				 * The ID of the swarm.
				 */
				ID: v.exactOptional(v.string()),
				/**
				 * The version number of the object such as node, service, etc. This is needed
				 * to avoid conflicting writes. The client must send the version number along
				 * with the modified specification when updating these objects.
				 *
				 * This approach ensures safe concurrency and determinism in that the change
				 * on the object may not be applied if the version number has changed from the
				 * last read. In other words, if two update requests specify the same base
				 * version, only one of the requests can succeed. As a result, two separate
				 * update requests that happen at the same time will not unintentionally
				 * overwrite each other.
				 */
				Version: v.exactOptional(
					v.strictObject({
						Index: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
				/**
				 * Date and time at which the swarm was initialised in
				 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
				 */
				CreatedAt: v.exactOptional(v.string()),
				/**
				 * Date and time at which the swarm was last updated in
				 * [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.
				 */
				UpdatedAt: v.exactOptional(v.string()),
				/**
				 * User modifiable swarm configuration.
				 */
				Spec: v.exactOptional(
					v.strictObject({
						/**
						 * Name of the swarm.
						 */
						Name: v.exactOptional(v.string()),
						/**
						 * User-defined key/value metadata.
						 */
						Labels: v.exactOptional(v.record(v.string(), v.unknown())),
						/**
						 * Orchestration configuration.
						 */
						Orchestration: v.exactOptional(
							v.nullable(
								v.strictObject({
									/**
									 * The number of historic tasks to keep per instance or node. If
									 * negative, never remove completed or failed tasks.
									 */
									TaskHistoryRetentionLimit: v.exactOptional(
										v.pipe(v.number(), v.integer()),
									),
								}),
							),
						),
						/**
						 * Raft configuration.
						 */
						Raft: v.exactOptional(
							v.strictObject({
								/**
								 * The number of log entries between snapshots.
								 */
								SnapshotInterval: v.exactOptional(
									v.pipe(v.number(), v.integer()),
								),
								/**
								 * The number of snapshots to keep beyond the current snapshot.
								 */
								KeepOldSnapshots: v.exactOptional(
									v.pipe(v.number(), v.integer()),
								),
								/**
								 * The number of log entries to keep around to sync up slow followers
								 * after a snapshot is created.
								 */
								LogEntriesForSlowFollowers: v.exactOptional(
									v.pipe(v.number(), v.integer()),
								),
								/**
								 * The number of ticks that a follower will wait for a message from
								 * the leader before becoming a candidate and starting an election.
								 * `ElectionTick` must be greater than `HeartbeatTick`.
								 *
								 * A tick currently defaults to one second, so these translate
								 * directly to seconds currently, but this is NOT guaranteed.
								 */
								ElectionTick: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
								 * the leader will send a heartbeat to the followers.
								 *
								 * A tick currently defaults to one second, so these translate
								 * directly to seconds currently, but this is NOT guaranteed.
								 */
								HeartbeatTick: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
						/**
						 * Dispatcher configuration.
						 */
						Dispatcher: v.exactOptional(
							v.nullable(
								v.strictObject({
									/**
									 * The delay for an agent to send a heartbeat to the dispatcher.
									 */
									HeartbeatPeriod: v.exactOptional(
										v.pipe(v.number(), v.integer()),
									),
								}),
							),
						),
						/**
						 * CA configuration.
						 */
						CAConfig: v.exactOptional(
							v.nullable(
								v.strictObject({
									/**
									 * The duration node certificates are issued for.
									 */
									NodeCertExpiry: v.exactOptional(
										v.pipe(v.number(), v.integer()),
									),
									/**
									 * Configuration for forwarding signing requests to an external
									 * certificate authority.
									 */
									ExternalCAs: v.exactOptional(
										v.array(
											v.strictObject({
												/**
												 * Protocol for communication with the external CA (currently
												 * only `cfssl` is supported).
												 */
												Protocol: v.exactOptional(v.picklist(["cfssl"])),
												/**
												 * URL where certificate signing requests should be sent.
												 */
												URL: v.exactOptional(v.string()),
												/**
												 * An object with key/value pairs that are interpreted as
												 * protocol-specific options for the external CA driver.
												 */
												Options: v.exactOptional(
													v.record(v.string(), v.unknown()),
												),
												/**
												 * The root CA certificate (in PEM format) this external CA uses
												 * to issue TLS certificates (assumed to be to the current swarm
												 * root CA certificate if not provided).
												 */
												CACert: v.exactOptional(v.string()),
											}),
										),
									),
									/**
									 * The desired signing CA certificate for all swarm node TLS leaf
									 * certificates, in PEM format.
									 */
									SigningCACert: v.exactOptional(v.string()),
									/**
									 * The desired signing CA key for all swarm node TLS leaf certificates,
									 * in PEM format.
									 */
									SigningCAKey: v.exactOptional(v.string()),
									/**
									 * An integer whose purpose is to force swarm to generate a new
									 * signing CA certificate and key, if none have been specified in
									 * `SigningCACert` and `SigningCAKey`
									 */
									ForceRotate: v.exactOptional(v.pipe(v.number(), v.integer())),
								}),
							),
						),
						/**
						 * Parameters related to encryption-at-rest.
						 */
						EncryptionConfig: v.exactOptional(
							v.strictObject({
								/**
								 * If set, generate a key and use it to lock data stored on the
								 * managers.
								 */
								AutoLockManagers: v.exactOptional(v.boolean()),
							}),
						),
						/**
						 * Defaults for creating tasks in this cluster.
						 */
						TaskDefaults: v.exactOptional(
							v.strictObject({
								/**
								 * The log driver to use for tasks created in the orchestrator if
								 * unspecified by a service.
								 *
								 * Updating this value only affects new tasks. Existing tasks continue
								 * to use their previously configured log driver until recreated.
								 */
								LogDriver: v.exactOptional(
									v.strictObject({
										/**
										 * The log driver to use as a default for new tasks.
										 */
										Name: v.exactOptional(v.string()),
										/**
										 * Driver-specific options for the selected log driver, specified
										 * as key/value pairs.
										 */
										Options: v.exactOptional(v.record(v.string(), v.unknown())),
									}),
								),
							}),
						),
					}),
				),
				/**
				 * Information about the issuer of leaf TLS certificates and the trusted root
				 * CA certificate.
				 */
				TLSInfo: v.exactOptional(
					v.strictObject({
						/**
						 * The root CA certificate(s) that are used to validate leaf TLS
						 * certificates.
						 */
						TrustRoot: v.exactOptional(v.string()),
						/**
						 * The base64-url-safe-encoded raw subject bytes of the issuer.
						 */
						CertIssuerSubject: v.exactOptional(v.string()),
						/**
						 * The base64-url-safe-encoded raw public key bytes of the issuer.
						 */
						CertIssuerPublicKey: v.exactOptional(v.string()),
					}),
				),
				/**
				 * Whether there is currently a root CA rotation in progress for the swarm
				 */
				RootRotationInProgress: v.exactOptional(v.boolean()),
				/**
				 * DataPathPort specifies the data path port number for data traffic.
				 * Acceptable port range is 1024 to 49151.
				 * If no port is set or is set to 0, the default port (4789) is used.
				 */
				DataPathPort: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Default Address Pool specifies default subnet pools for global scope
				 * networks.
				 */
				DefaultAddrPool: v.exactOptional(v.array(v.string())),
				/**
				 * SubnetSize specifies the subnet size of the networks created from the
				 * default subnet pool.
				 */
				SubnetSize: v.exactOptional(
					v.pipe(v.number(), v.integer(), v.maxValue(29)),
				),
			}),
		),
	),
});
/**
 * Current local status of this node.
 * @example "active"
 */
export const localNodeStateSchema = v.picklist([
	"",
	"inactive",
	"pending",
	"active",
	"error",
	"locked",
]);
/** Represents a peer-node in the swarm */
export const peerNodeSchema = v.strictObject({
	/**
	 * Unique identifier of for this node in the swarm.
	 */
	NodeID: v.exactOptional(v.string()),
	/**
	 * IP address and ports at which this node can be reached.
	 */
	Addr: v.exactOptional(v.string()),
});
/** Specifies how a service should be attached to a particular network. */
export const networkAttachmentConfigSchema = v.strictObject({
	/**
	 * The target network for attachment. Must be a network name or ID.
	 */
	Target: v.exactOptional(v.string()),
	/**
	 * Discoverable alternate names for the service on this network.
	 */
	Aliases: v.exactOptional(v.array(v.string())),
	/**
	 * Driver attachment options for the network target.
	 */
	DriverOpts: v.exactOptional(v.record(v.string(), v.unknown())),
});
/**
 * Actor describes something that generates events, like a container, network,
 * or a volume.
 */
export const eventActorSchema = v.strictObject({
	/**
	 * The ID of the object emitting the event
	 */
	ID: v.exactOptional(v.string()),
	/**
	 * Various key/value attributes of the object, depending on its type.
	 */
	Attributes: v.exactOptional(v.record(v.string(), v.unknown())),
});
/**
 * EventMessage represents the information an event contains.
 * @title SystemEventsResponse
 */
export const eventMessageSchema = v.strictObject({
	/**
	 * The type of object emitting the event
	 */
	Type: v.exactOptional(
		v.picklist([
			"builder",
			"config",
			"container",
			"daemon",
			"image",
			"network",
			"node",
			"plugin",
			"secret",
			"service",
			"volume",
		]),
	),
	/**
	 * The type of event
	 */
	Action: v.exactOptional(v.string()),
	/**
	 * Actor describes something that generates events, like a container, network,
	 * or a volume.
	 */
	Actor: v.exactOptional(
		v.strictObject({
			/**
			 * The ID of the object emitting the event
			 */
			ID: v.exactOptional(v.string()),
			/**
			 * Various key/value attributes of the object, depending on its type.
			 */
			Attributes: v.exactOptional(v.record(v.string(), v.unknown())),
		}),
	),
	/**
	 * Scope of the event. Engine events are `local` scope. Cluster (Swarm)
	 * events are `swarm` scope.
	 */
	scope: v.exactOptional(v.picklist(["local", "swarm"])),
	/**
	 * Timestamp of event
	 */
	time: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Timestamp of event, with nanosecond accuracy
	 */
	timeNano: v.exactOptional(v.pipe(v.number(), v.integer())),
});
/**
 * A descriptor struct containing digest, media type, and size, as defined in
 * the [OCI Content Descriptors
 * Specification](https://github.com/opencontainers/image-spec/blob/v1.0.1/descriptor.md).
 */
export const ociDescriptorSchema = v.strictObject({
	/**
	 * The media type of the object this schema refers to.
	 */
	mediaType: v.exactOptional(v.string()),
	/**
	 * The digest of the targeted content.
	 */
	digest: v.exactOptional(v.string()),
	/**
	 * The size in bytes of the blob.
	 */
	size: v.exactOptional(v.pipe(v.number(), v.integer())),
});
/**
 * Describes the platform which the image in the manifest runs on, as defined
 * in the [OCI Image Index
 * Specification](https://github.com/opencontainers/image-spec/blob/v1.0.1/image-index.md).
 */
export const ociPlatformSchema = v.strictObject({
	/**
	 * The CPU architecture, for example `amd64` or `ppc64`.
	 */
	architecture: v.exactOptional(v.string()),
	/**
	 * The operating system, for example `linux` or `windows`.
	 */
	os: v.exactOptional(v.string()),
	/**
	 * Optional field specifying the operating system version, for example on
	 * Windows `10.0.19041.1165`.
	 */
	"os.version": v.exactOptional(v.string()),
	/**
	 * Optional field specifying an array of strings, each listing a required
	 * OS feature (for example on Windows `win32k`).
	 */
	"os.features": v.exactOptional(v.array(v.string())),
	/**
	 * Optional field specifying a variant of the CPU, for example `v7` to
	 * specify ARMv7 when architecture is `arm`.
	 */
	variant: v.exactOptional(v.string()),
});
/**
 * Describes the result obtained from contacting the registry to retrieve
 * image metadata.
 * @title DistributionInspectResponse
 */
export const distributionInspectSchema = v.strictObject({
	/**
	 * A descriptor struct containing digest, media type, and size, as defined in
	 * the [OCI Content Descriptors
	 * Specification](https://github.com/opencontainers/image-spec/blob/v1.0.1/descriptor.md).
	 */
	Descriptor: v.strictObject({
		/**
		 * The media type of the object this schema refers to.
		 */
		mediaType: v.exactOptional(v.string()),
		/**
		 * The digest of the targeted content.
		 */
		digest: v.exactOptional(v.string()),
		/**
		 * The size in bytes of the blob.
		 */
		size: v.exactOptional(v.pipe(v.number(), v.integer())),
	}),
	/**
	 * An array containing all platforms supported by the image.
	 */
	Platforms: v.array(
		v.strictObject({
			/**
			 * The CPU architecture, for example `amd64` or `ppc64`.
			 */
			architecture: v.exactOptional(v.string()),
			/**
			 * The operating system, for example `linux` or `windows`.
			 */
			os: v.exactOptional(v.string()),
			/**
			 * Optional field specifying the operating system version, for example on
			 * Windows `10.0.19041.1165`.
			 */
			"os.version": v.exactOptional(v.string()),
			/**
			 * Optional field specifying an array of strings, each listing a required
			 * OS feature (for example on Windows `win32k`).
			 */
			"os.features": v.exactOptional(v.array(v.string())),
			/**
			 * Optional field specifying a variant of the CPU, for example `v7` to
			 * specify ARMv7 when architecture is `arm`.
			 */
			variant: v.exactOptional(v.string()),
		}),
	),
});
/**
 * Options and information specific to, and only present on, Swarm CSI
 * cluster volumes.
 */
export const clusterVolumeSchema = v.strictObject({
	/**
	 * The Swarm ID of this volume. Because cluster volumes are Swarm
	 * objects, they have an ID, unlike non-cluster volumes. This ID can
	 * be used to refer to the Volume instead of the name.
	 */
	ID: v.exactOptional(v.string()),
	/**
	 * The version number of the object such as node, service, etc. This is needed
	 * to avoid conflicting writes. The client must send the version number along
	 * with the modified specification when updating these objects.
	 *
	 * This approach ensures safe concurrency and determinism in that the change
	 * on the object may not be applied if the version number has changed from the
	 * last read. In other words, if two update requests specify the same base
	 * version, only one of the requests can succeed. As a result, two separate
	 * update requests that happen at the same time will not unintentionally
	 * overwrite each other.
	 */
	Version: v.exactOptional(
		v.strictObject({
			Index: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	CreatedAt: v.exactOptional(v.string()),
	UpdatedAt: v.exactOptional(v.string()),
	/**
	 * Cluster-specific options used to create the volume.
	 */
	Spec: v.exactOptional(
		v.strictObject({
			/**
			 * Group defines the volume group of this volume. Volumes belonging to
			 * the same group can be referred to by group name when creating
			 * Services.  Referring to a volume by group instructs Swarm to treat
			 * volumes in that group interchangeably for the purpose of scheduling.
			 * Volumes with an empty string for a group technically all belong to
			 * the same, emptystring group.
			 */
			Group: v.exactOptional(v.string()),
			/**
			 * Defines how the volume is used by tasks.
			 */
			AccessMode: v.exactOptional(
				v.strictObject({
					/**
					 * The set of nodes this volume can be used on at one time.
					 * - `single` The volume may only be scheduled to one node at a time.
					 * - `multi` the volume may be scheduled to any supported number of nodes at a
					 * time.
					 */
					Scope: v.exactOptional(v.picklist(["single", "multi"])),
					/**
					 * The number and way that different tasks can use this volume
					 * at one time.
					 * - `none` The volume may only be used by one task at a time.
					 * - `readonly` The volume may be used by any number of tasks, but they all
					 * must mount the volume as readonly
					 * - `onewriter` The volume may be used by any number of tasks, but only one
					 * may mount it as read/write.
					 * - `all` The volume may have any number of readers and writers.
					 */
					Sharing: v.exactOptional(
						v.picklist(["none", "readonly", "onewriter", "all"]),
					),
					/**
					 * Options for using this volume as a Mount-type volume.
					 *
					 *
					 * Either MountVolume or BlockVolume, but not both, must be
					 *
					 * present.
					 *
					 * properties:
					 *
					 * FsType:
					 *
					 * type: "string"
					 *
					 * description: |
					 *
					 * Specifies the filesystem type for the mount volume.
					 *
					 * Optional.
					 *
					 * MountFlags:
					 *
					 * type: "array"
					 *
					 * description: |
					 *
					 * Flags to pass when mounting the volume. Optional.
					 *
					 * items:
					 *
					 * type: "string"
					 * BlockVolume:
					 *
					 * type: "object"
					 *
					 * description: |
					 *
					 * Options for using this volume as a Block-type volume.
					 *
					 * Intentionally empty.
					 */
					MountVolume: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * Swarm Secrets that are passed to the CSI storage plugin when
					 * operating on this volume.
					 */
					Secrets: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Key is the name of the key of the key-value pair passed to
								 * the plugin.
								 */
								Key: v.exactOptional(v.string()),
								/**
								 * Secret is the swarm Secret object from which to read data.
								 * This can be a Secret name or ID. The Secret data is
								 * retrieved by swarm and used as the value of the key-value
								 * pair passed to the plugin.
								 */
								Secret: v.exactOptional(v.string()),
							}),
						),
					),
					/**
					 * Requirements for the accessible topology of the volume. These
					 * fields are optional. For an in-depth description of what these
					 * fields mean, see the CSI specification.
					 */
					AccessibilityRequirements: v.exactOptional(
						v.strictObject({
							/**
							 * A list of required topologies, at least one of which the
							 * volume must be accessible from.
							 */
							Requisite: v.exactOptional(
								v.array(v.record(v.string(), v.unknown())),
							),
							/**
							 * A list of topologies that the volume should attempt to be
							 * provisioned in.
							 */
							Preferred: v.exactOptional(
								v.array(v.record(v.string(), v.unknown())),
							),
						}),
					),
					/**
					 * The desired capacity that the volume should be created with. If
					 * empty, the plugin will decide the capacity.
					 */
					CapacityRange: v.exactOptional(
						v.strictObject({
							/**
							 * The volume must be at least this big. The value of 0
							 * indicates an unspecified minimum
							 */
							RequiredBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The volume must not be bigger than this. The value of 0
							 * indicates an unspecified maximum.
							 */
							LimitBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
					/**
					 * The availability of the volume for use in tasks.
					 * - `active` The volume is fully available for scheduling on the cluster
					 * - `pause` No new workloads should use the volume, but existing workloads
					 * are not stopped.
					 * - `drain` All workloads using this volume should be stopped and
					 * rescheduled, and no new ones should be started.
					 */
					Availability: v.exactOptional(
						v.picklist(["active", "pause", "drain"]),
					),
				}),
			),
		}),
	),
	/**
	 * Information about the global status of the volume.
	 */
	Info: v.exactOptional(
		v.strictObject({
			/**
			 * The capacity of the volume in bytes. A value of 0 indicates that
			 * the capacity is unknown.
			 */
			CapacityBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * A map of strings to strings returned from the storage plugin when
			 * the volume is created.
			 */
			VolumeContext: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * The ID of the volume as returned by the CSI storage plugin. This
			 * is distinct from the volume's ID as provided by Docker. This ID
			 * is never used by the user when communicating with Docker to refer
			 * to this volume. If the ID is blank, then the Volume has not been
			 * successfully created in the plugin yet.
			 */
			VolumeID: v.exactOptional(v.string()),
			/**
			 * The topology this volume is actually accessible from.
			 */
			AccessibleTopology: v.exactOptional(
				v.array(v.record(v.string(), v.unknown())),
			),
		}),
	),
	/**
	 * The status of the volume as it pertains to its publishing and use on
	 * specific nodes
	 */
	PublishStatus: v.exactOptional(
		v.array(
			v.strictObject({
				/**
				 * The ID of the Swarm node the volume is published on.
				 */
				NodeID: v.exactOptional(v.string()),
				/**
				 * The published state of the volume.
				 * * `pending-publish` The volume should be published to this node, but the
				 * call to the controller plugin to do so has not yet been successfully
				 * completed.
				 * * `published` The volume is published successfully to the node.
				 * * `pending-node-unpublish` The volume should be unpublished from the node,
				 * and the manager is awaiting confirmation from the worker that it has done
				 * so.
				 * * `pending-controller-unpublish` The volume is successfully unpublished
				 * from the node, but has not yet been successfully unpublished on the
				 * controller.
				 */
				State: v.exactOptional(
					v.picklist([
						"pending-publish",
						"published",
						"pending-node-unpublish",
						"pending-controller-unpublish",
					]),
				),
				/**
				 * A map of strings to strings returned by the CSI controller
				 * plugin when a volume is published.
				 */
				PublishContext: v.exactOptional(v.record(v.string(), v.unknown())),
			}),
		),
	),
});
/** Cluster-specific options used to create the volume. */
export const clusterVolumeSpecSchema = v.strictObject({
	/**
	 * Group defines the volume group of this volume. Volumes belonging to
	 * the same group can be referred to by group name when creating
	 * Services.  Referring to a volume by group instructs Swarm to treat
	 * volumes in that group interchangeably for the purpose of scheduling.
	 * Volumes with an empty string for a group technically all belong to
	 * the same, emptystring group.
	 */
	Group: v.exactOptional(v.string()),
	/**
	 * Defines how the volume is used by tasks.
	 */
	AccessMode: v.exactOptional(
		v.strictObject({
			/**
			 * The set of nodes this volume can be used on at one time.
			 * - `single` The volume may only be scheduled to one node at a time.
			 * - `multi` the volume may be scheduled to any supported number of nodes at a
			 * time.
			 */
			Scope: v.exactOptional(v.picklist(["single", "multi"])),
			/**
			 * The number and way that different tasks can use this volume
			 * at one time.
			 * - `none` The volume may only be used by one task at a time.
			 * - `readonly` The volume may be used by any number of tasks, but they all
			 * must mount the volume as readonly
			 * - `onewriter` The volume may be used by any number of tasks, but only one
			 * may mount it as read/write.
			 * - `all` The volume may have any number of readers and writers.
			 */
			Sharing: v.exactOptional(
				v.picklist(["none", "readonly", "onewriter", "all"]),
			),
			/**
			 * Options for using this volume as a Mount-type volume.
			 *
			 *
			 * Either MountVolume or BlockVolume, but not both, must be
			 *
			 * present.
			 *
			 * properties:
			 *
			 * FsType:
			 *
			 * type: "string"
			 *
			 * description: |
			 *
			 * Specifies the filesystem type for the mount volume.
			 *
			 * Optional.
			 *
			 * MountFlags:
			 *
			 * type: "array"
			 *
			 * description: |
			 *
			 * Flags to pass when mounting the volume. Optional.
			 *
			 * items:
			 *
			 * type: "string"
			 * BlockVolume:
			 *
			 * type: "object"
			 *
			 * description: |
			 *
			 * Options for using this volume as a Block-type volume.
			 *
			 * Intentionally empty.
			 */
			MountVolume: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * Swarm Secrets that are passed to the CSI storage plugin when
			 * operating on this volume.
			 */
			Secrets: v.exactOptional(
				v.array(
					v.strictObject({
						/**
						 * Key is the name of the key of the key-value pair passed to
						 * the plugin.
						 */
						Key: v.exactOptional(v.string()),
						/**
						 * Secret is the swarm Secret object from which to read data.
						 * This can be a Secret name or ID. The Secret data is
						 * retrieved by swarm and used as the value of the key-value
						 * pair passed to the plugin.
						 */
						Secret: v.exactOptional(v.string()),
					}),
				),
			),
			/**
			 * Requirements for the accessible topology of the volume. These
			 * fields are optional. For an in-depth description of what these
			 * fields mean, see the CSI specification.
			 */
			AccessibilityRequirements: v.exactOptional(
				v.strictObject({
					/**
					 * A list of required topologies, at least one of which the
					 * volume must be accessible from.
					 */
					Requisite: v.exactOptional(
						v.array(v.record(v.string(), v.unknown())),
					),
					/**
					 * A list of topologies that the volume should attempt to be
					 * provisioned in.
					 */
					Preferred: v.exactOptional(
						v.array(v.record(v.string(), v.unknown())),
					),
				}),
			),
			/**
			 * The desired capacity that the volume should be created with. If
			 * empty, the plugin will decide the capacity.
			 */
			CapacityRange: v.exactOptional(
				v.strictObject({
					/**
					 * The volume must be at least this big. The value of 0
					 * indicates an unspecified minimum
					 */
					RequiredBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The volume must not be bigger than this. The value of 0
					 * indicates an unspecified maximum.
					 */
					LimitBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
			/**
			 * The availability of the volume for use in tasks.
			 * - `active` The volume is fully available for scheduling on the cluster
			 * - `pause` No new workloads should use the volume, but existing workloads
			 * are not stopped.
			 * - `drain` All workloads using this volume should be stopped and
			 * rescheduled, and no new ones should be started.
			 */
			Availability: v.exactOptional(v.picklist(["active", "pause", "drain"])),
		}),
	),
});
/**
 * A map of topological domains to topological segments. For in depth
 * details, see documentation for the Topology object in the CSI
 * specification.
 */
export const topologySchema = v.record(v.string(), v.unknown());
export const containerListCommandQuerySchema = v.strictObject({
	all: v.exactOptional(v.boolean()),
	limit: v.exactOptional(v.pipe(v.number(), v.integer())),
	size: v.exactOptional(v.boolean()),
	filters: v.exactOptional(v.string()),
});
export const containerCreateCommandBodySchema = v.intersect([
	v.strictObject({
		/**
		 * The hostname to use for the container, as a valid RFC 1123 hostname.
		 */
		Hostname: v.exactOptional(v.string()),
		/**
		 * The domain name to use for the container.
		 */
		Domainname: v.exactOptional(v.string()),
		/**
		 * The user that commands are run as inside the container.
		 */
		User: v.exactOptional(v.string()),
		/**
		 * Whether to attach to `stdin`.
		 */
		AttachStdin: v.exactOptional(v.boolean()),
		/**
		 * Whether to attach to `stdout`.
		 */
		AttachStdout: v.exactOptional(v.boolean()),
		/**
		 * Whether to attach to `stderr`.
		 */
		AttachStderr: v.exactOptional(v.boolean()),
		/**
		 * An object mapping ports to an empty object in the form:
		 *
		 * `{"<port>/<tcp|udp|sctp>": {}}`
		 */
		ExposedPorts: v.exactOptional(
			v.nullable(v.record(v.string(), v.unknown())),
		),
		/**
		 * Attach standard streams to a TTY, including `stdin` if it is not closed.
		 */
		Tty: v.exactOptional(v.boolean()),
		/**
		 * Open `stdin`
		 */
		OpenStdin: v.exactOptional(v.boolean()),
		/**
		 * Close `stdin` after one attached client disconnects
		 */
		StdinOnce: v.exactOptional(v.boolean()),
		/**
		 * A list of environment variables to set inside the container in the
		 * form `["VAR=value", ...]`. A variable without `=` is removed from the
		 * environment, rather than to have an empty value.
		 */
		Env: v.exactOptional(v.array(v.string())),
		/**
		 * Command to run specified as a string or an array of strings.
		 */
		Cmd: v.exactOptional(v.array(v.string())),
		/**
		 * A test to perform to check that the container is healthy.
		 */
		Healthcheck: v.exactOptional(
			v.strictObject({
				/**
				 * The test to perform. Possible values are:
				 *
				 * - `[]` inherit healthcheck from image or parent image
				 * - `["NONE"]` disable healthcheck
				 * - `["CMD", args...]` exec arguments directly
				 * - `["CMD-SHELL", command]` run command with system's default shell
				 */
				Test: v.exactOptional(v.array(v.string())),
				/**
				 * The time to wait between checks in nanoseconds. It should be 0 or at
				 * least 1000000 (1 ms). 0 means inherit.
				 */
				Interval: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * The time to wait before considering the check to have hung. It should
				 * be 0 or at least 1000000 (1 ms). 0 means inherit.
				 */
				Timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * The number of consecutive failures needed to consider a container as
				 * unhealthy. 0 means inherit.
				 */
				Retries: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Start period for the container to initialize before starting
				 * health-retries countdown in nanoseconds. It should be 0 or at least
				 * 1000000 (1 ms). 0 means inherit.
				 */
				StartPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
			}),
		),
		/**
		 * Command is already escaped (Windows only)
		 */
		ArgsEscaped: v.exactOptional(v.nullable(v.boolean())),
		/**
		 * The name (or reference) of the image to use when creating the container,
		 * or which was used when the container was created.
		 */
		Image: v.exactOptional(v.string()),
		/**
		 * An object mapping mount point paths inside the container to empty
		 * objects.
		 */
		Volumes: v.exactOptional(v.record(v.string(), v.unknown())),
		/**
		 * The working directory for commands to run in.
		 */
		WorkingDir: v.exactOptional(v.string()),
		/**
		 * The entry point for the container as a string or an array of strings.
		 *
		 * If the array consists of exactly one empty string (`[""]`) then the
		 * entry point is reset to system default (i.e., the entry point used by
		 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
		 */
		Entrypoint: v.exactOptional(v.array(v.string())),
		/**
		 * Disable networking for the container.
		 */
		NetworkDisabled: v.exactOptional(v.nullable(v.boolean())),
		/**
		 * MAC address of the container.
		 */
		MacAddress: v.exactOptional(v.nullable(v.string())),
		/**
		 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
		 */
		OnBuild: v.exactOptional(v.nullable(v.array(v.string()))),
		/**
		 * User-defined key/value metadata.
		 */
		Labels: v.exactOptional(v.record(v.string(), v.unknown())),
		/**
		 * Signal to stop a container as a string or unsigned integer.
		 */
		StopSignal: v.exactOptional(v.nullable(v.string())),
		/**
		 * Timeout to stop a container in seconds.
		 */
		StopTimeout: v.exactOptional(v.nullable(v.pipe(v.number(), v.integer()))),
		/**
		 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
		 */
		Shell: v.exactOptional(v.nullable(v.array(v.string()))),
	}),
	v.strictObject({
		/**
		 * Container configuration that depends on the host we are running on
		 */
		HostConfig: v.exactOptional(
			v.intersect([
				v.strictObject({
					/**
					 * An integer value representing this container's relative CPU weight
					 * versus other containers.
					 */
					CpuShares: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Memory limit in bytes.
					 */
					Memory: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Path to `cgroups` under which the container's `cgroup` is created. If
					 * the path is not absolute, the path is considered to be relative to the
					 * `cgroups` path of the init process. Cgroups are created if they do not
					 * already exist.
					 */
					CgroupParent: v.exactOptional(v.string()),
					/**
					 * Block IO weight (relative weight).
					 */
					BlkioWeight: v.exactOptional(
						v.pipe(v.number(), v.integer(), v.minValue(0), v.maxValue(1000)),
					),
					/**
					 * Block IO weight (relative device weight) in the form:
					 *
					 * ```
					 * [{"Path": "device_path", "Weight": weight}]
					 * ```
					 */
					BlkioWeightDevice: v.exactOptional(
						v.array(
							v.strictObject({
								Path: v.exactOptional(v.string()),
								Weight: v.exactOptional(
									v.pipe(v.number(), v.integer(), v.minValue(0)),
								),
							}),
						),
					),
					/**
					 * Limit read rate (bytes per second) from a device, in the form:
					 *
					 * ```
					 * [{"Path": "device_path", "Rate": rate}]
					 * ```
					 */
					BlkioDeviceReadBps: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Device path
								 */
								Path: v.exactOptional(v.string()),
								/**
								 * Rate
								 */
								Rate: v.exactOptional(
									v.pipe(v.number(), v.integer(), v.minValue(0)),
								),
							}),
						),
					),
					/**
					 * Limit write rate (bytes per second) to a device, in the form:
					 *
					 * ```
					 * [{"Path": "device_path", "Rate": rate}]
					 * ```
					 */
					BlkioDeviceWriteBps: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Device path
								 */
								Path: v.exactOptional(v.string()),
								/**
								 * Rate
								 */
								Rate: v.exactOptional(
									v.pipe(v.number(), v.integer(), v.minValue(0)),
								),
							}),
						),
					),
					/**
					 * Limit read rate (IO per second) from a device, in the form:
					 *
					 * ```
					 * [{"Path": "device_path", "Rate": rate}]
					 * ```
					 */
					BlkioDeviceReadIOps: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Device path
								 */
								Path: v.exactOptional(v.string()),
								/**
								 * Rate
								 */
								Rate: v.exactOptional(
									v.pipe(v.number(), v.integer(), v.minValue(0)),
								),
							}),
						),
					),
					/**
					 * Limit write rate (IO per second) to a device, in the form:
					 *
					 * ```
					 * [{"Path": "device_path", "Rate": rate}]
					 * ```
					 */
					BlkioDeviceWriteIOps: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Device path
								 */
								Path: v.exactOptional(v.string()),
								/**
								 * Rate
								 */
								Rate: v.exactOptional(
									v.pipe(v.number(), v.integer(), v.minValue(0)),
								),
							}),
						),
					),
					/**
					 * The length of a CPU period in microseconds.
					 */
					CpuPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Microseconds of CPU time that the container can get in a CPU period.
					 */
					CpuQuota: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The length of a CPU real-time period in microseconds. Set to 0 to
					 * allocate no time allocated to real-time tasks.
					 */
					CpuRealtimePeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The length of a CPU real-time runtime in microseconds. Set to 0 to
					 * allocate no time allocated to real-time tasks.
					 */
					CpuRealtimeRuntime: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * CPUs in which to allow execution (e.g., `0-3`, `0,1`).
					 */
					CpusetCpus: v.exactOptional(v.string()),
					/**
					 * Memory nodes (MEMs) in which to allow execution (0-3, 0,1). Only
					 * effective on NUMA systems.
					 */
					CpusetMems: v.exactOptional(v.string()),
					/**
					 * A list of devices to add to the container.
					 */
					Devices: v.exactOptional(
						v.array(
							v.strictObject({
								PathOnHost: v.exactOptional(v.string()),
								PathInContainer: v.exactOptional(v.string()),
								CgroupPermissions: v.exactOptional(v.string()),
							}),
						),
					),
					/**
					 * a list of cgroup rules to apply to the container
					 */
					DeviceCgroupRules: v.exactOptional(v.array(v.string())),
					/**
					 * A list of requests for devices to be sent to device drivers.
					 */
					DeviceRequests: v.exactOptional(
						v.array(
							v.strictObject({
								Driver: v.exactOptional(v.string()),
								Count: v.exactOptional(v.pipe(v.number(), v.integer())),
								DeviceIDs: v.exactOptional(v.array(v.string())),
								/**
								 * A list of capabilities; an OR list of AND lists of capabilities.
								 */
								Capabilities: v.exactOptional(v.array(v.array(v.string()))),
								/**
								 * Driver-specific options, specified as a key/value pairs. These options
								 * are passed directly to the driver.
								 */
								Options: v.exactOptional(v.record(v.string(), v.unknown())),
							}),
						),
					),
					/**
					 * Hard limit for kernel TCP buffer memory (in bytes). Depending on the
					 * OCI runtime in use, this option may be ignored. It is no longer supported
					 * by the default (runc) runtime.
					 *
					 * This field is omitted when empty.
					 */
					KernelMemoryTCP: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Memory soft limit in bytes.
					 */
					MemoryReservation: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Total memory limit (memory + swap). Set as `-1` to enable unlimited
					 * swap.
					 */
					MemorySwap: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Tune a container's memory swappiness behavior. Accepts an integer
					 * between 0 and 100.
					 */
					MemorySwappiness: v.exactOptional(
						v.pipe(v.number(), v.integer(), v.minValue(0), v.maxValue(100)),
					),
					/**
					 * CPU quota in units of 10<sup>-9</sup> CPUs.
					 */
					NanoCpus: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Disable OOM Killer for the container.
					 */
					OomKillDisable: v.exactOptional(v.boolean()),
					/**
					 * Run an init inside the container that forwards signals and reaps
					 * processes. This field is omitted if empty, and the default (as
					 * configured on the daemon) is used.
					 */
					Init: v.exactOptional(v.nullable(v.boolean())),
					/**
					 * Tune a container's PIDs limit. Set `0` or `-1` for unlimited, or `null`
					 * to not change.
					 */
					PidsLimit: v.exactOptional(
						v.nullable(v.pipe(v.number(), v.integer())),
					),
					/**
					 * A list of resource limits to set in the container. For example:
					 *
					 * ```
					 * {"Name": "nofile", "Soft": 1024, "Hard": 2048}
					 * ```
					 */
					Ulimits: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Name of ulimit
								 */
								Name: v.exactOptional(v.string()),
								/**
								 * Soft limit
								 */
								Soft: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * Hard limit
								 */
								Hard: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
					),
					/**
					 * The number of usable CPUs (Windows only).
					 *
					 * On Windows Server containers, the processor resource controls are
					 * mutually exclusive. The order of precedence is `CPUCount` first, then
					 * `CPUShares`, and `CPUPercent` last.
					 */
					CpuCount: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The usable percentage of the available CPUs (Windows only).
					 *
					 * On Windows Server containers, the processor resource controls are
					 * mutually exclusive. The order of precedence is `CPUCount` first, then
					 * `CPUShares`, and `CPUPercent` last.
					 */
					CpuPercent: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Maximum IOps for the container system drive (Windows only)
					 */
					IOMaximumIOps: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Maximum IO in bytes per second for the container system drive
					 * (Windows only).
					 */
					IOMaximumBandwidth: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
				v.strictObject({
					/**
					 * A list of volume bindings for this container. Each volume binding
					 * is a string in one of these forms:
					 *
					 * - `host-src:container-dest[:options]` to bind-mount a host path
					 *
					 * into the container. Both `host-src`, and `container-dest` must
					 *
					 * be an _absolute_ path.
					 * - `volume-name:container-dest[:options]` to bind-mount a volume
					 *
					 * managed by a volume driver into the container. `container-dest`
					 *
					 * must be an _absolute_ path.
					 *
					 * `options` is an optional, comma-delimited list of:
					 *
					 * - `nocopy` disables automatic copying of data from the container
					 *
					 * path to the volume. The `nocopy` flag only applies to named volumes.
					 * - `[ro|rw]` mounts a volume read-only or read-write, respectively.
					 *
					 * If omitted or set to `rw`, volumes are mounted read-write.
					 * - `[z|Z]` applies SELinux labels to allow or deny multiple containers
					 *
					 * to read and write to the same volume.
					 *
					 * - `z`: a _shared_ content label is applied to the content. This
					 *
					 * label indicates that multiple containers can share the volume
					 *
					 * content, for both reading and writing.
					 *
					 * - `Z`: a _private unshared_ label is applied to the content.
					 *
					 * This label indicates that only the current container can use
					 *
					 * a private volume. Labeling systems such as SELinux require
					 *
					 * proper labels to be placed on volume content that is mounted
					 *
					 * into a container. Without a label, the security system can
					 *
					 * prevent a container's processes from using the content. By
					 *
					 * default, the labels set by the host operating system are not
					 *
					 * modified.
					 * - `[[r]shared|[r]slave|[r]private]` specifies mount
					 *
					 * [propagation
					 * behavior](https://www.kernel.org/doc/Documentation/filesystems/sharedsubtree.txt).
					 *
					 * This only applies to bind-mounted volumes, not internal volumes
					 *
					 * or named volumes. Mount propagation requires the source mount
					 *
					 * point (the location where the source directory is mounted in the
					 *
					 * host operating system) to have the correct propagation properties.
					 *
					 * For shared volumes, the source mount point must be set to `shared`.
					 *
					 * For slave volumes, the mount must be set to either `shared` or
					 *
					 * `slave`.
					 */
					Binds: v.exactOptional(v.array(v.string())),
					/**
					 * Path to a file where the container ID is written
					 */
					ContainerIDFile: v.exactOptional(v.string()),
					/**
					 * The logging configuration for this container
					 */
					LogConfig: v.exactOptional(
						v.strictObject({
							Type: v.exactOptional(
								v.picklist([
									"json-file",
									"syslog",
									"journald",
									"gelf",
									"fluentd",
									"awslogs",
									"splunk",
									"etwlogs",
									"none",
								]),
							),
							Config: v.exactOptional(v.record(v.string(), v.unknown())),
						}),
					),
					/**
					 * Network mode to use for this container. Supported standard values
					 * are: `bridge`, `host`, `none`, and `container:<name|id>`. Any
					 * other value is taken as a custom network's name to which this
					 * container should connect to.
					 */
					NetworkMode: v.exactOptional(v.string()),
					/**
					 * PortMap describes the mapping of container ports to host ports, using the
					 * container's port-number and protocol as key in the format
					 * `<port>/<protocol>`,
					 * for example, `80/udp`.
					 *
					 * If a container's port is mapped for multiple protocols, separate entries
					 * are added to the mapping table.
					 */
					PortBindings: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * The behavior to apply when the container exits. The default is not to
					 * restart.
					 *
					 * An ever increasing delay (double the previous delay, starting at 100ms) is
					 * added before each restart to prevent flooding the server.
					 */
					RestartPolicy: v.exactOptional(
						v.strictObject({
							/**
							 * - Empty string means not to restart
							 * - `no` Do not automatically restart
							 * - `always` Always restart
							 * - `unless-stopped` Restart always except when the user has manually stopped
							 * the container
							 * - `on-failure` Restart only when the container exit code is non-zero
							 */
							Name: v.exactOptional(
								v.picklist([
									"",
									"no",
									"always",
									"unless-stopped",
									"on-failure",
								]),
							),
							/**
							 * If `on-failure` is used, the number of times to retry before giving up.
							 */
							MaximumRetryCount: v.exactOptional(
								v.pipe(v.number(), v.integer()),
							),
						}),
					),
					/**
					 * Automatically remove the container when the container's process
					 * exits. This has no effect if `RestartPolicy` is set.
					 */
					AutoRemove: v.exactOptional(v.boolean()),
					/**
					 * Driver that this container uses to mount volumes.
					 */
					VolumeDriver: v.exactOptional(v.string()),
					/**
					 * A list of volumes to inherit from another container, specified in
					 * the form `<container name>[:<ro|rw>]`.
					 */
					VolumesFrom: v.exactOptional(v.array(v.string())),
					/**
					 * Specification for mounts to be added to the container.
					 */
					Mounts: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Container path.
								 */
								Target: v.exactOptional(v.string()),
								/**
								 * Mount source (e.g. a volume name, a host path).
								 */
								Source: v.exactOptional(v.string()),
								/**
								 * The mount type. Available types:
								 *
								 * - `bind` Mounts a file or directory from the host into the container. Must
								 * exist prior to creating the container.
								 * - `volume` Creates a volume with the given name and options (or uses a
								 * pre-existing volume with the same name and options). These are **not**
								 * removed when the container is removed.
								 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
								 * specified for tmpfs.
								 * - `npipe` Mounts a named pipe from the host into the container. Must exist
								 * prior to creating the container.
								 * - `cluster` a Swarm cluster volume
								 */
								Type: v.exactOptional(
									v.picklist(["bind", "volume", "tmpfs", "npipe", "cluster"]),
								),
								/**
								 * Whether the mount should be read-only.
								 */
								ReadOnly: v.exactOptional(v.boolean()),
								/**
								 * The consistency requirement for the mount: `default`, `consistent`,
								 * `cached`, or `delegated`.
								 */
								Consistency: v.exactOptional(v.string()),
								/**
								 * Optional configuration for the `bind` type.
								 */
								BindOptions: v.exactOptional(
									v.strictObject({
										/**
										 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
										 */
										Propagation: v.exactOptional(
											v.picklist([
												"private",
												"rprivate",
												"shared",
												"rshared",
												"slave",
												"rslave",
											]),
										),
										/**
										 * Disable recursive bind mount.
										 */
										NonRecursive: v.exactOptional(v.boolean()),
										/**
										 * Create mount point on host if missing
										 */
										CreateMountpoint: v.exactOptional(v.boolean()),
									}),
								),
								/**
								 * Optional configuration for the `volume` type.
								 */
								VolumeOptions: v.exactOptional(
									v.strictObject({
										/**
										 * Populate volume with data from the target.
										 */
										NoCopy: v.exactOptional(v.boolean()),
										/**
										 * User-defined key/value metadata.
										 */
										Labels: v.exactOptional(v.record(v.string(), v.unknown())),
										/**
										 * Map of driver specific options
										 */
										DriverConfig: v.exactOptional(
											v.strictObject({
												/**
												 * Name of the driver to use to create the volume.
												 */
												Name: v.exactOptional(v.string()),
												/**
												 * key/value map of driver specific options.
												 */
												Options: v.exactOptional(
													v.record(v.string(), v.unknown()),
												),
											}),
										),
									}),
								),
								/**
								 * Optional configuration for the `tmpfs` type.
								 */
								TmpfsOptions: v.exactOptional(
									v.strictObject({
										/**
										 * The size for the tmpfs mount in bytes.
										 */
										SizeBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
										/**
										 * The permission mode for the tmpfs mount in an integer.
										 */
										Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
									}),
								),
							}),
						),
					),
					/**
					 * Initial console size, as an `[height, width]` array.
					 */
					ConsoleSize: v.exactOptional(
						v.nullable(
							v.pipe(
								v.array(v.pipe(v.number(), v.integer(), v.minValue(0))),
								v.minLength(2),
								v.maxLength(2),
							),
						),
					),
					/**
					 * Arbitrary non-identifying metadata attached to container and
					 * provided to the runtime when the container is started.
					 */
					Annotations: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * A list of kernel capabilities to add to the container. Conflicts
					 * with option 'Capabilities'.
					 */
					CapAdd: v.exactOptional(v.array(v.string())),
					/**
					 * A list of kernel capabilities to drop from the container. Conflicts
					 * with option 'Capabilities'.
					 */
					CapDrop: v.exactOptional(v.array(v.string())),
					/**
					 * cgroup namespace mode for the container. Possible values are:
					 *
					 * - `"private"`: the container runs in its own private cgroup namespace
					 * - `"host"`: use the host system's cgroup namespace
					 *
					 * If not specified, the daemon default is used, which can either be
					 * `"private"`
					 * or `"host"`, depending on daemon version, kernel support and configuration.
					 */
					CgroupnsMode: v.exactOptional(v.picklist(["private", "host"])),
					/**
					 * A list of DNS servers for the container to use.
					 */
					Dns: v.exactOptional(v.array(v.string())),
					/**
					 * A list of DNS options.
					 */
					DnsOptions: v.exactOptional(v.array(v.string())),
					/**
					 * A list of DNS search domains.
					 */
					DnsSearch: v.exactOptional(v.array(v.string())),
					/**
					 * A list of hostnames/IP mappings to add to the container's `/etc/hosts`
					 * file. Specified in the form `["hostname:IP"]`.
					 */
					ExtraHosts: v.exactOptional(v.array(v.string())),
					/**
					 * A list of additional groups that the container process will run as.
					 */
					GroupAdd: v.exactOptional(v.array(v.string())),
					/**
					 * IPC sharing mode for the container. Possible values are:
					 *
					 * - `"none"`: own private IPC namespace, with /dev/shm not mounted
					 * - `"private"`: own private IPC namespace
					 * - `"shareable"`: own private IPC namespace, with a possibility to share it
					 * with other containers
					 * - `"container:<name|id>"`: join another (shareable) container's IPC
					 * namespace
					 * - `"host"`: use the host system's IPC namespace
					 *
					 * If not specified, daemon default is used, which can either be `"private"`
					 * or `"shareable"`, depending on daemon version and configuration.
					 */
					IpcMode: v.exactOptional(v.string()),
					/**
					 * Cgroup to use for the container.
					 */
					Cgroup: v.exactOptional(v.string()),
					/**
					 * A list of links for the container in the form `container_name:alias`.
					 */
					Links: v.exactOptional(v.array(v.string())),
					/**
					 * An integer value containing the score given to the container in
					 * order to tune OOM killer preferences.
					 */
					OomScoreAdj: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Set the PID (Process) Namespace mode for the container. It can be
					 * either:
					 *
					 * - `"container:<name|id>"`: joins another container's PID namespace
					 * - `"host"`: use the host's PID namespace inside the container
					 */
					PidMode: v.exactOptional(v.string()),
					/**
					 * Gives the container full access to the host.
					 */
					Privileged: v.exactOptional(v.boolean()),
					/**
					 * Allocates an ephemeral host port for all of a container's
					 * exposed ports.
					 *
					 * Ports are de-allocated when the container stops and allocated when
					 * the container starts. The allocated port might be changed when
					 * restarting the container.
					 *
					 * The port is selected from the ephemeral port range that depends on
					 * the kernel. For example, on Linux the range is defined by
					 * `/proc/sys/net/ipv4/ip_local_port_range`.
					 */
					PublishAllPorts: v.exactOptional(v.boolean()),
					/**
					 * Mount the container's root filesystem as read only.
					 */
					ReadonlyRootfs: v.exactOptional(v.boolean()),
					/**
					 * A list of string values to customize labels for MLS systems, such
					 * as SELinux.
					 */
					SecurityOpt: v.exactOptional(v.array(v.string())),
					/**
					 * Storage driver options for this container, in the form `{"size": "120G"}`.
					 */
					StorageOpt: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * A map of container directories which should be replaced by tmpfs
					 * mounts, and their corresponding mount options. For example:
					 *
					 * ```
					 * { "/run": "rw,noexec,nosuid,size=65536k" }
					 * ```
					 */
					Tmpfs: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * UTS namespace to use for the container.
					 */
					UTSMode: v.exactOptional(v.string()),
					/**
					 * Sets the usernamespace mode for the container when usernamespace
					 * remapping option is enabled.
					 */
					UsernsMode: v.exactOptional(v.string()),
					/**
					 * Size of `/dev/shm` in bytes. If omitted, the system uses 64MB.
					 */
					ShmSize: v.exactOptional(
						v.pipe(v.number(), v.integer(), v.minValue(0)),
					),
					/**
					 * A list of kernel parameters (sysctls) to set in the container.
					 * For example:
					 *
					 * ```
					 * {"net.ipv4.ip_forward": "1"}
					 * ```
					 */
					Sysctls: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * Runtime to use with this container.
					 */
					Runtime: v.exactOptional(v.string()),
					/**
					 * Isolation technology of the container. (Windows only)
					 */
					Isolation: v.exactOptional(
						v.picklist(["default", "process", "hyperv", ""]),
					),
					/**
					 * The list of paths to be masked inside the container (this overrides
					 * the default set of paths).
					 */
					MaskedPaths: v.exactOptional(v.array(v.string())),
					/**
					 * The list of paths to be set as read-only inside the container
					 * (this overrides the default set of paths).
					 */
					ReadonlyPaths: v.exactOptional(v.array(v.string())),
				}),
			]),
		),
		/**
		 * NetworkingConfig represents the container's networking configuration for
		 * each of its interfaces.
		 * It is used for the networking configs specified in the `docker create`
		 * and `docker network connect` commands.
		 */
		NetworkingConfig: v.exactOptional(
			v.strictObject({
				/**
				 * A mapping of network name to endpoint configuration for that network.
				 */
				EndpointsConfig: v.exactOptional(v.record(v.string(), v.unknown())),
			}),
		),
	}),
]);
export const containerCreateCommandQuerySchema = v.strictObject({
	name: v.exactOptional(
		v.pipe(v.string(), v.regex(/^\/?[a-zA-Z0-9][a-zA-Z0-9_.-]+$/)),
	),
	platform: v.exactOptional(v.string()),
});
export const containerInspectCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerInspectCommandQuerySchema = v.strictObject({
	size: v.exactOptional(v.boolean()),
});
export const containerTopCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerTopCommandQuerySchema = v.strictObject({
	ps_args: v.exactOptional(v.string()),
});
export const containerLogsCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerLogsCommandQuerySchema = v.strictObject({
	follow: v.exactOptional(v.boolean()),
	stdout: v.exactOptional(v.boolean()),
	stderr: v.exactOptional(v.boolean()),
	since: v.exactOptional(v.pipe(v.number(), v.integer())),
	until: v.exactOptional(v.pipe(v.number(), v.integer())),
	timestamps: v.exactOptional(v.boolean()),
	tail: v.exactOptional(v.string()),
});
export const containerChangesCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerExportCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerStatsCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerStatsCommandQuerySchema = v.strictObject({
	stream: v.exactOptional(v.boolean()),
	"one-shot": v.exactOptional(v.boolean()),
});
export const containerResizeCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerResizeCommandQuerySchema = v.strictObject({
	h: v.pipe(v.number(), v.integer()),
	w: v.pipe(v.number(), v.integer()),
});
export const containerStartCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerStartCommandQuerySchema = v.strictObject({
	detachKeys: v.exactOptional(v.string()),
});
export const containerStopCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerStopCommandQuerySchema = v.strictObject({
	signal: v.exactOptional(v.string()),
	t: v.exactOptional(v.pipe(v.number(), v.integer())),
});
export const containerRestartCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerRestartCommandQuerySchema = v.strictObject({
	signal: v.exactOptional(v.string()),
	t: v.exactOptional(v.pipe(v.number(), v.integer())),
});
export const containerKillCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerKillCommandQuerySchema = v.strictObject({
	signal: v.exactOptional(v.string()),
});
export const containerUpdateCommandBodySchema = v.intersect([
	v.strictObject({
		/**
		 * An integer value representing this container's relative CPU weight
		 * versus other containers.
		 */
		CpuShares: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Memory limit in bytes.
		 */
		Memory: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Path to `cgroups` under which the container's `cgroup` is created. If
		 * the path is not absolute, the path is considered to be relative to the
		 * `cgroups` path of the init process. Cgroups are created if they do not
		 * already exist.
		 */
		CgroupParent: v.exactOptional(v.string()),
		/**
		 * Block IO weight (relative weight).
		 */
		BlkioWeight: v.exactOptional(
			v.pipe(v.number(), v.integer(), v.minValue(0), v.maxValue(1000)),
		),
		/**
		 * Block IO weight (relative device weight) in the form:
		 *
		 * ```
		 * [{"Path": "device_path", "Weight": weight}]
		 * ```
		 */
		BlkioWeightDevice: v.exactOptional(
			v.array(
				v.strictObject({
					Path: v.exactOptional(v.string()),
					Weight: v.exactOptional(
						v.pipe(v.number(), v.integer(), v.minValue(0)),
					),
				}),
			),
		),
		/**
		 * Limit read rate (bytes per second) from a device, in the form:
		 *
		 * ```
		 * [{"Path": "device_path", "Rate": rate}]
		 * ```
		 */
		BlkioDeviceReadBps: v.exactOptional(
			v.array(
				v.strictObject({
					/**
					 * Device path
					 */
					Path: v.exactOptional(v.string()),
					/**
					 * Rate
					 */
					Rate: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
				}),
			),
		),
		/**
		 * Limit write rate (bytes per second) to a device, in the form:
		 *
		 * ```
		 * [{"Path": "device_path", "Rate": rate}]
		 * ```
		 */
		BlkioDeviceWriteBps: v.exactOptional(
			v.array(
				v.strictObject({
					/**
					 * Device path
					 */
					Path: v.exactOptional(v.string()),
					/**
					 * Rate
					 */
					Rate: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
				}),
			),
		),
		/**
		 * Limit read rate (IO per second) from a device, in the form:
		 *
		 * ```
		 * [{"Path": "device_path", "Rate": rate}]
		 * ```
		 */
		BlkioDeviceReadIOps: v.exactOptional(
			v.array(
				v.strictObject({
					/**
					 * Device path
					 */
					Path: v.exactOptional(v.string()),
					/**
					 * Rate
					 */
					Rate: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
				}),
			),
		),
		/**
		 * Limit write rate (IO per second) to a device, in the form:
		 *
		 * ```
		 * [{"Path": "device_path", "Rate": rate}]
		 * ```
		 */
		BlkioDeviceWriteIOps: v.exactOptional(
			v.array(
				v.strictObject({
					/**
					 * Device path
					 */
					Path: v.exactOptional(v.string()),
					/**
					 * Rate
					 */
					Rate: v.exactOptional(v.pipe(v.number(), v.integer(), v.minValue(0))),
				}),
			),
		),
		/**
		 * The length of a CPU period in microseconds.
		 */
		CpuPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Microseconds of CPU time that the container can get in a CPU period.
		 */
		CpuQuota: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * The length of a CPU real-time period in microseconds. Set to 0 to
		 * allocate no time allocated to real-time tasks.
		 */
		CpuRealtimePeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * The length of a CPU real-time runtime in microseconds. Set to 0 to
		 * allocate no time allocated to real-time tasks.
		 */
		CpuRealtimeRuntime: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * CPUs in which to allow execution (e.g., `0-3`, `0,1`).
		 */
		CpusetCpus: v.exactOptional(v.string()),
		/**
		 * Memory nodes (MEMs) in which to allow execution (0-3, 0,1). Only
		 * effective on NUMA systems.
		 */
		CpusetMems: v.exactOptional(v.string()),
		/**
		 * A list of devices to add to the container.
		 */
		Devices: v.exactOptional(
			v.array(
				v.strictObject({
					PathOnHost: v.exactOptional(v.string()),
					PathInContainer: v.exactOptional(v.string()),
					CgroupPermissions: v.exactOptional(v.string()),
				}),
			),
		),
		/**
		 * a list of cgroup rules to apply to the container
		 */
		DeviceCgroupRules: v.exactOptional(v.array(v.string())),
		/**
		 * A list of requests for devices to be sent to device drivers.
		 */
		DeviceRequests: v.exactOptional(
			v.array(
				v.strictObject({
					Driver: v.exactOptional(v.string()),
					Count: v.exactOptional(v.pipe(v.number(), v.integer())),
					DeviceIDs: v.exactOptional(v.array(v.string())),
					/**
					 * A list of capabilities; an OR list of AND lists of capabilities.
					 */
					Capabilities: v.exactOptional(v.array(v.array(v.string()))),
					/**
					 * Driver-specific options, specified as a key/value pairs. These options
					 * are passed directly to the driver.
					 */
					Options: v.exactOptional(v.record(v.string(), v.unknown())),
				}),
			),
		),
		/**
		 * Hard limit for kernel TCP buffer memory (in bytes). Depending on the
		 * OCI runtime in use, this option may be ignored. It is no longer supported
		 * by the default (runc) runtime.
		 *
		 * This field is omitted when empty.
		 */
		KernelMemoryTCP: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Memory soft limit in bytes.
		 */
		MemoryReservation: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Total memory limit (memory + swap). Set as `-1` to enable unlimited
		 * swap.
		 */
		MemorySwap: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Tune a container's memory swappiness behavior. Accepts an integer
		 * between 0 and 100.
		 */
		MemorySwappiness: v.exactOptional(
			v.pipe(v.number(), v.integer(), v.minValue(0), v.maxValue(100)),
		),
		/**
		 * CPU quota in units of 10<sup>-9</sup> CPUs.
		 */
		NanoCpus: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Disable OOM Killer for the container.
		 */
		OomKillDisable: v.exactOptional(v.boolean()),
		/**
		 * Run an init inside the container that forwards signals and reaps
		 * processes. This field is omitted if empty, and the default (as
		 * configured on the daemon) is used.
		 */
		Init: v.exactOptional(v.nullable(v.boolean())),
		/**
		 * Tune a container's PIDs limit. Set `0` or `-1` for unlimited, or `null`
		 * to not change.
		 */
		PidsLimit: v.exactOptional(v.nullable(v.pipe(v.number(), v.integer()))),
		/**
		 * A list of resource limits to set in the container. For example:
		 *
		 * ```
		 * {"Name": "nofile", "Soft": 1024, "Hard": 2048}
		 * ```
		 */
		Ulimits: v.exactOptional(
			v.array(
				v.strictObject({
					/**
					 * Name of ulimit
					 */
					Name: v.exactOptional(v.string()),
					/**
					 * Soft limit
					 */
					Soft: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * Hard limit
					 */
					Hard: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
		),
		/**
		 * The number of usable CPUs (Windows only).
		 *
		 * On Windows Server containers, the processor resource controls are
		 * mutually exclusive. The order of precedence is `CPUCount` first, then
		 * `CPUShares`, and `CPUPercent` last.
		 */
		CpuCount: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * The usable percentage of the available CPUs (Windows only).
		 *
		 * On Windows Server containers, the processor resource controls are
		 * mutually exclusive. The order of precedence is `CPUCount` first, then
		 * `CPUShares`, and `CPUPercent` last.
		 */
		CpuPercent: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Maximum IOps for the container system drive (Windows only)
		 */
		IOMaximumIOps: v.exactOptional(v.pipe(v.number(), v.integer())),
		/**
		 * Maximum IO in bytes per second for the container system drive
		 * (Windows only).
		 */
		IOMaximumBandwidth: v.exactOptional(v.pipe(v.number(), v.integer())),
	}),
	v.strictObject({
		/**
		 * The behavior to apply when the container exits. The default is not to
		 * restart.
		 *
		 * An ever increasing delay (double the previous delay, starting at 100ms) is
		 * added before each restart to prevent flooding the server.
		 */
		RestartPolicy: v.exactOptional(
			v.strictObject({
				/**
				 * - Empty string means not to restart
				 * - `no` Do not automatically restart
				 * - `always` Always restart
				 * - `unless-stopped` Restart always except when the user has manually stopped
				 * the container
				 * - `on-failure` Restart only when the container exit code is non-zero
				 */
				Name: v.exactOptional(
					v.picklist(["", "no", "always", "unless-stopped", "on-failure"]),
				),
				/**
				 * If `on-failure` is used, the number of times to retry before giving up.
				 */
				MaximumRetryCount: v.exactOptional(v.pipe(v.number(), v.integer())),
			}),
		),
	}),
]);
export const containerUpdateCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerRenameCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerRenameCommandQuerySchema = v.strictObject({
	name: v.string(),
});
export const containerPauseCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerUnpauseCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerAttachCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerAttachCommandQuerySchema = v.strictObject({
	detachKeys: v.exactOptional(v.string()),
	logs: v.exactOptional(v.boolean()),
	stream: v.exactOptional(v.boolean()),
	stdin: v.exactOptional(v.boolean()),
	stdout: v.exactOptional(v.boolean()),
	stderr: v.exactOptional(v.boolean()),
});
export const containerAttachWebsocketCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerAttachWebsocketCommandQuerySchema = v.strictObject({
	detachKeys: v.exactOptional(v.string()),
	logs: v.exactOptional(v.boolean()),
	stream: v.exactOptional(v.boolean()),
	stdin: v.exactOptional(v.boolean()),
	stdout: v.exactOptional(v.boolean()),
	stderr: v.exactOptional(v.boolean()),
});
export const containerWaitCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerWaitCommandQuerySchema = v.strictObject({
	condition: v.exactOptional(
		v.picklist(["not-running", "next-exit", "removed"]),
	),
});
export const containerDeleteCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerDeleteCommandQuerySchema = v.strictObject({
	v: v.exactOptional(v.boolean()),
	force: v.exactOptional(v.boolean()),
	link: v.exactOptional(v.boolean()),
});
export const containerArchiveCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerArchiveCommandQuerySchema = v.strictObject({
	path: v.string(),
});
export const putContainerArchiveCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const putContainerArchiveCommandQuerySchema = v.strictObject({
	path: v.string(),
	noOverwriteDirNonDir: v.exactOptional(v.string()),
	copyUIDGID: v.exactOptional(v.string()),
});
export const containerArchiveInfoCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const containerArchiveInfoCommandQuerySchema = v.strictObject({
	path: v.string(),
});
export const containerPruneCommandQuerySchema = v.strictObject({
	filters: v.exactOptional(v.string()),
});
export const imageListCommandQuerySchema = v.strictObject({
	all: v.exactOptional(v.boolean()),
	filters: v.exactOptional(v.string()),
	"shared-size": v.exactOptional(v.boolean()),
	digests: v.exactOptional(v.boolean()),
});
export const imageBuildCommandQuerySchema = v.strictObject({
	dockerfile: v.exactOptional(v.string()),
	t: v.exactOptional(v.string()),
	extrahosts: v.exactOptional(v.string()),
	remote: v.exactOptional(v.string()),
	q: v.exactOptional(v.boolean()),
	nocache: v.exactOptional(v.boolean()),
	cachefrom: v.exactOptional(v.string()),
	pull: v.exactOptional(v.string()),
	rm: v.exactOptional(v.boolean()),
	forcerm: v.exactOptional(v.boolean()),
	memory: v.exactOptional(v.pipe(v.number(), v.integer())),
	memswap: v.exactOptional(v.pipe(v.number(), v.integer())),
	cpushares: v.exactOptional(v.pipe(v.number(), v.integer())),
	cpusetcpus: v.exactOptional(v.string()),
	cpuperiod: v.exactOptional(v.pipe(v.number(), v.integer())),
	cpuquota: v.exactOptional(v.pipe(v.number(), v.integer())),
	buildargs: v.exactOptional(v.string()),
	shmsize: v.exactOptional(v.pipe(v.number(), v.integer())),
	squash: v.exactOptional(v.boolean()),
	labels: v.exactOptional(v.string()),
	networkmode: v.exactOptional(v.string()),
	platform: v.exactOptional(v.string()),
	target: v.exactOptional(v.string()),
	outputs: v.exactOptional(v.string()),
	version: v.exactOptional(v.picklist(["1", "2"])),
});
export const buildPruneCommandQuerySchema = v.strictObject({
	"keep-storage": v.exactOptional(v.pipe(v.number(), v.integer())),
	all: v.exactOptional(v.boolean()),
	filters: v.exactOptional(v.string()),
});
export const imageCreateCommandQuerySchema = v.strictObject({
	fromImage: v.exactOptional(v.string()),
	fromSrc: v.exactOptional(v.string()),
	repo: v.exactOptional(v.string()),
	tag: v.exactOptional(v.string()),
	message: v.exactOptional(v.string()),
	changes: v.exactOptional(v.array(v.string())),
	platform: v.exactOptional(v.string()),
});
export const imageInspectCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const imageHistoryCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const imagePushCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const imagePushCommandQuerySchema = v.strictObject({
	tag: v.exactOptional(v.string()),
});
export const imageTagCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const imageTagCommandQuerySchema = v.strictObject({
	repo: v.exactOptional(v.string()),
	tag: v.exactOptional(v.string()),
});
export const imageDeleteCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const imageDeleteCommandQuerySchema = v.strictObject({
	force: v.exactOptional(v.boolean()),
	noprune: v.exactOptional(v.boolean()),
});
export const imageSearchCommandQuerySchema = v.strictObject({
	term: v.string(),
	limit: v.exactOptional(v.pipe(v.number(), v.integer())),
	filters: v.exactOptional(v.string()),
});
export const imagePruneCommandQuerySchema = v.strictObject({
	filters: v.exactOptional(v.string()),
});
export const systemAuthCommandBodySchema = v.strictObject({
	username: v.exactOptional(v.string()),
	password: v.exactOptional(v.string()),
	email: v.exactOptional(v.string()),
	serveraddress: v.exactOptional(v.string()),
});
export const imageCommitCommandBodySchema = v.strictObject({
	/**
	 * The hostname to use for the container, as a valid RFC 1123 hostname.
	 */
	Hostname: v.exactOptional(v.string()),
	/**
	 * The domain name to use for the container.
	 */
	Domainname: v.exactOptional(v.string()),
	/**
	 * The user that commands are run as inside the container.
	 */
	User: v.exactOptional(v.string()),
	/**
	 * Whether to attach to `stdin`.
	 */
	AttachStdin: v.exactOptional(v.boolean()),
	/**
	 * Whether to attach to `stdout`.
	 */
	AttachStdout: v.exactOptional(v.boolean()),
	/**
	 * Whether to attach to `stderr`.
	 */
	AttachStderr: v.exactOptional(v.boolean()),
	/**
	 * An object mapping ports to an empty object in the form:
	 *
	 * `{"<port>/<tcp|udp|sctp>": {}}`
	 */
	ExposedPorts: v.exactOptional(v.nullable(v.record(v.string(), v.unknown()))),
	/**
	 * Attach standard streams to a TTY, including `stdin` if it is not closed.
	 */
	Tty: v.exactOptional(v.boolean()),
	/**
	 * Open `stdin`
	 */
	OpenStdin: v.exactOptional(v.boolean()),
	/**
	 * Close `stdin` after one attached client disconnects
	 */
	StdinOnce: v.exactOptional(v.boolean()),
	/**
	 * A list of environment variables to set inside the container in the
	 * form `["VAR=value", ...]`. A variable without `=` is removed from the
	 * environment, rather than to have an empty value.
	 */
	Env: v.exactOptional(v.array(v.string())),
	/**
	 * Command to run specified as a string or an array of strings.
	 */
	Cmd: v.exactOptional(v.array(v.string())),
	/**
	 * A test to perform to check that the container is healthy.
	 */
	Healthcheck: v.exactOptional(
		v.strictObject({
			/**
			 * The test to perform. Possible values are:
			 *
			 * - `[]` inherit healthcheck from image or parent image
			 * - `["NONE"]` disable healthcheck
			 * - `["CMD", args...]` exec arguments directly
			 * - `["CMD-SHELL", command]` run command with system's default shell
			 */
			Test: v.exactOptional(v.array(v.string())),
			/**
			 * The time to wait between checks in nanoseconds. It should be 0 or at
			 * least 1000000 (1 ms). 0 means inherit.
			 */
			Interval: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The time to wait before considering the check to have hung. It should
			 * be 0 or at least 1000000 (1 ms). 0 means inherit.
			 */
			Timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The number of consecutive failures needed to consider a container as
			 * unhealthy. 0 means inherit.
			 */
			Retries: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * Start period for the container to initialize before starting
			 * health-retries countdown in nanoseconds. It should be 0 or at least
			 * 1000000 (1 ms). 0 means inherit.
			 */
			StartPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	/**
	 * Command is already escaped (Windows only)
	 */
	ArgsEscaped: v.exactOptional(v.nullable(v.boolean())),
	/**
	 * The name (or reference) of the image to use when creating the container,
	 * or which was used when the container was created.
	 */
	Image: v.exactOptional(v.string()),
	/**
	 * An object mapping mount point paths inside the container to empty
	 * objects.
	 */
	Volumes: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * The working directory for commands to run in.
	 */
	WorkingDir: v.exactOptional(v.string()),
	/**
	 * The entry point for the container as a string or an array of strings.
	 *
	 * If the array consists of exactly one empty string (`[""]`) then the
	 * entry point is reset to system default (i.e., the entry point used by
	 * docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`).
	 */
	Entrypoint: v.exactOptional(v.array(v.string())),
	/**
	 * Disable networking for the container.
	 */
	NetworkDisabled: v.exactOptional(v.nullable(v.boolean())),
	/**
	 * MAC address of the container.
	 */
	MacAddress: v.exactOptional(v.nullable(v.string())),
	/**
	 * `ONBUILD` metadata that were defined in the image's `Dockerfile`.
	 */
	OnBuild: v.exactOptional(v.nullable(v.array(v.string()))),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Signal to stop a container as a string or unsigned integer.
	 */
	StopSignal: v.exactOptional(v.nullable(v.string())),
	/**
	 * Timeout to stop a container in seconds.
	 */
	StopTimeout: v.exactOptional(v.nullable(v.pipe(v.number(), v.integer()))),
	/**
	 * Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell.
	 */
	Shell: v.exactOptional(v.nullable(v.array(v.string()))),
});
export const imageCommitCommandQuerySchema = v.strictObject({
	container: v.exactOptional(v.string()),
	repo: v.exactOptional(v.string()),
	tag: v.exactOptional(v.string()),
	comment: v.exactOptional(v.string()),
	author: v.exactOptional(v.string()),
	pause: v.exactOptional(v.boolean()),
	changes: v.exactOptional(v.string()),
});
export const systemEventsCommandQuerySchema = v.strictObject({
	since: v.exactOptional(v.string()),
	until: v.exactOptional(v.string()),
	filters: v.exactOptional(v.string()),
});
export const systemDataUsageCommandQuerySchema = v.strictObject({
	type: v.exactOptional(
		v.array(v.picklist(["container", "image", "volume", "build-cache"])),
	),
});
export const imageGetCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const imageGetAllCommandQuerySchema = v.strictObject({
	names: v.exactOptional(v.array(v.string())),
});
export const imageLoadCommandQuerySchema = v.strictObject({
	quiet: v.exactOptional(v.boolean()),
});
export const containerExecCommandBodySchema = v.strictObject({
	/**
	 * Attach to `stdin` of the exec command.
	 */
	AttachStdin: v.exactOptional(v.boolean()),
	/**
	 * Attach to `stdout` of the exec command.
	 */
	AttachStdout: v.exactOptional(v.boolean()),
	/**
	 * Attach to `stderr` of the exec command.
	 */
	AttachStderr: v.exactOptional(v.boolean()),
	/**
	 * Initial console size, as an `[height, width]` array.
	 */
	ConsoleSize: v.exactOptional(
		v.nullable(
			v.pipe(
				v.array(v.pipe(v.number(), v.integer(), v.minValue(0))),
				v.minLength(2),
				v.maxLength(2),
			),
		),
	),
	/**
	 * Override the key sequence for detaching a container. Format is
	 * a single character `[a-Z]` or `ctrl-<value>` where `<value>`
	 * is one of: `a-z`, `@`, `^`, `[`, `,` or `_`.
	 */
	DetachKeys: v.exactOptional(v.string()),
	/**
	 * Allocate a pseudo-TTY.
	 */
	Tty: v.exactOptional(v.boolean()),
	/**
	 * A list of environment variables in the form `["VAR=value", ...]`.
	 */
	Env: v.exactOptional(v.array(v.string())),
	/**
	 * Command to run, as a string or array of strings.
	 */
	Cmd: v.exactOptional(v.array(v.string())),
	/**
	 * Runs the exec process with extended privileges.
	 */
	Privileged: v.exactOptional(v.boolean()),
	/**
	 * The user, and optionally, group to run the exec process inside
	 * the container. Format is one of: `user`, `user:group`, `uid`,
	 * or `uid:gid`.
	 */
	User: v.exactOptional(v.string()),
	/**
	 * The working directory for the exec process inside the container.
	 */
	WorkingDir: v.exactOptional(v.string()),
});
export const containerExecCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const execStartCommandBodySchema = v.strictObject({
	/**
	 * Detach from the command.
	 */
	Detach: v.exactOptional(v.boolean()),
	/**
	 * Allocate a pseudo-TTY.
	 */
	Tty: v.exactOptional(v.boolean()),
	/**
	 * Initial console size, as an `[height, width]` array.
	 */
	ConsoleSize: v.exactOptional(
		v.nullable(
			v.pipe(
				v.array(v.pipe(v.number(), v.integer(), v.minValue(0))),
				v.minLength(2),
				v.maxLength(2),
			),
		),
	),
});
export const execStartCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const execResizeCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const execResizeCommandQuerySchema = v.strictObject({
	h: v.pipe(v.number(), v.integer()),
	w: v.pipe(v.number(), v.integer()),
});
export const execInspectCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const volumeListCommandQuerySchema = v.strictObject({
	filters: v.exactOptional(v.string()),
});
export const volumeCreateCommandBodySchema = v.strictObject({
	/**
	 * The new volume's name. If not specified, Docker generates a name.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * Name of the volume driver to use.
	 */
	Driver: v.exactOptional(v.string()),
	/**
	 * A mapping of driver options and values. These options are
	 * passed directly to the driver and are driver specific.
	 */
	DriverOpts: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Cluster-specific options used to create the volume.
	 */
	ClusterVolumeSpec: v.exactOptional(
		v.strictObject({
			/**
			 * Group defines the volume group of this volume. Volumes belonging to
			 * the same group can be referred to by group name when creating
			 * Services.  Referring to a volume by group instructs Swarm to treat
			 * volumes in that group interchangeably for the purpose of scheduling.
			 * Volumes with an empty string for a group technically all belong to
			 * the same, emptystring group.
			 */
			Group: v.exactOptional(v.string()),
			/**
			 * Defines how the volume is used by tasks.
			 */
			AccessMode: v.exactOptional(
				v.strictObject({
					/**
					 * The set of nodes this volume can be used on at one time.
					 * - `single` The volume may only be scheduled to one node at a time.
					 * - `multi` the volume may be scheduled to any supported number of nodes at a
					 * time.
					 */
					Scope: v.exactOptional(v.picklist(["single", "multi"])),
					/**
					 * The number and way that different tasks can use this volume
					 * at one time.
					 * - `none` The volume may only be used by one task at a time.
					 * - `readonly` The volume may be used by any number of tasks, but they all
					 * must mount the volume as readonly
					 * - `onewriter` The volume may be used by any number of tasks, but only one
					 * may mount it as read/write.
					 * - `all` The volume may have any number of readers and writers.
					 */
					Sharing: v.exactOptional(
						v.picklist(["none", "readonly", "onewriter", "all"]),
					),
					/**
					 * Options for using this volume as a Mount-type volume.
					 *
					 *
					 * Either MountVolume or BlockVolume, but not both, must be
					 *
					 * present.
					 *
					 * properties:
					 *
					 * FsType:
					 *
					 * type: "string"
					 *
					 * description: |
					 *
					 * Specifies the filesystem type for the mount volume.
					 *
					 * Optional.
					 *
					 * MountFlags:
					 *
					 * type: "array"
					 *
					 * description: |
					 *
					 * Flags to pass when mounting the volume. Optional.
					 *
					 * items:
					 *
					 * type: "string"
					 * BlockVolume:
					 *
					 * type: "object"
					 *
					 * description: |
					 *
					 * Options for using this volume as a Block-type volume.
					 *
					 * Intentionally empty.
					 */
					MountVolume: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * Swarm Secrets that are passed to the CSI storage plugin when
					 * operating on this volume.
					 */
					Secrets: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Key is the name of the key of the key-value pair passed to
								 * the plugin.
								 */
								Key: v.exactOptional(v.string()),
								/**
								 * Secret is the swarm Secret object from which to read data.
								 * This can be a Secret name or ID. The Secret data is
								 * retrieved by swarm and used as the value of the key-value
								 * pair passed to the plugin.
								 */
								Secret: v.exactOptional(v.string()),
							}),
						),
					),
					/**
					 * Requirements for the accessible topology of the volume. These
					 * fields are optional. For an in-depth description of what these
					 * fields mean, see the CSI specification.
					 */
					AccessibilityRequirements: v.exactOptional(
						v.strictObject({
							/**
							 * A list of required topologies, at least one of which the
							 * volume must be accessible from.
							 */
							Requisite: v.exactOptional(
								v.array(v.record(v.string(), v.unknown())),
							),
							/**
							 * A list of topologies that the volume should attempt to be
							 * provisioned in.
							 */
							Preferred: v.exactOptional(
								v.array(v.record(v.string(), v.unknown())),
							),
						}),
					),
					/**
					 * The desired capacity that the volume should be created with. If
					 * empty, the plugin will decide the capacity.
					 */
					CapacityRange: v.exactOptional(
						v.strictObject({
							/**
							 * The volume must be at least this big. The value of 0
							 * indicates an unspecified minimum
							 */
							RequiredBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The volume must not be bigger than this. The value of 0
							 * indicates an unspecified maximum.
							 */
							LimitBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
					/**
					 * The availability of the volume for use in tasks.
					 * - `active` The volume is fully available for scheduling on the cluster
					 * - `pause` No new workloads should use the volume, but existing workloads
					 * are not stopped.
					 * - `drain` All workloads using this volume should be stopped and
					 * rescheduled, and no new ones should be started.
					 */
					Availability: v.exactOptional(
						v.picklist(["active", "pause", "drain"]),
					),
				}),
			),
		}),
	),
});
export const volumeInspectCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const volumeUpdateCommandBodySchema = v.strictObject({
	/**
	 * Cluster-specific options used to create the volume.
	 */
	Spec: v.exactOptional(
		v.strictObject({
			/**
			 * Group defines the volume group of this volume. Volumes belonging to
			 * the same group can be referred to by group name when creating
			 * Services.  Referring to a volume by group instructs Swarm to treat
			 * volumes in that group interchangeably for the purpose of scheduling.
			 * Volumes with an empty string for a group technically all belong to
			 * the same, emptystring group.
			 */
			Group: v.exactOptional(v.string()),
			/**
			 * Defines how the volume is used by tasks.
			 */
			AccessMode: v.exactOptional(
				v.strictObject({
					/**
					 * The set of nodes this volume can be used on at one time.
					 * - `single` The volume may only be scheduled to one node at a time.
					 * - `multi` the volume may be scheduled to any supported number of nodes at a
					 * time.
					 */
					Scope: v.exactOptional(v.picklist(["single", "multi"])),
					/**
					 * The number and way that different tasks can use this volume
					 * at one time.
					 * - `none` The volume may only be used by one task at a time.
					 * - `readonly` The volume may be used by any number of tasks, but they all
					 * must mount the volume as readonly
					 * - `onewriter` The volume may be used by any number of tasks, but only one
					 * may mount it as read/write.
					 * - `all` The volume may have any number of readers and writers.
					 */
					Sharing: v.exactOptional(
						v.picklist(["none", "readonly", "onewriter", "all"]),
					),
					/**
					 * Options for using this volume as a Mount-type volume.
					 *
					 *
					 * Either MountVolume or BlockVolume, but not both, must be
					 *
					 * present.
					 *
					 * properties:
					 *
					 * FsType:
					 *
					 * type: "string"
					 *
					 * description: |
					 *
					 * Specifies the filesystem type for the mount volume.
					 *
					 * Optional.
					 *
					 * MountFlags:
					 *
					 * type: "array"
					 *
					 * description: |
					 *
					 * Flags to pass when mounting the volume. Optional.
					 *
					 * items:
					 *
					 * type: "string"
					 * BlockVolume:
					 *
					 * type: "object"
					 *
					 * description: |
					 *
					 * Options for using this volume as a Block-type volume.
					 *
					 * Intentionally empty.
					 */
					MountVolume: v.exactOptional(v.record(v.string(), v.unknown())),
					/**
					 * Swarm Secrets that are passed to the CSI storage plugin when
					 * operating on this volume.
					 */
					Secrets: v.exactOptional(
						v.array(
							v.strictObject({
								/**
								 * Key is the name of the key of the key-value pair passed to
								 * the plugin.
								 */
								Key: v.exactOptional(v.string()),
								/**
								 * Secret is the swarm Secret object from which to read data.
								 * This can be a Secret name or ID. The Secret data is
								 * retrieved by swarm and used as the value of the key-value
								 * pair passed to the plugin.
								 */
								Secret: v.exactOptional(v.string()),
							}),
						),
					),
					/**
					 * Requirements for the accessible topology of the volume. These
					 * fields are optional. For an in-depth description of what these
					 * fields mean, see the CSI specification.
					 */
					AccessibilityRequirements: v.exactOptional(
						v.strictObject({
							/**
							 * A list of required topologies, at least one of which the
							 * volume must be accessible from.
							 */
							Requisite: v.exactOptional(
								v.array(v.record(v.string(), v.unknown())),
							),
							/**
							 * A list of topologies that the volume should attempt to be
							 * provisioned in.
							 */
							Preferred: v.exactOptional(
								v.array(v.record(v.string(), v.unknown())),
							),
						}),
					),
					/**
					 * The desired capacity that the volume should be created with. If
					 * empty, the plugin will decide the capacity.
					 */
					CapacityRange: v.exactOptional(
						v.strictObject({
							/**
							 * The volume must be at least this big. The value of 0
							 * indicates an unspecified minimum
							 */
							RequiredBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The volume must not be bigger than this. The value of 0
							 * indicates an unspecified maximum.
							 */
							LimitBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
						}),
					),
					/**
					 * The availability of the volume for use in tasks.
					 * - `active` The volume is fully available for scheduling on the cluster
					 * - `pause` No new workloads should use the volume, but existing workloads
					 * are not stopped.
					 * - `drain` All workloads using this volume should be stopped and
					 * rescheduled, and no new ones should be started.
					 */
					Availability: v.exactOptional(
						v.picklist(["active", "pause", "drain"]),
					),
				}),
			),
		}),
	),
});
export const volumeUpdateCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const volumeUpdateCommandQuerySchema = v.strictObject({
	version: v.pipe(v.number(), v.integer()),
});
export const volumeDeleteCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const volumeDeleteCommandQuerySchema = v.strictObject({
	force: v.exactOptional(v.boolean()),
});
export const volumePruneCommandQuerySchema = v.strictObject({
	filters: v.exactOptional(v.string()),
});
export const networkListCommandQuerySchema = v.strictObject({
	filters: v.exactOptional(v.string()),
});
export const networkInspectCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const networkInspectCommandQuerySchema = v.strictObject({
	verbose: v.exactOptional(v.boolean()),
	scope: v.exactOptional(v.string()),
});
export const networkDeleteCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const networkCreateCommandBodySchema = v.strictObject({
	/**
	 * The network's name.
	 */
	Name: v.string(),
	/**
	 * Check for networks with duplicate names. Since Network is
	 * primarily keyed based on a random ID and not on the name, and
	 * network name is strictly a user-friendly alias to the network
	 * which is uniquely identified using ID, there is no guaranteed
	 * way to check for duplicates. CheckDuplicate is there to provide
	 * a best effort checking of any networks which has the same name
	 * but it is not guaranteed to catch all name collisions.
	 */
	CheckDuplicate: v.exactOptional(v.boolean()),
	/**
	 * Name of the network driver plugin to use.
	 */
	Driver: v.exactOptional(v.string()),
	/**
	 * The level at which the network exists (e.g. `swarm` for cluster-wide
	 * or `local` for machine level).
	 */
	Scope: v.exactOptional(v.string()),
	/**
	 * Restrict external access to the network.
	 */
	Internal: v.exactOptional(v.boolean()),
	/**
	 * Globally scoped network is manually attachable by regular
	 * containers from workers in swarm mode.
	 */
	Attachable: v.exactOptional(v.boolean()),
	/**
	 * Ingress network is the network which provides the routing-mesh
	 * in swarm mode.
	 */
	Ingress: v.exactOptional(v.boolean()),
	/**
	 * Creates a config-only network. Config-only networks are placeholder
	 * networks for network configurations to be used by other networks.
	 * Config-only networks cannot be used directly to run containers
	 * or services.
	 */
	ConfigOnly: v.exactOptional(v.boolean()),
	/**
	 * The config-only network source to provide the configuration for
	 * this network.
	 */
	ConfigFrom: v.exactOptional(
		v.strictObject({
			/**
			 * The name of the config-only network that provides the network's
			 * configuration. The specified network must be an existing config-only
			 * network. Only network names are allowed, not network IDs.
			 */
			Network: v.exactOptional(v.string()),
		}),
	),
	IPAM: v.exactOptional(
		v.strictObject({
			/**
			 * Name of the IPAM driver to use.
			 */
			Driver: v.exactOptional(v.string()),
			/**
			 * List of IPAM configuration options, specified as a map:
			 *
			 * ```
			 * {"Subnet": <CIDR>, "IPRange": <CIDR>, "Gateway": <IP address>,
			 * "AuxAddress": <device_name:IP address>}
			 * ```
			 */
			Config: v.exactOptional(
				v.array(
					v.strictObject({
						Subnet: v.exactOptional(v.string()),
						IPRange: v.exactOptional(v.string()),
						Gateway: v.exactOptional(v.string()),
						AuxiliaryAddresses: v.exactOptional(
							v.record(v.string(), v.unknown()),
						),
					}),
				),
			),
			/**
			 * Driver-specific options, specified as a map.
			 */
			Options: v.exactOptional(v.record(v.string(), v.unknown())),
		}),
	),
	/**
	 * Enable IPv6 on the network.
	 */
	EnableIPv6: v.exactOptional(v.boolean()),
	/**
	 * Network specific options to be used by the drivers.
	 */
	Options: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
});
export const networkConnectCommandBodySchema = v.strictObject({
	/**
	 * The ID or name of the container to connect to the network.
	 */
	Container: v.exactOptional(v.string()),
	/**
	 * Configuration for a network endpoint.
	 */
	EndpointConfig: v.exactOptional(
		v.strictObject({
			/**
			 * EndpointIPAMConfig represents an endpoint's IPAM configuration.
			 */
			IPAMConfig: v.exactOptional(
				v.nullable(
					v.strictObject({
						IPv4Address: v.exactOptional(v.string()),
						IPv6Address: v.exactOptional(v.string()),
						LinkLocalIPs: v.exactOptional(v.array(v.string())),
					}),
				),
			),
			Links: v.exactOptional(v.array(v.string())),
			Aliases: v.exactOptional(v.array(v.string())),
			/**
			 * Unique ID of the network.
			 */
			NetworkID: v.exactOptional(v.string()),
			/**
			 * Unique ID for the service endpoint in a Sandbox.
			 */
			EndpointID: v.exactOptional(v.string()),
			/**
			 * Gateway address for this network.
			 */
			Gateway: v.exactOptional(v.string()),
			/**
			 * IPv4 address.
			 */
			IPAddress: v.exactOptional(v.string()),
			/**
			 * Mask length of the IPv4 address.
			 */
			IPPrefixLen: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * IPv6 gateway address.
			 */
			IPv6Gateway: v.exactOptional(v.string()),
			/**
			 * Global IPv6 address.
			 */
			GlobalIPv6Address: v.exactOptional(v.string()),
			/**
			 * Mask length of the global IPv6 address.
			 */
			GlobalIPv6PrefixLen: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * MAC address for the endpoint on this network.
			 */
			MacAddress: v.exactOptional(v.string()),
			/**
			 * DriverOpts is a mapping of driver options and values. These options
			 * are passed directly to the driver and are driver specific.
			 */
			DriverOpts: v.exactOptional(
				v.nullable(v.record(v.string(), v.unknown())),
			),
		}),
	),
});
export const networkConnectCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const networkDisconnectCommandBodySchema = v.strictObject({
	/**
	 * The ID or name of the container to disconnect from the network.
	 */
	Container: v.exactOptional(v.string()),
	/**
	 * Force the container to disconnect from the network.
	 */
	Force: v.exactOptional(v.boolean()),
});
export const networkDisconnectCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const networkPruneCommandQuerySchema = v.strictObject({
	filters: v.exactOptional(v.string()),
});
export const pluginListCommandQuerySchema = v.strictObject({
	filters: v.exactOptional(v.string()),
});
export const getPluginPrivilegesCommandQuerySchema = v.strictObject({
	remote: v.string(),
});
export const pluginPullCommandBodySchema = v.array(
	v.strictObject({
		Name: v.exactOptional(v.string()),
		Description: v.exactOptional(v.string()),
		Value: v.exactOptional(v.array(v.string())),
	}),
);
export const pluginPullCommandQuerySchema = v.strictObject({
	remote: v.string(),
	name: v.exactOptional(v.string()),
});
export const pluginInspectCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const pluginDeleteCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const pluginDeleteCommandQuerySchema = v.strictObject({
	force: v.exactOptional(v.boolean()),
});
export const pluginEnableCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const pluginEnableCommandQuerySchema = v.strictObject({
	timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
});
export const pluginDisableCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const pluginDisableCommandQuerySchema = v.strictObject({
	force: v.exactOptional(v.boolean()),
});
export const pluginUpgradeCommandBodySchema = v.array(
	v.strictObject({
		Name: v.exactOptional(v.string()),
		Description: v.exactOptional(v.string()),
		Value: v.exactOptional(v.array(v.string())),
	}),
);
export const pluginUpgradeCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const pluginUpgradeCommandQuerySchema = v.strictObject({
	remote: v.string(),
});
export const pluginCreateCommandQuerySchema = v.strictObject({
	name: v.string(),
});
export const pluginPushCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const pluginSetCommandBodySchema = v.array(v.string());
export const pluginSetCommandParamsSchema = v.strictObject({
	name: v.string(),
});
export const nodeListCommandQuerySchema = v.strictObject({
	filters: v.exactOptional(v.string()),
});
export const nodeInspectCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const nodeDeleteCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const nodeDeleteCommandQuerySchema = v.strictObject({
	force: v.exactOptional(v.boolean()),
});
export const nodeUpdateCommandBodySchema = v.strictObject({
	/**
	 * Name for the node.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Role of the node.
	 */
	Role: v.exactOptional(v.picklist(["worker", "manager"])),
	/**
	 * Availability of the node.
	 */
	Availability: v.exactOptional(v.picklist(["active", "pause", "drain"])),
});
export const nodeUpdateCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const nodeUpdateCommandQuerySchema = v.strictObject({
	version: v.pipe(v.number(), v.integer()),
});
export const swarmInitCommandBodySchema = v.strictObject({
	/**
	 * Listen address used for inter-manager communication, as well
	 * as determining the networking interface used for the VXLAN
	 * Tunnel Endpoint (VTEP). This can either be an address/port
	 * combination in the form `192.168.1.1:4567`, or an interface
	 * followed by a port number, like `eth0:4567`. If the port number
	 * is omitted, the default swarm listening port is used.
	 */
	ListenAddr: v.exactOptional(v.string()),
	/**
	 * Externally reachable address advertised to other nodes. This
	 * can either be an address/port combination in the form
	 * `192.168.1.1:4567`, or an interface followed by a port number,
	 * like `eth0:4567`. If the port number is omitted, the port
	 * number from the listen address is used. If `AdvertiseAddr` is
	 * not specified, it will be automatically detected when possible.
	 */
	AdvertiseAddr: v.exactOptional(v.string()),
	/**
	 * Address or interface to use for data path traffic (format:
	 * `<ip|interface>`), for example,  `192.168.1.1`, or an interface,
	 * like `eth0`. If `DataPathAddr` is unspecified, the same address
	 * as `AdvertiseAddr` is used.
	 *
	 * The `DataPathAddr` specifies the address that global scope
	 * network drivers will publish towards other  nodes in order to
	 * reach the containers running on this node. Using this parameter
	 * it is possible to separate the container data traffic from the
	 * management traffic of the cluster.
	 */
	DataPathAddr: v.exactOptional(v.string()),
	/**
	 * DataPathPort specifies the data path port number for data traffic.
	 * Acceptable port range is 1024 to 49151.
	 * if no port is set or is set to 0, default port 4789 will be used.
	 */
	DataPathPort: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * Default Address Pool specifies default subnet pools for global
	 * scope networks.
	 */
	DefaultAddrPool: v.exactOptional(v.array(v.string())),
	/**
	 * Force creation of a new swarm.
	 */
	ForceNewCluster: v.exactOptional(v.boolean()),
	/**
	 * SubnetSize specifies the subnet size of the networks created
	 * from the default subnet pool.
	 */
	SubnetSize: v.exactOptional(v.pipe(v.number(), v.integer())),
	/**
	 * User modifiable swarm configuration.
	 */
	Spec: v.exactOptional(
		v.strictObject({
			/**
			 * Name of the swarm.
			 */
			Name: v.exactOptional(v.string()),
			/**
			 * User-defined key/value metadata.
			 */
			Labels: v.exactOptional(v.record(v.string(), v.unknown())),
			/**
			 * Orchestration configuration.
			 */
			Orchestration: v.exactOptional(
				v.nullable(
					v.strictObject({
						/**
						 * The number of historic tasks to keep per instance or node. If
						 * negative, never remove completed or failed tasks.
						 */
						TaskHistoryRetentionLimit: v.exactOptional(
							v.pipe(v.number(), v.integer()),
						),
					}),
				),
			),
			/**
			 * Raft configuration.
			 */
			Raft: v.exactOptional(
				v.strictObject({
					/**
					 * The number of log entries between snapshots.
					 */
					SnapshotInterval: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The number of snapshots to keep beyond the current snapshot.
					 */
					KeepOldSnapshots: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The number of log entries to keep around to sync up slow followers
					 * after a snapshot is created.
					 */
					LogEntriesForSlowFollowers: v.exactOptional(
						v.pipe(v.number(), v.integer()),
					),
					/**
					 * The number of ticks that a follower will wait for a message from
					 * the leader before becoming a candidate and starting an election.
					 * `ElectionTick` must be greater than `HeartbeatTick`.
					 *
					 * A tick currently defaults to one second, so these translate
					 * directly to seconds currently, but this is NOT guaranteed.
					 */
					ElectionTick: v.exactOptional(v.pipe(v.number(), v.integer())),
					/**
					 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
					 * the leader will send a heartbeat to the followers.
					 *
					 * A tick currently defaults to one second, so these translate
					 * directly to seconds currently, but this is NOT guaranteed.
					 */
					HeartbeatTick: v.exactOptional(v.pipe(v.number(), v.integer())),
				}),
			),
			/**
			 * Dispatcher configuration.
			 */
			Dispatcher: v.exactOptional(
				v.nullable(
					v.strictObject({
						/**
						 * The delay for an agent to send a heartbeat to the dispatcher.
						 */
						HeartbeatPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
			),
			/**
			 * CA configuration.
			 */
			CAConfig: v.exactOptional(
				v.nullable(
					v.strictObject({
						/**
						 * The duration node certificates are issued for.
						 */
						NodeCertExpiry: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * Configuration for forwarding signing requests to an external
						 * certificate authority.
						 */
						ExternalCAs: v.exactOptional(
							v.array(
								v.strictObject({
									/**
									 * Protocol for communication with the external CA (currently
									 * only `cfssl` is supported).
									 */
									Protocol: v.exactOptional(v.picklist(["cfssl"])),
									/**
									 * URL where certificate signing requests should be sent.
									 */
									URL: v.exactOptional(v.string()),
									/**
									 * An object with key/value pairs that are interpreted as
									 * protocol-specific options for the external CA driver.
									 */
									Options: v.exactOptional(v.record(v.string(), v.unknown())),
									/**
									 * The root CA certificate (in PEM format) this external CA uses
									 * to issue TLS certificates (assumed to be to the current swarm
									 * root CA certificate if not provided).
									 */
									CACert: v.exactOptional(v.string()),
								}),
							),
						),
						/**
						 * The desired signing CA certificate for all swarm node TLS leaf
						 * certificates, in PEM format.
						 */
						SigningCACert: v.exactOptional(v.string()),
						/**
						 * The desired signing CA key for all swarm node TLS leaf certificates,
						 * in PEM format.
						 */
						SigningCAKey: v.exactOptional(v.string()),
						/**
						 * An integer whose purpose is to force swarm to generate a new
						 * signing CA certificate and key, if none have been specified in
						 * `SigningCACert` and `SigningCAKey`
						 */
						ForceRotate: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
			),
			/**
			 * Parameters related to encryption-at-rest.
			 */
			EncryptionConfig: v.exactOptional(
				v.strictObject({
					/**
					 * If set, generate a key and use it to lock data stored on the
					 * managers.
					 */
					AutoLockManagers: v.exactOptional(v.boolean()),
				}),
			),
			/**
			 * Defaults for creating tasks in this cluster.
			 */
			TaskDefaults: v.exactOptional(
				v.strictObject({
					/**
					 * The log driver to use for tasks created in the orchestrator if
					 * unspecified by a service.
					 *
					 * Updating this value only affects new tasks. Existing tasks continue
					 * to use their previously configured log driver until recreated.
					 */
					LogDriver: v.exactOptional(
						v.strictObject({
							/**
							 * The log driver to use as a default for new tasks.
							 */
							Name: v.exactOptional(v.string()),
							/**
							 * Driver-specific options for the selected log driver, specified
							 * as key/value pairs.
							 */
							Options: v.exactOptional(v.record(v.string(), v.unknown())),
						}),
					),
				}),
			),
		}),
	),
});
export const swarmJoinCommandBodySchema = v.strictObject({
	/**
	 * Listen address used for inter-manager communication if the node
	 * gets promoted to manager, as well as determining the networking
	 * interface used for the VXLAN Tunnel Endpoint (VTEP).
	 */
	ListenAddr: v.exactOptional(v.string()),
	/**
	 * Externally reachable address advertised to other nodes. This
	 * can either be an address/port combination in the form
	 * `192.168.1.1:4567`, or an interface followed by a port number,
	 * like `eth0:4567`. If the port number is omitted, the port
	 * number from the listen address is used. If `AdvertiseAddr` is
	 * not specified, it will be automatically detected when possible.
	 */
	AdvertiseAddr: v.exactOptional(v.string()),
	/**
	 * Address or interface to use for data path traffic (format:
	 * `<ip|interface>`), for example,  `192.168.1.1`, or an interface,
	 * like `eth0`. If `DataPathAddr` is unspecified, the same address
	 * as `AdvertiseAddr` is used.
	 *
	 * The `DataPathAddr` specifies the address that global scope
	 * network drivers will publish towards other nodes in order to
	 * reach the containers running on this node. Using this parameter
	 * it is possible to separate the container data traffic from the
	 * management traffic of the cluster.
	 */
	DataPathAddr: v.exactOptional(v.string()),
	/**
	 * Addresses of manager nodes already participating in the swarm.
	 */
	RemoteAddrs: v.exactOptional(v.array(v.string())),
	/**
	 * Secret token for joining this swarm.
	 */
	JoinToken: v.exactOptional(v.string()),
});
export const swarmLeaveCommandQuerySchema = v.strictObject({
	force: v.exactOptional(v.boolean()),
});
export const swarmUpdateCommandBodySchema = v.strictObject({
	/**
	 * Name of the swarm.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Orchestration configuration.
	 */
	Orchestration: v.exactOptional(
		v.nullable(
			v.strictObject({
				/**
				 * The number of historic tasks to keep per instance or node. If
				 * negative, never remove completed or failed tasks.
				 */
				TaskHistoryRetentionLimit: v.exactOptional(
					v.pipe(v.number(), v.integer()),
				),
			}),
		),
	),
	/**
	 * Raft configuration.
	 */
	Raft: v.exactOptional(
		v.strictObject({
			/**
			 * The number of log entries between snapshots.
			 */
			SnapshotInterval: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The number of snapshots to keep beyond the current snapshot.
			 */
			KeepOldSnapshots: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The number of log entries to keep around to sync up slow followers
			 * after a snapshot is created.
			 */
			LogEntriesForSlowFollowers: v.exactOptional(
				v.pipe(v.number(), v.integer()),
			),
			/**
			 * The number of ticks that a follower will wait for a message from
			 * the leader before becoming a candidate and starting an election.
			 * `ElectionTick` must be greater than `HeartbeatTick`.
			 *
			 * A tick currently defaults to one second, so these translate
			 * directly to seconds currently, but this is NOT guaranteed.
			 */
			ElectionTick: v.exactOptional(v.pipe(v.number(), v.integer())),
			/**
			 * The number of ticks between heartbeats. Every HeartbeatTick ticks,
			 * the leader will send a heartbeat to the followers.
			 *
			 * A tick currently defaults to one second, so these translate
			 * directly to seconds currently, but this is NOT guaranteed.
			 */
			HeartbeatTick: v.exactOptional(v.pipe(v.number(), v.integer())),
		}),
	),
	/**
	 * Dispatcher configuration.
	 */
	Dispatcher: v.exactOptional(
		v.nullable(
			v.strictObject({
				/**
				 * The delay for an agent to send a heartbeat to the dispatcher.
				 */
				HeartbeatPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
			}),
		),
	),
	/**
	 * CA configuration.
	 */
	CAConfig: v.exactOptional(
		v.nullable(
			v.strictObject({
				/**
				 * The duration node certificates are issued for.
				 */
				NodeCertExpiry: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Configuration for forwarding signing requests to an external
				 * certificate authority.
				 */
				ExternalCAs: v.exactOptional(
					v.array(
						v.strictObject({
							/**
							 * Protocol for communication with the external CA (currently
							 * only `cfssl` is supported).
							 */
							Protocol: v.exactOptional(v.picklist(["cfssl"])),
							/**
							 * URL where certificate signing requests should be sent.
							 */
							URL: v.exactOptional(v.string()),
							/**
							 * An object with key/value pairs that are interpreted as
							 * protocol-specific options for the external CA driver.
							 */
							Options: v.exactOptional(v.record(v.string(), v.unknown())),
							/**
							 * The root CA certificate (in PEM format) this external CA uses
							 * to issue TLS certificates (assumed to be to the current swarm
							 * root CA certificate if not provided).
							 */
							CACert: v.exactOptional(v.string()),
						}),
					),
				),
				/**
				 * The desired signing CA certificate for all swarm node TLS leaf
				 * certificates, in PEM format.
				 */
				SigningCACert: v.exactOptional(v.string()),
				/**
				 * The desired signing CA key for all swarm node TLS leaf certificates,
				 * in PEM format.
				 */
				SigningCAKey: v.exactOptional(v.string()),
				/**
				 * An integer whose purpose is to force swarm to generate a new
				 * signing CA certificate and key, if none have been specified in
				 * `SigningCACert` and `SigningCAKey`
				 */
				ForceRotate: v.exactOptional(v.pipe(v.number(), v.integer())),
			}),
		),
	),
	/**
	 * Parameters related to encryption-at-rest.
	 */
	EncryptionConfig: v.exactOptional(
		v.strictObject({
			/**
			 * If set, generate a key and use it to lock data stored on the
			 * managers.
			 */
			AutoLockManagers: v.exactOptional(v.boolean()),
		}),
	),
	/**
	 * Defaults for creating tasks in this cluster.
	 */
	TaskDefaults: v.exactOptional(
		v.strictObject({
			/**
			 * The log driver to use for tasks created in the orchestrator if
			 * unspecified by a service.
			 *
			 * Updating this value only affects new tasks. Existing tasks continue
			 * to use their previously configured log driver until recreated.
			 */
			LogDriver: v.exactOptional(
				v.strictObject({
					/**
					 * The log driver to use as a default for new tasks.
					 */
					Name: v.exactOptional(v.string()),
					/**
					 * Driver-specific options for the selected log driver, specified
					 * as key/value pairs.
					 */
					Options: v.exactOptional(v.record(v.string(), v.unknown())),
				}),
			),
		}),
	),
});
export const swarmUpdateCommandQuerySchema = v.strictObject({
	version: v.pipe(v.number(), v.integer()),
	rotateWorkerToken: v.exactOptional(v.boolean()),
	rotateManagerToken: v.exactOptional(v.boolean()),
	rotateManagerUnlockKey: v.exactOptional(v.boolean()),
});
export const swarmUnlockCommandBodySchema = v.strictObject({
	/**
	 * The swarm's unlock key.
	 */
	UnlockKey: v.exactOptional(v.string()),
});
export const serviceListCommandQuerySchema = v.strictObject({
	filters: v.exactOptional(v.string()),
	status: v.exactOptional(v.boolean()),
});
export const serviceCreateCommandBodySchema = v.intersect([
	v.strictObject({
		/**
		 * Name of the service.
		 */
		Name: v.exactOptional(v.string()),
		/**
		 * User-defined key/value metadata.
		 */
		Labels: v.exactOptional(v.record(v.string(), v.unknown())),
		/**
		 * User modifiable task configuration.
		 */
		TaskTemplate: v.exactOptional(
			v.strictObject({
				/**
				 * Plugin spec for the service.  *(Experimental release only.)*
				 *
				 * <p><br /></p>
				 *
				 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
				 * > mutually exclusive. PluginSpec is only used when the Runtime field
				 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
				 * > field is set to `attachment`.
				 */
				PluginSpec: v.exactOptional(
					v.strictObject({
						/**
						 * The name or 'alias' to use for the plugin.
						 */
						Name: v.exactOptional(v.string()),
						/**
						 * The plugin image reference to use.
						 */
						Remote: v.exactOptional(v.string()),
						/**
						 * Disable the plugin once scheduled.
						 */
						Disabled: v.exactOptional(v.boolean()),
						PluginPrivilege: v.exactOptional(
							v.array(
								v.strictObject({
									Name: v.exactOptional(v.string()),
									Description: v.exactOptional(v.string()),
									Value: v.exactOptional(v.array(v.string())),
								}),
							),
						),
					}),
				),
				/**
				 * Container spec for the service.
				 *
				 * <p><br /></p>
				 *
				 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
				 * > mutually exclusive. PluginSpec is only used when the Runtime field
				 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
				 * > field is set to `attachment`.
				 */
				ContainerSpec: v.exactOptional(
					v.strictObject({
						/**
						 * The image name to use for the container
						 */
						Image: v.exactOptional(v.string()),
						/**
						 * User-defined key/value data.
						 */
						Labels: v.exactOptional(v.record(v.string(), v.unknown())),
						/**
						 * The command to be run in the image.
						 */
						Command: v.exactOptional(v.array(v.string())),
						/**
						 * Arguments to the command.
						 */
						Args: v.exactOptional(v.array(v.string())),
						/**
						 * The hostname to use for the container, as a valid
						 * [RFC 1123](https://tools.ietf.org/html/rfc1123) hostname.
						 */
						Hostname: v.exactOptional(v.string()),
						/**
						 * A list of environment variables in the form `VAR=value`.
						 */
						Env: v.exactOptional(v.array(v.string())),
						/**
						 * The working directory for commands to run in.
						 */
						Dir: v.exactOptional(v.string()),
						/**
						 * The user inside the container.
						 */
						User: v.exactOptional(v.string()),
						/**
						 * A list of additional groups that the container process will run as.
						 */
						Groups: v.exactOptional(v.array(v.string())),
						/**
						 * Security options for the container
						 */
						Privileges: v.exactOptional(
							v.strictObject({
								/**
								 * CredentialSpec for managed service account (Windows only)
								 */
								CredentialSpec: v.exactOptional(
									v.strictObject({
										/**
										 * Load credential spec from a Swarm Config with the given ID.
										 * The specified config must also be present in the Configs
										 * field with the Runtime property set.
										 *
										 * <p><br /></p>
										 *
										 *
										 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
										 * > and `CredentialSpec.Config` are mutually exclusive.
										 */
										Config: v.exactOptional(v.string()),
										/**
										 * Load credential spec from this file. The file is read by
										 * the daemon, and must be present in the `CredentialSpecs`
										 * subdirectory in the docker data directory, which defaults
										 * to `C:\ProgramData\Docker\` on Windows.
										 *
										 * For example, specifying `spec.json` loads
										 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
										 *
										 * <p><br /></p>
										 *
										 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
										 * > and `CredentialSpec.Config` are mutually exclusive.
										 */
										File: v.exactOptional(v.string()),
										/**
										 * Load credential spec from this value in the Windows
										 * registry. The specified registry value must be located in:
										 *
										 * `HKLM\SOFTWARE\Microsoft\Windows
										 * NT\CurrentVersion\Virtualization\Containers\CredentialSpecs`
										 *
										 * <p><br /></p>
										 *
										 *
										 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
										 * > and `CredentialSpec.Config` are mutually exclusive.
										 */
										Registry: v.exactOptional(v.string()),
									}),
								),
								/**
								 * SELinux labels of the container
								 */
								SELinuxContext: v.exactOptional(
									v.strictObject({
										/**
										 * Disable SELinux
										 */
										Disable: v.exactOptional(v.boolean()),
										/**
										 * SELinux user label
										 */
										User: v.exactOptional(v.string()),
										/**
										 * SELinux role label
										 */
										Role: v.exactOptional(v.string()),
										/**
										 * SELinux type label
										 */
										Type: v.exactOptional(v.string()),
										/**
										 * SELinux level label
										 */
										Level: v.exactOptional(v.string()),
									}),
								),
							}),
						),
						/**
						 * Whether a pseudo-TTY should be allocated.
						 */
						TTY: v.exactOptional(v.boolean()),
						/**
						 * Open `stdin`
						 */
						OpenStdin: v.exactOptional(v.boolean()),
						/**
						 * Mount the container's root filesystem as read only.
						 */
						ReadOnly: v.exactOptional(v.boolean()),
						/**
						 * Specification for mounts to be added to containers created as part
						 * of the service.
						 */
						Mounts: v.exactOptional(
							v.array(
								v.strictObject({
									/**
									 * Container path.
									 */
									Target: v.exactOptional(v.string()),
									/**
									 * Mount source (e.g. a volume name, a host path).
									 */
									Source: v.exactOptional(v.string()),
									/**
									 * The mount type. Available types:
									 *
									 * - `bind` Mounts a file or directory from the host into the container. Must
									 * exist prior to creating the container.
									 * - `volume` Creates a volume with the given name and options (or uses a
									 * pre-existing volume with the same name and options). These are **not**
									 * removed when the container is removed.
									 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
									 * specified for tmpfs.
									 * - `npipe` Mounts a named pipe from the host into the container. Must exist
									 * prior to creating the container.
									 * - `cluster` a Swarm cluster volume
									 */
									Type: v.exactOptional(
										v.picklist(["bind", "volume", "tmpfs", "npipe", "cluster"]),
									),
									/**
									 * Whether the mount should be read-only.
									 */
									ReadOnly: v.exactOptional(v.boolean()),
									/**
									 * The consistency requirement for the mount: `default`, `consistent`,
									 * `cached`, or `delegated`.
									 */
									Consistency: v.exactOptional(v.string()),
									/**
									 * Optional configuration for the `bind` type.
									 */
									BindOptions: v.exactOptional(
										v.strictObject({
											/**
											 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
											 */
											Propagation: v.exactOptional(
												v.picklist([
													"private",
													"rprivate",
													"shared",
													"rshared",
													"slave",
													"rslave",
												]),
											),
											/**
											 * Disable recursive bind mount.
											 */
											NonRecursive: v.exactOptional(v.boolean()),
											/**
											 * Create mount point on host if missing
											 */
											CreateMountpoint: v.exactOptional(v.boolean()),
										}),
									),
									/**
									 * Optional configuration for the `volume` type.
									 */
									VolumeOptions: v.exactOptional(
										v.strictObject({
											/**
											 * Populate volume with data from the target.
											 */
											NoCopy: v.exactOptional(v.boolean()),
											/**
											 * User-defined key/value metadata.
											 */
											Labels: v.exactOptional(
												v.record(v.string(), v.unknown()),
											),
											/**
											 * Map of driver specific options
											 */
											DriverConfig: v.exactOptional(
												v.strictObject({
													/**
													 * Name of the driver to use to create the volume.
													 */
													Name: v.exactOptional(v.string()),
													/**
													 * key/value map of driver specific options.
													 */
													Options: v.exactOptional(
														v.record(v.string(), v.unknown()),
													),
												}),
											),
										}),
									),
									/**
									 * Optional configuration for the `tmpfs` type.
									 */
									TmpfsOptions: v.exactOptional(
										v.strictObject({
											/**
											 * The size for the tmpfs mount in bytes.
											 */
											SizeBytes: v.exactOptional(
												v.pipe(v.number(), v.integer()),
											),
											/**
											 * The permission mode for the tmpfs mount in an integer.
											 */
											Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
										}),
									),
								}),
							),
						),
						/**
						 * Signal to stop the container.
						 */
						StopSignal: v.exactOptional(v.string()),
						/**
						 * Amount of time to wait for the container to terminate before
						 * forcefully killing it.
						 */
						StopGracePeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * A test to perform to check that the container is healthy.
						 */
						HealthCheck: v.exactOptional(
							v.strictObject({
								/**
								 * The test to perform. Possible values are:
								 *
								 * - `[]` inherit healthcheck from image or parent image
								 * - `["NONE"]` disable healthcheck
								 * - `["CMD", args...]` exec arguments directly
								 * - `["CMD-SHELL", command]` run command with system's default shell
								 */
								Test: v.exactOptional(v.array(v.string())),
								/**
								 * The time to wait between checks in nanoseconds. It should be 0 or at
								 * least 1000000 (1 ms). 0 means inherit.
								 */
								Interval: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * The time to wait before considering the check to have hung. It should
								 * be 0 or at least 1000000 (1 ms). 0 means inherit.
								 */
								Timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * The number of consecutive failures needed to consider a container as
								 * unhealthy. 0 means inherit.
								 */
								Retries: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * Start period for the container to initialize before starting
								 * health-retries countdown in nanoseconds. It should be 0 or at least
								 * 1000000 (1 ms). 0 means inherit.
								 */
								StartPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
						/**
						 * A list of hostname/IP mappings to add to the container's `hosts`
						 * file. The format of extra hosts is specified in the
						 * [hosts(5)](http://man7.org/linux/man-pages/man5/hosts.5.html)
						 * man page:
						 *
						 *
						 * IP_address canonical_hostname [aliases...]
						 */
						Hosts: v.exactOptional(v.array(v.string())),
						/**
						 * Specification for DNS related configurations in resolver configuration
						 * file (`resolv.conf`).
						 */
						DNSConfig: v.exactOptional(
							v.strictObject({
								/**
								 * The IP addresses of the name servers.
								 */
								Nameservers: v.exactOptional(v.array(v.string())),
								/**
								 * A search list for host-name lookup.
								 */
								Search: v.exactOptional(v.array(v.string())),
								/**
								 * A list of internal resolver variables to be modified (e.g.,
								 * `debug`, `ndots:3`, etc.).
								 */
								Options: v.exactOptional(v.array(v.string())),
							}),
						),
						/**
						 * Secrets contains references to zero or more secrets that will be
						 * exposed to the service.
						 */
						Secrets: v.exactOptional(
							v.array(
								v.strictObject({
									/**
									 * File represents a specific target that is backed by a file.
									 */
									File: v.exactOptional(
										v.strictObject({
											/**
											 * Name represents the final filename in the filesystem.
											 */
											Name: v.exactOptional(v.string()),
											/**
											 * UID represents the file UID.
											 */
											UID: v.exactOptional(v.string()),
											/**
											 * GID represents the file GID.
											 */
											GID: v.exactOptional(v.string()),
											/**
											 * Mode represents the FileMode of the file.
											 */
											Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
										}),
									),
									/**
									 * SecretID represents the ID of the specific secret that we're
									 * referencing.
									 */
									SecretID: v.exactOptional(v.string()),
									/**
									 * SecretName is the name of the secret that this references,
									 * but this is just provided for lookup/display purposes. The
									 * secret in the reference will be identified by its ID.
									 */
									SecretName: v.exactOptional(v.string()),
								}),
							),
						),
						/**
						 * Configs contains references to zero or more configs that will be
						 * exposed to the service.
						 */
						Configs: v.exactOptional(
							v.array(
								v.strictObject({
									/**
									 * File represents a specific target that is backed by a file.
									 *
									 * <p><br /><p>
									 *
									 * > **Note**: `Configs.File` and `Configs.Runtime` are mutually exclusive
									 */
									File: v.exactOptional(
										v.strictObject({
											/**
											 * Name represents the final filename in the filesystem.
											 */
											Name: v.exactOptional(v.string()),
											/**
											 * UID represents the file UID.
											 */
											UID: v.exactOptional(v.string()),
											/**
											 * GID represents the file GID.
											 */
											GID: v.exactOptional(v.string()),
											/**
											 * Mode represents the FileMode of the file.
											 */
											Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
										}),
									),
									/**
									 * Runtime represents a target that is not mounted into the
									 * container but is used by the task
									 *
									 * <p><br /><p>
									 *
									 * > **Note**: `Configs.File` and `Configs.Runtime` are mutually
									 * > exclusive
									 */
									Runtime: v.exactOptional(v.record(v.string(), v.unknown())),
									/**
									 * ConfigID represents the ID of the specific config that we're
									 * referencing.
									 */
									ConfigID: v.exactOptional(v.string()),
									/**
									 * ConfigName is the name of the config that this references,
									 * but this is just provided for lookup/display purposes. The
									 * config in the reference will be identified by its ID.
									 */
									ConfigName: v.exactOptional(v.string()),
								}),
							),
						),
						/**
						 * Isolation technology of the containers running the service.
						 * (Windows only)
						 */
						Isolation: v.exactOptional(
							v.picklist(["default", "process", "hyperv", ""]),
						),
						/**
						 * Run an init inside the container that forwards signals and reaps
						 * processes. This field is omitted if empty, and the default (as
						 * configured on the daemon) is used.
						 */
						Init: v.exactOptional(v.nullable(v.boolean())),
						/**
						 * Set kernel namedspaced parameters (sysctls) in the container.
						 * The Sysctls option on services accepts the same sysctls as the
						 * are supported on containers. Note that while the same sysctls are
						 * supported, no guarantees or checks are made about their
						 * suitability for a clustered environment, and it's up to the user
						 * to determine whether a given sysctl will work properly in a
						 * Service.
						 */
						Sysctls: v.exactOptional(v.record(v.string(), v.unknown())),
						/**
						 * A list of kernel capabilities to add to the default set
						 * for the container.
						 */
						CapabilityAdd: v.exactOptional(v.array(v.string())),
						/**
						 * A list of kernel capabilities to drop from the default set
						 * for the container.
						 */
						CapabilityDrop: v.exactOptional(v.array(v.string())),
						/**
						 * A list of resource limits to set in the container. For example: `{"Name":
						 * "nofile", "Soft": 1024, "Hard": 2048}`"
						 */
						Ulimits: v.exactOptional(
							v.array(
								v.strictObject({
									/**
									 * Name of ulimit
									 */
									Name: v.exactOptional(v.string()),
									/**
									 * Soft limit
									 */
									Soft: v.exactOptional(v.pipe(v.number(), v.integer())),
									/**
									 * Hard limit
									 */
									Hard: v.exactOptional(v.pipe(v.number(), v.integer())),
								}),
							),
						),
					}),
				),
				/**
				 * Read-only spec type for non-swarm containers attached to swarm overlay
				 * networks.
				 *
				 * <p><br /></p>
				 *
				 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
				 * > mutually exclusive. PluginSpec is only used when the Runtime field
				 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
				 * > field is set to `attachment`.
				 */
				NetworkAttachmentSpec: v.exactOptional(
					v.strictObject({
						/**
						 * ID of the container represented by this task
						 */
						ContainerID: v.exactOptional(v.string()),
					}),
				),
				/**
				 * Resource requirements which apply to each individual container created
				 * as part of the service.
				 */
				Resources: v.exactOptional(
					v.strictObject({
						/**
						 * An object describing a limit on resources which can be requested by a task.
						 */
						Limits: v.exactOptional(
							v.strictObject({
								NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
								MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
								 */
								Pids: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
						/**
						 * An object describing the resources which can be advertised by a node and
						 * requested by a task.
						 */
						Reservations: v.exactOptional(
							v.strictObject({
								NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
								MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
								 * String resources (e.g, `GPU=UUID1`).
								 */
								GenericResources: v.exactOptional(
									v.array(
										v.strictObject({
											NamedResourceSpec: v.exactOptional(
												v.strictObject({
													Kind: v.exactOptional(v.string()),
													Value: v.exactOptional(v.string()),
												}),
											),
											DiscreteResourceSpec: v.exactOptional(
												v.strictObject({
													Kind: v.exactOptional(v.string()),
													Value: v.exactOptional(
														v.pipe(v.number(), v.integer()),
													),
												}),
											),
										}),
									),
								),
							}),
						),
					}),
				),
				/**
				 * Specification for the restart policy which applies to containers
				 * created as part of this service.
				 */
				RestartPolicy: v.exactOptional(
					v.strictObject({
						/**
						 * Condition for restart.
						 */
						Condition: v.exactOptional(
							v.picklist(["none", "on-failure", "any"]),
						),
						/**
						 * Delay between restart attempts.
						 */
						Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * Maximum attempts to restart a given container before giving up
						 * (default value is 0, which is ignored).
						 */
						MaxAttempts: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * Windows is the time window used to evaluate the restart policy
						 * (default value is 0, which is unbounded).
						 */
						Window: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
				Placement: v.exactOptional(
					v.strictObject({
						/**
						 * An array of constraint expressions to limit the set of nodes where
						 * a task can be scheduled. Constraint expressions can either use a
						 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
						 * nodes that satisfy every expression (AND match). Constraints can
						 * match node or Docker Engine labels as follows:
						 *
						 * node attribute       | matches                        | example
						 * ---------------------|--------------------------------|-----------------------------------------------
						 * `node.id`            | Node ID                        |
						 * `node.id==2ivku8v2gvtg4`
						 * `node.hostname`      | Node hostname                  |
						 * `node.hostname!=node-2`
						 * `node.role`          | Node role (`manager`/`worker`) |
						 * `node.role==manager`
						 * `node.platform.os`   | Node operating system          |
						 * `node.platform.os==windows`
						 * `node.platform.arch` | Node architecture              |
						 * `node.platform.arch==x86_64`
						 * `node.labels`        | User-defined node labels       |
						 * `node.labels.security==high`
						 * `engine.labels`      | Docker Engine's labels         |
						 * `engine.labels.operatingsystem==ubuntu-14.04`
						 *
						 * `engine.labels` apply to Docker Engine labels like operating system,
						 * drivers, etc. Swarm administrators add `node.labels` for operational
						 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
						 */
						Constraints: v.exactOptional(v.array(v.string())),
						/**
						 * Preferences provide a way to make the scheduler aware of factors
						 * such as topology. They are provided in order from highest to
						 * lowest precedence.
						 */
						Preferences: v.exactOptional(
							v.array(
								v.strictObject({
									Spread: v.exactOptional(
										v.strictObject({
											/**
											 * label descriptor, such as `engine.labels.az`.
											 */
											SpreadDescriptor: v.exactOptional(v.string()),
										}),
									),
								}),
							),
						),
						/**
						 * Maximum number of replicas for per node (default value is 0, which
						 * is unlimited)
						 */
						MaxReplicas: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * Platforms stores all the platforms that the service's image can
						 * run on. This field is used in the platform filter for scheduling.
						 * If empty, then the platform filter is off, meaning there are no
						 * scheduling restrictions.
						 */
						Platforms: v.exactOptional(
							v.array(
								v.strictObject({
									/**
									 * Architecture represents the hardware architecture (for example,
									 * `x86_64`).
									 */
									Architecture: v.exactOptional(v.string()),
									/**
									 * OS represents the Operating System (for example, `linux` or `windows`).
									 */
									OS: v.exactOptional(v.string()),
								}),
							),
						),
					}),
				),
				/**
				 * A counter that triggers an update even if no relevant parameters have
				 * been changed.
				 */
				ForceUpdate: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Runtime is the type of runtime specified for the task executor.
				 */
				Runtime: v.exactOptional(v.string()),
				/**
				 * Specifies which networks the service should attach to.
				 */
				Networks: v.exactOptional(
					v.array(
						v.strictObject({
							/**
							 * The target network for attachment. Must be a network name or ID.
							 */
							Target: v.exactOptional(v.string()),
							/**
							 * Discoverable alternate names for the service on this network.
							 */
							Aliases: v.exactOptional(v.array(v.string())),
							/**
							 * Driver attachment options for the network target.
							 */
							DriverOpts: v.exactOptional(v.record(v.string(), v.unknown())),
						}),
					),
				),
				/**
				 * Specifies the log driver to use for tasks created from this spec. If
				 * not present, the default one for the swarm will be used, finally
				 * falling back to the engine default if not specified.
				 */
				LogDriver: v.exactOptional(
					v.strictObject({
						Name: v.exactOptional(v.string()),
						Options: v.exactOptional(v.record(v.string(), v.unknown())),
					}),
				),
			}),
		),
		/**
		 * Scheduling mode for the service.
		 */
		Mode: v.exactOptional(
			v.strictObject({
				Replicated: v.exactOptional(
					v.strictObject({
						Replicas: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
				Global: v.exactOptional(v.record(v.string(), v.unknown())),
				/**
				 * The mode used for services with a finite number of tasks that run
				 * to a completed state.
				 */
				ReplicatedJob: v.exactOptional(
					v.strictObject({
						/**
						 * The maximum number of replicas to run simultaneously.
						 */
						MaxConcurrent: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * The total number of replicas desired to reach the Completed
						 * state. If unset, will default to the value of `MaxConcurrent`
						 */
						TotalCompletions: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
				/**
				 * The mode used for services which run a task to the completed state
				 * on each valid node.
				 */
				GlobalJob: v.exactOptional(v.record(v.string(), v.unknown())),
			}),
		),
		/**
		 * Specification for the update strategy of the service.
		 */
		UpdateConfig: v.exactOptional(
			v.strictObject({
				/**
				 * Maximum number of tasks to be updated in one iteration (0 means
				 * unlimited parallelism).
				 */
				Parallelism: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Amount of time between updates, in nanoseconds.
				 */
				Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Action to take if an updated task fails to run, or stops running
				 * during the update.
				 */
				FailureAction: v.exactOptional(
					v.picklist(["continue", "pause", "rollback"]),
				),
				/**
				 * Amount of time to monitor each updated task for failures, in
				 * nanoseconds.
				 */
				Monitor: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * The fraction of tasks that may fail during an update before the
				 * failure action is invoked, specified as a floating point number
				 * between 0 and 1.
				 */
				MaxFailureRatio: v.exactOptional(v.number()),
				/**
				 * The order of operations when rolling out an updated task. Either
				 * the old task is shut down before the new task is started, or the
				 * new task is started before the old task is shut down.
				 */
				Order: v.exactOptional(v.picklist(["stop-first", "start-first"])),
			}),
		),
		/**
		 * Specification for the rollback strategy of the service.
		 */
		RollbackConfig: v.exactOptional(
			v.strictObject({
				/**
				 * Maximum number of tasks to be rolled back in one iteration (0 means
				 * unlimited parallelism).
				 */
				Parallelism: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Amount of time between rollback iterations, in nanoseconds.
				 */
				Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Action to take if an rolled back task fails to run, or stops
				 * running during the rollback.
				 */
				FailureAction: v.exactOptional(v.picklist(["continue", "pause"])),
				/**
				 * Amount of time to monitor each rolled back task for failures, in
				 * nanoseconds.
				 */
				Monitor: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * The fraction of tasks that may fail during a rollback before the
				 * failure action is invoked, specified as a floating point number
				 * between 0 and 1.
				 */
				MaxFailureRatio: v.exactOptional(v.number()),
				/**
				 * The order of operations when rolling back a task. Either the old
				 * task is shut down before the new task is started, or the new task
				 * is started before the old task is shut down.
				 */
				Order: v.exactOptional(v.picklist(["stop-first", "start-first"])),
			}),
		),
		/**
		 * Specifies which networks the service should attach to.
		 */
		Networks: v.exactOptional(
			v.array(
				v.strictObject({
					/**
					 * The target network for attachment. Must be a network name or ID.
					 */
					Target: v.exactOptional(v.string()),
					/**
					 * Discoverable alternate names for the service on this network.
					 */
					Aliases: v.exactOptional(v.array(v.string())),
					/**
					 * Driver attachment options for the network target.
					 */
					DriverOpts: v.exactOptional(v.record(v.string(), v.unknown())),
				}),
			),
		),
		/**
		 * Properties that can be configured to access and load balance a service.
		 */
		EndpointSpec: v.exactOptional(
			v.strictObject({
				/**
				 * The mode of resolution to use for internal load balancing between tasks.
				 */
				Mode: v.exactOptional(v.picklist(["vip", "dnsrr"])),
				/**
				 * List of exposed ports that this service is accessible on from the
				 * outside. Ports can only be provided if `vip` resolution mode is used.
				 */
				Ports: v.exactOptional(
					v.array(
						v.strictObject({
							Name: v.exactOptional(v.string()),
							Protocol: v.exactOptional(v.picklist(["tcp", "udp", "sctp"])),
							/**
							 * The port inside the container.
							 */
							TargetPort: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The port on the swarm hosts.
							 */
							PublishedPort: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The mode in which port is published.
							 *
							 * <p><br /></p>
							 *
							 * - "ingress" makes the target port accessible on every node,
							 *
							 * regardless of whether there is a task for the service running on
							 *
							 * that node or not.
							 * - "host" bypasses the routing mesh and publish the port directly on
							 *
							 * the swarm node where that service is running.
							 */
							PublishMode: v.exactOptional(v.picklist(["ingress", "host"])),
						}),
					),
				),
			}),
		),
	}),
	v.record(v.string(), v.unknown()),
]);
export const serviceInspectCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const serviceInspectCommandQuerySchema = v.strictObject({
	insertDefaults: v.exactOptional(v.boolean()),
});
export const serviceDeleteCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const serviceUpdateCommandBodySchema = v.intersect([
	v.strictObject({
		/**
		 * Name of the service.
		 */
		Name: v.exactOptional(v.string()),
		/**
		 * User-defined key/value metadata.
		 */
		Labels: v.exactOptional(v.record(v.string(), v.unknown())),
		/**
		 * User modifiable task configuration.
		 */
		TaskTemplate: v.exactOptional(
			v.strictObject({
				/**
				 * Plugin spec for the service.  *(Experimental release only.)*
				 *
				 * <p><br /></p>
				 *
				 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
				 * > mutually exclusive. PluginSpec is only used when the Runtime field
				 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
				 * > field is set to `attachment`.
				 */
				PluginSpec: v.exactOptional(
					v.strictObject({
						/**
						 * The name or 'alias' to use for the plugin.
						 */
						Name: v.exactOptional(v.string()),
						/**
						 * The plugin image reference to use.
						 */
						Remote: v.exactOptional(v.string()),
						/**
						 * Disable the plugin once scheduled.
						 */
						Disabled: v.exactOptional(v.boolean()),
						PluginPrivilege: v.exactOptional(
							v.array(
								v.strictObject({
									Name: v.exactOptional(v.string()),
									Description: v.exactOptional(v.string()),
									Value: v.exactOptional(v.array(v.string())),
								}),
							),
						),
					}),
				),
				/**
				 * Container spec for the service.
				 *
				 * <p><br /></p>
				 *
				 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
				 * > mutually exclusive. PluginSpec is only used when the Runtime field
				 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
				 * > field is set to `attachment`.
				 */
				ContainerSpec: v.exactOptional(
					v.strictObject({
						/**
						 * The image name to use for the container
						 */
						Image: v.exactOptional(v.string()),
						/**
						 * User-defined key/value data.
						 */
						Labels: v.exactOptional(v.record(v.string(), v.unknown())),
						/**
						 * The command to be run in the image.
						 */
						Command: v.exactOptional(v.array(v.string())),
						/**
						 * Arguments to the command.
						 */
						Args: v.exactOptional(v.array(v.string())),
						/**
						 * The hostname to use for the container, as a valid
						 * [RFC 1123](https://tools.ietf.org/html/rfc1123) hostname.
						 */
						Hostname: v.exactOptional(v.string()),
						/**
						 * A list of environment variables in the form `VAR=value`.
						 */
						Env: v.exactOptional(v.array(v.string())),
						/**
						 * The working directory for commands to run in.
						 */
						Dir: v.exactOptional(v.string()),
						/**
						 * The user inside the container.
						 */
						User: v.exactOptional(v.string()),
						/**
						 * A list of additional groups that the container process will run as.
						 */
						Groups: v.exactOptional(v.array(v.string())),
						/**
						 * Security options for the container
						 */
						Privileges: v.exactOptional(
							v.strictObject({
								/**
								 * CredentialSpec for managed service account (Windows only)
								 */
								CredentialSpec: v.exactOptional(
									v.strictObject({
										/**
										 * Load credential spec from a Swarm Config with the given ID.
										 * The specified config must also be present in the Configs
										 * field with the Runtime property set.
										 *
										 * <p><br /></p>
										 *
										 *
										 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
										 * > and `CredentialSpec.Config` are mutually exclusive.
										 */
										Config: v.exactOptional(v.string()),
										/**
										 * Load credential spec from this file. The file is read by
										 * the daemon, and must be present in the `CredentialSpecs`
										 * subdirectory in the docker data directory, which defaults
										 * to `C:\ProgramData\Docker\` on Windows.
										 *
										 * For example, specifying `spec.json` loads
										 * `C:\ProgramData\Docker\CredentialSpecs\spec.json`.
										 *
										 * <p><br /></p>
										 *
										 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
										 * > and `CredentialSpec.Config` are mutually exclusive.
										 */
										File: v.exactOptional(v.string()),
										/**
										 * Load credential spec from this value in the Windows
										 * registry. The specified registry value must be located in:
										 *
										 * `HKLM\SOFTWARE\Microsoft\Windows
										 * NT\CurrentVersion\Virtualization\Containers\CredentialSpecs`
										 *
										 * <p><br /></p>
										 *
										 *
										 * > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`,
										 * > and `CredentialSpec.Config` are mutually exclusive.
										 */
										Registry: v.exactOptional(v.string()),
									}),
								),
								/**
								 * SELinux labels of the container
								 */
								SELinuxContext: v.exactOptional(
									v.strictObject({
										/**
										 * Disable SELinux
										 */
										Disable: v.exactOptional(v.boolean()),
										/**
										 * SELinux user label
										 */
										User: v.exactOptional(v.string()),
										/**
										 * SELinux role label
										 */
										Role: v.exactOptional(v.string()),
										/**
										 * SELinux type label
										 */
										Type: v.exactOptional(v.string()),
										/**
										 * SELinux level label
										 */
										Level: v.exactOptional(v.string()),
									}),
								),
							}),
						),
						/**
						 * Whether a pseudo-TTY should be allocated.
						 */
						TTY: v.exactOptional(v.boolean()),
						/**
						 * Open `stdin`
						 */
						OpenStdin: v.exactOptional(v.boolean()),
						/**
						 * Mount the container's root filesystem as read only.
						 */
						ReadOnly: v.exactOptional(v.boolean()),
						/**
						 * Specification for mounts to be added to containers created as part
						 * of the service.
						 */
						Mounts: v.exactOptional(
							v.array(
								v.strictObject({
									/**
									 * Container path.
									 */
									Target: v.exactOptional(v.string()),
									/**
									 * Mount source (e.g. a volume name, a host path).
									 */
									Source: v.exactOptional(v.string()),
									/**
									 * The mount type. Available types:
									 *
									 * - `bind` Mounts a file or directory from the host into the container. Must
									 * exist prior to creating the container.
									 * - `volume` Creates a volume with the given name and options (or uses a
									 * pre-existing volume with the same name and options). These are **not**
									 * removed when the container is removed.
									 * - `tmpfs` Create a tmpfs with the given options. The mount source cannot be
									 * specified for tmpfs.
									 * - `npipe` Mounts a named pipe from the host into the container. Must exist
									 * prior to creating the container.
									 * - `cluster` a Swarm cluster volume
									 */
									Type: v.exactOptional(
										v.picklist(["bind", "volume", "tmpfs", "npipe", "cluster"]),
									),
									/**
									 * Whether the mount should be read-only.
									 */
									ReadOnly: v.exactOptional(v.boolean()),
									/**
									 * The consistency requirement for the mount: `default`, `consistent`,
									 * `cached`, or `delegated`.
									 */
									Consistency: v.exactOptional(v.string()),
									/**
									 * Optional configuration for the `bind` type.
									 */
									BindOptions: v.exactOptional(
										v.strictObject({
											/**
											 * A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`.
											 */
											Propagation: v.exactOptional(
												v.picklist([
													"private",
													"rprivate",
													"shared",
													"rshared",
													"slave",
													"rslave",
												]),
											),
											/**
											 * Disable recursive bind mount.
											 */
											NonRecursive: v.exactOptional(v.boolean()),
											/**
											 * Create mount point on host if missing
											 */
											CreateMountpoint: v.exactOptional(v.boolean()),
										}),
									),
									/**
									 * Optional configuration for the `volume` type.
									 */
									VolumeOptions: v.exactOptional(
										v.strictObject({
											/**
											 * Populate volume with data from the target.
											 */
											NoCopy: v.exactOptional(v.boolean()),
											/**
											 * User-defined key/value metadata.
											 */
											Labels: v.exactOptional(
												v.record(v.string(), v.unknown()),
											),
											/**
											 * Map of driver specific options
											 */
											DriverConfig: v.exactOptional(
												v.strictObject({
													/**
													 * Name of the driver to use to create the volume.
													 */
													Name: v.exactOptional(v.string()),
													/**
													 * key/value map of driver specific options.
													 */
													Options: v.exactOptional(
														v.record(v.string(), v.unknown()),
													),
												}),
											),
										}),
									),
									/**
									 * Optional configuration for the `tmpfs` type.
									 */
									TmpfsOptions: v.exactOptional(
										v.strictObject({
											/**
											 * The size for the tmpfs mount in bytes.
											 */
											SizeBytes: v.exactOptional(
												v.pipe(v.number(), v.integer()),
											),
											/**
											 * The permission mode for the tmpfs mount in an integer.
											 */
											Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
										}),
									),
								}),
							),
						),
						/**
						 * Signal to stop the container.
						 */
						StopSignal: v.exactOptional(v.string()),
						/**
						 * Amount of time to wait for the container to terminate before
						 * forcefully killing it.
						 */
						StopGracePeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * A test to perform to check that the container is healthy.
						 */
						HealthCheck: v.exactOptional(
							v.strictObject({
								/**
								 * The test to perform. Possible values are:
								 *
								 * - `[]` inherit healthcheck from image or parent image
								 * - `["NONE"]` disable healthcheck
								 * - `["CMD", args...]` exec arguments directly
								 * - `["CMD-SHELL", command]` run command with system's default shell
								 */
								Test: v.exactOptional(v.array(v.string())),
								/**
								 * The time to wait between checks in nanoseconds. It should be 0 or at
								 * least 1000000 (1 ms). 0 means inherit.
								 */
								Interval: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * The time to wait before considering the check to have hung. It should
								 * be 0 or at least 1000000 (1 ms). 0 means inherit.
								 */
								Timeout: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * The number of consecutive failures needed to consider a container as
								 * unhealthy. 0 means inherit.
								 */
								Retries: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * Start period for the container to initialize before starting
								 * health-retries countdown in nanoseconds. It should be 0 or at least
								 * 1000000 (1 ms). 0 means inherit.
								 */
								StartPeriod: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
						/**
						 * A list of hostname/IP mappings to add to the container's `hosts`
						 * file. The format of extra hosts is specified in the
						 * [hosts(5)](http://man7.org/linux/man-pages/man5/hosts.5.html)
						 * man page:
						 *
						 *
						 * IP_address canonical_hostname [aliases...]
						 */
						Hosts: v.exactOptional(v.array(v.string())),
						/**
						 * Specification for DNS related configurations in resolver configuration
						 * file (`resolv.conf`).
						 */
						DNSConfig: v.exactOptional(
							v.strictObject({
								/**
								 * The IP addresses of the name servers.
								 */
								Nameservers: v.exactOptional(v.array(v.string())),
								/**
								 * A search list for host-name lookup.
								 */
								Search: v.exactOptional(v.array(v.string())),
								/**
								 * A list of internal resolver variables to be modified (e.g.,
								 * `debug`, `ndots:3`, etc.).
								 */
								Options: v.exactOptional(v.array(v.string())),
							}),
						),
						/**
						 * Secrets contains references to zero or more secrets that will be
						 * exposed to the service.
						 */
						Secrets: v.exactOptional(
							v.array(
								v.strictObject({
									/**
									 * File represents a specific target that is backed by a file.
									 */
									File: v.exactOptional(
										v.strictObject({
											/**
											 * Name represents the final filename in the filesystem.
											 */
											Name: v.exactOptional(v.string()),
											/**
											 * UID represents the file UID.
											 */
											UID: v.exactOptional(v.string()),
											/**
											 * GID represents the file GID.
											 */
											GID: v.exactOptional(v.string()),
											/**
											 * Mode represents the FileMode of the file.
											 */
											Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
										}),
									),
									/**
									 * SecretID represents the ID of the specific secret that we're
									 * referencing.
									 */
									SecretID: v.exactOptional(v.string()),
									/**
									 * SecretName is the name of the secret that this references,
									 * but this is just provided for lookup/display purposes. The
									 * secret in the reference will be identified by its ID.
									 */
									SecretName: v.exactOptional(v.string()),
								}),
							),
						),
						/**
						 * Configs contains references to zero or more configs that will be
						 * exposed to the service.
						 */
						Configs: v.exactOptional(
							v.array(
								v.strictObject({
									/**
									 * File represents a specific target that is backed by a file.
									 *
									 * <p><br /><p>
									 *
									 * > **Note**: `Configs.File` and `Configs.Runtime` are mutually exclusive
									 */
									File: v.exactOptional(
										v.strictObject({
											/**
											 * Name represents the final filename in the filesystem.
											 */
											Name: v.exactOptional(v.string()),
											/**
											 * UID represents the file UID.
											 */
											UID: v.exactOptional(v.string()),
											/**
											 * GID represents the file GID.
											 */
											GID: v.exactOptional(v.string()),
											/**
											 * Mode represents the FileMode of the file.
											 */
											Mode: v.exactOptional(v.pipe(v.number(), v.integer())),
										}),
									),
									/**
									 * Runtime represents a target that is not mounted into the
									 * container but is used by the task
									 *
									 * <p><br /><p>
									 *
									 * > **Note**: `Configs.File` and `Configs.Runtime` are mutually
									 * > exclusive
									 */
									Runtime: v.exactOptional(v.record(v.string(), v.unknown())),
									/**
									 * ConfigID represents the ID of the specific config that we're
									 * referencing.
									 */
									ConfigID: v.exactOptional(v.string()),
									/**
									 * ConfigName is the name of the config that this references,
									 * but this is just provided for lookup/display purposes. The
									 * config in the reference will be identified by its ID.
									 */
									ConfigName: v.exactOptional(v.string()),
								}),
							),
						),
						/**
						 * Isolation technology of the containers running the service.
						 * (Windows only)
						 */
						Isolation: v.exactOptional(
							v.picklist(["default", "process", "hyperv", ""]),
						),
						/**
						 * Run an init inside the container that forwards signals and reaps
						 * processes. This field is omitted if empty, and the default (as
						 * configured on the daemon) is used.
						 */
						Init: v.exactOptional(v.nullable(v.boolean())),
						/**
						 * Set kernel namedspaced parameters (sysctls) in the container.
						 * The Sysctls option on services accepts the same sysctls as the
						 * are supported on containers. Note that while the same sysctls are
						 * supported, no guarantees or checks are made about their
						 * suitability for a clustered environment, and it's up to the user
						 * to determine whether a given sysctl will work properly in a
						 * Service.
						 */
						Sysctls: v.exactOptional(v.record(v.string(), v.unknown())),
						/**
						 * A list of kernel capabilities to add to the default set
						 * for the container.
						 */
						CapabilityAdd: v.exactOptional(v.array(v.string())),
						/**
						 * A list of kernel capabilities to drop from the default set
						 * for the container.
						 */
						CapabilityDrop: v.exactOptional(v.array(v.string())),
						/**
						 * A list of resource limits to set in the container. For example: `{"Name":
						 * "nofile", "Soft": 1024, "Hard": 2048}`"
						 */
						Ulimits: v.exactOptional(
							v.array(
								v.strictObject({
									/**
									 * Name of ulimit
									 */
									Name: v.exactOptional(v.string()),
									/**
									 * Soft limit
									 */
									Soft: v.exactOptional(v.pipe(v.number(), v.integer())),
									/**
									 * Hard limit
									 */
									Hard: v.exactOptional(v.pipe(v.number(), v.integer())),
								}),
							),
						),
					}),
				),
				/**
				 * Read-only spec type for non-swarm containers attached to swarm overlay
				 * networks.
				 *
				 * <p><br /></p>
				 *
				 * > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are
				 * > mutually exclusive. PluginSpec is only used when the Runtime field
				 * > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime
				 * > field is set to `attachment`.
				 */
				NetworkAttachmentSpec: v.exactOptional(
					v.strictObject({
						/**
						 * ID of the container represented by this task
						 */
						ContainerID: v.exactOptional(v.string()),
					}),
				),
				/**
				 * Resource requirements which apply to each individual container created
				 * as part of the service.
				 */
				Resources: v.exactOptional(
					v.strictObject({
						/**
						 * An object describing a limit on resources which can be requested by a task.
						 */
						Limits: v.exactOptional(
							v.strictObject({
								NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
								MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * Limits the maximum number of PIDs in the container. Set `0` for unlimited.
								 */
								Pids: v.exactOptional(v.pipe(v.number(), v.integer())),
							}),
						),
						/**
						 * An object describing the resources which can be advertised by a node and
						 * requested by a task.
						 */
						Reservations: v.exactOptional(
							v.strictObject({
								NanoCPUs: v.exactOptional(v.pipe(v.number(), v.integer())),
								MemoryBytes: v.exactOptional(v.pipe(v.number(), v.integer())),
								/**
								 * User-defined resources can be either Integer resources (e.g, `SSD=3`) or
								 * String resources (e.g, `GPU=UUID1`).
								 */
								GenericResources: v.exactOptional(
									v.array(
										v.strictObject({
											NamedResourceSpec: v.exactOptional(
												v.strictObject({
													Kind: v.exactOptional(v.string()),
													Value: v.exactOptional(v.string()),
												}),
											),
											DiscreteResourceSpec: v.exactOptional(
												v.strictObject({
													Kind: v.exactOptional(v.string()),
													Value: v.exactOptional(
														v.pipe(v.number(), v.integer()),
													),
												}),
											),
										}),
									),
								),
							}),
						),
					}),
				),
				/**
				 * Specification for the restart policy which applies to containers
				 * created as part of this service.
				 */
				RestartPolicy: v.exactOptional(
					v.strictObject({
						/**
						 * Condition for restart.
						 */
						Condition: v.exactOptional(
							v.picklist(["none", "on-failure", "any"]),
						),
						/**
						 * Delay between restart attempts.
						 */
						Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * Maximum attempts to restart a given container before giving up
						 * (default value is 0, which is ignored).
						 */
						MaxAttempts: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * Windows is the time window used to evaluate the restart policy
						 * (default value is 0, which is unbounded).
						 */
						Window: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
				Placement: v.exactOptional(
					v.strictObject({
						/**
						 * An array of constraint expressions to limit the set of nodes where
						 * a task can be scheduled. Constraint expressions can either use a
						 * _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find
						 * nodes that satisfy every expression (AND match). Constraints can
						 * match node or Docker Engine labels as follows:
						 *
						 * node attribute       | matches                        | example
						 * ---------------------|--------------------------------|-----------------------------------------------
						 * `node.id`            | Node ID                        |
						 * `node.id==2ivku8v2gvtg4`
						 * `node.hostname`      | Node hostname                  |
						 * `node.hostname!=node-2`
						 * `node.role`          | Node role (`manager`/`worker`) |
						 * `node.role==manager`
						 * `node.platform.os`   | Node operating system          |
						 * `node.platform.os==windows`
						 * `node.platform.arch` | Node architecture              |
						 * `node.platform.arch==x86_64`
						 * `node.labels`        | User-defined node labels       |
						 * `node.labels.security==high`
						 * `engine.labels`      | Docker Engine's labels         |
						 * `engine.labels.operatingsystem==ubuntu-14.04`
						 *
						 * `engine.labels` apply to Docker Engine labels like operating system,
						 * drivers, etc. Swarm administrators add `node.labels` for operational
						 * purposes by using the [`node update endpoint`](#operation/NodeUpdate).
						 */
						Constraints: v.exactOptional(v.array(v.string())),
						/**
						 * Preferences provide a way to make the scheduler aware of factors
						 * such as topology. They are provided in order from highest to
						 * lowest precedence.
						 */
						Preferences: v.exactOptional(
							v.array(
								v.strictObject({
									Spread: v.exactOptional(
										v.strictObject({
											/**
											 * label descriptor, such as `engine.labels.az`.
											 */
											SpreadDescriptor: v.exactOptional(v.string()),
										}),
									),
								}),
							),
						),
						/**
						 * Maximum number of replicas for per node (default value is 0, which
						 * is unlimited)
						 */
						MaxReplicas: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * Platforms stores all the platforms that the service's image can
						 * run on. This field is used in the platform filter for scheduling.
						 * If empty, then the platform filter is off, meaning there are no
						 * scheduling restrictions.
						 */
						Platforms: v.exactOptional(
							v.array(
								v.strictObject({
									/**
									 * Architecture represents the hardware architecture (for example,
									 * `x86_64`).
									 */
									Architecture: v.exactOptional(v.string()),
									/**
									 * OS represents the Operating System (for example, `linux` or `windows`).
									 */
									OS: v.exactOptional(v.string()),
								}),
							),
						),
					}),
				),
				/**
				 * A counter that triggers an update even if no relevant parameters have
				 * been changed.
				 */
				ForceUpdate: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Runtime is the type of runtime specified for the task executor.
				 */
				Runtime: v.exactOptional(v.string()),
				/**
				 * Specifies which networks the service should attach to.
				 */
				Networks: v.exactOptional(
					v.array(
						v.strictObject({
							/**
							 * The target network for attachment. Must be a network name or ID.
							 */
							Target: v.exactOptional(v.string()),
							/**
							 * Discoverable alternate names for the service on this network.
							 */
							Aliases: v.exactOptional(v.array(v.string())),
							/**
							 * Driver attachment options for the network target.
							 */
							DriverOpts: v.exactOptional(v.record(v.string(), v.unknown())),
						}),
					),
				),
				/**
				 * Specifies the log driver to use for tasks created from this spec. If
				 * not present, the default one for the swarm will be used, finally
				 * falling back to the engine default if not specified.
				 */
				LogDriver: v.exactOptional(
					v.strictObject({
						Name: v.exactOptional(v.string()),
						Options: v.exactOptional(v.record(v.string(), v.unknown())),
					}),
				),
			}),
		),
		/**
		 * Scheduling mode for the service.
		 */
		Mode: v.exactOptional(
			v.strictObject({
				Replicated: v.exactOptional(
					v.strictObject({
						Replicas: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
				Global: v.exactOptional(v.record(v.string(), v.unknown())),
				/**
				 * The mode used for services with a finite number of tasks that run
				 * to a completed state.
				 */
				ReplicatedJob: v.exactOptional(
					v.strictObject({
						/**
						 * The maximum number of replicas to run simultaneously.
						 */
						MaxConcurrent: v.exactOptional(v.pipe(v.number(), v.integer())),
						/**
						 * The total number of replicas desired to reach the Completed
						 * state. If unset, will default to the value of `MaxConcurrent`
						 */
						TotalCompletions: v.exactOptional(v.pipe(v.number(), v.integer())),
					}),
				),
				/**
				 * The mode used for services which run a task to the completed state
				 * on each valid node.
				 */
				GlobalJob: v.exactOptional(v.record(v.string(), v.unknown())),
			}),
		),
		/**
		 * Specification for the update strategy of the service.
		 */
		UpdateConfig: v.exactOptional(
			v.strictObject({
				/**
				 * Maximum number of tasks to be updated in one iteration (0 means
				 * unlimited parallelism).
				 */
				Parallelism: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Amount of time between updates, in nanoseconds.
				 */
				Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Action to take if an updated task fails to run, or stops running
				 * during the update.
				 */
				FailureAction: v.exactOptional(
					v.picklist(["continue", "pause", "rollback"]),
				),
				/**
				 * Amount of time to monitor each updated task for failures, in
				 * nanoseconds.
				 */
				Monitor: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * The fraction of tasks that may fail during an update before the
				 * failure action is invoked, specified as a floating point number
				 * between 0 and 1.
				 */
				MaxFailureRatio: v.exactOptional(v.number()),
				/**
				 * The order of operations when rolling out an updated task. Either
				 * the old task is shut down before the new task is started, or the
				 * new task is started before the old task is shut down.
				 */
				Order: v.exactOptional(v.picklist(["stop-first", "start-first"])),
			}),
		),
		/**
		 * Specification for the rollback strategy of the service.
		 */
		RollbackConfig: v.exactOptional(
			v.strictObject({
				/**
				 * Maximum number of tasks to be rolled back in one iteration (0 means
				 * unlimited parallelism).
				 */
				Parallelism: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Amount of time between rollback iterations, in nanoseconds.
				 */
				Delay: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * Action to take if an rolled back task fails to run, or stops
				 * running during the rollback.
				 */
				FailureAction: v.exactOptional(v.picklist(["continue", "pause"])),
				/**
				 * Amount of time to monitor each rolled back task for failures, in
				 * nanoseconds.
				 */
				Monitor: v.exactOptional(v.pipe(v.number(), v.integer())),
				/**
				 * The fraction of tasks that may fail during a rollback before the
				 * failure action is invoked, specified as a floating point number
				 * between 0 and 1.
				 */
				MaxFailureRatio: v.exactOptional(v.number()),
				/**
				 * The order of operations when rolling back a task. Either the old
				 * task is shut down before the new task is started, or the new task
				 * is started before the old task is shut down.
				 */
				Order: v.exactOptional(v.picklist(["stop-first", "start-first"])),
			}),
		),
		/**
		 * Specifies which networks the service should attach to.
		 */
		Networks: v.exactOptional(
			v.array(
				v.strictObject({
					/**
					 * The target network for attachment. Must be a network name or ID.
					 */
					Target: v.exactOptional(v.string()),
					/**
					 * Discoverable alternate names for the service on this network.
					 */
					Aliases: v.exactOptional(v.array(v.string())),
					/**
					 * Driver attachment options for the network target.
					 */
					DriverOpts: v.exactOptional(v.record(v.string(), v.unknown())),
				}),
			),
		),
		/**
		 * Properties that can be configured to access and load balance a service.
		 */
		EndpointSpec: v.exactOptional(
			v.strictObject({
				/**
				 * The mode of resolution to use for internal load balancing between tasks.
				 */
				Mode: v.exactOptional(v.picklist(["vip", "dnsrr"])),
				/**
				 * List of exposed ports that this service is accessible on from the
				 * outside. Ports can only be provided if `vip` resolution mode is used.
				 */
				Ports: v.exactOptional(
					v.array(
						v.strictObject({
							Name: v.exactOptional(v.string()),
							Protocol: v.exactOptional(v.picklist(["tcp", "udp", "sctp"])),
							/**
							 * The port inside the container.
							 */
							TargetPort: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The port on the swarm hosts.
							 */
							PublishedPort: v.exactOptional(v.pipe(v.number(), v.integer())),
							/**
							 * The mode in which port is published.
							 *
							 * <p><br /></p>
							 *
							 * - "ingress" makes the target port accessible on every node,
							 *
							 * regardless of whether there is a task for the service running on
							 *
							 * that node or not.
							 * - "host" bypasses the routing mesh and publish the port directly on
							 *
							 * the swarm node where that service is running.
							 */
							PublishMode: v.exactOptional(v.picklist(["ingress", "host"])),
						}),
					),
				),
			}),
		),
	}),
	v.record(v.string(), v.unknown()),
]);
export const serviceUpdateCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const serviceUpdateCommandQuerySchema = v.strictObject({
	version: v.pipe(v.number(), v.integer()),
	registryAuthFrom: v.exactOptional(v.picklist(["spec", "previous-spec"])),
	rollback: v.exactOptional(v.string()),
});
export const serviceLogsCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const serviceLogsCommandQuerySchema = v.strictObject({
	details: v.exactOptional(v.boolean()),
	follow: v.exactOptional(v.boolean()),
	stdout: v.exactOptional(v.boolean()),
	stderr: v.exactOptional(v.boolean()),
	since: v.exactOptional(v.pipe(v.number(), v.integer())),
	timestamps: v.exactOptional(v.boolean()),
	tail: v.exactOptional(v.string()),
});
export const taskListCommandQuerySchema = v.strictObject({
	filters: v.exactOptional(v.string()),
});
export const taskInspectCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const taskLogsCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const taskLogsCommandQuerySchema = v.strictObject({
	details: v.exactOptional(v.boolean()),
	follow: v.exactOptional(v.boolean()),
	stdout: v.exactOptional(v.boolean()),
	stderr: v.exactOptional(v.boolean()),
	since: v.exactOptional(v.pipe(v.number(), v.integer())),
	timestamps: v.exactOptional(v.boolean()),
	tail: v.exactOptional(v.string()),
});
export const secretListCommandQuerySchema = v.strictObject({
	filters: v.exactOptional(v.string()),
});
export const secretCreateCommandBodySchema = v.intersect([
	v.strictObject({
		/**
		 * User-defined name of the secret.
		 */
		Name: v.exactOptional(v.string()),
		/**
		 * User-defined key/value metadata.
		 */
		Labels: v.exactOptional(v.record(v.string(), v.unknown())),
		/**
		 * Base64-url-safe-encoded ([RFC
		 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
		 * data to store as secret.
		 *
		 * This field is only used to _create_ a secret, and is not returned by
		 * other endpoints.
		 */
		Data: v.exactOptional(v.string()),
		/**
		 * Driver represents a driver (network, logging, secrets).
		 */
		Driver: v.exactOptional(
			v.strictObject({
				/**
				 * Name of the driver.
				 */
				Name: v.string(),
				/**
				 * Key/value map of driver-specific options.
				 */
				Options: v.exactOptional(v.record(v.string(), v.unknown())),
			}),
		),
		/**
		 * Driver represents a driver (network, logging, secrets).
		 */
		Templating: v.exactOptional(
			v.strictObject({
				/**
				 * Name of the driver.
				 */
				Name: v.string(),
				/**
				 * Key/value map of driver-specific options.
				 */
				Options: v.exactOptional(v.record(v.string(), v.unknown())),
			}),
		),
	}),
	v.record(v.string(), v.unknown()),
]);
export const secretInspectCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const secretDeleteCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const secretUpdateCommandBodySchema = v.strictObject({
	/**
	 * User-defined name of the secret.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Base64-url-safe-encoded ([RFC
	 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
	 * data to store as secret.
	 *
	 * This field is only used to _create_ a secret, and is not returned by
	 * other endpoints.
	 */
	Data: v.exactOptional(v.string()),
	/**
	 * Driver represents a driver (network, logging, secrets).
	 */
	Driver: v.exactOptional(
		v.strictObject({
			/**
			 * Name of the driver.
			 */
			Name: v.string(),
			/**
			 * Key/value map of driver-specific options.
			 */
			Options: v.exactOptional(v.record(v.string(), v.unknown())),
		}),
	),
	/**
	 * Driver represents a driver (network, logging, secrets).
	 */
	Templating: v.exactOptional(
		v.strictObject({
			/**
			 * Name of the driver.
			 */
			Name: v.string(),
			/**
			 * Key/value map of driver-specific options.
			 */
			Options: v.exactOptional(v.record(v.string(), v.unknown())),
		}),
	),
});
export const secretUpdateCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const secretUpdateCommandQuerySchema = v.strictObject({
	version: v.pipe(v.number(), v.integer()),
});
export const configListCommandQuerySchema = v.strictObject({
	filters: v.exactOptional(v.string()),
});
export const configCreateCommandBodySchema = v.intersect([
	v.strictObject({
		/**
		 * User-defined name of the config.
		 */
		Name: v.exactOptional(v.string()),
		/**
		 * User-defined key/value metadata.
		 */
		Labels: v.exactOptional(v.record(v.string(), v.unknown())),
		/**
		 * Base64-url-safe-encoded ([RFC
		 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
		 * config data.
		 */
		Data: v.exactOptional(v.string()),
		/**
		 * Driver represents a driver (network, logging, secrets).
		 */
		Templating: v.exactOptional(
			v.strictObject({
				/**
				 * Name of the driver.
				 */
				Name: v.string(),
				/**
				 * Key/value map of driver-specific options.
				 */
				Options: v.exactOptional(v.record(v.string(), v.unknown())),
			}),
		),
	}),
	v.record(v.string(), v.unknown()),
]);
export const configInspectCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const configDeleteCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const configUpdateCommandBodySchema = v.strictObject({
	/**
	 * User-defined name of the config.
	 */
	Name: v.exactOptional(v.string()),
	/**
	 * User-defined key/value metadata.
	 */
	Labels: v.exactOptional(v.record(v.string(), v.unknown())),
	/**
	 * Base64-url-safe-encoded ([RFC
	 * 4648](https://tools.ietf.org/html/rfc4648#section-5))
	 * config data.
	 */
	Data: v.exactOptional(v.string()),
	/**
	 * Driver represents a driver (network, logging, secrets).
	 */
	Templating: v.exactOptional(
		v.strictObject({
			/**
			 * Name of the driver.
			 */
			Name: v.string(),
			/**
			 * Key/value map of driver-specific options.
			 */
			Options: v.exactOptional(v.record(v.string(), v.unknown())),
		}),
	),
});
export const configUpdateCommandParamsSchema = v.strictObject({
	id: v.string(),
});
export const configUpdateCommandQuerySchema = v.strictObject({
	version: v.pipe(v.number(), v.integer()),
});
export const distributionInspectCommandParamsSchema = v.strictObject({
	name: v.string(),
});
