/**
 * @generated by @block65/openapi-codegen
 *
 * Do not edit directly
 */

import { PublicValidationError } from "@block65/rest-client";
import { sValidator } from "@hono/standard-validator";
import type { ValidationTargets } from "hono";
import {
	addPetCommandBodySchema,
	deletePetCommandParamsSchema,
	findPetByIdCommandParamsSchema,
	findPetWrappedCommandParamsSchema,
	findPetsCommandQuerySchema,
} from "./valibot.js";

type StandardSchema = Parameters<typeof sValidator>[1];

function validate<
	TSchema extends StandardSchema,
	TTarget extends keyof ValidationTargets,
>(target: TTarget, schema: TSchema) {
	return sValidator(target, schema, (result) => {
		if (!result.success) {
			throw PublicValidationError.fromIssues(result.error);
		}
	});
}

export const findPetsQueryParams = [
	{ name: "tags", type: "array", style: "form", explode: true },
] as const;
export const findPets = [
	validate("query", findPetsCommandQuerySchema),
] as const;
export const addPet = [validate("json", addPetCommandBodySchema)] as const;
export const findpetbyid = [
	validate("param", findPetByIdCommandParamsSchema),
] as const;
export const deletePet = [
	validate("param", deletePetCommandParamsSchema),
] as const;
export const findPetWrapped = [
	validate("param", findPetWrappedCommandParamsSchema),
] as const;
