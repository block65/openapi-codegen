{
	"extends": ["@block65/tsconfig/nodejs26", "@block65/tsconfig/noprojref"],

	"compilerOptions": {
		"types": ["node"],

		// the generator reads the OpenAPI documents it is pointed at
		"resolveJsonModule": true
	},

	"include": ["bin", "lib", "oxlint.config.ts"]
}
